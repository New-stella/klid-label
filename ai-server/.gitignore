__pycache__/
*.pyc
.venv/
venv/
.mypy_cache/
.pytest_cache/
.coverage

.env
.env.local

# 가중치 바이너리는 제외하되, 출처 기록(README)과 무결성 해시는 git 에 남긴다.
#   ⚠ 'weights/' 로 디렉터리째 제외하면 하위 파일을 '!' 로 되살릴 수 없다(git 사양).
#     그래서 'weights/*' 로 내용만 제외한다.
weights/*
!weights/README.md
!weights/yolox_s.onnx.sha256
/models/
