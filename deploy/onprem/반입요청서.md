# 소프트웨어 반입 요청서 — 학습데이터 저작도구 온프렘 서버 배포

> **대상 시스템**: AI 기반 지방정부 CCTV 관제지원시스템(2차) — 학습데이터 저작도구
> **반입 목적**: 인터넷이 차단된 폐쇄망(에어갭) **레드햇 엔터프라이즈 리눅스 8.9 서버 2대**에
> 저작도구(백엔드·프론트엔드·AI 추론 서버)를 설치·구동하기 위한 애플리케이션 산출물,
> 런타임, 오프라인 의존성, 시스템 패키지, 모델 가중치, 제3자 라이선스 고지 및 대응 소스 일체
> **작성일**: 2026-08-30
> **작성 근거**: 반입 매체 실물(`deploy/onprem/`)에서 파일 수·용량·SHA256 을 직접 산출.
> 버전은 `scripts/lib/versions.sh` 의 고정 핀과 실제 파일명에서 읽음.
> 라이선스는 `licenses/ALL.tsv`·`INVENTORY.md`(자동 생성, 반입물에서 직접 추출)에서 읽음.

---

## 1. 요청 개요

| 항목 | 내용 |
|------|------|
| 반입 형태 | 오프라인 설치 매체(USB/보안 전송) 1식 — 폐쇄망 설치 시점에 네트워크 접근 0 |
| 반입 단위 | **매체는 하나**이며 설치할 때 역할만 고른다(`--role=app` / `--role=ai`) |
| 대상 장비 | RHEL 8.9 (x86_64, glibc 2.28) **2대** |
| 총 파일 수 | **5,845개** (본 요청서 자체 제외한 매체 실측) |
| 총 용량 | **1,620,348,641 B (약 1.51 GiB)** — 아래 §7 잔재 정리 후 **약 1.47 GiB** |
| 구성요소 라이선스 | 제3자 오픈소스 **371건** — 전문 동봉 371 / 미확인 **0** (§6) |
| 카피레프트 | **7건**(개별 열거·이행 방법 §6-3). LGPL 대응 소스 4건 + GPL 대응 소스(SRPM) 3건 **실물 동봉** |
| 무결성 근거 | 영역별 `SHA256SUMS` **11개 파일**(§5). 대상 서버에서 대조 후 설치 |
| 실행 파일 성격 | 전부 CPU 전용(GPU·CUDA 미포함). 외부로 나가는 통신 없음 — 폐쇄망 내부 연동만 |

---

## 2. 반입 대상 서버 구성과 역할별 반입물

매체는 한 벌이고, 설치 시 `--role` 로 어느 서버인지만 고른다. 역할을 주지 않으면 한 대에 전부 설치된다.

| | **서버 A `--role=app`** | **서버 B `--role=ai`** |
|---|---|---|
| 역할 | 프론트엔드 + 백엔드 | AI 추론 서버 |
| 올라가는 것 | httpd(정적 자산 서빙 + `/api` 리버스프록시) · **외부 WAS 에 `api.war`** · (옵션)PostgreSQL 16 | ai-server(파이썬 3.11 + 오프라인 휠 + YOLOX/SAM2 모델) |
| systemd 유닛 | `httpd` (+`postgresql-16`) — **백엔드는 유닛이 아니다**(WAS 가 기동 주체) | `klid-ai-server` |
| 쓰는 반입물 | `artifacts/backend`(api.war) · `artifacts/frontend/dist` · `syspkgs/{rpm,gpg,postgresql,ffmpeg,ffmpeg-src}` · `db` · `config` | `vendor/{wheels,sam2}` · `models/{weights,hf-cache}` · `runtimes/python` · `syspkgs/{rpm,gpg}` |
| ffmpeg | **전제조건** — 관제지원시스템 측이 설치. 우리는 검증만 한다(§3-3) | 사용하지 않는다 |
| GPU | 해당 없음 | 장비에 GPU 가 있으나 **이번 반입은 CPU 전용** |
| 파이썬 | 필요 없음 | **장비에 파이썬이 없어 런타임 전체를 반입** |

> 서버 A 는 서버 B 의 9300 포트에 도달해야 한다. `AI_SERVER_URL` 기본값이 `http://127.0.0.1:9300`
> (단일 서버 기준)이므로 2대 구성에서는 **반드시 서버 B 주소로 바꿔야 한다.**

---

## 3. ★ 반입 제외 대상 (명시)

무엇을 반입하지 **않는지**를 먼저 밝힌다. 아래 세 가지는 의도적 제외이며 각각 근거가 다르다.

### 3-1. 자바 런타임 (JDK/JRE) — 반입하지 않는다

대상 서버 A 에는 관제지원시스템 WAS 와 동일 사양인 **Tomcat 10.1.x + Java 17** 이 이미
설치·구동 중이다(실측 17.0.19). 백엔드는 그 WAS 에 **`api.war` 로 반입**되며 프로세스 수명주기는
WAS 가 소유한다. 자바 런타임을 한 벌 더 반입하면 같은 장비에 두 벌이 공존해 운영 혼선만 생긴다.

- 매체의 `runtimes/jdk/` 는 **비어 있다**(디렉터리 골격만 남김).
- 설치 스크립트도 자바를 설치하지 않는다. `runtimes/jdk` 가 비어 있어도 설치가 완주한다.

> ⚠ **ai-server 의 파이썬 런타임은 이와 무관하게 그대로 반입한다.** 자바를 뺐다고 함께 빠지지 않는다 —
> 서버 B 에는 파이썬이 아예 없기 때문이다.

### 3-2. 현장 재빌드 도구 — 반입하지 않는다

계약상 **폐쇄망 현장에서 소스를 재빌드할 요구가 없음**을 확인했다. 따라서 아래를 반입하지 않는다.

| 미반입 품목 | 원래 용도 | 현재 상태 |
|---|---|---|
| JDK 17 full (javac) | 백엔드 `bootJar` 재빌드 | `buildtools/jdk/` 비어 있음 |
| Node.js 20 | 프론트엔드 `vite build` 재빌드 | `buildtools/node/` 비어 있음 |
| Gradle 8.8 + 의존성 캐시 | 백엔드 오프라인 빌드 | `buildtools/gradle*/` 비어 있음 |
| `node_modules`(645 패키지) | 프론트엔드 오프라인 빌드 | 미수집 |

대신 **사전 빌드된 산출물**(`api.war`, 프론트엔드 `dist`)을 반입한다.
빌드 도구를 켜서 반입하려면 `WITH_BUILDTOOLS=1` 로 수집해야 하며, 그때는 그 목록(특히
`node_modules` 645 패키지)의 **라이선스 고지 의무가 새로 생긴다** — 이번 반입은 그 대상이 아니다.

### 3-3. ffmpeg — 설치하지 않는다. 단 **예비물로는 매체에 담는다**

이 구분이 중요하다. 두 문장은 서로 다른 사실이다.

| | 사실 |
|---|---|
| **설치 주체** | **관제지원시스템 측이 서버 A 에 설치한다.** 우리 설치 스크립트는 **자동 설치하지 않고 검증만** 한다(`19-verify-ffmpeg.sh`). 없으면 설치를 **중단**한다 |
| **매체 동봉** | **담는다.** `syspkgs/ffmpeg/` 에 RPM 261개(전이 의존성 전량, 로컬 저장소 포함) + `syspkgs/ffmpeg-src/` 에 GPL 대응 소스 SRPM 3건 |
| **설치 방법** | 자동 설치 경로 없음. 관제 측이 설치하지 않은 것이 확인됐을 때만 **사람이** `sudo ./scripts/install/install-ffmpeg.sh` 로 별도 실행 |

**왜 자동이 아닌가**: 서버 A 는 관제지원시스템과 **공동 배치**다. 관제가 이미 설치한 ffmpeg 를
우리 설치가 덮어쓰면 관제 기능이 깨진다. 그래서 `install.sh` 의 단계 목록에 `install-ffmpeg.sh` 를
**넣지 않았다**(파일명에 번호 접두가 없는 것이 그 표식이다).

**왜 그래도 담는가**: 폐쇄망이라 필요해졌을 때 받아올 수 없다. ffmpeg 는 RHEL 8 의 base/AppStream 에
없고 EPEL + RPM Fusion + PowerTools(CRB) 세 리포가 필요해, 현장에서 조달할 방법이 사실상 없다.

> **라이선스**: 예비물 ffmpeg 는 RPM Fusion 의 **GPLv3+** 빌드다. 설치하지 않더라도 **매체에 담아
> 넘기는 것 자체가 재배포**이므로 대응 소스(SRPM)를 함께 반입한다(§6-4).

### 3-4. 그 밖에 반입하지 않는 것

- **애플리케이션 소스코드**: 반입 대상이 아니다(사전 빌드 산출물만 반입).
- **`klid-backend.jar`**: 개발 환경 전용 산출물이다. 매체에 함께 만들어지지만 **WAR 반입 형상에서는
  설치가 배치하지 않는다**(`12-install-backend.sh` 가 명시적으로 건너뛴다). 반입 목록에서 빼려면
  매체 생성 후 삭제하면 되고, 남겨도 설치·구동에 영향이 없다.
- **KPST 비식별 서버**: 외부 시스템이며 폐쇄망에 별도 설치되어 있어야 한다(본 패키지 비포함).

---

## 4. 반입 품목 상세

용량·파일 수는 매체 실물에서 산출한 값이다(2026-08-30 기준).

### 4-1. 영역별 요약

| 영역 | 파일 수 | 용량(바이트) | 용량 | 대상 서버 |
|---|---:|---:|---:|:---:|
| `artifacts/backend` | 4 | 175,326,784 | 167.2 MiB | A |
| `artifacts/frontend` | 2,441 | 53,162,455 | 50.7 MiB | A |
| `artifacts/ai-server` | 209 | 3,060,430 | 2.9 MiB | B |
| `vendor/wheels` | 78 | 391,094,542 | 373.0 MiB | B |
| `vendor/sam2` | 566 | 64,563,627 | 61.6 MiB | B |
| `models/weights` | 3 | 35,858,160 | 34.2 MiB | B |
| `models/hf-cache` | 24 | 311,847,272 | 297.4 MiB | B |
| `runtimes/python` | 2 | 48,919,537 | 46.7 MiB | B |
| `syspkgs/rpm` | 205 | 117,581,022 | 112.1 MiB | A·B |
| `syspkgs/ffmpeg` | 270 | 150,139,710 | 143.2 MiB | A(예비물) |
| `syspkgs/ffmpeg-src` | 5 | 11,954,334 | 11.4 MiB | A(라이선스 의무) |
| `syspkgs/postgresql` | 218 | 140,481,418 | 134.0 MiB | A(옵션) |
| `syspkgs/gpg` | 10 | 14,487 | 0.01 MiB | A·B |
| `licenses` | 403 | 73,301,147 | 69.9 MiB | — (문서·소스) |
| `scripts`·`docs`·`config`·`db` | 61 | 910,137 | 0.9 MiB | A·B |
| 그 밖(최상위 `README.md`·`VERSION`·`.gitignore` 등 4 + 빈 디렉터리 골격 6) | 10 | 25,982 | 0.02 MiB | — |
| **소계** | **4,509** | **1,578,241,044** | **1,505.1 MiB** | |
| `src`(잔재 — §7 참조, 반입 제외) | 1,336 | 42,107,597 | 40.2 MiB | — |
| **합계(현재 작업본)** | **5,845** | **1,620,348,641** | **1,545.3 MiB** | |

### 4-2. 애플리케이션 산출물

| 품목 | 내용 | 용량 | SHA256 |
|---|---|---:|---|
| `artifacts/backend/api.war` | 백엔드 — 외부 WAS 반입 정본. **파일명이 곧 웹 컨텍스트 `/api` 이므로 이름을 바꾸면 안 된다** | 87,664,106 B (83.6 MiB) | `2a70ac7db99d861f65c29bbf30d8fae33906ba1cee874c1aa6d86b3bea91dff6` |
| `artifacts/backend/klid-backend.jar` | 개발 환경 전용 — **설치가 배치하지 않는다**(§3-4) | 87,662,440 B (83.6 MiB) | `17627fd689c12611abbb5328a4f29d389ac55254344b782929b5b85ad9cb2526` |
| `artifacts/frontend/dist/` | 프론트엔드 정적 산출물(2,439 파일 — 번들·글꼴·npm 라이선스 고지 포함) | 53,162,017 B (50.7 MiB) | 파일별 `dist/SHA256SUMS` (2,438행) |
| `artifacts/ai-server/` | AI 추론 서버 애플리케이션 소스(FastAPI) | 3,060,430 B (2.9 MiB) | — |

- 백엔드 기술 스택: Java 17 / Spring Boot 3.3 (외부 WAS 구동)
- 프론트엔드 빌드 기록: `artifacts/frontend/BUILD-INFO.txt` — 빌드 시각 `2026-08-30 17:44:58+0900`, 소스 커밋 `2e8bfd40`

### 4-3. 런타임 (서버 B 전용)

| 품목 | 버전 | 용도 | 출처 | 라이선스 | 용량 |
|---|---|---|---|---|---:|
| CPython standalone (`install_only`) | 3.11.15 (릴리스 태그 20260623) | ai-server 파이썬 런타임 — 대상 장비에 파이썬이 없다 | astral-sh/python-build-standalone 릴리스 | PSF-2.0 + 번들 제3자 19종(§6-2) | 48,919,537 B (46.7 MiB) |

- 고정 핀 SHA256: `60295e3e703b48c270e8d8c685195b8d5c2f0b8a596c1a910d7e24a2cc55afdd`
  (`scripts/lib/versions.sh` 의 `PYTHON_STANDALONE_SHA256`, 릴리스 `SHA256SUMS` 대조 확인)
- **자바 런타임은 반입하지 않는다**(§3-1). 웹 서버도 별도 바이너리가 아니라 배포판 httpd RPM 이다.

### 4-4. 파이썬 오프라인 의존성 (서버 B 전용)

| 품목 | 개수 | 용량 | 비고 |
|---|---:|---:|---|
| `vendor/wheels/*.whl` | 74 | | `cp311` / `manylinux_2_28_x86_64` — glibc 2.28 기준선과 정확히 일치 |
| `vendor/wheels/*.tar.gz` (sdist) | 2 | | `antlr4-python3-runtime-4.9.3`, `iopath-0.1.10` — 둘 다 순수 파이썬 |
| 소계 | **76 패키지** | 391,094,542 B (373.0 MiB) | 파일별 `vendor/wheels/SHA256SUMS`(77행) |
| `vendor/sam2/sam2-src/` | 565 파일 | 64,563,627 B (61.6 MiB) | SAM2 소스(git 이력 제외). 커밋 고정 `2b90b9f5ceec907a1c18123530e92e794ad901a4` |

주요 패키지: `torch 2.12.0+cpu` · `torchvision 0.27.0+cpu` · `onnxruntime 1.27.0` ·
`opencv-python 4.13.0.92` · `numpy 2.4.6` · `fastapi 0.137.0` · `supervision 0.28.0` · `trackers 2.4.0`

> **CPU 전용**: torch·torchvision 은 PyTorch **CPU 인덱스**에서 받은 판이다. CUDA 런타임은 반입하지 않는다.
> **빌드 백엔드 동봉**: `setuptools 81.0.0` · `wheel 0.48.0` 을 명시 반입한다. 폐쇄망에서는 PEP 517
> 빌드 격리 환경이 이 둘을 외부에서 찾을 수 없어, 빠지면 설치가 그 자리에서 멈춘다.

### 4-5. 모델 가중치 (서버 B 전용)

| 품목 | 판/태그 | 용도 | 출처 | 라이선스 | 용량 | SHA256 |
|---|---|---|---|---|---:|---|
| `models/weights/yolox_s.onnx` | YOLOX 릴리스 **0.1.1rc0** | 객체 탐지(ONNX Runtime) | **Megvii-BaseDetection/YOLOX 릴리스 자산** (`github.com/Megvii-BaseDetection/YOLOX/releases/download/0.1.1rc0/yolox_s.onnx`) | Apache-2.0 | 35,858,002 B (34.2 MiB) | `c5c2d13e59ae883e6af3b45daea64af4833a4951c92d116ec270d9ddbe998063` |
| `models/hf-cache/` | `facebook/sam2-hiera-tiny` (스냅샷 `7c218beaf0bb87874785f32b582f640134fc1c09`) | SAM2 분할·추적 | HuggingFace Hub 캐시 | Apache-2.0 | 311,847,272 B (297.4 MiB) | — (캐시 구조, 스냅샷 해시로 식별) |

- YOLOX 가중치의 기대 해시는 저장소가 관리하는 단일 출처(`ai-server/weights/yolox_s.onnx.sha256`)이며,
  수집 스크립트가 복사 전에 **대조하고 불일치면 중단**한다. 매체의 `models/weights/SHA256SUMS` 와 일치함을 확인했다.
- 라이선스 전문은 **같은 태그(0.1.1rc0)의 저장소 LICENSE** 를 받아 `licenses/manual/texts/yolox-weights-Apache-2.0.txt` 에 동봉했다
  (ONNX 바이너리 안에는 전문이 들어 있지 않기 때문).
- **SAM2 모델을 빼면 안 된다** — 빠져도 서버 기동·헬스체크·API 응답이 모두 정상이고,
  SAM2 분할·추적만 조용히 모의 응답으로 동작한다. 운영 중에는 드러나지 않는다.

### 4-6. 시스템 패키지 (RPM, el8 / x86_64)

전부 **배포판·공식 리포지토리 RPM** 이며, 오프라인 설치를 위해 **전이 의존성까지 전량** 받아
`createrepo_c` 로 로컬 저장소(`repodata/`)를 만들어 담았다.

| 디렉터리 | RPM 수 | 용량 | 내용 | 대상 |
|---|---:|---:|---|:---:|
| `syspkgs/rpm/` | 196 | 117,581,022 B (112.1 MiB) | `httpd`·`httpd-tools`·`httpd-filesystem`(웹 서버, el8 모듈러) · `mesa-libGL`·`libglvnd-glx`·`glib2`(opencv 런타임 의존) · `policycoreutils-python-utils` 외(SELinux 관리 도구) | A·B |
| `syspkgs/ffmpeg/` | 261 | 150,139,710 B (143.2 MiB) | **예비물**(§3-3). `ffmpeg 4.4.8-1.el8`·`ffmpeg-libs`·`x264-libs`·`x265-libs` + 전이 의존 전량 | A |
| `syspkgs/postgresql/` | 209 | 140,481,418 B (134.0 MiB) | PostgreSQL **16**(PGDG) — `postgresql16-server`·`postgresql16`·`postgresql16-libs`·`postgresql16-contrib` + 전이 의존 | A(옵션) |
| `syspkgs/ffmpeg-src/` | 3 (SRPM) | 11,954,334 B (11.4 MiB) | **GPL 대응 소스** — 설치 대상 아님(§6-4) | A |

- **번들 PostgreSQL 은 옵션이다.** 대상 장비에 이미 PostgreSQL 이 있으면
  `USE_BUNDLED_POSTGRES=0` 으로 끄고 접속 정보만 준다.
- **실측 설치량**: 로컬 저장소에서 dnf 가 필요한 것만 고르므로, 261개를 반입해도 실제 설치는 105개이며
  **glibc 는 변경되지 않는다**. 반입 파일을 `dnf install <파일들>` 로 직접 넘기면 기반 패키지 충돌로
  설치가 통째로 실패하므로, 반드시 로컬 저장소 방식으로 설치한다.

### 4-7. RPM 서명 검증용 GPG 공개키

오프라인 설치에서도 **`gpgcheck=1` 로 서명을 검증**하기 위해 공개키를 함께 반입한다.
키가 없으면 dnf 가 설치를 거부한다.

| 파일 | 발급처 |
|---|---|
| `RPM-GPG-KEY-EPEL-8` | Fedora EPEL 8 |
| `RPM-GPG-KEY-rpmfusion-free-el-8` | RPM Fusion free (el8) |
| `RPM-GPG-KEY-Rocky-8` / `RPM-GPG-KEY-rockyofficial` | Rocky Linux 8 |
| `RPM-GPG-KEY-Rocky-8-Testing` / `RPM-GPG-KEY-rockytesting` | Rocky Linux 8 (testing) |
| `PGDG-RPM-GPG-KEY-RHEL` | PostgreSQL Global Development Group |

- 각 키가 어느 RPM 을 덮는지는 `syspkgs/gpg/KEY-COVERAGE-syspkgs.txt`·`KEY-COVERAGE-postgresql.txt` 에 기록되어 있다.
- 10개 파일 전체의 SHA256 은 `syspkgs/gpg/SHA256SUMS` 에 있다.

### 4-8. 설치 스크립트 · 설정 템플릿 · DB 스키마 · 문서

| 항목 | 내용 |
|---|---|
| `scripts/` (33 파일) | `install.sh`(역할별 오케스트레이션) + `install/10~20`(PostgreSQL·런타임·백엔드·ai-server·프론트엔드·DB 초기화·ffmpeg 검증·설정 검증) + `package/`(빌드머신 수집) + `uninstall.sh` |
| `config/` (13 파일) | 백엔드 `application.properties`·`env`·`was.env` 템플릿, ai-server `env` 템플릿, 프론트엔드 `frontend.env`·httpd 설정 템플릿, WAS 설정 예시(`setenv.sh`·`context-api.xml`·`server-connector.xml`), systemd 유닛 |
| `db/` (2 파일) | `schema.sql`(저작도구 스키마 `klid_at`) · `portal-schema.sql`. 설치 단계가 **1회 로드**한다 — 애플리케이션은 마이그레이션을 돌리지 않고 매핑 검증만 한다 |
| `docs/` (13 파일) | 00 개요 · 01 사전요구 · 02 빌드/패키징 · 03 설치 · 04 설정 · 05 기동/검증 · 06 문제해결 · 07 제거/롤백 · 08 소스 재빌드 · 09 운영 런북 · 10 WAS 설정 이관 · 10 백업/DR · 11 GPU 전환 |

---

## 5. 무결성 근거

### 5-1. 체크섬 파일 목록 (매체 동봉)

| 파일 | 항목 수 | 대상 |
|---|---:|---|
| `artifacts/backend/SHA256SUMS` | 3 | `api.war` · `klid-backend.jar` |
| `artifacts/frontend/dist/SHA256SUMS` | 2,438 | 프론트엔드 정적 산출물 전량 |
| `vendor/wheels/SHA256SUMS` | 77 | pip 휠·sdist 전량 |
| `models/weights/SHA256SUMS` | 2 | `yolox_s.onnx` |
| `syspkgs/rpm/SHA256SUMS` | 204 | 시스템 RPM |
| `syspkgs/ffmpeg/SHA256SUMS` | 269 | ffmpeg RPM(예비물) |
| `syspkgs/ffmpeg-src/SHA256SUMS` | 4 | GPL 대응 소스 SRPM |
| `syspkgs/postgresql/SHA256SUMS` | 217 | PostgreSQL 16 RPM |
| `syspkgs/gpg/SHA256SUMS` | 9 | GPG 공개키 |
| `licenses/SHA256SUMS` | 401 | 라이선스 고지 원문 전량 |
| `licenses/copyleft-sources/SHA256SUMS` | 5 | 카피레프트 대응 소스 |

### 5-2. 상류 출처 대조가 이미 끝난 항목

아래는 **공식 배포처가 게시한 SHA256** 과 대조한 뒤 매체에 담은 것이며, 그 값이
`scripts/lib/versions.sh` 에 코드로 고정되어 있어 다음 수집에서도 같은 파일임이 강제된다.

| 품목 | 고정 SHA256 | 대조한 출처 |
|---|---|---|
| CPython 3.11.15 standalone | `60295e3e703b48c270e8d8c685195b8d5c2f0b8a596c1a910d7e24a2cc55afdd` | python-build-standalone 릴리스 `SHA256SUMS` |
| `yolox_s.onnx` | `c5c2d13e59ae883e6af3b45daea64af4833a4951c92d116ec270d9ddbe998063` | 저장소 단일 출처 `ai-server/weights/yolox_s.onnx.sha256`, 수집 시 대조·불일치 시 중단 |
| FFmpeg 8.0.1 소스(LGPL 대응) | `05ee0b03119b45c0bdb4df654b96802e909e0a752f72e4fe3794f487229e5a41` | `ffmpeg.org/releases` |
| qtbase 5.15.18 소스(LGPL 대응) | `7b632550ea1048fc10c741e46e2e3b093e5ca94dfa6209e9e0848800e247023b` | `download.qt.io` |
| hibernate-core 6.5.2.Final 소스 | `176260131e510337fd476eee3f61d157251dda7411b87daebefa547f33183528` | Maven Central(게시 `.sha1` 대조 후 sha256 산출) |
| hibernate-commons-annotations 6.0.6.Final 소스 | `5f9ef9fcd3a6942102f4857a4c7fb2a85e203d9a6ece7d700ec7d1f789411991` | 동일 |

### 5-3. 버전을 고정하지 않는 항목과 그 근거

**RPM 은 버전 핀을 두지 않는다.** 리포지토리의 현재 판을 받기 때문이며(정적 tarball 과 다르다),
그 대신 ①받은 파일의 SHA256 을 영역별 `SHA256SUMS` 로 고정하고 ②설치 시 **GPG 서명을 검증**한다.
매체에 담긴 실제 판은 아래와 같다.

- ffmpeg **4.4.8-1.el8** (Vendor: RPM Fusion, License: GPLv3+)
- httpd **2.4.37-65.module+el8.10.0+40257+286895ef.9** (el8 모듈러 패키지)
- PostgreSQL **16** (PGDG)

---

## 6. ★ 라이선스 종합

이 매체를 고객에게 인도하는 것은 **재배포**다. 따라서 반입물에 포함된 모든 제3자 오픈소스에 대해
고지·전문 동봉·(해당 시) 대응 소스 제공 의무가 발생한다. 아래는 그 이행 내역이다.

> **⚠ "의무 없음"인 라이선스는 하나도 없다.** MIT·BSD·Apache-2.0 도 재배포 시
> **저작권 고지와 라이선스 사본을 함께 제공**해야 한다(MIT/BSD 는 고지 원문 유지,
> Apache-2.0 은 §4(a) 라이선스 사본 제공 + §4(d) NOTICE 전파). "허용적(permissive)"은
> **의무가 없다는 뜻이 아니라 소스 공개 의무가 없다는 뜻**이다.

### 6-1. 전체 분포 (371건)

목록은 손으로 적은 것이 아니라 **반입물 실물에서 추출**했다(WAR 안의 jar POM, npm 패키지,
파이썬 wheel `METADATA`·`dist-info`, 번들 CPython 의 `licenses/`). 출처는 `licenses/ALL.tsv`.

| 구분 | 건수 | 비고 |
|---|---:|---|
| MIT 계열 | 150 | 고지 원문 유지 의무 |
| Apache-2.0 계열 | 127 | 라이선스 사본 + NOTICE 전파 의무 |
| BSD 계열(0BSD 포함) | 27 | 고지 원문 유지 의무 |
| ISC | 13 | 고지 원문 유지 의무 |
| EPL / EDL 계열 | 9 | 약한 카피레프트 — 대응 소스 의무는 §6-3 표 참조 |
| **카피레프트 표기** | **7** | **개별 열거 §6-3** |
| OFL-1.1 (글꼴 2종) | 2 | 글꼴 재배포 시 라이선스 동봉·판매 금지 조건 |
| PSF-2.0 | 1 | |
| CC0 / 퍼블릭도메인 | 1 | |
| 기타 개별 표기 | 3 | `Dual License`(python-dateutil) 등 배포자 원 표기 그대로 보존 |
| 라이선스 **이름** 표기가 비어 있는 항목 | 31 | 메타데이터에 이름 필드가 없을 뿐, **전문은 전부 동봉**되어 있다(아래) |
| **합계** | **371** | |

| 이행 상태 | 건수 |
|---|---:|
| **OK (라이선스 전문 동봉 완료)** | **371** |
| TEXT_MISSING (이름만 알고 전문 없음) | **0** |
| UNRESOLVED (라이선스 미확인) | **0** |

고지 원문의 출처 내역: 반입물 안에 들어 있던 고지를 그대로 복사(ARCHIVE) **284건** /
메타데이터에서 이름을 읽고 전문을 확보(METADATA) **78건** / 사람이 상류에서 받아 채움(MANUAL) **9건**.

> **이름이 빈 31건에 대하여** — 대부분 번들 CPython 이 정적으로 안고 있는 제3자 19종
> (OpenSSL 1.1·OpenSSL 3·Berkeley DB·ncurses·libedit·SQLite·Tcl/Tix·zlib·bzip2·liblzma·expat·
> libffi·libuuid·libX11·libXau·libxcb·mpdecimal·cpython) 이다. 이들은 라이선스 **이름 필드가
> 없는 텍스트 파일 형태**로 제공되므로 이름 칸이 비어 있을 뿐, **전문은 `licenses/python-runtime/`
> 에 20개 파일로 전부 동봉**되어 있다. 나머지는 `scipy`·`matplotlib`·`pydantic` 등 wheel 메타데이터에
> 라이선스 이름 필드가 없는 패키지이며 역시 전문이 동봉되어 있다.

### 6-2. 고지 원문 동봉물

| 위치 | 파일 수 | 내용 |
|---|---:|---|
| `licenses/NOTICE` | 1 (61,635 B) | 반입물 전체의 고지 집합(Apache-2.0 §4(d) NOTICE 전파 이행) |
| `licenses/INVENTORY.md` | 1 (24,761 B) | 구성요소 × 버전 × 라이선스 × 고지출처 × 상태 전수 표 |
| `licenses/ALL.tsv` | 1 | 위와 같은 내용의 기계 판독 형식 |
| `licenses/UNRESOLVED.md` | 1 | 미확인 항목(현재 0건) + 카피레프트 계열 전수 목록 |
| `licenses/backend/` | 117 | WAR 에 실린 자바 라이브러리 고지 원문 |
| `licenses/frontend/` | 131 | 웹 산출물에 실린 npm 패키지·글꼴 고지 원문 |
| `licenses/python-packages/` | 108 | ai-server 파이썬 패키지·모델 고지 원문 |
| `licenses/python-runtime/` | 20 | 번들 CPython 이 안고 있는 제3자 고지 원문 |
| `licenses/manual/` | 14 | 사람이 상류에서 확보한 전문 + 대응 소스 안내 |
| `artifacts/frontend/dist/licenses/` | (dist 내부) | 웹으로도 열람 가능하도록 산출물에 함께 배치 |

### 6-3. ★ 카피레프트 7건 — 개별 열거와 이행 방법

`licenses/UNRESOLVED.md` 가 자동 검출한 전수 목록이다. 각 항목의 이행 방법은
`licenses/manual/LGPL-SOURCE-OFFER.md` 에 근거와 함께 기록되어 있다.

| # | 구성요소 | 버전 | 라이선스 표기 | 의무 판단 | 이행 방법 |
|:-:|---|---|---|---|---|
| 1 | `hibernate-core` | 6.5.2.Final | GNU **Library** General Public License v2.1 or later | **대응 소스 제공 의무 있음** | 소스 jar **실물 동봉** — `licenses/copyleft-sources/hibernate-core-6.5.2.Final-sources.jar` (7.3 MiB) |
| 2 | `hibernate-commons-annotations` | 6.0.6.Final | GNU Library General Public License v2.1 or later | **대응 소스 제공 의무 있음** | 소스 jar **실물 동봉** — 동 디렉터리 (42,927 B) |
| 3 | `logback-classic` | 1.5.6 | EPL-1.0 **또는** LGPL-2.1 (선택적 이중) | **EPL-1.0 을 선택** → 소스 제공 의무 없음 | 라이선스 전문 동봉. 선택 사실을 `LGPL-SOURCE-OFFER.md` §4 에 명시 |
| 4 | `logback-core` | 1.5.6 | EPL-1.0 **또는** LGPL-2.1 (선택적 이중) | **EPL-1.0 을 선택** → 소스 제공 의무 없음 | 동일 |
| 5 | `mchange-commons-java` | 0.2.15 | LGPL-2.1 **또는** EPL-1.0 (선택적 이중) | **EPL-1.0 을 선택** → 소스 제공 의무 없음 | 동일 |
| 6 | `jakarta.transaction-api` | 2.0.1 | EPL-2.0 **또는** GPL-2.0 w/ Classpath Exception | **EPL-2.0 을 선택** → 소스 제공 의무 없음 | 동일 |
| 7 | `certifi` | 2026.5.20 | MPL-2.0 | 파일 단위 카피레프트. **수정하지 않고 그대로 재배포**하므로 변경분 공개 대상 없음 | 라이선스 전문 동봉. 상류 배포 원형 그대로 반입 |

> **검출 함정(기록)** — 1·2번의 POM 표기는 `GNU **Library** General Public License` 라
> `GPL` 도 `Lesser` 도 문자열에 없다. `GPL|Lesser` 로만 훑는 검사기는 이 두 건을 **놓친다.**
> 실제로 그 실수를 밟았고, 그래서 검사 패턴에 `General Public` 를 통째로 넣었다.

**위 7건 외에 wheel 안에 번들되어 검출 목록에 이름이 뜨지 않는 카피레프트가 2건 더 있다.**
`opencv-python` wheel 이 FFmpeg 과 Qt 5 공유 라이브러리를 자기 안에 넣어 배포하기 때문이며,
이것도 우리가 재배포하는 것이므로 대응 소스를 함께 싣는다(§6-4 표의 1·2행).

### 6-4. ★ 대응 소스 실물 동봉 — 총 5건 / 82,540,515 B (78.7 MiB)

폐쇄망 반입이라 *"고객이 인터넷에서 받으면 된다"* 가 성립하지 않는다. 서면 제공 확약도 법적으로
가능하지만, 확약을 받은 담당자가 폐쇄망 안에서 그것을 행사하려면 다시 공급자를 거쳐야 한다.
그래서 **비용이 감당되는 것은 전부 실어서 보낸다.**

| # | 구성요소 | 판 | 라이선스 | 실린 위치 | 용량 |
|:-:|---|---|---|---|---:|
| 1 | FFmpeg (opencv-python wheel 번들) | 8.0.1 | **LGPL-2.1-or-later** | `licenses/copyleft-sources/ffmpeg-8.0.1.tar.xz` | 11,388,848 B (10.9 MiB) |
| 2 | Qt 5 qtbase (opencv-python wheel 번들) | 5.15.18 | **LGPL-3.0** | `licenses/copyleft-sources/qtbase-everywhere-opensource-src-5.15.18.tar.xz` | 51,492,796 B (49.1 MiB) |
| 3 | hibernate-core | 6.5.2.Final | LGPL-2.1-or-later | `licenses/copyleft-sources/hibernate-core-6.5.2.Final-sources.jar` | 7,661,988 B (7.3 MiB) |
| 4 | hibernate-commons-annotations | 6.0.6.Final | LGPL-2.1-or-later | `licenses/copyleft-sources/hibernate-commons-annotations-6.0.6.Final-sources.jar` | 42,927 B (41.9 KiB) |
| 5 | ffmpeg / x264 / x265 (시스템 RPM, RPM Fusion) | 4.4.8-1.el8 / 0.157 / 3.1.2 | **GPLv3+** | `syspkgs/ffmpeg-src/*.src.rpm` (3건) | 11,953,956 B (11.4 MiB) |
| | **합계** | | | | **82,540,515 B (78.7 MiB)** |

**5번은 1번과 다른 물건이다.** 1번은 `opencv-python` wheel 안에 번들된 **LGPL-2.1+ FFmpeg** 이고,
5번은 백엔드가 별도 프로세스로 호출하는 `/usr/bin/ffmpeg`(**GPLv3+**)의 소스다.
**판도 라이선스도 다르므로 어느 한쪽을 "중복"으로 지우면 안 된다.**

> **GPLv3+ ffmpeg 을 쓰는 것이 저작도구 코드에 전염되지 않는 근거**: 저작도구는 ffmpeg 을
> **별도 프로세스로 호출**할 뿐 링크하지 않는다. 관제지원시스템도 같은 형상(RPM Fusion 배포판
> ffmpeg)을 쓰고 있어 대상 장비의 기존 형상과도 일치한다.

#### 대응성 검증 — 눈이 아니라 기계로 대조했다

LGPL 이 요구하는 것은 "소스"가 아니라 **그 바이너리에 대응하는 소스**다. 판이 어긋난 소스를
대응 소스라고 부르는 것은 이행이 아니라 **틀린 주장**이므로, 수집 스크립트가 **매 수집마다
자동으로 대조하고 불일치면 그 자리에서 중단**한다(경고가 아니라 중단이다).

- **FFmpeg**: wheel 안 `libav*.so` 의 soname 과 `ffmpeg-8.0.1` 소스 `version.h` 선언을 대조 —
  `libavcodec 62.11.100` · `libavformat 62.3.100` · `libavutil 60.8.100` · `libswscale 9.1.100` **4/4 일치**.
  바이너리에서 직접 읽은 `configure` 에 `--enable-gpl`·`--enable-nonfree` 가 **없어** LGPL 빌드임을 확인했다.
- **Qt 5**: wheel 이 번들한 모듈은 `Core`·`Gui`·`Widgets`·`Test`·`XcbQpa` 다섯이며 **전부 qtbase 소속**이다.
  soname `5.15.18` 과 qtbase `.qmake.conf` 의 `MODULE_VERSION = 5.15.18` **일치**. 그래서
  `qt-everywhere`(664 MiB) 대신 `qtbase`(51 MiB)로 의무가 닫힌다 — LGPL-3.0 이 요구하는 것은
  **그 라이브러리**의 대응 소스이지 Qt 제품군 전체가 아니다.
- 검증 기록은 `licenses/copyleft-sources/MANIFEST.tsv` 머리말에 날짜와 함께 남아 있다.

#### 보완적 서면 제공 확약

위 동봉이 어떤 사유로 누락된 매체를 인도받은 경우를 대비해, `licenses/manual/LGPL-SOURCE-OFFER.md` §3 에
**인도일로부터 3년간 실비 이내로 대응 소스를 제공한다**는 확약을 문서로 함께 넣었다
(LGPL-2.1 §6(b) 및 LGPL-3.0 §4(d) → GPLv3 §6(b) 가 허용하는 이행 방법).

### 6-5. 라이선스 목록이 낡지 않는 구조

손으로 적은 목록은 반드시 낡는다. 의존성 lock 은 바뀌는데 문서는 안 바뀌기 때문이다.
그래서 이 인벤토리는 **수집·빌드 시점에 실제 반입물에서 긁어** 만든다.
스캐너는 **값을 지어내지 않는다** — 못 찾으면 `UNRESOLVED.md` 에 남기고 멈춘다.
틀린 고지는 고지가 없는 것보다 나쁘기 때문이다.

반출 전 체크리스트(`licenses/README.md` §2)는 다음을 요구하며, 현재 **전부 충족**한다:
`UNRESOLVED` 절이 비어 있을 것 · `TEXT_MISSING` 절의 각 항목을 판단했을 것 ·
카피레프트 목록을 대응 소스 안내와 대조했을 것 · 대응성 검증 통과 기록이 있을 것 · `NOTICE` 가 비어 있지 않을 것.

---

## 7. 매체 잔재 정리 — 완료

아래 두 건은 **정리했다**(2026-08-30). 이 절은 조치 결과 기록이다.

### 7-1. 빌드용 소스 사본 `deploy/onprem/src/` — 삭제 완료

**1,336 파일(42,107,597 B / 40.2 MiB)을 삭제했다.**

이 디렉터리는 §3-2 의 **오프라인 재빌드 키트가 쓰던 소스 사본**이며, 2026-08-30 에 재빌드 도구를
반입 대상에서 제외하면서 **더 이상 채워지지 않는다.** 남아 있던 것은 그 정책 변경 이전
(2026-06-29 · 일부 2026-07-25)에 수집된 잔재였다.

- **반입 대상이 아니었다.** 설치 스크립트 어느 단계도 이 디렉터리를 읽지 않는다.
- **삭제해도 되돌릴 수 있다.** 이 디렉터리는 정본이 아니라 생성물이다 — 재빌드 키트를 다시
  반입해야 하면 `WITH_BUILDTOOLS=1 ./scripts/package.sh` 가 저장소 정본에서 새로 채운다.
- 함께 검토할 것(미조치): `artifacts/backend/klid-backend.jar`(83.6 MiB, §3-4) — 설치가 배치하지
  않으므로 빼도 무방하다. 빼면 약 1,487,805,000 B (1.39 GiB).

### 7-2. 파이썬 바이트코드 캐시 — 삭제 완료

**`__pycache__` 6 개 디렉터리 / `.pyc` 146 개를 삭제했다**(약 2.8 MiB).

그중 **61 개가 `cpython-314`** — 대상 런타임은 Python 3.11 인데 **빌드머신(개발 PC)의 3.14 가
컴파일한 바이트코드**가 매체에 실려 있었다. 동작에는 영향이 없으나(3.11 은 자기 태그가 아닌
캐시를 무시한다) 반입물 위생 문제라 제거했다.

- 함께 발견·수정: `artifacts/ai-server/tests/` 가 수집 스크립트의 `rm -rf` 누락으로 재수집마다
  한 겹씩 중첩되고 있었다(`tests/tests`). 중복분을 지우고 스크립트를 고쳤다.
- **재발 방지**: 수집 스크립트(`30-collect-ai-server.sh`)가 복사 직후 바이트코드를 제거하고,
  패키징 마지막에 매체 전체를 한 번 더 훑는다(`package.sh`).

### 7-3. 정리 후 매체 용량

**약 1,575,467,000 B (1.46 GiB) · 4,343 파일** — 위 두 건 반영 후 실측값이다.
⚠ 문서를 손볼 때마다 수 KB 단위로 움직이므로 **매체 생성 직전에 다시 측정**한다:
`find deploy/onprem -type f -exec stat -f%z {} + | awk '{s+=$1} END{print s}'` (리눅스는 `stat -c%s`).

---

## 8. ★ 개발용 화면이 켜진 채 반입된다 — 사실과 근거

심의에서 *"운영 반입본에 왜 개발용 로그인 화면이 있는가"* 라는 질문이 나올 것이므로 먼저 밝힌다.

### 8-1. 사실

프론트엔드 산출물의 두 설정이 **`true` 인 채로 빌드되어 반입된다**
(`artifacts/frontend/BUILD-INFO.txt` 실측):

```
VITE_DEV_LOGIN_ENABLED=true      →  /dev/login      (개발용 토큰 발급 화면)
VITE_DEV_UPLOAD_ENABLED=true     →  /admin/uploads  (영상 수동 업로드 화면)
```

설치 시 생성되는 런타임 설정(`/etc/klid/frontend.env` → `klid-config.js`)의 템플릿 기본값도
**둘 다 `true`** 다. 즉 **운영자가 손대지 않으면 두 화면 모두 노출된다.**

### 8-2. 두 화면의 성격은 다르다 — 하나로 묶어 읽으면 안 된다

| | `/dev/login` (개발용 로그인) | `/admin/uploads` (수동 업로드) |
|---|---|---|
| 성격 | **폐쇄망 브링업 전용** | **운영 상시 기능**(2026-08-24 확정) |
| 대응 백엔드 | `/v1/dev/tokens` | `/v1/dev/upload` |
| 백엔드 기본값 | **`false`**(fail-closed) | 운영 프로파일에서 **`true`**(의도적) |
| 운영 프로파일에서 | **기동 가드가 켜짐을 금지한다** — `prd`·`stg` 에서 이 토글이 켜져 있으면 **애플리케이션이 부팅을 거부**한다 | 정상 동작. **검수자(REVIEWER) 인가**가 걸려 있다 |
| 결과 | 화면은 보이지만 **토큰을 받을 수 없다**(엔드포인트 404) | 화면과 API 가 함께 동작한다 |

### 8-3. 근거

1. **개발용 로그인 화면만으로는 아무것도 얻을 수 없다.**
   `/v1/dev/tokens` 는 인증 없이 임의 권한 토큰을 발급하는 경로라, 애플리케이션에 **부팅 거부
   가드**가 있다. 운영 계열 프로파일(`prd`·`stg`)에서 이 토글이 켜져 있으면 애플리케이션이
   기동하지 않는다. 설정 파일의 `false` 한 줄에 의존하지 않고(환경변수 하나로 덮일 수 있으므로)
   **기동 자체를 실패시켜** 정책을 강제한다. 따라서 운영 형상에서 이 화면은 **동작하지 않는 껍데기**다.

2. **폐쇄망 브링업에 필요하다.**
   저작도구는 자체 로그인 UI 가 없고 관제서버·포털이 발급한 토큰을 인계받는다. 설치 직후
   관제서버가 아직 연결되지 않은 구간에서는 **저작도구가 살아 있는지 확인할 방법이 이 화면뿐**이다.
   화면을 지운 채 반입하면 브링업 실패 시 원인 판별 수단이 사라진다.

3. **수동 업로드는 개발용이 아니라 운영 동선이다.**
   외부에서 받은 영상을 운영자가 직접 올려야 하는 동선이 실제로 있고 그 입구가 이 화면이다.
   설정 키 이름에 `DEV` 가 들어 있어 오해를 부르지만, 2026-08-24 에 **운영 노출로 확정**된 기능이며
   검수자 권한 인가가 걸려 있다.

4. **재빌드 없이 끌 수 있다.** ← 과거에는 재빌드가 필요했으나 지금은 아니다.
   프론트엔드 설정의 정본은 산출물이 아니라 **`/etc/klid/frontend.env`** 다.

   ```bash
   sudo $EDITOR /etc/klid/frontend.env
   #   VITE_DEV_LOGIN_ENABLED=false
   #   VITE_DEV_UPLOAD_ENABLED=false        ← 업로드까지 끌 경우에만
   sudo /opt/klid/bin/klid-frontend-config   # 반영 — 재빌드·재설치·웹서버 재기동 모두 불필요
   ```

   이 명령이 설정을 읽어 웹 문서 루트의 `klid-config.js` 를 다시 생성하고, 브라우저는 그 파일을
   번들보다 먼저 읽는다. 캐시 금지 헤더가 걸려 있어 즉시 반영된다.
   생성기는 **명시 열거된 키만** 내보내므로(allowlist) 설정 파일의 다른 값이 브라우저로 새지 않는다.

   > ⚠ **수동 업로드를 끌 때는 백엔드도 함께 꺼야 한다** — `/etc/klid` 의 백엔드 설정에
   > `DEV_UPLOAD_ENABLED=false` 를 명시하고 WAS 를 재기동한다. 한쪽만 끄면 화면과 API 중
   > 하나만 사라져 운영자가 원인을 찾기 어렵다.
   > 개발용 로그인은 백엔드가 이미 꺼져 있으므로 프론트엔드만 끄면 된다.

### 8-4. 권고

브링업 검증이 끝난 시점에 **`VITE_DEV_LOGIN_ENABLED=false` 로 바꾸고 위 반영 명령을 1회 실행**할
것을 권고한다. 그대로 두어도 보안 노출은 없으나(§8-3-1), 운영자가 그 화면을 눌러 보고
"왜 안 되는가"를 묻게 된다.

---

## 9. ★ 검증 현황 — 확인된 것과 확인하지 못한 것

무네트워크(`--network none`) 컨테이너 리허설로 실제 확인한 항목과, **확인하지 못해 현장에서
확인해야 하는 항목**을 나누어 적는다. 뒤쪽 목록이 곧 현장 인수 점검 항목이다.

### 9-1. 확인된 것 (리허설 실측)

리허설 환경: `rockylinux/rockylinux:8` (Rocky Linux **8.10** / glibc 2.28 / x86_64), `--network none`.
리허설 시작 시 `curl https://pypi.org` 이 실패함을 먼저 확인해 네트워크 차단을 증명한 뒤 진행했다.

| # | 확인 항목 | 결과 |
|:-:|---|---|
| 1 | 네트워크 차단 상태에서 GPG 공개키 등록 | 성공 — `gpgcheck=1` 상태로 RPM 설치 |
| 2 | 로컬 저장소 오프라인 RPM 설치 | 성공 — 261개 반입 중 **105개 설치, glibc 무변경** |
| 3 | el8 모듈러 패키지(httpd) 설치 | 성공 — 로컬 저장소에 `module_hotfixes=1` 을 넣어 해소. `httpd -v` 동작 |
| 4 | SELinux 관리 도구 설치 | 성공 — `policycoreutils-python-utils` 등 |
| 5 | ffmpeg / ffprobe 설치·실행 | 성공 — `/usr/bin/ffmpeg` 4.4.8, `/usr/bin/ffprobe` 4.4.8 |
| 6 | ffmpeg 실동작 | 성공 — 테스트 영상 생성 후 `ffprobe` 로 `duration=3.000000` 산출 |
| 7 | `import cv2` (opencv 런타임 의존) | 성공 — `libGL.so.1` 해소 확인 |
| 8 | `import torch` / `import onnxruntime` | 성공 — CPU 판 |
| 9 | sam2 소스 설치·`import sam2` | 성공 — PEP 517 빌드가 번들 `setuptools 81.0.0` 을 찾아 완주 |
| 10 | `sudo ./scripts/install.sh --role=ai` | **완주** |
| 11 | `sudo ./scripts/install.sh --role=app` | **완주** |

### 9-2. ★ 확인하지 못한 것 (현장 확인 필요)

| # | 미확인 항목 | 왜 확인 못 했나 | 현장에서 확인할 방법 |
|:-:|---|---|---|
| 1 | **WAS 기동과 `api.war` 실행** | 리허설 컨테이너에 Tomcat 이 없다 | WAR 배포 후 `curl http://127.0.0.1:8080/api/actuator/health/liveness` |
| 2 | **★ WAS 설정 이관 효과 — 대용량 업로드** | 동일 | **대상 장비에서 대용량 영상 업로드를 사람이 1회 수행해야 한다.** 기동 성공으로는 검증되지 않는다 |
| 3 | WAS 스레드 예산·비동기 타임아웃 이관 | 동일 | 긴 스트리밍 응답을 1회 수행해 중간에 끊기지 않는지 확인 |
| 4 | WAS 에 프록시 IP 치환 밸브가 없음 | 동일 | WAS 설정 파일에 `RemoteIpValve` 가 없음을 **사람이 직접 확인**. 애플리케이션 가드는 외부 WAS 설정을 볼 수 없다 |
| 5 | **PostgreSQL 실기동** | 리허설은 `systemctl` 이 스텁이라 서비스가 실제로 뜨지 않았다 | `systemctl status postgresql-16` + 스키마 로드 결과 확인 |
| 6 | **SELinux Enforcing 하의 파일 문맥** | 컨테이너에서 SELinux 정책이 강제되지 않는다 | 웹 문서 루트·NAS 마운트·로그 경로의 문맥 확인. 거부 시 `ausearch -m avc` |
| 7 | **ai-server 실기동 및 추론** | 리허설은 설치까지만 수행 | `systemctl status klid-ai-server` + `curl http://127.0.0.1:9300/health` + YOLOX·SAM2 실추론 1회 |
| 8 | **방화벽 규칙** | 컨테이너에 방화벽이 없다 | 서버 A→B 의 9300, 서버 A 의 80·8080, PostgreSQL 5432, KPST 비식별 포트 도달 확인 |
| 9 | **NAS 마운트** | 리허설에 NAS 가 없다 | `STORAGE_RAW_PATH`(예 `/nas-storage`) 마운트·쓰기 권한 확인 |
| 10 | **실제 RHEL 8.9** | 리허설은 **Rocky Linux 8.10** 에서 수행 | 같은 RHEL 8 계열·같은 glibc 2.28 이나 동일 배포판은 아니다. 특히 PowerTools(Rocky) ↔ CodeReady Builder(RHEL) 리포 명칭이 다르다(오프라인 설치라 대상에서는 리포가 필요 없지만, 추가 조달 시 차이가 난다) |
| 11 | 관제지원시스템과의 공동 배치 간섭 | 리허설 장비에 관제가 없다 | 우리 설치 후 관제 기능(특히 기존 ffmpeg·WAS·httpd)이 정상인지 확인 |
| 12 | KPST 비식별 서버 연동 | 외부 시스템 | 백엔드 설정 후 연동 스모크 |

> **리허설 범위에 대한 정직한 진술**: 위 9-1 은 **최소 컨테이너 이미지**에서 수행한 결과다.
> 실제 서버는 설치된 패키지가 더 많으므로, 리허설을 통과했다면 실서버에서는 더 안전한 쪽이다.
> **반대는 성립하지 않는다** — 실서버에만 있는 조건(SELinux Enforcing, 방화벽, 기존 PostgreSQL,
> 기존 WAS, 관제 공동 배치)은 리허설에서 재현되지 않았다.

---

## 10. 대상 장비 전제조건

아래는 **반입물이 아니라 대상 장비가 갖추고 있어야 하는 것**이다.

| 항목 | 요구 | 서버 |
|---|---|:---:|
| OS / 아키텍처 | **레드햇 엔터프라이즈 리눅스 8.9** (x86_64, glibc 2.28, dnf/rpm) | A·B |
| init | **systemd** | A·B |
| **WAS** | **Tomcat 10.1.x + Java 17 이 이미 설치·구동 중**. 관제지원시스템 WAS 와 동일 사양(실측 17.0.19). 배포 디렉터리 쓰기 권한 필요 | A |
| **ffmpeg / ffprobe** | **전제조건 — 관제지원시스템 측이 설치.** 없으면 설치가 중단된다(§3-3) | A |
| **PostgreSQL** | **16**. ① 매체의 번들 RPM 설치(기본) 또는 ② 기존 PostgreSQL 사용. 어느 쪽이든 **빈 데이터베이스 2개**(control / portal) + 접속 사용자만 준비 | A |
| 권한 | 설치는 root/sudo | A·B |
| CPU / GPU | **이번 반입은 CPU 전용**. 서버 B 에 GPU 가 있으나 사용하지 않는다(CUDA 미반입). GPU 를 쓰려면 재수집이 필요하며 이번 범위가 아니다 | A·B |
| 메모리 | 권장 8 GiB 이상 (WAS JVM + ai-server torch CPU) | A·B |
| 디스크 | 앱·런타임·모델 약 2 GiB + 영상 저장소(별도 산정) | A·B |
| **NAS 마운트** | 영상·프레임 저장 경로(예 `/nas-storage`)가 마운트되고 서비스 계정이 쓸 수 있어야 함 | A |
| **KPST 비식별 서버** | 폐쇄망에 별도 설치·접근 가능(본 패키지 비포함, 외부 시스템). 비식별은 파이프라인 선두 필수 단계라 운영에서 우회할 수 없다 | A |
| **네트워크 규칙** | 서버 A: 80(외부 인바운드는 이것만 노출 권장) · 8080(WAS) · 5432(DB) · KPST 비식별 포트. **서버 A → 서버 B 의 9300 도달 필수** | A·B |

---

## 11. 반입 및 설치 절차 요약

```
[빌드머신 — 인터넷 필요. RPM·pip 휠은 el8 x86_64 환경에서 수집]
  1) ./scripts/package.sh
       api.war · 프론트 dist · pip 휠(CPU) · 파이썬 런타임 · 시스템 RPM · ffmpeg RPM(예비물)
       · GPG 키 · 모델 · (L)GPL 대응 소스 · 라이선스 고지 집합
  2) 무네트워크(--network none) 설치 리허설 통과 확인
  3) deploy/onprem/ 폴더 전체를 반입 매체로 복사 (src/ 잔재 제거 — §7)

[서버 A — 폐쇄망, RHEL 8.9. 프론트 + 백엔드]
  4) 체크섬 대조 (§5-1 의 SHA256SUMS)
  5) sudo ./scripts/install.sh --role=app
       (옵션)PostgreSQL → api.war 배치 → 프론트 dist + httpd → DB 스키마 로드
       → ffmpeg 전제조건 검증(없으면 여기서 중단)
  6) sudo $EDITOR /etc/klid/application.properties     # DB 접속·JWT 시크릿 등
     sudo $EDITOR /etc/klid/was.env                    # WAS 현장값
     sudo $EDITOR /etc/klid/frontend.env               # 상위 로그인 주소(필수) · 개발화면 토글(§8)
  7) ★ WAS 설정 이관 — docs/10-was-settings.md 체크리스트 완수
       (건너뛰면 기동·일반 요청은 정상이고 대용량 업로드만 조용히 깨진다)
  8) ★ /opt/klid/app/api.war 를 WAS 배포 디렉터리로 사람이 복사 (파일명 변경 금지)
  9) sudo systemctl enable --now httpd

[서버 B — 폐쇄망, RHEL 8.9. ai-server]
 10) 체크섬 대조
 11) sudo ./scripts/install.sh --role=ai
 12) sudo $EDITOR /etc/klid/ai-server.env
 13) sudo systemctl enable --now klid-ai-server
     ★ 서버 A 의 AI_SERVER_URL 을 이 장비 주소로 바꿀 것 (기본값은 127.0.0.1)
```

설치 검증:

```bash
curl -fsS http://127.0.0.1:8080/api/actuator/health/liveness   # 백엔드 (WAS 기동 후, 서버 A)
curl -fsS http://127.0.0.1/                                     # 프론트엔드 (서버 A)
curl -fsS http://127.0.0.1:9300/health                          # ai-server (서버 B)
```

---

## 12. 심의 유의사항

1. **자바 런타임 미반입** — 대상 WAS 가 이미 Java 17 로 구동 중이므로 반입하지 않는다(§3-1).
   따라서 이 매체만으로는 백엔드가 단독 실행되지 않으며, WAS 가 전제조건이다.
2. **ffmpeg 은 담되 설치하지 않는다** — 관제지원시스템과 공동 배치라 자동 설치가 관제를 깨뜨릴 수
   있다. 예비물로만 동봉하며 별도 명령으로만 설치한다(§3-3). 담아서 넘기는 것 자체가 재배포이므로
   GPL 대응 소스(SRPM)를 함께 반입한다.
3. **카피레프트 7건 + wheel 번들 2건에 대한 대응 소스를 실물로 동봉했다**(§6-3·§6-4).
   대응성은 soname ↔ 소스 선언 버전 기계 대조로 확인했으며, 불일치 시 수집이 중단되도록 자동화되어 있다.
4. **"허용적 라이선스라 의무가 없다"는 서술을 쓰지 않았다** — MIT·BSD·Apache-2.0 도 재배포 시
   고지·사본 제공 의무가 있으며, 371건 전부의 전문을 동봉해 이행했다(§6-1·§6-2).
5. **개발용 로그인 화면이 켜진 채 반입된다**(§8). 운영 프로파일에서는 백엔드 기동 가드가 해당
   엔드포인트를 구조적으로 차단하므로 실질 노출은 없으나, **사실을 먼저 밝히고 재빌드 없이 끄는
   절차를 함께 제공**한다.
6. **검증 범위의 한계**(§9-2) — 무네트워크 리허설은 조달 누락과 오프라인 설치를 검증하지만,
   **WAS 기동·대용량 업로드·SELinux Enforcing·실기동 추론·방화벽·NAS 는 검증하지 않았다.**
   특히 **WAS 설정 이관은 누락돼도 기동·일반 요청이 정상이라 신호가 없으므로**, 대상 장비에서
   대용량 업로드를 1회 수행해 확인해야 한다.
7. **리허설 배포판이 대상과 다르다** — Rocky Linux 8.10 에서 수행했다. 같은 RHEL 8 계열이고
   glibc 2.28 로 동일하나 동일 배포판은 아니다.
8. **매체 잔재 정리**(§7) — `src/` 40.2 MiB 는 반입 대상이 아니므로 매체 생성 시 제거한다.

---

**첨부**: `deploy/onprem/` 매체 일체
(설치 스크립트 `scripts/` · 단계별 문서 `docs/00~11` · 설정 템플릿 `config/` ·
라이선스 고지와 대응 소스 `licenses/` · DB 스키마 `db/`)
