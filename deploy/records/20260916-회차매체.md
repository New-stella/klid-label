# 회차 매체 기록 — 2026-09-16  (`klid-at-patch-20260916`)

> ★★ **최신 판 = 2026-09-17 08:05 재조립본(`cc122df58`, sha256 `6459614c…2b44`).** 같은 회차 ID 를 유지하고 내용을 갱신했다.
> 이전 판(09-16 22:11, `131dc133f`, sha256 `a17b6d33…`)은 **폐기**다 — 손에 있으면 쓰지 말 것.
> ★ **소스를 싣지 않았다**(`--no-src`, 사용자 확정 — *"소스 필요없고"*). `VERSION.txt` 에 `source=none`.

★ **아직 나가지 않았다.** 이 문서는 「무엇을 만들었나」다. 현장에 얹으면 **그날** `20260916-현장배포기록.md` 를 남긴다 —
다음 회차의 `--baseline` 은 그쪽에서 나온다.

⚠⚠ **`20260910-현장배포기록.md` 이 없다.** 직전 회차(09-10)가 현장에 얹혔다는 것은 **사용자 구두 확인**뿐이다.
그래서 이번 기준선 `6c679fd01` 도, **현장에 `V37` 이 적용됐는지**도 문서 근거가 없다. 이번에 얹을 때 함께 확인해 기록한다.

## ★★★ 배포할 것 (현장 작업 순서)

매체: `deploy/klid-at-patch-20260916.tar.gz` (208M) + `.sha256`

| # | 축 | 할 일 | 매체 안 위치 |
|---|---|---|---|
| 0 | 반입 | `shasum -a 256 -c klid-at-patch-20260916.tar.gz.sha256` → 풀고 `shasum -a 256 -c SHA256SUMS.patch` | 매체 루트 |
| 1 | **DB** | `db/` 폴더를 DB 서버로 옮겨 **`apply.sh`** 실행 — **`V32`~`V39` 누적 전량**이 실려 있고 이미 적용된 것은 이력으로 건너뛴다. 이번 신규는 **`V38`·`V39`**, 현장 적용 여부 미확인은 **`V37`** | `onprem/db/apply.sh` · `README-apply.md` · `incremental/` |
| 2 | **WAS** | **`api.war`** 를 올린다(passthrough 향, 컨텍스트 `/label-studio/api`). ⚠ `api-strip.war` 를 올리면 화면은 뜨는데 **API 만 전건 404** — 신호가 없다 | `onprem/artifacts/backend/api.war` |
| 3 | **웹** | web-01 · web-02 둘 다 `dist/control/` 로 교체. ⚠ **`klid-config.js` 는 현장 파일을 보존**(매체의 것은 빈 기본형) — 교체 후 비지 않았는지 확인 | `onprem/artifacts/frontend/dist/control/` |
| 4 | **설정** | 설정 화면 → 연동 서버 주소 → **「관제 계정 창구」** 주소 입력(아래 ★★ — 폴백 없음) | — |
| 5 | **확인** | `onprem/scripts/verify/verify-deploy.sh` 실행 · 필요 시 `verify-queries.sql` (**14절** = 비식별 재시작 진단) | `onprem/scripts/verify/` |
| 6 | **관찰** | 얹은 뒤 새 영상의 **비식별이 `OUTPUT_WRITE_FAILED` 로 끝나면** 산출물 디렉터리 권한부터 본다(아래 ⚠ 4) | — |
| 7 | **기록** | 그날 `20260916-현장배포기록.md` 작성 — 올린 WAR 향 · 적용된 증분(특히 `V37`) · web 두 대 · `klid-config.js` 상태 · 계정 창구 주소 입력 여부 | 이 폴더 |

**얹은 뒤 화면에서 확인할 것**: 비식별이 실패한 영상에 영상 상세 「배치 처리 실패」 패널과 재기동 버튼이 뜬다 ·
목록에서 골라 일괄 재시작하면 결과 창에 접수 건수와 영상별 사유가 나온다.

## 판본 좌표

| | |
|---|---|
| 기준선(현장에 깔린 판) | **`6c679fd01`** — 09-10 회차의 `patch_commit`. ⚠ 근거는 사용자 구두 |
| 이번 판 | **`cc122df58`** (`main` — PR #229 까지) ⚠ 구 판 `131dc133f` 폐기 |
| 담긴 `main` | `cc122df58` — PR #229 까지(이번 판 = `main` 그 자체) |
| 사이 커밋 | 180 · 매체에 실리는 축 파일 645 |
| DB 증분 (이번 신규) | **`V38`·`V39`** |
| DB 증분 (실제로 실린 것) | **`V32`~`V39` 누적 전량** |
| 프론트 채널 | **관제(`control`) 단독** — 이 현장은 관제 채널이라 포털 산출물이 갈 자리가 없다 |
| 매체 | 227M / 2,532파일 · tar.gz **208,178,078 B** (208M) · **소스 미포함** |
| sha256 | `6459614c4ed6482552aff76106446061bf35a427111c62296bff5fe7886f2b44` |
| 조립 시각 | 2026-09-17 08:05:18 (`FE_CHANNELS=control ./scripts/make-patch-media.sh --baseline=6c679fd01 --id=klid-at-patch-20260916 --no-src`) |

## ★★ 얹은 뒤 반드시 — 설정 페이지에서 「관제 계정 창구」 주소를 넣는다

**이번 회차의 핵심이 이것이다.** 09-10 배포본에 있던 **토큰 갱신 오류**가 이 회차에서 닫힌다.

관제는 **계정 창구(`/api/account/`)와 데이터셋 창구(`/api/data-set/`)를 서로 다른 WAS 에 둔다.**
그동안 세션 연장·로그아웃 중계가 **통지 수신처 주소를 재사용**했기 때문에, 통지 WAS 를 넣은
현장에서 세션 연장이 **404 로 전부 실패**했고 누적 실패로 서킷이 열려 **로그아웃 중계까지** 막혔다.

- 주소는 **설정 화면 → 연동 서버 주소 → 「관제 계정 창구」** 에 넣는다.
- ★**폴백이 없다.** 비어 있어도 통지 주소로 대신하지 않으며 배포 기본값도 비어 있다.
  ⇒ **넣지 않으면 매체만 얹어도 증상이 그대로다.** 「고쳤는데 안 고쳐진」 상태가 된다.
- 맞는 주소인지 판정하는 법:
  `POST <주소>/api/account/auth/refresh` → **401 JSON 이면 맞는 주소 · 404 HTML 이면 틀린 주소**
- ⚠ 그 호출은 **앱과 같은 자리에서** 해야 한다. 다른 장비에서 부르면 이름이 안 풀려 `000` 이 나오고,
  그걸 「주소가 틀렸다」로 오판하기 쉽다(실제로 한 번 그랬다).

개발서버 실측(2026-09-16)이 이 구조를 실물로 보여 준다 — 통지 `eap8:18080` · 계정 `eap7:28080`.
**장비도 포트도 다르다.**

## ★ 이번 회차에 달라진 것

### 1. 배포 확인·장애 진단 도구를 매체에 실었다 (신설)

`onprem/scripts/verify/` — 손으로 조립한 원격 한 줄 명령은 반드시 깨진다. 실제로 이번 회차에
따옴표 중첩으로 죽었고, 포트를 섞어 부르면 **전건 000** 이 나와 배포 실패로 오판한다.

- `verify-deploy.sh` — 배포 형상 · 엔드포인트 · 연동 주소 · 스키마 판본 · 기능 케이스.
  **현장(WAS·systemd)과 개발서버(도커)를 한 스크립트로** 덮는다. 판정은 도커 유무로 자동.
- `verify-queries.sql` — 케이스별 조회 + **장애 진단** 쿼리. 블록마다 「무엇이 정상인지」를 적었다.

★ **개발서버에서 실제로 돌려 결함 넷을 잡아 고쳤다.** 돌려보지 않았으면 그대로 실렸을 것들이다.
1. 기동 로그를 짧게 잘라 봐서 컨텍스트 줄이 **정상인데도 빈 출력**
2. 오류를 낱말로 찾아 경고 줄에 든 `observed an error` 를 집는 **거짓 양성**
3. 계정 창구를 호스트에서 불러 `000` — 앱과 같은 자리에서 불러야 한다
4. ★**SQL 이 프레임을 영상으로 조인** — 컬럼이 실재해 **오류 없이 조용히 틀린 답**을 냈다

4번이 이 라운드에서 가장 값진 것이다. 같은 파일에 *"확인하지 못한 표는 추측하지 말고 스키마를
먼저 보라"* 고 써 놓고 바로 아래에서 어겼다. 실측으로 판정했다 — **프레임 매칭 20/20 · 12/12
인데 영상 매칭은 15/20 · 3/12**. 영상 쪽이 100% 가 아니므로 영상일 수 없다. 식별자 범위가
겹쳐 일부가 우연히 맞는 것이었다.
⇒ **규칙: 컬럼 이름이 `SRC_SN` 이면 프레임으로 읽는다. 영상이려면 `RAW_SN` 이라야 한다.**

### 2. 스키마 증분 둘

- **`V38`** `ls_task_evnt_log.actor_role_cd` — 작업 이력에 행위자 역할. CHECK(ADMIN|REVIEWER|WORKER).
  신규 컬럼이라 **과거 행은 NULL 이 정상**이다(소급 채우지 않는다).
- **`V39`** `ls_data_raw.excl_yn` — 영상 목록 제외(숨김). `char(1) NOT NULL DEFAULT 'N'`.
  ⚠ **화면 시야일 뿐이다** — 배치·관제 통지·데이터마트 뷰·산출물에 영향이 없다.
  제외한 영상이 데이터마트 뷰에 그대로 보이는 것이 **정상**이며 결함이 아니다.

### 3. 비식별 실패 영상 재시작 (2026-09-17 재조립분)

- **새 버튼·새 창구가 없다.** 기존 배치 재시작(영상 상세 건별 · 영상 목록 일괄)이 비식별이 실패하고 마킹 준비 전인 영상을 받으면 **비식별부터 다시** 수행한다. 성공하면 마킹할 수 있다.
- 거부되는 경우: 열린 비식별 누락 신고 · 승인 이력 · 진행 중 위탁 · 동시 요청(409) · 파생영상(400). 결과 창에 영상별 사유가 나온다.
- 다시 위탁할 때 비식별 솔루션 프로젝트 이름이 `raw{영상번호}r{이력번호}` 로 바뀐다(첫 위탁은 종전 `raw{영상번호}`). ⚠ 현장 비식별 솔루션 화면에서 **같은 영상의 프로젝트가 여러 개** 보이는 것이 정상이다.
- 확인: `verify-deploy.sh` E 섹션 · `verify-queries.sql` **14절**(재시작 대상 · 거부 사유 · 남은 재시작 잠금).
- **스키마 변경 없음**(재시작 잠금은 기존 작업 잠금 표를 식별자 접두 `DEIDRETRY-` 로 구분).
- 개발서버 실측(2026-09-17): 목업을 내린 채 올린 영상이 위탁 전 실패 → 재시작 → 위탁 후 폴링 실패 → 재시작 → **`raw44r45` 로 위탁·완료, 마킹 준비 도달, 잠금 해제**. 열린 신고 영상(raw 21)은 409.

### ⚠ 4. 현장 확인이 필요한 위험 — 산출물 디렉터리 권한 (`CO-20260917-비식별산출물-디렉터리-권한`)

개발서버에서 **WAS(root)가 만든 비식별 산출물 디렉터리(755)에 다른 계정으로 도는 비식별 솔루션이 쓰지 못해** 비식별이
`OUTPUT_WRITE_FAILED` 로 끝났다. 현장에서 WAS 실행 계정과 비식별 솔루션 계정이 다르면 **같은 일이 난다.**
얹은 뒤 비식별이 실패하면 먼저 산출물 디렉터리 소유·권한과 두 계정의 그룹·umask 를 본다. 방향은 미확정이다.

## 이 회차에 담긴 변경

| PR | 무엇 |
|---|---|
| **#229** | 재위탁 시 비식별 프로젝트 이름이 겹치지 않게 한다(회차별 이름) |
| **#228** | 비식별이 실패한 영상을 기존 배치 재시작(건별·일괄)으로 다시 비식별한다 · 확인 쿼리 14절 · 매체 `--no-src` |
| **#226** | 관리 화면에서 사용자 표시 이름을 고칠 수 있게 한다(관리자 전용 · 스키마 변경 0) |
| **#225** | 외부 연동 호출을 공통 필터 한 곳에서 기록한다 |
| **#224** | 온프렘 마킹 영상 재생 차단 3건 — 경로 접두 이중화 · 비식별 산출물 인덱스 위치 · 재생 인증 만료 |
| **#223** 외 | 포털 데이터셋 해제본 파서 · 실물 배포본 대응 · 킷 부품 이관 다수 |
| CO-20260915 | 영상 목록 제외(숨김) + 배정 해제 창구 |
| CO-20260914 | **관제 계정 창구 주소 분리** — 위 ★★ 항목 |

## ⚠ 얹기 전에 반드시

- ★★ **`api.war` 를 올린다.** 매체는 두 향을 싣지만 이 현장이 쓰는 것은 passthrough 향
  (`/label-studio/api`) 하나다. `api-strip.war` 를 올리면 **WAS 는 정상 기동하고 화면도 뜨는데
  API 만 전건 404** 가 된다 — 아무 신호가 없다.
- **DB 증분 `V38`·`V39` 를 적용한다.** 컬럼 추가 둘이라 되돌리기는 쉽지만, 적용하지 않으면
  앱이 없는 컬럼을 읽는다.
- **얹은 뒤 `onprem/scripts/verify/verify-deploy.sh` 를 돌린다.** 확인 항목이 한 번에 나온다.
- ★ **설정 페이지에서 관제 계정 창구 주소를 넣는다**(위 ★★ 항목). 이걸 빠뜨리면 이번 회차의
  주목적이 달성되지 않는다.

## 검증

| | |
|---|---|
| 회귀 | 백엔드 **9,956건** · 프론트 **5,804건** 전건 통과(`cc122df58` 기준, 09-17) · 빌드 관제·포털 두 채널 성공 |
| 매체 무결성 | `SHA256SUMS.patch` **2,531항목** · `shasum -c` 전건 OK · **커버리지 양방향 차집합 0건** · tar.gz sha256 일치 |
| 두 향 WAR | `artifacts/backend/SHA256SUMS` 에 `api.war`·`api-strip.war` **둘 다 등재**(09-06 구멍 재발 없음) |
| 매체 스크립트 | `bash -n` **56개 전건 통과**(소스 미포함으로 줄었다) · `verify-deploy.sh`·`db/apply.sh` 실행권한이 **tar 안에서도 유지** |
| 채널 | `control` 단독 — 조립기 양성 대조 통과(`control:2`) · portal 산출물 **0건** |
| WAR 내용 | 두 향 모두 재시작(`LeadDeidentRetryService`)·재위탁 이름 쿼리 포함 · 컨텍스트 `/label-studio/api` · `/label-studio` |
| 소스 | 매체·tar 모두 `src/` **0건** |
| 개발서버 실배포 | 246 에 `cc122df58` 을 올려 확인(09-17) — health 200 · context `/label-studio/api` · Flyway V39 · 실패 0 · 재시작 거부·성공 경로 실측 |

### 재확인 완료된 경고 2건

조립기가 매번 내는 `[WARN] 이름 대조로는 판정 불가` 다.

```
V36__relax_ls_ai_srvr_srvr_id_format.sql     : REPLACED|ck_ls_ai_srvr_srvr_id_format
V38__add_actor_role_cd_to_ls_task_evnt_log.sql : REPLACED|ck_ls_task_evnt_log_actor_role
```

**이름은 그대로 두고 정의만 교체**해서 대조기가 판정을 못 하는 것이지 어긋난 것이 아니다.
사람이 확인해 닫았다 — 개발서버 실 DB 의 제약 정의와 매체 `schema.sql` 이 **문자 그대로 일치**한다.

```
ck_ls_ai_srvr_srvr_id_format   : CHECK (srvr_id ~ '^[a-z0-9_-]{1,20}$')
ck_ls_task_evnt_log_actor_role : CHECK (actor_role_cd = ANY (ADMIN|REVIEWER|WORKER))
```

**같은 모양의 증분이 또 오면 같은 경고가 뜬다.** 그때도 실 DB 와 대조해 닫는다.

## 남은 위험 (정직하게)

- ★ **현장 `V37` 적용 여부가 기록에 없다** — 09-10 현장 기록 부재 탓. `apply.sh` 가 건너뛰거나 적용하므로 작업은 같고, **얹은 뒤 이력 표를 조회해 기록**한다.
- ★ **조립기 기본값으로는 포털 빌드에서 멈춘다** — `make-patch-media.sh` 가 관제 자산 경로(`VITE_BASE_PATH=/label-studio/`)를 전역으로 내보내 포털 빌드의 고정 경로 가드에 걸린다. 이 회차는 관제 단독이라 `FE_CHANNELS=control` 로 돌렸다(어제 판도 같았다). 두 채널을 싣는 회차가 오면 조립기를 먼저 고쳐야 한다.
- **재조립 판(09-17)의 재시작 기능은 개발서버에서만 돌려 봤다** — 실제 비식별 솔루션의 동일 이름 거부·이름 길이 상한은 규격 문구로만 확인했다.

- **기준선 근거가 문서가 아니라 구두다** — 위 ⚠⚠ 항목. 현장 적용 현황을 조회하지 않았다.
- ~~소스와 매체의 좌표가 `main` 이 아니다~~ — **닫혔다(09-17 재조립).** 이번 판 `cc122df58` 은 `main` 머지 커밋이다.
- **`verify-deploy.sh` 는 개발서버(도커)에서만 돌려 봤다** — 현장 축(WAS 프로세스 탐색·`WAS_LOG`·
  `/etc/klid/` 설정 읽기)은 **한 번도 실행되지 않았다.** 그 분기는 문법만 확인했다.
- **재생 축 확인이 미완이다** — #224 가 고친 재생(서명 TTL·쿠키·접두)은 브라우저로만 판정되는데
  아직 하지 않았다. 개발서버에서 이어서 한다.
- **실제 장비에서 돌려 본 적이 없다** — 매체는 조립을 통과했을 뿐이다.
