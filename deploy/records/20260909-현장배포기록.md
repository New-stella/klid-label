# 현장 배포 기록 — 2026-09-09  (`klid-at-patch-20260909`)

다음 회차의 `--baseline` 이 여기서 나온다. **이 문서가 「지금 현장에 무엇이 깔려 있는가」의 정본이다.**

⚠ **뒤늦게 쓴 기록이다 (2026-09-10 작성).** 얹은 그날 남기지 못했고, 다음 회차를 만들 때 기준선을
물어서야 채웠다. 그 사이 `20260909-회차매체.md` 가 *"아직 나가지 않았다"* 로 남아 있어 기준선을
한 회차 과거(`d26aee0e`)로 잡을 뻔했다. **얹은 날 남긴다.**

## 판본 좌표

| | |
|---|---|
| 직전 판(기준선) | `d26aee0e` — `20260907-현장배포기록.md` |
| **이번에 나간 매체** | **`klid-at-patch-20260909`** · 소스 **`2238be779`** · **2026-09-09 배포** |
| DB 증분 | **`V32`~`V36` 적용 완료** |

★ **`2238be779` 가 정본이다.** `20260909-회차매체.md` 가 처음 적었던 `1ea2815a1` 은 그 문서를
*쓰던 시점*의 HEAD 였다 — 매체는 그 문서를 담은 커밋으로부터 **55초 뒤**에 빌드됐다
(커밋 `00:16:31` → 빌드 `00:17:26`). **판본은 산출물(`VERSION.txt`)이 스스로 적은 것을 읽는다.**
두 커밋 차이는 문서 2개뿐이라(마이그레이션 0 · 런타임 0) 실린 것은 같다.

## 실제로 한 것

| 축 | 결과 |
|---|---|
| **DB** | **마이그레이션까지 적용 완료** — 매체가 실은 `V32`~`V36` 누적 전량 |
| **애플리케이션** | 매체를 얹어 배포 완료 |

★ **`V36` 은 두 축 모두 확인됐다 (2026-09-10).**

| 축 | 결과 |
|---|---|
| 현장 적용 | **적용 완료** — `V36` 까지 돌았다 |
| 통합 `schema.sql` | **완화판이 들어 있다** — `schema.sql:96` 이 `'^[a-z0-9_-]{1,20}$'` 다 |

⚠ 매체 조립기가 이 항목을 「확인불가」로 넘긴 것은 **검사가 이름만 대조하기 때문**이지 어긋나서가
아니었다. 제약을 같은 이름으로 지웠다 다시 만드는 형태라 이름으로는 새 정의인지 알 수 없다 —
**앞으로도 같은 모양이면 같은 경고가 뜬다.** 그때는 정의를 직접 대조한다.

## ⚠ 이 기록에 없는 것 (지어내지 않는다)

뒤늦게 쓴 탓에 아래는 **모른다.** 필요해지면 현장에서 확인해 채운다.

- **얹은 시각** — 날짜는 `2026-09-09` 로 확인됐다(사용자). 시각은 모른다
- **WAR 향** — `api.war`(`/label-studio/api`) 인지 `api-strip.war`(`/label-studio`) 인지.
  판정법: `https://<사이트>/label-studio/api/actuator/health/liveness` 를 열어 JSON 이 나오면
  passthrough 향이다(그 경로는 인증 없이 열려 있다). 도구로는 `./scripts/site/klid-war`.
- **웹 노드** — web-01 · web-02 둘 다 교체했는지
- **현장에서 물린 것** — 09-07 처럼 스크립트가 죽거나 `klid-config.js` 가 비는 일이 있었는지
- **09-07 이 남긴 미결** — 인계 로그인(HS512 `JWT_SECRET` 64바이트)·사용자/역할 재생성·
  WAS systemd 정합이 이 회차에 해소됐는지

## 남은 일 (09-07 에서 넘어온 것)

- [ ] **인계 로그인** — 운영 관제가 HS512 라 `JWT_SECRET` 이 64바이트 이상이어야 한다.
      `./scripts/site/klid-jwt` 로 판정
- [ ] **사용자·역할 재생성** — 09-07 에 스키마를 새로 만들어 전부 비었다. 관리자 부트스트랩부터
- [ ] **WAS systemd 정합** — 재부팅 시 자동 기동되지 않는 형상
- [ ] **옛 상태점검 잡 정의 제거** — `09-operations-runbook.md` §8-1. 자동으로 사라지지 않는다
