# 현장 배포 기록 — 2026-09-07

다음 회차의 `--baseline` 이 여기서 나온다. **이 문서가 「지금 현장에 무엇이 깔려 있는가」의 정본이다.**

## 판본 좌표

| | |
|---|---|
| 직전 판(기준선) | `c3e97792` (2026-09-04 14:00) — 금요일 18시 이전 마지막 |
| **이번에 나간 매체** | `klid-at-patch-20260907` · WAR `sha256:38b9ed2f…` · 소스 **`5b2887d8`** |
| 저장소 최종 판 | `d26aee0e` (배포 스크립트만 추가 — 백엔드·프론트 소스는 `5b2887d8` 과 동일) |
| DB 증분 | `V32` · `V33` |

⚠ **현장 매체는 14:29 빌드**다. 그날 매체를 세 번 떴고(14:25 · 14:29 · 14:36) 현장에 간 것은
가운데 것이다. **백엔드·프론트 소스는 세 판이 같고**(HEAD `5b2887d8`) 매체 안 스크립트·README 만
다르다. 그래서 `SHA256SUMS.patch` 대조는 **그 매체 안의 것**으로 해야 한다 — 다른 판의 해시와
비교하면 정상인데도 불일치로 보인다.

## 실제로 한 것

| 축 | 결과 |
|---|---|
| **DB** | `klid_at` 을 **드랍하고 `schema.sql` 로 재적재**했다(적재 데이터가 없다고 사용자가 확정). 통합본이라 V32·V33 내용이 포함된다 — 증분을 따로 넣지 않았다. `ls_schm_aplcn_hstry` 는 **비어 있다**(다음 회차에 V32·V33 이 미적용으로 보이며, 둘 다 `IF NOT EXISTS` 라 다시 돌려도 무해하다) |
| **웹** | web-01 · web-02 **둘 다 오늘 판으로 교체 완료**. web-02 는 설치 스크립트가 중간에 죽어 `klid-config.js` 가 빈 값으로 남았다가 백업에서 복원했다 |
| **WAS** | **오늘 판으로 교체 완료**(`--restart` 없이 마커로 재배포) |
| **AI(GPU)** | 휠 교체·검증 통과(torch `2.12.0+cu126` · H100 ×2). `LD_LIBRARY_PATH` 를 `ai-server.env` 에 추가 |

## 현장 형상 — 우리 문서와 달랐던 것

```
DB        klid_system_pg_prod  ·  klid_at 스키마  ·  postgres 계정
접속      CONTROL_DB_HOST=10.177.199.148:19999,10.177.199.149   ← JDBC 다중호스트 문자열
          maxscale(19999) → 실서버 10.177.199.146:15432
설정 파일 /etc/klid/application.properties        (backend.env 아님 — WAR 형상)
WAS       /GCLOUD/JBOSS/jboss-eap-8.1  ·  systemd 유닛 밖에서 기동 중
웹        klid-web-01 · klid-web-02   ·  DocumentRoot /GCLOUD/WebApp/label-studio
```

**★ 관제 인계 토큰(운영)** — 개발서버(246)와 다르다.

```
alg      HS512                       (246 은 HS256)
클레임   userId · userNm · authority=PLTF_MANAGER   (246 은 sub · role · channel)
```

⚠ `CLAUDE.md` 의 「관제 인계 JWT 실측 계약(2026-09-05)」은 **246 기준**이다. 운영은 위 모양이며,
`ADR-063` 이 가정했던 형태(비숫자 신원·HS512)가 **운영에서는 실제 경로**다.
⚠ **HS512 는 `JWT_SECRET` 이 64바이트 이상이어야 한다** — 짧으면 값이 맞아도 검증이 실패하고
기동은 성공한다. `./scripts/site/klid-jwt` 로 판정한다.

## 남은 일

- [ ] **인계 로그인** — `./scripts/site/klid-jwt` 로 사유 확정.
      운영 관제가 HS512 라 `JWT_SECRET` 이 64바이트 이상이어야 한다
- [ ] **사용자·역할 재생성** — 스키마를 새로 만들어 전부 비었다. 관리자 부트스트랩부터
- [ ] **WAS systemd 정합** — 지금 형상이면 재부팅 시 자동 기동되지 않는다.
      유닛은 `failed` 인데 프로세스는 9월 4일부터 살아 있다

## 실행 계정 (2026-09-07 확정, 구속)

```
WAS  jboss      웹  apache      ← klid 계정은 만들지 않는다
```

설치가 계정을 만들지 않는다. 이미 있는 계정을 쓰고, 지정하지 않으면 그 장비에서 실제로
도는 프로세스의 소유자를 보고 정한다. **root 로 돌려도 만들어진 것의 소유자는 그 계정**이다.

## 이 회차에서 저장소에 반영한 것

오늘 물린 결함을 그대로 고쳤다 — 상세는 커밋 메시지.

```
site-install.sh          --backend 없으면 조용히 죽던 것
14-install-frontend.sh   SKIP 플래그가 httpd conf 쓰기를 못 막던 것 (이번 web 장애의 원인)
lib/common.sh            host:port,host 정규화 · 설정 파일에서 접속값 읽기 · JBOSS 탐지 일원화
deploy-update.sh         매체 무결성 대조 · 판본 기록 판독
install.sh               KLID_NO_USER_CREATE
JwtAuthenticationFilter  거부 사유 WARN + alg·키길이 진단
SecretKeyResolver        HS512 대비 키 길이 경고
scripts/site/            현장 도구 5종 (klid-status · db · jwt · war · web)
```
