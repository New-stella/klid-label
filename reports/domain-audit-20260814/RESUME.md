# 도메인 점검 — 다음 세션 인계서

> **여기서 중단됨**: D006 배치 B 1차(stale·policy·diagram) 완료 후. 사용자 지시로 새 에이전트 추가 투입 없이 종료.
> 재개하려면 아래 §재개 절차부터 읽으면 된다. **이 문서만 있으면 컨텍스트 없이 이어받을 수 있다.**

## 1. 기본 정보

| 항목 | 값 |
|---|---|
| 스킬 | `mc-logi-domain-review` v1.5.0 (read-only — ITEM 수정 안 함) |
| project_id | `4ece2c3f-8e99-46f5-9580-71108a76e578` (KLID-저작도구) |
| 범위 | 활성 도메인 **14개 × 10차원 = 140 에이전트** |
| 산출 위치 | `reports/domain-audit-20260814/raw/DOMAIN-NNN.md` (도메인별 확정) |
| auditor | `subagent_type: "mc-logi-domain-review:logi-domain-auditor"` |
| 룰 파일 | `/Users/ck/.claude/plugins/cache/logicraft/mc-logi-domain-review/1.5.0/skills/mc-logi-domain-review/dimensions/<dim>.md` + `checklist.md` |

## 2. 진행 현황

| # | 도메인 | 상태 | 갭 |
|---|---|---|---|
| 1 | DOMAIN-001 사용자·권한 | ✅ 10/10 | **58** (P0 13 / P1 35 / P2 10) |
| 2 | DOMAIN-003 영상·프레임 수집 | ✅ 10/10 | **101** (P0 18 / P1 58 / P2 25) |
| 3 | DOMAIN-004 AI 보조 라벨링 | ✅ 10/10 | **85** (P0 12 / P1 43 / P2 30) |
| 4 | DOMAIN-005 검수 | ✅ 10/10 | **106** (P0 27 / P1 58 / P2 21) |
| 5 | **DOMAIN-006 통계·대시보드** | ⏸ **7/10** | **55** (P0 11 / P1 29 / P2 15) — `raw/DOMAIN-006.md` 확정됨 |
| 6 | DOMAIN-007 데이터 증강 | ⬜ 미착수 | |
| 7 | DOMAIN-009 게시판·공지 | ⬜ 미착수 | |
| 8 | DOMAIN-010 라벨링 | ⬜ 미착수 | |
| 9 | DOMAIN-011 마킹 | ⬜ 미착수 | |
| 10 | DOMAIN-012 비식별화 | ⬜ 미착수 | |
| 11 | DOMAIN-013 포털 | ⬜ 미착수 | |
| 12 | DOMAIN-014 시스템 설정 | ⬜ 미착수 | |
| 13 | DOMAIN-015 작업 배정 | ⬜ 미착수 | |
| 14 | DOMAIN-016 관제 통지 | ⬜ 미착수 | |

**누적 380+건 / P0 73+**

### D006 잔여 (재개 시 첫 작업)
**`acceptance` · `requirement` · `test_scenario` 3차원 미실행.** ⚠ 이 도메인은 **UC·AC·SEQ·TEST 가 전역 스캔 결과 0건**이므로:
- `acceptance` → 부재 자체가 ACC-001/002 갭 (SKIP 하지 말 것)
- `test_scenario` → 귀속 TEST 0건 확인 후 TST-001/002 판정
- `requirement` → 통계 축 FEAT 가 프로젝트 9건 중 0건이라 REQ 귀속도 0일 가능성 → 모드 A SKIP 가능성 높음

## 3. 재개 절차

1. `raw/DOMAIN-001.md` ~ `DOMAIN-006.md` 를 읽어 누적 패턴 파악 (아래 §5 요약으로 대체 가능)
2. D006 잔여 3차원 실행 → `raw/DOMAIN-006.md` 확정
3. D007부터 순서대로 진행 (§4 템플릿 사용)
4. 전 도메인 완료 후 **통합 리포트**(`REPORT.md`) 작성 — 중복 클러스터 병합 + P0/P1/P2 정렬 + `auto_fixable` 분리

## 4. 프롬프트 템플릿 (누적 학습 반영 — 그대로 복사해 도메인만 바꿀 것)

```
당신은 logi-domain-auditor 입니다. 아래 도메인의 **<DIM>** 차원을 감사하세요.

# 입력
project_id: 4ece2c3f-8e99-46f5-9580-71108a76e578
domain_id: DOMAIN-NNN
domain_title: <제목>
dimension: <dim>
legacy_grep_enabled: false

# Dimension 룰 — 반드시 Read 로 정독하고 그 룰만 적용
/Users/ck/.claude/plugins/cache/logicraft/mc-logi-domain-review/1.5.0/skills/mc-logi-domain-review/dimensions/<dim>.md
# 공통 체크리스트 — 반드시 Read
/Users/ck/.claude/plugins/cache/logicraft/mc-logi-domain-review/1.5.0/skills/mc-logi-domain-review/checklist.md

# ★★ 도메인 스코프 규칙 (실측 확인된 함정)
1. `list_items` 는 `domain_id` 를 **지원한다**(SKILL.md 의 "미지원" 주석은 낡음). 지원 타입엔 반드시 걸 것. 전역 목록을 도메인 목록으로 세지 말 것.
2. **`use_case`·`acceptance`·`requirement`·`test_scenario`·`diagram_sequence` 는 `domain_id` 미보유라 필터가 0건**(실측: `UC-030` 이 DOMAIN-001 소속인데 필터 0건). **0건을 "없다"로 단정 금지** — 필터 없이 재조회 후 링크로 귀속 판정.
3. ⚠ **부분 read(projection)의 빈 결과를 "필드 부재"로 단정하지 말 것** — 실제 필드명이 `scenario.then` 인데 `fields=[then]` 으로 읽어 `data={}` 를 받고 오판한 사례가 실제로 있었다. 불확실하면 projection 없이 전체를 읽어라.
4. seed = `get_neighbors(DOMAIN-NNN)` backward. "토큰 절약"·"열거 생략" 회피 금지.

# ★ severity 정규화
- `stale: true` 플래그는 증상이지 결함이 아니다. 본문을 열어 **구 모델 서술이 실제로 남은 것만** P0/P1, 플래그만이면 P2. (과거 감사에서 플래그만 보고 P0 26건 → 재판정 전부 0건)
- 반대도 있다 — `stale=false`·`approved` 인데 본문이 통째로 폐기 모델인 사례 다수(`SEQ-001`·`CDIAG-008`·`API-055`). **플래그를 신뢰하지 말 것.**
- 추정 언어로 P0 를 올리지 말 것.

<차원별 특화 블록 — §5 패턴 목록에서 해당 차원 것을 넣는다>
<domain_context — 그 도메인의 확정 정책 + 폐기 모델 표지>

# 출력
STEP A~F 완료 후 **YAML 한 블록만** 출력하고 종료. 자유 텍스트 금지. gap.id 접두는 `DNNN-<CODE>-`.
```

**dim 코드**: COV / LINK / SCH / STL / POL / ACC / RQ / CNT / DIAG / TST

## 5. 누적 학습 — 차원별 주입 블록

### 전 차원 공통 (반복 검출 패턴)
- `DFEAT.implemented_by_endpoints`·`persists_in_tables`·`invokes_apis`·`triggers`·`related_acceptances` **전건 빈 배열**인데 본문엔 API·테이블을 글로 열거 → **도메인 API 전건 orphan** (D001 8 / D003 33 / D004 9 / D005 12 / D006 4)
- `API.implements_features` 전건 빈 배열 / `SCREEN.realizes_use_cases` 전건 빈 배열
- `SCREEN` 에 `references_features`·`references_dfeats` **키 자체 부재**
- **제목·`brownfield.diff_summary` 는 정정됐는데 `description`·`user_story` 만 뒤처짐** — D003 `DFEAT-007`, D005 `DFEAT-021`·`024`, D006 `DFEAT-027`·`028`
- **`change_summary` 가 본문을 부정** — D005 `API-009`("REVIEWER 전용" ↔ "검수자 전용이 아니다")
- **부속 배열 cascade 누락** — `relationships`·`external_dependencies`·`components`·`classes[]`·`brownfield`. D003 `CMP-001`, D004 `CMP-008`, **D005 `CMP-005`(3개 배열 전부)**
- deprecated ITEM 을 활성 ITEM 이 참조 / `brownfield.decided_by` 가 superseded ADR 또는 **주제 무관 ADR**(D004 `UC-005`→`ADR-001`)
- `realizes_features` 가 **엉뚱한 FEAT** 를 가리킴(D005 `MOD-010`→증강 `FEAT-004`, 올바른 값은 `FEAT-009`)
  > ⚠ **주의 — 이건 결함이 아니다**: `realizes_features` 에 **DFEAT ID 가 들어간 것**은 정상이다. `get_item_schema(class_diagram).hints.workflow_notes` 원문이 **`realizes_features : 이 다이어그램이 실현하는 FEAT/DFEAT id 목록`** 이고, 이 필드는 `link_types_from` 4종에 없어 **그래프 링크를 만들지 않는 정보성 필드**다.
  > 메인이 이를 "필드 오용"으로 오판해 D004·D005 프롬프트에 주입했고 갭으로 보고됐다 → **`D004-LINK`(CDIAG-005)·`D005-LINK-004`(CDIAG-006) 소급 기각.** 다음 세션 프롬프트에 이 항목을 넣지 말 것.
- **타 도메인 API 를 `implemented_by_endpoints` 로 지목**(D004 `DFEAT-020`→DOMAIN-010)
- `EVT` 의 `documented_emitters` 는 **표시 전용** — 링크는 발행자 `triggers`/소비자 `consumes` 에 적어야 생김(D005 `EVT-006`)
- `implementation.status=implemented/100` 인데 `modules`·`records` 빈 배열

### schema 차원 — DB 표준 (**CSV 정본 grep 으로 판정**, 검색 API 는 상한 때문에 "0건=미등록" 오판)
- 정본: `docs/LogiCraft-공공표준용어-2026.08.05 151557/` · `docs/LogiCraft-사업용어-2026.08.05 151552/` (인코딩 `utf-8-sig`, **디렉터리명에 공백** → python `os.path.join`)
- **실제 검출된 위반**: `UPD_DT`→**`MDFCN_DT`**(D003·D005 **2회**) / `HSTRY_SEQ`→`HSTRY_SN` / `LOCK_OWNER_ID`→`LCK_OWNR_ID` / `QUE_SN`→`QUEUE_SN` / **`PRCS`(처리) vs `PROC`(프로세스) 혼용** / **코드값 `varchar(16)`**(등록 도메인은 코드V10·코드V20·코드C1~C12 뿐 — D003 `PRVC_TYPE_CD`, D004 `LBL_TYPE_CD`) / 같은 논리용어 두 컬럼 중복 / 같은 용어 테이블마다 다른 타입 / **같은 테이블이 두 ERD 에 다른 컬럼 집합**(D005 `LS_DATA_ISSUE`)
- ⚠ **사전이 낡은 경우도 있다** — D003: 사전 `NEXT_RTRY_DT` vs ERD 확정명 `NXTM_RTRY_DT` → **ERD 가 맞았다**. 기계적으로 맞추지 말 것
- **★"ITEM 이 공표한 값이 실재하지 않음" — 5건 검출됨**: `channel:"CONTROL"`(구현 `INTERNAL`, D001) · `BATCH_COMPLETED`(enum 밖, D003) · `MANUAL_TRIGGER`(사문화 상수, D004) · `ISSUE_COMMENT_SN`(실재 `CMNT_SN`, D005) · `eventTypeCd:"FALL"`(실재는 `EV`-코드, D006). **이 축을 반드시 확인하라**
- **역방향도 있다** — 화면·구현엔 있는데 **API 스키마에만 없음**(D006 `API-055`·`API-057` 의 `approved*`·`dailyCounts`)

### content 차원 — 본문 오염 + 한글 손상
판정: **"이 문장이 이 저장소를 모르는 제3자에게도 참인가?"**
- **금지**: 레포명 · 파일 경로/확장자 · 커밋 해시 · PR 번호 · **구현 상태**("스텁","placeholder","미커밋","구현 중") · **브랜치명** · 감사 이력 · **지시문**("되돌리지 말 것","통일하지 말 것") · **자기 리비전 서술**("구 본문은 ~라고 적었으나") · **마이그레이션 버전 번호** · 어노테이션(`@Pattern`·`@Valid`) · 내부 클래스명(`…Service`·`…Controller`·`…Step`·`…Guard`·`…Runner`·`…Job`·`…Policy`·`…Bridge`·`…Listener`·`…Repository`·`…IT`) · **FE 훅·`data-testid`·CSS 클래스·차트 라이브러리명** · **외부 서버 IP·포트** · **문서 개정 대조 메모**
- **오염 아님**: 컴포넌트·엔티티 이름 / `…Request`·`…Response`·`…Dto` / **DB 테이블·컬럼 물리명·뷰명** / UI 라벨 / `[폐기]` 표기 / `API-*`·`SCREEN-*` 상호참조
- `change_summary` 는 **검사 제외**. **중첩·부속 배열 필수 검사**(D001 `API-006.responses[]`, D003 `TEST-001.steps[].note` 의 **외부 서버 IP**, D004 `API-123.responses[].trackId`)

**★한글 표기 손상 검사 (D006 이 확립한 방법 — 이대로 지시할 것)**
```
① 알려진 오타 grep 15종: 컴럼·챜·찍·보람다·남린다·곀·냙관적·옥겨·좀히·컴러·뺔·릺·컴버스·섮이지·옆기는데
② 희귀 음절 빈도 분석: 본문 전체 음절 중 2회 이하 등장분을 전수 문맥 확인 (②만이 미지의 오타를 잡는다)
③ 스캔 범위(ITEM 수·필드 목록·방법)를 notes_for_main 에 명시 — 미검출을 부재로 읽지 않도록
```
- 실제 검출: **D005 `CMP-005` "보람다"(보낸다)** · **D005 `API-008` "남린다"(남긴다)**. D006 은 252 문자열 전수 스캔 → 0건
- ⚠ `API-008` 은 형제 API 들이 이미 같은 유형을 정정한 스윕에서 **혼자 누락**됐다 → 과거 정정 작업이 전수가 아니었음

### diagram 차원
- 메인이 `list_diagram_coverage(project_id, domain_id=...)` 를 **대신 호출해 결과를 주입할 것** — **auditor 도구 목록에 이 도구가 없다**
- 도구가 DIAG-001/002/003/004 를 확정해 주므로 auditor 는 **`DIAG-005` 본문 stale** 에 집중시킬 것
- **부속 배열을 따로 열게 지시** (위 cascade 패턴)

### 전 프로젝트 `list_diagram_coverage` 실측 (재호출 불필요)
```
DOMAIN-009  0%   missing=[DFEAT-037, DFEAT-038]
DOMAIN-010  63%  missing=[DFEAT-050, DFEAT-051, DFEAT-052]
DOMAIN-013  67%  missing=[DFEAT-053]
DOMAIN-005  80%  missing=[DFEAT-049]
DOMAIN-001/003/004/006/007/011/012/014/015/016  100%
deprecated 참조 0건 · dangling 0건 (전 프로젝트)
depicts 공란 13건: CMP-001~009 전부 · CTX-001 · CNT-001 · CDIAG-014 · CDIAG-015
```

## 6. 공통 입력 (재수집 불필요)

- **ADR**: 활성 40건 / retired 5건 — **superseded: 005→020, 011→014, 016→042, 017→021, 028→044**
- **RFP**: 7건 — RFP-001 SFR-06 / RFP-002 SFR-07 / **RFP-003 SFR-08(저작도구 고도화)** / RFP-004 SFR-09 / RFP-005 SFR-11 / RFP-006 SFR-16 / RFP-007 SFR-17
- **deprecated 도메인**: DOMAIN-002(프로젝트 관리) · DOMAIN-008(생성형 AI 연동) — **둘 다 14개 감사 대상 밖이라 아무도 안 보는 사각**
- **REQ**: 활성 22건, 전량 `derived_from_rfp` 보유. **`acceptance_criteria` 는 전건 빈 배열**(RQ-005 축, 프로젝트 전반)
- **TEST**: 5건 전부 `approved`·`kind=integration`. **`kind=system` 0건**. **5건 전부 `related_domains`·`verifies_requirements`·`verifies_nfrs` 공란** → **프로젝트 레벨 이슈, 도메인마다 중복 보고됨 → 최종 dedupe 대상**

## 7. 미해결 인계 항목

### 7-1. 도메인 간 인계 (다음 감사에서 반드시 확인)
- **→ DOMAIN-016(관제 통지)**: **`AC-009` 가 폐기된 `TASK_MODIFIED` 페이로드를 검증** 중 — 구 *"마지막 수정 일시 + 변경 프레임 목록(`SRC_SN`+변경종류) + 요약 카운트 + 요청 ID"*. **현행은 `job_id` + `changed_items{images,jsons}` + `ver_expln` 3필드**. 또 *"검수 완료 영상의 비식별 신고 → `TASK_MODIFIED` 발행"* 분기도 폐기(승인 이력 영상은 신고 접수 자체가 412)
- **→ DOMAIN-016**: `UC-009.main_flow[4]` 가 같은 구 페이로드 서술 보유 → TEST 와 세트로 정정 필요
- **→ DOMAIN-003**: `STATE-001.description` 말미가 구 파이프라인 순서("마킹(원본) 트리거→VLM→비식별") 서술
- **→ DOMAIN-010**: `API-066`·`API-067`(메타 조회·수정)이 DOMAIN-010 소유인데 VLM 메타 서브시스템의 나머지는 D004·D005 에 흩어져 있음

### 7-2. auditor 간 상충 (메인이 판정해야 함)
| 사안 | schema 차원 | links 차원 | 상태 |
|---|---|---|---|
| `API-055`·`API-056` 의 `brownfield.diff_summary` 가 deprecated `ERD-003`·폐기 테이블 `LS_PJT_JOB_STATS` 인용 | **정정 대상**(P1) | **청소 대상 아님** — `brownfield` 는 legacy provenance 필드이고 "1차"를 명시해 의미상 정확 | **D006 policy 차원에 판정 위임함 — 그 결과를 확인할 것** |

### 7-3. 이미 판정된 오탐 (재보고 금지)
- `SEQ-006`(외곽 밀착)은 **폐기 '자석 올가미' 축이 아니다** — 본문이 폐기를 명시하고 VOS 분할 축임을 선언
- `DFEAT-022`('2차 검수')·`ERD-002` 는 **이미 deprecated** 이고 backward 0건 — 활성 위반 아님
- `DFEAT-045`(타 도메인)의 `AC-006` verifies 는 **의도된 링크**
- `ERD-003` 테이블에 소유 DFEAT 없는 것은 **의도적 폐기 보존분**이라 정상
- `PRVC_TYPE_CD` 컬럼·`privacyTypeCd` 필드는 **존치**(화면 노출만 폐기) — 제거 요구를 갭으로 올리지 말 것
- 정렬 키 응답 코드가 엔드포인트별로 다른 것(`board*` 400 / `reviews*` 200)은 **의도된 차등** — 통일 제안 금지

## 8. 운영 주의

- **동시 실행은 3~4건**. 5건 동시에서 **전건 `Agent stalled 600s` 실패**가 1회 발생했다(D005 배치 A). 3건으로 낮춰 재시도하니 전부 성공
- **실패는 인프라 이슈이지 프롬프트 문제가 아니다** — 같은 프롬프트가 재시도에서 정상 완주
- 차원 1건당 **약 24만 토큰 / 5~8분**. 도메인 1개(10차원) ≈ **250만 토큰 / 25~35분**
- 도메인 완료 시마다 **즉시 `raw/DOMAIN-NNN.md` 확정** — 중단돼도 그때까지가 남는다

---

# 재개 세션 추가 학습 (2026-08-14 이어서 — D006 완료 · D007 진행)

## ★★ 프롬프트 주입 전제 정정 — "Export 는 범위 외"는 폐기됐다

D007 policy 차원이 실측: **`ADR-005`(Export·데이터마트 범위 외)는 `ADR-020` 으로 superseded** 됐고,
ADR-020 은 *"export 산출을 저작도구 범위로 포함 + 승인 후 7개 수정경로까지 전량 재생성 확대(채택)"* 이다.
**남은 범위 외는 데이터마트 구축·검색·다운로드뿐이다.**

⚠ **후속 도메인 프롬프트의 domain_context 에 "학습데이터셋 Export 는 저작도구 범위 밖"을 확정 정책으로 넣지 말 것.**
(메인이 D007 prompt 에 그렇게 주입했고, auditor 가 이를 근거로 `DFEAT-031` deprecated 를 "정합"이라 판정할 뻔했다.)
`superseded` 목록(005→020, 011→014, 016→042, 017→021, 028→044)은 **인용 여부 검사에 반드시 쓰라** — 실제로 도메인 최상위 ITEM 이 retired ADR 을 근거로 들고 있었다.

## 새로 확인된 반복 패턴 (D007 에서 신규 검출 — 다음 도메인 프롬프트에 주입할 것)

1. **`stale=false` 인데 본문이 폐기 모델** — D007 P0 4건이 전부 이 유형이다(`API-062`·`API-063`·`DFEAT-030`).
   **플래그를 신뢰하지 말라는 지침이 이 라운드 최대 소득**이다. 계속 강조할 것.
2. **최상위 description 은 정정됐는데 부속 배열만 뒤처짐** — `ERD-011.tables[].columns[]`·`CDIAG-010.classes[]`·`CMP-007.components[]`.
   **같은 ITEM 안에서 상위와 하위가 갈리는** 형태라 상위만 읽으면 놓친다. **부속 배열을 따로 열게 지시할 것.**
3. **ERD 가 공표한 것이 실물과 다른 3종** — ①없는 컬럼을 "잔존"이라 공표 ②실제 소유 테이블이 ERD 에 통째로 없음 ③참조 대상 테이블 오기.
   ⇒ schema 차원에 **"ERD ↔ `V1__baseline.sql` 실물 대조"** 를 명시적으로 지시할 것(D007 P0 3건이 전부 여기서 나왔다).
4. **모델 수준 폐기 잔재** — `CDIAG-010` 의 aggregate root 가 폐기된 `accept()/reject()` 메서드 보유.
5. **`brownfield.status=modified` 인데 `legacy_source` 없음** 9건 / **`decided_by` 누락·주제 불일치** 다수.
6. **한글 손상은 계속 나온다** — D007 에서 2건(`API-179` "그 개을"→갭을, `API-165` "비콜간"→비껴간).
   D005 2건에 이어 재현이므로 **content 차원의 ①②(알려진 오타 grep + 희귀 음절 분석)를 매 도메인 강제**할 것.

## 도메인 경계 재배치 후보 (사용자 결정 필요 — 최종 REPORT 에 모을 것)

| 대상 | 현재 | 권고 | 근거 요지 |
|---|---|---|---|
| `API-092` POST /v1/videos/{rawSn}/resolution | DOMAIN-003 | **DOMAIN-007** | 저장모델·응답 필드가 전부 증강 모델 / 같은 리소스 GET(API-179)·백필(API-180)은 이미 D007 / UC-003·SCREEN-022·CMP-007 전부 D007. path prefix 는 귀속 기준 아님. ⚠ `status=approved` 라 승인 필요 |
| `UC-010` 증강 영상 활용 여부 검수 | DOMAIN-005 | **DOMAIN-007** | 링크 누락이 아니라 실제로 D005 귀속. 검수 대상 데이터(`LS_DATA_AUG_RVW`)·등재 게이트·폐기 배치가 전부 D007 소유 / 흐름 구성물(SCREEN-023·API-062/063/190·SEQ-011·TEST-003) 전부 D007. **REVIEWER 가 행위자라는 사실만으로 귀속이 정해지지 않는다** |

## 운영 실측 갱신

- 차원 1건당 **3~15분** 편차가 크다(D007 schema 는 887초 / 69 tool calls). 토큰은 21만~34만.
- **동시 3건 유지 중 실패 0건.** coverage 완료 후 빈 슬롯을 다음 차원으로 즉시 채우는 **파이프라인 방식**이 도메인 단위 배치보다 빠르다.
- 도메인 완료를 기다리지 말고 **차원 결과가 오는 즉시 `raw/DOMAIN-NNN.md` 에 append** — 중단 내성 확보.

## ★★★ 한글 손상 검사 방법론 정정 (D007 content 가 확립 — 이후 전 도메인 필수)

**기존 ①②만으로는 못 잡는 손상이 있다.** D007 실측: 실제 손상 2건을 **①도 ②도 둘 다 놓쳤다.**
- ① 알려진 오타 15종 grep → 유효 0건 (오탐 1건: *"표식이 찍힌"* 은 정상 어휘)
- ② 희귀 음절 빈도 분석(2회 이하 전수) → 유효 0건
- **사각의 정체**: 손상 *결과* 음절이 **고빈도**면 희귀도 필터를 통과한다. `갭→개`·`비껴간→비콜간` 이 그 경우다.

**따라서 content 차원 프롬프트에 아래 (a)(b)를 반드시 추가한다:**
```
(a) 1음절 어간 + 목적격 조사(을/를) 패턴 스캔 — 정당한 단음절 명사는 whitelist 로 제외
(b) 낱말 고립도 분석 — 단발 출현 + 동족 어형(활용형·파생어) 0건인 낱말을 문맥 확인
    ★ change_summary 에 원래 낱말이 보존돼 있는 경우가 많아 대조 근거가 된다(실제로 "갭" 이 거기 있었다)
```
누적 검출: D005 2건(`보람다`·`남린다`) · D006 0건 · **D007 2건**(`개을`·`비콜간`).

## 추가 반복 패턴 (D007 content·diagram 신규)

7. **과거 오염 제거 스윕이 최상위 필드만 훑는다** — `API-165.change_summary` 가 *"내부 클래스명을 걷어낸다"* 고 **선언했는데 중첩 필드엔 그대로**. D001 `API-006`·D004 `API-123` 과 **동일 축의 3번째 재발**. ⇒ content 프롬프트에 **중첩 배열 필수 검사**를 계속 강제.
8. **`[헤더] 설명` 구조의 형제 API 들 사이에 한 줄짜리가 섞여 있다** — 가장 복잡한 계약이 오히려 비어 있는 역전(API-061: 페이징 2축·resultState 8종인데 28자).
9. **콜백 시퀀스의 HMAC 잔재** — `ADR-031` 이 VLM·증강 콜백을 **함께** 무서명으로 전환했으므로 **D004·D005(완료분) 소급 + D016 확인 필요**.
10. **`components[].name` vs 산문 클래스명** — 전자는 사양 어휘라 오염 아님. **경로 기반으로 갈라야** 오탐이 안 난다(content 프롬프트에 이 경계를 명시할 것).

## ★★★ 주입 전제 오류 2건 — 메인(PM)이 준 domain_context 가 ITEM 보다 낡았던 사례

**패턴**: 메인이 프로젝트 `CLAUDE.md` 에서 확정 정책을 뽑아 프롬프트에 넣는데, **그 서술 자체가 뒤처진 경우**가 있다.
auditor 가 ITEM 실측으로 이를 뒤집었고 **둘 다 "ITEM 이 맞고 CLAUDE.md 가 낡았다"** 로 판정됐다.

| # | 도메인 | 주입한 전제 | 실제 |
|---|---|---|---|
| 1 | D007 | *"학습데이터셋 Export 는 저작도구 범위 밖"* | **`ADR-005` 는 `ADR-020` 으로 superseded** — export 산출은 저작도구 범위. 남은 범위 외는 데이터마트 구축·검색·다운로드뿐 |
| 2 | D010 | *"버전 1건 클릭 = 작업본 비교 / 2건 체크 = 두 버전 비교"*(2026-08-05) | **2026-08-11 사용자 확정으로 그 화면 동선 폐기** — *"되돌리기는 고른 버전을 불러온 뒤 저장으로 확정"*. `SCREEN-010` v34: *"두 버전을 골라 서로 비교하는 방식은 두지 않는다"*. ⚠ **백엔드 축(롤백 시맨틱·R7·400·`/diff` 장애격리)은 그대로 유효** |

**⇒ 앞으로 프롬프트를 쓸 때**
- 확정 정책을 주입하되 **"이 서술이 ITEM 보다 낡았을 수 있으니, 어긋나면 어느 쪽이 최신인지 날짜·리비전으로 판정하라"** 를 함께 넣는다.
- auditor 가 역전을 발견하면 **gap 이 아니라 `notes_for_main` 에 보고**하게 한다(D010 policy 가 정확히 그렇게 했다).
- **최종 REPORT 에 "CLAUDE.md 갱신 필요" 별도 섹션**을 둔다 — 이건 LogiCraft 갭이 아니라 **프로젝트 문서 갭**이다.

## D010 에서 새로 확인된 검사 사각

- ★**deprecated ITEM 은 REST `kit-export` 에서 빠진다** — `API-033`·`DFEAT-013`·`DFEAT-040`·`ERD-008` 이 배치 모집단에서 제외돼 **개별 `get_item` 으로만** 확인됐다. D009 content 도 같은 이유로 `ERD-006` 을 제외했다. **`kit-export` 로 모집단을 만드는 모든 검사(content 오염·한글 스캔 포함)의 구조적 사각.**
- ★**`stale` 플래그의 검출력이 0 인 도메인이 연속 2건**(D009·D010). D010 은 `ERD-019` 가 `last_updated 2026-08-07` 인데 **V180·V181·V183(08-11) 이후 미갱신**이고 **플래그는 `false`** 다 → **P0 3건이 플래그로는 안 잡힌다.**
- ★**`code_module` 의 SD ID 오인용이 D009 4건·D010 4건으로 도메인 간 반복 확정** — 공통 구조는 **SD↔MOD forward 링크 부재로 본문 인용이 유일 추적 수단**.

## ★★ 스코프 함정 추가 — NFR 은 `domain_id` 필터로 귀속 판정 금지 (D010 실측)

`list_items(type=nfr, domain_id=DOMAIN-NNN)` → **0건을 반환한다.** NFR 의 도메인 귀속은 **`applies_to_domains` 배열**로만 판정 가능하다.
⇒ **`domain_id` 미보유 타입 목록에 `nfr` 을 추가**한다: `use_case`·`acceptance`·`requirement`·`test_scenario`·`diagram_sequence`·**`nfr`**.
(D010 auditor 가 *"다른 도메인 auditor 도 같은 함정에 빠질 수 있음"* 이라 경고했다. 남은 도메인 프롬프트에 반드시 넣을 것.)

## 완료 도메인 소급 확인 항목 (D010 test_scenario 가 발견)

| 대상 | 내용 | 심각도 |
|---|---|---|
| **D003(완료)** | `TEST-001.exercises_screens=[SCREEN-007]` 인데 **`SCREEN-007` 은 deprecated**("[폐기] 영상 목록 화면") — **TST-004 급 P0** 인데 그 도메인 감사에서 미검출 | P0 후보 |
| **D004/D005(완료)** | `TEST-002.steps[2]` 가 `META_KEY='activity'` 를 적재·검수큐 진입 — **확정 metaKey 규격(`vlm.description`·`vlm.accuracy`·레거시 구간 키·`video.*`) 밖**이고 검수큐는 `vlm.description` 1건만 | 폐기 모델 가능성 |
| **D007(완료)** | ADR-031 **HMAC 잔재 스윕** — VLM 콜백도 함께 무서명 전환됐으므로 D004·D005 소급 + D016 확인 | 기록됨 |
| **전 도메인** | `TEST-002`·`004`·`005` 가 **동일 문구("바꿀 내용이 없어 내용 변경 없이 확인만 기록")로 stale 해소**됐는데 **D010 실측상 그 판정이 오판이었다** — **"무변경 확인 리비전"의 신뢰도 재점검 필요** | 방법론 |

## ★★★ 한글 손상 검사 — **④b 절단-빈도비 추가** (D010 이 확립. ①②③④ 가 전부 놓친 손상을 잡았다)

D010 실측: `바운딩박` ×3 ⊂ `바운딩박스` ×21 (ratio 7) — **①②③④ 네 방법이 모두 통과시켰다.**
- ④(낱말 고립도)가 놓친 이유: **손상어가 3회 출현해 "단발 출현" 필터에 안 걸렸다.**
- ②(희귀 음절)가 놓친 이유: **손상 결과 음절 `박`이 고빈도.**
- **손상 위치 하나가 `ubiquitous_language[0].term` — 도메인 용어사전 항목명 자체였다.**

**⇒ content 차원 프롬프트에 ④b 를 추가한다:**
```
④b 절단-빈도비 검사 — 어떤 낱말 A 가 더 긴 낱말 B 의 접두(A ⊂ B)이고 A 의 출현 빈도가 B 보다 현저히 낮으면
    (예: ratio ≥ 3) A 를 절단 손상 후보로 보고 문맥 확인. ★"단발 출현" 조건을 걸지 말 것 — 3회 출현해도 손상이다.
```
누적 검출: D005 2 · D006 0 · D007 2 · D009 0 · **D010 1종 3곳**.

## D010 이 확인한 추가 사실

- **`kit-export` 의 deprecated 제외를 정량 재확인** — 2건 요청 시 `count=1`. 모집단 90 요청 → **86 반환**.
- **오염 전이 실증 1건** — deprecated `API-033` 의 커밋 해시 `28db2ee` 가 **활성 `UC-007` 본문에도** 있다. ⇒ **폐기 ITEM 의 서술이 활성으로 옮겨붙는 경로가 실재**한다.
- **규칙 문단 복제가 드리프트를 만든다** — 신고 412 문단이 **10 ITEM/12회**, 롤백 문단이 6 ITEM/7회 복제됐는데 **롤백 no-op 조항은 6 중 3 에만** 있다.

---

## ★★★ 스코프 밖 P0 발견 — `INTSPEC-003` (D011 policy auditor, 2026-08-14)

**소유 도메인이 D011 이 아니라 계상하지 않았으나, 최종 REPORT 에 반드시 별도 항목으로 올릴 것.**

`INTSPEC-003`(`status=draft`, **version 2.0.2**, `integration_point=INT-002`) — **제목만이 아니라 문서 전체가 폐기 규격**이다.
이 ITEM 은 **`INT-002` 의 규격 정본**이고 **§6 이 *"더 이상 스텁 아님 — 구현 완료"* 로 단언**한다.

| # | 본문 실측 | 확정 정책 |
|---|---|---|
| ① | §1 `Path: /v1/videovlm/**describe**` + §6 *"반영 완료"* | **`verify` 하나** — *"`describe` 는 벤더 규격서에 존재하나 **우리는 호출하지 않는다**"* |
| ② | §5 *"**event_type 미전송** … 필요하면 별도 verify 인터페이스(**본 범위 외**)"* | **정반대** — *"`event_type` 은 위탁을 막지 않는다. 미수신·미허용이어도 **항상 호출**"* |
| ③ | §1 *"배치 단계 **block 45s**"* | *"구 동기 호출(45s) **폐기** — `subscribe()` 논블로킹 제출"* |
| ④ | §4 *"`framerate` \| 1초 간격 프레임 계산 기준 **FPS**"* | *"**FPS 가 아니라 몇 프레임당 1장**(`FRME_INTV_NOCS`)"* — ★**CLAUDE.md 가 *"이름만 보고 FPS 로 단정하는 오해가 실제로 한 번 발생한 지점"* 이라고 못박은 바로 그 함정** |

**위험도**: 방치하면 **구현 키트가 폐기 엔드포인트 + 45s block + `event_type` 미전송으로 배선**된다.
⚠ **정정 시 §8(신고 구간 위탁 보류 `SKIPPED`, `ADR-024` 축)은 현행 유효 정책이라 보존**할 것.

> **함의**: `integration_spec` 타입은 **어느 도메인 감사에서도 모집단에 들어오지 않았다**(도메인 귀속 축이 아니라 `INT-*` 귀속). **`integration_spec` 전수는 이번 감사의 검사 사각**이며, 최종 REPORT 의 「검사 사각」 절에 올린다.

---

## ★★★ 주입 전제 오류 — 4건으로 늘었다 (2026-08-14, D011)

| # | 도메인 | 내가 주입한 전제 | 실제 |
|---|---|---|---|
| 1 | D007 | *"Export 는 저작도구 범위 밖"*(`ADR-005`) | **`ADR-020` 이 supersede** — 범위 안으로 복귀 |
| 2 | D010 | *"버전 1건 클릭=작업본 비교 / 2건=두 버전 비교"* | **2026-08-11 사용자 확정으로 폐기**(`UC-008` v8·`SCREEN-010` v34) |
| **3** | **D011** | *"`REQ-019`·`REQ-026` 도 마킹을 명시하지 않는다"* | **`REQ-019` 는 명시한다.** 마킹 호명 REQ 는 **4건**(`REQ-012`·`014`·`019`·`021`) |
| **4** | **D011** | *"`RFP-003`(SFR-08) 이 유력 후보"* | **추적표는 `RFP-005`+`RFP-007` 로 지정.** `RFP-003` 에는 마킹 bullet 없음 |

### ★★ 3번은 성격이 다르다 — **auditor 보고를 검증 없이 재주입했다**
1·2·4 는 **CLAUDE.md/내 판단**이 출처지만, **3 은 acceptance auditor 의 보고**(*"활성 REQ 22건에 마킹 전담 REQ 가 없다"*)를 내가 그대로 다음 프롬프트에 넘긴 것이다.
⇒ **규칙: 한 auditor 의 "없다" 판정을 다음 차원 프롬프트의 전제로 넘기지 말 것.** 넘기더라도 *"선행 보고이며 재확인 대상"* 으로 명시하고 **"직접 재확인하라"** 를 붙인다.
(다행히 requirement auditor 가 전수 통독으로 반증했다 — 이번엔 **프롬프트에 "제목만 보고 단정 금지 + 본문을 열어라"** 를 넣어 둔 것이 작동했다.)

### 4번의 함의 — **CLAUDE.md 갱신 후보**
CLAUDE.md 는 *"저작도구 잔존 책임(외부 VLM 시계열 호출 연동 등)은 **모두 SFR-08 에 흡수**"* 라고 적었으나,
**마킹 축에서는 그래프·추적표 어느 쪽에도 그 흡수가 반영돼 있지 않다**(실제 귀속은 SFR-11·SFR-17).
⇒ 최종 REPORT 의 **「프로젝트 CLAUDE.md 갱신 필요」** 절에 **3번째 항목**으로 올린다.

## ★★ 검사 사각 — `diagram_state` (2026-08-14, D011 diagram)

**`STATE-001` 은 내 프롬프트의 다이어그램 스코프(SEQ·CDIAG·CMP)에 없었다.** auditor 가 *"프롬프트 스코프에 없었으나 누락 방지 위해 project-level 조회"* 로 자발 포함해 **P0 2건**을 찾았다.
⇒ **D001~D010 열 도메인 전부 `diagram_state` 미개봉.**

- `STATE-001` 은 **프로젝트 전체에 1건**이고 `domain_id` 선언이 없어 **어느 도메인 필터에도 안 걸린다.**
- 내용은 `LS_RAW_DATA_STATUS` 축이라 **배치·검수·마킹 세 도메인이 공유**한다.
- v3 · **2026-06-02** 로 2개월 뒤처져 **현행을 `planned` 로, 폐기 순서를 `현재 코드`로** 적고 있다.

**남은 도메인 프롬프트에 `diagram_state` 를 명시 추가할 것.** 단 1건뿐이라 **D012~D016 에서 중복 보고될 수 있으니** 최종 dedupe 대상으로 표시.

> 같은 계열 사각: **`integration_spec`**(→ 위 `INTSPEC-003` 절). 둘 다 **도메인 귀속 축이 없는 타입**이라 도메인 단위 감사에서 구조적으로 빠진다. **타입 목록을 도메인이 아니라 "전역 타입"으로 한 번 훑는 별도 라운드가 필요**하다 — 최종 REPORT 권고에 포함.

## ★★★★ 드리프트의 메커니즘 — 일괄 "재확인, 바꿀 내용 없음" 스탬프 (2026-08-14, D012 stale)

**D001~D011 에서 계속 관측된 *"결정이 최근 손댄 ITEM 에만 도달한다"* 의 원인 쪽 설명이다.**

D012 에서 **2026-08-07 06:32~06:51(19분)** 사이 도메인 ITEM **17건**이 동일 정형문으로 스탬프됐다:
> *"연결 ITEM 갱신에 따른 재확인 — **바꿀 내용이 없어 내용 변경 없이 확인만 기록한다**"*

**그 판정이 최소 3건에서 사실과 달랐다** — `TEST-001` 은 전날 정정된 폐기 컬럼명을, `AC-016` 은 제거된 서비스 인용을 그대로 갖고 있었다.
⇒ **그 스탬프가 컷오프가 되어 이후(08-07 오후·08-10·08-11) 확정분이 도달하지 못했다.** "확인했다"는 기록이 드리프트를 덮었다.

### 남은 도메인·최종 REPORT 에서 할 것
1. **다른 도메인에도 같은 일괄 스탬프가 있는지** `change_summary` 동일 문자열로 확인(D001~D011 소급 포함).
2. **`last_updated` 가 그 스탬프 시각에 몰린 ITEM 은 "검증된 최신"이 아니라 "미검증 통과"로 취급**한다.
3. 최종 REPORT 권고: **무변경 확인은 `status`-only 콜로 처리**하고, `change_summary` 에는 **무엇을 어떤 정본과 비교했는지** 남긴다.

> 이것은 개별 ITEM 결함이 아니라 **작업 방식의 결함**이라, 최종 REPORT 에서 **별도 절(프로세스 권고)** 로 다룬다.

## ★★★ 교차 클러스터 — **활성 ADR 이 폐기 정책을 보유한다** (★5건, D013 에서 1건 추가)

| ADR | 상태 | 담고 있는 폐기 정책 | 발견 |
|---|---|---|---|
| **`ADR-008`** | `approved`·v1·06-01·supersede 없음 | 구 파이프라인 순서(마킹 선두) · 폐기 VLM 규격 · 미정의 `BATCH_QUEUED` | D011 stale·policy·diagram **3차원 독립 도달** |
| **`ADR-006`** | `approved`·v2·07-30 | PRVC/PSDO 게이팅 · 콜백 수신 · **재시도 큐 "유지한다"** | D012 links·stale·policy |
| **`ADR-022`** | `approved`·v1·07-29·**무갱신** | 차단 범위 6항목 미달 · 접수 3조건 미규정 · 해소 이벤트 "2종"(현행 3종) | D012 stale·policy |
| **`ADR-032`** | `approved`·**v3·08-07 갱신** | export 원천 3필드 `null`(**08-04 "되돌리지 말 것"으로 폐기**) | D012 policy |

### 왜 이게 최상위 클러스터인가
1. **ADR 은 `decided_by` 로 하위 ITEM 이 인용하는 결정 근거**다 — 하위만 고치면 **근거가 남아 되살아난다**.
2. **`stale` 플래그가 전부 `false`** — 자동 검출이 불가능하다.
3. ★**`ADR-032` 는 폐기 *이후* 에 갱신됐는데도 폐기본을 유지**했다 ⇒ **"갱신됐으니 최신"이 성립하지 않는다.**
4. ★**`ADR-006`·`ADR-032` 는 자기 본문에 모순을 스스로 기록**해 뒀다(*"원 정책은 유지한다"* / *"정면 충돌한다"*).

### 최종 REPORT 처리
- **수정 순서 의존 클러스터**로 묶어 **ADR 선행**을 명시.
- **남은 도메인(D013~D016)에서 ADR 본문을 반드시 열게** 프롬프트에 넣는다.
- ★**ADR 45건 중 36건이 아직 본문 미확인** ⇒ **ADR 전수 스윕을 별도 라운드 권고**로 올린다(`integration_spec`·`diagram_state` 전역 타입 라운드와 같은 성격).

## ★★★★ 한글 검사법 — **④d 추가 (2026-08-14, D012 content)**

**①②③④④b 5종이 전부 놓친 손상을 신규 ④d 가 잡았다.**
- 손상: **`SEQ-014.data.source`(mermaid)의 `감사로개만`** ← 정상 `감사 로그만`
- **④가 놓친 이유 = 접두 kin(`감사`)이 존재해 "고립도" 필터에서 걸러졌다** ← **구조적 사각**
- ②가 놓친 이유 = 손상 결과 음절 `개`·`만` 이 고빈도

```
★④d — kin 필터 제외분 재검사
   ④(낱말 고립도)가 "동족 어형 있음"으로 걸러낸 토큰 중
   freq == 1 이고 4음절 이상인 것을 전수 육안 확인한다.
   (D012 실측: 493건 → 손상 1건 검출)
```
⚠ **함께 시도한 4c(교차 ITEM 8-gram 편집거리 1)는 후보 378건 전부 자연 어미 변이라 신호 대 잡음이 낮다 — 채택하지 않는다.**

### 누적 검출 이력 (검사법 진화)
| 회차 | 손상 | 잡은 방법 |
|---|---|---|
| D005 | 2 | ①② |
| D007 | 2 | **③④ 신규 도입**(①②가 놓침) |
| D010 | 1종 3곳(`바운딩박`) | **④b 신규 도입**(①②③④가 놓침) |
| **D012** | 1종 1곳(`감사로개만`) | **★④d 신규 도입**(①②③④④b 가 놓침) |

⇒ **회차마다 새 사각이 드러나는 패턴.** 최종 REPORT 「검사 사각」에 *"한글 검사법은 아직 닫히지 않았다"* 를 명시하고, **남은 도메인 프롬프트에 ④d 를 포함**한다.


### ★ 5번째 — `ADR-026` (D013 policy, 2026-08-14) : **고립 ADR 이라 더 위험하다**

| ADR | 상태 | 폐기 정책 | 특이점 |
|---|---|---|---|
| **`ADR-026`** | `approved`·v1·07-29·supersede 없음 | **"포털 SAM2 경로"를 전제하고 그 인코딩을 규정** — 그 경로는 **08-03 제거·재구현 금지 확정** | ★**`references` 에 커밋 URL 1건뿐, 어떤 ITEM 과도 링크 없음** |

★★**이것이 stale 검출의 구조적 사각이다** — *"stale 판정 로직이 **링크 없는 고립 ADR** 을 놓친다"*.
`API-130`·`131` 은 deprecated 됐는데 **그 상위 정책 기록만 무표기 활성**으로 남아 *"포털 SAM2 를 비식별본으로 하면 승인됨"* 으로 읽힌다.
⚠**단 "AI 추론 전송은 비식별본만" 원칙 자체는 내부 경로에 유효 — 삭제 금지, supersede/표기 정정으로 처리.**

⇒ **ADR 전수 스윕 권고에 "고립 ADR(링크 0) 탐지"를 명시 항목으로 추가한다.**


## ★★★ 한글 검사법 — **② 를 생략하면 안 된다 (D013 실증)**

D013 에서 신규 손상 **`옳겨갈`**(정상 `옮겨갈`, `API-169`)이 **② 희귀음절 축으로만** 잡혔다.
①(목록 미등재) ③ ④ ④b ★④d ★신규 Hamming 축이 **전부 놓쳤다**.

**놓친 이유** — *"정상형 `옮겨갈`이 토큰 집합에 **0건**이라 치환쌍이 성립하지 않았고, **kin(`갈리면`)이 ④를 무력화**"*

### 검사법 현재 상태 (6축 + 1)
```
① 알려진 오타 grep — ★목록에 `옳겨` 추가할 것 (`옥겨`와 같은 정상형 `옮겨`의 다른 손상형)
② 희귀 음절(≤2회) 전수 문맥 확인  ← ★★생략 금지. D013 에서 유일 검출 경로였다
③ 1음절+목적격 (오탐 잦음 — D011 3/3, D012 9/9, D013 5/5 전부 오탐)
④ 낱말 고립도 (kin 존재 시 무력화)
④b 절단-빈도비
★④d ④가 kin 으로 걸러낸 것 중 freq==1 & 4음절+ 전수 (D012 유일 검출)
★신규: 어간 치환 검출기 — 동일 길이·Hamming=1·비어말·음절빈도≤3 (D013 에서 `흘름` 독립 재검출)
```
★**축마다 잡는 손상 유형이 다르다** — D012 는 ④d 만, D013 은 ② 만 잡았다. **전 축을 다 돌려야 한다.**
⚠**③ 은 3개 도메인 연속 전건 오탐** — 유지하되 문맥 확인 비용을 감안할 것.

## ★ 신규 오탐 등재 (남은 도메인 프롬프트에 추가)
- **`diagram_c4_component.depicts_dfeats` 미선언** — 표본 3건 확인, **전역 패턴** ⇒ 도메인 gap 금지, **프로젝트 차원 1건으로 통합**
- **`brownfield.diff_summary` 의 `Phase N`·`R16` 표식 + 컨트롤러#메서드** — D013 에서 **17건 일괄** 발견, *"등록 시 템플릿 문제"* 가능성 ⇒ **다른 도메인도 확인**

## ★★★ 주입 전제 오류 — **6건으로 늘었다** (D014 에서 1건 확정)

| # | 도메인 | 전제 | 실제 |
|---|---|---|---|
| 1 | D007 | Export 범위 밖 | `ADR-020` supersede |
| 2 | D010 | 버전 화면 동선 | 08-11 확정으로 폐기 |
| 3 | D011 | `REQ-019` 마킹 미명시 | **명시한다**(호명 REQ 4건) |
| 4 | D011 | RFP `SFR-08` 귀속 | 추적표는 **SFR-11+SFR-17** |
| 5 | D013 | 미제공 목록(좁음) | ITEM 이 **더 넓다**(08-06 확대) |
| **6** | **D014** | *"비식별 옵션은 **저작도구 전용 화면 없이** 외부 솔루션 화면에서 설정"* | ★**ITEM 3건이 "관리 화면 보유"로 일치**하고 **근거 ADR 에 그 취지 문장 없음** |

### 6번 상세 — **두 auditor 가 독립적으로 검증**
- **stale**: 인용 근거(`D012 requirement 추적표 주1`)에 **도달 실패**(`list_items(requirement, domain_id=DOMAIN-012)` → **0건**) + **반대 증거**(`DFEAT-042` 는 *"결과·이력 검토"* 이관만 말함)
- **policy**: **4중 근거로 "ITEM 이 현행" 판정** — 리비전·본문·구현 실측 명시·ADR 대조
⇒ ★**내가 D012 requirement 보고를 요약하며 범위를 넓게 옮겼을 가능성.** **최종 REPORT 판정 요청 + CLAUDE.md 갱신 후보.**

### ★ 패턴 — 전제 오류 6건 중 **4건이 "내가 좁게/넓게 옮긴 것"**
2·3·5·6 이 모두 **원문보다 범위가 어긋난** 유형이다. 1·4 만 진짜 supersede/오귀속.
⇒ **남은 도메인에서 정책을 주입할 때 원문 표현을 그대로 인용**하고, **내 요약을 전제로 쓰지 말 것.**

---

## ★★★ 스킬 룰 파일이 다른 프로젝트 것이다 — D015 policy 에서 확정 (2026-08-15)

D015 policy 감사자가 룰 `POL-2 ~ POL-7` **6개를 전부 SKIP** 했다. 사유가 "해당 없음"이 아니라 **룰이 지목한 ADR 번호가 이 프로젝트와 불일치**한다.

| 룰이 지목 | 룰이 말하는 내용 | 이 프로젝트의 실제 |
|---|---|---|
| `ADR-051` | 8대 이벤트 범위 | **존재하지 않음** (최대 ADR-045) |
| `ADR-045` | BFF 제거 | 증강 생성결과 축 vs 검수결정 축 분리 |
| `ADR-028` | 지자체 data scope | 증강 요청 dedup (superseded by ADR-044) |
| `ADR-027` | x-access-token 통일 | 재비식별 강제 재생성 + resolve fail-closed |
| `ADR-036` | outbound/inbound 통신 방향 | event_annotation 메타탭 수동편집 |

**감사자가 번호를 그대로 믿었다면 존재하지 않는 정책 위반 6건을 만들어 냈을 것이다.** 지금까지 도메인들의 policy 차원이 이 룰을 어떻게 처리했는지 **소급 확인 대상**이며, 최종 리포트 「검사 사각」에 올린다.

## ★★★ 주입 전제 오류 — 8건으로 늘었다

8번째: **`API-187.implementation.status='planned'`** 를 근거로 미구현으로 다뤘으나, schema 감사자가 **실물 구현을 찾았다**(`AssignmentController.eventTypes` + `AssignmentService.listEventTypeOptions`, 계약도 `EventTypeOptionsResponse` 와 정확히 일치).

→ **"`implementation.status: planned` = 미구현"으로 읽지 마라.** 이 메타는 낡을 수 있다. D015 에서 `ERD-014`·`CDIAG-007` 도 planned 0% 인데 본문은 산출물 실재를 단언한다.

## ★★ 필터 함정에 감사자가 실제로 빠졌다 — 0건 보고를 전제로 넘기지 마라

D015 **stale 감사자**가 *"도메인에 use_case·acceptance·test_scenario·nfr·diagram_sequence·diagram_state 가 0건이다(list_items domain_id 필터 실측)"* 라고 보고했다. **거짓이다** — links 감사가 같은 도메인에서 `UC-029`(alternate_flows 보유)를 확인했다.

이건 착수 시부터 알고 있던 함정인데도 **차원 감사자가 자기 차원 밖 타입을 필터로 확인하고 0건으로 단정**했다. → 후속 acceptance·requirement 프롬프트에 이 오보고를 **명시적으로 부인**해 넣었다.

**규칙**: 어느 감사자가 보고한 "0건"이든 **그 감사자의 소관 차원 밖이면 전제로 쓰지 않는다.**

## ★★ D015 는 D014 와 정반대 형태 — 자산은 있는데 연결이 끊겼다

- **D014**: 확정 정책 3건을 담은 **ADR 이 아예 0건**
- **D015**: 10개 정책 중 대응 ADR **8건 보유**, 그런데 **실제 인용은 1건뿐**(`ADR-001`). `ADR-038`·`ADR-045`·`ADR-003` 이 **전부 완전 고립**(forward 0 / backward 0)

→ 최종 리포트에서 "ADR 부재"와 "ADR 고립"을 **다른 결함 유형으로 갈라야 한다.** 전자는 신규 등록, 후자는 배선이 처방이다.
→ 재도입 위험은 **ADR 이 아예 없는 2건**(재배정 이력 단일화 · **이벤트유형 그룹 축**)에 집중되며, 그룹 축은 **ADR 부재 + 본문 위반 4건이 겹친 유일한 축**이다.

## ★★ `source_hash` 오라클의 사각 — `sections` 만 덮는다

D015 `SCREEN-012` 미러가 ITEM(08-14 v33)보다 하루 낡아 **폐기 경로 `/tasks` 를 계속 게시**한다. v33 의 change_summary 가 *"이대로 만들면 도달할 수 없는 주소가 되는 것을 바로잡았다"* 고 한 바로 그 값이다.

**`source_hash` 는 `sections` 의 sha256 이라 `route`·`purpose` 축 stale 을 구조적으로 못 잡는다.** 미러 층 판정에 `source_hash` 만 쓰면 이 유형이 통과한다 — 최종 리포트 「검사 사각」에 추가.

## ★ 신규 오탐 등재

- `EVENT_APPROVE` 는 **자바 상수명**이고 DB 저장값은 `APPROVE` 다. `ERD-014.code_values` 가 `APPROVE` 인 것은 **정상**이다(내가 상수명 기준으로 지시해 감사자가 정정했다).
- ERD 자기등록 표준용어 6건(출처 = `KLID-저작도구 ERD-014`)은 **독립 근거로 채택 불가** — schema 감사자가 `출처` 컬럼을 확인해 배제했다. 이 절차를 다음 도메인에도 유지할 것.

---

## ★★★★ 감사자의 "구조상 불가능" 주장을 대조군 없이 받지 마라 (2026-08-15)

D015 acceptance 감사자가 *"`use_case`·`acceptance` 는 스키마상 도메인 필드가 없다(`allowed_fields` 부재, `hints.domain_required=false`) — 필터 0건은 누락이 아니라 필연이며 **D012·D013·D014 의 같은 판정은 전부 오탐**"* 이라고 보고했다.

**기각했다. 반증이 이미 이 감사 안에 있었다:**

1. **D014 감사자가 대조군을 실측**했다 — *"★전역 관례 아님 대조 확인(`DOMAIN-010` 3건·`DOMAIN-005` 1건 보유)"*. 구조상 불가능하면 보유 건이 0이어야 한다.
2. **D013 감사자가 이미 정확히 짚었다** — *"`get_item_schema` 로 확인: **`data` 패치가 아니라 `domain_id` 설정**이어야 한다"*.
3. **D015 requirement 감사자가 독립 확인** — `UC-020`·`UC-029` 실재하며 *"둘 다 `belongs_to_domain` 링크가 없어 `domain_id` 필터에는 잡히지 않는다"*.

⇒ `allowed_fields` 는 **`data` 페이로드의 허용 키**이고 `domain_id` 는 **`data` 밖 ITEM 레벨 필드**다. 감사자는 **다른 층을 보고 부재를 결론**했다 — 이 감사가 내내 경고한 **"부분 read 의 빈 결과 ≠ 필드 부재"** 와 같은 함정이다.

**✅ D012·D013·D014·D015 의 `belongs_to_domain` 보고는 유효하다. 소급 재검토 불필요.**
**단 처방은 `data` 패치가 아니라 ITEM 레벨 `domain_id` 설정**이라는 구분을 최종 리포트에 명시할 것.

**표준 규칙으로 승격**: 감사자가 *"스키마상 불가능/필연"* 이라고 보고하면 **반드시 대조군**(그 필드를 실제로 가진 ITEM 이 하나라도 있는가)을 확인한 뒤 받는다. 하나라도 있으면 그 주장은 기각이다.

⚠ 단 **스키마 자기모순 자체는 사실**이다 — `allowed_fields` 에 없는데 `link_types_from` 에는 `belongs_to_domain(to_type=domain)` 이 선언돼 있어 **스키마만 읽으면 정확히 이 오판을 하게 된다**. 「검사 사각」 등재.

## ★★★ 추적표는 존재하지 않는다 — 내가 D011·D014 에서 인용한 "추적표"의 정체 확인 필요

D015 requirement 감사자가 전수 확인했다: 추적표(traceability matrix)에 해당하는 **별도 ITEM·note 가 0건**이고(`list_notes` 5건은 전부 플랫폼 system-feedback), 매트릭스 성격의 필드는 **`RFP-003.related_requirements`(6건) 하나뿐**이며 나머지 RFP 6건에는 그 필드 자체가 없다.

⚠ 그런데 나는 **D011 에서 *"추적표는 `SFR-11`+`SFR-17`"*, D014 에서 *"`REQ-008` 은 직접 추적표 매핑"*** 이라고 전제를 정정한 바 있다. **그 "추적표"가 무엇이었는지 최종 리포트 작성 전 반드시 재확인**할 것 — 존재하지 않는 근거로 전제를 두 번 정정했을 가능성이 있다.

## ★★ 완료 도메인 소급 항목 추가 — D012 `REQ-014`

`REQ-014.constraints` 에 *"검수 완료(승인) 영상을 신고하면 상위 시스템에 수정 통지를 발행하고, 해소 시 산출물을 재산출해 재통지한다"* 가 남아 있다. 확정 정책은 **2026-08-10 이후 "승인 이력이 있는 영상은 신고 접수 자체를 412 로 거부"** 라 **도달 불가 경로**다. (D015 requirement 감사자가 도메인 밖 발견으로 라우팅)

## ★ NFR 귀속 — 이번엔 본문이 대상을 명시한 사례

`NFR-011.description` 이 *"라벨링·검수·마킹·**목록** 등 저작도구 전 화면"* 이라고 대상을 열거하는데 `applies_to_domains` 는 `[010, 005, 011, 006]` 로 **"목록"에 해당하는 DOMAIN-015 만 빠졌다.** D014 의 `NFR-008` 귀속 문제와 달리 **본문이 스스로 대상을 지목**하고 있어 판정이 명확하다 → `auto_fixable`.

활성 NFR **14건 전수** `applies_to_domains` 실측 결과 DOMAIN-015 귀속은 **0건**이고, `NFR-014`·`NFR-017`·`NFR-021` 은 `applies_to_domains` 가 **빈 배열**이다(전역 NFR 모델링 관례 확인 필요 — 판정 요청 항목).

---

## ★★★★ 한글 검사는 이제 **8축**이다 — ⑧ 다-종결 어절 census 상설 편입 (2026-08-15)

D015 content 감사자가 **자기가 만든 보조축 ⑧로 `주은다`(→죽는다)를 잡았다. 규정된 7축 중 어느 것도 잡지 못한 건이다.**

**⑧ = 다-종결 어절 전수 census(해당 도메인 95종) + 빈도.** 한국어 서술문이 `-다` 로 끝나므로 종결형 어절을 전수로 세면 비정상 활용이 빈도 1로 드러난다.

축별 실제 검출(D015, 32,671자): **①0 ②1 ③0 ④2 ④b0 ④d1 ⑦0 ⑧2**

| 손상 | 단독 검출 축 | 다른 축이 놓친 이유 |
|---|---|---|
| `캐프처` | ② | 3음절 → ④ 미달 · `캡처` 와 길이 달라 ⑦ 미달 |
| `고정도던` | ④ | 음절 전부 상용 → ② 미달 |
| `검수자에는` | ④d | `검수자에게는`·`검수자에` 와 접두/접미 겹침 → ④ 탈락 |
| **`주은다`** | **⑧** | 주5·은97·다242 상용 → ② 미달 / 3음절 → ④ 미달 / `죽는다` 와 **거리 2** → ⑦ 미달 |

**부수 확정 — ⑦Hamming 은 이 규모에서 사실상 무력**하다(57쌍 전량 오탐, 손상 5건 중 0건). 정상형 출현이 0~2회라 치환쌍이 성립하지 않기 때문. ①도 신규 0건이었다(유일 매치는 `시스템` 부분일치 오탐).

→ **D016 프롬프트부터 ⑧을 필수 축으로 넣는다.** 「검사 사각」에 *"⑦의 유효 조건은 정상형이 충분히 출현할 때뿐"* 을 명시.

## ★★★ 주입 전제 오류 — 이번엔 진짜 9건째. **같은 실수를 규칙까지 세우고 재발시켰다**

내가 *"`SD-003` 의 `data` 가 `{status:"draft"}` 뿐"* 이라고 주입했으나 **실측은 키 8개 / JSON 679자**이고 description 97자에 실질 내용이 있으며 renders 도 실재한다. **`data` 최상위의 `status:"draft"` 키를 "data 가 그것뿐"으로 오독**한 stale 감사자의 보고를 **내가 검증 없이 다음 차원 프롬프트에 전제로 넘긴 것**이다.

⚠ **D011 에서 똑같은 실수(감사자의 "부재" 보고를 무검증 전달)를 하고 규칙까지 세웠는데 재발했다.** 규칙만으로는 안 막힌다 → **차원 프롬프트에 "이미 확정된 사실"로 넣는 항목은 원문 인용이 있는 것만** 넣고, 요약·해석은 넣지 않는다.

정정: `D015-STL-012` 의 SD-003 부분 철회 · `API-072` description 은 21자가 아니라 **24자**.

## ★★★ `TEST-001~005 status 불일치` 소급 항목 — 실체 확정

**envelope `status:"approved"` ↔ `data.status:"draft"`**(5건 전부). **`UC-029` 도 같은 패턴**이다.

⚠ D012 감사자의 *"`TEST-001` 이 옳고 `UC-011` 이 낡았다"* 는 **본문 내용 축** 판정이고 이건 **필드 축**이다 — **합치지 말 것.** 정본은 envelope 으로 보이나(리비전 3~7 갱신 + change_summary 가 승인 유지 전제) **확정은 사용자 판단** → 판정 요청 항목.

**프로젝트 전역 1건**으로 세고 도메인별로 곱하지 말 것.

## ★★★★ 두 검증 체계 갈림 — **5연속**. 도메인별 보고를 그만두고 단일 결정 사안으로 승격

D011·D012·D013·D014·D015 전부. D015 에서는 저장소 쪽이 압도적으로 풍부했다 — `TC-ASSIGN` 38건 이상 + `TC-SORT-001~003` 이 **LogiCraft 에 없는 최신 정책**(V4 재배정 이력 단일화 · 이벤트유형 그룹 축 · strict/lenient 통일 금지 · IDOR CWE-639)을 이미 반영하고 있다.

→ 최종 리포트에서 **도메인별 P1 5건이 아니라 「검증 산출물 층을 LogiCraft 로 올릴 것인가」 판정 요청 1건**으로 묶는다.

## ★ 전역 미착수 축 — 도메인별로 곱하지 말 것 (누적 3종)

1. **REQ `acceptance_criteria` 공란** — 본문을 연 REQ 12건 전부(D015 requirement 확인)
2. **`kind=system` TEST 0건** — 5건 전부 `integration`
3. **`verifies_requirements`·`verifies_nfrs` 공란** — 5/5 전부

---

## ★★★★★ 주입 전제 오류 10건째 — **이번엔 프로젝트 CLAUDE.md 자체가 의심된다** (2026-08-15)

내가 준 `TASK_COMPLETED` 페이로드 산문(= 프로젝트 CLAUDE.md 원문)과 **`INT-007` v5(2026-08-12, 도메인 최신) 실제 필드 목록이 집합 자체로 다르다.**

- **`INT-007` v5 required 9**: `job_id`·`event_type_cd`·`evnt_cls_cd`·`evnt_ctgry_cd`·**`lclgv_cd`**·**`lclgv_nm`**·`duration_sec`·`image_count`·`gen_ai_yn` + 선택 **`output_ver_no`**
- **CLAUDE.md 산문**: 이벤트 타입 + 작업 ID + 영상 메타(파일명·길이·채널) + **검수 완료 일시** + 프레임 개수 + **결과 요약 카운트** + **요청 ID** + `evnt_cls_cd`·`evnt_ctgry_cd`·`gen_ai_yn`

산문의 **`검수 완료 일시`·`결과 요약 카운트`·`요청 ID` 가 9필드에 하나도 없고**, 반대로 **`lclgv_cd`·`lclgv_nm`·`event_type_cd` 는 산문에 없다.** 즉 드리프트가 **"6→9" 가 아니라 필드 집합 불일치**다.

⇒ **최종 리포트 「프로젝트 CLAUDE.md 갱신 필요」 최상위 항목.**
⚠ **어느 쪽이 정본인지는 관제 규격 원문 대조가 선행**돼야 하며 이 감사로는 확정 불가 — **CLAUDE.md 를 고치라고 단정하지 말고 "둘이 다르다"를 보고**할 것.

## ★★★★ 플랫폼 한계 신규 확정 — EVT→EVT 체인이 materialize 되지 않는다

`EVT-003.consumes=[EVT-009]` · `EVT-009.triggers=[EVT-003]` · `EVT-009.consumes=[EVT-006]` **3건 선언 · 간선 0건.** 타 도메인 대조군 `EVT-001` 도 동일. ⇒ 구속 정책 「`EVT-006`→`EVT-009`→`EVT-003`」 직렬화 체인이 **선언으로만 존재하고 `analyze_impact` 로 전파되지 않는다.** 우리 ITEM 을 고쳐도 해소 안 됨 — **플랫폼 이슈 보고 후보**(`links.unresolved`·`brownfield.decided_by` 와 같은 계열).

★ 같은 계열 **`used_by_domains`(INT→DOMAIN)도 materialize 안 됨**. 반면 **`implements_domains`(MOD→DOMAIN)는 정상**. ⇒ **역방향 선언 필드가 축마다 동작이 달라 필드 하나만 보고 링크 부재를 결론내면 안 된다.**

## ★★★ 「검사 사각」 추가 — `documented_emitters`/`documented_consumers` 는 표시 전용

`domain_event` 스키마가 명시한다: *"`documented_emitters`/`documented_consumers` 는 표시 전용 — 링크 생성 안 함. 발행자 쪽 `triggers` 와 소비자 쪽 `consumes` 에 EVT id 를 적어야 그래프 링크가 만들어진다."*

⇒ EVT 쪽에만 적어두면 **본인은 선언했다고 보이는데 그래프엔 없다.** D016 에서 발송 게이트 `EVT-009` 가 정확히 이 상태였다.

## ★★ stale 플래그 무력화 — 8연속, 그리고 D016 에서 최악

D009~D015 **7연속**에 이어 D016 은 **역방향 8건**(`stale=false` + 본문 폐기 모델)이고 **P0 6건 중 플래그가 짚어준 것은 0건**이다. 더구나 `UC-009`·`TEST-004` 는 **플래그가 가리키는 축(SCREEN 참조)과 실제 손상 축(페이로드)이 다르다.**

⇒ 최종 리포트에서 **"`stale` 플래그를 결함 탐지에 쓰지 말 것"** 을 프로세스 권고로 명시.

## ★★ 일괄 스탬프 — D016 이 최대치이고 컷오프가 가장 선명하다

**12건 / 도메인 22건 중 55%**(2026-08-07 06:32~06:51, 19분). 덮어확정형 7건. **스탬프 이후 도달한 확정 3건이 전부 미반영.**

★ **반례 확보** — 같은 날 **05:49~06:11 라운드는 성공**했다(V174 뷰 출력명 6종 구 이름 잔존 **0건**). ⇒ **"08-07 오전이면 다 나쁘다"가 아니라 내용 정합 라운드와 스탬프 라운드의 결과가 정반대**다. 최종 리포트의 프로세스 권고를 이 대비로 쓸 것.

## ⚠ 작업 사고 1건 — `cd` 실패가 append 를 삼켰다

`cd raw && cat >> ...` 를 이미 `raw` 인 상태에서 실행해 `cd` 가 실패했고, `&&` 때문에 `cat` 이 안 돌았는데 **뒤이은 `echo done` 이 성공을 출력**했다. 재확인(`wc -l`)으로 발견해 재작성했다. → **append 후에는 반드시 `wc -l` 로 확인**하고, `cd` 를 명령에 붙이지 말 것.

## ★★★★★ 전제 오류 10건째 **해소** — 구현이 `INT-007` 편이다. CLAUDE.md 가 낡았다 (2026-08-15)

D016 schema 감사자가 **실물 DTO 를 열어 확정**했다:

- **`dto/TaskCompletedPayload.java` = record 10 컴포넌트**: `job_id`·`event_type_cd`·`evnt_cls_cd`·`evnt_ctgry_cd`·`lclgv_cd`·`lclgv_nm`·`duration_sec`·`image_count`·`gen_ai_yn`·`output_ver_no`(NON_NULL)
- **`dto/TaskModifiedPayload.java` = 4필드**: `job_id`·`changed_items`·`ver_expln`·**`output_ver_no`**

**⇒ 프로젝트 CLAUDE.md 의 통지 페이로드 서술이 폐기본이다. 확정 3건:**

1. CLAUDE.md 가 든 **`검수 완료 일시`·`결과 요약 카운트`** 는 페이로드에 **없다**(각 0건).
2. **`요청 ID` 도 바디에 없다** — `ControlNotifyService` 가 `UUID.randomUUID()` 로 만들어 **fallback 큐 `IDMP_KEY` 로만** 쓴다.
3. **`TASK_MODIFIED` 는 3필드가 아니라 4필드** — CLAUDE.md 에 **`output_ver_no` 누락**.

★ `TaskModifiedPayload` javadoc: *"내부 식별자(`SRC_SN`)가 아니라 산출 폴더의 실제 파일명"* — **`SRC_SN` 은 명시적 배제 대상**.

**⇒ 최종 리포트 「프로젝트 CLAUDE.md 갱신 필요」 1순위 · 근거 확정 · 관제 규격 재대조 불필요.**

## ★★★ 사라진 마이그레이션 V번호 인용 — **3연속. 전역 스윕 후보로 승격**

D014 `ERD-016`(V11·V36) · D015 `ERD-014`(V36) · D016 `ERD-021`/`ERD-027`(V39·V44·V144·V146). Flyway 스쿼시로 라이브 경로엔 V1~V6 만 있다. → 도메인별 P1 이 아니라 **전역 스윕 1건**으로 묶는다.

## ★★ 표준용어 스윕이 하루 전에 돌았는데 1건이 빠졌다

2026-08-14(감사 하루 전) `ERD-020`·`ERD-026` 의 `STTS_CD` 를 **20→16** 으로 고쳤는데 **`ERD-027` 만 20 으로 남았다.** 같은 도메인 `ERD-021` 은 16 이라 **한 도메인에 두 크기**다. ⚠ **실물도 20 이라 ERD 단독 수정 금지**(마이그레이션 동반).

⇒ 「프로세스 권고」에 **"스윕은 대상 전수를 먼저 확정하고 돌려라"** 추가.

## ★★ 신규 표준용어 결함 유형 — 약어가 **다른 뜻의 표준단어와 충돌**

`ERD-021.APLY_DT` 논리명이 **'적용'일시**인데 **`APLY` 는 행안부 표준단어 '신청'** 이고 '적용'은 **`APLCN`** 이다. 독립 근거(`DE_IDNTF_APLCN_DT`)는 `APLCN` 을 쓴다 ⇒ **한 프로젝트에서 '적용'이 두 약어로 갈리고, `APLY` 는 '신청'과 의미 충돌.**

기존 유형(미등록 약어 / 도메인 크기 불일치)과 **다른 축**이므로 최종 리포트에서 별도 유형으로 분류.

---

# ✅ 감사 완료 — 2026-08-15

**14개 도메인 × 10차원 = 감사 에이전트 140개 전량 완료.**
총 **1,263건 / P0 211 / P1 703 / P2 349**.

- 통합 리포트: **`REPORT.md`** (교차 클러스터 9종 · CLAUDE.md 갱신 필요 · 판정 요청 11건 · 검사 사각 · 프로세스 권고 8 · 소급 항목)
- 원본: `raw/DOMAIN-XXX.md` 14개
- 진행 기록: `PROGRESS.md`

**ITEM 수정 0건 — read-only 감사였다.** 수정은 `/mc-logi-update` 로 별도 진행하며, 그때 `REPORT.md` §5 「수정 순서 의존」을 반드시 먼저 읽을 것.

## 다음 세션이 바로 쓸 수 있는 것

1. **`REPORT.md` §1 교차 클러스터 C1~C9** — 한 번 고치면 여러 도메인 P0 가 함께 닫힌다.
2. **§4 판정 요청 11건** — 사용자 결정 없이는 진행 불가.
3. **§6 검사 사각** — 이 감사가 **7개 층 중 2~3개만** 봤다. 미러·키트·위키 층은 **별도 라운드** 필요.
4. **§7-8 프로세스 권고** — 일괄 스탬프 금지 · 스윕 전수 확정 · 룰 파일 교체 · 전역 타입 스윕.
