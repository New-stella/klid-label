# DOMAIN-014 시스템 설정 — 원본 감사 결과

> project_id `4ece2c3f-8e99-46f5-9580-71108a76e578` · 스킬 `mc-logi-domain-review` v1.5.0 · read-only · legacy grep OFF

## 인벤토리 (coverage 실측 — `get_neighbors` backward 14건으로 확정)

| 타입 | 건수 |
|---|---|
| domain_feature | **1** (DFEAT-045) |
| api_endpoint | **7** (API-068·069·090·118·141·193·194) |
| screen_spec | 1 (SCREEN-025, `/manage/settings`) · screen_design SD-015 |
| erd | ERD-016 (**LS_SYSTEM_CONFIG** 1테이블) |
| class_diagram | CDIAG-012 · **diagram_c4_component CMP-011**(★11건 전량 조회로 확인) |
| code_module | MOD-016 |
| use_case | **2**(실질) — ★**`UC-031` 이 `belongs_to_domain` 링크 자체가 없다** |
| acceptance | 1 (AC-006 — ★**`UC-006` 파생이라 `UC-031` 축 AC 는 0건**) |
| requirement / test_scenario / nfr / domain_event | **전부 0건** |

---

## coverage (7건 — P0 2 / P1 4 / P2 1)

### ★★★ 이번엔 **도메인 description 이 낡았다** (D013 과 반대)

| ITEM | 버전·일자 | 서술 |
|---|---|---|
| **`DOMAIN-014`** | v2 · **2026-08-04** | *"[계층 2] **외부 연동 URL**·DB 접속·토글 등은 **화면에서 바꾸지 않고** 배포 형상으로 관리한다"* |
| `API-069` | v5 · **08-10** | *"연동 서버 주소 키 4종 … **관리자 단기 토큰**을 함께 요구 … **재기동 없이 즉시 반영**"* |
| `SCREEN-025` | v24 · **08-13** | *"운영 파라미터(… **연동 서버 주소**)를 조회·수정"* |

⇒ ★**내 주입 정책은 API/SCREEN 과 일치했고 도메인 description 만 뒤처졌다.**

### P0 2건
**`COV-004`** — **orphan**. ★**두 기준 모두 명시**: ①DFEAT 매핑만 → **5건** ②SCREEN consumes 확장 → **3건**. ★**공통 orphan 3건 중 `API-193` 은 backward 0** 이고 *"의도된 소비자 `SCREEN-005` 의 `consumes_apis` **39건 목록에 없다**"* ⇒ **완전 고아**. ⚠**`API-141`(`/health`)은 인프라성이라 구분 필요** ·
**`COV-005`** — **`UC-031` 실현 SEQ 0건**. ★**대조**: *"같은 화면을 다루는 `UC-013` 은 `SEQ-013` 을 보유 ⇒ **프로젝트가 이 축을 실제로 운용하며 `UC-031` 만 결손**"*. main_flow 6단계 + alternate 3종으로 **소재는 충분한데** 없다

### P1 4건
`COV-001` 위 인벤토리 오염 + **누락 책임 3종**(연동 주소 변경 · 관리자 단기 세션 · **헬스 모니터링 5초 폴링 + 위험 액션**) ·
`COV-002` ★**도메인이 `★` 로 강조한 fail-closed 기동 가드 책임을 맡는 DFEAT 0건** — **유일 DFEAT 라 대체 후보 없음** ·
`COV-003` **`DFEAT-045` 가 R1 을 부분만 실현** — 연동 주소 4종·관리자 세션·스킴 검증 **언급 0** ·
`COV-006` **`DFEAT-045` UC backing 0** + `UC-031.realizes_dfeats=[]`. ★**전역 관례 아님을 대조로 확인**(`UC-023` 은 DFEAT 3건 연결)

### P2 1건
`COV-007` ★**위험 액션(시스템 초기화·큐 초기화·캐시 삭제) API 가 도메인 7건 어디에도 없다** — `UC-031` 은 요구하고 `postconditions` 까지 있는데. ⚠**타 도메인 소재 가능성을 배제 못 해 P1→P2 하향**

### ★★ 설정 키 화이트리스트가 **세 곳에서 갈린다** (schema 로 이관)
`ERD-016`: *"**4개** 키 화이트리스트"* / `CMP-011`: **ALLOWED 6개** 열거 / **확정 정책의 연동 주소 4종 + `DFEAT-045` 의 `portal.upload.frame-interval-sec` 는 어느 목록에도 없음**

### ★ 근거 있는 오탐 배제 2건
`COV-3`(DFEAT SCREEN backing) — ★**대조군 `SCREEN-024`·`018`·`005` 전부 `references_features` 부재** ⇒ **전역 관례 확정** ·
`DFEAT-045.specializes_feature` 미선언 — `DFEAT-044` 도 동일 ⇒ **전역 패턴**

### ★ coverage 가 넘긴 단서
- ★**`NFR-013`·`NFR-020` 역방향 후보** — 둘 다 description 이 *"**관리 화면**(/manage/*)"*·*"관리 화면은 검수자 전용"* 으로 **이 도메인을 정면 규율**하는데 `applies_to_domains` 에 D014 없음(**`[D001,D013]`**)
- **stale=true 3건이 전부 `API-069` v5(연동 주소 정책 반전) 미전파** — `COV-003` 과 **같은 뿌리**
- ★**`CMP-011` 이 도메인 경계를 넘는다** — title 이 *"시스템 설정 컴포넌트 (**비식별 옵션 포함**)"* 이고 첫 문장이 *"**UC-013** 비식별 옵션 설정을 포함한"* 인데 **`UC-013` 은 `DOMAIN-012` 소유**
- 도메인이 화면 경로를 **`/manage/configs`(실제 API 경로)** 로 적었으나 `SCREEN-025.route` 는 **`/manage/settings`**

### coverage 미확인
1차 grep OFF(★**도메인 `diff_summary` 자체가 `[추정 — 1차 설정 관리 존재 여부 확인 필요]`**) ·
위험 액션 API **전 프로젝트 대조 미수행** · NFR **description 전문은 2건만** 대조

---

## links (11건 — P0 0 / P1 9 / P2 2)

### ★★★★ 주입 정책 ↔ ITEM 정면 충돌 — **비식별 옵션 화면** (사용자 확정 필요)

| | 주장 |
|---|---|
| **내 주입 정책** (D012 requirement 실측 — 추적표 주1) | *"저작도구 **전용 화면 없이** 외부 솔루션 화면에서 설정하고 **값만 보관**"* |
| **ITEM 3건 일관** | `SCREEN-025.sections[1]` 에 **`DeidentConfigCard` 카드 실재** — 마스킹 방식(색상/모자이크/블러)·범위(0.5~2.0)·프레임 저장 여부 **입력 컴포넌트** / `UC-013.main_flow[1]` *"**관리 화면에서** 비식별 옵션을 설정한다"* / `SEQ-013` 동일 |

**리비전 판정**: `SCREEN-025` v24·**08-13**, `CDIAG-012` v2·**08-13** = **이 도메인 최신** ↔ `UC-013` v6·08-07, `SEQ-013` v2·08-07
⇒ ★**auditor 는 판정을 보류하고 사용자 확정으로 넘겼다** — 정당하다. **최종 REPORT 판정 요청 항목.**

### ★ 반대로 — **연동 주소 정책은 정합 확인**
`SCREEN-025.sections` 에 **애플리케이션 속성명 그대로 `binds_to`**:
`configs.kpst.deid.base-url` · `configs.authoring.integration.ai-server.base-url` · `configs.vlm.client.url` · `configs.authoring.control-notify.url`
★**폐기된 구 후보 `authoring.integration.deidentify.base-url` 은 등장하지 않는다.** 대역 차단 폐지도 **`API-069` v5 + sections validation 문구(*"주소 대역에 따른 차단은 하지 않는다"*)에 반영 완료.**

### P1 9건
`LNK-001` `DFEAT-045` 가 7건 중 **2건만 매핑**(`API-090`·`193`·`194` 누락) ·
`LNK-002` **`API-118`·`API-141` 완전 고립**(forward 는 `belongs_to_domain` 하나뿐, backward 0) — ★도메인이 **운영 헬스 채널 책임을 명시하지 않는다** ·
`LNK-003` **`SCREEN-025.realizes_use_cases=[]`** — UC 3건이 지목하는데 **weak `references` 만** ·
`LNK-004` ★**`UC-031` 이 도메인 이름과 그대로 겹치는데 `belongs_to_domain` 없음** ⇒ **도메인 UC 인벤토리 0건**. ★**전역 관례 아님 대조 확인**(`DOMAIN-010` 3건·`DOMAIN-005` 1건 보유) ·
`LNK-006` `UC-031`·`UC-013` **`realizes_dfeats=[]`** ·
`LNK-007` ★**`DFEAT-045` 만 `specializes_feature` 미설정** — **도메인 다른 6건이 전부 `FEAT-007` 을 가리키는데** ·
`LNK-008` **`persists_in_tables=[]`** — 유일 테이블 `LS_SYSTEM_CONFIG` 책임 DFEAT 0 ·
`LNK-009` **`SEQ-013`·`SEQ-007` `invokes_apis=[]`** — 둘 다 mermaid 에 `PUT /v1/manage/configs/{key}` 명시 ·
`LNK-010` ★**`MOD-016`(백엔드 패키지)이 화면·FEAT 만 realizes 하고 DFEAT 와 미연결** — *"백엔드 패키지가 `screen_spec` 만 realizes 하는 비대칭"*

### P2 2건
`LNK-005` `UC-013` 귀속 판단 필요 — ★**양쪽 근거 병기**(D014: `CMP-011` title·`SEQ-013` / D012: `brownfield.decided_by=ADR-006`) ·
`LNK-011` ★★**`decided_by` 를 걸 ADR 자체가 없다** — **45건 제목 전수 확인 결과 「연동 주소 설정화」·「관리자 단기 토큰」·「대역 차단 폐지」 대응 ADR 0건**. ⇒ *"결정 기록이 없어 **다음 세션이 폐기된 구 정책(IP 대역 차단)을 되살릴 위험**이 남는다"*

### ★★ `unresolved` — **대리 측정 0건**(측정 불가와 구분해 명시)
`DFEAT-045` 3/3 · `SCREEN-025` 5/5 · `UC-031` 2/2 · `SEQ-013` 1/1 · `CMP-011`·`CDIAG-012` **전부 materialize** ⇒ *"0건이 아니라 **'측정 불가 + 대리 측정 0건'**"*

### ★ deprecated 참조 **0건** — 판정 범위 명시
활성 14건 중 **본문을 실제로 연 것**을 열거하고, **retired 집합 21건**(도메인 2·UC 7·NFR 7·ADR 5)과 대조. ★**유일 UC 인용(`CMP-011` 의 `UC-013`)은 `approved` 활성이라 잔재 아님.**

### ★ links 가 넘긴 단서
- ★**`CMP-011` 이 `depicts_dfeats` 정상 materialize** ⇒ **전역 패턴이 이 도메인엔 해당 없음**
- ★**위험 액션 3종이 `references_apis=[]`** — *"링크 누락이 아니라 **API 인벤토리 자체의 공백**"*
- ★**비식별 옵션 3항목만 `binds_to` 없음** — 다른 카드는 전부 보유 ⇒ **설정 키 이름이 설계에 없다**
- ★**컬럼 물리명이 ITEM 마다 다르다** — ERD `STNG_KEY`/`STNG_VALUE`/`STNG_TYPE_CD` ↔ CDIAG `configKey`/`configVl`/`configTypeCd`(주석 `CONFIG_TYPE_CD`)
- **화이트리스트 3중 불일치** — ERD **4** ↔ CMP **6** ↔ SCREEN 실바인딩 **6+4=10**
- `DFEAT-045.implementation` 이 `planned/0` 인데 **`MOD-016` 실재 + `CMP-011` 이 실제 클래스명까지 서술**
- ★**`API-193` backward 0** ⇒ *"라벨링 화면(D010)의 `consumes_apis` 누락 가능성"* — **타 도메인이라 미계상**

### links 미확인
**`SD-015` 본문 미개봉**(deprecated 인용 여부 **미검사 사각**) · 정적 렌더 미러 미조회 · NFR `applies_to_domains` 본문 미대조(coverage 축으로 보류)

---

## stale (10건 — P0 2 / P1 3 / P2 5)

### ★★★★ **"최근 갱신 = 최신"이 성립하지 않는다** (D014-STL-002)

**`ERD-016` 은 이 도메인에서 가장 최근 갱신본**(v9 · **2026-08-13T07:54**)인데 **구 모델 3곳 잔존**:
①description *"… **4개 키 화이트리스트**만 보유"* ②`tables[0]` *"편집 가능 키 화이트리스트(**4종**)"* ③`STNG_KEY` *"화이트리스트 **4개 키만** 등록·갱신 허용"*
★**열거 키가 현행과 하나도 일치하지 않는다** — ERD 의 `FFMPEG_THREADS`·`FFMPEG_OUTPUT_FPS` 는 **도메인 어느 활성 ITEM 에도 없다**. 실제는 `SCREEN-025` **10종 바인딩**.
★**논리적 확증**: *"08-10 확정이 감사를 '`LS_SYSTEM_CONFIG` 수정자·수정일시로 충당'한다고 규정하므로 **주소 4종 키가 이 테이블에 반드시 들어간다** — '4개 키만'과 양립하지 않는다"*

### P0 2건
`STL-001` `DOMAIN-014`(**`stale=false`**) 계층 2 가 *"**외부 연동 URL** … 화면에서 바꾸지 않고"* ⇒ **08-10 확정과 정반대**. ★**단 DB 접속·토글 부분은 여전히 참**(정밀 판정) ·
`STL-002` 위 ERD

### P1 3건
`STL-003` `DFEAT-045`(**`stale=true`**) — 연동 주소·관리자 세션·스킴 검증 **서술 0건** + `API-193`·`194` 누락 ·
`STL-004` `MOD-016`(**`stale=true`**) — 관리자 세션·스킴 검증 책임 없음. ★**`data` 에 API 링크 필드 자체가 없어 어느 엔드포인트를 담는 패키지인지도 추적 불가** ·
**`STL-005`** ★★**반전이 ADR 로 등재되지 않아 cascade 기준점이 없다** — *"반전 근거가 **`change_summary` 에만 흩어져 있다** … **리비전 메타라 다른 ITEM 이 참조할 수 없어** 같은 반전이 도메인·ERD 에 도달하지 않았다"* ⇒ **`STL-001`·`002` 가 살아남은 근본 원인**

### P2 5건
`STL-006` ★**일괄 스탬프 = 컷오프형(D012형)** — 3건(+링크 대상 2건), 창 **06:36~06:50**(D012·D013 과 **동일 창**). ★*"D012(17)·D013(8)보다 **건수는 작지만 컷오프 폭은 더 크다**(미도달 확정 **3파**: 08-08·08-10·08-13)"*. ★**`API-068` 은 `stale=false` 라 자동 감지 밖** ·
`STL-007` `[추정 …]` 미해결 · `STL-008` `implementation` 갈림(하위 3건 `implemented/100` ↔ **상위 `DFEAT-045`·`ERD-016` 은 `planned/0`**) · `STL-009` brownfield 메타 3건 ·
**`STL-010`** ★★**내 주입 전제 7 의 근거가 실측 실패**

### ★★★★ 주입 전제 7 — **근거를 확인할 수 없었고, 반대 증거가 나왔다**

내 전제: *"비식별 옵션은 저작도구 전용 화면 없이 외부 솔루션 화면에서 설정하고 값만 보관"*(출처: D012 requirement 의 **추적표 주1**)

**auditor 의 검증 시도 결과**:
① ★**`list_items(type=requirement, domain_id=DOMAIN-012)` → `total 0`** ⇒ *"인용된 근거에 **도달 못 함**"*
② ★★**반대 증거** — `DFEAT-042`(D012) v5 는 *"기존 '**비식별 결과·이력 검토**'는 외부 솔루션으로 이관"* 이라 적어 **결과·이력 검토의 이관만 말하고 옵션 설정의 이관은 말하지 않는다**
③ **약한 반대 신호** — 그 카드는 **5개 중 유일하게 `triggers_api`·`binds_to` 가 없다**

⇒ ★**auditor 가 확정 보고를 보류하고 P2 advisory 로 낮춘 것이 정확한 처리다.** **최종 REPORT 판정 요청.**

### ★ ADR 판정
**활성 40건 제목 전수 스캔** → 시스템 설정 축 **0건**(★**고립 ADR 포함 스캔**). 개봉 2건 — `ADR-039`(축 가장 근접, **본문 폐기 모델 없음**, 단 **링크 0건**) · `ADR-006`(★**폐기 게이팅 문장 확증**하나 **D012 소유이고 이미 보고된 5건 중 하나라 재보고 안 함**)

### ★ deprecated 참조 **0건** + 축 불일치 1건
retired 집합을 13개 ITEM 전 필드에 대조 → **인용 0**.
⚠★**단 `SCREEN-025` 가 `ai-server.base-url` 을 편집 대상으로 두는데 그 연동을 서술하는 `INT-001` 이 `status=deprecated`** — **요건 미충족이라 미보고, cross-check 필요**

### ★ stale 이 넘긴 단서
- ★**`FEAT-007`("라벨링 정밀도 조절")을 `API-194`(관리자 세션)·`API-068`(설정 목록)이 implements 로 선언** ⇒ **범위 오염 의심**(FEAT-007 도 08-07 스탬프 대상)
- ★★**화면이 서버가 거부하는 구간을 입력 가능하게 노출** — `YOLO_IOU` 서버 **30~80** ↔ 화면 **25~80**(다른 2건은 화면이 더 좁아 안전)

### stale 미확인
1차 grep OFF · 코드 실측 없음(**ERD 의 "4개 키" 반증은 ITEM 대조로만**) · ADR 5건 중 4건 미개봉(**도메인 근거 ADR 0건이라 축 무관 판단**)

---

## schema (14건 — P0 4 / P1 9 / P2 1)

### ★★★★ 화이트리스트 확정 — **4도 6도 아니고 16이다** (D014-SCH-001)

**진짜 목록 = `ConfigKeys.ALLOWED` 16개** (기계 카운트) · **시드 12행** · ★**연동 주소 4종은 "시드하지 않는 것이 설계"**

★★**코드가 이미 이 실패 유형을 경고하고 있었다** — `ConfigKeys.java` 클래스 javadoc:
> *"⚠ 여기에 **키 개수를 적지 않는다**. 키가 늘 때마다 서술이 낡아 실제와 어긋난다 (구 서술 **"편집 가능한 5개 키만 등록한다" 가 실제 12개인 상태로 오래 남아 있었다**)."*

⇒ **그 경고를 받은 코드는 개수를 안 적었는데, ERD·CMP 는 각각 "4개"·"6개"를 적어 둘 다 틀렸다.**
★**`ERD-016` 이 예시로 든 `FFMPEG_THREADS` 는 `application.yml` 환경변수일 뿐 설정 키가 아니고, `FFMPEG_OUTPUT_FPS` 는 레포 전체 0건**(가공의 키).

### P0 4건
`SCH-001` 위 화이트리스트 ·
`SCH-002` **`STNG_TYPE_CD` 에 `DECIMAL` 누락**(4종 ↔ 실물 5종) — ★**시드 2행 실재 + `getDouble` 이 `DECIMAL` 아니면 예외 + 전용 `DECIMAL_RANGE` 검증축** ⇒ **그 축이 통째로 누락 판정된다**(D012 동형) ·
`SCH-003` **`API-069` `request_body` 부재** — ★**400 설명이 *"@NotBlank/@Size … 검증 실패"* 로 존재하지 않는 바디의 검증 실패만 규정** ·
`SCH-004` ★★**`API-194` 필드명이 실물과 다르다** — 계약 `password` ↔ 실물 **`adminPassword`** ⇒ **계약대로 구현·호출하면 전량 400**. 길이 제약(4~100)도 없음

### P1 9건
`SCH-005` `API-068`·`069` example 의 **`configTypeCd:"INT"`** — ★**레포 전수 grep 0건**, 실제는 `NUMBER`(**계약끼리도 자기모순**) ·
`SCH-006` ★**토큰을 어디 싣는지 `parameters` 에 없다** — 실물은 **`X-Admin-Session` 헤더**(★javadoc: *"바디에 두면 설정 값과 인증 자격이 한 구조에 섞여…"*) ·
`SCH-007` `API-194` **400 누락**(구현 `@ApiResponses` 는 공표) ·
`SCH-008` ★**200 스키마에 `token` 이 없다** — *"전달 방식은 구현 계약"* 이라 비워 둬 **소비자가 `API-069` 에 실을 토큰을 어디서 얻는지 알 수 없다** ·
`SCH-009` ★**`SCREEN-025` 가 API 에 없는 것을 명세** — `latencyMs`·`OUT_OF_SERVICE`·디스크/관제/포털. **실물은 3종(deidentify·aiServer·database)·`UP`/`DOWN`·최상위 `UP`/`DEGRADED`** ·
`SCH-010` **`ERD-016` 이 스쿼시로 아카이브된 `V11`·`V36` 을 진실원으로 인용** — ★**최종 갱신이 스쿼시와 같은 날(08-13)인데 인용은 미갱신** ·
`SCH-011` `STNG_VALUE` 설명이 **자기 ERD 에도 없는 `CONFIG_TYPE_CD`** 인용 ·
`SCH-012` `DFEAT-045.persists_in_tables=[]` ·
`SCH-013` ★**`EXPLN varchar(500)` 이 표준도메인 어디에도 없다** — 설명 계열은 **내용V1000/V2000/V4000·V200/V255**(500 없음). ★**뒷받침 사업용어 출처가 `KLID-저작도구 ERD-016` 자기등록**

### P2 1건
`SCH-014` **비식별 옵션 3개만 `binds_to` 없음** — ★**실물 키는 존재**(`kpst.deid.masking-type`·`masking-range`·`db-save`, **시드 3행**)

### ★★ 정합 확인 11건 — **근거 있는 "위반 0"**
★**컬럼 6종 이름·타입·길이·nullable 전부 일치**(D012 형 22↔28 없음) · **인덱스 PK 하나뿐로 일치** · **FK 양쪽 0건** ·
★**감사 정책 실동작 확인**(`MDFR_ID` nullable · `MDFCN_DT` DEFAULT CURRENT_TIMESTAMP NOT NULL) ·
★**`API-090`·`118`·`141`·`193` 은 실물과 완전 일치** — 특히 **`API-193` 은 계약 자체가 온전**(*"고아 문제는 coverage 축"*) ·
★★**패스워드 노출 금지 실물 준수** — 실패 로그가 `reason=invalid_password` 만 남기고 값 미기재, 응답 DTO 에 필드 없음, 연동 주소 감사 로그도 **URL 마스킹 + 로그 정제** 적용 ·
★**기본 10분·상한 30분**은 `DEFAULT_TTL`/`MAX_TTL`/`clampTtl` 로 확인

### ★ CSV 대조
6파일 전량 · 단어 13 · 용어 6 · **도메인 13종** 조회. ★**자기등록 2건 배제**(`STNG_TYPE_CD`·`EXPLN`) — ⚠**단 `STNG_TYPE_CD` 의 도메인 `코드V20` 은 독립 등록이라 `varchar(20)` 자체는 정당**(정밀 구분).

### ★ schema 가 넘긴 단서
- ★**`CMP-011` 에 생성자 주입 3종이 통째로 빠졌다**(토큰 서비스·URL 검증기·신뢰 가드)
- ★**실물 16키 중 3키는 어떤 화면에도 편집 경로가 없다**(`portal.upload.frame-interval-sec`·`autolabel.polygon.max-boxes`·`eventtype.excluded-class-codes`)
- ★**`BOOLEAN` 은 검증 분기만 있고 시드·ALLOWED 키가 0건**(사용처 없는 선언) ⇒ **폐기/존치 판단은 정책 축**
- `SCREEN-025` 미러에 `latencyMs`·`OUT_OF_SERVICE` 잔재 가능성 ⇒ `source_hash` 대조 권장

---

## policy (6건 — P0 0 / P1 2 / P2 4)

### ★★★★ 비식별 옵션 판정 — **ITEM 이 현행이고 내 전제가 틀렸다**

> *"판정: ★**ITEM 3건이 현행**이고 주입된 전제가 **낡았거나 근거 없음**. **억지 gap 을 만들지 않았다.**"*

**근거 4중**:
① **리비전** — `SCREEN-025` v24(**08-13**)·`UC-013` v6·`SEQ-013` v2, **셋 다 일치하며 상호 모순 없음**
② **본문 실측** — `DeidentConfigCard`(note: *"위탁 시 전송하는 비식별 처리 옵션. **전역 1벌**"*) + 입력 3종 실재, ★**게시 렌더 HTML 에도 반영**
③ **`SEQ-013` 이 구현 실측 기반임을 명시** — *"비식별 옵션 전용 별도 엔드포인트는 없으며 일반 시스템 설정 키-값 경로를 공유한다. **코드 기준**"*
④ ★**ADR 대조** — `ADR-006` 전문 확인 결과 *"외부 솔루션 화면에서 설정"* 취지 문장 **없음**. 오히려 `CMP-011` 이 *"설정값은 … 소비측이 읽어간다"* 로 **저작도구 보관+전달 모델**을 서술

⇒ ★**auditor 판정 요청**: *"전제가 사용자 확정 정책이라면 **3건이 통째로 폐기 대상이 되는 대형 반전**이다. 반대라면 **조치 불필요**. **ITEM 근거만으로는 '화면 보유'가 현행**이다."*

### ★★ 정책 5(대역 차단 폐지) — **미러 층까지 확인해 잔재 0건**
★**게시 렌더 `main.html`(14,480자)을 무인증 GET 후 grep**: *"'대역' 4건·'차단' 4건이 **전부 '차단은 하지 않는다' 부정문**이고, `127.0.0.1`/`192.168`/`169.254`/`rebinding`/내부망/사설은 **0건**"*
⇒ **7개 층 중 미러 층까지 실제로 연 첫 사례.**

### ★ 정책 4·7 도 전수 확인
**정책 4**: ★**죽은 빈 키 `deidentify.base-url` 출현 0건**(렌더 grep 실측 `count=0`), `kpst.deid.base-url` 1건 ·
**정책 7**: ★**패스워드·토큰 노출 위반 0건** — example `"********"` 마스킹 · 200 응답에 **토큰 원문 미노출** · 렌더 HTML 에 `password` 0건, "패스워드" 6건은 **전부 라벨·미표시 규약 문구**

### P1 2건
`POL-001` ★**`ADR-006` 이 이 도메인 비식별 옵션 체인의 유일 근거인데 폐기 게이팅 보유** — *"`PRVC_TYPE_CD` 가 PRVC/PSDO일 때만 위탁"*. ★**`decision.justification` 에는 없고 `context` 문장만 홀로 낡았다**(정밀 판정). ⚠**D012 와 이중 보고 가능 — 병합 필요** ·
`POL-002` ★★**정책 6 이 API 층에만 있고 설계 모델 3층에 없다** — `CMP-011` *"REVIEWER **이중** 검증"* 뿐이고 **관리자 세션 컴포넌트 0건** / `SEQ-013` 분기 **3갈래뿐**(토큰 없음/만료 → 403 없음) / `CDIAG-012` 에 세션 개념 없음 ⇒ *"그 층대로 구현하면 **주소 키가 관리자 토큰 없이 변경된다**"*

### P2 4건
`POL-003` ★**토큰 전달 채널을 두 계약이 서로에게 미룬다** — `API-194` *"전달 방식은 **구현 계약**"* ↔ `API-069` 는 요구만 하고 `parameters` 1건·`security` 단일 ⇒ *"**403 조건을 판정할 입력이 계약에 정의돼 있지 않다**"* ·
`POL-004` **감사 금지 규약이 발급 EP 1건에만** — 토큰을 실제로 수반하는 `API-069` 에 로그·감사 서술 **0건** ·
`POL-005` `[추정 …]` — ★**대조: 같은 도메인 다른 4건은 확정 서술만 보유** ·
`POL-006` `DFEAT-045` 만 `change_kind`·`diff_summary` 동시 공란

### ★ ADR 5건 개별 귀속 판정 (정밀)
`ADR-006` **귀속 O + 폐기 정책 실재** → `POL-001` / `ADR-008`·`022`·`032`·`026` **전부 귀속 X**(각각 마킹·비식별신고·메타·포털 축)
⇒ ★*"**5건 중 이 도메인이 책임질 것은 `ADR-006` 하나**"*

### ★ POL-2~POL-7 skip — **각 근거 + 충돌 경고**
`ADR-045`=증강 2축(**tags 에 `bff` 0건**) · **`ADR-051` 부재** · `ADR-028`=증강 dedup(**`LOCAL_USER` 개념 없음**) · `ADR-027`=재비식별(**4종 API 전부 `security` 통일돼 P0 조건 미해당**) · `ADR-036`=event_annotation ·
★★**`ADR-036`(POL-6) 은 적용하면 오히려 위반**: *"확정 정책 5 가 스킴 **http/https 를 모두 허용**하므로 **HTTPS 강제 룰을 적용하면 확정 정책과 충돌**한다"*

### policy 미확인
★**`SD-015` 렌더 URL 3개 후보 전부 404** — 미러 바이트 확보 실패(**v3 가 `SCREEN-025` v24 보다 나중이라 시각상 최신이나 본문 미확인**) ·
`MOD-016`·`API-118`·`141`·`193` 본문 미조회(**대역차단 잔재 0건 판정의 사각**) · 정책 8 은 **대조 대상 ITEM 0건이라 판정 불가**

---

## acceptance (7건 — P0 0 / P1 2 / P2 5)

### ★★★★ 네 번째 "두 체계가 갈렸다" — **그런데 이번엔 진짜 공백 3축을 갈라냈다**

**확정 정책 9축 중 7축이 레포에서 검증 중**(다수 **Critical** 등급):
`TC-SYSCFG` 9 · `TC-CACHE` 5 · **`TC-ADMS` 27** · **`TC-ENDP` 37** · `TC-CFG` 6 · `TC-PROF`(기동가드) · `TC-FE-622~633`(연동 주소 카드)
★**정밀 대응 실증**: `TC-ADMS-018`(10분/30분 상한) · `019`(0·음수 폴백) · **`011`("유효성 판정을 서버가 소유한다")** · `021`(토큰 없이 403) · **`025`(스킴 위반 400 + **내부망 주소 200** — 대역 차단 폐지 반영)** · `TC-ENDP-019`(키 = 실효 속성명) · `022`(**실제 새 주소로 나감**)

★★**단 3축은 두 체계 모두 0건 — 진짜 검증 공백**:
**⑧위험 액션 확인 다이얼로그**(P1) · ⑨헬스 5초 폴링·`API-090` · YOLO_IOU 30/29 경계

### P1 2건
`ACC-001` **`UC-031` AC 0건**(backward 도 0) — ★**활성 25건 전량 `derived_from_use_cases` 대조**로 **참조 UC 18종 열거 후 `UC-031` 미포함** 확정 ·
`ACC-002` ★**위험 액션이 AC 에도 레포에도 0건** — ★**grep 매치가 전부 다른 도메인**(`TC-FE-137` 검수 승인 · `TC-FE-641` 버전 불러오기) ⇒ *"다른 축과 달리 **대체 검증체계조차 없다**"*

### P2 5건
`ACC-003` **negative AC 0건** — ★**프로젝트 전체에서 `is_negative=true` 는 `AC-026` 1건뿐**(타 도메인). ⚠**단 분기 대부분이 레포에서 Critical 로 검증 중이라 "검증 공백이 아니라 AC 축 추적성 결손" ⇒ P2** ·
`ACC-004` **`AC-006` 커버가 설정 키 1종의 PUT 뿐** — ★**`ACC-002`(완전 부재)는 성립 안 함**(링크 자체는 정상)을 명시한 정밀 판정 ·
`ACC-005` ★**`UC-031` 이 알려진 불일치를 본문·`tech_considerations` **두 곳에** 명시**(*"화면 25~80 vs 서버 30~80"*)하는데 **그 경계를 고정하는 AC 가 없다**. ★grep 으로 `TC-AIYOLO-07`(0~1 정규화, **다른 축**)·`TC-SYSCFG-004`(키 무관 일반 룰) 구분 ·
`ACC-006` 헬스 — ★**`TC-HEALTH` 3건 전부 `actuator` 비식별 축**이고 `manage/health`·`latencyMs` grep **0건** ·
`ACC-007` ★**`AC-006` 의 파생원 `UC-006` 이 `DOMAIN-004` 소속** ⇒ *"AC 중심 뷰에서 이 도메인 AC 는 **0으로 표시**되고 DFEAT 경유 `verifies` 를 따라가야만 도달"*

### ★★ 작업 순서 의존성 4단계 (auditor 명시)
**선행 1** `UC-031.belongs_to_domain` — *"지금 상태로 AC 를 신설하면 … **새 AC 가 어느 도메인에도 안 붙는다**"*(D012·D013 동일 함정)
**선행 2** 위험 액션은 **API·DFEAT 자체가 없다** — *"ITEM 이 없는 상태에서 시나리오를 쓰면 **존재하지 않는 엔드포인트를 인용**"*
**선행 3** YOLO_IOU **정본 확정** 후에야 기대값 확정
**선행 4** `API-090`·`194` 를 DFEAT 에 연결해야 **verifies 축으로 도달 가능**

### ★ 정밀한 오탐 억제
`ACC-004`·`005`(deprecated 인용) **검사했으나 위반 0** — ★*"`AC-006` 의 when 이 `API-069` 활성 path 와 정확히 일치하며 retired 3건은 이 도메인 비귀속. **이중 인용 요건을 못 채워 `ACC-006` 도 미보고**"* ·
`ACC-003`(도메인 REQ 검증 AC) — **REQ 귀속 모호로 룰대로 skip** ·
★**`AC-006.derived_domain_ids=[]` 는 "플랫폼 자동계산 미반영 가능성"으로 단정 회피**

### ★ 이 도메인의 실제 형상
> *"**정책은 최신 / AC 는 미도입**" — API·도메인은 확정 정책을 이미 반영했고 **AC 축만 08-07 이전 상태**에 머물러 있다.*

---

## content (13건 — P0 0 / P1 7 / P2 6)

### ★★ 한글 7축 **전부 통과 — 손상 0건** (처음으로 전 축 클린)
①18종 grep **0** · ②희귀음절 **394 중 108종 전수** → 0(★검토 예 명시: `김`=끊김·`괴`=파괴적·`텝`=32 스텝·`흐`=운영 흐름) ·
③2음절 어간 **10종** → **전건 정상**(D011~D013 **4연속 전건 오탐** 패턴) · ④고립어 **197종** → 0 ·
④b **실질 후보 28쌍** → 0 · ★④d **190종 전수** → 0 · ★Hamming **111히트/유니크 31쌍** → 0
★**정밀 확인 예**: `텍스트로`~`토스트로` — **둘 다 정상이고 각각 다른 필드에 실재**

### ★★ `SD-015` 검사 완료 — **gap 0건** (links 가 남긴 사각 해소)
★*"`renders[].url` 의 `/uploads/designs/…` 는 **서버가 발급하는 업로드 산출물 경로(시스템 메타)이지 저자가 쓴 산문이 아니므로** 오염으로 계상하지 않았다"* — 정밀 판정.
★**부수 수확**: **`SD-015` 는 카드 5종으로 정확한데 `UC-031` 만 3종** ⇒ **`CNT-010` 의 반증 근거로 사용**

### P1 7건
`CNT-001` `DOMAIN-014` 에 **금지 3종 동시** — 클래스명 + ★**저장소 사고 이력**(*"`.env` 빈값이 `${KEY:default}` 를 무력화한 **사고 이력이 근거다**"*) + `★` 마커 ·
`CNT-003` `ERD-016` **진실원 파일 경로·`V11`/`V36`·엔티티명**. ★**content 는 "본문 오염" 축, schema 는 "인용 대상 부재" 축 — 명시적 분리** ·
`CNT-004` `CMP-011` **어노테이션 7종 + `CWE-117` + 도출 노트**(*"실제 생성자 의존성에서 도출"*). ★**`components[].name` 은 노드명이라 오염 아님, 산문 안 재인용만** ·
`CNT-005` `API-069` 400 에 `@NotBlank/@Size`·`(Service)` — ★**같은 ITEM 403/404 는 깨끗**(대비) ·
`CNT-009` `SCREEN-025` 에 **FE 훅 `useHealth()`·상태 변수 `isDirty`** ·
**`CNT-010`** ★★**`UC-031` 이 "3종 설정 카드"로 단정하는데 진실원 3곳이 5종** — `SCREEN-025.purpose`·`consumes_apis`(★`API-194` 포함)·**`SD-015`**. ⇒ **비식별 옵션·연동 주소·관리자 세션 흐름이 main_flow 6단계 어디에도 없다** ·
`CNT-011` ★**실재하지 않는 `CONFIG_TYPE_CD` 를 3 ITEM 이 인용** — ★**`ERD-016` 은 같은 테이블 안에서 자기모순**(컬럼은 `STNG_TYPE_CD`). ★**`CDIAG-012` 가 스스로 "ERD-016 기반"이라 선언해 대조 기준 확정**

### P2 6건
`CNT-002` `[추정 …]` · `CNT-006` `API-090` **구현 상태**(*"코드엔 구현됨, LogiCraft 미등록이었던 것 보완"*) · `CNT-007` `API-141` 클래스명+*"갭 대조로 신규 등록"* · `CNT-008` `AC-006` 의 `@Valid` — ★**같은 배열 다음 원소는 어노테이션 없어 표기 규약이 갈린다** ·
`CNT-012` `UC-031` 이 **`description` ↔ `tech_considerations` 에 같은 100자 중복** + 560자 1문단 ·
`CNT-013` ★**`CMP-011` 제목이 실제보다 넓다** — *"비식별 옵션 포함"* 인데 **components 4종에 담당 요소 0건**이고 ★**같은 ITEM 이 스스로 *"본 컴포넌트는 설정 저장/제공만 담당"* 이라 한정**

### ★ 스캔 실적
**18건 전수 kit-export**(count=18, 결손 0, **148,179 bytes 디스크 파싱 — 전사 없음**) ·
**문자열 1,271 → 본문 1,051 → 한글 346** · 순회 경로 **50여종 재귀** ·
★**`source` 필드 부재 확인**(구조화 필드만 보유) · **`implementation.*` 는 규약상 검사 제외** 명시

### ★ content 가 넘긴 판정 기준 질문
★**`MOD-016` 의 `ConfigKeys`·`SystemConfigService` 는 `code_module` 타입의 *서술 대상 자체*라 오염 제외** / **`FEAT-007.implementation.subtasks[]` 의 클래스명·`V54` 는 `implementation` 필드라 제외** ⇒ *"**판정 기준 변경 시 재검토 필요**"*

---

## requirement (5건 — P0 0 / P1 3 / P2 2)

### ★★★★ 역전의 방향이 반대다 — **그래프에는 있는데 추적표에 없다**

> *"D011·D013 유형이 **아니라 그래프에는 있는데 추적표에 없는 역방향 역전**"*

**`UC-031` 추적표 실측**: `grep -c "UC-029|UC-030|UC-031"` → **0**
★**본문·결번 목록·범위 제외 목록 어디에도 없다.** 유스케이스 기준선이 **19건이고 `UC-028` 에서 끊긴다.** 유스케이스명세서도 **0건**.
★★**제외표에도 없다** — 부록 2 제외 4건은 전부 외부/보안 인프라 축 ⇒ *"**제외 결정조차 내려진 적이 없다**"*

### ★★★ 내 전제 2건이 또 틀렸다 (#7)
**① "최근접 `REQ-008` — `FEAT-007` 축(간접)"** → ★**실측은 직접 매핑**. 추적표가 `RQ-SFR-08-03` 을 **`UC-006`**(UC-031 아님)에 매핑하고 **화면 `SC-025`·테이블 `TB-062` 를 직접 지정**한다.
**② 전제에 없던 발견** — ★**`REQ-015`(RQ-SFR-09-04)도 `TB-062` 로 매핑**되어 **이 도메인 축 REQ 는 1건이 아니라 2건**. ⇒ *"이 누락이 `REQ-004`(divergence)를 낳았다"*
⇒ ★**모드 A(SKIP)가 아니라 모드 C 로 수행**한 근거가 됐다.

### P1 3건
`REQ-001` ★**`NFR-013` 귀속 누락** — 본문이 *"관리 화면(/manage/*)"* 을 **명시 규율**하는데 `[D001,D013]`. ★★**본문 단서와 확정 정책이 충돌**: *"저작도구는 **JWT 검증만 담당**" ↔ `API-194` 는 **저작도구가 스스로 발급·만료시키는 유일한 세션*** ·
`REQ-002` ★**`NFR-020` — 추적표에는 이미 경로가 있다**(`NFR-020`→`UC-006`→**`SC-025`**) **그래프만 누락** ⇒ **추적표 有 / 그래프 無 역전** ·
`REQ-003` ★**대표 운영 축이 REQ 에도 제외표에도 없다**(배치 파라미터·헬스·위험 액션·연동 주소·관리자 세션)

### P2 2건
`REQ-004` `REQ-015` **divergence 미명시** — ★**추적표 주1 이 *"전용 화면 없이 … 설정값은 `TB-062` 에 보관"* 을 명시**하는데 REQ 본문은 *"관제지원시스템 **UI로 구현**"* ·
`REQ-005` (advisory) **비책임 범위 미명시** — ★**"신규 REQ 신설 금지"** 를 권고에 명시(책임 범위 오염 방지)

### ★★ `NFR-013`/`020` 판정 — **D013 과 방향이 반대인 이유를 명시**
> *"D013 의 `NFR-008` 사례(본문이 내부 파이프라인만 규정 → 포털은 그 3단계를 제공하지 않으므로 **귀속 제외가 정합**)와 **방향이 반대**다 — `NFR-013`/`020` 본문은 관리 화면을 **명시**하고 DOMAIN-014 는 `/manage/*` 계약면 **4건 + `SCREEN-025` 를 실제로 소유**한다."*
★**기존 귀속 2건과 겹치지 않음도 확인**: D001=역할·인증 집행 / D013=채널 격리 ↔ **D014 고유 = 관리 화면 실물 + 자체 발급 세션**

### ★ 억지 gap 배제
★**`NFR-011`·`NFR-015` 미포함은 *"D014 특정 누락이 아니라 열거 자체가 대표 도메인만 담는 전역 관례"*** (D001·D002·D009 도 동일) ⇒ **미보고** ·
**전역 3건(`applies_to_domains=[]`)은 자동 포함** ⇒ 누락 아님

### ★ 부재의 성격 — **"처음부터 없었다"** (3중 근거)
①retired 4건 중 설정 축 **0** ②**요구사항정의서 grep → `RQ-SFR-08-03` 1건만**(그것도 *"시스템 설정값으로 관리"* 문맥) ③**제외표 미기재**

---

## diagram (9건 — P0 0 / P1 6 / P2 3)

### ★★ auditor 가 **내 단서를 정정** — 속성명은 결함이 아니다
> *"★주입 단서 일부 정정: **'속성명 `configKey`/`configVl`/`configTypeCd` 가 `STNG_*` 와 다르다'는 축은 결함이 아니다** — **`API-069` 200 응답 스키마가 동일하게** 그 이름을 쓰므로 클래스 속성명은 **DTO·필드 어휘로 정합**이다. **실제 드리프트는 description 의 물리 컬럼명 표기 1종뿐**이다."*
⇒ **내가 links 단서를 그대로 넘긴 것을 실측으로 좁혔다.**

### P1 6건
`DIA-001` ★**`CDIAG-012` enum 4종 ↔ `CMP-011` 본문이 `DECIMAL_RANGE`·*"DECIMAL 범위"*** ⇒ **같은 도메인 두 다이어그램이 값 유형 집합을 다르게 그린다** ·
`DIA-002` `CDIAG-012` 가 **실재하지 않는 `CONFIG_TYPE_CD`** 를 두 곳에 인용 ·
`DIA-003` ★★**시퀀스가 통과시키는 키군이 구조 노드에 없다** — `CMP-011` title 은 *"비식별 옵션 포함"* 이고 `SEQ-013` 은 *"(비식별 옵션 value)"* 를 그리는데 **`ConfigKeys` 열거 6개에 비식별·연동 주소 키 0건** ⇒ *"두 다이어그램이 **서로를 부정**한다"* ·
`DIA-005` ★**검증 위치가 어긋난다** — 시퀀스는 **`@Valid` 로 Service 위임 없이 400**, 계약·컴포넌트는 **범위 검증을 Service 소관**으로. ★부수: *"Service 단 REVIEWER **이중** 검증"도 안 그려 **인가가 Controller 1곳에만 있는 것처럼 읽힌다"* ·
`DIA-006` **404·401 분기 부재** — ★**`CMP-011` 이 명시한 보안 동작**(*"CWE-117 방어로 허용 안된 키는 원본 메시지 미노출"*)의 경로가 **어느 흐름에도 없다** ·
`DIA-008` **설정 목록 조회·헬스 5초 폴링·위험 액션이 구조·흐름 양쪽에 노드 0건**

### P2 3건
`DIA-004` ★**경계 판정을 신중히** — *"설정 서비스가 타 도메인 UC 에 소비되는 관계는 **룰상 정상**이고 `external_dependencies` 표현도 **적절**하다. 계상한 것은 **title·첫 문장이 타 도메인 UC 를 이 다이어그램의 정체성으로 선언한 점**뿐"* ·
`DIA-007` ★**캐시 시맨틱이 세 곳에서 다르다** — `SEQ-013` *"캐시 갱신"* / `SEQ-007` *"캐시 무효화/갱신"* / 실제 **`@CacheEvict allEntries`(전체 무효화)** ⇒ *"'캐시 갱신'은 **새 값을 캐시에 써넣는 것으로 읽혀** evict-all 과 다르다"* ·
`DIA-009` **fail-closed 기동 가드가 정형 모델에 0건**. ★**판정 범위를 좁힌 근거 명시**: *"도메인 서술 자체가 낡았으므로 **계층 2 전체가 아니라 기동 가드 축으로만 한정**"*

### ★ DIAG-001~004 결과 — **깨끗**
`coverage_pct=100`(DFEAT-045 를 **둘 다 depicts**, `get_neighbors` materialize 확인) · `deprecated_refs 0` · `dangling_refs 0` · `undepicted 0`
⇒ *"이번 갭은 전부 **DIAG-005(본문) 계열 + DIAG-001 1건**"*

### ★ policy 중복 회피를 명시적으로 수행
`POL-002` 3건 재계상 안 함 + ★**주입 단서의 "생성자 주입 3종" 중 관리자 세션 서비스도 같은 이유로 제외**하고, **남은 2종은 ITEM 내부 근거를 못 얻어 단독 gap 대신 `DIA-003` 에 흡수** — **억지 gap 배제**

### ★ diagram 이 넘긴 구조적 원인
> ★*"`SEQ-013`·`SEQ-007` 의 `invokes_apis=[]` 라 mermaid 가 문자열로만 적은 경로가 `API-069` 로 연결되지 않는다. **이 미연결이 계약(401/404/검증 위치) 표류를 자동 검출하지 못하게 만든 구조적 원인**이다."*

### diagram 미확인
`list_diagram_coverage` 도구 부재(수동 fallback) · ★**`UC-013` 소속 도메인 직접 확정 실패**(*"`DOMAIN-012` 소유라는 주입 전제는 검증하지 않았다"*) · `diagram_c4_context`/`container`/`deployment` **미조회**

---

## test_scenario (3건 — P0 0 / P1 3)

### ★★★★ **"귀속 0건"의 성격이 뒤집혔다 — 5건 중 4건이 이 도메인을 스쳐 간다**

> ★*"연동 주소 4종 중 **3종이 이미 시험 본문에 전제로 등장**하고, 그 전제의 **변경 가능성·즉시 반영은 어디서도 검증되지 않는다**"*

| TEST | 전제로 인용하는 이 도메인 설정 |
|---|---|
| `TEST-001` | ★`steps[3].note` 에 **실 IP 를 고정 기재**(`kpst.deid.base-url`) |
| `TEST-002` | `vlm.client.enabled` 토글 |
| `TEST-003` | *"**설정된 콜백 기본 URL**"* 의존 |
| `TEST-004` | `authoring.control-notify.url` 로 outbound |
| `TEST-005` | **포털 DB 접속정보뿐 = 판정기준 비대상** ⇒ **유일하게 무관** |

⇒ ★**미접촉이 아니라 "미검증 전제"** 다. *"TEST 자신의 선정 기준(**대외 연동 경계 5종**) 안에서 이 도메인만 빠졌다"*

### ★★ 9축 매트릭스 — **LogiCraft 0/9, 레포 7/9** (접두별 직접 계수)
`TC-SYSCFG` 9 · `TC-CACHE` 5 · **`TC-ENDP` 37** · **`TC-ADMS` 27** · **`TC-PROF` 20** · `TC-CLAIM` 28 · `TC-CFG` **5**
★**정밀 대응**: `TC-ENDP-010~013`(즉시 반영) · `018~020`(**실주소 전환**) · **`TC-ENDP-006` 폐기**(대역 차단 반전) · `TC-ADMS-011`(**서버 소유**, Critical) · `TC-ENDP-017`+`TC-LOG-006`(감사·**토큰 미기록**)

### P1 3건
`TST-001` `UC-031` 6단계 미검증. ★**`priority='should'`(must 아님) + 전역 미도입 정황으로 P0→P1** ·
`TST-002` 위 전제 축 ·
`TST-003` ★★**두 체계 동시 공백 2축** — **헬스 `API-090`**(★`grep manage/health|useHealth` **0건**, `TC-HEALTH` 3건은 전량 `actuator` 축) · **위험 액션**(★검출된 reset 류는 **HMAC 카운터·YOLO 트래커·목서버·목록 필터**로 전부 타 도메인). ⚠**`API-090` 은 `implemented/100` 인데 검증이 양쪽 0**

### ★ 내 주입값 3건 정정 (auditor 실측)
① **`TC-CFG` 6 → 실측 5** (나머지 4개 접두 수치는 일치, `TC-PROF`·`TC-CLAIM` 은 신규 실측) ·
② ★**방향 재확인** — *"주입 정책이 낡은 게 아니라 **ITEM 이 낡았다**"* ·
③ ★**`UC-031` main_flow 6단계에도 연동 주소·관리자 유효창이 없다**(R11 이전 형상) ⇒ **그래서 `TST-001` 은 현행 6단계 기준으로만 판정하고 그 축은 `TST-002` 로 분리**

### ★ YOLO_IOU 경계 — **0건 재확인 후 acceptance 로 이관**
★**세 후보를 각각 배제**: `TC-AIYOLO-07`(ai-server `0~1` 축)·`TC-BATCH-127`(폴백 축)·`TC-FE-150`(FE CRUD 축) ⇒ *"모두 다른 축"*.
★**`SCREEN-025` 가 불일치를 스스로 명문화**해 근거가 확정적: *"화면 슬라이더는 25~80으로 노출되어 **25~29 구간을 입력할 수 있으나 이 구간은 서버가 거부**한다"*

### ★ 전역 vs 도메인 판정 — **혼합** (근거는 D013 과 다르다)
**전역 미도입**(반복 계상 금지): `kind` 5/5 integration(**`system` 0건**) · 4축 공란 · `preconditions` 5/5 공란 · `status` 불일치 5/5 · **`stale=true` 5/5** ·
**도메인 결손**: ★*"**TEST 자신의 선정 기준 안에서** 이 도메인만 빠졌다"* ⇒ **룰 (a) 아닌 (b)**

---

# DOMAIN-014 시스템 설정 — 최종 집계

| 차원 | 건수 | P0 | P1 | P2 |
|---|---:|---:|---:|---:|
| coverage | 7 | 2 | 4 | 1 |
| links | 11 | 0 | 9 | 2 |
| stale | 10 | 2 | 3 | 5 |
| schema | 14 | 4 | 9 | 1 |
| policy | 6 | 0 | 2 | 4 |
| acceptance | 7 | 0 | 2 | 5 |
| requirement | 5 | 0 | 3 | 2 |
| content | 13 | 0 | 7 | 6 |
| diagram | 9 | 0 | 6 | 3 |
| test_scenario | 3 | 0 | 3 | 0 |
| **합계** | **85** | **8** | **48** | **29** |

## ★ 이 도메인의 특징
1. ★★**"정책은 최신 / 설계 산출물은 08-07 이전"** — API·SCREEN 은 08-10~13 확정 반영, **도메인·ERD·DFEAT·MOD·UC·AC 는 미도달**
2. ★**"최근 갱신 = 최신" 반증** — `ERD-016` 이 **가장 최근(08-13)인데 구 모델 3곳**
3. ★**일괄 스탬프 3번째 확인** — **컷오프형**, 건수 최소(3)지만 **미도달 확정 3파로 폭은 최대**
4. ★**다이어그램 구조 링크는 깨끗**(coverage 100%) — 갭이 전부 **본문 계열**
5. ★**한글 7축 전부 통과 0건**(첫 사례)
6. ★**확정 정책 3건을 담은 ADR 이 0건** ⇒ *"다음 세션이 **폐기된 IP 대역 차단을 되살릴 위험**"*

## ★ 판정 요청·소급
**비식별 옵션 화면 보유 여부**(★두 auditor 가 "ITEM 이 현행" 판정) · **화이트리스트 16키 확정** · `NFR-013`·`NFR-020` 귀속 · **`UC-031` 이 추적표에 0건**(역방향 역전)
