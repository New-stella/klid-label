# DOMAIN-012 비식별화 — 원본 감사 결과

> project_id `4ece2c3f-8e99-46f5-9580-71108a76e578` · 스킬 `mc-logi-domain-review` v1.5.0 · read-only
> legacy grep OFF

## 인벤토리 (coverage auditor 실측)

| 타입 | 활성 | 폐기 |
|---|---|---|
| domain_feature | **3** (DFEAT-041 위탁·결과저장 / DFEAT-042 대상·범위 / DFEAT-048 누락신고) | 0 |
| api_endpoint | **5** (API-094·109·112·183·184) | **4** (API-051~054) |
| screen_spec | **1** (SCREEN-032) | 2 (SCREEN-016·017) |
| erd | ERD-017 (v16) | — |
| domain_event | 2 (EVT-007·008) | — |
| diagram_sequence | 3 (SEQ-012·013·014) | — |
| class_diagram | CDIAG-003 | — |
| integration_point | 2 (INT-004 위탁 · INT-005 폴링) | — |
| code_module | MOD-005 | — |
| use_case | 3 (UC-011·013·**016**) | 3 |
| acceptance | 4 | 3 |
| requirement | 5 | — |
| test_scenario | **1** (TEST-001) | — |
| nfr | 1 정방향(NFR-012) | — |

★**`UC-016` 은 `belongs_to_domain` 링크가 없어 무소속** — 도메인 필터만 썼으면 **누락됐을 건**이다(본문 기준 귀속 판정).
★**`SVC`/`IAPI`/`LIB` 는 프로젝트 전량 0건**(COV-10 해당 없음).

---

## coverage (6건 — P0 1 / P1 5 / P2 0)

### ★★★ 최대 발견 — **도메인 description 자신이 폐기 정책을 들고 있다** (cross-dimension)

| ITEM | 버전·일자 | 서술 |
|---|---|---|
| **`DOMAIN-012`** | v4 · **2026-08-04** | *"신고 → 작업락 + `DE_IDNTF_YN='F'` + **개인정보 3필드 리셋**(프레임 축·영상 축 모두)"* |
| `DFEAT-048` | v11 · **2026-08-10** | *"2026-08-04 **폐기** — 개인정보 판정도 **보존**한다"* |
| `UC-016` | v14 · 2026-08-07 | 동일(보존) |
| `SCREEN-032` | v21 · **2026-08-13** | 동일(보존) |

**리비전 날짜로 판정하면 DFEAT/UC/SCREEN 이 최신이고 `DOMAIN-012` 가 stale 이다.**
⇒ ★**이것이 coverage 판정의 단일 진실원**이라 우선 정정 대상. (stale 차원과 교차 — dedupe 주의)

### P0 1건
**`D012-COV-001`** — ★**활성 API 5건 전부가 orphan(100%)**. 어떤 DFEAT 의 `implemented_by_endpoints` 에도 없다.
- `DFEAT-041` → **`["API-053","API-054"]` 둘 다 deprecated** ⇒ 활성 매핑 0
- `DFEAT-042` → `[]`
- `DFEAT-048` → `["API-091","API-032"]` **둘 다 타 도메인/무도메인 소유**
- ★**`API-112`(재비식별 요청)는 backward 가 통째로 0** — DFEAT·SCREEN·SEQ 전부 없는 **완전 고립**
- ⚠ **`API-054`(비식별 결과 콜백)는 `DFEAT-041` 자신이 *"그 경로는 코드에 존재하지 않는다 — KPST 폴링으로 단일화되어 콜백 경로가 제거됐다"* 고 폐기를 선언한 바로 그 엔드포인트**다

### P1 5건
`COV-002` **DFEAT 3건 전부 backing UC 0건**. ★**이 필드는 전역 미사용이 아니다** — `FEAT-005` 를 realize 하는 UC 는 `UC-007`·`UC-008` 로 실재하는데 **전부 버전관리 UC** 이고 정작 비식별 UC 3건은 링크가 없다(**오연결 의심**) ·
`COV-003` `DFEAT-048` 을 인용하는 SCREEN **0건** — ★DFEAT 자신이 *"마킹 화면에 신고 버튼이 없어 **도달 경로가 0** 이었고 2026-08-05 FE 신설로 해소"* 라고 사용자 기능임을 명시하는데도. (`DFEAT-041`·`042` 는 백엔드 전용이라 **정상**으로 판정 — 억지 gap 배제) ·
`COV-004` **SEQ 3건 전부 `happy_path`, error 0건**. ★`SEQ-014` 는 `change_summary` 에 *"거부 분기를 하나 더 뒀다"* 면서 **happy_path 안에** 넣어 뒀다 ·
`COV-005` ★**`API-183`·`184`(개인정보 메타)의 책임이 도메인 인벤토리 어디에도 없다** — description 이 *"위탁·결과 저장·대상 범위·누락 신고**만** 보유한다"* 로 **닫힌 목록**인데 두 계약이 `belongs_to_domain` 으로 실재한다. **실소비자는 `SCREEN-005`(DOMAIN-010)** ·
`COV-006` ★**`DFEAT-048` 차단 범위가 확정보다 3경로 부족** — ⑩라벨 속성값 ⑪이벤트 어노테이션 저장/승인/반려 ⑫검수 승인. ★**DFEAT 는 08-10 갱신으로 그 확정(08-07)보다 뒤인데도 반영 안 됨.** *"저장만 막고 승인·속성값이 열려 있으면 신고 구간 판정이 해소 후 그대로 확정돼 게이트가 우회된다"*

### ★ orphan 계상 기준 (D010 의 23 vs 27 분기를 닫는다)
(a) 모집단 = `belongs_to_domain==DOMAIN-012` **AND** 활성 = **5** ·
(b) 매핑 = 전 프로젝트 DFEAT 의 `implemented_by_endpoints` 합집합(**도메인 무관**) ·
(c) **타 도메인 소유 API 는 D012 DFEAT 가 claim 해도 세지 않는다**(`API-091`·`API-032` 분자·분모 양쪽 제외) ·
(d) deprecated 4건 제외 ·
(e) SCREEN·SEQ 의 consumes 는 DFEAT 매핑을 대체하지 않음
⇒ **5/5(100%)**. 기준 (c) 를 넓혀도 **분모 7 / orphan 5**.

### ★ `integration_spec` 전량 조회 — **이 축은 갭 없음**
**4건 전량**: **`INTSPEC-001`(KPST 비식별 규격, `approved`·v5·08-07·`stale=false`)** ⇒ ★**비식별 외부 연동 규격은 실재하고 최신**이다.
나머지 3건은 타 도메인(`INTSPEC-002`·`003` VLM · `004` export). **`INTSPEC-003` 은 이관 권고**(이미 별도 보고).
⇒ **"도메인 필터로 0건이 나오는 사각"이 실증됐다.**

### ★ 주입 정책 13개 대조 결과
**커버 OK 10건**(1~8·10·11) · **부분 9건**(→`COV-006`) · **정책 12는 `UC-016` 이 보유**(*"`VDO_FRM_NO` 로 추출한다 — `FRM_NO` 로 추출하면 비식별 영상 맨 앞을 뽑아 붙인다"*, 레거시 NULL skip 포함) ⇒ **커버 갭 아님** · **정책 13(파일 열기 규약)은 D012 자산에 없으나 이 도메인 책임 밖**이라 갭 미판정(소유 도메인 확정 필요).
★**이번엔 주입 전제가 아니라 도메인 ITEM 쪽이 낡았다**(정책 11).

### coverage 가 넘긴 단서
- ★**`UC-016.realizes_features=['FEAT-006']` 인데 도메인 description 은 *"FEAT-006 … 폐기"*** 라고 선언. **실제 `FEAT-006` 은 `status=approved`** ⇒ **3자 불일치**
- `DFEAT-042.related_acceptances` 3건 중 **`AC-012`·`AC-015` 가 deprecated**(활성은 `AC-013` 하나)
- `SCREEN-032.realizes_use_cases=[]` — 도메인 유일 활성 화면이 UC 를 하나도 realize 하지 않음
- **`UC-016` 이 명시한 이벤트 3종 중 `DeidentStageResumeEvent` 만 EVT ITEM 이 없다**
- ★**`NFR-008` 이 또 나왔다** — description 이 *"제작(**비식별**→마킹→오토라벨링) 단계별"* 인데 `applies_to_domains` 에 DOMAIN-012 없음(**D011 과 동일 패턴**)
- ★**`UC-013`·`UC-016` 은 TEST 커버 0건** — 특히 신고·해소는 **게이트 12경로 + 단계별 재개**를 가진 최대 복잡도 축

### coverage 미확인
**COV-6 전면 skip**(1차 grep OFF — ★`brownfield.diff_summary` 가 스스로 *"[추정 — 1차 도메인 경계 확인 필요]"* 라 **이 축은 실측 근거 0**) ·
NFR 역방향 **14건 중 5건만 열람**(9건 미확인)

---

## links (16건 — P0 4 / P1 11 / P2 1)

### ★★★ orphan 계상 분기가 **명시적으로 해소**됐다 (D010 의 23 vs 27 문제)

| 차원 | 기준 | 결과 |
|---|---|---|
| coverage | **DFEAT 매핑만** 인정 | **5/5 (100%)** |
| links | **SCREEN·SEQ 의 consumes 도 인정** | **1건**(`API-112`) |

**둘 다 자기 기준을 명시했고 모순이 아니다** — `API-094`·`109` 는 `SCREEN-032`·`SEQ-014` 가, `API-183`·`184` 는 `SCREEN-005` 가 소비한다. **다만 5건 전부 DFEAT 매핑은 0**이다.
⇒ **최종 REPORT 는 두 수치를 기준과 함께 병기**한다(택일 금지).

### ★★ 판정 요청 — **`FEAT-006` 이 폐기인가 활성인가** (3자 모순)

| 주장 | 근거 |
|---|---|
| **폐기** | `DOMAIN-012.description` · `DFEAT-042.description` 이 *"FEAT-006 … 폐기"* 라고 **명시** |
| **활성** | **`FEAT-006.status = approved`** · `UC-016.realizes_features=["FEAT-006"]` · `MOD-005` 도 참조 · **본문이 현행(KPST 폴링)으로 갱신돼 있다** |

⇒ **어느 쪽이냐에 따라 severity 가 갈린다** — 진짜 폐기면 `LNK-013`·`MOD-005` 링크가 **P0 deprecated 참조로 승격**되고, 아니면 두 본문의 *"폐기"* 표기를 지워야 한다. **사용자 판정 필요.**

### P0 4건 — 전부 **deprecated 참조 잔재**

| ID | 내용 |
|---|---|
| `LNK-001` | ★`DFEAT-041.implemented_by_endpoints=["API-053","API-054"]` **둘 다 deprecated** ⇒ 활성 구현 매핑 0. **`DFEAT-041` 자신이 `API-054` 를 *"그 경로는 코드에 존재하지 않는다 — KPST 폴링으로 단일화되어 콜백 경로가 제거됐다"* 고 폐기 선언** |
| `LNK-002` | `DFEAT-042.related_acceptances` 3건 중 **`AC-012`·`AC-015` deprecated**. ★**`DFEAT-042` 본문 자신이 *"(FEAT-006·UC-012·SCREEN-016/017·API-051/052 폐기)"* 라고 적으면서 링크는 남겨 뒀다** |
| `LNK-003` | `MOD-005.realizes_screens=["SCREEN-016","SCREEN-017"]` **둘 다 `[폐기]`**. ★**`MOD-005.stale_reason` 이 *"SCREEN-017의 7개 필드 변경"* — 폐기 화면이 계속 cascade 를 밀어넣고 있다** |
| `LNK-004` | `DFEAT-048` 이 **해소 엔드포인트를 본문에 명시 인용**하면서 `implemented_by_endpoints` 엔 접수 2경로만 ⇒ **신고 라이프사이클의 절반(해소)이 링크에서 누락** |

### P1 11건
`LNK-005` **`API-112` backward 0 — 완전 고립**. ★`SEQ-012` 는 *"재비식별 진입점 = `/v1/videos/{rawSn}/redeident`"* 로 정정했는데 **`DFEAT-041` 은 아직 구 경로(`/reprocess`, 202)를 진입점으로 적는다** ·
`LNK-006` 활성 API 5건 + `API-032` 의 **`implements_features` 전부 공란**. ★**대조군 `API-091` 은 `["FEAT-005"]` 로 채워져 있다**(같은 DFEAT 의 다른 엔드포인트) ·
`LNK-007` `UC-016` → `SCREEN-032` 는 선언, **역방향 `realizes_use_cases=[]`** ⇒ weak `references` 한 방향만 남음(같은 backward 의 `SD-021` designs·`NAV-001` navigates_to 는 정상) ·
`LNK-008`·`016` `SEQ-012`·`SEQ-013` **`invokes_apis=[]`**(본문·source 엔 경로 명시) ·
`LNK-009` `SEQ-014` 가 **`alt 마킹 단계` 로 2경로를 그려 놓고** `invokes_apis` 엔 라벨링만 ·
`LNK-010` ★**`EVT-007`·`EVT-008` backward 0** — `SEQ-014.publishes_events=[]`·`DFEAT-048.triggers=[]` 인데 **본문은 이벤트 3종을 상세 서술** ·
`LNK-011` `SEQ-012` 가 KPST 위탁·폴링을 통째로 그리면서 **`referenced_integrations=[]`** ⇒ **`INT-005` backward 0(완전 고립)** ·
`LNK-012` **`UC-011`·`UC-013` 이 `realizes_features`·`realizes_dfeats` 둘 다 공란**(`priority=must` 인데) ·
`LNK-013` `UC-016` 이 **`FEAT-006` 만 선언** — 그런데 **FEAT-006 을 specialize 하는 DFEAT 가 0건**이라 구현 단위로 내려갈 경로 없음 ·
`LNK-014` **`LS_DEIDENT_PROC_LOG` 를 `persists_in_tables` 로 선언한 DFEAT 0건**(`DFEAT-041` 본문은 반복 인용)

### P2 1건
`LNK-015` `DFEAT-041`·`042` 만 `decided_by` 공란(형제 3건은 보유). ★**단 후보 `ADR-006` 본문에 폐기 조항이 남아 있다** — *"실패 시 … **재시도 큐**, 원본은 절대 삭제하지 않는 원 정책은 유지한다"*. **D011 의 `ADR-008` 과 같은 유형** ⇒ **ADR 정정이 선행**돼야 한다.

### ★ 플랫폼 사양 확인 — 오탐 회피
★**`get_item_schema(domain_feature).workflow_notes` 실측**: *"`persists_in_tables` 는 ERD 테이블 **이름(텍스트)**. ITEM 매칭 안 되므로 **그래프 링크 생성하지 않음**"*
⇒ **`get_neighbors(ERD-017).backward=[]` 는 결함이 아니다.** `LNK-014` 는 **텍스트 대조로만** 판정했다.
★**`erd` 의 테이블 필드는 `entities` 가 아니라 `tables`** — 첫 조회에서 빈 결과를 받았으나 **스키마 확인 후 정정**(부분 read 함정 회피 성공). `get_item_schema` **5회 호출**.

### ★ 억지 gap 배제 2건
`SEQ-012` 가 `UC-016` 을 인용하나 `realizes_use_cases=[UC-011]` 만 — **상호참조로 판단해 미보고** ·
`UC-011`·`UC-013` 의 `related_screens` 단방향 2건 — **타 도메인 소유 화면**이라 중복 계상 회피(D003·D014 감사와 대조 필요)

### links 미확인
**`links.unresolved` 실수치 측정 불가**(쓰기 응답 전용 · read-only 감사) — **대리 측정만** 수행 ·
`SEQ-013` 의 `PUT /v1/manage/configs/{key}` **API ID 미확정**(추정 금지) ·
**`STATE-001` 은 forward·backward 둘 다 `[]` 로 완전 고립 + `domain_id` 없음** — 비식별 접점이 있으나 주 소유가 배치 축이라 **D012 gap 미계상**(★**어느 도메인이든 이 링크 0건을 처리해야 한다**)

---

## stale (18건 — P0 8 / P1 6 / P2 4)

### ★★★★ 드리프트의 **메커니즘**을 찾았다 (D012-STL-018)

**2026-08-07 06:32~06:51 (19분) 사이 이 도메인 ITEM 17건이 동일 정형문으로 스탬프됐다:**
> *"연결 ITEM 갱신에 따른 재확인 — **바꿀 내용이 없어 내용 변경 없이 확인만 기록한다**"*

대상: `AC-019`·`AC-011`·`AC-016`·`DFEAT-041`·`DFEAT-042`·`FEAT-006`·`UC-011`·`UC-013`·`UC-016`·`TEST-001`·`SEQ-012`·`SEQ-013`·`MOD-005`·`API-051`~`054`

**그런데 그 시점에 실제로는 폐기 정책이 본문에 남아 있었다 — 최소 3건에서 "바꿀 내용이 없다"가 사실과 달랐다:**
- `TEST-001` 은 **전날(08-06) ERD 가 정정한 폐기 컬럼명**을 그대로 보유
- `AC-016` 은 **제거된 콜백 서비스** 인용 보유
- `AC-016`·`UC-016` 은 **같은 날 15:42 확정될 반전**을 이후에도 못 받음

⇒ ★**이 스탬프가 컷오프가 되어, 그 이후(08-07 오후·08-10·08-11) 확정분이 도메인 ITEM 대부분에 도달하지 못했다.**
**"확인했다"는 기록이 드리프트를 덮은 것이다.** 이것은 D001~D011 에서 반복 관측된 *"최근 손댄 ITEM 에만 도달"* 패턴의 **원인 쪽 설명**이다.

### ★★ 갱신 시각 대조 — 확정 5건의 전파율

| 확정 | 반영 | 미반영 |
|---|---|---|
| 08-04 3필드 리셋 폐기 | `DFEAT-048`·`UC-016`·`AC-016`·`SEQ-014` | ★**`DOMAIN-012`(같은 날짜인데 change_summary 5항목에 없음)** |
| 08-06 ERD 컬럼명 정정 | — | **`TEST-001`**(다음날 "바꿀 내용 없음"으로 통과) |
| **08-07T15:42 resolve 반전** | **`EVT-008` 단 1건** | `UC-016`(8h53m 빠름) · `AC-016` · ★**`SEQ-014`(08-10 으로 더 늦은데도 미정정)** |
| 08-10 승인 영상 412 | `DFEAT-048`·`SEQ-014` | `UC-016`·`AC-016`·`DOMAIN-012` |
| 08-11 판정축 "상태→이력" | — | **4건 전부** |

★**최소 비용 경로**: `SEQ-014`·`DFEAT-048`(08-10)을 정본으로 `UC-016`·`AC-016`·`DOMAIN-012` 를 **역방향 정합**.

### P0 8건

| ID | 요지 |
|---|---|
| `STL-001` | `DOMAIN-012`(**`stale=false`**) 본문·용어사전이 **폐기된 3필드 리셋**을 현행으로 서술 |
| `STL-002` | ★★**`ADR-006`(활성·`approved`·`stale=false`)이 폐기 3정책 보유** — 게이팅·콜백·재시도 큐. **`"원 정책은 유지한다"` 고 명시.** `DOMAIN-012`·`UC-011`·`UC-013` 의 `decided_by` 근거라 **따라 구현하면 폐기 3정책이 재도입된다**(D011 `ADR-008` 동형) |
| `STL-003` | `UC-016`·`AC-016` 에 *"**검수 완료 영상 신고가 정상 동선**"* + *"APPROVED 영상 신고 시 `TASK_MODIFIED` 발행"* 잔존(08-10 폐기) |
| `STL-004` | **resolve 반전 미전파 3건** — 아직 *"해소 즉시 export 재산출·관제 재통지"* |
| `STL-005` | ★**`TEST-001` 검증 SQL 이 실행 불가** — `KPST_PRJ_ID`·`POLL_ATTEMPT_CNT` 는 ERD 가 *"실재하지 않는 이름"* 이라고 정정한 컬럼(실물은 `DE_IDNTF_PJT_ID`·`POLL_ATMPT_CNT`) |
| `STL-006`~`008` | deprecated 참조 3건(**links 와 중복 — dedupe**). 단 `STL-008` 은 신규: **`UC-011`·`TEST-001` 이 `[폐기] SCREEN-007` 을** `related_screens`·`exercises_screens`·**`screen_ref` 2곳**에서 참조 |

### P1 6건
`STL-009` `DOMAIN-012` 가 차단을 *"**조회만**"* 으로 좁게 서술(현행은 저장·PUT 까지 412) + 승인 이력 차단 누락 ·
`STL-010` ★**`FEAT-006` 3자 모순의 정답** — *"실제로 폐기된 것은 `UC-012`·`SCREEN-016/017`·`API-051/052` 이고, **`FEAT-006` 은 deep 검토 폐기 + 경량 책임 유지로 축소·재정의**된 것"*. ⇒ **links 의 판정 요청에 답이 나왔다 — 활성이 맞고 두 본문의 "폐기" 표기가 틀렸다** ·
`STL-011` **`ADR-022` 가 해소 이벤트를 "2종"으로 확정 열거** — 현행 3종(`DeidentStageResumeEvent` 는 그 ADR 보다 나중 신설) ·
`STL-012` **판정축 "현재 상태→승인 이력" 확대가 4건 전부 미반영** ·
`STL-013` `AC-016.notes` 가 **제거된 `DeidentifyResultService`** 를 이력 기록 근거로 인용 ·
`STL-014` `DOMAIN-012.brownfield.status=modified` 인데 **`legacy_source` 부재** + *"[추정 — 1차 도메인 경계 확인 필요]"* 미해소

### P2 4건
`STL-015` stale 플래그 실영향 **없음 2건**(`UC-011`·`UC-013`) — ★**전역 잡음 확인: 활성 UC 25건 중 24건이 `stale=true`** 라 이 도메인 고유 문제 아님 ·
`STL-016` **11건이 `planned/0`** 인데 형제 3건은 `implemented/100`, 본문은 *"구현됐다"* 명시 ·
`STL-017` `ERD-017` `status=new` 인데 `legacy_source` 존재(identifier 없음) ·
`STL-018` 위 일괄 스탬프

### ★★ stale=false 인데 본문이 폐기 모델 — **4건** (플래그로 검출 불가)
**`DOMAIN-012`** · **`ADR-006`** · **`AC-016`** · **`ADR-022`** ⇒ D009~D011 패턴 재확인.
**30일 초과 stale 0건** — STL-1 의 P0 승격 해당 없음.

### ★ 보안 관측 (content/policy 로 이관)
**`TEST-001` steps[2].note 에 외부 KPST 실서버 IP·포트가 평문**으로 박혀 있다.

### ★ `STATE-001` — 비식별 축에서도 P0 급 (D011 중복이라 미계상)
*"신 설계(**타깃, planned**): 비식별화가 마킹 선행 자동 단계로 이동 … **현재 코드는 마킹(원본) 트리거→VLM→비식별 순**"*
★**추가 발견**: mermaid `source` 에 **`MARKING_READY` 상태가 아예 없다**(`PENDING --> BATCH_QUEUED`). **2026-06-02 이후 2개월 반 무갱신.**

### stale 미확인 사각
★**`INTSPEC-001` 본문 미정독** — `stale=false`·*"바꿀 내용 없음"* 재확인이지만 **그것이 08-07 일괄 스탬프 대상**이라 **컷오프 위험군에 그대로 해당**. ⇒ **다음 라운드 최우선 정독 대상** ·
`SEQ-001`·`012`·`013` · 활성 API 5건 **본문 미정독**(제목·change_summary 상 현행이나 **D011 교훈상 사각**) ·
`MOD-005` 본문 미조회 · `INTSPEC-004`(export 축)와 `STL-004` 교차 미확인

---

## schema (13건 — P0 5 / P1 6 / P2 2)

### P0 5건

**`D012-SCH-001`** — ★**ERD 에 실물 컬럼 6종이 통째로 없다**(R14 비식별 리포트 축). ERD 22 ↔ **실물 28**.
누락: `face_dtct_cnt` · `noplt_dtct_cnt` · `frme_cnt` · `prcs_bgng_dt` · `prcs_end_dt` · `rpt_file_path_nm`
★**휴면 코드 아님** — 프로덕션 경로가 `retrieveReport` 를 호출하고 **API 응답으로도 나간다**(`VideoDetailResponse`).
★`rpt_file_path_nm` COMMENT 가 함정을 명시: *"**결과 파일명이 아니라 원본 입력파일의 절대경로**다"*

**`D012-SCH-002`** — **FK `on_delete` 가 정반대** — ERD `restrict` ↔ 실물 **`CASCADE`**(두 테이블 모두).
★*"`restrict` 는 '이력이 영상 삭제를 막는다', `CASCADE` 는 '영상을 지우면 이력·신고가 함께 사라진다'로 **의미가 반대**"* — **파생영상 폐기 배치가 명시 DELETE 목록을 짤 때 이 값을 근거로 삼는다.**

**`D012-SCH-003`** — `POLL_STTS_CD` 에 **`FAILED` 누락**(3종 ↔ 실물 4종). ★**누락된 값이 바로 *"무한 재폴링·중복 다운로드를 막는 종결 상태"*** ⇒ 계약대로 구현하면 **그 방어선이 통째로 빠진다**(D011 유형 3 동형).

**`D012-SCH-004`** — `API-112` 가 **제거된 필드 `kpstPrjId`** 를 202 계약으로 공표. ★**DTO javadoc 이 제거를 명시**: *"논블로킹 제출로 응답 시점에는 프로젝트 ID 가 **존재할 수 없게** 됐다(항상 null) … FE 사용처 0건 확인 후 제거"* ⇒ **소비자가 영원히 오지 않는 값을 기다린다.**

**`D012-SCH-005`** — `INTSPEC-001` 이 **실재하지 않는 물리명** `KPST_PRJ_ID`/`DATASET_ID` 공표. ★**`ERD-017` 은 v16(08-06)에서 *바로 그 결함*을 자기 설명에서 고쳤는데 규격 ITEM 만 남았다** — *"설명이 곧 검색·인용의 진입점이므로 정정한다"* 고 적어 놓고 규격은 안 고침.

### P1 6건
`SCH-006` `API-094` 가 **실재하지 않는 `LS_DEIDENT_REPORT.RPRT_SN`** 인용(실물 `DEIDENT_REPORT_SN`). ★**형제 `API-109` 는 이미 맞게 적는다** ·
`SCH-007` ERD `indexes[]` 에 **복합 인덱스 `(DATA_RAW_SN, PROC_LOG_SN)` 누락**(4 ↔ 실물 5) ·
`SCH-008` **`LS_DEIDENT_PROC_LOG` 를 `persists_in_tables` 로 선언한 DFEAT 0건** — ★`DFEAT-041` 본문은 그 테이블 쓰기를 명시하는데 구조화 필드엔 없다 ·
**`SCH-009`** ★**표준용어 위반 4건** — `DEIDENT_REPORT_SN`·`REPORTER_NO`·`REPORT_STTS_CD`·`RESOLVED_DT`. **행안부에 `DCLR`(신고)·`DCL`(신고자)·`RSLV`(해소)가 등록돼 있는데 미등록 영문 원형을 그대로 사용.** ★**뒷받침 사업용어 4건이 전부 `KLID-저작도구 ERD-017` 자기등록**이라 독립 근거 아님. ★**같은 테이블 안에 `DCLR_DT`·`DCLR_STP_CD`(DCLR) 와 `REPORT_STTS_CD`(REPORT) 가 공존** ·
`SCH-010` `RSN` 이 **3갈래로 어긋남** — 논리명 "신고사유"의 행안부 표준은 **`DCLR_RSN`(내용V4000)** / 차용한 `RSN` 은 사업표준상 **"사유"(다른 용어)** / 그 도메인 **V500 ≠ 실제 V1000**. ⚠**엔티티 javadoc 은 길이 이탈을 의식적 선택으로 적으나 ERD 엔 그 사유가 없다** ·
**`SCH-011`** ★★**`INTSPEC-001` 이 `/retrieve_report` 를 "미연동"으로 공표하는데 실제로는 프로덕션 배선돼 있다** — *"핵심 4종만 사용 … 리포트·수동비식별·프레임조회·로그도 **현재 미연동**"*. **그 응답이 바로 `SCH-001` 누락 6컬럼의 원천**이고, **그것을 담는 `integration_point` ITEM 자체가 없다.**

### P2 2건
`SCH-012` 4계층 잔여 3건 — **`DCLR_STP_CD` 양쪽 사전 미등록**(구성 단어는 전부 등록, 조합만 미등록) · **`REQ_DT` 논리명"요청일시"의 표준은 `DMND_DT`**(★`REQ` 의 행안부 등록 의미는 **"소요"** 이지 "요청"이 아니다) · `DATA_RAW_SN` 논리명 임의 표기 ·
`SCH-013` `API-094` `fileName` 에 **구현이 강제하는 `maxLength 255` 없음**(★javadoc: *"CWE-770 방어"* — 의도된 방어선인데 계약 미노출)

### ★ CSV 정본 대조 실적 (6파일 전량)
공통 3,284/123/13,176 · 사업 471/45/1,373 — **MCP 검색 미사용**(미등록 오판 위험).
**컬럼 28건 + 물리약어 역방향 35건 양방향 대조**, 한글 역검색 8종.
★**자기등록 13건 배제**(출처 `KLID-저작도구 ERD-017`). ★**독립 근거로 채택**: 행안부 전체 · 출처 공란(KLID-BM 배포분) · 타 프로젝트 출처.

### ★ schema 가 넘긴 단서
- ★**`API-112.change_summary` 가 *"바꿀 내용이 없어 확인만 기록"* 인데 그 시점에 이미 `kpstPrjId` 가 제거돼 있었다** ⇒ **stale 이 찾은 일괄 스탬프 메커니즘의 4번째 실증**
- **`GET .../deident-candidates` 는 구현돼 있는데 `api_endpoint` ITEM 이 없다** ⇒ `API-094` 의 `fileName` **후보 조달 경로가 계약에서 끊긴다**
- DFEAT 3건 전부 **`emits_events` 미보유**인데 `EVT-007`·`008` 이 존재
- `API-109`·`API-184` 가 `planned` 인데 **둘 다 구현·배선 완료**
- (코드 주석) `VideoPrivacyMetaController` `@ApiResponses` 에 **412 없음** — Swagger 만 뒤처짐

### ★ 갭 0 판정 (근거 있음)
★**"파일명 고정 공표" 갭 0건** — `ERD-017`·`DFEAT-041`·`API-094`·`INT-005` **전부** *"적재값을 읽고 조합·추측하지 않는다"* 로 정확히 서술. **주입 정책 4번은 완전 커버.**
`SCH-5`(SEQ↔ERD) — **DOMAIN-012 에 `diagram_sequence` 0건**으로 조회돼 대상 없음(⚠ **coverage 는 3건으로 셌다 — 필터 차이, dedupe 시 확인**).

### schema 미확인
★**CSV 스냅샷이 2026-08-05 자** — 이후 등록분은 "미등록"으로 잡힌다(**`FACE` 는 R14 에서 신규 등록된 기록이 있어 `SCH-001` 의 미등록 표기는 시점 한정**) ·
★**사업표준단어 "비식별"의 등록 약어는 `ANONY`** 인데 이 도메인은 `DE_IDNTF` 사용 — **그러나 `ANONY` 는 이 저장소에서 이미 "익명" 값으로 광범위 사용**되어 사전대로 바꾸면 의미 충돌 ⇒ **갭 미보고, 사용자 판단** ·
`docs/v2-wiki/22-deid-solution-api.md` 미개봉

---

## policy (9건 — P0 5 / P1 3 / P2 1)

### P0 5건

**`D012-POL-001`** — ★**`ADR-006`(활성·유일한 비식별 결정 기록)이 폐기 3정책 보유**.
★**정면 역전**: `DFEAT-041` 이 *"[폐기된 구 서술] ①게이팅 폐지 ②콜백 경로 제거 ③재시도 큐 폐기"* 로 **열거한 바로 그 3건을**, 그 DFEAT 가 **근거로 인용하는 ADR** 이 *"원 정책은 **유지한다**"* 로 규정.

**`D012-POL-002`** — ★★**`CMP-003` — description 은 고쳤는데 구조 층이 안 고쳐졌다**.
description: *"[★결과 수신 = 폴링 (구 '콜백' 서술 폐기)]"* · *"자동 재비식별 큐는 없다"*
그런데 **title `비식별 컴포넌트 (위탁·콜백·리포트)`** + `components[]` 에 **콜백 수신 컨트롤러·서비스** + `relationships[]` 에 **`콜백 (HTTP)`·`재트라이 큐 적재`** + `external_dependencies[]` 에 **`BatchRetryQueue`** 가 **그대로**.
★**대조군 `CDIAG-003`(v7)은 구조 배열까지 전부 정합** ⇒ `CMP-003` 만 산문/구조 층이 갈렸다.
⚠**`CMP-003` 은 coverage 인벤토리에 없었다** — policy 가 발견.

**`D012-POL-003`** — `UC-011` main_flow 가 **폐기된 업로드형 연동**(`POST /upload` → `POST /project` → `GET /download`)을 정상 시나리오로 규정.
★**`ADR-006` 자신이 그것을 `considered_options[1] "업로드형 외부 연동(구 설계, 폐기)"` 로 기각**했고 **실측 계약(`INT-004`)에 `/upload`·`/download` 는 존재하지 않는다**(*"파일 본문 미전송 — 경로로만 주고받는다"*).

**`D012-POL-004`** — `DFEAT-041` → **deprecated `API-054`**(콜백) 링크(**links·stale 과 중복 — dedupe**)

**`D012-POL-009`** — ★★**`ADR-032`(활성·v3·**08-07 갱신**)가 2026-08-04 *"되돌리지 말 것"* 으로 폐기된 정책 보유**:
> *"② … 원천(orgnl) 산출물은 **3필드 전부 null**(판정하지 않음 …)"*

★**그 ADR 자신의 `risks[1]` 이 충돌을 기록**: *"ADR-042 계획의 Phase 6 는 본 2026-08-03 확정과 **정면 충돌한다**"*.
⇒ **폐기(08-04) 이후인 08-07 에 갱신됐는데도 폐기본을 그대로 보유.**

### P1 3건
`POL-005` **`ADR-022`(신고 게이트 단일 진실원)가 차단 범위를 12종보다 6항목 좁게** 열거 + 접수 3조건(`MARKING_READY` 한정·승인 이력·파생영상) **미규정**. ★*"이 ADR 만 읽고 구현하면 구 동작이 재현된다"* ·
`POL-006` `DFEAT-048` 판정축이 *"검수가 승인된 영상"* 이라 **현재 상태 판정과 구분되지 않는다** — ★**v11(08-10) < 정책 확정(08-12)** ·
`POL-007` **`API-112` 가 승인 영상 재비식별을 정상 동선으로 기술** — 확정 19(조치 수단 0)와 반대. ★*"'기비식별'이 요구 조건인지 배제 조건인지 문면상 갈리지 않는다"* ⇒ **그 모호성 때문에 P0 아닌 P1**

### P2 1건
`POL-008` ★**핵심 확정 5건(선두·게이팅 폐지·논블로킹·폴링 단일화·큐 폐기)을 기록한 ADR 이 프로젝트에 0건**. **ADR 45건 제목 전수 확인** 결과 없고, 비식별 ADR 7건 중 **`ADR-006` 은 정반대를 규정**.

### ★ POL-2~POL-7 전량 skip — **각각 근거 명시**
`ADR-045`=증강 2축 분리(**BFF 개념 부재**) · **`ADR-051` 미존재**(최대 45) · `ADR-028`=증강 dedup(**superseded**) · `ADR-027`=재비식별 게이트(**x-access-token 부재**) · `ADR-036`=event_annotation(**중계서버 부재**) · `ADR-038`=목록 정렬(**`EV99999999` 0건**).
**ADR 45건 제목 전수 + 본문 9건 열람.** 위반: `ADR-006`·`ADR-032`·`ADR-022` / **정합 확인: `ADR-023`·`024`·`025`·`026`·`027`**.

### ★ 19개 정책 매트릭스 요약
**COVER 9** — ①선두 ③논블로킹 ⑦단계분화 ⑨파생 바깥 ⑪no-store ⑫3필드 보존 ⑬라벨 보존 ⑭판정범위(★**전파 부활 잔재 0건 — 철회 사유까지 ADR 에 기록됨**) ·
**VIOLATION 6** — ②⑤⑥⑧⑩⑲ ·
**부분 1** — ④(`UC-011` 만) ·
**미검증 4** — ⑮(뷰·통지 축 미개봉) ⑯(EVT-008 본문 필요) ⑰⑱(대상 ITEM 미개봉/타 도메인)

### policy 미확인
★**미개봉 16 ITEM 명시**(`ERD-017`·`SCREEN-032`·`EVT-007`·`008`·`MOD-005`·`INT-005`·API 8종 등) — *"검사하지 않았다(0건이 아니다)"* ·
**ADR 45건 중 36건 본문 미확인** · 정적 렌더 미러·위키·테스트케이스·코드 층 미검사

---

## acceptance (8건 — P0 0 / P1 5 / P2 3)

### ★★ 구조적 발견 — **AC 4건이 도메인 스코프 뷰에서 아예 안 보인다**
`AC-011`·`013`·`016`·`019` 의 **`derived_domain_ids` 가 전부 `[]`**.
★**대조군 `AC-023` 은 `UC-021` 경유로 `[DOMAIN-010]` 을 자동 획득**한다.
⇒ 원인은 AC 가 아니라 **도메인 UC 3건(`UC-011`·`013`·`016`) 전부 `belongs_to_domain` 미설정**.
★**수정은 AC 가 아니라 UC 에 해야 한다.** (coverage 는 `UC-016` 만 지목했으나 **3건 전부**였다)

### ★ 15개 정책 매트릭스 — **✔8 / △3 / ✘3**
**✔완전 8** — ①선두·`MARKING_READY` ②게이팅 폐지 ③논블로킹(★**ACK 창 30분 / 콜백 창 360분 구분 + 원자 클레임**까지) ④**파일명 조합 금지**(★`mock=deidentified.mp4` / `KPST={stem}-mask{ext}` 병기) ⑤폴링 단일화 ⑥큐 폐기 ⑦**단계별 재개 분기** ⑫3필드·라벨 보존 ⑬원자 클레임
**△부분 3** — ⑨파생(정방향만, **역방향 무전파 없음**) ⑩차단 **12종 중 7종** ⑪`no-store` **원칙만·5경로 미열거**
**✘미커버 3** — ⑧승인 이력 판정 ⑭`VDO_FRM_NO`·NULL skip ⑮조치 수단 0

### P1 5건
`ACC-001` **`AC-011`·`AC-013` 에 `verifies` 키 자체가 없다** — ★**1:1 주제 대응이 기계적으로 성립**(`REQ-012`↔`AC-011` · `REQ-015`↔`AC-013`)하고 **형제 `AC-016`·`AC-019` 는 이미 채워져 있다** ·
`ACC-002` `REQ-013`(솔루션 API·가이드 연동) **검증 AC 도메인 전체 0건** — ★후보 `AC-011` 은 *"규격 준수"* 축이 아니라 위탁 동작만 판정 ·
`ACC-003` ★**행위 표면이 가장 큰 `DFEAT-048` 이 AC 0건**(직접·UC 경유 모두). **실질 검증 AC 는 존재하는데**(`AC-016`·`AC-019`) **연결만 없다** ·
`ACC-004` ★**`DFEAT-042` 의 본체 명제(게이팅 폐지)를 검증하는 AC 가 그 DFEAT 에 없다** — 유일한 활성 연결 `AC-013` 은 옵션 설정만 판정하고, **그 명제를 실제로 검증하는 문장은 `AC-011`** 에 있는데 `DFEAT-041` 에만 연결 ·
`ACC-005` 차단 **5종 + 행위 중립 문구** 미검증(포털·export·라벨 속성값·이벤트 어노테이션·검수 승인). ⚠★**`UC-016`·`DFEAT-048`(v11, 08-10)도 6종만 열거** ⇒ **AC 만 고쳐서는 정합되지 않는다**

### P2 3건
`ACC-006` **negative AC 0건** — `and_examples` 로 부분 대체(`AC-016` 5건·`AC-011` 3건)이나 ★**`AC-013`·`AC-019` 는 `and_examples` 조차 0** ⇒ **`REQ-015`·`REQ-021` 은 어떤 거부 분기도 검증되지 않는다** ·
`ACC-007` `AC-019` 가 **given/when/then 각 1줄로 위탁·이력·신고·해소·원본보존 전체를 압축** — ★*"한 when 안에 서로 다른 두 흐름이 접합돼 **어느 쪽이 실패해도 같은 then 으로 귀결**"* ·
`ACC-008` ★**AC 가 스스로 미정의를 선언** — `AC-013.notes`: *"구체 옵션 항목 **미정의**(서브에이전트 GAPS) — 옵션 스펙 확정 시 시나리오 보강 필요"*. `verification_method=manual_test` 인데 **판정 항목이 없어 수동 검증도 성립 안 함**

### ★ AC→REQ 추적 — **6건 중 3건만 도달**
**도달**: `REQ-014`·`REQ-016` ← `AC-016` / `REQ-021` ← `AC-019`
**미도달**: **`REQ-012`(SFR-09-01 핵심)** · `REQ-013` · `REQ-015`
★**REQ→RFP 상단은 정상**(전부 `RFP-004` derived_from) ⇒ **끊긴 지점은 AC↔REQ 한 칸뿐**.
★**AC↔UC 양방향 대칭은 전건 정상** — `ACC-007` 룰 해당 없음.

### acceptance 미확인
AC 4건 전부 `implementation.status=planned`(`AC-016` 은 `unverified`) — 실제 테스트 존재 여부 판정 불가 ·
정책 ⑩⑪⑫ 확정 시각과 `AC-016` v5 갱신 시각의 선후를 메타만으로 확정 못 함(★**단 더 나중인 `DFEAT-048` v11 에도 없어 "도메인 전반 미반영"으로 판정**) ·
`AC-023` 은 `derived_domain_ids=[DOMAIN-010]` 이라 **비식별 AC 로 미계상**(귀속 모호 시 skip)

---

## requirement (6건 — P0 1 / P1 3 / P2 2)

### ★ 주입 전제 검증 — **이번엔 맞았다** (D011 의 오귀속 재발 없음)
추적표 정본을 **직접 열어 확인**: L57~61 이 `RQ-SFR-09-01~05` 를 SFR-09 로, L64 가 `RQ-SFR-11-06`(=`REQ-021`)을 SFR-11 로 명시.
⇒ **주 상위 = `RFP-004`(SFR-09) 확정 · 보조 = `RFP-005` 1건.**
★추가 확인: L142~143 대외 인터페이스가 **`비식별 처리 위탁`(송신) · `비식별 진행 상태 조회`(송신, 주기)** 로 기재 ⇒ **폴링 단일화 정책과 정합.**

### ★ `RFP-004` 세부 7항목 대조 — **RQ-004(미하향) 0건**
heading1 5항목 **전부 활성 REQ 로 하향 완료**(`REQ-012`~`016`) · heading2 2항목은 **비책임 판정으로 폐기**(`REQ-017`·`018`).

### P0 1건
**`D012-RQ-001`** — **`REQ-014.constraints[8]`**(`priority=must`)가 **08-10 폐기된 모델** 보유:
> *"검수 완료(승인) 영상을 신고하면 상위 시스템에 **수정 통지를 발행**하고, 해소 시 산출물을 재산출해 **재통지**한다"*

현행에서는 **접수 자체가 412 라 도달 불가**이고 발행 분기도 제거됐다.
★**같은 REQ 의 다른 조항(마킹 `MARKING_READY` 한정·파생 412·라벨/판정 보존)은 현행과 일치** — **이 1개 조항만 stale**.

### P1 3건
`RQ-002` ★**`REQ-015` 가 RFP 원문을 그대로 복창**(*"관제지원시스템 UI로 구현"*)하는데 **확정 설계는 저작도구 전용 화면 없이 외부 솔루션 화면을 쓴다**(추적표 주1). **divergence 미명시** — `AC-013` 도 *"관리 화면에서"* 로 저작도구 화면을 전제 ·
`RQ-003` `AC-011`·`AC-013` 에 **`verifies` 키 부재**(★**전체 read 로 확인** — 부분 read 함정 회피). ⇒ **`REQ-012`·`013`·`015` 역추적 0**. ★**매핑은 모호하지 않다** — 추적표 L121/L122 가 확정 ·
**`RQ-004`** ★**NFR 14건 전수 확인 완료**(coverage 의 5건 사각 폐쇄). **DOMAIN-012 포함은 `NFR-012` 단 1건**. **`NFR-008` 누락 확정** — 본문이 *"제작(**비식별**→마킹→오토라벨링)"* 으로 비식별을 **첫 번째로 열거**하는데 `applies_to_domains=[D003,D010,D005]` 로 **그 단계만 빠졌다**

### P2 2건
`RQ-005` (advisory) heading2 2항목 비책임이 **활성 REQ 어디에도 없다**. ★★**반증 있음** — *"개인정보 유형 분류체계"* 는 **폐기 당시 "역할 경계 밖" 판정과 달리 현재 저작도구가 보유**한다(`API-183`·`184` 활성 + 도메인 본문 언급) ⇒ **재확인 필요** ·
`RQ-006` `RFP-004.related_requirements` 가 **deprecated 2건을 활성 5건과 구분 없이 나열** ⇒ 커버리지가 **7/7 로 보이나 실제 5/7**

### ★★ requirement 가 찾은 역전 — **활성 API 가 폐기 요구를 구현한다**
`API-183`·`API-184`(privacy-meta GET/PUT)는 **DOMAIN-012 소속 활성 API** 인데, 그 기능의 상위 요구 **`REQ-018`(개인정보 유형 분류체계)은 "역할 경계 밖"으로 폐기**돼 있다.
⇒ coverage 의 **"책임 인벤토리에 없는 API"**(`COV-005`)와 **같은 뿌리**이며, 이쪽이 **원인을 설명**한다.

### ★ NFR 전수 스윕 결과 (누락 아님 판정 근거)
`NFR-009` 열거 기법에 비식별 미언급 · `NFR-011`/`018`/`019` 는 **D012 에 활성 화면이 사실상 없음**(폐기 2 + 옵션 화면 부재) · `NFR-013`/`020` 은 인증 축 · `NFR-014`/`017`/`021` 은 `[]`(**전역 적용 — 누락 아님**) ·
★`NFR-016`(데이터 표준 준수)은 `[D010]` 단독인데 **전 도메인이 동일하게 빠져 있어 D012 고유 갭 아님** ⇒ **프로젝트 전역 이슈로 이관**.

### requirement 미확인
`REQ-015` 의 *"솔루션 규격 확정 시 보완"* 유효성 — ★**KPST 옵션 코드표가 확정됐다는 단서가 있으나 그것을 담은 ITEM 을 못 찾아 이중 인용 요건 미충족** ⇒ 미보고 ·
`REQ-025` 의 *"비식별(가명처리)"* 표기 — 게이팅 폐지 대비 좁히는 잔재 가능성이나 **"가명처리"는 법적 하위 유형이라 모순 단정 불가** ⇒ 단서만 ·
★추적표 주4: **`RQ-SFR-09-03`·`04`·`05` 에 통합시험 시나리오 미배정** ⇒ test_scenario 로 이관 ·
`REQ-021` 이 `planned/0` 인데 **동일 기능이라 스스로 선언한 `REQ-012`·`014` 는 `implemented/100`**

---

## diagram (11건 — P0 1 / P1 9 / P2 1)

### ★★ 차원 간 불일치 해소 — **SEQ 는 0건이 아니라 3건이다**
schema auditor 가 `list_items(diagram_sequence, domain_id=DOMAIN-012)` 로 **0건**을 받았는데, diagram auditor 가 **project-level 14건 전량 조회 후 귀속 판정**해 **`SEQ-012`·`013`·`014` 3건 실재**를 확정했다.
⇒ ★**"필터 미동작에 의한 0건"이며 "시퀀스 없음"이 아니다.** 같은 함정을 ERD 에서도 겪었다(`fields=['entities']` → `data={}`, 정식명은 **`tables`**).

### P0 1건
**`D012-DIA-001`** — `SEQ-014` mermaid + `CMP-003` 이 **폐기된 *"해소 즉시 export 재산출 + 관제 재통지"*** 를 그린다.
```
opt APPROVED 영상
    DRS->>EP: DeidentReportResolvedEvent — export 재산출 + 관제 재통지
```
★**`SEQ-014` v5(08-10)는 `EVT-008` v4(08-07T15:42)보다 3일 뒤 갱신됐는데도 구 동작 유지.**

### P1 9건
`DIA-002` `CMP-003.depicts_dfeats=[]`(★**전체 read 로 확인** — 부분 read 아님) ·
`DIA-003` ★★**`CMP-003` 에 현행 핵심 컴포넌트가 0건** — `SEQ-012`/`014` 가 participant 로 그리는 **7종이 전부 없다**(위탁 서비스·폴링 잡·선두 트리거 브리지·게이트 판정 단일 원천·프레임 재부착·재비식별 진입점·미결 스위퍼). 남은 노드도 구 모델(*"배치 **2단계**"* ↔ 현행 **선두** / 제거된 어댑터 인용) ·
`DIA-004` `CMP-003`·`SEQ-012` 가 **deprecated API 를 폐기 표기 없이 인용** ·
`DIA-005` ★**`CDIAG-003` 속성 8종이 실재하지 않는 이름 계열** — `kpstPrjId`·`kpstDatasetId`·`pollAttemptCnt`·`reqKindCd`(**개명 전**)·`resDt`·`errorCd`·`errorMsg`·`reportDt`. ★**`CDIAG-003` v7(08-07)이 ERD 정정(08-06)보다 뒤인데도 미반영** ·
`DIA-006` **리포트 회수 축이 클래스에 속성 0건** — 어느 다이어그램에도 안 그려짐 ·
`DIA-007` **SEQ 3건 전부 정형 링크 공란** — mermaid 는 API·이벤트·외부 연동을 그리는데 `invokes_apis`·`publishes_events`·`referenced_integrations` 미선언 ·
`DIA-008` **`STATE-001` 비식별 축 본문 stale** ·
`DIA-009` ★**비식별 축 상태 머신 2개가 어느 다이어그램에도 없다**(배치 단계·폴링 상태) ·
`DIA-010` **재비식별 요청·미결 스위퍼·리포트 회수 3흐름이 Note 언급뿐** — `API-112` 는 활성인데 **전용 시퀀스 0건**

### P2 1건
`DIA-011` `SEQ-012` 가 `POLL_STTS`(실물 `POLL_STTS_CD`) · **`SUCCESS`/`FAIL`(등재 코드값은 `SUCCEEDED`/`FAILED`)** 표기

### ★★ 정밀한 오탐 회피 — `STATE-001` 판정
auditor 가 **`STATE-001` 은 `LS_RAW_DATA_STATUS`(작업/검수 축)이지 `LS_DATA_RAW.DATA_STTS_CD`(배치 단계 축)가 아니다** 라고 갈라,
★**mermaid 에 `MARKING_READY` 가 없는 것 자체는 "축이 달라 결함이 아니다"** 로 판정했다(**두 컬럼 책임 분리는 확정 모델과 정합**).
⇒ **D011 diagram 이 `MARKING_READY` 부재를 지적한 것과 결론이 갈리므로 최종 dedupe 시 확인 필요.**
또 `BatchRetryQueue` 인용도 *"일반 배치 재시도 축이라 '자동 재비식별 큐 없음' 정책 위반이 아니다"* 로 **미계상**.

### ★ policy 중복 배제를 명시적으로 수행
`CMP-003` 의 policy 보고 4곳(title·콜백 컴포넌트 2·콜백/재트라이 relationship·`BatchRetryQueue`)을 **재계상하지 않고**, **그 밖의 4축**만 보고.

### diagram 미확인 (중요)
★**`SEQ-001`(영상수집 파이프라인, `approved`·v7·`stale=false`) 미개봉** — *"title 에 '비식별'을 포함하고 파이프라인 선두 단계를 그릴 가능성이 높으나 realizes 축이 DOMAIN-003 이라 제외"*. **비식별 선두 반영 여부가 복제됐을 수 있다** ⇒ D003 또는 별도 라운드 ·
`diagram_c4_context`/`container`/`deployment` 미조회 · 정적 렌더 미러 미확인 ·
`SEQ-013` 의 설정 API ID **특정 실패**(추정 금지) · `SEQ-013` 이 관리자 토큰 게이트를 안 그리는 건 **대상이 마스킹 옵션인지 연동 주소인지 불확정이라 미보고** ·
외부 `GET /`(헬스체크)·`POST /delete_project_id`(**프로덕션 호출부 0건**) 미표현은 **정상일 수 있어 미계상**

---

## content (16건 — P0 4 / P1 9 / P2 3)

### ★★★★ 한글 손상 — **①②③④④b 5종이 전부 놓쳤고 신규 ④d 만 잡았다**

**`SEQ-014.data.source`(mermaid)**: `WARN 감사**로개만**)` ← 정상은 `감사 로그만`
**확정 근거**: 형제 3곳이 전부 정상 표기(`SEQ-014.description`·`DFEAT-048`·`UC-016`) · 토큰 **코퍼스 전체 1회** · 동족 어형 0건.

| 방법 | 결과 | 놓친 이유 |
|---|---|---|
| ① 알려진 오타 15종 | 0건 | 신규 오타 |
| ② 희귀 음절(≤2회) | **98종 전수 확인 → 0건** | `개`·`만` 이 고빈도 |
| ③ 1음절+목적격 | 후보 9건 **전부 오탐** | 해당 패턴 아님 |
| ④ 낱말 고립도 | 174건 확인 → 0건 | ★**접두 kin(`감사`) 존재로 필터링** |
| ④b 절단-빈도비 | 후보 67건 → 0건 | 절단형이 아님 |
| **★④d (신규)** | **1건 검출** | ④가 kin 필터로 제외한 **freq==1·4음절+ 토큰 493건 전수 육안** |

⇒ ★**`④` 의 kin 필터가 구조적 사각이다.** 다음 회차부터 **④d 필수**.
(보조로 시도한 4c(8-gram 편집거리)는 후보 378건 전부 자연 어미 변이라 **신호 대 잡음이 낮아 비추천**)
**누적**: D005 2 · D006 0 · D007 2 · D009 0 · D010 1종3곳 · D011 0 · **D012 1종1곳**

### ★★ IP·포트 전수 — **1건이 아니라 4건**
사전 관측은 `TEST-001` 1건이었으나 **전수 확장으로 `INT-004` 에 3건 추가 발견**:
```
| local | ... | http://222.118.130.251:9989 | mock-mode=true |
| dev   | ... | http://222.118.130.251:9989 | ... |
| prd   | ... | https://127.0.0.1:9201 |
```
★**같은 실서버 IP 가 프로파일 표에 그대로** + `application.yml`·`env.template`·`ca.crt` 배포 파일명 동반.
⚠`INTSPEC-001` 의 `http://IP:port` 는 **마스킹돼 있어 오염 아님**(정확한 판별).

### P0 4건
`CNT-001` 위 IP·포트 4건 · `CNT-002` **코드 경로·`CLAUDE.md` 인용·커밋 해시**(`16bf3a5`·`f2ad2a1`) — ★`SEQ-012`·`014` 가 *"진실원: **CLAUDE.md** 「…」 + 코드(...)"* 로 **레포 문서를 진실원으로 선언** · `CNT-003` 위 한글 손상 · `CNT-004` `TEST-001` 검증 SQL 실행 불가(**stale 과 중복 — dedupe**)

### P1 9건 — 오염 유형별 전수 집계
`CNT-005` **내부 클래스명 87회 / 19 ITEM**(30 중) ·
`CNT-006` **마이그레이션 번호 28회 / 7 ITEM**(`V21`~`V171`) ·
`CNT-007` **CWE 번호 18건 + finding 코드 3건**(`C-ISSUE-22`·`B-ISSUE-28`·`D-ISSUE-25`) ·
`CNT-008` 어노테이션 6건 + ★**프레임워크 런타임 내부**(*"`publishOn` 은 … **reactor-netty 이벤트 루프**로 흘러 JPA 쓰기를…"*) ·
`CNT-009` 구현 상태(`[구현 현황]`·`202 placeholder`). ⚠★**사전 관측 후보 `DFEAT-041·042 의 "(설계 타깃 … planned)"` 은 오염이 아니었다** — *"설계 타깃" 문자열 코퍼스 0건이고 `planned` 75건은 전부 `implementation.status` 스키마 필드* ⇒ **내 관측이 틀렸다** ·
`CNT-010` **자기 리비전 24회 / 11 ITEM** — ★`DFEAT-048` 은 **2,850자 중 상당 부분이 폐기 이력**이고 현행 사양이 그 사이에 끼여 있다 ·
`CNT-011` ★**작업 노트가 본문에** — `INTSPEC-001` 에 *"## ★ 재정합 확인 (2026-07-30) … `current_version` … `stale 플래그 해소`"*, `AC-013.notes` 에 *"**서브에이전트 GAPS**"* ·
`CNT-012` `UC-016` **제목이 본문을 담지 못함** — title 은 *"상태·이력 확인"* 인데 **본문 3,446자의 약 95%가 신고·해소 축**(main_flow 8단계 중 상태 확인은 1개) ·
`CNT-013` **단일 필드 2,000~2,900자**(`DFEAT-048` 2,850 · `SEQ-014` 2,651). ★**비교군 `API-112` 347 · `ERD-017` 306 으로 같은 정보 밀도 유지**

### P2 3건
`CNT-014` `DFEAT-042` **slug 가 구 제목 잔존**(`비식별-결과이력-검토-및-옵션유형-설정`) + 본문이 `DFEAT-041` 과 중복 ⇒ **고유 사양이 한 줄뿐** ·
`CNT-015` `MOD-005` 60자·`AC-013` 시나리오 3줄 동어반복 ·
`CNT-016` ★**동일 문단 6~7 ITEM 복제**("논블로킹 제출" 6곳 · "파일명 조합 금지" 7곳) ⇒ *"한 곳만 갱신되면 나머지가 조용히 낡는 구조이며, **실제로 그렇게 됐다**"*

### ★ 스캔 실적
모집단 **30건 전수**(kit-export count=30, 1:1 일치) · **문자열 1,456건 / 63,820자** · 순회 필드 **30여 경로 재귀**.
★**deprecated 전이 확인**: `API-053`·`SCREEN-016`·`UC-012` 개별 조회 → **활성 본문으로의 전이 흔적 없음**(남은 건 ID 참조뿐 = links/stale 축).
**오탐 제외**: `implementation.*` 75건 · `source_hash`/`url` 16진 문자열(커밋 해시 오탐) · `classes[].name`·`custom_name`.

### content 미확인
**정적 렌더 미러** — `SCREEN-032.static_renders[main]`(`source_hash=42f0d937…`, 08-13) · `SD-021` HTML/CSS **미검사** ·
mermaid participant alias 는 **판정 근거가 갈려 evidence 에만 넣고 미계상** · `MOD-005.file_path` 는 **스키마 필드라 제외**(규약 확인 필요)

---

## test_scenario (6건 — P0 1 / P1 2 / P2 3)

### ★★★ 11개 축 커버 매트릭스 — **레포 11/11, LogiCraft 2/11**

| | LogiCraft | 레포 카탈로그 |
|---|---|---|
| **양 체계 공통 공백** | — | ★**0축** |
| **LogiCraft 만의 공백** | ★**9축 전부 미커버** + 2축 부분 | — |

⇒ ★**도메인 검증 축의 82%(9/11)가 LogiCraft 그래프에서 추적 불가.**
**LogiCraft TEST 1건 ↔ 레포 `TC-DEID` 182건**(+ `TC-LABEL` 신고축). **D011 의 `TC-MARK` 46건과 동형.**

### P0 1건
**`D012-TST-003`** — ★**`TEST-001` steps[0] 이 소멸한 테이블을 검증한다**:
`SELECT … FROM **MNG_CLIP_MASTER** WHERE JOB_DMND_YN='Y'`
**정본 baseline 에 정의 0건**(아카이브 `V167` DROP). 그 DDL 헤더가 사유를 명시: *"구 적재 소스… **적재 주체 반전으로 스캔은 폐지**"*.
`objective`·`notes` 도 *"관제 공유 DB(MNG_*) 스캔"* 으로 같은 폐기 흐름. **현행 `UC-018` 은 제목부터 *"관제 인입 테이블 직접 INSERT → 폴링 적재"***.
⚠★**`STL-005` 와 다른 결함**: 그건 컬럼명 오기(steps[3]·[4]), 이건 **테이블·흐름 자체 소멸**(steps[0]).

### P1 2건
`TST-001` ★**최대 복잡도 축(신고→차단→수동 재비식별→resolve→단계별 재개)이 TEST 0건** — `UC-016` 은 **main_flow 8스텝 + alternate_flows 6종**인데 어느 TEST 에도 없다. ★**`TEST-001.notes` 가 스스로 배제**: *"정상 흐름(happy path)**만** 수록"* ·
`TST-004` ★**seq3·seq4 가 폐기된 동기 모델을 기대결과로** — *"위탁이 성공하고 처리결과코드·프로젝트 식별자를 **응답받는다**"*. 현행은 **선커밋 후 비동기 디스패치**라 **그 시점 `prjId` 는 NULL**. 레포는 이미 폐기를 명시(`TC-DEID-060`: *"구 '`createProject` → WAITING' **동기 서술 폐기**"*)

### P2 3건
`TST-002` 옵션 설정 → 위탁 payload 반영 통합 지점 미검증(★`TEST-001` 은 **상수로 박아** 검증: `【마스킹 종류】"0"`) ·
`TST-005` `related_domains=[]`(**전역 5/5 미배선**) ·
`TST-006` `notes` 가 지목한 **4개 식별자가 링크로 미승격**

### ★★ auditor 가 내 오탐을 막았다 — **낡은 쪽은 TEST 가 아니라 UC 였다**
> *"★`TEST-001` steps[2] 는 업로드 없이 `createProject` 만 검증해 **오히려 현행과 맞다** — 낡은 쪽은 `UC-011` 이다(**TEST 쪽 오탐 방지**)"*

`UC-011` 이 인용하는 `POST /upload`·`GET /download` 는 **실측 클라이언트 5경로에 없다**. ⇒ **policy `POL-003` 을 독립 확증.**

### ★ `TEST-001` 전 step 검증 (7 step)
**통과 3**(seq2·6·7) / **실패 3**(seq1 **신규 P0** · seq4·5 는 `STL-005`) / **부분 1**(seq3)
★**품질 자체는 높다** — *"전 step 이 검증 SQL·기대값을 구체적으로 명시해 **측정 가능성은 높다**. 결손은 '측정 방법'이 아니라 **'검증 대상 모델이 낡았다'** 와 **'정상 흐름만 있다'** 두 축"*
★**seq7 이 실물과 맞고 도메인 용어집이 틀렸다** — 용어집은 `DE_IDNTF_YN`, 실물·TEST 는 **`DE_IDENT_YN`**

### ★ 근거 있는 skip
`TST-002` — ★**3중 근거**: ①`requirement` 귀속 **0건** ②`notes` 가 지목한 `RQ-SFR-09-01`·`11-04` 는 **`get_item` → `E_NOT_FOUND`**(요구사항정의서 외부 코드) ③후보 `NFR-005` 는 **deprecated** ⇒ *"어디에 없는지를 특정할 수 없다"* ·
**전역 `kind='system'` 0건** — 도메인 결손 아님 ·
**`status`/`data.status` 불일치(전역 5/5)** — TEST 룰 밖이라 **schema 로 이관**

### test_scenario 미확인
★**콜백 창 360분 임계**가 KPST 폴링 축에도 있는지 미확정(ACK 창 축만 확인) ·
**두 체계 간 공식 추적 링크 부재** — *"두 체계 모두 상대를 인용하지 않는다"*

---

# DOMAIN-012 비식별화 — 최종 집계

| 차원 | 건수 | P0 | P1 | P2 |
|---|---:|---:|---:|---:|
| coverage | 6 | 1 | 5 | 0 |
| links | 16 | 4 | 11 | 1 |
| stale | 18 | 8 | 6 | 4 |
| schema | 13 | 5 | 6 | 2 |
| policy | 9 | 5 | 3 | 1 |
| acceptance | 8 | 0 | 5 | 3 |
| requirement | 6 | 1 | 3 | 2 |
| content | 16 | 4 | 9 | 3 |
| diagram | 11 | 1 | 9 | 1 |
| test_scenario | 6 | 1 | 2 | 3 |
| **합계** | **109** | **30** | **59** | **20** |

## ★ 이 도메인의 3대 근본 원인
1. ★**일괄 "재확인, 바꿀 내용 없음" 스탬프 17건**(08-07 06:32~06:51) — **컷오프가 되어 이후 확정 4건이 미도달**
2. ★**활성 ADR 2건이 폐기 정책 보유**(`ADR-006` 게이팅·콜백·재시도큐 / `ADR-022` 차단범위 미달) + `ADR-032`
3. ★**`UC` 3건 전부 `belongs_to_domain` 미설정** → AC 4건이 **도메인 스코프 뷰에서 소멸**

## ★ 소급·이관 항목
`FEAT-006` 판정 확정(**활성이 맞고 두 본문의 "폐기" 표기가 틀렸다**) · **④d 한글 검사법** · `NFR-008` 귀속 누락(D011 과 동일) · `NFR-016` 전 도메인 누락(**전역 이슈**) · `SEQ-001` 미개봉(D003 이관) · `STATE-001` 축 판정이 D011 과 갈림(**dedupe 확인**)
