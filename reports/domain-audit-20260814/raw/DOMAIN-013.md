# DOMAIN-013 포털 — 원본 감사 결과

> project_id `4ece2c3f-8e99-46f5-9580-71108a76e578` · 스킬 `mc-logi-domain-review` v1.5.0 · read-only · legacy grep OFF

## 인벤토리 (coverage 실측)

| 타입 | 활성 | 폐기 |
|---|---|---|
| domain_feature | **3** (DFEAT-043 마트영상 Load / DFEAT-044 라벨 수정·저장 / DFEAT-053 자산 업로드) | 0 |
| api_endpoint | **21** | **10** (★`API-130`·`131` = 포털 SAM2, ADR-013 근거 폐기) |
| screen_spec | 4 (SCREEN-028·029·033·034) | 0 |
| erd | 2 (ERD-018 **tables 1건뿐** · ERD-026) | — |
| domain_event | 1 (EVT-012) | — |
| use_case | 2 (UC-024·027) | — |
| **acceptance** | **0** | — |
| **requirement** | **0** | — |
| test_scenario | 1 (TEST-005 — **경로 B 축 0건**) | — |
| nfr | 3 (NFR-013·015·020) | ★**NFR-006 폐기** |
| diagram_sequence | **0** | — |
| class_diagram | CDIAG-011 (**DFEAT-053 미 depicts**) | — |
| SVC/IAPI/LIB | **프로젝트 전량 0** → COV-10 skip | — |

---

## coverage (6건 — P0 2 / P1 2 / P2 2)

### ★★★ 주입 정책 2건 교정 — **방향이 반대다**

**① 정책 3 은 내가 낡았다** — 내가 준 *"오토라벨링·VLM·버전관리·검수 미제공"* 보다 **ITEM 이 더 넓다**.
`DOMAIN-013` v3(**2026-08-06**)는 **SAM2 분할·SAM2 추적·키포인트(SKELETON)까지 두 경로 모두 미제공**으로 확대하고, **구 서술 *"SAM2·키포인트는 경로 A 에만 부분 노출(ADR-013 부분 override)"* 을 명시 폐기**했다. `API-130`·`131` 도 같은 날 deprecated.
⇒ **ITEM 이 최신. CLAUDE.md 갱신 후보.**

**② 정책 7 은 ITEM 이 낡았다** — *"포털로 내보내는 내부 파이프라인 프레임도 비식별 게이트·`no-store`"* 가 **`DOMAIN-013` 본문에 통째로 없고** `API-110`·`111` 의 `change_summary` 에만 있다 ⇒ `COV-006`.

### P0 2건
**`D013-COV-001`** — **orphan 3건**(`API-115`·`110`·`111`). ★특히 **`API-115`(`GET /v1/portal/datamart/videos`)는 도메인 책임 R1 의 진입 API** 인데 그 책임을 선언한 `DFEAT-043` 이 **`API-081` 하나만** 물고 있다 ·
**`D013-COV-002`** — ★**포털 축 `diagram_sequence` 가 0건**(프로젝트 14건 전량 조회 확인). 활성 UC 2건 모두 **happy path 조차 없다**

### P1 2건
**`COV-003`** ★★**WCAG 2.1 AA 요구가 그래프에서 소멸했다** — 도메인이 책임으로 선언하는데 **유일 보유처 `NFR-006` 이 deprecated**. 그 `change_summary` 자신이 경고: *"이 항목이 요구사항 그래프상 **유일한 보유처**였으므로 비활성화로 그래프에서는 소멸한다"*. ★**`NFR-015` 는 *"접근성과는 **별개**의 웹표준 관점"* 이라며 자기 범위 밖으로 명시 선언** ⇒ **보유처 0** ·
**`COV-004`** ★**`LS_PORTAL_*` 전용 테이블 4종이 프로젝트 전 ERD 어디에도 없다** — `DFEAT-053.persists_in_tables` 가 4종을 선언하는데 **`ERD-018` 의 `tables` 는 `LS_PORTAL_USER_LABEL` 1건뿐**이고 **활성 ERD 17건 어디에도 없다** ⇒ **구현 키트가 이 스키마를 모른다**

### P2 2건
`COV-005` `DFEAT-044` 가 **도형 allowlist·좌표 개수 강제(positive 계약)를 안 담고 미제공(negative)만 서술** — ★**형제 `DFEAT-053` 은 담고 있어 비대칭** ·
`COV-006` 위 정책 7 축 부재 — ⚠★**auditor 가 오독 위험을 짚었다**: R5 의 *"비식별 미적용(본인 데이터)"* 는 **경로 B 업로드 자산 축**이라 **이 축과 반대 방향**이며 한 문장으로 읽으면 오독된다

### ★ orphan 계상 기준 (D012 선례대로 양쪽 명시)
**기준 A**(DFEAT 매핑만, COV-005 정의) → **3건** ← 채택 ·
**기준 B**(SCREEN consumes 확장) → **0건**(3건 전부 `SCREEN-028`/`029` 가 consumes)
⇒ **links 가 0 을 보고해도 모순이 아니다.**

### ★ 억지 gap 배제 (근거 있음)
`COV-002` 위반 **0** — DFEAT 3/3 backing 확보 ·
`COV-003`(DFEAT→SCREEN) — ★*"이 프로젝트는 **SCREEN 이 UC 를 realizes 하고 UC 가 SCREEN 을 references** 하는 관례"* 라 **UC 경유로 화면 도달** ⇒ **가짜 gap 금지 룰 적용** ·
`COV-008` 위반 **0** — ★**X1 위반 후보였던 `API-130`·`131` 이 이미 deprecated 이고 활성 DFEAT 어디에도 없음을 확인** ·
`NFR-017`(채널 격리)의 `applies_to_domains=[]` — **전역 규약 표기로 보여 미보고**(`NFR-014`·`021` 동일)

### coverage 가 넘긴 단서
- **`EVT-012` 활성인데 `DFEAT-053.triggers=[]`·`consumes=[]`**
- ★**`DFEAT-053` 자기모순** — `data.implementation.status='planned'/progress 0` 인데 **`data.status='implemented'`**
- `ERD-018` last_updated **2026-07-10** 으로 도메인(08-06)·`DFEAT-053`(07-30)보다 낡음
- **`CDIAG-011` 이 `DFEAT-043`·`044` 만 depicts** — `DFEAT-053`(경로 B) 누락
- ★**AC·REQ 귀속 0건** ⇒ **포털이 어느 `RQ-SFR-*` 에도 매달려 있지 않아 상위 추적성이 없다**
- **`NFR-006` 폐기의 영향은 D010 등 4개 도메인 공통** 가능

### coverage 미확인
1차 grep OFF(COV-6 전면 skip) · 코드 실구현 미대조(`API-110`/`111` 게이트 서술은 **`change_summary` 인용**이며 실측 아님)

---

## links (13건 — P0 0 / P1 9 / P2 4)

### ★★ orphan 이 **두 개의 직교하는 집합**이다 (교집합 0)

| 기준 | orphan | 의미 |
|---|---|---|
| **① DFEAT 기준** (21 − DFEAT 매핑 18) | `API-110`·`111`·`115` | **화면은 쓰는데 기능 귀속이 없다** |
| **② 화면 기준** (21 − 화면 consumes 18) | **`API-081`·`083`·`147`** | **기능엔 매달렸는데 아무 화면도 안 쓴다** |

★**두 집합의 교집합이 0 이며 서로 다른 결함을 가리킨다.** coverage 는 ①만 셌다.

### P1 9건
`LNK-001` `API-115`(마트 영상 목록)·`API-111`(프레임 이미지)이 **`DFEAT-043` 책임에 명백히 속하는데 미귀속** ·
`LNK-002` **`API-110` 은 귀속처가 모호** — ★`SCREEN-029` 가 *"**데이터마트 원본 + 본인 작업분 병합**"* 이라 **두 DFEAT 에 동시에 걸친다** ⇒ 사용자 확정 ·
`LNK-003` `SCREEN-029.realizes_use_cases=[]` — weak `references` 한 방향만(★**대조군 `SCREEN-033`/`034` 는 정상 형성**) ·
`LNK-004` ★**`SCREEN-028`(포털 홈)은 양방향 모두 끊김** — `UC-024.main_flow[1]` 이 *"데이터마트에서 영상을 선택한다"* 인데 그 화면이 어느 UC 도 실현하지 않고 UC 도 참조하지 않는다 ·
`LNK-005` **`UC-024` 만 `belongs_to_domain` 없음**(`UC-027` 은 정상) — ★**D012 와 달리 전건이 아니라 2건 중 1건**. ★`get_item_schema` 로 확인: **`data` 패치가 아니라 `domain_id` 설정**이어야 한다 ·
`LNK-006` **`EVT-012` 완전 고립**(backward 0) — ★`documented_emitters`/`consumers` 에 **문자열로만** 기록돼 있고 `DFEAT-053.triggers`·`consumes` 는 `[]` ·
`LNK-007` ★**`LS_PORTAL_*` 4종 ERD 미정의 — 원인 확정**: `ERD-018.description` 이 *"오토라벨링·**업로드(TUS)는 ADR-013으로 미제공**(LS_PORTAL_USER_VIDEO·V9 폐기)"* 로 **ADR-013 예외(07-17) 이전 서술**을 유지 ⇒ **그래서 4종이 없다** ·
`LNK-008` (역방향) **`ERD-026`(메타 복제 발신함) 테이블을 소유한 DFEAT 0건** ·
`LNK-009` `CDIAG-011` 이 **본문에서 경로 B 저장모델 4종을 상세 서술하면서** `depicts_dfeats` 에 `DFEAT-053` 누락 ·
`LNK-010` **도메인 integration 링크 전량 공란** — `INT-009`(포털 DB Load)·`EXTSYS-006` 이 실재하는데 `uses_integrations`·`integrates_with` 가 `[]` 이고 **`INT-009` backward 0**

### P2 4건
`LNK-011` `API-081`·`083`·`147` **화면 소비처 0** — ★`SCREEN-029` 는 라벨 로드를 **`API-110` 이 담당**한다고 명시 ⇒ **잔재 의심** ·
`LNK-012` `implements_features` 역방향 18건 공란 — ★**타 도메인 대조군도 `[]` 라 전역 관례로 보여 P1→P2 하향** ·
`LNK-013` `decided_by` 미지정 3건. ★**`ADR-013` 현행성 확인함**(v3·08-06 이 SAM2 반전 반영한 최신본) ⇒ **연결해도 오염 없음**(D012 의 `ADR-006` 과 반대)

### ★★ deprecated 참조 — **gap 0건** (정밀 판정)
활성 ITEM 이 deprecated ID 를 인용하는 3군데를 찾았으나 **전부 *"폐기됐다/retired"* 를 명시한 이력 서술**이라 잔재가 아니다:
`UC-024`: *"✔ **후속 정리 완료(2026-08-06)** — `API-130`·`131` 은 `status=deprecated` 로 전이됐고"* · `DFEAT-044`: *"구 구현 경로 `API-080` 는 **retired**"* · `ADR-013` 은 *"`API-079` (폐기)"* 표기 선례.
⇒ **D012 와 정반대 — 이 도메인은 폐기 정리가 잘 돼 있다.**

### ★ links 가 넘긴 단서
- ★**`API-081` 과 `API-110` 이 둘 다 "기존 라벨 Load"** — 중복 후보. **어느 쪽이 현행인지 확정되면 `LNK-002` 귀속 모호와 `LNK-011` 잔재 판정이 동시에 풀린다**
- ★**`UC-027.brownfield.notes` 가 낡았다** — *"전용 domain_feature **미등록** … `realizes_dfeats` **공란**"* 이라 적었는데 **실제로는 `[DFEAT-053]` 로 이미 연결돼 있다**
- **포털 상태 머신(`UPLOADED`→`PROCESSING`→`READY`|`FAILED`)을 담는 `diagram_state` 없음** ⇒ **ITEM 부재라 diagram 으로 이관**
- `DOMAIN-013.brownfield.diff_summary` 에 **`[추정 — 1차 포털 존재 여부 확인 필요]`** 미해결
- **`DFEAT-053` 은 `brownfield` 객체 자체가 없다**(decided_by 만이 아니라 전반 미기재)

### links 미확인
`unresolved` 실수치 **측정 불가**(대리 측정만 — 확정된 미형성은 `EVT-012`·`INT-009` 2건) ·
**LINK-3/4 대상 0건**(포털 SEQ 부재) · `ERD-026` 은 `tables` 미조회(**description 인용으로 판정**) ·
★**`SCREEN-033`·`034` 는 `consumes_apis` 만 부분 read** ⇒ **그 두 화면의 LINK-5 미검증**

---

## stale (11건 — P0 3 / P1 5 / P2 3)

### ★★★★ 일괄 스탬프 — **이 도메인에도 있었고, 작동 방식이 D012 와 다르다**

**같은 문자열로 08-07 06:34~06:49 사이 이 도메인 8건 적중**:
`MOD-017`·`DFEAT-043`·`044`·`053`·`CDIAG-011`·`TEST-005`·`UC-024`·`027`

★**"바꿀 내용 없음" 판정이 틀린 건이 4건** — **D012 의 최소 3건보다 많다**:
①`CDIAG-011` enum 에 `SEGMENT`·`TRACK` 잔존(**같은 ITEM 산문은 이미 BBOX/POLYGON 만이라 적고 있었다**) ②`MOD-017` 경로 B 통째 누락 ③`TEST-005` 전제가 이미 무효 ④`DFEAT-053` status 모순 방치

★★**그런데 컷오프 효과는 관찰되지 않았다** — 스탬프 이후 같은 날 12:13~12:22 에 API 3건이 갱신되며 stale 이 **정상 재발화**했다.
⇒ **D012 형(컷오프로 이후 확정 미도달)이 아니라 *"스탬프 시점에 이미 존재하던 어긋남을 스탬프가 덮어 확정해 버린"* 유형.**
**같은 스탬프가 두 가지 다른 방식으로 해를 끼친다.**

### ★★★ 근본 원인 — **스탬프가 가장 낡은 ITEM 만 건너뛰었다**
> *"2026-08-07 일괄 스윕이 이 도메인 8개 ITEM 을 훑으면서 **가장 낡은 `ERD-018` 만 건너뛰었다**. 그 결과 SAM2 폐기 정정이 **산문 층에만 도달하고 모델 층(ERD `code_values`·class enum)에는 두 곳 모두 미도달** — **층 단위가 아니라 타입 단위로 스윕한 전형적 결과**."*

**`ERD-018` v5 = 2026-07-10 — 36일 미갱신.** 확정 3건이 어느 것도 도달 못 함:
①07-17 ADR-013 예외(TUS·`LS_PORTAL_*`) ②08-03 SAM2 서버 제거·fail-closed ③08-06 ADR-013 v3

### P0 3건 — **전부 `stale=false`** (플래그 스윕으로는 영원히 안 걸림)
`STL-001` ★**`CDIAG-011` 자기모순** — 산문은 *"BBOX/POLYGON만 · 그 외 400 거부"* 인데 **`LabelType` enum 이 `SEGMENT`·`TRACK`(SAM2) 보유**. **`TRACK` 은 08-03 보안 HIGH 로 서버에서 제거된 축** ·
`STL-002` `ERD-018.LBL_TYPE_CD.code_values` 에 **`TRACK: "트랙(SAM2)"`·`SEGMENT`** — `STL-001` 과 **같은 뿌리**(둘 다 *"label 도메인 `LabelItemDto` 와 동일 체계"* 전제를 복제) ·
`STL-003` ★**`ERD-018` 이 *"업로드(TUS)는 ADR-013으로 **미제공**"* 이라 단언** — ADR-013 은 **07-17 예외로 이미 반전**됐고 **ERD 가 그 결정보다 7일 앞선 판에서 멈춰 있다**

### P1 5건
`STL-004` `LS_PORTAL_*` 4종 ERD 부재(**links 와 중복 — dedupe**). ★**`CDIAG-011` 산문에는 이미 4종이 서술돼 있다** ·
`STL-005` ★**`MOD-017` 이 경로 A 만 서술** — 자기 `responsibility` 는 *"포털 **업로드**·라벨링·Load"* 인데 본문에 업로드 없음 + `realizes_screens` 에 `SCREEN-033`·`034` 누락. ★**주입 목록에 없던 5번째 `stale=true` ITEM** ·
`STL-006` ★**`TEST-005` 가 *"포털 전용 화면 **미확인** — 공용 SC-005 재사용"* 전제로 내부 화면을 3 step 전부에 걸어 뒀다** — **`SCREEN-029` 는 활성**이고 `UC-024` 가 그것을 선언한다 ·
`STL-007` **`DFEAT-053` 삼중 모순** — item `approved` / `data.status="implemented"` / `implementation "planned"·0. ★**형제 2건은 `designed`**. ★**실제 구현 근거 있음**(API 5건 `change_summary` 가 *"코드 실측 신규 등록"*) ⇒ **implementation 축이 낡은 쪽** ·
`STL-008` **`DFEAT-053` 은 `brownfield` 키 자체가 없다** — 형제 2건·`UC-027` 은 보유

### P2 3건
`STL-009` **플래그만 3건** — ★**원인 ITEM 변경 축을 하나씩 대조해 무영향 확인**(`API-171`=응답코드 / `SCREEN-029`=와이어프레임 생성 / `SCREEN-034`=`domain_id`) ·
`STL-010` `[추정 — 1차 포털 존재 여부 확인 필요]` **77일 미해소** ·
`STL-011` **도메인 전 ITEM `planned`/0** — ERD 9컬럼·CDIAG 9속성 포함

### ★★ coverage 와 판정이 갈린 건 — **WCAG(`NFR-006`)**
stale auditor 판정: **결함 아님**. 근거 — `NFR-006.change_summary` 가 *"그래프에서는 소멸한다 … 근거는 CLAUDE.md 포털 절과 kickoff 에 **존치**"* 로 **의도를 명시**했고 활성 참조도 0건 ⇒ **체크리스트 "의도적 상태" 예외**.
⚠★**coverage 는 P1 로 올렸다** ⇒ **최종 dedupe 에서 판정 통일 필요.**

### ★ `ADR-013` — **정합 양호** (D009~D012 의 폐기 ADR 4건과 다르다)
v3·08-06·`approved`. ★*"`context`·`considered_options`·`brownfield` 에 07-15 SAM2 노출 → 08-03 철회 반전 이력을 **보존 서술로 명시**하고 `decision` 은 **현행(미제공)만 주장**"* ⇒ **해당 유형 아님.**
`references` 의 deprecated 3건도 **`(폐기)` 라벨 동반**이라 gap 아님.

### ★ deprecated 참조 **실측 0건** (범위 명시)
`SCREEN` 4건 `consumes_apis` 전량 · DFEAT 3건 `implemented_by_endpoints` 18건 · `MOD-017`·`CDIAG-011` — **전부 활성**.

### ★ stale 이 넘긴 단서
- ★★**한글 손상 2건** — `TEST-005.notes` 의 **`정상 흘름`·`읽기 Load 흘름은`**(흐름→**흘름**) ⇒ **content 로 이관**
- `SCREEN-034.purpose` 가 **`SCREEN-005` 를 "데이터마트 라벨링 화면"으로 오지칭**(실제는 `SCREEN-029`) — `TEST-005` 와 **동일 혼동 축**
- `SCREEN-028.purpose` 는 `/portal/label/{srcSn}`, `SCREEN-029.route` 는 `/portal/label/:id` — **경로변수명 불일치**

### stale 미확인
1차 grep OFF(★**단 `STL-001`·`002` 는 ITEM 내부 자기모순이라 grep 없이도 확정**) · `V47`·`V107`/`V108` 실물 미대조(**컬럼 수준 갭 미판정**) · `ADR-013` 외 ADR 미열람(**이 도메인 `decided_by` 가 `ADR-013` 단일**)

---

## policy (7건 — P0 1 / P1 4 / P2 2)

### ★★★ P0 — **`ADR-026` : 5번째 폐기 정책 ADR, 그런데 이번엔 *고립* 이라 더 위험하다**

`ADR-026`(**`approved`·v1·2026-07-29·supersede 표기 없음**)
> title: *"**포털 SAM2**(AI추론 프레임 인코딩)는 게이트된 비식별본만 사용"*
> `decision_drivers[1]`: *"…**포털 인터랙티브 경로**는 이 전제가 성립하지 않음"*
> `consequences.positive[0]`: *"**포털 SAM2 경로**의 원본 픽셀 외부 전송 유출 경로 폐쇄"*

**그 경로는 2026-08-03 보안 판정으로 서버에서 완전 제거**됐고 `ADR-013` v3 는 *"포털에 SAM2·키포인트를 **다시 만들지 말 것** — 되살리려면 2026-08-03 보안 판정부터 뒤집어야 한다"* 로 확정했다.
⇒ ★**두 활성 승인 ADR 이 서로 반대를 말한다.** `API-130`·`131` 은 같은 근거로 deprecated 됐는데 **그 상위 정책 기록만 무표기 활성**으로 남아 *"포털 SAM2 를 비식별본으로 하면 승인됨"* 으로 읽힌다.

★★**구조적 사각**: `ADR-026.references` 에 **커밋 URL 1건뿐이고 `DOMAIN-013`·`ADR-013`·API 어디와도 링크가 없다.**
⇒ *"stale 판정 로직이 **링크 없는 고립 ADR** 을 놓치는 구조적 사각"* — **역추적 경로가 아예 없다.**
⚠**단 "AI 추론 전송은 비식별본만" 원칙 자체는 내부 경로에 유효하므로 삭제 금지.**

### P1 4건
`POL-002` ★**폐기 근거 오귀속 6건** — 구 업로드 API 가 *"포털 업로드 **폐기**(ADR-013)"* 라 적었으나 **`ADR-013` v3 는 업로드를 *허용*** 한다(07-17 예외). **실제 사유는 정책 폐지가 아니라 경로 교체** — ★**다른 ITEM 이 이미 기록**(`API-142`: *"동일 slug 를 retired `API-078` 이 점유… **도메인 완전 교체, 재활성 아님**"*) ·
`POL-003` ★**도형 allowlist 가 경로 A 계약에만 없다** — `API-154`(경로 B)는 `enum:["BBOX","POLYGON"]` + 좌표 개수까지 완비인데 **`API-082` 는 `request_body` 키 자체가 부재**하고 400 사유에도 없다. ★★**남은 흔적이 `[폐기]` 표기된 200 블록뿐** — *"allowlist 정보가 '폐기'로 표기된 블록에만 남아 계약으로 읽히지 않는다"*(**D012 의 "폐기 블록이 더 정확한 역전"과 동형**) ·
`POL-004` **`SCREEN-028`·`DFEAT-053` 은 `brownfield` 객체 자체가 없다**(형제 4건은 보유) ·
`POL-005` ★**`SCREEN-029` 자기모순** — `brownfield.diff_summary` 가 **`"2차 포털 간편 라벨링·체험"`** 인데 **같은 ITEM `purpose` 는 정반대**(*"AI 보조는 포털 **전 구간**에서 제공하지 않는다"*). ★**"체험"·"간편 라벨링"은 `ADR-013` 이 기각·폐기한 어휘**(`considered_options[1]` = *"오토라벨링 **체험**·업로드 포함(기각안)"*)

### P2 2건
`POL-006` `DFEAT-043` 만 **미제공 범위를 단정하면서 ADR 인용 0건**(형제 4건 전부 명시) ·
`POL-007` ★**`no-store` 계약이 200 이 아니라 412 설명에만** — *"no-store 는 **캐시될 수 있는 200 바이너리 응답**에 필요한 헤더인데"*. ★**`ADR-025` 가 이 경로를 5경로 중 하나로 명시하고 `risks[0]` 에 *"누락 시 동일 결함 재발"* 을 적어 둔 지점**

### ★ 10개 정책 매트릭스 — **위반 0 이 6건**
**커버 6** — ①외부채널 ②단방향 ⑤업로드 상태·간격 ⑥**기여도 점수 잔재 0건**(전수 확인) ⑧게이트 두 축 구분 ⑩채널 격리(**전 API `security=[{jwt:[PORTAL_USER]}]`**)
**부분 위반 2** — ③(ITEM 층 전부 정합, **ADR 층만 잔존**) ⑦(경로 A 만)
**오귀속 1** — ④ · **미계상 1** — ⑨(coverage 중복)

### ★★ 이번엔 **주입 정책이 낡지 않았다**
> *"이번 라운드에서는 주입 정책이 ITEM 보다 낡은 사례를 **발견하지 못했다** — 정책 3(08-06 확대판)은 `DOMAIN-013` v3·`ADR-013` v3 와 **문면까지 일치**"*
⇒ **낡은 쪽은 주입 정책이 아니라 ADR 층이다.**

### ★ POL-2~POL-7 전량 skip — **각 근거 명시**
`ADR-045`=증강 2축(**포털 API 21건 tags 에 bff 0건**) · **`ADR-051` 부재** · `ADR-028`=증강 dedup(**`LOCAL_USER`·`ROLE-004/005` 미존재** — ★포털 격리는 *"지자체 관할"* 이 아니라 **"사용자별 격리"**) · `ADR-027`=재비식별(**인증은 `ADR-012` 축**) · `ADR-036`=event_annotation(**포털은 outbound 없음**) · `ADR-038`=목록 정렬(**`EV9` 잔재 0건**)
★**활성 40 전수 title 스캔 결과 포털을 직접 규율하는 ADR 은 `ADR-013`·`ADR-026` 두 건뿐이고 그중 1건이 폐기 정책 보유.**

### ★ `ADR-013` 은 gap 0 — **모범 사례**
*"`references` 의 폐기 ITEM 3건은 전부 `(폐기)` 표기 · `considered_options` 의 구 서술은 **기각안·반전 이력으로 명시 보존**(오히려 **재구현 방지 장치**) · 업로드 예외가 `context`·`decision`·`considered_options` **3곳에 모두 반영**"*

### policy 미확인
**정적 렌더 미러 미호출** — 포털 4화면 렌더에 폐기 어휘(체험·간편 라벨링·SAM2·키포인트) 잔존 여부 미확인 ·
`ADR-026` 의 조건이 이후 뒤집혔는지 **그래프상 확인 불가**(고립) ·
★*"본 보고의 '위반 0' 판정은 **전부 ITEM 본문 층 한정**이다"*

---

## schema (10건 — P0 3 / P1 6 / P2 1)

### ★★★ `LS_PORTAL_*` 4종 — **성격 확정: "가공의 테이블"이 아니라 "ERD 미정의"**

**3중 대조로 실재 확인**: 베이스라인(2687/2707/2776/2803) + 온프렘 `schema.sql`(1469/1489/1523/1536) + **JPA 엔티티 5종**.

⇒ ★**설계 산출물 밖에 있는 실물**:
**테이블 4 · 컬럼 40 · PK 4 · 인덱스 6**(★`idx_lptu_user_stts` 는 **부분 인덱스**) · **FK 3(전부 `ON DELETE CASCADE`)** · **시퀀스 3**(★`INCREMENT BY 50`) · **상태 enum 2세트**(`UPLOADED|PROCESSING|READY|FAILED` / `IN_PROGRESS|COMPLETED|CANCELLED`)

### P0 3건
`SCH-001` 위 4종 ERD 미정의 ·
`SCH-002` `LBL_TYPE_CD.code_values` 에 **`TRACK`·`SEGMENT`** — ★**실물 판정기가 fail-closed**: `"허용되지 않는 lblTypeCd 입니다. 허용: BBOX, POLYGON"` ⇒ **적재될 수 없는 값을 공표**(stale 과 중복 — **dedupe**) ·
`SCH-003` ★**`API-082` 에 `request_body` 키 자체가 없다** — 실물은 **5필드(필수 4)** 를 받는다. ★**형제 3건은 전부 보유**(`API-154`·`139`·`169`)

### P1 6건
`SCH-004` **`API-082` 에 429 없음** — 실물이 `acquireSavePermit` 으로 던진다(★**형제 `API-139` 는 429 보유**) ·
`SCH-005` ★**폐기 블록 역전** — `[폐기]` 200 에는 `enum:["BBOX","POLYGON"]` 이 있는데 **실제 성공인 201 에는 없다** ·
`SCH-006` ★★**`relationships[]` 가 비어 있고 FK 미공표인데 실물은 `ON DELETE CASCADE`** ⇒ *"ERD 설명은 '원본 미수정 — **참조만**'으로 읽히나 **실제로는 부모 삭제가 자식(사용자 작업분)을 지운다**"*. 미정의 4종에도 **CASCADE FK 3건 추가** ·
`SCH-007` **표준용어 미등록 8건** — ★**`ULD_OFFSET`**: 등록 용어는 `UPLOAD_OFFSET` 인데 *"단어 '업로드' 표준약어는 양 사전 모두 `ULD` 라 **등록 용어 자체가 단어 표준과 어긋나 있다**"* / ★**`FRME_NO`** vs 등록 **`FRM_NO`** ·
`SCH-008` **표준도메인 이탈 4건** — `ULD_STTS_CD` **16 ≠ 코드V20** · `ULD_ID` **문자열 도메인 ↔ `uuid`** · `VER` **↔ `bigint`** · `ORGNL_FILE_NM` **255(양쪽 어디에도 없는 크기)** ·
`SCH-009` ★**테이블명 `LS_PORTAL_USER_LABEL` 이 미등록 낱말 2개 사용** — **`PORTAL`·`LABEL` 둘 다 양 사전 0건**이고 **표준 약어는 `LBL`**. ⇒ *"같은 도메인의 신설 4종은 모두 `LBL` 을 써서 **한 도메인 안에 `LABEL`/`LBL` 이 공존**"*

### P2 1건
`SCH-010` 논리 ERD 부재 — ★**활성 17건 전부 물리, 논리 0건** ⇒ **전역 이슈라 P2 하향**

### ★ CSV 정본 대조 실적
6파일 전량 · **단어 37 + 한글 역조회 9 + 용어 31 + 40컬럼 전수 역매핑** · **도메인 조회 37**.
★**자기등록 9건 배제** — 그중 **`ERD-018` 정의 9컬럼 중 4개가 표준 근거가 자기등록뿐**.
★**출처 공란(KLID-BM 배포분)은 독립 근거로 채택**(11건).

### ★ 정책 제약 — **정상 확인 6건** (근거 있는 "위반 0")
이미지 20MB·50장·확장자 → `API-139` 명시 + `PortalUploadProperties` 일치 ·
영상 5GB → **`API-163.parameters[Upload-Length].schema.maximum=5368709120`** 명시 ·
도형 2점/3~200점 → `API-154` **request_body·400 양쪽** ·
프레임 5초·2000 → 설정·시드 확인 ·
경로 A 별도 적재 → 3층 일치 ·
★**412 게이트가 포털 4경로에 일관**하고 **`API-149`(업로드 자산)는 비대상이라 미보유가 맞다**

### ★ schema 가 넘긴 단서
- ★**`ERD-018` 이 폐기된 마이그레이션 파일을 진실원으로 인용** — *"`V47__…sql`·`V93`"* 인데 **08-13 스쿼시로 `V1~V6` 만 존재**
- ★**`API-082` 403 사유 누락** — 실물은 *"데이터마트에 노출되지 않은 영상입니다"* 에도 FORBIDDEN 을 던지는데 설명엔 채널/역할만

### schema 미확인
활성 ERD 17건 **전수 `tables[]` 미조회**(★단 4종이 `LS_PORTAL_` 접두라 **귀속처가 ERD-018 뿐임은 제목 전수로 확인**) ·
★**API 21건 중 계약 본문을 연 것은 5건** ⇒ **나머지 16건의 `request_body` 부재·상태코드 누락 미검사** ·
운영 DB 직접 조회 없음(파일 DDL 2종 + 엔티티 3중 대조)

---

## acceptance (5건 — P0 0 / **P1 5** / P2 0)

### ★★★ 부재의 성격 판정 — **전역 미도입이 아니라 도메인 개별 결손**

활성 AC 25건 `derived_domain_ids` 실측 분포 + **본문 재분류**:
**AC 를 가진 도메인 9개**(003·004·005·007·010·011·012·014·016) / **전무 5개**(001·006·009·**013 포털**·015)
⇒ ★*"AC 체계는 프로젝트 다수 도메인에 **실제로 적용돼 있고**, 포털은 그중 빠진 5개에 속하는 **도메인 개별 결손**"* ⇒ **P1 유지 타당.**

★★**retired AC 3건 성격 확인 — 폐기 잔재가 아니라 원천 결손**: `AC-012`·`014`·`015` **셋 다 비식별·보안 축이며 포털과 무관** ⇒ *"과거에 포털 AC 가 있었다가 폐기된 것이 아니라 **처음부터 작성된 적이 없다**"*

### ★★★ 세 번째 "두 체계가 갈렸다" — **`F-portal.md` 178 TC**
| | LogiCraft | 레포 |
|---|---|---|
| 포털 | **AC 0 · TEST 0(경로 B)** | ★**`F-portal.md` 178 TC**(11개 절) |

접두: `TC-PORTAL` 97 · `TC-PORTALUP` 73 · `TC-TUS` 45.
★**13개 정책을 사실상 전수 커버** — `TC-PORTALUP-005/006`(20MB 경계) · `003`(50장) · `TC-TUS-001/004`(5GB) · `062/063`(5초 폴백) · `065/066`(**2000 균등 샘플링**) · `TC-PORTAL-095`(**POLYGON 201/2점, BBOX 3점**) · `TC-PORTAL-043`(**SKELETON 폐기 → 400**) · `035/051`(**412 + resolve 후 200 복원**).
★**`F-4` 절이 "포털 SAM2 2026-08-03 제거 완료 — ADR-013 정합"** 으로 이미 반영돼 있다.

⇒ ★*"**무엇을 검증할지 모른다**"가 아니라 "**이미 검증하고 있는 것이 설계 산출물의 수용 기준으로 승격되지 않았다**"* ⇒ **신규 AC 는 백지에서 만들 필요 없다.**
⚠**단 AC 본문에 레포 파일명·TC 코드를 인용하면 오염 규칙 위반** — 사양 서술로 옮겨 적어야 한다.

### P1 5건
`ACC-001` `UC-024`(경로 A) AC 0 · `ACC-002` `UC-027`(경로 B) AC 0 — ★**경로 B 는 AC·TEST 양쪽 0건으로 검증 체계에서 이중 이탈** ·
`ACC-003`·`004`·`005` **DFEAT 3건 전부 `priority=must` 인데 미검증**(직접·UC 경유 모두)

### ★ 13개 정책별 "있어야 하는데 없는 AC" — **전부 부재**
특히 ★**P2(단방향)는 도메인 최상위 불변식인데 판정 기준이 없다** · ★**`is_negative=true` 인 포털 AC 0건** — 이 도메인은 **400/403/404/409/412/413 이 판정의 중심**인데도.
⚠★**P8(업로드 영상 비식별 미적용)은 `UC-027` 에만 있고 `DFEAT-053` 에는 없다** — **서술 축도 편측**.

### ★★ 작업 순서 의존성 (중요)
> *"**`UC-024` 에 `belongs_to_domain` 부재** … 이 상태로 AC 를 신규 작성해 연결해도 `derived_domain_ids` 는 **계속 빈 배열로 남아** 포털 도메인 화면·Trace Matrix 에 **잡히지 않는다**. ⇒ **AC 신설보다 `belongs_to_domain` 수복이 선행**되어야 효과가 난다."*

★추가 순서 의존: **REQ 확정 → AC `verifies` 설계**(*"AC 만 먼저 만들면 `verifies` 가 빈 채로 남아 **고립 AC 를 새로 만든다**"*) · **`DFEAT-053` stale 해소 선행**(*"낡은 응답 규약을 수용 기준으로 **굳힐 위험**"*)

### ★ 근거 있는 skip 5건
`ACC-003`(도메인 REQ 검증 AC) — **포털 REQ 0건이라 집합 자체가 성립 안 함** ·
`ACC-004`·`005`·`006`(stale)·`ACC-007`(고립)·`ACC-008`(빈약) — **판정 대상 없음**(*"stale 될 AC 도, 링크가 비대칭일 AC 도 존재하지 않는다"*)

### ★ 이번에도 주입 정책은 최신 (정합 확인)
정책 10(08-06 확대)이 `DOMAIN-013` v3 `change_summary` 와 **문면 일치** · 정책 2·3·13 ↔ `DFEAT-044` v5 · 4~8 ↔ `UC-027` v6·`DFEAT-053` v2 **전부 정합**.

### acceptance 미확인
AC 28건 **전건 본문 확인 완료**(제목 아닌 시나리오 기준) · 정책 12(채널 격리)는 **AC 0건 사실만 확인**(인가 표면 자체는 policy/links 소관) · `API-171` 본문 미개봉(**주입 실측 인용**)

---

## requirement (5건 — P0 0 / P1 2 / P2 3)

### ★★★★ `NFR-006` 판정 불일치 해소 — **둘 다 맞았고 대상이 달랐다**

> *"coverage 의 P1 과 stale 의 '결함 아님' 중 어느 한쪽이 옳다기보다, **대상이 다르다** — **'폐기 행위'는 정당**(stale 판정 유지), **'폐기 후 미정리'는 결함**(P1)"*

**폐기가 정당한 근거**: `change_summary` 가 사용자 결정(08-06)·사유·대체 위치를 모두 기재 · R1 부록 3 과 추적표 부록 2 가 같은 판정을 문서화 · `get_neighbors` forward/backward **0** ⇒ 링크 단절 없음.
**그런데 미정리 2건**: ①**폐기 근거 인용이 유스케이스명세서 부록 3 에 살아남아** R1 부록 5 의 *"인용 가능 ID 는 `NFR-008`~`021` 이 전부"* 방침 위반 ②**`change_summary` 가 스스로 지시한 후속 조치가 미이행**인 채 도메인은 여전히 WCAG 준수를 선언
★**대조**: 추적표는 *"dangling 0"* 를 자체 선언해 방침을 지켰다 — **위반 지점은 유스케이스명세서 한 곳**.

### P1 2건
`REQ-001` 위 폐기 후 미정리 ·
`REQ-002` ★★**진실원과 파생물의 위치 역전** — **R1 「범위 외(제외) 항목」 5종에 포털이 없다**(부록 2 결번 4건에도 없음). **R1 전체에서 "포털"은 NFR 4행뿐이고 기능 요구 22건 본문엔 0건.**
그런데 **판정은 하위 산출물에만 있다** — 추적표 *"범위 제외 유스케이스 … **번호도 재사용하지 않는다**"*, 유스케이스명세서 *"포털 서브시스템은 … 제외"*, UI설계서 *"채널 범위 밖"*.
★★**그런데 그래프에는 활성 산출물이 실재한다** — DFEAT 3 · SCREEN 4 · **`UC-024`·`UC-027` 둘 다 `approved`**.
⇒ *"추적표가 **'번호도 재사용하지 않는다'고 결번 처리한 UC 2건이 그래프에서는 approved 활성**"*

### P2 3건
`REQ-003` ★**`RFP-003.related_requirements` 가 결번 `REQ-011` 을 계속 참조** ⇒ **상위 추적이 폐기 노드로 이어진다**. + *"마트 본체=외부 / **포털의 단방향 Load=우리**인데 요구 계층에 흔적 0"* ·
`REQ-004` ★**`NFR-012` 본문이 *"듀얼 데이터소스(**관제/포털**)"* 를 명시하는데 `applies_to_domains` 에 DOMAIN-013 없음** ·
`REQ-005` (advisory) 화면 축 **`NFR-011`·`018`·`019` 가 동일 4도메인만 귀속**하고 화면 4건 보유한 포털 제외 — ★**같은 화면 축 `NFR-015` 는 포털 포함**이라 기준이 어긋남

### ★★ `NFR-008` — **이 도메인은 해당 없음** (D011·D012 와 다르다)
> *"본문이 '수집·제작(비식별→마킹→오토라벨링)·검수'로 **내부 파이프라인만 규정**하고 포털을 언급하지 않으며, **포털은 그 3단계를 모두 제공하지 않으므로 귀속 제외가 정합적**"*
⇒ **2연속 발견된 패턴이 여기서 끊겼고, 그 근거가 명확하다.**
★`NFR-017` 도 포털 격리를 상세 규정하나 **`applies_to_domains=[]`(전역)이라 누락 아님**.

### ★ 부재의 성격 — **"처음부터 없었다"** (retired 4건 전수 확인)
`REQ-002`(프롬프트 편의성)·`REQ-011`(마트 동기화)·`REQ-017`·`REQ-018`(개인정보) — **어느 것도 포털 REQ 가 아니다.**
★**R1 정본 grep 교차 확인**: 기능 요구 22건 본문에 **"포털" 0건**.
⇒ ★**추적표 축에서는 "포털 REQ 0건"이 의도적·문서화된 상태이며 그 자체는 결함이 아니다** — **결함은 위치 역전과 근거가 폐기 NFR 이라는 점.**

### ★ 근거 있는 skip
**모드 A 변형** — 도메인 귀속 REQ 0건이라 `RQ-001`~`RQ-005` **비교 축 자체가 없어 전부 SKIP**. 지시대로 **부재 성격 판정 + `RQ-006` advisory + NFR 양방향 전수**만 수행.

### requirement 미확인
1차 grep OFF · `UC-024` `belongs_to_domain` 은 **links/coverage 축으로 이관**

---

## diagram (6건 — P0 0 / P1 4 / P2 2)

### ★★★ 이 도메인 결함의 **일관된 패턴** — *"산문에만 있고 구조 노드에 없음"*

> *"이번 차원의 결함은 **산문 stale 이 아니라 '산문에만 있고 구조 노드에 없음'**(DIAG-001 계열)으로 일관된다"*

★**산문은 최신이다** — `CDIAG-011`(v4·08-07)·`CMP-009`(v5·**08-13**) 둘 다 두 경로 분리·미제공 확대·도형 allowlist 400 을 **모두 반영**. **그런데 구조 배열이 안 따라왔다.**
⇒ **D012 의 `CMP-003`(산문 정정 ↔ 구조 층 미정정)과 같은 유형이 이 도메인에서는 지배적 패턴이다.**

### ★ `diagram_c4_component` 는 **있었다** — `CMP-009`
내가 *"확인 필요"* 로 남긴 축인데 **11건 전량 조회로 `CMP-009`("배정·포털·관제 통지 컴포넌트") 실재 확인**.
(D012 의 `CMP-003` 이 coverage 인벤토리에서 빠졌던 것과 같은 사각 — **이번엔 전량 조회로 잡았다**)

### P1 4건
`DIA-001` ★**`CDIAG-011.classes` 가 2노드뿐** — **`DFEAT-053`(경로 B, API 15건 보유 핵심)의 `LS_PORTAL_*` 4종이 클래스로 전혀 없다**(산문에만). ★**관계도 1건뿐이라 `ON DELETE CASCADE` 3건을 composition 으로 표현할 자리 자체가 없다** ·
`DIA-002` ★★**`CMP-009` 자기모순** — description 은 *"`PortalUploadController`/`PortalTusUploadController`/`PortalUploadLabelController` … `PortalVideoUploadedEvent`(EVT-012) → `PortalFrameExtractBridge`"* 를 명시하는데 **`components[]` 13개 중 포털 노드 3개가 전부 경로 A** 이고 **업로드·TUS·프레임추출 간선 0건** ·
`DIA-003` **포털 SEQ 0건** — ★**14건 전량 조회 후 제목·범위를 하나씩 대조해 재확인**(D012 함정 회피). `DFEAT-053` 만으로도 **endpoint 15건**이라 미문서화 규모가 작지 않다 ·
`DIA-004` **포털 상태 머신 2세트 미등재** — ★**TUS 세션 축(`IN_PROGRESS`→`COMPLETED`→`CANCELLED`)은 DFEAT 본문에도 `CDIAG-011` 산문에도 없어 축 자체가 그래프 미등재**

### P2 2건
`DIA-005` `CMP-009.depicts_dfeats=[]` — ★★**표본 3건(`CMP-001`·`CMP-004`·`CTX-001`) 확인 결과 프로젝트 전반 패턴** ⇒ *"도메인별로 각각 P2 를 계상하면 **과대 집계**되므로 **프로젝트 차원 1건으로 승격·통합 권장**"* ·
`DIA-006` 경로 A 의 *"원본 미수정·참조만"* 경계가 **간선으로 표현되지 않음**(속성 이름으로만 유추). ★**fix 지침이 정확**: *"composition/aggregation 을 쓰지 말 것 — 소유로 그리면 **'포털 저장이 원본·마트에 정합된다'는 정반대 오독**을 만든다"*

### ★ 정합 확인 (오탐 방지)
★**`CDIAG-011` ↔ `ERD-018` 속성 대조 정합** — **9속성 ↔ 9컬럼 1:1, 이름 드리프트 없음** ·
★**`mermaid source` 부재 확인** — `get_item_schema` 로 선택 필드임을 확인하고 **full read 결과 `source` 키 없음** ⇒ *"별도로 읽지 못한 mermaid 원문은 존재하지 않는다"*(구조체 전량 정독) ·
★**`CTX-001.persons` 의 *"포털 회원 … 업로드, 간편 라벨링"* 은 ADR-013 예외 이후 정합** ⇒ **오탐 방지용으로 명시 기록**

### ★ diagram 이 넘긴 도메인 밖 관측
★**`CTX-001.relationships` 에 *"외부 VLM 서비스 … 시계열 요청 (**동기 45s**)"* 잔존** — 확정은 **논블로킹 제출**. **project-level ITEM 이라 D013 gap 미계상** ⇒ **VLM/비식별 도메인 또는 프로젝트 차원 처리 권고**

### diagram 미확인
`list_diagram_coverage` **도구 미제공**(수동 차집합) · 1차 grep OFF(**신설 클래스 속성은 초안 수준, 컬럼 전수 미확정**) ·
`CMP` 11건 중 **`depicts_dfeats` 는 표본 3건만** 확인 · `CNT-001`·`DEP-001` 본문 미정독

---

## content (25건 — P0 0 / P1 15 / P2 10) — **content 차원 최다**

### ★★★★ 한글 손상 — **신규 1건이 ② 로만 잡혔다**

| 손상 | 위치 | 잡은 축 |
|---|---|---|
| `흘름` ×2 (정상 `흐름`) | `TEST-005.notes` | ①② + **★신규 Hamming 축**(교차 확인) |
| ★**`옳겨갈`** (정상 `옮겨갈`) | `API-169.responses.404` | ★**② 만** |

★★**`옳겨갈` 은 ①③④④b④d + Hamming 축이 전부 놓쳤다**:
> *"정상형 `옮겨갈`이 토큰 집합에 **0건**이라 치환쌍이 성립하지 않았고, **kin(`갈리면`)이 ④를 무력화**했기 때문. **결론 — 이 도메인에서는 ②가 유일한 검출 경로였으며, ②를 생략하면 신규 손상 1건이 통과한다.**"*

★**auditor 권고**: *"`옥겨`(알려진 목록)와 `옳겨`는 **같은 정상형(`옮겨`)의 다른 손상형**이므로 ① 목록에 **`옳겨` 추가**"*
★**신규 축 도입**: **어간 치환 검출기**(동일 길이·Hamming=1·비어말·해당 음절 빈도≤3) — 후보 28건 → `흘름` **독립 재검출**.
**누적**: D005 2 · D007 2 · D010 1종3곳 · D012 1종1곳 · **D013 2종3곳**

### P1 15건 — 오염이 지배적
`CNT-001` **`DOMAIN-013` 1,640자 중 절반이 리비전 이력·구현 상태·클래스명** — ★**테스트 클래스 `PortalSam2RemovedTest`** · *"Phase 9"* 작업 회차 ·
`CNT-002` ★**`UC-024` 후반이 ITEM 상태 변경 감사 로그** — *"`status=deprecated` 로 전이됐고 … 구 change_summary 는 **오판정으로 확인됨**"* ·
`CNT-003`·`004` `DFEAT-044`·`CDIAG-011` 자기 리비전 + `V107/V108` ·
`CNT-005` ★**`CDIAG-011` 본문↔구조 모순** — *"두 저장모델을 갖는다"* 선언하는데 **`classes` 는 경로 A 2건뿐** ·
`CNT-006`·`007` `ERD-018`·`ERD-026` **진실원 파일 경로**(★`ERD-026` 은 **파일 4종 + `V98`·`V146`**) + **`@PrePersist`/`@PreUpdate`** ·
`CNT-008` `EVT-012` 코드 경로 ·
`CNT-009`·`010` ★★**`INT-009`(1,643자)·`EXTSYS-006`(1,256자)에 `## 평가` 절** — *"**좁은 범위 구현이다**"* + **IP:포트 `127.0.0.1:5432`** + *"2026-07-27 **실측**"*·*"신규 개발 시 **혼동 주의**"* ·
`CNT-012` ★**본문 `RETRY_CNT` ↔ 실물 `RTRY_NMTM`**(ERD-026 8컬럼 대조로 확정) ·
`CNT-013`·`014` 위 한글 손상 ·
`CNT-015` ★**`TEST-005` 기대결과 안에 *"⚠ 포털 전용 화면 미확인"*** ⇒ *"판정 기준인데 '미확인'이 들어 있어 **시험 수행자가 무엇으로 판정할지 알 수 없다**"* ·
`CNT-016` ★**`INT-009` 3자 불일치** — title `Load`(읽기) ↔ `direction=outbound` ↔ 본문 **outbox push(쓰기)**

### P2 10건
`CNT-011` ★**`EXTSYS-006`↔`INT-009` description **61% 동일**(`difflib` ratio 0.613, 40자+ 동일 블록 7개·377자) ·
`CNT-017`~`020` 어노테이션·**FE 훅 6곳**(★`custom_name` 자리에 **`useLabels(portalMode)`** 훅 호출)·**CSS `h-14`·`w-72`** ·
`CNT-021` ★**`SCREEN-034` 에 *"추정"* 미확인 표기** — **그 경로의 확정 ITEM(`API-149`)이 실재하는데** ·
`CNT-022` `SCREEN-028` **구현 상태 3곳 중복**(*"현재 0 고정 … 후속 과제"*) ·
`CNT-023` ★**`brownfield.diff_summary` 17건 일괄 오염**(`V107` 6 · 컨트롤러#메서드 14 · `Phase 4` 4 · `R16` 2) ⇒ *"**등록 시 템플릿 문제**일 가능성 — 다른 도메인도 확인 권장"* ·
`CNT-024` **CWE 7곳 + 내부 이슈번호 `#3`·`#11` + `AC6` 표식** ·
`CNT-025` `UC-027` 에 **설계도구 내부 메타**(*"LogiCraft `screen_spec` 에 등록되어 있고 `realizes_use_cases` 로 연결"*)

### ★ 스캔 실적
활성 **39건 전수 덤프**(누락 0) · **문자열 3,168 → 검사 2,856 → 한글 1,002** · 순회 경로 **40여종 재귀** ·
★**deprecated 10건 kit-export `count=0` 실증** 후 개별 확인 → **활성 전이 0건** ·
★**`source`·`content_inline` 0건** — *"검사했으나 대상 없음"* ·
**오탐 제외 11종 명시**(ARIA `role` · TUS `fileId` hex · 산출물 파일명 규격 · `[폐기]` 표기 등)

### content 미확인
**정적 렌더 미러 4건 미검사** — ★*"ITEM `sections` 를 고치면 미러가 **즉시 stale** 이 되므로 수정 시 `source_hash` 대조로 재생성 필요"* ·
`DFEAT-053` 의 **설정 키 표기 불일치**(kebab ↔ camel 혼용) — **오염 목록에 설정 키가 없어 미보고, 판정 필요** ·
문법 오류(*"두 경로를 갖으며"*)는 **문체라 제외**

---

## test_scenario (7건 — P0 0 / P1 3 / P2 4)

### ★★★ 13축 매트릭스 — **LogiCraft 커버율 0%, 레포 10/13**

| | ✓ | △ | ✗ |
|---|---:|---:|---:|
| **LogiCraft** | **0** | 1(②) | **12** |
| **레포 `F-portal.md`** | **10** | 1(⑪ 5/7) | 2 |

### ★★ **양 체계 공통 공백 2축** — 지금까지 중 가장 넓다 (D011 1축 · D012 0축)
**③ 경로 A 본인 다운로드 + 기간 제한** — ★`UC-024.main_flow step4` 에 사양은 있는데 **레포 다운로드 TC 3건이 전부 경로 B 자산**이고, ★**`F-portal.md` 말미가 *"#11 다운로드 기간 제한(미확정 유지)"*** ⇒ **사양 자체가 미확정이라 양쪽 다 못 세웠다**. ⚠**활성 API 21건에 경로 A 다운로드 엔드포인트도 없다** ·
**⑪ 미제공 7종 중 `YOLO 오토라벨`·`버전관리` 회귀 가드 0건** — ★*"SAM2 는 **실제 위반이 발생한 뒤에야** 가드가 생겼다(`API-130/131` 폐기 경위). 남은 2종도 **같은 방식으로 새어나갈 수 있는 축**"*

### P1 3건
`TST-001` **경로 B TEST 0건** — ★`UC-027` 은 **완결된 서브시스템**(화면 2·전용 테이블 4종·API 다수)인데 커버 0. 레포엔 **`TC-PORTALUP` 73 + `TC-TUS` 38** ·
`TST-002` `TEST-005` 가 **4 step 중 1~3 만** — **step 4(다운로드)·소유자 격리 대체흐름 미검증**. ★*"소유자 격리는 확정 보안 축"* (`API-171`: *"응답 코드가 **남의 세션 존재 여부를 알려주는 오라클**"*) ·
`TST-003` ★★**APPROVED 게이트가 `preconditions` 어디에도 없다** ⇒ *"**게이트가 깨져도 TEST-005 는 그대로 통과한다**"*. 레포 `TC-PORTAL-039` 는 *"`rawSn` 하나로 **전건 열람이 가능했다**"* 는 실사고를 기록

### P2 4건
`TST-004` 미제공·allowlist·채널 격리 검증 0건. ★**`TEST-005` 에 SAM2 문자열 0건** ⇒ *"결정을 **잘못 검증하고 있지는 않다**(폐기 인용 없음). 다만 그 결정을 **알지도 못한다**"* — **부재이지 stale 이 아님** ·
`TST-005` ★**seq1 이 상정한 응답 구조가 현행 계약 어디에도 없다**(영상정보+라벨+시계열메타 1응답 ↔ `API-081` 은 라벨만·**시계열 메타 엔드포인트는 21건 중 0건**). ⚠**`API-081` 자신도 description ↔ schema 어긋남** ⇒ **API 계약 확정이 선행** ·
`TST-006` ★**도메인 1순위 불변식(단방향)이 유일하게 측정되지 않는 축** — 산문 단언만 있고 SQL 없음 ·
`TST-007` `related_domains=[]`

### ★ `TEST-005` 전 step 검증 — **형식 품질은 높다**
> *"steps 3건 모두 6필드가 채워져 있다 → **형식 품질 자체는 높다**(D012 와 같은 성질: 결손은 '측정 방법'이 아니라 **'검증 대상 범위'**)"*

★**seq3 검증 SQL 5컬럼 전수 대조 = 전건 일치**(`schema.sql` 실물). ★*"〔`PORTAL_USER_NO`=토큰 sub에서 세팅, 입력 아님〕 주석 — **입력 신뢰 경계를 정확히 표기**" ✓*

### ★★ 전역 vs 도메인 판정 — **혼합** (acceptance 와 성질이 다르다)
**전역 미도입 4축**(도메인별 5회 반복 계상 금지): `related_domains`·`related_apis`·`verifies_requirements`·`verifies_nfrs` **5/5 전건 공란** + `status` 불일치 **5/5**
**도메인 고유 결손**: ★*"이 도메인만 **경로가 2개**인데 TEST 는 1개 경로에만 있다. 다른 도메인은 경로 분기 자체가 없다"* ⇒ **UC 기준 50% · 정책 축 기준 0~1/13** (전역 산포 36%보다 현저히 낮다)

### test_scenario 미확인
`TST-002` **룰상 SKIP** — ★근거 3중(REQ 0건 · NFR 14건 전부 전역 · **포털 전용이던 `NFR-006` 이 소멸**) · `RQ-SFR-19` 실재 여부는 requirement 소관

---

# DOMAIN-013 포털 — 최종 집계

| 차원 | 건수 | P0 | P1 | P2 |
|---|---:|---:|---:|---:|
| coverage | 6 | 2 | 2 | 2 |
| links | 13 | 0 | 9 | 4 |
| stale | 11 | 3 | 5 | 3 |
| schema | 10 | 3 | 6 | 1 |
| policy | 7 | 1 | 4 | 2 |
| acceptance | 5 | 0 | 5 | 0 |
| requirement | 5 | 0 | 2 | 3 |
| content | 25 | 0 | 15 | 10 |
| diagram | 6 | 0 | 4 | 2 |
| test_scenario | 7 | 0 | 3 | 4 |
| **합계** | **95** | **9** | **55** | **31** |

## ★ 이 도메인의 특징 (D011·D012 와 다르다)
1. ★**폐기 정리가 잘 돼 있다** — deprecated 참조 **0건**(인용 3군데 전부 `(폐기)` 라벨 동반). `ADR-013` 은 **모범 사례**(기각안·반전 이력을 **재구현 방지 장치**로 보존)
2. ★**결함이 "산문 stale" 이 아니라 "산문에만 있고 구조 노드에 없음"** — `CDIAG-011`·`CMP-009` 산문은 최신인데 구조 배열 미반영
3. ★**P0 가 9건으로 적다**(D012 30) — 대신 **content 25건**으로 오염이 집중
4. ★**상위 추적성 전무** — AC 0 · REQ 0 · 경로 B TEST 0

## ★ 소급·이관
`ADR-026` 고립(**ADR 전수 스윕**) · `CTX-001` 의 *"동기 45s"* · `diagram_c4_component.depicts_dfeats` **전역 통합** · `brownfield.diff_summary` **17건 템플릿 오염**(타 도메인 확인) · TEST **전역 미도입 4축** · **① 목록에 `옳겨` 추가**
