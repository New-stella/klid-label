/**
 * 증적 컷 캡션 — 단위시험 결과서(I2) 「비고」에 그대로 실린다.
 *
 * ── 왜 코드에 두는가 (Critical)
 *
 * 캡처 원장(`capture-manifest.json`)은 촬영 하네스가 **매 실행마다 파일을 훑어 다시 만든다**
 * (`writeManifest`). 그래서 원장에 캡션을 손으로 적으면 다음 실행에 조용히 지워진다.
 * 캡션이 살아남는 자리는 코드뿐이고, 촬영 코드 바로 옆이라 무엇을 찍었는지와 어긋나기도 어렵다.
 *
 * ── 무엇을 적는가
 *
 * **그 한 장이 무엇을 보여주는 순간인지**를 적는다. 컷이 여럿인 케이스는 그림만으로 어느 것이
 * 작업 전이고 어느 것이 작업 후인지 알 수 없다 — 그 구별이 이 칸의 존재 이유다.
 * 케이스명을 되풀이하지 않고(그 칸은 따로 있다) **이 컷에만 있는 것**을 적는다.
 *
 * ⚠ 컷 수가 바뀌면 여기도 함께 고친다 — `writeManifest` 가 캡션 없는 컷을 이름으로 열거하므로
 *   빠뜨리면 조용히 넘어가지 않는다(그 고지가 이 표의 유일한 검사 장치다).
 *
 * 키는 짧은 케이스 ID(`NNN-MM`), 값은 컷 순서대로의 캡션 배열이다.
 */
export const CAPTIONS = {
  // ── 진입·기반 설정
  '001-01': ['관제 토큰으로 인계 진입한 검수자 대시보드 — 역할 표시와 관리 메뉴 노출',
             '검수자에게 열린 관리 화면 진입',
             '권한 밖 화면 접근 시 거부 안내'],
  '002-01': ['시스템 설정 화면 진입 — 배치 처리·AI 추론 파라미터',
             '배치 주기 값을 바꿔 저장한 직후의 성공 안내'],
  '002-02': ['외부 연동 상태 카드 — 비식별·AI 추론·데이터베이스 각각의 응답 상태'],
  '003-01': ['라벨링 정밀도 설정 작업 전 — 허용오차 기본값 1.0px',
             '허용오차를 2.5px 로 바꿔 저장한 직후의 성공 토스트',
             '재진입 후 2.5px 유지 확인'],
  '004-01': ['비식별 마스킹 옵션 카드 작업 전 — 마스킹 범위 배율 기본값',
             '범위 배율을 바꿔 저장한 뒤 — 재진입해도 같은 값이 유지된다'],
  '005-01': ['라벨 관리 화면 — 등록 전 라벨 목록',
             '라벨 추가 창에 라벨명·형태·색상 입력',
             '저장 후 목록에 새 라벨이 나타남'],
  '005-02': ['라벨 마스터 표 — 검출 분류(COCO) 매핑 열',
             '작업자 좌측 메뉴 — 라벨 관리 항목이 보이지 않음(열람 권한 분리)'],
  '006-01': ['프리셋 등록 전 — 대상 이벤트 유형의 기존 프리셋 정리',
             '프리셋 등록 창에서 라벨 2종 선택',
             '등록 결과 — 이벤트 유형에 프리셋이 연결됨'],
  '006-02': ['프리셋 수정 창에서 라벨 세트 교체(3종 선택)',
             '교체된 라벨 세트가 목록에 반영됨 — 마스터 라벨명을 그대로 따름'],

  // ── 영상 인입·배치
  '007-01': ['관제 인입 등록 전 영상 처리 현황 — 대상 클립이 목록에 없음',
             '인입 등록 후 폴링 적재됨 — 목록에 그 영상 행이 나타남'],
  '008-01': ['적재부터 라벨링 준비까지 — 영상 처리 현황의 단계별 상태 전이'],

  // ── 비식별
  '009-01': ['비식별 완료 후 마킹 대기로 전이된 영상 행'],
  '009-02': ['비식별 위탁 요청 적재 결과 — 위탁 원장 조회'],
  '010-01': ['비식별 누락 신고 창에 누락 위치·사유 입력',
             '신고 접수 후 그 영상의 라벨 조회가 차단된 안내 화면'],
  '010-02': ['비식별 신고 목록의 해소 버튼',
             '재비식별 산출물 선택 창 — 후보 파일명·크기·수정시각'],

  // ── 마킹·시계열
  '011-01': ['자동/수동 이벤트 마킹 생성 결과 — 현재 마킹 목록',
             '마킹 완료 후 잔여 배치가 시작된 영상 처리 현황'],
  '012-01': ['시계열 메타 검토 패널 — 승인 처리된 시계열 서술'],

  // ── 작업 배정
  '013-01': ['작업 목록 화면 — 상태별 요약 카드와 전체 목록',
             '이벤트 유형(교통사고)으로 거른 결과'],
  '013-02': ['배정 창에서 작업자 선택',
             '배정 저장 후 목록에 담당자와 배정 이력이 기록됨'],

  // ── 라벨링
  '014-01': ['박스 라벨을 새로 그린 직후 — 편집 중 표시',
             '임시저장 후 저장됨 표시로 전환'],
  '015-01': ['현재 프레임 AI 탐지 실행 전 — 프레임의 라벨을 모두 지운 상태',
             'AI 탐지 실행 후 — 검출 결과가 캔버스에 반영됨'],
  '015-02': ['탐지 대상 분류 선택 창 — 검출 분류가 매핑된 라벨만 선택 가능'],
  '016-01': ['AI 분할 도구를 골라 분할할 대상을 화면에서 지정',
             '분할 결과 — 클릭한 도로면의 외곽선을 따라 폴리곤이 자동 생성됨'],
  '017-01': ['키프레임 사이 자동 보간 작업 전 — 현재 프레임 객체 수',
             '보간 실행 후 — 사이 프레임에 좌표가 채워진 결과',
             '보간 결과 저장 후 저장됨 표시'],
  '017-02': ['YOLO 자동 검출로 만들어진 라벨 — 대상 분류 지정 결과'],
  '018-01': ['구간 자동 추적 패널 — 추적 구간·모드 설정',
             '자동 추적 실행 결과 — 반영 건수 표시'],

  // ── 검수·버전
  '019-01': ['검수 화면 진입 — 검수중 배지',
             '승인 처리 후 재진입 — 이미 승인 처리됨 안내'],
  '019-02': ['검수 화면의 반려 버튼',
             '반려 사유 입력창에 사유 기재',
             '반려 처리 후 상태 전환 확인'],
  '020-01': ['검수 승인 시점에 만들어진 프레임별 라벨 스냅샷'],
  '020-02': ['검수 완료·수정 관제 통지의 발송 결과와 본문'],
  '020-03': ['승인·수정 이벤트별로 갈린 통지 종류'],
  '021-01': ['버전 히스토리에서 승인 버전을 골라 현재 작업본과 비교한 diff'],
  '021-02': ['버전 목록에서 되돌릴 이전 버전 선택',
             '이 버전으로 시작 — 아직 저장되지 않았다는 안내',
             '저장하여 복원 확정 — 저장됨 표시'],
  '022-01': ['검수 승인·재승인 시 발송된 관제 통지 이력'],

  // ── 증강·해상도
  '023-01': ['증강 요청 화면 — 생성 조건 5항목(계절 겨울·날씨 눈 등) 지정',
             '요청 접수 후 증강 결과 화면으로 이동 · 처리 대기 상태'],
  '024-01': ['증강 결과 수신으로 새 영상이 등록된 결과'],
  '025-01': ['증강 결과 활용 결정 대기 카드 — 채택 대상',
             '채택 처리 후 상태 전환'],
  '025-02': ['거부 사유 입력 후 반려 처리',
             '반려된 증강 결과가 활용 결정 대기에서 빠짐'],
  '026-01': ['해상도 다운스케일로 만들어진 이미지셋'],
  '026-02': ['해상도 변경 요청 화면 — 대상 프리셋 선택',
             '요청 접수 결과 — 생성된 파생영상 목록'],

  // ── 현황·공지
  '027-01': ['전체 구축 현황 — 검수완료 기준과 전체 기준 병기 · 이벤트 유형 분포'],
  '027-02': ['작업자별 현황 표',
             '작업자를 고르기 전의 안내 — 선택해야 상세가 열림'],
  '028-01': ['공지 작성 화면에 제목·내용 입력',
             '작성 직후 목록에 나타난 공지(미발행)',
             '발행 처리 후 상태 전환',
             '작업자 계정의 공지 목록에 발행 공지가 보임'],
  '028-02': ['공지에 첨부한 파일이 목록에 표시됨'],

  // ── 포털
  '029-01': ['포털 내 저장 작업 목록',
             '포털 라벨링 화면 — 데이터마트 영상의 기존 라벨을 불러온 상태'],
  '029-02': ['본인 작업 데이터 목록 — 만료 예정일과 내려받기'],
  '030-01': ['포털 영상 업로드 화면 — 허용 형식 안내',
             '업로드한 영상의 마킹·프레임 추출 결과'],
  '030-02': ['업로드 영상에 수동 라벨링',
             '저장 후 다시 열어 라벨이 남아 있음을 확인'],
};

export default CAPTIONS;
