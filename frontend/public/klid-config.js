/*
 * klid-label 프론트엔드 런타임 설정 — <기본(빈) 형상>.
 *
 * 이 파일은 번들보다 먼저 로드되어 환경마다 다른 값을 앱에 넘긴다(index.html 참조).
 * 여기 있는 판은 값이 비어 있고, 그러면 앱은 <빌드 시점 값>으로 떨어진다 — 그래서
 * `npm run dev` 와 컨테이너 이미지는 종전과 똑같이 동작한다.
 *
 * 대상 서버에서는 설치·반영 스크립트(render-frontend-config.sh)가 /etc/klid/frontend.env 를
 * 읽어 이 파일을 <덮어쓴다>. 값의 정본은 언제나 그 설정 파일이며, 이 파일은 생성물이다.
 *
 * ★ 이 파일은 브라우저로 그대로 내려간다 — 비밀값을 넣는 자리가 아니다.
 */
window.__KLID_RUNTIME_CONFIG__ = {};
