# 16. 보안 정책

> 출처: 아키텍처설계서 §2, 통합설계서 §18, 코드규격서(보안 관리) — **모두 PDF 원문 미포함**(진실원 아님, 확인 불가). 예외: 16.4의 "비식별화 우선 수행" 1개 항목만 관리자매뉴얼 §4.3.3에서 원문 확인됨
> 관련: [03 역할·권한](03-roles-permissions.md) · [15 데이터베이스](15-database.md)
>
> ⚠ **원문 미확인(2026-08-19)** — 1차 PDF 3종(화면정의서 v0.7·관리자매뉴얼 Rev.1.0·UI설계서 D2 V1.1) 전문에서 `GPKI`·`공개키`·`SAST`·`Snyk`·`Dependabot`·`Vault`·`OWASP`를 검색했으나 **0건**이다. 로그인·인증 관련해서는 화면정의서·UI설계서에 "로그인 시" 같은 네비게이션 문맥만 있을 뿐 인증 방식(GPKI 여부)을 서술하는 대목이 없고, 관리자매뉴얼 §2(권한 관리)도 슈퍼관리자/담당자/업로더의 **메뉴 접근 권한 매트릭스**만 다루며 인증 메커니즘은 다루지 않는다. 아래 16.1~16.3·16.5는 이번 1차 PDF 세트로는 확인도 반박도 할 수 없다 — `sources/` 분석본(진실원 아님) 그대로 존치한다. 근거: `01-UIUX설계서.txt`·`02-관리자매뉴얼.txt` §1·§2·`03-UI설계서V1.1.txt` 전문 검색(`grep -a`)

## 16.1 사용자 인증

- **GPKI(정부 공개키 기반구조)** 연동 인증 적용
- 비밀번호 보안 정책 강제 적용
- 역할 기반 접근 제어 (RBAC)

## 16.2 네트워크 · 입력 보안

- 모든 사용자 입력 값 **서버 사이드 검증**
- XSS 공격 방지 처리
- 파일 업로드 검증 (유형, 크기, 악성코드 검사)
- HTTPS/TLS 암호화 통신

## 16.3 데이터 보안

- `crontab` 기반 정기 데이터 백업 + 백업 데이터 암호화
- 이력·백업 데이터는 시스템과 별개의 **NAS 저장소**에 보관
- 감사 로깅 — 모든 접근·작업 기록

## 16.4 개인정보 보호

- 비식별화 작업 **다른 라벨링 전 우선 수행** 강제 ([07 §블러](07-labeling-tools.md#블러--비식별화)) — **원문 확인**: 관리자매뉴얼 §4.3.3 "라벨링 작업 기능_비식별화"(`02-관리자매뉴얼.txt`) 원문: "비식별화 처리 작업은 바운딩 박스나 폴리곤 등 이미지 위에 라벨링 작업을 진행한 후에는 진행하지 못하므로, 비식별화 작업이 가장 먼저 선행되어야 합니다." 같은 절에서 "구축 완료된 학습 데이터셋은 **데이터 마켓**에 공개되어 사람들이 이용할 수 있기 때문에 CCTV 영상에 찍힌 사람의 얼굴이나 자동차 번호판, 전화번호 등 개인정보들을 비식별화하는 작업은 라벨링 과정 중 가장 중요한 작업"이라는 배경 설명도 있다.
- 추적 플래그 3종(원문 미확인 — DB 컬럼명이라 15장과 동일하게 PDF에 없음):
  - `DE_IDNTF_YN` — 개인정보 포함 여부
  - `PRVC_YN` — 비식별(가명처리) 여부
  - `AI_CRT_YN` — AI 생성 여부(합성 데이터 식별)
- 개인정보보호 관련 법령 준수 (원문 미확인)

## 16.5 코드 보안 (코드규격서)

- OWASP Top 10 기반 취약점 검사 + SAST
- 의존성 보안 스캔 (Snyk, Dependabot)
- 시크릿 관리 (환경 변수, Vault)
- 접근 제어(RBAC) + 감사 로깅 + 암호화 통신

> **v2 참고**: v2 보안 기준은 [`.claude/rules/security.md`](../../.claude/rules/security.md)(OWASP Top 10 2025, CWE Top 25, Fortify/CodeQL)이며 훨씬 상세하다. 인증은 **GPKI가 아니라 관제/포털 발급 JWT 인계**(`JwtAuthenticationFilter`), 독립 로그인 UI 없음. 비식별은 외부 비식별 서버를 `DeidentifyClient`(Resilience4j)로 호출. 로그 마스킹은 Logback MaskingPatternLayout.
