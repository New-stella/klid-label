# D11-R1 단위시험 절차서 (기능 정상동작 한정 · 컴포넌트 기준 · UI 시험절차)

## 작성 목적
> 본 절차서는 `D11-R1-단위시험절차서.md`(전체)에서 **예외·경계·권한·검증 케이스를 제외하고 각 기능의 정상 동작 케이스만** 추린 기능 검증용 변형본이다. 혼합 케이스는 정상 절만 추출했다(원문 verbatim 아님). 활성 R1 기능 유스케이스 18종은 모두 정상 동작 케이스를 1건 이상 보유한다. 본 판본은 단위시험을 **컴포넌트(CO) 단위 블록**으로 재편하고, 케이스 ID를 `KLID-AT-UT-{컴포넌트번호}-NN`로 부여한 컴포넌트 기준본이다. 본 상세본은 시험항목 및 처리절차·예상결과 및 검증방법을 실제 코드·DB 설계(D8·D9) 기준으로 다단계 절차/테이블·컬럼 검증 수준까지 상세화한 판본이다. 본 판본은 시험항목 및 처리절차를 실제 UI/기능 시험 절차(번호 단계 + ':' 상세 설명)로 기술한다(UI 근거: D2 사용자인터페이스설계서). 화면이 없는 배치·내부·콜백 케이스는 기능 트리거 절차로 표기하며, 예상결과 및 검증방법은 코드·D8/D9 기반 검증 상세를 유지한다.

### 제·개정 이력

| 날짜 | 버전 | 제·개정 내역 | 작성자 | 검토자 |
|------|------|------------|--------|--------|
| 2026-06-30 | 1.0 | 전체 단위시험 절차서에서 기능 정상동작만 추출 | - | - |
| 2026-07-01 | 1.1 | CBD 표준 단위시험 케이스 구조로 블록 머리표(단위시험ID·설명·관련 컴포넌트/프로그램 ID) 추가, 케이스표 헤더 표준화 | - | - |
| 2026-07-01 | 2.0 | 단위시험을 컴포넌트(CO) 단위로 재편, 단위시험ID=KLID-AT-UT-{컴포넌트번호}, 케이스ID를 블록별 01부터 순번 재부여(원본 D11 TC 번호 제거), 다중 CO 블록 분리 | - | - |
| 2026-07-01 | 3.0 | 시험항목 및 처리절차·예상결과 및 검증방법을 코드·D8/D9 기반 다단계 절차·테이블/컬럼/상태 검증 수준으로 상세화 | - | - |
| 2026-07-01 | 4.0 | 시험항목 및 처리절차를 실제 UI/기능 시험 절차(번호 단계+':' 상세, D2 근거)로 재작성, UI 없는 배치/내부 케이스는 기능 트리거 절차로 표기 | - | - |
| 2026-07-01 | 4.1 | 시험항목 및 처리절차에서 불필요한 API 엔드포인트 제거, 예상결과 및 검증방법의 DB 항목을 INSERT 서술 → 확인 SELECT 쿼리로 전환(D8/D9 실측 키) | - | - |
| 2026-07-01 | 4.2 | 관련 프로그램 ID를 D3 컴포넌트설계서 인터페이스 ID(KLID-AT-IF-NNN) 기준으로 기입, 외부 연계 컴포넌트는 D4 송수신 프로그램 ID 병기 | - | - |
| 2026-07-01 | 4.3 | UT-002 라벨 보존율 계산 케이스(구 002-01) 제거 — 계산기(LabelIntegrityCalculator)가 실제 실행 흐름에 미연동(호출처 없음)이라 기능 정상동작 시험 대상 아님. 002-02·03 → 002-01·02 재번호 | - | - |
| 2026-07-01 | 4.4 | UT-004-02 보간 저장 검증에 좌표 값 대조 SELECT 추가 — 건수 확인만으로는 저장된 보간 좌표의 선형 정확성을 검증 못 해 값 대조 보강 | - | - |
| 2026-07-01 | 4.5 | 중복 케이스 병합 — UT-002·005·019의 각 2케이스를 동일 동작의 부속 측면이라 1케이스로 통합(검증 항목 합집합 보존). 총 33→30케이스 | - | - |
| 2026-07-01 | 4.6 | 각 단위시험 '설명'을 REF 위주 → 기능 서술형(누가 무엇을 한다 + 세부 불릿)으로 재작성, RQ·UC 참조 제거 | - | - |
| 2026-07-01 | 4.7 | 배치/내부 케이스 시험항목·처리절차를 실제 수행·관찰 가능한 절차로 재작성, 케이스명 단순화. UT-004 보간 계산·저장 병합(3→2). 총 30→29케이스 | - | - |
| 2026-07-01 | 4.8 | UT-005 설명·절차 정합 — SAM2 외곽 분할을 '오토라벨 파이프라인 자동'에서 '사용자 박스 지정 기반 반자동'으로 정정, 'AI 프롬프트' 용어를 일상어로 대체 | - | - |
| 2026-07-01 | 4.9 | UT-006 정밀도 설정 2케이스 병합 — '설정 저장·반영'에 화이트리스트·값 범위 검증을 흡수(1케이스). 총 29→28케이스 | - | - |
| 2026-07-01 | 5.0 | UT-007-02(LS_DATA_LBL_HSTRY 삭제 audit) 제거 — 사용자 대면 라벨 이력 조회는 검수완료 버전(LS_LABEL_VERSION, 007-01·008)만 대상이며 삭제 audit은 내부 감사(비식별 신고 삭제 시 기록)로 기능정상 시험 대상 아님. 007-03·04·05 → 02·03·04 재번호. 총 28→27케이스 | - | - |
| 2026-07-01 | 5.1 | 정합성 정리(케이스 수 불변 27) — ①UT-007 설명에서 5.0 제거된 삭제 audit 관련 '변경 이력 적재·조회' 불릿 삭제 및 출처의 '이력' 표현 정리 ②UT-002 출처 stale('라벨 보존율 계산 케이스', 개정 4.3 제거분) 정정 ③메뉴 경로 `&gt;`→`>` 표기 정규화 | - | - |
| 2026-07-01 | 5.2 | 케이스 명 정규화 — HTTP 상태코드·DB 테이블/컬럼·상태코드 나열형 케이스명 16건을 검증 시나리오 서술형으로 재작성(검증 상세는 '예상결과 및 검증방법' 칸 유지, 케이스 수·내용 불변) | - | - |
| 2026-07-01 | 5.3 | UT-007 통지 케이스 병합 — 구 007-02(통지 발송)·007-03(통지 본문)을 동일 발송 1건의 부속 측면이라 007-02(발송·본문)로 통합(검증 항목 합집합 보존), 007-04(이벤트별 분기)→007-03 재번호. 총 27→26케이스 | - | - |
| 2026-07-01 | 5.4 | UT-007-02 검증을 실제 DB 관찰로 보강 — 통지 발송 결과 상태 적재 기능(LS_CONTROL_NOTIFY_FALLBACK.SEND_RSLT_CD 신규 컬럼, 즉시 성공 시 SUCCESS 행) 구현·검증(빌드/코드리뷰/DB/요구 4종 게이트 PASS)에 맞춰, 기존 metrics/폴백 미적재 관찰을 `SELECT SEND_RSLT_CD='SUCCESS'` 실제 SELECT로 대체 | - | - |
| 2026-07-01 | 5.5 | UT-019 검증에 UI 관찰 보강 — 마킹 완료가 잔여 배치를 트리거해 영상 상태(LS_DATA_RAW.DATA_STTS_CD)가 MARKING_READY→PROCESSING(배치 시작)→COMPLETED 로 전이됨을 영상 처리 현황 화면으로 관찰하는 근거 추가(LS_DATA_RAW SELECT 뒷받침). 전이는 @Async 배치라 즉시 아님·폴링 필요 명시 | - | - |
| 2026-07-01 | 5.6 | DB-only 케이스에 '화면 표시' 관찰 보강(19건) — 사용자가 화면에서 보는 결과(캔버스 자동라벨/폴리곤, 증강 결과 결정 카드 배지, 버전 목록, 영상 처리 현황 상태 배지, 검수 상태, 설정 반영, 작업락 잠금/해제 등)를 검증란에 추가. FE 화면 표시가 실재하는 케이스만 반영하고, 응답 DTO에 상태 미노출인 011-02(비식별 위탁)·022-01(VLM 메타 검토)은 UI 없음으로 DB 검증 유지. 019 등의 API 표기는 화면 표시 서술로 정정 | - | - |

### 헤더

| D11-R1 | 단위시험 절차서 (R1 한정) |
|-------|---------|
| 시스템명 | AI 기반 지방정부 CCTV 관제지원시스템 구축(2차) | 서브시스템명 | 학습데이터 저작도구 (AT) |
| 단계명 | 설계 | 작성일자 | 2026-06-30 | 버전 | 1.0 |

> 각 블록은 컴포넌트(CO) 1:1 단위시험으로, [단위시험ID(=관련 컴포넌트 번호) · 설명 · 관련 컴포넌트 ID · 관련 프로그램 ID] 머리표로 시작한다(설명 칸에 R1 요구사항 ID·UC 보존, 관련 프로그램 ID 칸에 클래스명). 케이스 ID는 `KLID-AT-UT-{컴포넌트번호}-NN`(블록마다 01부터 순번). 케이스표 컬럼: 케이스 ID · 케이스 명 · 작업 권한 · 시험 데이터 · 시험항목 및 처리절차 · 예상결과 및 검증방법 · 시험 결과(공란).

---

## 단위시험 KLID-AT-UT-001

| 단위시험ID | KLID-AT-UT-001 | | |
|---|---|---|---|
| 설명 | 검수자가 증강 요청 화면에서 검수 완료 영상을 선택해 생성형 AI 증강 처리를 외부 서비스에 위탁 요청한다. <br>- 처리 종류(증강 AI·해상도 변경 중 하나)와 대상 영상을 각각 단일 선택한다. 증강 AI 는 생성 조건 5개 항목(시간대·계절·날씨 등)을 모두 채워야 하며, 겨울·야간·우천은 그 항목을 한 번에 채우는 프리셋이다. <br>- 검수 완료(승인) 영상만 요청 대상이며 미승인 영상은 선택할 수 없다. <br>- 요청 성공 시 증강 결과 화면으로 이동한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-001 | 관련 프로그램 ID | 증강 요청 · 외부 증강 연동 (D4 송신 프로그램: 증강위탁, II-005) |
> 출처: D11 UT-03(외부 증강). 증강 요청·결과 묶음 조회 케이스. RQ-SFR-11-05는 07-01과 동일 기능이라 통합.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-001-01 | 증강 AI 단일 요청(생성 조건 겨울) | REVIEWER | 검수완료 영상·증강 AI·생성조건 겨울 | 1. 데이터 > 증강 요청 메뉴에서 증강 요청 화면 진입 <br>2. 검수 완료 영상 목록에서 대상 영상 1건을 선택하고 처리 종류 '증강 AI' 를 선택한 뒤 생성 조건 프리셋 '겨울' 로 5개 항목을 채움 <br>3. 하단 요약에서 대상 영상·처리 종류를 확인한 뒤 처리 요청 실행 <br>: 검수 완료(승인) 영상만 목록에 노출되며 미승인 영상은 선택 불가 <br>: 처리 종류는 증강 AI·해상도 변경 중 하나만 선택(처리 종류·대상 영상 각 단일 선택). 겨울·야간·우천은 처리 종류가 아니라 생성 조건 프리셋이다 <br>: 요청 성공 시 증강 결과 화면으로 이동하고 성공 알림 표시 | · `SELECT AUG_TYPE_CD, AUG_PROC_STTS_CD, IDMP_KEY, OTSD_JOB_ID FROM LS_DATA_AUG WHERE SRC_SN = :srcSn AND AUG_TYPE_CD = 'AUGMENT'` → AUGMENT · PENDING · IDMP_KEY/OTSD_JOB_ID 채워짐 1건 <br>· AugmentRequestResponse(jobId·requestedAt·videoCount=1·typeCount=1) 200 반환 <br>· (화면 표시) 요청 성공 후 증강 결과 화면으로 이동하며 성공 알림이 뜨고, 증강 요청/결과 목록에 해당 요청이 '처리 대기' 상태로 표시됨 <br>· 검증: HTTP 200·응답 jobId 존재·위 SELECT 신규 row 1건 | |

## 단위시험 KLID-AT-UT-002

| 단위시험ID | KLID-AT-UT-002 | | |
|---|---|---|---|
| 설명 | 외부 증강 서비스가 성공 결과를 콜백으로 전송하면 시스템이 원본을 부모로 하는 새 영상을 생성·등록한다. <br>- 서명(위·변조) 검증과 중복 수신 방지(멱등)를 통과한 결과만 미검수 상태로 등록한다. <br>- 원본 프레임·라벨·메타를 새 영상으로 복사하고 원본↔복사 라벨의 대응 관계를 매핑한다. <br>- 해상도가 동일해 좌표를 재계산하지 않는다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-002 | 관련 프로그램 ID | 증강 결과 수신 (D4 수신 프로그램: 증강결과수신, II-006) |
> 출처: D11 UT-20(외부 콜백 웹훅, UC-002 증강 결과 수신·등록) 새영상 생성·라벨 복사 케이스(UC-002 수신·등록 직접 검증). (구 UT-03 라벨 보존율 계산 케이스는 미연동으로 제거됨 — 개정 4.3.)

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-002-01 | 증강 결과 수신·새 영상 등록 | - | 증강 SUCCESS·원본 라벨·메타 | 1. 외부 위탁 증강 처리가 끝나 결과 콜백이 수신된다 <br>2. 검수자가 증강 결과 화면(SC-023)에 진입해 해당 위탁 결과가 새 영상으로 등록·표시되는지 확인 <br>3. 새 영상을 열어 원본 프레임·라벨·메타가 그대로 복사됐는지 확인 <br>: 서명(위·변조) 검증과 중복 수신 방지(멱등) 통과 시 원본을 부모로 하는 새 영상이 미검수 상태로 등록됨 <br>: 원본 대비 새 영상(부모 참조) 1건이 목록에 추가되고 프레임/라벨/메타 건수가 원본과 동일함(해상도 동일 → 좌표 재계산 없음) | · `SELECT RAW_SN, PARENT_RAW_SN, DATA_STTS_CD, DE_IDNTF_YN FROM LS_DATA_RAW WHERE PARENT_RAW_SN = :orgnlRawSn` → 신규 RAW_SN 1건 · PENDING · N <br>· `SELECT COUNT(*) FROM LS_DATA_SRC WHERE RAW_SN = :newRawSn` → 원본 프레임 건수와 동일 <br>· `SELECT COUNT(*) FROM LS_DATA_LBL WHERE SRC_SN IN (SELECT SRC_SN FROM LS_DATA_SRC WHERE RAW_SN = :newRawSn)` → 원본 라벨 건수와 동일 <br>· `SELECT COUNT(*) FROM LS_DATA_META WHERE RAW_SN = :newRawSn` → 원본 메타 건수와 동일 <br>· 각 원본 SRC_SN → 신규 SRC_SN 매핑 정확·라벨이 대응 프레임에 복사 <br>· `SELECT ORGNL_DATA_LBL_SN, DATA_LBL_SN, COORD_RECALC_YN, SCALE_X, SCALE_Y FROM LS_DATA_AUG_LBL_MAP WHERE DATA_AUG_SN = :dataAugSn` → 원본 라벨 건수만큼 매핑 행 · 각 행 COORD_RECALC_YN='N' · SCALE_X/SCALE_Y NULL <br>· (화면 표시) 증강 결과 화면(SC-023)에 콜백 수신된 증강 결과가 '미검수'로 표시되고, 영상 목록에 새 영상이 '미검수(PENDING)'로 나타남(콜백 배치라 잠시 후 갱신) — 원본↔새영상 부모관계는 화면 미표시라 DB로만 검증 <br>· 검증: 신규 RAW_SN 1건·PENDING·N + 프레임/라벨/메타 건수 원본 일치 + 매핑 row 수 원본 라벨 수 일치 · 프레임별 라벨 좌표(POINT_CN) 원본 일치 | |

## 단위시험 KLID-AT-UT-003

| 단위시험ID | KLID-AT-UT-003 | | |
|---|---|---|---|
| 설명 | 검수자가 증강 요청 화면에서 검수 완료 영상의 프레임 이미지셋을 낮은 해상도로 다운스케일한다. <br>- 720P 등 타겟 해상도를 지정하며 원본보다 높은 업스케일 요청은 거부한다. <br>- 새 영상은 생성하지 않고 라벨 좌표도 복사하지 않는다. <br>- 변환 결과를 이력 1행으로 기록한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-003 | 관련 프로그램 ID | 해상도 변경 |
> 출처: D11 UT-22(영상·스트리밍·해상도). 해상도 변경(다운스케일 이미지셋, SFR-06-03) 케이스만 추출(영상 목록·스트리밍·서명 URL은 인프라/공통으로 제외).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-003-01 | 해상도 다운스케일 이미지셋 생성 | REVIEWER | 해상도 preset | 1. 데이터 > 증강 요청 화면(KLID-AT-SC-022) 진입 <br>2. 검수 완료 영상 1건 선택 후 처리 종류 '해상도 변경' 선택 <br>3. 타겟 해상도 720P 지정 후 하단바 처리 요청 실행 <br>: 원본보다 낮은 해상도로 프레임 이미지셋을 다운스케일 제공(업스케일 요청 거부), 변환 결과를 화면에 표시 | · `SELECT DATA_RAW_SN, TARGET_RES_CD, ORGNL_W, ORGNL_H, TARGET_W, TARGET_H, FRAME_CNT, OUTPUT_DIR_PATH, REG_ID FROM LS_RESOLUTION_EXPORT WHERE DATA_RAW_SN={대상 rawSn} AND TARGET_RES_CD='RES_720P'` → 1행(모든 컬럼 값 채워짐) <br>· 다운스케일 이미지셋 `{base}/resolution/{rawSn}/{TARGET_RES_CD}/` 하위 생성 <br>· 신규 LS_DATA_RAW 미생성·라벨 좌표 미복사 <br>· (화면 표시) 변환 실행 후 화면에 '변환 완료 — {원본W}×{원본H}→{타겟W}×{타겟H}, 프레임 {개수}장 (export #{exportSn})' 메시지 즉시 표시 <br>· 검증: `SELECT COUNT(*) FROM LS_RESOLUTION_EXPORT WHERE DATA_RAW_SN={대상 rawSn} AND TARGET_RES_CD='RES_720P'` → 1(UK 유일) | |
| KLID-AT-UT-003-02 | 해상도 변경 요청 정상 접수 | REVIEWER | preset | 1. 데이터 > 증강 요청 화면에서 영상 1건·처리 종류 '해상도 변경'·타겟 720P 선택 <br>2. 하단바 처리 요청 버튼 실행 <br>: 종류 1건과 영상 1건이 선택되면(해상도 변경 시 타겟 해상도 포함) 처리 요청이 활성화되고, 정상 접수 시 성공 알림 표시 | · HTTP 201 Created <br>· data = `{exportSn, srcW, srcH, targetW, targetH, frameCount}` <br>· OUTPUT_DIR_PATH 등 내부 파일 경로 응답 미포함(CWE-209) <br>· 검증: 응답 상태코드·바디 필드 존재/경로 부재 | |

## 단위시험 KLID-AT-UT-004

| 단위시험ID | KLID-AT-UT-004 | | |
|---|---|---|---|
| 설명 | 오토라벨 파이프라인이 트랙 보간과 YOLO 검출로 객체 위치를 자동 추적·라벨링한다. <br>- 두 키프레임 사이 프레임의 바운딩박스를 선형 보간해 자동 라벨을 생성한다. <br>- 같은 추적 ID의 키프레임 사이 프레임만 보간하고 키프레임 자체는 건너뛴다. <br>- YOLO 검출 결과를 신뢰도(0~1)와 함께 자동 라벨로 저장한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-004 | 관련 프로그램 ID | 객체 추적 · AI 추론 연동 |
> 출처: D11 UT-11(트랙 보간, 직접 UC-004) + UT-09(오토라벨링 YOLO, 간접 UC-004 — SFR-08 라벨링 핵심이므로 포함).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-004-01 | 키프레임 사이 자동 보간 | - | 키프레임 2~3개·trackId | 1. 라벨링 화면(SC-005)에서 한 객체(동일 추적 ID)에 시작·끝 프레임 박스(키프레임 2개) 지정 <br>2. 추적/보간 실행 <br>3. 사이 프레임(6·7·8·9)에 박스 자동 생성 확인 <br>: 키프레임 3개면 각 구간 독립 보간, 마지막 키프레임 이후 생성 없음 <br>: 생성 좌표가 두 키프레임의 선형 중간값 | · 반환 Map(frame→Bbox)에 6/7/8/9 선형 보간값 산출 <br>· 키프레임 자체(5·10) 포함, `outside=true` 이후·마지막 키프레임 이후 propagate 없음 <br>· `SELECT COUNT(*) FROM LS_DATA_LBL WHERE TRCK_ID={대상 trackId} AND AUTO_LBL_YN='Y' AND LBL_TYPE_CD='BBOX'` → 4건(CONF_SCORE=0·POINT_CN nested) <br>· `SELECT COUNT(*) FROM LS_DATA_LBL_AI_INFO WHERE LBL_SRC_CD='INTERPOLATE' AND DATA_RAW_SN={대상 rawSn}` → 4건(DATA_SRC_SN 매핑) <br>· `SELECT SRC_SN, POINT_CN FROM LS_DATA_LBL WHERE TRCK_ID = :trackId AND AUTO_LBL_YN = 'Y' ORDER BY SRC_SN` → 6·7·8·9 프레임 POINT_CN 좌표가 키프레임 5·10 BBOX의 프레임 위치별 **선형 보간값(중간값)** 과 일치 <br>· (화면 표시) 라벨링 캔버스(SC-005)에 두 키프레임 사이 프레임(6·7·8·9)의 박스가 자동으로 그려지고 '자동 라벨'로 구분 표시됨 <br>· 검증: 반환 Map 좌표 계산 일치 + 저장 4건 + SRC_SN(frameNo 6/7/8/9) 매핑 + 저장 좌표가 선형 보간값과 일치(건수뿐 아니라 좌표 값 대조) | |
| KLID-AT-UT-004-02 | YOLO 자동 검출 라벨 생성 | - | 검출 응답 | 1. 영상이 오토라벨 처리 완료된 뒤 해당 영상 라벨링 화면 열기 <br>2. 자동 검출 박스에 '자동 라벨' 표시·신뢰도 확인 <br>: 사람이 그린 라벨과 구분되는 자동 표시 | · `SELECT AUTO_LBL_YN, LBL_TYPE_CD, CONF_SCORE FROM LS_DATA_LBL WHERE SRC_SN={대상 프레임 srcSn}` → AUTO_LBL_YN='Y'·LBL_TYPE_CD='BBOX'·CONF_SCORE 0.0~1.0 <br>· `SELECT LBL_SRC_CD, CONF_SCORE FROM LS_DATA_LBL_AI_INFO WHERE DATA_SRC_SN={대상 프레임 srcSn}` → LBL_SRC_CD='YOLO'·CONF_SCORE 존재 <br>· (화면 표시) 라벨링 캔버스에 YOLO 자동 검출 박스가 '자동 라벨' 배지·신뢰도와 함께 표시되어 사람이 그린 라벨과 구분됨 <br>· 검증: 위 조회 행 필드 값 일치 | |

## 단위시험 KLID-AT-UT-005

| 단위시험ID | KLID-AT-UT-005 | | |
|---|---|---|---|
| 설명 | 작업자가 라벨링 화면에서 객체에 박스를 그려 지정하면, 그 박스 영역을 기준으로 객체 외곽 경계에 밀착하는 폴리곤(외곽선)을 자동 생성한다. <br>- 사용자가 지정한 박스 영역을 입력으로 AI 추론 서버의 외곽선 분할을 호출한다. <br>- 생성된 외곽 경계 폴리곤을 자동 생성 라벨로 저장한다. <br>- 폴리곤 1건당 AI 정보(출처 SAM2)를 함께 저장한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-005 | 관련 프로그램 ID | 외곽 분할 · AI 추론 연동 |
> 출처: D11 UT-10(배치 단계 — SAM2 분할).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-005-01 | SAM2 외곽선 자동 분할 | - | BBOX 있음·저장 결과 | 1. 라벨링 화면(SC-005)에서 대상 객체의 박스를 선택 <br>2. 도구바 [분할] 실행 <br>3. 객체 외곽 경계에 밀착한 폴리곤(외곽선)이 자동 생성됨 확인 <br>: 자동 생성된 폴리곤은 자동 라벨로 저장되고 폴리곤 1건당 AI 정보(출처 SAM2)가 함께 기록됨 | · `AiServerClient.segment` 호출 발생 <br>· `SELECT LBL_TYPE_CD, AUTO_LBL_YN, POINT_CN, CONF_SCORE FROM LS_DATA_LBL WHERE SRC_SN={대상 프레임 srcSn} AND LBL_TYPE_CD='POLYGON'` → LBL_TYPE_CD='POLYGON'·AUTO_LBL_YN='Y'·POINT_CN=폴리곤 JSON·CONF_SCORE(setScale 4) <br>· `SELECT LBL_SRC_CD, DATA_LBL_SN, DATA_RAW_SN, DATA_SRC_SN, CONF_SCORE FROM LS_DATA_LBL_AI_INFO WHERE DATA_LBL_SN={저장 라벨 PK}` → 1행(LBL_SRC_CD='SAM2'·DATA_RAW_SN·DATA_SRC_SN·CONF_SCORE 채워짐) <br>· POLYGON 라벨 1건당 AiInfo 1건 매칭 <br>· (화면 표시) 라벨링 캔버스에 객체 외곽에 밀착한 폴리곤(외곽선)이 자동으로 그려져 표시됨 <br>· 검증: 위 조회 POLYGON 행 저장 확인 + `SELECT COUNT(*) FROM LS_DATA_LBL_AI_INFO WHERE LBL_SRC_CD='SAM2' AND DATA_LBL_SN={저장 라벨 PK}` → 1(행 존재) | |

## 단위시험 KLID-AT-UT-006

| 단위시험ID | KLID-AT-UT-006 | | |
|---|---|---|---|
| 설명 | 검수자가 시스템 설정 화면에서 라벨링 정밀도(외곽선 단순화 허용오차)를 조절한다. <br>- 경계 세밀함 허용오차 값을 0.0~50.0 범위로 저장한다. <br>- 저장값이 이후 객체 추적·외곽 밀착 결과의 경계 세밀함에 반영된다. <br>- 허용 설정 키와 값 범위를 검증한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-006 | 관련 프로그램 ID | 시스템 설정 |
> 출처: D11 UT-26(시스템 설정) 중 POLYGON_SIMPLIFY_TOLERANCE(정밀도 설정값) 케이스만 추출(YOLO 파라미터·배치 주기 설정은 인프라/공통으로 제외).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-006-01 | 라벨링 정밀도(외곽선 단순화 허용오차) 설정 저장·반영 | 검수자 | 허용오차 값·설정 키 정의 | 1. 관리 > 시스템 설정 화면(KLID-AT-SC-025) 진입 <br>2. 라벨링 정밀도 카드의 경계 세밀함(외곽선 단순화 허용오차) 값을 2.5로 수정 후 저장 <br>: 기본값 1.0 → 2.5 저장(허용 범위 0.0~50.0) — 저장 후 객체 추적·외곽 밀착 결과의 경계 세밀함에 반영 <br>: 설정 변경은 허용된 키(화이트리스트)·정의된 범위 내 값만 반영된다 | · `SELECT STNG_VALUE FROM LS_SYSTEM_CONFIG WHERE STNG_KEY='POLYGON_SIMPLIFY_TOLERANCE'` → '2.5'(기본 '1.0'에서 변경) <br>· 캐시 무효화 → 다음 조회에서 2.5 반영 <br>· 설정 키 화이트리스트(ALLOWED) 포함: YOLO_CONF_THRESHOLD·YOLO_IMGSZ·YOLO_IOU·POLYGON_SIMPLIFY_TOLERANCE + 기존 배치 키(BATCH_INTERVAL_SEC·BATCH_CONCURRENCY) 유지 <br>· 값 범위: YOLO_CONF_THRESHOLD 25~80·YOLO_IMGSZ 320~1920·YOLO_IOU 30~80·POLYGON_SIMPLIFY_TOLERANCE 0.0~50.0 <br>· (화면 표시) 시스템 설정 화면(SC-025) 재진입 시 경계 세밀함(허용오차) 입력칸에 저장값 2.5가 표시됨 <br>· 검증: 응답 ConfigResponse.value='2.5' + 위 SELECT 일치 + 화이트리스트·범위 정의 정확 | |

## 단위시험 KLID-AT-UT-007

| 단위시험ID | KLID-AT-UT-007 | | |
|---|---|---|---|
| 설명 | 검수자의 검수 승인 시점에 라벨 스냅샷을 저장하고 관제서버로 완료·수정을 통지한다. <br>- 승인 시 프레임별 현재 라벨 전체를 DB 스냅샷(APPROVED 버전)으로 저장한다. <br>- 검수 완료·수정 시 관제서버로 완료·수정 통지를 송신하되 라벨/메타 본문은 포함하지 않는다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-007 | 관련 프로그램 ID | 버전 스냅샷 · 관제 통지 연동 (D4 송신 프로그램: 관제통지, II-007) |
> 출처: D11 UT-18(버전관리) 중 검수 승인 스냅샷 케이스. + UT-19(관제 통지, UC-009 검수 완료·수정 통지) 통지 케이스 추가. UC-009는 08-04·08-05 공통이며 KLID-AT-UT-008 블록도 동일 UC-009를 실현한다(케이스는 본 블록에 집약).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-007-01 | 검수 승인 시 프레임별 라벨 스냅샷 생성 | REVIEWER | 라벨 보유 영상 | 1. 검수 화면에 진입해 제출된 영상의 프레임·라벨을 읽기 전용으로 확인한다 <br>2. 승인 버튼을 눌러 승인 확인 후 검수를 완료 처리한다 <br>: 검수 승인 시점(KLID-AT-SD-007)에 별도 조작 없이 해당 영상 프레임의 현재 라벨 전체가 DB 스냅샷 APPROVED 버전으로 자동 저장된다. 라벨을 가진 다중 프레임은 프레임마다 각각 스냅샷이 생성되고, 라벨 0건 프레임은 생성에서 제외된다. | · `SELECT SAVE_REASON_CD, ACTVTN_YN, VERSION_HASH, VERSION_NO, LBL_PAYLOAD FROM LS_LABEL_VERSION WHERE DATA_SRC_SN={대상 프레임} AND ACTVTN_YN='Y'` → 프레임당 1행, SAVE_REASON_CD='APPROVED', VERSION_HASH=payload SHA-256, LBL_PAYLOAD=라벨 전체 JSON, VERSION_NO 증가 <br>· `SELECT COUNT(*) FROM LS_LABEL_VERSION WHERE DATA_SRC_SN={대상 프레임} AND ACTVTN_YN='Y'` → 1건(동일 프레임 기존 active 는 ACTVTN_YN='N' 으로 deactivate) <br>· `SELECT COUNT(*) FROM LS_LABEL_VERSION WHERE DATA_SRC_SN={라벨 0건 프레임}` → 0건(스냅샷 미생성·스킵), 반환값 `CommitResult(created, skipped)` <br>· (화면 표시) 버전 관리 화면(SC-010) 커밋(버전) 목록에 새 승인 버전이 최신 활성 버전으로 추가 표시됨 <br>· 검증: 프레임 수 = 생성 버전 수, SAVE_REASON_CD/ACTVTN_YN/VERSION_HASH 확인 | |
| KLID-AT-UT-007-02 | 검수 완료·수정 관제 통지 발송·본문 | - | 통지 페이로드 | 1. 검수 완료(승인) 또는 검수 완료 후 라벨/메타 수정을 발생시킨다 <br>2. 관제서버(수신처 mock)로 완료(TASK_COMPLETED)·수정(TASK_MODIFIED) 통지가 발송되는지 확인한다 — 발송 결과가 `LS_CONTROL_NOTIFY_FALLBACK`에 발송 결과 상태(SEND_RSLT_CD)로 적재되어 DB로 관찰 가능하다 <br>3. 수신된 통지 본문에 이벤트 타입·작업 ID(RAW_SN)·요청 ID(멱등키)·변경 프레임/변경 종류 등 필수 항목이 포함되고, 라벨/메타 본문·PII·토큰·원본 이미지 경로가 포함되지 않는지 확인한다 <br>: 정상 발송 시 발송 결과 상태(SEND_RSLT_CD='SUCCESS')로 DB 관찰, 통지 본문의 필수 항목 유무·금지 항목 미포함(CWE-359)을 수신 본문으로 관찰 | · 예외 없이 정상 반환, `metrics.incrementCompletedSuccess()`/`incrementModifiedSuccess()` 증가 <br>· `SELECT SEND_RSLT_CD, STTS_CD FROM LS_CONTROL_NOTIFY_FALLBACK WHERE RAW_SN=:rawSn AND IDMP_KEY=:requestId` → SEND_RSLT_CD='SUCCESS' · STTS_CD='SUCCEEDED'(터미널) 1행 — 즉시 성공 발송 DB 관찰 <br>· 위 성공행은 재시도 큐(STTS_CD IN PENDING,RETRYING)·depth 게이지 대상 아님 <br>· 발송 실패 시에만 STTS_CD='PENDING' · SEND_RSLT_CD='FAILED'로 폴백 큐 적재(재시도 진입) <br>· eventType 고정값("TASK_COMPLETED"/"TASK_MODIFIED"), requestId=UUID(idempotency) <br>· TaskModifiedPayload.changeTypes ⊆ {LABEL_ADDED, LABEL_UPDATED, LABEL_DELETED, META_UPDATED}(ChangeType.ALL), frameIds=SRC_SN 목록 <br>· PII·토큰·라벨/메타 본문·원본 이미지 경로 미포함(CWE-359) <br>· 검증: 응답 200 + 위 SELECT SEND_RSLT_CD='SUCCESS' + record 필드 값 assert | |
| KLID-AT-UT-007-03 | 승인/수정 이벤트별 통지 분기 | - | 이벤트 | 1. 검수 승인을 완료하면(트랜잭션 커밋 후) 완료 통지가 1회 발송되는지 확인한다 <br>2. 검수 완료 후 같은 영상 라벨을 여러 번 수정한 뒤, 중복 프레임이 합쳐진 수정 통지가 1회 발송되는지 확인한다 <br>3. 변경이 롤백된 경우 통지가 발송되지 않는지 확인한다 <br>: 승인 이벤트→완료 통지, 수정 이벤트→디바운스 후 수정 통지로 발송이 분기됨을 수신 로그로 관찰 | · ReviewApprovedEvent 수신 시 sendCompleted 1회 호출 <br>· TaskModifiedEvent 수신 시 rawSn 윈도우에 frameIds/changeTypes 누적(중복 srcSn 합쳐짐), 윈도우 만료 후 sendModified 1회 flush <br>· 롤백 트랜잭션에서는 AFTER_COMMIT 미호출 <br>· 검증: mock notifyService/debouncer 호출 횟수·인자 검증 | |

## 단위시험 KLID-AT-UT-008

| 단위시험ID | KLID-AT-UT-008 | | |
|---|---|---|---|
| 설명 | 작업자가 버전 관리 화면에서 검수 승인 스냅샷을 비교하고 이전 버전으로 복구한다. <br>- 두 버전의 라벨을 비교해 추가·수정·삭제로 구분 표시한다. <br>- 좌표 변경은 BBOX·POLYGON 형태의 이전·이후 shape로 보여준다. <br>- 본인 배정 영상만 롤백하며 새 활성 ROLLBACK 버전 이력을 생성한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-008 | 관련 프로그램 ID | 버전 비교·복구 |
> 출처: D11 UT-18(버전관리) 중 diff/롤백 케이스. (UC-009 검수 완료·수정 통지 케이스는 KLID-AT-UT-007 블록에 집약 — 중복 회피.)

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-008-01 | 버전 간 라벨 비교(diff) 결과 표시 | WORKER | 스냅샷쌍 | 1. 버전 관리 화면(KLID-AT-SC-010)에 진입해 프레임의 커밋(버전 스냅샷) 목록을 확인한다 <br>2. 커밋 목록에서 비교할 두 버전을 선택해 변경 내용(Diff)을 확인한다 <br>: 검수 승인 스냅샷 두 건의 라벨 JSON을 라벨 단위로 비교해 추가·수정·삭제로 구분 표시한다. 좌표 변경은 BBOX(2점)·POLYGON(3점+) 형태의 이전·이후 shape 로 보여주고, 서로 다른 프레임이거나 동일 내용은 결과에서 제외한다. | · from 에만 존재=REMOVED(before만), to 에만 존재=ADDED(after만), 양쪽 존재+좌표/lblTypeCd/label 차이=MODIFIED(before+after) <br>· ShapeDto: 2점=BBOX(left/top/right/bottom), 3점+=POLYGON(flat points 배열) <br>· 동일 내용은 결과 제외, JSON 파싱 실패 시 빈 리스트(장애 격리) <br>· 검증: 응답 배열의 type·objectId·before/after shape 확인 | |
| KLID-AT-UT-008-02 | 본인 배정 영상 이전 버전 롤백 복원 | WORKER | 스냅샷·본인 프레임 | 1. 버전 관리 화면에서 최신이 아닌 이전 버전을 선택한다 <br>2. 롤백 버튼을 눌러 확인 후 해당 스냅샷으로 복원한다 <br>: 본인에게 배정된 영상만 롤백 가능하며 타인 배정·작업락 영상은 거부된다. 대상 스냅샷 라벨로 프레임 라벨을 교체하고 새 활성 ROLLBACK 버전 이력 1건을 생성하며, 검수 완료(APPROVED) 영상이면 수정 통지가 발행된다. 복원 후 라벨이 스냅샷과 일치함을 확인한다. | · `SELECT SAVE_REASON_CD, ACTVTN_YN FROM LS_LABEL_VERSION WHERE DATA_SRC_SN={대상 프레임} AND ACTVTN_YN='Y'` → SAVE_REASON_CD='ROLLBACK', 1행(동일 해시면 기존 행 재활성, 이전 active 는 'N') <br>· `SELECT LBL_TYPE_CD, LBL_NM, POINT_CN FROM LS_DATA_LBL WHERE SRC_SN={대상 프레임}` → 대상 스냅샷 라벨과 일치(lblTypeCd·label·좌표) — 라벨링 캔버스/V_COMPLETED_LABEL 즉시 반영 <br>· 라벨 교체·버전 전환·통지가 동일 트랜잭션에 묶여 원자적 커밋/롤백 <br>· APPROVED 영상은 TASK_MODIFIED(LABEL_UPDATED) 발행 <br>· (화면 표시) 롤백 후 라벨링 캔버스의 라벨이 대상 스냅샷 상태로 즉시 복원되어 표시되고, 버전 목록에 새 롤백 버전이 활성으로 표시됨 <br>· 검증: DB 조회로 LS_DATA_LBL vs 스냅샷 items 비교 + LS_LABEL_VERSION active 행(SAVE_REASON_CD/ACTVTN_YN) 확인 | |

## 단위시험 KLID-AT-UT-009

| 단위시험ID | KLID-AT-UT-009 | | |
|---|---|---|---|
| 설명 | 검수 완료(승인) 시 작업 완료를, 완료된 영상의 수정이 재검수에서 승인될 때 수정 사실을 관제서버로 단방향 통지해 데이터마트 동기화가 가능하게 한다. <br>- 통지 본문에는 라벨 · 메타 본문과 개인정보 · 토큰 · 절대 경로를 담지 않는다. <br>- 수정만으로는 통지하지 않고 재승인 시점에 묶어 내보낸다. <br>- 전송에 실패해도 유실되지 않고 다시 보낸다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-009 | 관련 프로그램 ID | 완료 통지 · 수정 통지 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 TaskPayloadStructureTest·LsControlNotifyFallbackTest·ControlNotifyDebouncer 관련 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-009-01 | 검수 승인 · 재승인 시 관제 통지 발송 | REVIEWER | 검수 대기 영상, 승인 후 라벨을 수정한 영상 | 1. 검수 상세 화면(KLID-AT-SC-019)에서 영상을 승인하고 관제서버로 완료 통지가 나갔는지 확인 <br>2. 통지 본문의 항목 구성을 확인 <br>3. 승인된 영상의 라벨을 수정하고 그 시점에는 통지가 나가지 않는 것을 확인 <br>4. 재검수를 승인해 수정 통지가 나가는지 확인 <br>: 통지는 학습데이터 산출이 성공한 뒤에 나간다 | 승인 시 작업 식별자 · 이벤트 유형 · 지자체 · 재생 시간 · 이미지 수 등 약정한 항목이 담긴 완료 통지가 나가고, 값이 없는 필수 항목도 키가 남으며 라벨 · 메타 본문과 개인정보 · 토큰은 담기지 않는다. 수정 직후에는 통지가 보류되고 재승인 후 한 번 나가며, 산출물을 다시 만든 경우 변경 파일 목록이 담긴다. 전송에 실패한 통지는 보관됐다가 다시 보내지고 같은 요청 식별자는 한 번만 반영된다 | |

## 단위시험 KLID-AT-UT-010

| 단위시험ID | KLID-AT-UT-010 | | |
|---|---|---|---|
| 설명 | 검수자가 증강 결과 화면에서 증강 결과의 활용 여부를 채택·거부한다. <br>- 원본·증강 프레임 페어와 라벨 무결성을 확인한 뒤 채택한다. <br>- 미검수(처리 대기) 상태의 결과만 검수 대상이다. <br>- 거부 시 거부 사유(1~500자)를 필수로 입력해 함께 저장한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-010 | 관련 프로그램 ID | 증강 활용 검수 (활용 결정 외부 동기화) |
> 출처: D11 UT-03(외부 증강) 중 수락/반려 검수 케이스.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-010-01 | 증강 결과 채택 처리 | REVIEWER | 증강 결과 | 1. 데이터 > 증강 요청 > 증강 결과 메뉴에서 증강 결과 화면 진입 <br>2. 증강 유형 탭에서 원본·증강 프레임 페어와 라벨 무결성을 확인한 뒤 채택 실행 <br>: 미검수(처리 대기) 상태의 증강 결과만 채택 대상이며 이미 처리된 결과는 채택 불가 <br>: 채택 시 활용 여부 검수 결과가 '채택'으로 기록되고 처리 결과 알림 표시 | · `SELECT AUG_PROC_STTS_CD FROM LS_DATA_AUG WHERE DATA_AUG_SN = :dataAugSn` → ACCEPTED <br>· `SELECT RVW_STTS_CD, RVW_ID, RVW_DT FROM LS_DATA_AUG_RVW WHERE DATA_AUG_SN = :dataAugSn` → ACCEPTED · RVW_ID/RVW_DT 채워짐 1건 <br>· (화면 표시) 증강 결과 화면의 결정 카드가 '채택됨'(초록 배지)으로 바뀌고 결정 일시가 표시됨 <br>· 검증: HTTP 200·위 두 SELECT 결과 확인 | |
| KLID-AT-UT-010-02 | 증강 결과 거부 처리(사유 저장) | REVIEWER | 반려 사유 | 1. 데이터 > 증강 요청 > 증강 결과 메뉴에서 증강 결과 화면 진입 <br>2. 대상 증강 결과에서 거부를 선택하고 거부 사유(1~500자)를 입력해 실행 <br>: 거부 사유는 필수이며 공백이면 처리되지 않음 <br>: 거부 시 활용 여부 검수 결과가 '거부'로 기록되고 입력 사유가 함께 저장됨(처리 결과 알림 표시) | · `SELECT AUG_PROC_STTS_CD FROM LS_DATA_AUG WHERE DATA_AUG_SN = :dataAugSn` → REJECTED <br>· `SELECT RVW_STTS_CD, REJECT_RSN FROM LS_DATA_AUG_RVW WHERE DATA_AUG_SN = :dataAugSn` → REJECTED · REJECT_RSN=입력 사유 <br>· (화면 표시) 증강 결과 화면의 결정 카드가 '거부됨'(빨강 배지)으로 바뀌고 거부 사유와 결정 일시가 표시됨 <br>· 검증: HTTP 200·위 SELECT REJECT_RSN 값 확인 | |

## 단위시험 KLID-AT-UT-011

| 단위시험ID | KLID-AT-UT-011 | | |
|---|---|---|---|
| 설명 | 영상 적재 직후 파이프라인 선두 비식별 배치가 원본 영상을 비식별 처리한다. <br>- 처리 시작 시 비식별 처리 이력을 '요청'으로 적재한다. <br>- 정상 완료 응답이면 비식별 경로로 안전 복사한 뒤 비식별 완료·마킹 준비 상태로 전이한다. <br>- KPST 위탁 경로는 프로젝트 생성을 위탁하고 폴링 대기로 두며 완료는 별도 폴링 잡이 담당한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-011 | 관련 프로그램 ID | 비식별 처리 · 외부 비식별 연동 (D4 송신 프로그램: 비식별위탁 II-001·비식별진행폴링 II-002) |
> 출처: D11 UT-07(배치 단계 — 비식별). RQ-SFR-11-06은 09-01과 동일 기능이라 통합.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-011-01 | 비식별 처리 완료 상태 반영 | - | 정상 응답 | 1. 원본 영상 1건이 적재되면 파이프라인 선두 비식별 배치가 자동 실행된다 <br>2. 비식별이 정상 완료되면 해당 영상이 비식별 완료로 표시되고 마킹 진입이 가능한 상태(MARKING_READY)로 바뀌는지 확인한다 <br>3. 영상 처리 현황에서 비식별 완료·마킹 준비 상태 전이를 관찰한다 <br>: 처리 시작 시 비식별 처리 이력이 '요청'으로 남고 완료 시 '성공'으로 전이됨 | · `SELECT PROC_STTS_CD FROM LS_DEIDENT_PROC_LOG WHERE DATA_RAW_SN={대상 RAW_SN}` → 'SUCCEEDED' (REQUESTED→SUCCEEDED 전이 완료) <br>· `SELECT DE_IDNTF_FILE_PATH_NM FROM LS_DEIDENT_PROC_LOG WHERE DATA_RAW_SN={대상 RAW_SN}` → 산출 비식별 영상 경로 <br>· `SELECT DE_IDENT_YN FROM LS_DATA_RAW WHERE RAW_SN={대상 RAW_SN}` → 'Y' <br>· `SELECT DATA_STTS_CD FROM LS_DATA_RAW WHERE RAW_SN={대상 RAW_SN}` → 'MARKING_READY' <br>· (화면 표시) 영상 처리 현황 화면에서 대상 영상이 '마킹 대기' 상태로 표시되고 비식별 완료로 나타남(비식별 배치라 잠시 후 갱신) | |
| KLID-AT-UT-011-02 | 비식별 위탁 요청 적재 | - | 위탁 요청 | 1. 외부 비식별 위탁 경로(위탁 사용 설정)로 비식별 배치를 실행해 외부 시스템에 프로젝트 생성을 위탁한다 <br>2. 위탁 직후 비식별 처리 이력이 '요청' 상태로 남고 요청 식별자·폴링 대기 상태로 적재되는지 확인한다 <br>3. 완료는 별도 폴링이 담당하므로 위탁 직후에는 비식별 완료로 전이되지 않음을 확인한다 <br>: 위탁 직후 처리 이력이 '요청'으로 유지되고 완료 미전이 상태로 관찰 | · `SELECT PROC_STTS_CD FROM LS_DEIDENT_PROC_LOG WHERE DATA_RAW_SN={대상 RAW_SN}` → 'REQUESTED' (위탁 직후 유지) <br>· `SELECT REQ_ID, KPST_PRJ_ID, POLL_STTS_CD FROM LS_DEIDENT_PROC_LOG WHERE DATA_RAW_SN={대상 RAW_SN}` → REQ_ID(요청 idempotency 식별자) 기록·KPST_PRJ_ID 기록·POLL_STTS_CD='WAITING' <br>· `SELECT DE_IDENT_YN FROM LS_DATA_RAW WHERE RAW_SN={대상 RAW_SN}` → 미전이(완료는 폴링 잡이 담당) | |

## 단위시험 KLID-AT-UT-013

| 단위시험ID | KLID-AT-UT-013 | | |
|---|---|---|---|
| 설명 | [설계] 검수자가 시스템 설정 화면에서 비식별 마스킹 옵션을 설정한다. <br>- 마스킹 타입·범위·출력 화질·포맷 값을 입력·저장한다. <br>- 허용 옵션값(화이트리스트)을 검증한다. <br>- 저장된 옵션값이 이후 비식별 위탁 요청 구성에 적용된다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-013 | 관련 프로그램 ID | 시스템 설정 (비식별 옵션 설정 변경) |
> **⚠ 설계 케이스(구현·테스트 후속)** — D3 CO-013은 비식별 옵션 설정을 **SystemConfig 메커니즘**(SystemConfigController/Service·LsSystemConfig, 비식별 옵션 설정 변경)으로 실현하도록 설계돼 있다. SystemConfig 의 설정 저장·화이트리스트 검증·권한·캐시 메커니즘 자체는 **실코드로 구현·검증됨**(KLID-AT-UT-006 블록 `KLID-AT-UT-006-01` POLYGON_SIMPLIFY_TOLERANCE 등 UT-26 실케이스). 다만 **비식별 마스킹 옵션 키(마스킹 타입·범위·출력 화질·포맷)는 현재 `ConfigKeys` 미등록**이고, 비식별 옵션은 `KpstProjectRequest.withDefaults()` 기본값(masking_type=0·db_save=0·masking_range=1·exp_quality=0·exp_format=1)으로 고정돼 위탁된다(사용자 설정 미연동). 따라서 아래는 **D11 실코드 추출이 아닌 설계 케이스**이며, 비식별 옵션 키 등록 시 KLID-AT-UT-006 패턴과 동일하게 실테스트로 승격한다. (케이스 ID는 본 절차서 자체 채번 — D11 UT 출처 없음.)

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-013-01 | [설계] 비식별 마스킹 옵션 설정 저장·위탁 반영 | REVIEWER | 허용 옵션값 | [설계] 1. 검수자(REVIEWER)로 시스템 설정 화면(관리 > 시스템 설정)에 진입한다 <br>2. [설계] 비식별 마스킹 옵션(마스킹 타입·범위·출력 화질·포맷) 항목에 값을 입력하고 저장하면 → 허용 옵션값(화이트리스트) 검증 후 설정에 저장·반영되고, 이후 비식별 위탁 요청이 저장된 옵션값(masking_type·masking_range·exp_quality·exp_format)으로 구성되어 위탁된다 <br>: [설계] 시스템 설정 화면(KLID-AT-SC-025)의 설정 저장 동선. ※ 현행: 비식별 옵션 키 미등록 → 위탁은 기본값(masking_type=0·db_save=0·masking_range=1·exp_quality=0·exp_format=1)으로 고정 위탁된다. 옵션 키 등록 시 KLID-AT-UT-006 패턴 실테스트로 승격 | · [설계] `SELECT STNG_VALUE FROM LS_SYSTEM_CONFIG WHERE STNG_KEY={비식별 옵션 키}` → 저장 옵션값(허용 옵션값만 통과, 그 외 400) <br>· [설계] 저장 후 재조회 시 반영된 옵션값 확인 <br>· [설계] 위탁 요청의 KpstProjectRequest 필드가 저장 옵션값과 일치 <br>· 검증: 위 LS_SYSTEM_CONFIG SELECT + KpstProjectRequest 필드 확인(옵션 키 등록 시 KLID-AT-UT-006 패턴 실테스트로 승격) | |

## 단위시험 KLID-AT-UT-016

| 단위시험ID | KLID-AT-UT-016 | | |
|---|---|---|---|
| 설명 | 작업자가 라벨링 중 개인정보 노출을 발견해 비식별 누락을 신고하고, 수동 비식별 후 신고를 해제한다. <br>- 신고 시 영상 전체 라벨을 스냅샷 보존 후 삭제하고 영상을 잠그며 비식별 완료 표시를 해제한다. <br>- 본인 배정 영상만 신고할 수 있다. <br>- 외부 솔루션으로 수동 비식별을 완료한 뒤 신고를 해제하면 작업락이 함께 해제된다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-016 | 관련 프로그램 ID | 비식별 신고 |
> 출처: D11 UT-15(비식별 누락 신고). 신고→락+라벨 스냅샷 후 삭제, 수동 비식별 후 resolve 케이스.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-016-01 | 본인 배정 영상 비식별 누락 신고 | WORKER | 본인 배정 영상 | 1. 라벨링 작업 중 개인정보 노출을 발견한 작업자(WORKER)가 본인 배정 프레임의 라벨링 캔버스 화면에서 '비식별 누락 신고'를 선택한다 <br>2. 신고 사유(1~1000자)를 입력해 신고하면 → 본인 배정 검증(이미 잠긴 영상은 거부) 통과 시 신고가 접수(OPEN)되고, 해당 영상 전체 라벨을 스냅샷 보존 후 안전 삭제하며, 영상이 잠기고(LOCKED) 비식별 완료 표시가 해제(DE_IDNTF='F')된 뒤 검수자에게 알림된다 <br>: 라벨링 캔버스 화면(KLID-AT-SC-005) '비식별 누락 신고' 동선. 신고 후 영상 잠금 상태에서는 저장이 비활성화된다 | · HTTP 201 Created (body=생성된 DEIDENT_REPORT_SN) <br>· `SELECT REPORT_STTS_CD, DATA_RAW_SN, REPORTER_NO, RSN FROM LS_DEIDENT_REPORT WHERE DATA_RAW_SN={대상 RAW_SN}` → REPORT_STTS_CD='OPEN' 1건(신고자·사유 기록) <br>· `SELECT LOCK_TARGET_CD, LOCK_STTS_CD FROM LS_AUTH_WORK_LOCK WHERE DATA_RAW_SN={대상 RAW_SN}` → LOCK_TARGET_CD='RAW'·LOCK_STTS_CD='LOCKED' 1건 <br>· `SELECT DE_IDENT_YN FROM LS_DATA_RAW WHERE RAW_SN={대상 RAW_SN}` → 'F' <br>· (화면 표시) 신고 직후 라벨링 캔버스(SC-005)가 잠금 상태가 되어 저장 버튼이 비활성화되고 잠금 표시가 나타남 | |
| KLID-AT-UT-016-02 | 수동 비식별 완료 후 신고 해제 | WORKER | OPEN 신고 | 1. 신고로 잠긴 영상을 외부 비식별 솔루션으로 수동 비식별화 완료한다 <br>2. 해당 신고에 대해 신고 해제(resolve)를 요청하면 → 본인 배정(검수자는 전체) 검증 통과 후 신고가 '해제됨'(OPEN→RESOLVED, 해제 일시 기록)으로 전이되고 작업락이 동일 트랜잭션에서 원자적으로 해제되어 작업을 재개할 수 있다(이미 처리된 신고 재해제는 거부) <br>: 수동 비식별 완료 후 신고 해제 동선. 별도 신고 관리 화면 없이 잠긴 영상의 신고 해제 요청으로 처리되며, 신고 상태·작업락 해제를 확인한다 | · HTTP 200 OK <br>· `SELECT REPORT_STTS_CD, RESOLVED_DT FROM LS_DEIDENT_REPORT WHERE DEIDENT_REPORT_SN={대상 신고 SN}` → 'RESOLVED' (OPEN→RESOLVED 전이)·RESOLVED_DT 기록 <br>· `SELECT LOCK_STTS_CD, RELEASE_DT, RELEASE_RSN FROM LS_AUTH_WORK_LOCK WHERE DATA_RAW_SN={대상 RAW_SN}` → 'RELEASED' (LOCKED→RELEASED)·RELEASE_RSN='MANUAL_DEIDENT_DONE' <br>· (화면 표시) 신고 해제 후 라벨링 캔버스의 잠금이 풀려 저장 버튼이 다시 활성화되고 작업을 재개할 수 있음 | |

## 단위시험 KLID-AT-UT-018

| 단위시험ID | KLID-AT-UT-018 | | |
|---|---|---|---|
| 설명 | 주기 배치가 관제 학습용으로 지정된 클립을 픽업해 저작도구에 적재한다. <br>- 관제 공유 DB에서 학습용 지정 클립을 조회한다. <br>- 클립별 독립 트랜잭션으로 적재한 뒤 비식별 선두 이벤트를 발행한다. <br>- 식별자가 비었거나 이미 적재된 중복 클립은 건너뛴다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-018 | 관련 프로그램 ID | 학습용 적재 |
> 출처: UC-018(영상 적재 관제 학습용 설정)은 D11에 별도 UT 블록이 없으나 백엔드 실테스트(TrainingVideoIngestServiceTest·ControlTrainingVideoScanJobTest)가 존재하여 적재 케이스(실코드 출처·D11 미수록)를 추가했다. 적재(TUS 업로드, D11 UT-23)는 관제 학습용 설정 기반 적재로 대체되는 폐지 예정 경로(CLAUDE.md)라 TUS 업로드(UT-23)는 제외.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-018-01 | 관제 학습용 영상 적재 | - | 작업수요 설정 클립 | 1. 관제서버가 공유 DB에서 영상을 학습용으로 설정한다 <br>2. 주기 배치(1건/분)가 학습용 지정 영상을 픽업해 저작도구에 적재한다 <br>3. 적재된 영상이 작업 목록(영상 처리 현황)에 새로 나타나는지 확인한다 <br>: 식별자가 비었거나 이미 적재된 중복 영상은 목록에 추가되지 않음(건너뜀) | · `SELECT VMS_CLIP_ID, RAW_FILE_PATH_NM, PRVC_TYPE_CD FROM LS_DATA_RAW WHERE VMS_CLIP_ID={설정클립 CLIP_ID}` → VMS_CLIP_ID=CLIP_ID(UK)·RAW_FILE_PATH_NM=FILE_PATH·PRVC_TYPE_CD=ANONY (1건) <br>· `SELECT DATA_STTS_CD FROM LS_DATA_RAW WHERE VMS_CLIP_ID={설정클립 CLIP_ID}` → DATA_STTS_CD=PENDING (적재 초기 단계) <br>· VideoIngestedEvent 발행 (비식별 트리거) <br>· 중복/blank 식별자 클립은 skip → 신규 적재 건수 미포함 <br>· (화면 표시) 적재 후 영상 처리 현황 목록에 신규 영상이 '미검수(PENDING)' 상태로 새로 나타남(주기 배치라 잠시 후 갱신) <br>· 검증: `SELECT COUNT(*) FROM LS_DATA_RAW WHERE VMS_CLIP_ID={설정클립 CLIP_ID}` → 1건 + scanAndIngest() 반환 ingested 건수 일치 | |

## 단위시험 KLID-AT-UT-019

| 단위시험ID | KLID-AT-UT-019 | | |
|---|---|---|---|
| 설명 | 작업자가 마킹 화면에서 비식별 영상을 배속 재생하며 자동/수동으로 이벤트 시점을 마킹한다. <br>- 자동 모드는 지정한 프레임 간격으로, 수동은 단축키로 이벤트 시점을 마킹한다. <br>- 비식별이 완료되고 마킹 준비된 본인 배정 영상만 대상이다. <br>- 마킹 완료 시 잔여 배치(VLM·프레임 추출·오토라벨링)를 트리거한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-019 | 관련 프로그램 ID | 이벤트 마킹 |
> 출처: D11 UT-13(마킹). 비식별 영상 자동(프레임간격)/수동(이벤트시점) 마킹 케이스.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-019-01 | 자동/수동 이벤트 마킹 생성 | WORKER | 자동·수동 요청·intervalFrames·duration | 1. 마킹 화면 진입(영상 > 영상 처리 현황 > 마킹, KLID-AT-SC-006) <br>2. 비식별 영상 재생 후 자동/수동 마킹 생성(자동 모드는 마킹 간격 intervalFrames 지정) <br>3. [마킹 완료] 실행 후 생성된 marks 개수 확인 <br>4. 마킹 완료 후 영상 처리 현황(영상 목록/상세)에서 대상 영상의 처리 상태가 '마킹 대기'→'처리중'으로 바뀌는지 확인(잔여 배치 시작 — @Async라 수초 후 폴링) <br>: 작업자가 본인 배정 영상의 마킹 화면에서 비식별 영상을 배속 재생(0.25x~4x)하며 마킹 모드(자동/수동)를 선택한다. 자동은 지정 프레임 간격으로, 수동은 단축키(마킹/삭제/완료)로 이벤트 시점을 마킹하며 이벤트명은 관제 클립 메타에서 자동 소싱한다. [마킹 완료] 실행 시 `LS_MARKING` 저장 + `MarkingCompletedEvent` 발행. 본인 미배정 WORKER는 403, 비식별 미완료(`DE_IDENT_YN≠'Y'`)·비-`MARKING_READY` 영상은 차단. <br>: 자동 모드로 `durationSec`·`intervalFrames`(1~3600) 지정 시 `generateAutoMarks`가 totalFrames=durationSec×30(NATIVE_FPS 고정 가정)을 frameIndex 0부터 intervalFrames 간격으로 `frameIndex < totalFrames`까지 생성(끝 경계 totalFrames 미포함, off-by-one 방지)한다. `durationSec` null/0 이하는 INVALID_INPUT 거부(퇴화 방지). 예: 120초×30fps=3600프레임 → interval 30=120건, interval 60=60건. 확인 방법: 응답 201 + DB 조회(`LS_MARKING.MARKING_SN` 존재·`MARK_CN` 파싱 marks 배열 개수) | · HTTP 201 Created <br>· `SELECT MARKING_SN, EVNT_NM, MARK_MODE_CD, MARK_CN, STTS_CD FROM LS_MARKING WHERE RAW_SN={마킹영상}` → 이벤트명(EVNT_NM)·모드(MARK_MODE_CD)·marks JSON(MARK_CN)·상태(STTS_CD) 저장 (1건) <br>· `SELECT FRME_INTV_NOCS FROM LS_MARKING WHERE RAW_SN={마킹영상}` → AUTO 시 FRME_INTV_NOCS=intervalFrames <br>· 자동 모드 marks 개수 = totalFrames를 intervalFrames로 나눈 스텝 수(끝 경계 totalFrames 제외), 예: 120초×30fps=3600프레임 → interval=30 시 120건, interval=60 시 60건 <br>· MarkingCompletedEvent 발행 (잔여 배치 VLM→프레임추출→오토라벨링 트리거) <br>· (화면 표시) 마킹 완료 후 영상 처리 현황 화면에서 대상 영상 상태 배지가 '마킹 대기'→'처리중'(잔여 배치 시작)→'완료'(배치 완료)로 순차 전이됨 <br>· `SELECT DATA_STTS_CD FROM LS_DATA_RAW WHERE RAW_SN={마킹영상}` → 마킹 직후 MARKING_READY, 배치 시작 후 PROCESSING, 완료 후 COMPLETED <br>· ⚠ 상태 전이는 @Async 배치라 마킹 API 응답 직후 즉시 아님 — 폴링/대기(wait until PROCESSING)로 관찰 <br>· 검증: 응답 201 + `SELECT MARKING_SN FROM LS_MARKING WHERE RAW_SN={마킹영상}` → MARKING_SN 존재(1건 이상) + `SELECT MARK_CN FROM LS_MARKING WHERE RAW_SN={자동마킹영상}` → 파싱한 marks 배열 개수 = interval=30 시 120 / interval=60 시 60 + (폴링) 영상 dataSttsCd PROCESSING 전이 | |

## 단위시험 KLID-AT-UT-021

| 단위시험ID | KLID-AT-UT-021 | | |
|---|---|---|---|
| 설명 | 작업자가 라벨링 캔버스에서 객체를 생성·수정한 뒤 임시저장한다. <br>- 도구바로 바운딩박스·폴리곤 등을 그리고 우측 패널에서 라벨·좌표·속성을 편집한다. <br>- 신규 라벨은 수동으로 저장하고 오토 라벨 수정 시 자동 라벨 여부를 유지한다. <br>- 본인 배정 프레임만 편집하며 검수 완료 후 수정만 관제 수정 통지를 발행한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-021 | 관련 프로그램 ID | 라벨 편집 |
> 출처: D11 UT-14(라벨링) 중 프레임 라벨 편집(임시저장 upsert)·검증 케이스(SAM2 분할/Track은 08-01·08-02 블록에서 다룸).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-021-01 | 박스 라벨 임시저장(수동 신규·오토 수정 구분) | WORKER | 수동·오토 라벨 | 1. 라벨링 캔버스 진입(작업 > 라벨링 캔버스, KLID-AT-SC-005) <br>2. 좌측 도구바 [박스]로 bbox 그리기·수정 <br>3. [저장] 실행 <br>: 작업자가 본인 배정 프레임에서 도구바(선택/박스/폴리곤/분할/추적/삭제)로 객체를 생성·수정하고 우측 패널에서 라벨·좌표·속성을 편집한 뒤 [저장](임시저장 upsert)한다. 신규 라벨(item.id==null)은 `LsDataLbl.createManual`로 `AUTO_LBL_YN='N'` INSERT, 오토 라벨 수정(item.id!=null)은 `updateUserContent`로 좌표/라벨만 UPDATE하고 `AUTO_LBL_YN='Y'` 유지(요청 autoLblYn 무시 — Mass Assignment 차단)한다. 본인 미배정 프레임·재비식별 락(LOCKED)은 차단, 좌표 음수/신규 1000점 초과는 검증·Douglas-Peucker simplify. 검수완료(APPROVED) 후 수정만 `TaskModifiedEvent`(LABEL_UPDATED) 발행. 확인 방법: DB 조회(`LS_DATA_LBL.AUTO_LBL_YN` 값 N/Y) | · (화면 표시) 저장 후 라벨링 캔버스에 편집한 라벨이 반영되어 표시됨(신규=수동 라벨, 오토 라벨 수정=자동 표시 유지) <br>· `SELECT AUTO_LBL_YN, POINT_CN FROM LS_DATA_LBL WHERE SRC_SN={대상프레임}` → 신규 라벨 AUTO_LBL_YN='N'(수동 INSERT) / 오토 라벨 수정 AUTO_LBL_YN='Y' 유지(요청 autoLblYn 무시), POINT_CN=저장 좌표 <br>· 검수 전 저장은 통지 미발행(TASK_MODIFIED는 APPROVED 후 수정만) <br>· 검증: 위 AUTO_LBL_YN → 신규=N / 오토수정=Y | |

## 단위시험 KLID-AT-UT-022

| 단위시험ID | KLID-AT-UT-022 | | |
|---|---|---|---|
| 설명 | 검수자가 라벨링 캔버스 우측 패널에서 외부 VLM 시계열 메타를 검토·승인한다. <br>- 외부 시계열 분석 결과(메타 텍스트)를 조회·검토한다. <br>- 승인 시 검토 상태를 전이하고 검토자·처리 일시를 기록한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-022 | 관련 프로그램 ID | 시계열 결과 수신 · 메타 검토 (D4 수신 프로그램: VLM결과수신, II-004) |
> 출처: D11 UT-16(외부 VLM 메타 검토 승인/반려).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-022-01 | VLM 시계열 메타 검토 승인 | REVIEWER | 승인 요청 | 1. 라벨링 캔버스 진입(작업 > 라벨링 캔버스, KLID-AT-SC-005) <br>2. 우측 패널에서 시계열 메타(외부 VLM/생성 결과) 검토 <br>3. 검토 승인 처리 <br>: 검수자가 우측 패널의 외부 시계열 분석 결과(메타 텍스트)를 조회·검토(시계열 메타 검토·저장, KLID-AT-SD-022)하고 승인하면 `MetaService.approveReview(metaReviewSn)`가 `LS_DATA_META_REVIEW`를 조회(미존재 404)해 `review.approve`로 `RVW_STTS_CD`를 PENDING→APPROVED 전이하고 `RVW_ID`(검토자 sub)·`RVW_DT` 기록. 미인증 401, 비-REVIEWER 403. 확인 방법: 응답 200 + DB 조회(`LS_DATA_META_REVIEW.RVW_STTS_CD=APPROVED`) | · HTTP 200 <br>· `SELECT RVW_STTS_CD FROM LS_DATA_META_REVIEW WHERE DATA_META_REVIEW_SN={대상 metaReviewSn}` → RVW_STTS_CD PENDING→APPROVED 전이 <br>· `SELECT RVW_ID, RVW_DT FROM LS_DATA_META_REVIEW WHERE DATA_META_REVIEW_SN={대상 metaReviewSn}` → RVW_ID=검토자 sub·RVW_DT=처리 일시 기록 <br>· 검증: 응답 200 + `SELECT RVW_STTS_CD FROM LS_DATA_META_REVIEW WHERE DATA_META_REVIEW_SN={대상 metaReviewSn}` → RVW_STTS_CD=APPROVED | |

## 단위시험 KLID-AT-UT-023

| 단위시험ID | KLID-AT-UT-023 | | |
|---|---|---|---|
| 설명 | 검수자가 검수 상세 화면에서 제출 영상을 승인 또는 반려한다. <br>- 읽기 전용 캔버스로 프레임 라벨을 확인한다. <br>- 승인 시 검수 상태를 전이하고 라벨 전체 스냅샷을 생성하며 완료 통지를 연계한다. <br>- 반려 시 반려 사유(1~1000자)를 필수 입력하고 반려 이슈를 생성하며 상태를 전이한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-023 | 관련 프로그램 ID | 검수 처리 |
> 출처: D11 UT-17(검수 승인/반려).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-023-01 | 검수 승인 처리 | REVIEWER | IN_REVIEW 영상 | 1. 검수 상세 화면 진입(작업 > 검수 목록 > 검수 상세, KLID-AT-SC-019) <br>2. 읽기 전용 캔버스로 라벨 확인 <br>3. [승인] 클릭 <br>: 검수자가 제출 영상의 프레임 라벨을 읽기 전용 캔버스로 확인한 뒤 [승인]하면 `ReviewService.approve(videoId)`가 `LS_RAW_DATA_STATUS`를 IN_REVIEW→APPROVED로 전이(`ReviewStateMachine.verify`)하고 `VersionService.commitApproved`로 라벨 전체 스냅샷(`LS_LABEL_VERSION`, SAVE_REASON=APPROVED) 생성 + `ReviewApprovedEvent`(TASK_COMPLETED 연계) 발행(동일 트랜잭션)한다. `@Version` 낙관적 락으로 동시 승인 시 1건만 성공·나머지 409. 미존재 상태 404. 확인 방법: DB 조회(`LS_RAW_DATA_STATUS.DATA_STTS_CD=APPROVED`) | · `SELECT DATA_STTS_CD FROM LS_RAW_DATA_STATUS WHERE RAW_DATA_ID={대상영상}` → DATA_STTS_CD IN_REVIEW→APPROVED 전이 <br>· `SELECT COUNT(*) FROM LS_LABEL_VERSION WHERE DATA_RAW_SN={대상영상}` → 스냅샷 1건 생성(검수 확정 학습데이터 버전) <br>· ReviewApprovedEvent 발행(TASK_COMPLETED 통지 연계) <br>· 동시 승인 시 1건만 성공·나머지 409 CONFLICT <br>· (화면 표시) 검수 상세/목록 화면에서 대상 영상 상태가 '검수 완료(승인)'로 표시됨 <br>· 검증: `SELECT DATA_STTS_CD FROM LS_RAW_DATA_STATUS WHERE RAW_DATA_ID={대상영상}` → DATA_STTS_CD=APPROVED | |
| KLID-AT-UT-023-02 | 검수 반려 처리(사유·이슈 생성) | REVIEWER | 반려 사유 | 1. 검수 상세 화면 진입(KLID-AT-SC-019) <br>2. 검수 메모에 반려 사유 입력 <br>3. [반려] 클릭 <br>: 검수자가 반려 사유(1~1000자 필수)를 입력하고 [반려]하면 `ReviewService.reject(videoId, reason)`가 직전 반려 이슈를 조회해 계층 연결(`UP_DATA_ISSUE_SN`)하고 `LsDataIssue.create/createWithParent`로 `LS_DATA_ISSUE` INSERT(ISSUE_TYPE_CD=REJECTION, ISSUE_STTS_CD=RESOLVED, ISSUE_RSN=사유) + `LS_RAW_DATA_STATUS`를 IN_REVIEW→REJECTED로 전이(`ReviewStateMachine.verify`)한다. `LsTaskEventLog.reject` 기록 + flush(락 충돌 시 409). 확인 방법: DB 조회(`LS_DATA_ISSUE` 생성 + `LS_RAW_DATA_STATUS.DATA_STTS_CD=REJECTED`) | · `SELECT ISSUE_TYPE_CD, ISSUE_STTS_CD, ISSUE_RSN FROM LS_DATA_ISSUE WHERE DATA_RAW_SN={대상영상}` → ISSUE_TYPE_CD=REJECTION·ISSUE_STTS_CD=RESOLVED·ISSUE_RSN=반려사유 <br>· `SELECT DATA_STTS_CD FROM LS_RAW_DATA_STATUS WHERE RAW_DATA_ID={대상영상}` → DATA_STTS_CD IN_REVIEW→REJECTED 전이 <br>· `SELECT UP_DATA_ISSUE_SN FROM LS_DATA_ISSUE WHERE DATA_RAW_SN={대상영상}` → 직전 반려 존재 시 상위 이슈 SN 연결 <br>· (화면 표시) 검수 상세 화면에서 상태가 '반려'로 표시되고 입력한 반려 사유가 이슈로 표시됨 <br>· 검증: `SELECT COUNT(*) FROM LS_DATA_ISSUE WHERE DATA_RAW_SN={대상영상}` → 1건 생성 + `SELECT DATA_STTS_CD FROM LS_RAW_DATA_STATUS WHERE RAW_DATA_ID={대상영상}` → DATA_STTS_CD=REJECTED | |

## 단위시험 KLID-AT-UT-024

| 단위시험ID | KLID-AT-UT-024 | | |
|---|---|---|---|
| 설명 | 데이터마트 영상을 대상으로 기존 라벨과 메타·이벤트 어노테이션을 확인·수정·추가하고 사용자별 작업 데이터로 저장·다운로드한다. <br>- 저장은 본인 작업 데이터로만 남고 원본과 데이터마트를 수정하지 않는다. <br>- 검수 완료된 영상만 목록에 오르며 비식별 누락 신고가 선 영상은 열리지 않는다. <br>- 보존기간이 지나면 본인 작업 데이터를 더 이상 받을 수 없다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-024 | 관련 프로그램 ID | 포털 라벨 작업 조회·수정 · 본인 작업 데이터 내려받기 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 PortalDatamartVideosServiceTest·PortalUserLabelServiceTest·PortalDatamartDownloadServiceTest·PortalWorkMetaServiceTest·PortalRetentionPolicyTest 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-024-01 | 데이터마트 영상 라벨·메타 수정과 본인 저장 | PORTAL_USER | 검수 완료 영상, 미승인 영상, 비식별 누락 신고가 선 영상 | 1. 포털 내 작업 화면(KLID-AT-SC-028)에서 데이터마트 영상 목록을 확인하고 대상을 선택 <br>2. 포털 라벨링 화면(KLID-AT-SC-029)에서 기존 라벨과 메타·이벤트 어노테이션이 실려 오는지 확인 <br>3. 라벨과 메타를 고쳐 저장한 뒤 다시 열어 본인이 고친 값이 보이는지 확인 <br>4. 원본 영상과 데이터마트가 바뀌지 않았는지 확인 <br>5. 미승인 영상과 비식별 신고가 선 영상을 각각 열어 본다 <br>: 저장은 본인 작업 데이터로만 남는다 | 검수 완료 영상만 목록에 오르고 프레임이 없는 영상은 빠진다. 저장한 값은 본인 작업 데이터로만 남아 원본과 데이터마트가 그대로이며, 원본에 있던 값을 가린 경우 가림 표시가 서고 새로 추가한 값에는 서지 않는다. 미승인 영상은 접근이 거부되고 비식별 신고가 선 영상은 신고 구간임을 알리며 막히되 신고가 풀리면 다시 열린다 | |
| KLID-AT-UT-024-02 | 본인 작업 데이터 내려받기와 보존기간 | PORTAL_USER | 본인 저장 라벨이 있는 영상과 없는 영상, 보존기간 설정 | 1. 포털 내 작업 화면에서 본인 작업이 있는 영상의 만료 예정 시각을 확인 <br>2. 그 영상의 본인 작업 데이터를 내려받아 라벨과 이미지가 담겼는지 확인 <br>3. 본인 저장 라벨이 하나도 없는 영상에서 내려받기를 시도 <br>4. 목록에 없는 영상과 비식별 신고가 선 영상에서 각각 내려받기를 시도 <br>5. 보존기간 설정을 바꾼 뒤 만료 예정 시각이 갱신되는지 확인 <br>: 내려받기는 본인이 저장한 작업 데이터를 기준으로 한다 | 만료 예정 시각은 본인 저작 최초 저장일에 보존기간을 더해 계산되고 저작물이 없으면 비어 있으며 설정을 바꾸면 갱신된다. 내려받은 묶음에는 본인 라벨과 이미지가 담기고 비식별 영상이 있으면 함께 담기되 원본 영상은 어떤 경우에도 담기지 않는다. 저장 라벨이 없으면 받을 것이 없다고 응답하고, 목록에 없는 영상과 신고 구간 영상은 거부된다 | |

## 단위시험 KLID-AT-UT-027

| 단위시험ID | KLID-AT-UT-027 | | |
|---|---|---|---|
| 설명 | 본인 영상을 직접 업로드해 이벤트 구간을 마킹하고, 그 마킹 지점에서 추출된 프레임에 사각형·다각형 수동 라벨링을 수행한 뒤 본인 데이터를 다운로드한다. <br>- 업로드한 영상은 마킹을 해야 프레임이 추출된다. <br>- 자동 마킹의 기본 간격은 300프레임이며 사용자가 바꿀 수 있다. <br>- 오토라벨링과 자동 추적·검수·버전관리는 이 경로에 없다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-027 | 관련 프로그램 ID | 포털 자산 업로드 · 업로드 영상 마킹 · 수동 라벨링 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 PortalUploadServiceTest·PortalUploadMarkingServiceTest·PortalFrameExtractRunnerTest·PortalUploadLabelServiceTest·PortalRetentionPolicyTest 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-027-01 | 영상 업로드와 마킹·프레임 추출 | PORTAL_USER | 본인 영상, 허용되지 않는 확장자, 확장자와 내용이 다른 파일 | 1. 포털 업로드 화면(KLID-AT-SC-033)에서 본인 영상을 업로드 <br>2. 포털 업로드 영상 마킹 화면(KLID-AT-SC-045)에서 자동 마킹의 기본 간격을 확인하고 값을 바꿔 저장 <br>3. 수동으로 이벤트 지점을 찍어 저장 <br>4. 마킹이 끝난 뒤 프레임이 추출되고 상태가 준비 완료로 바뀌는지 확인 <br>5. 허용되지 않는 확장자와 확장자만 영상인 파일로 업로드를 시도 <br>: 마킹을 해야 프레임이 추출된다 | 업로드한 영상이 마킹 대기 상태가 되고, 자동 마킹의 기본 간격은 300프레임이며 바꾼 값이 저장된다. 상한을 넘는 지점은 거부가 아니라 잘리고 잘린 사실이 응답에 드러나며, 수동에서 지점을 하나도 찍지 않으면 서버가 간격으로 대신 채우지 않고 거부한다. 마킹이 정한 프레임 번호만 그대로 추출되고 추출에 실패하면 만들다 만 파일이 정리된다. 허용되지 않는 확장자와 내용이 다른 파일은 저장되지 않는다 | |
| KLID-AT-UT-027-02 | 수동 라벨링과 본인 데이터 내려받기 | PORTAL_USER | 추출된 프레임, 타인 자산 | 1. 포털 라벨링 화면(KLID-AT-SC-029)에서 추출된 프레임에 사각형·다각형 라벨을 그려 저장 <br>2. 화면에 오토라벨링·자동 추적 기능이 없는지 확인 <br>3. 저장한 라벨을 다시 열어 확인하고 본인 데이터를 내려받는다 <br>4. 타인 자산과 존재하지 않는 자산에 각각 접근을 시도 <br>5. 업로드 자산의 만료 예정 시각을 확인 <br>: 본인 자산에만 접근할 수 있다 | 사각형·다각형 라벨이 저장되고 다시 열어 확인·수정할 수 있으며 화면에 오토라벨링·자동 추적·검수 기능이 없다. 메타는 오버레이가 아니라 그 자산의 원장에 적재된다. 타인 자산과 없는 자산은 있는지조차 알 수 없는 같은 응답으로 거절되고, 만료 예정 시각은 등록일과 라벨 마지막 저장일 중 늦은 쪽을 기준으로 계산된다 | |

## 단위시험 KLID-AT-UT-028

| 단위시험ID | KLID-AT-UT-028 | | |
|---|---|---|---|
| 설명 | 라벨 마스터와 속성 정의를 등록 · 수정 · 삭제하여 프리셋 · 오토라벨 · 라벨링 화면 전체가 참조하는 단일 진실원을 유지한다. <br>- 라벨명 · 형태 · 표시 색상은 마스터가 소유하며 화면과 프리셋은 이를 참조한다. <br>- AI 검출 분류를 이어 두어야 그 라벨이 자동 탐지 대상이 된다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-028 | 관련 프로그램 ID | 라벨 마스터 관리 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 LabelMasterControllerTest·LabelMasterServiceTest·LsLabelRepositoryTopOneDefenseTest 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-028-01 | 라벨 마스터 등록과 화면 반영 | REVIEWER | 라벨명 · 형태 · 색상, 중복 이름, 잘못된 색상 형식 | 1. 관리 > 라벨 관리 화면(KLID-AT-SC-035)에서 라벨명 · 형태 · 표시 색상을 지정해 등록 <br>2. 라벨링 캔버스 화면(KLID-AT-SC-005)에서 그 라벨이 선택 가능하고 지정한 색으로 표시되는지 확인 <br>3. 마스터에서 라벨명과 색상을 바꾼 뒤 기존 라벨에도 반영되는지 확인 <br>4. 이미 있는 이름, 대소문자 · 공백만 다른 이름, 잘못된 색상 형식으로 등록을 시도 <br>: 라벨명 · 형태 · 색상은 마스터가 단일 진실원이다 | 등록한 라벨이 라벨링 화면과 프리셋 선택 목록에 나타나고 마스터에서 바꾼 이름 · 색상이 기존 라벨에 즉시 반영된다. 중복 이름과 대소문자 · 공백만 다른 이름은 충돌로 거부되고 라벨명은 앞뒤 공백이 제거되어 저장되며, 잘못된 색상 형식과 빈 라벨명은 입력 검증에서 거부된다 | |
| KLID-AT-UT-028-02 | 검출 분류 매핑과 열람 권한 | REVIEWER·WORKER | 검출 분류를 이은 라벨과 잇지 않은 라벨 | 1. 라벨 관리 화면에서 라벨에 AI 검출 분류를 지정 <br>2. 라벨링 화면의 AI 탐지 대상 목록에서 그 라벨이 선택 가능한지 확인 <br>3. 검출 분류를 잇지 않은 라벨이 어떻게 표시되는지 확인 <br>4. 작업자 계정으로 라벨 목록 조회와 등록을 각각 시도 <br>: 검출 분류가 이어진 라벨만 자동 탐지 대상이 된다 | 검출 분류를 이은 라벨만 탐지 대상으로 선택되고 잇지 않은 라벨은 표시되되 선택할 수 없다. 작업자는 라벨 목록을 볼 수 있으나 등록 · 수정은 거부된다 | |

## 단위시험 KLID-AT-UT-029

| 단위시험ID | KLID-AT-UT-029 | | |
|---|---|---|---|
| 설명 | 작업자는 본인 배정 작업을, 검수자는 배치가 끝난 전체 작업을 시간축 한 축으로 정렬해 조회하고, 이벤트 유형(표시명 그룹) 필터와 상태별 요약 카드로 좁혀 본 뒤 배정 · 재배정과 라벨링 · 마킹 화면 진입을 수행한다. <br>- 요약 카드의 수치는 현재 쪽이 아니라 전체를 기준으로 센다. <br>- 정렬은 시간축 한 축이며 상태가 정렬 우선순위로 끼어들지 않는다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-029 | 관련 프로그램 ID | 작업 목록 조회 · 배정 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 TaskBoardSummaryTest·TaskBoardSearchConditionTest·AssignmentServiceTest·BoardWorkStatusContractTest 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-029-01 | 작업 목록 조회와 요약 · 필터 | REVIEWER·WORKER | 상태가 섞인 작업 다건, 표시명이 같은 상세 이벤트 코드 | 1. 작업 목록 화면(KLID-AT-SC-012)에서 검수자 · 작업자 계정으로 각각 보이는 범위를 확인 <br>2. 상단 상태별 요약 카드 수치를 확인하고 쪽을 넘겨도 바뀌지 않는지 확인 <br>3. 검색어와 이벤트 유형으로 걸러 목록과 카드가 함께 좁혀지는지 확인 <br>4. 목록에서 라벨링 · 마킹 화면으로 진입 <br>: 카드는 현재 쪽이 아니라 전체를 기준으로 센다 | 검수자는 전체를, 작업자는 본인 배정분만 보며 목록이 시간축으로 정렬되고 상태가 정렬에 끼어들지 않는다. 카드 수치는 쪽 크기와 무관하게 전체 기준이고 각 칸의 합이 총계와 일치하며 필터가 목록과 카드에 동일하게 적용된다. 이벤트 유형 필터에 같은 표시명이 중복되지 않고 대표 코드를 고르면 그 그룹의 영상이 모두 걸린다 | |
| KLID-AT-UT-029-02 | 배정 · 재배정과 이력 기록 | REVIEWER·WORKER | 미배정 작업, 이미 배정된 작업 | 1. 작업 목록 화면에서 미배정 작업을 작업자에게 배정 <br>2. 이미 배정된 작업을 다른 작업자로 재배정 <br>3. 배정 이력을 조회해 기록을 확인 <br>4. 작업자 · 포털 회원 계정으로 목록 조회와 배정을 각각 시도 <br>: 배정과 재배정은 검수자 이상이 수행한다 | 배정이 반영되고 재배정 시 이력에 재배정 기록이 남으며 이력이 없으면 최초 배정 한 건만 조회된다. 배정 처리가 중간에 실패하면 전체가 되돌아가고, 포털 회원과 역할 미배정 계정은 목록 조회가 거부되며 정의되지 않은 상태 값으로 거르면 조용히 무시되지 않고 거부된다 | |

## 단위시험 KLID-AT-UT-030

| 단위시험ID | KLID-AT-UT-030 | | |
|---|---|---|---|
| 설명 | 관리자가 사용자 목록을 검색 · 역할로 걸러 조회하고 사용자의 역할을 변경한다. <br>- 화면 진입에 관리자 역할이 필요하고, 역할 변경에는 거기에 관리자 단기 유효창이 함께 필요하다. <br>- 역할이 없는 내부 진입자는 작업자로 자동 등록된다. <br>- 마지막 관리자는 강등할 수 없다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-030 | 관련 프로그램 ID | 사용자 · 역할 관리 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 UserControllerTest·UserRoleFilterPagingIT·UserSearchKeywordAxisIT·LastAdminGuardIT·AutoWorkerRegistrationIT 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-030-01 | 사용자 목록 조회와 검색 · 필터 | ADMIN·REVIEWER | 역할이 섞인 사용자 다건 | 1. 관리 > 사용자 관리 화면(KLID-AT-SC-024)에서 로그인 아이디와 이름으로 각각 검색 <br>2. 역할로 걸러 조회하고 쪽을 넘겨 확인 <br>3. 해당 역할이 한 명도 없는 조건으로 조회 <br>4. 역할이 없는 내부 사용자가 진입한 뒤 목록에 어떻게 나타나는지 확인 <br>: 검색은 로그인 아이디와 이름을 대상으로 한다 | 로그인 아이디 · 이름으로는 부분 일치 검색이 되고 이메일로는 검색되지 않으나 응답의 이메일 값은 그대로 남는다. 역할 필터의 총 건수가 실제 매칭 수와 같고 쪽을 넘겨 전건에 도달하며 결과가 없으면 총 건수 0으로 표시된다. 역할이 없는 내부 진입자는 작업자로 자동 등록되고 관리자가 지정한 역할은 그 등록으로 되돌아가지 않는다 | |
| KLID-AT-UT-030-02 | 역할 변경과 마지막 관리자 보호 | ADMIN·REVIEWER | 관리자가 한 명인 상태와 두 명인 상태 | 1. 관리자 계정으로 유효창 없이 역할 변경을 시도 <br>2. 관리자 페이지 진입 화면(KLID-AT-SC-040)에서 유효창을 연 뒤 다시 역할 변경 <br>3. 관리자가 한 명뿐인 상태에서 그 관리자를 강등 시도 <br>4. 관리자를 한 명 더 만든 뒤 한 명을 내려 본다 <br>5. 검수자 계정으로 역할 변경을 시도 <br>: 역할 변경은 관리자 역할과 유효창을 함께 요구한다 | 유효창 없이는 역할 변경이 거부되고 유효창을 연 뒤에 반영되며 검수자는 조회만 가능하다. 관리자가 한 명뿐이면 강등이 거부되고 두 명이면 한 명을 내릴 수 있어 보호가 과하게 잠그지 않으며, 동시 요청이 겹쳐도 관리자가 0명이 되지 않는다 | |

## 단위시험 KLID-AT-UT-031

| 단위시험ID | KLID-AT-UT-031 | | |
|---|---|---|---|
| 설명 | 검수자가 시스템 설정 화면에서 배치 처리·AI 탐지 추론 · 라벨링 정밀도 · 비식별 옵션 값을 조회 · 수정하고 외부 연동 상태를 확인하며, 관리자가 연동 서버 주소를 조회 · 변경한다. <br>- 설정 키는 정해진 목록만 허용하고 값의 형식과 범위를 검증한다. <br>- 연동 서버 주소 변경에는 관리자 단기 유효창이 필요하다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-031 | 관련 프로그램 ID | 시스템 설정 관리 · 연동 상태 조회 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 SystemConfigControllerTest·AiDefaultsControllerTest·ManageHealthControllerTest 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-031-01 | 시스템 설정 조회 · 수정과 값 검증 | REVIEWER·WORKER | 배치 간격 · 동시 처리 수, 형식 위반 값, 목록 밖 키 | 1. 관리 > 시스템 설정 화면(KLID-AT-SC-025)에서 배치 처리·AI 추론 · 라벨링 정밀도 · 비식별 옵션 값을 확인 <br>2. 배치 처리 간격을 바꿔 저장하고 반영되는지 확인 <br>3. 숫자 항목에 문자열을, 동시 처리 수에 범위 밖 값을 넣어 저장을 시도 <br>4. 허용 목록에 없는 설정 키로 저장을 시도 <br>5. 작업자 계정으로 설정 변경을 시도 <br>: 설정 변경은 검수자 이상이 수행한다 | 허용된 설정 키 전체가 조회되고 값을 바꾸면 저장과 함께 반영된다. 형식 위반 · 범위 초과 · 목록 밖 키는 모두 거부되어 저장되지 않고, 작업자는 설정 변경이 거부된다 | |
| KLID-AT-UT-031-02 | 외부 연동 상태 확인과 서버 주소 변경 | REVIEWER·ADMIN·WORKER | 비식별 서버·AI 추론 서버 · 데이터베이스, 형식이 잘못된 주소 | 1. 시스템 설정 화면에서 외부 연동 상태를 확인하고 작업자 계정으로도 시도 <br>2. 관리자 페이지에서 연동 서버 주소 관리 화면(KLID-AT-SC-042) 진입 <br>3. 유효창 없이 주소 변경을 시도한 뒤 유효창을 열고 다시 변경 <br>4. 형식이 잘못된 주소를 입력해 저장을 시도 <br>: 주소 변경은 관리자 유효창을 요구한다 | 비식별 서버·AI 추론 서버 · 데이터베이스 상태가 표시되고 작업자는 조회가 거부된다. 유효창 없이는 주소 변경이 거부되고 유효창을 연 뒤에 반영되어 다음 호출부터 새 주소가 쓰이며, 형식이 잘못된 주소는 거부된다 | |

## 단위시험 KLID-AT-UT-032

| 단위시험ID | KLID-AT-UT-032 | | |
|---|---|---|---|
| 설명 | 검수자가 이벤트유형별 라벨 프리셋을 관리한다 — 이벤트유형 1건에 오토라벨 대상 라벨 세트를 지정하고 라벨 마스터와 연결한다. <br>- 이벤트유형 하나에 프리셋은 하나만 둔다. <br>- 프리셋은 라벨명 · 형태를 스스로 갖지 않고 마스터를 참조한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-032 | 관련 프로그램 ID | 라벨 프리셋 관리 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 PresetControllerTest·PresetServiceTest·LsLabelPresetTest·LsLabelPresetRepositoryTest·PresetRequestEventTypeCdSizeTest 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-032-01 | 프리셋 등록과 오토라벨 반영 | REVIEWER | 이벤트유형 1건, 라벨 마스터 여러 건, 이미 프리셋이 있는 이벤트유형 | 1. 관리 > 프리셋 관리 화면(KLID-AT-SC-026)에서 이벤트유형을 고르고 오토라벨 대상 라벨을 선택해 저장 <br>2. 라벨별 검출 분류 매핑과 실효 여부가 표시되는지 확인 <br>3. 그 이벤트유형의 영상으로 자동 처리를 돌려 선택한 라벨만 남는지 확인 <br>4. 이미 프리셋이 있는 이벤트유형과 등록되지 않은 이벤트유형으로 등록을 시도 <br>: 이벤트유형 하나에 프리셋은 하나만 둔다 | 프리셋이 등록되고 라벨별 검출 분류 매핑과 실효 여부가 함께 표시되며 자동 처리 결과에 고른 라벨만 남는다. 중복 이벤트유형은 어느 프리셋과 겹치는지 알리며 거부되고, 미등록 이벤트유형과 빈 값 · 길이 초과는 입력 검증에서 거부된다 | |
| KLID-AT-UT-032-02 | 라벨 세트 교체와 마스터 참조 | REVIEWER·WORKER | 라벨 여러 건, 빈 목록, 마스터에 없는 라벨 | 1. 프리셋의 라벨 목록을 바꿔 저장하고 순서가 반영되는지 확인 <br>2. 라벨 목록을 모두 비워 저장 <br>3. 마스터에 없는 라벨로 저장을 시도 <br>4. 마스터에서 라벨명을 바꾼 뒤 프리셋 조회에 반영되는지 확인 <br>5. 작업자 계정으로 프리셋 등록을 시도 <br>: 저장은 전체 교체이고 프리셋은 조회 시점에 마스터를 참조한다 | 목록에서 빠진 라벨은 삭제되고 순서가 갱신되며 같은 라벨을 두 번 넣어도 한 건으로 정리된다. 목록을 비우면 오토라벨 제외 선언이 되고, 마스터에 없는 라벨은 거부되며 마스터에서 바꾼 라벨명이 즉시 반영된다. 작업자는 등록이 거부된다 | |

## 단위시험 KLID-AT-UT-033

| 단위시험ID | KLID-AT-UT-033 | | |
|---|---|---|---|
| 설명 | 검수완료(APPROVED) 영상만 집계한 확정 기준 수치를 전체(진행중 포함) 수치와 함께, 관제 이벤트 유형의 표시명 그룹 기준으로 확인해 학습데이터 산출물 목표 대비 진척과 병목을 파악한다. <br>- 검수완료 기준과 전체 기준을 나란히 보여 주며 두 축을 섞지 않는다. <br>- 폐기한 프레임은 검수완료 이미지 수에서 빠지고 전체 누적에는 남는다. <br>- 표시명이 같은 상세 이벤트 코드는 대표 코드 한 칸으로 합산한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-033 | 관련 프로그램 ID | 전체 구축 현황 조회 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 StatsService·StatsController·StatsApprovedAggregationIT·StatsEventTypeGroupDistributionIT·OverallStatReportCsvWriter 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-033-01 | 검수완료 기준과 전체 기준 병기 조회 | REVIEWER·WORKER | 승인 · 미승인 영상 혼재, 폐기 프레임, 표시명이 같은 상세 이벤트 코드 | 1. 통계 > 전체 구축 현황 화면(KLID-AT-SC-021)에서 검수완료 기준과 전체 기준 수치가 나란히 표시되는지 확인 <br>2. 폐기한 프레임이 있는 영상으로 두 수치를 비교 <br>3. 이벤트 유형 분포에서 표시명이 같은 상세 코드가 어떻게 표시되는지 확인 <br>4. 일별 작업량이 30일 전량으로 나오는지 확인 <br>5. 작업자 계정으로 화면 진입을 시도 <br>: 검수완료 기준과 전체 기준은 다른 축이다 | 승인 영상만 검수완료 건수에 잡히고 폐기 프레임은 검수완료 이미지 수에서 빠지되 전체 누적에는 남으며, 검수완료 수치는 항상 전체 이하다. 표시명이 같은 상세 코드는 대표 코드 한 칸으로 합산되고 수집 대상이 아닌 코드는 어떤 칸에도 잡히지 않는다. 일별 작업량은 데이터가 없는 날도 0으로 채워 30일 전량이 나오고, 작업자는 접근이 거부된다 | |
| KLID-AT-UT-033-02 | 작업자별 현황과 보고서 내려받기 | REVIEWER | 작업자별 작업 실적, 작업자 0건 상태 | 1. 전체 구축 현황 화면에서 작업자별 현황 표의 항목 구성을 확인 <br>2. 오토라벨 비율이 표시되는지 확인 <br>3. 화면 내려받기로 보고서를 저장해 내용 확인 <br>4. 작업자 실적이 0건인 상태에서 다시 확인 <br>: 작업자행은 기존 5개 항목에 진행중 건수 · 오토라벨 비율을 더한 7개 항목이다 | 두 비율이 같은 백분율 축으로 표시되고 전체 통계와 작업자 통계의 오토라벨 비율이 환산 후 같다. 내려받은 보고서는 생성일시 첫 행과 5개 블록으로 구성되며 0인 날의 행도 빠지지 않고, 작업자가 0건이면 예외 없이 안내 문구만 남는다 | |

## 단위시험 KLID-AT-UT-034

| 단위시험ID | KLID-AT-UT-034 | | |
|---|---|---|---|
| 설명 | 작업자가 프레임 구간에서 여러 객체를 한 번에 찾아 따라가게 해, 프레임마다 손으로 그리는 수고를 던다. <br>- 시작 프레임에서 지정한 객체를 이후 프레임으로 전파해 좌표를 제안한다. <br>- 제안 결과는 저장하지 않고 좌표만 돌려주며 사람이 확인 · 수정한 뒤 저장한다. <br>- 한 번에 처리할 수 있는 분량에 상한이 있어 상한에 닿으면 거기까지의 결과와 이어 보낼 지점을 함께 준다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-034 | 관련 프로그램 ID | 온디맨드 자동 추적 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 Sam2TrackService·Sam2TrackBudget·Sam2TrackMockMessageWiring·Sam2TrackIdPattern 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-034-01 | 구간 자동 추적 실행과 결과 반영 | WORKER | 본인 배정 영상, 상한을 넘는 긴 구간, 타인 배정 영상 | 1. 라벨링 캔버스 화면(KLID-AT-SC-005)에서 시작 프레임의 객체를 지정하고 따라갈 구간을 정해 자동 추적 실행 <br>2. 이후 프레임에서 제안된 좌표를 확인하고 필요한 곳을 수정한 뒤 저장 <br>3. 상한을 넘는 긴 구간으로 실행해 결과가 어디까지 채워지는지 확인하고 이어갈 지점부터 다시 실행 <br>4. 실행 중 취소하고, 타인 배정 영상에서도 실행을 시도 <br>: 실행만으로는 저장되지 않는다 | 후속 프레임에 좌표가 제안되어 캔버스에 반영되고 저장 전에는 라벨이 남지 않는다. 지정한 형태가 박스면 외접 사각형으로, 폴리곤이면 폴리곤으로 돌아온다. 상한에 닿아도 그때까지의 결과는 살아 있고 이어 보낼 지점이 함께 오며, 취소하면 남은 프레임을 추론하지 않는다. 타인 배정 영상은 거부되고 외부 추론이 호출되지 않는다 | |

## 단위시험 KLID-AT-UT-035

| 단위시험ID | KLID-AT-UT-035 | | |
|---|---|---|---|
| 설명 | 외부에서 이미 라벨링이 끝난 산출물을 저작도구로 가져와 검수할 수 있는 상태로 만든다. <br>- 먼저 폴더를 검사해 무엇이 들어올지 확인하고, 검사는 아무것도 저장하지 않는다. <br>- 적재하면 영상과 프레임이 만들어지고 배정 없이 검수를 시작할 수 있다. <br>- 원본 상태로 적재하면 검수 승인 보류가 서고, 비식별이 끝난 것으로 적재하면 서지 않는다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-035 | 관련 프로그램 ID | 산출물 검사 · 산출물 적재 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 ImportScanService·ImportControllerIT·FirstAnnotationParser·ImportFileStager 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-035-01 | 산출물 폴더 검사와 미리보기 | REVIEWER | 표본 산출물 폴더(문서 5건 · 짝 없는 이미지 1장), 이미 가져온 산출물 | 1. 데이터 > 산출물 가져오기 화면(KLID-AT-SC-039)에서 폴더 경로를 지정하고 검사 실행 <br>2. 들어올 프레임 수와 라벨 수, 경고 목록을 확인 <br>3. 같은 폴더로 검사를 한 번 더 실행 <br>4. 이미 가져온 산출물과 허용 범위 밖 경로로 각각 검사를 시도 <br>: 검사는 아무것도 저장하지 않아 여러 번 해도 결과가 같다 | 문서 5건이 프레임으로 잡히고 짝 없는 이미지도 라벨 없는 프레임으로 잡히며, 짝이 없거나 선언 건수가 다른 항목은 거부하지 않고 경고로만 알린다. 검사 후에도 영상 · 프레임이 생성되지 않는다. 이미 가져온 산출물은 그 영상을 알려 주며 막고, 허용 범위 밖 경로는 아무것도 남기지 않고 거부된다 | |
| KLID-AT-UT-035-02 | 산출물 적재와 검수 착수 | ADMIN·REVIEWER | 비식별이 끝난 산출물, 원본 산출물, 미매핑 분류가 남은 산출물 | 1. 검사를 통과한 폴더를 적재하고 검수 목록 화면(KLID-AT-SC-018)에서 확인 <br>2. 프레임을 열어 라벨과 촬영환경 메타가 들어왔는지 확인하고 승인 <br>3. 원본 상태로 지정해 적재한 뒤 승인 보류가 서는지 확인 <br>4. 미매핑 분류가 남은 상태로 적재를 시도하고, 검수자 계정으로도 적재를 시도 <br>: 적재된 영상은 배정 없이 검수 목록에 나타난다 | 검수 대기 영상과 실제 파일 기준 프레임이 만들어지고 도형 라벨과 텍스트 항목이 갈라져 담기며, 승인하면 학습데이터 산출이 성공으로 마감된다. 원본 적재 시 승인 보류가 서고 프레임 개인정보는 메타에 원문으로 보관된다. 미매핑 분류가 남으면 적재되지 않고, 검수자는 적재에서만 막히며 검사와 이력 조회는 통과한다 | |

## 단위시험 KLID-AT-UT-036

| 단위시험ID | KLID-AT-UT-036 | | |
|---|---|---|---|
| 설명 | 이관 이력을 목록과 상세로 확인해 언제 누가 어떤 폴더를 가져왔고 얼마나 들어왔는지 알고, 실패한 이관은 그 사유를 되짚으며, 검수 승인 보류가 선 영상을 목록에서 가려낸다. <br>- 승인 보류 여부는 이관 성공 여부와 다른 축이라 따로 표시한다. <br>- 목록은 최근순 한 축으로만 정렬한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-036 | 관련 프로그램 ID | 이관 이력 조회 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 ImportHistoryQueryService·ImportHistoryControllerIT 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-036-01 | 이관 이력 목록 · 상세 조회 | REVIEWER·WORKER | 성공 · 실패 이력 혼재, 같은 성공 상태에 보류가 갈리는 영상 | 1. 데이터 > 산출물 가져오기 화면(KLID-AT-SC-039)의 이관 이력에서 이관 시각 · 수행자 · 폴더 경로 · 들어온 건수를 확인 <br>2. 이관은 성공했으나 승인 보류가 선 영상이 어떻게 표시되는지 확인 <br>3. 실패한 이력을 선택해 상세에서 경로와 실패 사유를 확인 <br>4. 쪽을 넘겨 조회하고 이관 상태로 걸러 본다 <br>5. 작업자 계정과 인증 없는 상태로 각각 조회를 시도 <br>: 승인 보류 여부는 이관 상태와 다른 축이다 | 목록이 최근순으로 정렬되고 상태가 정렬 우선순위로 끼어들지 않으며, 같은 성공 상태여도 영상마다 보류 판정이 갈려 표시된다. 실패 사유가 사람이 읽고 고칠 수 있는 문장으로 나오고 없는 이력은 찾을 수 없다는 응답이 온다. 계약에 없는 상태 값으로 거르면 조용히 0건이 아니라 거부되며, 작업자는 볼 수 없고 인증이 없으면 조회되지 않는다 | |

## 단위시험 KLID-AT-UT-037

| 단위시험ID | KLID-AT-UT-037 | | |
|---|---|---|---|
| 설명 | 외부에서 이벤트 마킹까지 끝낸 영상 여러 건을 한 번에 올려, 저작도구의 비식별부터 라벨링까지 이어지는 처리에 태운다. <br>- 마킹 문서와 영상을 이름으로 짝지어 건별로 적재 가능 여부를 먼저 판정한다. <br>- 적재하면 영상을 저작도구 저장소로 복사하고 문서의 시점으로 예약 마킹을 만든다. <br>- 처리 도중 오래 멈춘 항목은 다시 집을 수 있게 되돌린다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-037 | 관련 프로그램 ID | 마킹 영상 일괄 반입 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 MarkingImportScanService·MarkingImportItemProcessor·MarkingFolderScanner·MarkingDocumentParser·MarkingImportRecoveryIT 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-037-01 | 마킹 폴더 검사와 건별 판정 | REVIEWER·WORKER | 문서와 영상이 각각 다른 자리에 있는 폴더, 이름이 겹치는 영상, 속도가 어긋난 항목 | 1. 데이터 > 산출물 가져오기 화면(KLID-AT-SC-039)의 마킹 영상 반입에서 폴더 경로를 지정하고 검사 실행 <br>2. 문서와 영상이 이름으로 짝지어졌는지, 건별 판정과 사유가 나오는지 확인 <br>3. 어느 문서도 가리키지 않은 영상 수가 함께 표시되는지 확인 <br>4. 같은 폴더로 검사를 한 번 더 실행하고, 작업자 계정으로도 시도 <br>: 검사는 아무것도 저장하지 않아 여러 번 해도 결과가 같다 | 짝이 맞고 재생 속도가 일치하면 적재 가능으로 판정된다. 가리키는 영상이 없거나 같은 이름의 영상이 둘 이상이면 짐작하지 않고 건너뛸 대상으로 남기며 사유를 표시하고, 속도가 허용 오차를 넘어 다르면 적재할 수 없다고 판정한다. 미참조 영상 수는 이름 표시 상한과 무관하게 실제 전체 수로 알리며, 허용 범위 밖 폴더와 작업자 요청은 거부된다 | |
| KLID-AT-UT-037-02 | 마킹 영상 일괄 적재와 예약 마킹 생성 | ADMIN·REVIEWER | 검사를 통과한 항목 다건, 중복 식별자 항목 | 1. 검사 결과에서 적재할 항목을 골라 적재하고 요청이 곧바로 반환되는지 확인 <br>2. 영상 처리 현황 화면(KLID-AT-SC-008)에서 적재된 영상과 상태를 확인 <br>3. 마킹 화면(KLID-AT-SC-006)에서 예약 마킹의 시점이 문서와 같은지 확인 <br>4. 중복 식별자가 섞인 상태로 적재하고, 검수자 계정으로도 적재를 시도 <br>: 적재 경로는 원래 자리가 아니라 저작도구 저장소 안이다 | 파일이 저작도구 저장소로 복사되고 영상과 예약 마킹이 만들어지며 이관 이력에 한 줄로 남는다. 예약 마킹의 재생 속도는 영상에서 읽은 값을 쓰고 읽지 못하면 문서 역산값으로 물러선다. 중복 식별자와 속도가 크게 어긋난 항목은 복사도 적재도 하지 않고 건너뛰며, 고른 항목만 적재되고 검수자는 적재가 거부된다. 처리 중 오래 멈춘 항목은 이후 다시 집을 수 있게 되돌아간다 | |

## 단위시험 KLID-AT-UT-038

| 단위시험ID | KLID-AT-UT-038 | | |
|---|---|---|---|
| 설명 | 적재된 영상을 비식별을 선두로 자동 처리하고, 마킹이 끝나면 시계열 위탁 · 프레임 추출 · 오토라벨링 · 트랙 보간으로 이어지는 잔여 단계를 수행해 라벨링을 시작할 수 있는 상태로 만든다. <br>- 비식별이 끝나야 마킹을 시작할 수 있고, 마킹이 끝나야 잔여 단계가 돈다. <br>- 같은 영상에 여러 처리 요청이 겹쳐도 한 번만 처리한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-038 | 관련 프로그램 ID | 배치 진입 · 단계 실행 · 상태 전이 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 BatchPipelineReorderFlowTest·BatchPipelineConfigTest·BatchOrchestratorConcurrentEntryIT·YoloMockGatePipelineAbortTest 시험 본문에서 도출. 단계별 개별 검증은 KLID-AT-UT-006~012 가 담당한다.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-038-01 | 적재부터 라벨링 준비까지 전체 흐름 | - | 새로 적재된 영상, 비식별이 실패한 영상, 동시 진입 요청 | 1. 영상이 적재된 직후 영상 처리 현황 화면(KLID-AT-SC-008)에서 상태를 확인 <br>2. 비식별이 끝나 마킹을 시작할 수 있는 상태가 되는지 확인하고 마킹 화면(KLID-AT-SC-006)에서 마킹을 완료 <br>3. 잔여 단계가 돌아 라벨링을 시작할 수 있는 상태가 되는지 확인 <br>4. 비식별이 실패한 영상에 마킹을 시도 <br>5. 같은 영상에 처리 요청이 동시에 겹치게 한다 <br>: 처리 순서는 비식별을 선두로, 마킹 이후 시계열 위탁 · 프레임 추출 · 오토라벨링 · 트랙 보간이다 | 상태가 적재 대기에서 마킹 가능으로, 잔여 단계 완료 후 처리 완료로 전이하고 라벨링 캔버스에서 프레임과 자동 생성 라벨을 확인할 수 있다. 비식별이 실패하면 마킹 가능 상태가 되지 않고 마킹을 완료해도 잔여 단계가 시작되지 않으며, 앞 단계가 실패하면 뒤 단계가 강행되지 않는다. 동시 요청 중 한 건만 처리되고 나머지는 건너뛰며, 정상 완료 · 예외 어느 경우에도 처리 중 표시가 해제되어 재진입이 가능하다 | |

## 단위시험 KLID-AT-UT-039

| 단위시험ID | KLID-AT-UT-039 | | |
|---|---|---|---|
| 설명 | 라벨링 화면에서 현재 프레임에 대해 AI 탐지를 실행해 객체 후보 좌표를 받아 캔버스에 반영하고, 사람이 확인 · 수정한 뒤 저장한다. <br>- 탐지 결과는 저장하지 않고 좌표만 돌려주며 기존 라벨을 지우지 않는다. <br>- 라벨 마스터에 검출 분류가 이어진 라벨만 탐지 대상이 된다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-039 | 관련 프로그램 ID | 온라인 객체 탐지 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 AutolabelOnlineServiceTest 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-039-01 | 현재 프레임 AI 탐지 실행과 반영 | WORKER | 본인 배정 영상, 기존 라벨이 있는 프레임, 타인 배정 프레임 | 1. 라벨링 캔버스 화면(KLID-AT-SC-005)에서 현재 프레임에 AI 탐지를 실행 <br>2. 제안된 객체 후보가 캔버스에 그려지고 기존 라벨이 남아 있는지 확인 <br>3. 후보를 확인 · 수정한 뒤 저장 <br>4. 타인 배정 프레임과 다른 사람이 작업 중인 영상에서 각각 탐지를 시도 <br>: 탐지 실행만으로는 저장되지 않는다 | 좌표가 반환되어 캔버스에 반영되고 기존 라벨 행은 삭제되지 않으며, 검출이 없으면 0건으로 정상 반환된다. 이미지 경계를 벗어난 좌표는 경계로 맞춰 반영되고 형식 자체가 어긋난 응답은 전체를 반영하지 않는다. 타인 배정과 잠긴 영상은 거부되고 외부 추론이 호출되지 않으며, 실패해도 진행 표시가 해제되어 곧바로 다시 시도할 수 있다 | |
| KLID-AT-UT-039-02 | 탐지 대상 분류 지정과 게이팅 | WORKER | 검출 분류가 이어진 라벨과 이어지지 않은 라벨 | 1. 탐지할 분류를 지정해 실행하고, 지정하지 않고도 실행 <br>2. 검출 분류가 이어지지 않은 라벨만 골라 실행 <br>3. 이어진 라벨이 하나도 없는 상태에서 실행 <br>: 라벨 마스터에 검출 분류가 이어진 라벨만 탐지 대상이 된다 | 지정한 분류 중 이어진 것만 탐지에 쓰이고 지정하지 않으면 이어진 전체 분류로 탐지한다. 요청이 전부 이어지지 않은 분류이거나 이어진 라벨이 하나도 없으면 외부 추론을 호출하지 않고 빈 결과와 안내를 표시한다 | |

## 단위시험 KLID-AT-UT-040

| 단위시험ID | KLID-AT-UT-040 | | |
|---|---|---|---|
| 설명 | 검수자 이상이 공지 · 가이드라인 게시글을 작성 · 수정 · 삭제하고 발행 상태를 제어하며, 작업자는 발행된 글과 첨부를 열람한다. <br>- 작성 직후에는 발행 전 상태이며 발행해야 작업자에게 보인다. <br>- 첨부는 허용한 확장자와 크기만 받고 원본 파일명은 보관하되 저장은 다른 이름으로 한다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-040 | 관련 프로그램 ID | 공지 관리 · 열람 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 NoticeControllerTest·NoticeAttachControllerTest·NoticeServiceRoleHierarchyTest 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-040-01 | 공지 작성 · 발행과 열람 범위 | REVIEWER·WORKER·ADMIN | 공지 본문, 고정글, 제목이 다양한 공지 다건 | 1. 공지 작성 화면(KLID-AT-SC-036)에서 공지를 작성하고 공지 목록 화면(KLID-AT-SC-030)에서 발행 전 상태를 확인 <br>2. 작업자 계정으로 목록과 상세를 확인 <br>3. 검수자가 발행한 뒤 작업자 계정에서 다시 확인 <br>4. 제목으로 검색하고 쪽을 넘겨 목록을 조회 <br>5. 관리자 계정으로 발행 전 글의 목록 · 상세를 확인 <br>: 작성 직후는 발행 전 상태이며 발행해야 작업자에게 보인다 | 작성 직후 발행 전 상태로 생성되고 작업자 목록에는 발행된 글만 나오며 발행 전 글의 상세는 열리지 않는다. 발행하면 발행 상태와 일시가 설정되고 재발행해도 일시는 바뀌지 않는다. 검수자 목록에는 발행 전 글도 포함되고 고정글이 위로 오며, 검색은 키워드를 포함한 공지만 반환하고 특수문자가 만능 문자로 동작하지 않는다. 관리자는 검수자와 동일하게 발행 전 글을 다룬다 | |
| KLID-AT-UT-040-02 | 첨부 업로드 · 다운로드와 삭제 | REVIEWER·WORKER | 허용 확장자 파일, 실행 파일, 크기 초과 파일, 경로 문자가 섞인 파일명 | 1. 공지 작성 · 수정 화면에서 허용 확장자 파일을 첨부하고 원본 파일명이 보존되는지 확인 <br>2. 실행 파일과 크기 상한을 넘는 파일, 경로 문자가 섞인 파일명으로 첨부를 시도 <br>3. 작업자 계정으로 발행글 첨부를 내려받고 발행 전 글의 첨부도 시도 <br>4. 첨부와 공지를 각각 삭제하고 저장된 파일이 정리되는지 확인 <br>5. 작업자 계정으로 작성 · 업로드 · 삭제를 시도 <br>: 저장 파일명과 화면에 보이는 원본 파일명은 다르다 | 허용 확장자는 저장되고 원본 파일명이 보존되며 실행 파일과 크기 초과는 거부된다. 경로 문자가 섞인 파일명은 임의 이름으로 저장되고 원본 이름만 보관된다. 작업자는 발행글 첨부를 받을 수 있고 발행 전 글의 첨부와 다른 공지의 첨부 번호로는 받을 수 없다. 첨부 · 공지 삭제 시 저장된 파일도 함께 정리되고, 작업자의 작성 · 업로드 · 삭제는 거부된다 | |

## 단위시험 KLID-AT-UT-041

| 단위시험ID | KLID-AT-UT-041 | | |
|---|---|---|---|
| 설명 | 관제서버나 포털이 발급한 토큰을 인계받아 저작도구에 진입하고, 채널과 저작도구가 보관한 역할로 접근할 수 있는 기능이 갈린다. <br>- 저작도구는 독립 로그인 화면을 두지 않고 상위 시스템이 발급한 토큰을 검증한다. <br>- 채널이 다르면 역할이 같아도 접근 범위가 다르다. <br>- 관리자가 한 명도 없을 때만 최초 관리자 등록 창구가 열린다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-041 | 관련 프로그램 ID | 인증 인계 · 인가 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 SecurityConfigChannelTest·SecurityConfigRoleTest·SecurityRoleResolutionPhase3Test·AdminRoleAuthorizationTest·RoleClaimAutoRegisterIT·RoleClaimAvailabilityIT·AdminBootstrapReachabilityIT 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-041-01 | 토큰 인계 진입과 채널 · 역할 인가 | REVIEWER·WORKER·PORTAL_USER·ADMIN | 관제서버 발급 토큰, 포털 채널 토큰, 발급처가 다른 토큰 | 1. 관제서버에서 저작도구로 이동해 세션 인계 진입 화면(KLID-AT-SC-001)을 거쳐 진입하고 표시되는 역할과 메뉴를 확인 <br>2. 포털 채널 토큰으로 내부 영상 · 관리 기능에 접근을 시도하고 포털 기능에도 접근 <br>3. 내부 채널 토큰으로 포털 기능에 접근을 시도 <br>4. 관리자 계정으로 검수자 전용 기능과 작업자 전용 기능에 각각 접근 <br>5. 발급처가 다른 토큰으로 진입을 시도 <br>: 채널 격리는 역할로 뚫리지 않는다 | 인계받은 토큰으로 진입되고 저작도구가 보관한 역할로 인가가 이뤄지며, 역할이 해석되지 않는 사용자는 보호된 기능에서 거부된다. 포털 채널은 내부 · 관리 기능에서 거부되고 포털 기능만 접근되며 내부 채널은 포털 기능에서 거부된다. 관리자는 검수자 전용 기능에 통과하고 작업자 전용 기능에서는 거부되며, 발급처가 다른 토큰은 인증에 실패한다 | |
| KLID-AT-UT-041-02 | 최초 관리자 등록 창구의 개폐 | ADMIN | 관리자가 0명인 상태, 1명 이상인 상태 | 1. 관리자가 한 명도 없는 상태에서 관리자 등록 화면(KLID-AT-SC-002)에 진입해 개폐 상태를 확인 <br>2. 등록을 제출해 최초 관리자가 되는지 확인 <br>3. 관리자가 생긴 뒤 같은 화면에 다시 진입해 개폐 상태와 제출 결과를 확인 <br>4. 포털 채널 토큰으로 개폐 조회를 시도 <br>: 관리자가 0명일 때만 열린다 | 관리자가 0명이면 창구가 열려 있고 제출하면 관리자 역할이 부여되며, 역할이 없던 사용자도 이미 검수자인 사용자도 최초 관리자가 될 수 있다. 관리자가 한 명이라도 생기면 개폐 조회가 닫힘이 되고 제출도 거부되며 응답에 관리자 인원수는 담기지 않는다. 포털 채널의 조회는 거절된다 | |

## 단위시험 KLID-AT-UT-042

| 단위시험ID | KLID-AT-UT-042 | | |
|---|---|---|---|
| 설명 | 관제 인입을 기다리지 않고 손에 있는 영상 한 건을 저작도구에 직접 올려 처리에 태우고, 그 영상이 마킹 대기에 이르렀는지 결과 패널에서 확인해 다음 걸음으로 이어 간다. <br>- 대용량 영상을 나눠 올리며 중간에 끊겨도 이어 올릴 수 있다. <br>- 영상 기술 정보는 사람이 채우지 않으면 서버가 파일에서 읽어 채운다. | | |
| 관련 컴포넌트 ID | KLID-AT-CO-042 | 관련 프로그램 ID | 파일 업로드 영상 투입 |
> 출처: 신규 작성(개정 5.0). 검증 근거는 TusUploadServiceTest·UploadAdminSessionGateTest·InternalUploadMetaResolverTest·InternalUploadPathResolverTest·InternalUploadCreateRequestValidationTest·UploadMediaProbeFfprobeTest 시험 본문에서 도출.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-042-01 | 영상 한 건 업로드와 처리 투입 | ADMIN·REVIEWER | 영상 파일 1건, 필수 항목, 기술 항목을 비운 요청 | 1. 관리자 페이지에서 파일 업로드 화면(KLID-AT-SC-027)에 진입해 영상 파일과 필수 항목을 채워 업로드 <br>2. 업로드 완료 후 결과 패널에서 적재 결과를 확인 <br>3. 영상 처리 현황 화면(KLID-AT-SC-008)에서 그 영상이 마킹 대기에 이르렀는지 확인 <br>4. 영상 상세 화면(KLID-AT-SC-009)에서 해상도 · 프레임률 · 재생 시간 · 코덱이 채워졌는지 확인 <br>5. 검수자 계정으로 업로드 시작을 시도 <br>: 사람이 채우지 않은 기술 항목은 서버가 파일에서 읽어 채운다 | 업로드 시작 시 인입 기록이 대기 상태로 한 건 생기고 완료 후 파일이 인입 영역으로 옮겨지며 인입 기록이 중복 생성되지 않는다. 영상이 비식별을 거쳐 마킹 대기 상태에 이르고, 비운 기술 항목은 서버가 읽어 채우되 사람이 채운 값은 그대로 유지된다. 업로드 시작은 관리자 자리라 검수자는 거부된다 | |
| KLID-AT-UT-042-02 | 나눠 올리기 · 이어 올리기와 입력 검증 | ADMIN | 대용량 영상, 중단 후 재개, 잘못된 클립 식별자, 확장자를 위장한 파일 | 1. 대용량 영상을 업로드하는 도중 중단하고 진행 위치를 확인한 뒤 이어서 업로드 <br>2. 마지막 조각을 다시 보내 본다 <br>3. 클립 식별자에 경로 문자를 넣거나 길이를 넘겨 업로드를 시도 <br>4. 확장자만 영상인 파일을 올려 완료를 시도 <br>5. 업로드를 취소한 뒤 같은 식별자로 다시 올려 본다 <br>: 클립 식별자는 저장 파일명이 되므로 형식을 입구에서 제한한다 | 중단 지점부터 이어 올릴 수 있고 마지막 조각을 다시 보내도 결과가 같으며 인입 기록은 한 건이다. 경로 문자 · 길이 초과 · 형식 위반은 업로드 시작 단계에서 거부되어 인입 기록도 만들지 않는다. 파일 앞머리가 영상이 아니면 완료 시 거부되고 임시 파일이 정리되며, 취소하면 임시 파일이 지워지고 같은 식별자로 다시 올릴 수 있다 | |

---

유스케이스 커버리지: 활성 R1 UC 18종 전부 정상 케이스 보유
