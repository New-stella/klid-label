---
logicraft_item: ROLE-003
type: permission_role
version: 17
domain: null
project_id: 4ece2c3f-8e99-46f5-9580-71108a76e578
synced_at: 2026-09-15T13:22:05.879Z
status: CHANGED
prev_version: 15
content_hash: e9efd6e27054026100cbf168fb81d6118ec4afa5d9f149ea7e32bbba90197547
stale: false
raw: ./_raw/ROLE-003.json
links:
  based_on: ["[[ADR-013]]"]
  granted_on: ["[[API-254]]", "[[API-255]]", "[[API-256]]", "[[API-257]]", "[[API-258]]", "[[SCREEN-028]]", "[[SCREEN-029]]", "[[SCREEN-033]]", "[[SCREEN-044]]"]
  implements: ["[[IMPREC-103]]"]
  requires_backward: ["[[NAV-002]]", "[[SCREEN-028]]", "[[SCREEN-029]]", "[[SCREEN-033]]", "[[SCREEN-044]]", "[[SCREEN-045]]", "[[SCREEN-046]]"]
---

# 포털 회원 (PORTAL_USER)

## name

PORTAL_USER

## brownfield

### notes

### 2026-09-15 — ADR-013 반전 반영(포털 라벨링 AI 보조 제공)
- 사용자 확정(원문): 「포털향 라벨링 화면도 관제향이랑 동일하게 오토라벨링 기능이 들어가야해 … 스켈레톤빼고」 · 「ai 탐지, 분할, ai 자동 추적 3가지 기능」.
- 설명의 「이 역할에 두지 않는 것」에서 오토라벨링 · 인터랙티브 분할 · 자동추적을 빼고 AI 보조 절을 더했다. 미제공은 키포인트 · 선택 객체 AI 추적 · 트랙 번호 변경/병합/삭제/분할 · 시계열 위탁 연동 · 검수 · 버전관리 · 기여도 점수로 좁혔다. 구 서술은 폐기 표기로 남겼다.
- 권한 목록에 포털 AI 창구 넷을 더했다 — AI 탐지 API-255 · AI 분할 API-257 · AI 자동 추적 API-254(각 execute) · 기본값 조회 API-256(view). 기존 화면 권한 네 건은 지우지 않았고, 포털 라벨링 화면 권한의 조건에 AI 보조 사용 문장을 덧붙였다.
- 포털 AI 요청 취소 창구(사용자가 진행 중인 AI 요청을 끊는 포털 대응 창구)는 API-258 로 채번되어 권한 목록에 execute 로 더했다. 소유자 판정에 채널을 포함한다는 확정이 함께 있다.
- 선택 객체 추적의 포털 창구는 두지 않으므로 권한도 없다.

### status

preserved

### decided_by

ADR-013

### change_kind

- preserved
- permission-change

### diff_summary

1차 포털 회원 역할 보존(간편 라벨링·다운로드). 2차에서 데이터마트 로드분의 메타·이벤트 어노테이션 편집과 업로드 영상 한정 이벤트구간 마킹·AI 증강 연동 요청이 더해졌다 2026-09-15 에 라벨링 화면 안의 AI 보조(AI 탐지 · AI 분할 · AI 자동 추적) 사용이 더해졌다 — 포털 전용 창구로만 호출한다.

### legacy_source

#### type

role

#### identifier

PORTAL_USER

## description

포털 회원(외부 채널). 접근 범위는 본인 작업 데이터로 한정된다.

**데이터마트 경로** — 데이터마트에 등재된 영상을 고르는 목록은 포털이 자기 화면에서 제공한다. 거기서 고른 영상은 라벨링 화면으로 바로 들어가 새로 작업을 시작하고, 이미 저장해 둔 작업은 저작도구가 제공하는 본인 작업 목록에서 골라 마지막으로 저장한 프레임에서 이어서 작업한다. 포털이 맡는 것은 그 목록 화면과 진입 링크뿐이고, 라벨링 화면과 소재 적재·저장·본인 작업 목록·내려받기는 저작도구가 소유한다. 어느 경로로 들어가든 기존에 저장된 라벨·메타를 불러와 확인·수정·저장하며, 저장은 원본과 데이터마트를 수정하지 않는 단방향이라 사용자별 작업 데이터로 별도 적재된다.

**본인 자산 업로드 경로** — 본인 영상(mp4·mov·avi, 5GB, 재개 가능 업로드)을 직접 올려 수동 라벨링(사각형·다각형)한 뒤 본인 데이터를 내려받는다(작업 결과 JSON·원본). 이 경로는 내부 파이프라인(비식별 → 마킹 → 배치 → 검수)과 완전히 분리돼 있어 내부 적재 흐름과 데이터마트에 섞이지 않는다. 본인이 올린 영상에 한해 이벤트구간 마킹과 AI 증강 연동 요청도 수행한다. 이 증강 연동은 서버가 서버로 보내는 비동기 위탁이라, 사용자가 외부 채널에서 직접 호출하는 추론 엔드포인트와 성질이 다른 축이다. 이 증강은 저작도구가 자체 수행하며 요청·결과 확인·후속 작업·내려받기 넷이 모두 저작도구 안에서 끝난다 — 외부 채널이 제공하는 증강 요청 창구를 호출하지 않는다.

**메타·이벤트 어노테이션 편집** — 데이터마트에서 불러온 영상의 메타(촬영환경 · 프레임 설명 · 개인정보 판정 · 시계열 메타)와 이벤트 어노테이션을 화면에서 확인하고 직접 수정·추가한다. 결과는 라벨 저장과 같은 단방향이라 포털 전용 저장소에만 적재되고 데이터마트와 원본 동결본을 수정하지 않는다.

**다운로드** — 본인 작업 데이터를 기간 내에 내려받는다.

**AI 보조** — 라벨링 화면에서 AI 탐지 · AI 분할 · AI 자동 추적을 포털 전용 창구로 쓴다(2026-09-15 확정). 대상은 본인 작업 대상 프레임뿐이고 요청량 제한을 받으며, 결과는 저장되지 않아 기존 저장으로 확정한다.

**이 역할에 두지 않는 것** — 키포인트 · 선택 객체 AI 추적 · 트랙 편집 · 외부 시계열 분석 서버로 나가는 위탁 연동(호출 · 콜백) · 검수 · 버전관리 · 기여도 점수. 두지 않는 것은 위탁 연동이지 그 결과물인 시계열 메타가 아니다. 사용자가 직접 호출하는 추론은 포털 전용 창구 · 본인 작업 대상 인가 · 요청량 제한을 함께 둔 형태로만 열며 이 조건은 약화하지 않는다(구 「분할 보조 미제공」 서술 폐기).

진입 경로: 저작도구는 자체 로그인 화면을 갖지 않고 상위 시스템이 발급한 토큰을 인계받으며, 토큰의 역할 클레임과 채널 클레임으로 접근을 분기한다. 포털 회원은 외부 채널로 진입해 내부 채널(검수자·작업자)과 진입 경로가 다르다. 관리 권한은 관리자 역할이 소유하며 이 역할은 관리 화면에 접근하지 않는다. 관리자가 검수자를 물려받는 계층에 이 역할은 포함되지 않는다 — 채널이 달라 계층에 넣으면 외부 채널 사용자가 내부 권한을 얻는다. 다만 이 제한은 화면 축이며, 라벨링 화면이 라벨 분류와 표시명·색상을 그리기 위해 읽는 라벨 마스터 조회는 관리 화면이 아니라 라벨링이 의존하는 공용 읽기 계약이라 이 역할에도 허용된다. 노출되는 값은 라벨 분류 정의(라벨명·형태·색상·검출 클래스 매핑)이고 개인정보가 아니다. 라벨 마스터의 등록·수정·삭제는 검수자 전용이다.

선행조건(권한과 구분) — 데이터마트 경로에서 불러오는 내부 파이프라인 영상은 비식별 누락 신고가 열려 있는 동안 차단되며, 이 차단은 권한이 아니라 선행조건이라 역할과 무관하게 적용된다. 본인이 올린 업로드 자산은 내부 파이프라인 밖에 있어 이 신고 체계의 대상이 아니다.

## permissions

### [1]

**actions**:

- view

- **condition**: 포털 내 작업 화면 — 본인이 저장한 작업 목록에서 이어서 작업하고 결과물을 내려받는다. 저작 저장물의 파일 내려받기는 이 화면에서만 일어나며 외부 채널은 목록 조회까지만 가능하다. 데이터마트 영상을 고르는 목록은 포털이 자기 화면에서 제공한다
- **target_id**: SCREEN-028
- **target_kind**: screen_spec

### [2]

**actions**:

- view
- create
- update
- delete

- **condition**: 포털 라벨링 — 자산 출처를 가리지 않는 하나의 라벨링 화면이다. 데이터마트에서 불러온 영상과 본인이 올린 영상 모두에서 기존 라벨과 메타(촬영환경 · 프레임 설명 · 개인정보 판정 · 시계열 메타) · 이벤트 어노테이션을 확인·수정·추가하고 저장하며, 본인이 올린 영상에 한해 이벤트구간 마킹도 한다. 수동 라벨링은 사각형·다각형으로 한정된다. 저장처는 서버가 자산 출처로 판정한다 — 데이터마트 자산은 단방향이라 원본과 데이터마트를 수정하지 않고, 본인이 올린 자산은 그 자산에 그대로 쌓인다. 어느 출처든 본인 것만 대상이다 라벨링 화면 안에서 AI 보조 세 기능(AI 탐지 · AI 분할 · AI 자동 추적)을 쓸 수 있고, 그 결과도 같은 저장으로 확정한다 — 저장되는 도형은 여전히 사각형 · 다각형이다.
- **target_id**: SCREEN-029
- **target_kind**: screen_spec

### [3]

**actions**:

- view
- create
- delete

- **condition**: 포털 업로드 — 본인 자산만. 내부 파이프라인과 분리된 경로이며 본인 데이터 다운로드를 포함한다
- **target_id**: SCREEN-033
- **target_kind**: screen_spec

### [4]

**actions**:

- view

- **condition**: 포털 증강 화면 — 본인이 올린 영상의 증강 요청 현황과 결과를 확인하고, 결과물에 이어서 작업하러 가거나 결과물을 내려받는다. 증강 요청 자체는 포털 업로드 화면의 자산 행에서 건다
- **target_id**: SCREEN-044
- **target_kind**: screen_spec

### [5]

**actions**:

- execute

- **condition**: 포털 AI 탐지 — 한 프레임의 객체를 탐지해 사각형 · 다각형 후보를 돌려받는다. 대상 프레임이 본인 작업 대상(데이터마트 노출 영상 또는 본인 업로드 자산)일 때만 허용되고 그 밖은 거부된다. 결과는 저장되지 않으며 확정은 포털 라벨 저장으로 한다. 사용자별 요청량 제한을 받는다
- **target_id**: API-255
- **target_kind**: api_endpoint

### [6]

**actions**:

- execute

- **condition**: 포털 AI 분할 — 클릭이나 박스로 한 객체의 영역을 분할해 돌려받는다. 대상 프레임 인가 · 결과 미저장 · 사용자별 요청량 제한은 AI 탐지와 같다
- **target_id**: API-257
- **target_kind**: api_endpoint

### [7]

**actions**:

- execute

- **condition**: 포털 AI 자동 추적 — 시작 프레임과 뒤따르는 프레임 전부가 본인 작업 대상이고 같은 영상이어야 한다. 여러 객체를 트랙으로 묶은 결과를 돌려받으며 저장되지 않는다. 사용자별 요청량 제한을 받는다
- **target_id**: API-254
- **target_kind**: api_endpoint

### [8]

**actions**:

- view

- **condition**: 포털 AI 기본값 조회 — AI 보조의 정밀도 기본값과 AI 대기 예산을 읽는다
- **target_id**: API-256
- **target_kind**: api_endpoint

### [9]

**actions**:

- execute

- **condition**: 포털 AI 요청 취소 — 본인이 시작한 온디맨드 AI 추론(AI 탐지 · AI 분할 · AI 자동 추적)을 진행 중에 끊는다. 본인 여부는 채널과 사용자 식별자를 함께 보고 판정하며, 남의 요청이나 이미 끝난 요청은 끊지 못한 것으로 같게 응답한다
- **target_id**: API-258
- **target_kind**: api_endpoint

## inherits_from

_(empty)_

## attached_files

_(empty)_

## implementation

### status

implemented

### modules

_(empty)_

### records

- IMPREC-103

### progress

100

### subtasks

_(empty)_

### last_updated

2026-08-25T01:21:32.459Z

### module_paths

_(empty)_

## typical_actors

- 포털 일반 사용자
