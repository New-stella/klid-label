# 설계문서 정합성 검토 — 변경 이력

| 검토일 | 2026-05-26 |
|--------|-----------|
| 검토 범위 | D1~D9 설계문서 9종 ↔ CLAUDE.md 교차 대조 |
| 검토 방법 | 설계문서 전수 분석 → CLAUDE.md 불일치 식별 → 사용자 확인 후 양방향 반영 |

---

## 1. CLAUDE.md 변경 사항

### 1.1 DB 정책 전면 수정 (D9 기준 정합)

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| Flyway 설명 | `klid_system 공유 DB — 제한된 DDL만, 관제서버팀 협의` | `klid_system` DB 내 `klid_at` 스키마 — 저작도구 전용 테이블은 독립, MNG_* 공유 테이블 변경 시 관제서버팀 협의 |
| 테이블 수 | 재사용 31개 · `LS_DATA_LBL` 3컬럼 추가 · 신규 0개 | **저작도구 전용 신규 34개(LS_*) + 관제서버 재사용 9개(MNG_*) = 총 43개 테이블** |
| DB 공유 정책 | 전용 독립 DB 없음, 신규 테이블 없음 | `klid_at` 스키마에 LS_* 34개 신규 운영, MNG_* 9개 재사용 (READ 위주, JPA validate) |
| DB 공유 주의 | 1줄 (Flyway 협의만) | MNG_* 스키마 변경 시 Hibernate validate 기동 불가 위험 추가 |
| Quartz 중복 | 78행, 80행에 Quartz 11개 테이블 중복 기술 | 80행 1곳으로 통합 |

### 1.2 인증·진입 방식 변경

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| JWT 전달 방식 | 미명시 (D4는 URL 쿼리 파라미터 `?token=<JWT>`) | **동일 도메인 브라우저 스토리지(localStorage/sessionStorage) 공유**. URL 쿼리 파라미터 미사용 |

### 1.3 TASK_MODIFIED 통지 설계 변경

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 페이로드 | 변경 프레임별 라벨/메타 본문 데이터 포함 | **수정 요약만 전달** (프레임 ID + 변경 종류). 라벨/메타 본문 미포함 |
| 조회 패턴 | (미명시) | **관제서버가 통지 수신 후 저작도구 API를 호출하여 상세 데이터 직접 조회** |
| 외부 연동 주의 | 단방향 outbound만 부활 | 단방향 outbound 통지 + inbound 조회 API 제공 |

### 1.4 기술 스택 보강

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| Caffeine | 미기재 | **Caffeine** (로컬 캐시 — 시스템 설정 TTL 60s) 추가 |
| Micrometer | 미기재 | **Micrometer + Prometheus** (메트릭 수집) 추가 |
| Flyway 버전 | 미기재 | **Flyway 10.13** 버전 명시 |

### 1.5 배포·스케일링 모델 수정

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 배치 성능 | Quartz 1건/분 처리만 기술 | Spring Boot + Quartz는 **단일 인스턴스 서비스** 배포 (Docker/Pod 미사용, Quartz 클러스터 미적용). **ai-server(YOLO/SAM2)만 다중 인스턴스 수평 확장** |

### 1.6 도메인 범위 변경

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 프로젝트 목적 | "영상/이미지 라벨링, 검수, 비식별화, **학습데이터셋 내보내기**, 외부 메타 검토" | "영상/이미지 라벨링, 검수, 비식별화, 외부 메타 검토" (내보내기 제거) |
| 주요 도메인 | 내보내기 포함 | **내보내기 제거** |
| 범위 외 | V1.8까지 | **V1.9 추가**: 학습데이터셋 내보내기(Export) — 범위 외로 변경 |

---

## 2. D4 인터페이스설계서 변경 사항

### 2.1 II-009/010 JWT 인계 방식 변경

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 인터페이스 목록 II-009 방식 | `HTTPS Redirect + JWT(HS256)` | `동일 도메인 브라우저 스토리지 공유 + JWT(HS256)` |
| 인터페이스 목록 II-009 비고 | `INTERNAL 채널` | `INTERNAL 채널, 동일 도메인` |
| II-009 상세 제목 | 관제서버 JWT **발급** | 관제서버 JWT **인계** |
| II-009 방식 | `HTTP 302 Redirect (?token=<JWT>) → 본 도구 /ingress` | 동일 도메인 브라우저 스토리지 공유. URL 쿼리 파라미터 미사용 |
| II-009 수신 프로그램 | `SessionIngressPage / JwtAuthenticationFilter` | `JwtAuthenticationFilter` |
| II-010 상세 제목 | 포털서버 JWT **발급** | 포털서버 JWT **인계** |
| II-010 방식 | `HTTP 302 Redirect (?token=<JWT>)` | 동일 도메인 브라우저 스토리지 공유 (II-009와 동일 패턴) |
| II-010 수신 프로그램 | `SessionIngressPage` | `JwtAuthenticationFilter` |
| 시퀀스 다이어그램 | `JWT(REDIRECT)` | `JWT(스토리지 공유)` |

### 2.2 II-012 TASK_MODIFIED 통지 변경

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 인터페이스 목록 II-012 | `HTTPS REST + Idempotency + Chunked` | `HTTPS REST + Idempotency` / 비고: "수정 요약만, 관제가 조회" |
| 분할 | 64KB 초과 시 chunked | **제거** → 조회 패턴으로 변경 (관제서버가 저작도구 API 호출) |
| 속성 명세 제목 | (일반) | "수정 요약만 전달 — 라벨/메타 본문 미포함" 명시 |
| rawSn 설명 | (없음) | "수정된 영상 ID" |
| frameIds 설명 | "누적 변경 프레임" | "누적 변경 프레임 SRC_SN 목록" |
| changeTypes | (없음) | **신규 추가**: `String[]` — 변경 종류 (`LABEL_ADDED`, `LABEL_UPDATED`, `LABEL_DELETED`, `META_UPDATED`) |
| 조회 안내 | (없음) | "관제서버는 rawSn + frameIds를 기반으로 저작도구 조회 API를 호출" 주석 추가 |

---

## 3. D5 아키텍처설계서 변경 사항

### 3.1 배포 모델 — Docker/Pod → 서비스 배포

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| AD-05 결정 근거 | "단일 인스턴스 락 + 클러스터링 가능" | "단일 인스턴스 서비스 배포 (클러스터 미적용)" |
| 배포 다이어그램 App Tier | `Spring Boot Pod x N` + `Quartz Worker Pod x 1+` | **Spring Boot 단일 서비스 (REST + Webhook + Quartz 내장)** |
| 배포 다이어그램 AI Tier | `ai-server (FastAPI) GPU Worker` | **ai-server (FastAPI) GPU Worker x N (수평 확장)** |
| 다이어그램 화살표 | CTRL/PORT → `JWT redirect` | CTRL/PORT → `JWT(스토리지 공유)` |
| 다이어그램 외부 연동 | Quartz Worker Pod → DEID/VLM | Spring Boot 서비스 → DEID/VLM/ai-server 직접 호출 |
| RQ-NFR-PRF-02 | "Quartz SKIP LOCKED 큐 + 워커 Pod 수평 확장 + ai-server GPU 워커 분리" | "Quartz 단일 인스턴스 Job 처리 + **ai-server GPU Worker 다중 프로세스 수평 확장**" |
| RQ-NFR-AVL-02 | "Pod replica ≥ 2, Quartz 클러스터 모드" | "Spring Boot 단일 서비스 + **systemd 자동 재시작**" |
| 환경별 Quartz | stg: 클러스터, prd: 클러스터 | stg: **단일**, prd: **단일** |

---

## 4. 변경하지 않은 항목 (검토 후 현행 유지)

| 이슈 | 사유 |
|------|------|
| D9 `LS_DATA_LBL.LBL_SRC_CD` CHECK에 VLM 미추가 | VLM은 라벨 소스가 아닌 메타데이터 — `LBL_SRC_CD`와 무관 |
| D3 FE/ai-server 컴포넌트 미추가 | BE 컴포넌트만으로 충분. FE/ai-server는 코드로 관리 |
| D5 배포 토폴로지 상세 (replica 수 등) | CLAUDE.md에는 코드에 영향 주는 정보만 기재. 배포 상세는 D5 전담 |
