---
logicraft_item: API-230
type: api_endpoint
version: 3
domain: DOMAIN-004
project_id: 4ece2c3f-8e99-46f5-9580-71108a76e578
synced_at: 2026-09-03T07:39:10.263Z
status: NEW
prev_version: null
content_hash: 289250770a6eadb1cab2121f03bd9f5dc2aecd4a8c9bf6a3727bcc06c403dcf9
stale: false
raw: ./_raw/API-230.json
links:
  belongs_to_domain: ["[[DOMAIN-004]]"]
  verifies: ["[[AC-1090]]", "[[AC-1091]]"]
  consumes_backward: ["[[SCREEN-042]]"]
  references_backward: ["[[SCREEN-042]]"]
---

# DELETE /v1/manage/ai-servers/{srvrId}

## path

/v1/manage/ai-servers/{srvrId}

## tags

- manage
- ai-server

## method

DELETE

## summary

AI 장비 삭제

## security

### [1]

**jwt**:

- ADMIN

## triggers

_(empty)_

## responses

### 204

#### description

삭제됨. 본문 없음.

#### content_type

application/json

### 403

#### description

ADMIN 권한 없음. 관리자 단기 유효창이 없거나 만료된 경우에도 같은 응답이며 둘 중 어느 쪽인지 구분해 알리지 않는다 — 거부 사유에 내부 상태를 싣지 않는다. ErrorCode=FORBIDDEN

#### content_type

application/json

### 404

#### description

그 식별자의 장비가 없다. ErrorCode=NOT_FOUND

#### content_type

application/json

### 409

#### description

그 유형의 마지막 가용 장비라 지울 수 없다. ErrorCode=CONFLICT

#### content_type

application/json

## deprecated

false

## parameters

### srvrId

- **in**: path

**schema**:

#### type

string

- **required**: true
- **description**: 장비 식별자.

## description

AI 추론·외부 시계열 분석 장비의 노드 원장을 다루는 관리 창구다. 이 원장이 장비 주소의 진실원이며, 배포 설정값은 원장이 비어 있을 때 최초 1회 씨앗으로만 쓰인다.

주소 검증은 진실원이 옮겨갔다고 사라지지 않는다 — 저장되는 주소에도 스킴(http 또는 https)·형식·예약 대역 거부가 그대로 걸린다. **평문 http 와 사설 대역은 통과가 정상이다**(대역 차단은 폐기됐다). 거부되는 것은 클라우드 메타데이터·링크로컬 같은 예약 대역과 비허용 스킴·형식 위반이며, 거부 사유에 입력 원문·호스트·해석 결과를 싣지 않는다 — 실으면 그 응답이 내부망을 훑는 수단이 된다.

장비 유형은 추론(탐지·분할·추적)과 외부 시계열 분석 둘이며 유형마다 여러 대를 둘 수 있다. 부하를 세는 축이 유형마다 다르다 — 추론은 동기 호출이라 큐 길이가 곧 부하지만, 시계열은 위탁 후 콜백이라 큐를 볼 수 없어 우리 원장의 미결 위탁 수를 센다.

이 창구는 운영·관리 성격의 쓰기라 관리자 권한에 더해 유효한 관리자 단기 유효창을 함께 요구한다. 유효창은 권한을 대체하지 않고 가산되며 역할을 승격시키지도 않는다 — 관리자 권한은 그대로 필요하다. ⚠ 검수자는 이 창구를 열 수 없다. 계층(ROLE_ADMIN > ROLE_REVIEWER)은 관리자가 검수자 자리를 통과하게 할 뿐 그 반대는 성립하지 않는다.

★그 유형의 마지막 가용 장비는 지울 수 없다 — 상태 전이와 같은 이유다.

⚠ 위탁 원장의 장비 식별자는 외래키로 묶지 않는다. 그 원장은 감사 기록이라 장비 원장보다 오래 살아야 하고, 장비 행이 지워져도 어느 장비였는지는 남아야 하기 때문이다.

## attached_files

_(empty)_

## calls_external

_(empty)_

## implementation

### status

implemented

### modules

_(empty)_

### records

_(empty)_

### progress

100

### subtasks

_(empty)_

### last_updated

2026-09-01T14:34:13.771Z

### module_paths

_(empty)_

## uses_constants

_(empty)_

## implements_features

_(empty)_

## verifies_acceptance

- AC-1090
- AC-1091
