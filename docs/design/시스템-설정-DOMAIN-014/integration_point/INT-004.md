---
logicraft_item: INT-004
type: integration_point
version: 13
domain: DOMAIN-012
project_id: 4ece2c3f-8e99-46f5-9580-71108a76e578
synced_at: 2026-09-03T07:39:10.275Z
status: CHANGED
prev_version: 11
content_hash: 17703ce5993274c337724d4184adbb94e5a618cc94d8db66f537a1b9bce44a9d
stale: true
raw: ./_raw/INT-004.json
links:
  based_on: ["[[ADR-006]]"]
  belongs_to_domain: ["[[DOMAIN-012]]"]
  provided_by: ["[[EXTSYS-003]]"]
  references: ["[[INTSPEC-001]]"]
  consumes_backward: ["[[API-112]]"]
  references_backward: ["[[ADR-046]]", "[[ADR-062]]", "[[INTSPEC-001]]", "[[SEQ-012]]", "[[SEQ-025]]", "[[SEQ-030]]", "[[SEQ-031]]", "[[SEQ-035]]"]
---

# 비식별 처리 위탁 요청

## status

draft

## protocol

rest

## auth_type

none

## direction

outbound

## brownfield

### status

new

### decided_by

ADR-006

### change_kind

- component-replace

### diff_summary

캔버스 수동 블러 → 외부 비식별 위탁

## auth_detail

### notes

HMAC·토큰 인증 **없음**. 통신 신뢰는 스킴에만 의존 — `https` 일 때만 HTTP 클라이언트가 자체 CA(`ca.crt`)로 **서버 인증서를 검증**하며 클라이언트 인증서는 사용하지 않는다. `http` 스킴(내부망 운영 시)이면 완전 평문이므로 망 분리가 사실상의 방어선이다.

## data_format

json

## description

저작도구가 외부 비식별 솔루션(KPST)에 영상 비식별 처리를 위탁하는 outbound 요청. 비식별화는 **파이프라인 선두 단계**로, 영상 적재 직후 자동 트리거되며 성공해야 마킹 단계로 진입한다.

## 실측 계약 (2026-07-27 확인)
- 호출 주체: 저작도구의 비식별 위탁 연동(제출 오케스트레이션 포함)
- `GET /` — 연결 확인(connect)
- `POST /project` — 프로젝트 생성(위탁 제출)
  - 요청 `KpstProjectRequest`: `project_name` · `creator` · `export_path` · `input_path` · `files[]` · `masking_type` · `db_save` · `masking_range` · `exp_quality` · `exp_format`
  - 응답 `KpstProjectResponse`: `result` · `prj_id` (이후 폴링 INT-005 의 키)
- `POST /delete_project_id` — 재위탁 전 stale export 정리(구 산출물 오회수 방지)
- 파일 본문 미전송 — `input_path`/`export_path` 공유 경로로 주고받고 KPST 가 공유 스토리지에 직접 산출

## 활성화 — 프로파일별 실측
토글 `kpst.deid.enabled` 기본 `${KPST_DEID_ENABLED:true}`
| 프로파일 | enabled | base-url | 실질 동작 |
|---|---|---|---|
| 공통 기본값 | true | `https://localhost:9201` | — |
| local | true | `http://klid-mock-server:9400` | 목 서버를 바라본다 — 내부 mock 모드 미사용 |
| dev | true(상속) | `http://klid-mock-server:9400` | 목 서버를 바라본다 — 내부 mock 모드 미사용 |
| stg | true(상속) | 오버라이드 없음 | **실제 KPST 활성** |
| prd | true | env.template 기본 `https://127.0.0.1:9201`(실배포 시 실주소 교체 필수) | **실제 KPST 활성** |

**mock-mode** `authoring.integration.deidentify.mock-mode`(`${DEIDENTIFY_MOCK_MODE:false}`) — ⚠ **[폐기] "local `true` · dev `true` · stg/prd 오버라이드 없음→`false`"** 는 사실과 다르다. 이 모드는 **어느 프로파일에서도 설정하지 않으며 전 프로파일에서 꺼져 있다**(기본값 꺼짐). ⚠ **[폐기] "운영 프로파일에서 이 모드가 켜져 있으면 기동을 거부한다(fail-closed)"** — 판정하는 자리가 옮겨졌다. `ADR-062`(2026-09-03 승인) 「외부 연동 판정을 기동 시점에서 사용 시점으로 옮긴다」가 이 판정을 기동 시점 밖으로 옮겼다(옮겨간 자리는 아래 대체 규칙). 문장을 지우지 않고 무엇으로 대체됐는지를 함께 남긴다.

**대체 규칙 — 기동은 통과, 기록은 남긴다.** 운영 프로파일에서 이 모드가 켜져 있어도 **기동은 통과**하며, 기동 시점에는 예외 대신 **오류 수준 기록**을 남긴다. ⚠ **[폐기] "대신 비식별 산출이 일어나는 순간 거부한다"** — 그 자리는 **두지 않는다**(아래 전제 정정).

⚠ **[폐기] 전제 — "이 모드는 외부로 요청을 보내지 않고 원본을 그대로 복사할 뿐이다."** 그 복사 경로는 **폐지됐고 두지 않는다.** 이 모드는 비식별 산출물을 스스로 만들지 않으며 남은 자리는 **판정뿐**이다. 그때 이렇게 적은 이유는 이 판정이 지키려던 것 — **비식별되지 않은 영상이 비식별본 행세를 하고 산출물로 나가는 것** — 을 산출 지점에서 막으려 했기 때문이다. 지키려던 대상은 그대로 유효하나 전제가 사실과 다르다.

⚠ **[폐기] "막는 자리는 산출물이 만들어지는 지점이다."** — **대체 규칙**: 막을 산출 지점이 실재하지 않으므로, 남은 토글은 **위탁 요청층에서 거부**한다. 그 형상에서 비식별은 **전건 실패**하며 그 사실이 기동 기록과 상태로 드러난다.

★ **되살아날 때를 위한 경고.** 자체 복사 산출 경로가 다시 생기면 **이 배선으로는 막히지 않는다**(그 경로는 외부 요청을 타지 않는다). 그런 경로를 다시 만든다면 **산출 지점에 별도 차단을 함께** 넣어야 한다.

**프로파일별 강도는 그대로다** — 개발 계열(local · dev · stg)에서는 이 모드가 정상 동작하고, 운영 프로파일(prd)에서만 거부된다.

⚠ **대가(인지·수용)**: 운영에서 이 모드가 켜진 채 배포되면 **앱은 정상으로 떠 있는데 비식별만 전건 실패**한다. 조용한 실패가 이 결정의 주된 위험이므로 그 사실이 반드시 **기록과 상태로 드러나야 한다** — 기동 시 오류 수준 기록, 그리고 위탁을 시도할 때마다 남는 실패 기록과 그 영상의 비식별 실패 상태.

※ 로컬 검증은 내부 mock 모드가 아니라 **목 서버(:9400)를 바라보게 배선**하는 것이 운영 원칙이다(본 프로그램이 자체적으로 결과를 채우면 검증이 무효해짐).

## idempotency

true

## retry_policy

Resilience4j Retry `kpstDeid` — max-attempts 3, wait 1s, ×2 backoff, **4xx(400/403/404/409) 재시도 제외**. CircuitBreaker `kpstDeid` — 50% / window 10 / min-calls 5 / open 30s, 4xx ignore. 타임아웃: 기본 45s(DEFAULT_TIMEOUT), Netty connect 10s · response 60s 안전망.

## triggers_apis

_(empty)_

## attached_files

_(empty)_

## implementation

### status

implemented

### modules

_(empty)_

### records

_(empty)_

### progress

100

### subtasks

_(empty)_

### module_paths

_(empty)_

## pii_in_payload

true

## related_events

_(empty)_

## spec_artifacts

- INTSPEC-001

## transport_meta

### path

/project (생성) · / (GET, 연결확인) · /delete_project_id (POST, 정리)

### notes

KPST 제공 endpoint. base-url 은 `kpst.deid.base-url`. 재위탁 전 stale export 정리를 위해 delete_project_id 를 선행 호출하는 경로가 있다.

### method

POST

## external_system

EXTSYS-003

## invoked_by_apis

_(empty)_

## used_by_domains

- DOMAIN-012

## endpoints_per_env

_(empty)_

## referenced_in_sequences

_(empty)_
