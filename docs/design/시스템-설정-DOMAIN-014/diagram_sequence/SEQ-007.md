---
logicraft_item: SEQ-007
type: diagram_sequence
version: 7
domain: null
project_id: 4ece2c3f-8e99-46f5-9580-71108a76e578
synced_at: 2026-08-29T01:27:26.100Z
status: CHANGED
prev_version: 7
content_hash: 41ef8683bf976914f77b289602184dbe9172e3f2d4bd839a84be280df85469e3
stale: false
raw: ./_raw/SEQ-007.json
links:
  consumes: ["[[API-069]]"]
  realizes: ["[[UC-006]]"]
---

# 라벨링 정밀도 조절 — 설정 변경부터 SAM2 단순화 적용까지

## theme

neutral

## title

라벨링 정밀도 조절 — 설정 변경부터 SAM2 단순화 적용까지

## source

sequenceDiagram
    autonumber
    actor RV as 검수자(REVIEWER)
    participant SCC as SystemConfigController
    participant SCS as SystemConfigService
    participant DB as PostgreSQL (설정)
    participant CA as Caffeine 캐시 (TTL 60s)
    participant STS as Sam2TrackService

    RV->>SCC: PUT /v1/manage/configs/POLYGON_SIMPLIFY_TOLERANCE (value)
    alt WORKER 호출
        SCC-->>RV: 403 (REVIEWER 전용)
    else 값 범위 초과
        SCC-->>RV: 400 (@Valid)
    else 정상
        SCC->>SCS: update(key, value, claims)
        SCS->>DB: 설정값 저장
        SCS->>CA: 캐시 전체 무효화
        SCS-->>SCC: ConfigResponse
        SCC-->>RV: 200 OK
    end
    Note over STS,CA: 이후 SAM2 track 시 적용
    STS->>SCS: getDouble(POLYGON_SIMPLIFY_TOLERANCE)
    SCS->>CA: 캐시 조회
    alt 조회 실패
        STS->>STS: 기본값 1.0px fail-safe
    else 설정값
        CA-->>STS: epsilon
    end
    STS->>STS: PolygonSimplifier.simplify(epsilon)

## messages

_(empty)_

## fragments

_(empty)_

## description

SystemConfigController.update(PUT /v1/manage/configs/{key}, REVIEWER 전용, WORKER 호출 시 403) → SystemConfigService.update 가 POLYGON_SIMPLIFY_TOLERANCE 값을 LS 설정 테이블에 저장 + Caffeine 캐시(TTL 60s) 전체 무효화. 이후 Sam2TrackService.readSimplifyTolerance 가 해당 epsilon 을 조회해 폴리곤 단순화에 적용하며, 조회 실패 시 기본값(1.0px)으로 fail-safe 폴백.

## invokes_apis

- API-069

## participants

_(empty)_

## scenario_type

happy_path

## implementation

### status

implemented

### modules

_(empty)_

### records

- IMPREC-378

### progress

100

### subtasks

_(empty)_

### last_updated

2026-08-29T01:26:56.284Z

### module_paths

_(empty)_

## publishes_events

_(empty)_

## referenced_items

_(empty)_

## realizes_use_cases

- UC-006

## related_acceptances

_(empty)_

## related_requirements

_(empty)_

## referenced_integrations

_(empty)_
