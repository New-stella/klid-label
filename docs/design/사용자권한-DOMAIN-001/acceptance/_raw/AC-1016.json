{
  "item": {
    "id": "AC-1016",
    "project_id": "4ece2c3f-8e99-46f5-9580-71108a76e578",
    "type": "acceptance",
    "title": "UC-041 인증 인계와 채널·역할 인가 — 토큰 인계→발급처 검증→채널·역할 해석→인가 (happy)",
    "slug": "uc-041-인증-인계와-채널역할-인가-토큰-인계발급처-검증채널역할-해석인가-happy",
    "sub_id": null,
    "status": "draft",
    "data": {
      "notes": "증빙과 마지막 실행 기록은 2026-08-31 시점의 사실이라 그대로 두되, 그것이 덮는 범위는 그때 이 기준이 담고 있던 축까지다 — 그 뒤에 더해진 축과 AC-1098 로 옮겨 간 다섯 축은 이 증빙이 덮은 적이 없다. 검증 상태를 판단할 때 이 증빙 하나로 기준 전체가 확인됐다고 읽지 말 것.",
      "evidence": [
        {
          "type": "document",
          "result": "pass",
          "reference": "화면 시나리오 — [세션 인계 /ingress] 1) 관제/포털이 발급한 토큰으로 진입 2) 채널·역할이 해석되어 헤더에 역할 배지 표시 · 기대: 발급처 검증 통과·채널로 내부/포털 구분·저작도구 보관 역할로 인가, 역할 없는 내부 진입자는 작업자로 자동 등록",
          "captured_at": "2026-08-31T03:00:00Z"
        },
        {
          "type": "log",
          "result": "pass",
          "reference": "select ur.role_cd, count(*) from klid_at.ls_user_role ur group by ur.role_cd order by 1;\n\n결과 (검증환경 dev · 상용은 동일 쿼리로 확인):\n+-------------+-------+\n|   role_cd   | count |\n+-------------+-------+\n| PORTAL_USER |     1 |\n| REVIEWER    |     2 |\n| WORKER      |     3 |\n+-------------+-------+",
          "captured_at": "2026-08-31T03:00:00Z"
        }
      ],
      "last_run": {
        "passed": true,
        "run_at": "2026-08-31T03:00:00Z",
        "duration_ms": 0
      },
      "priority": "critical",
      "scenario": {
        "then": [
          "발급처(iss)를 검증한다 — iss 클레임이 없는 경우를 통과시킨다. iss 가 있는데 허용목록 밖이면 여전히 거부한다. 시크릿 서명검증과 exp(만료) 필수는 그대로 유지한다",
          "채널은 배포 향이 정한다 — 포털 향 배포본은 토큰의 채널 값을 읽지 않고 포털로 확정하고, 관제 향은 값이 있으면 그 값을 쓰고 없으면 내부로 본다. 배포 향이 선언되지 않았거나 값역 밖이면 내부로 닫는다",
          "내부 채널이면 저작도구가 보관한 역할로 인가를 해석한다 — userId 클레임으로 저작도구 마스터 LS_ACNT_USER.USER_ID 로 조회한다. 다중 매칭이면 어느 행으로 잇는지가 추측이 되므로 fail-closed 로 무권한 처리한다(CWE-639). 0건 매칭이면 인가 계층에서는 여전히 무권한(role=null)이지만 식별 계층에서는 진입 순간 로컬 userNo 를 발급한다 — 0건이 곧 아무것도 안 만듦은 아니다. 기존 숫자 sub(USER_NO) 식별 경로는 그대로 두고 바꾸지 않는다",
          "역할 없는 내부 진입자는 작업자로 자동 등록되고 배정 대상 목록에 나타난다(기존 역할 미덮음). 단 개발용 로그인이 발급하는 표준 계정은 이 자동 등록 대상이 아니라 역할 없는 상태로 남는다",
          "관리자는 검수자 자리를 계층으로 물려받되 작업자 전용 자리는 열리지 않는다",
          "세션 만료 시 각 상위 시스템 로그인 화면으로 되돌아간다",
          "내부 채널에서 비숫자 sub 를 userId 로 저작도구 마스터 LS_ACNT_USER.USER_ID 에서 유일 해석하면, principal 의 주체 식별자(sub)가 해석된 userNo(문자열)로 정규화되어 이후 per-user 스코핑·소유권·감사·작업 잠금이 raw 로그인 ID 가 아니라 실제 userNo 를 본다. 숫자 sub(USER_NO, 내부·포털) 경로는 정규화 전후 값이 같아 동작이 동일하다(회귀 없음)",
          "관제(비숫자 sub·INTERNAL 채널) 진입자가 userId 로 저작도구 마스터 LS_ACNT_USER.USER_ID 조회 0건이면, 진입 순간 로컬 userNo 를 disjoint 상위 시퀀스로 발급하고 USER_ID 안정키로 원자적 등록한다(role=null). 재진입·2노드 동시진입에 같은 USER_ID 는 같은 userNo 로 수렴한다. 발급된 userNo 로 principal 정규화가 이어져 role-claim 식별이 성립하고, 첫 진입자는 관리자 비밀번호로 ADMIN 부트스트랩에 성공한다. 신설 프로비저닝은 INTERNAL 채널 + 비숫자 sub 에만 적용되며 숫자 sub(내부·포털)·PORTAL 채널 경로는 무영향이다(회귀 없음)",
          "화면을 다시 불러올 때마다 인가 역할을 저작도구에 다시 물어 확보한다 — 서버가 알려 준 역할은 그 화면이 살아 있는 동안만 유효한 값이고 인계 토큰과 함께 보존되지 않기 때문이다. 그래서 화면을 다시 불러와도 역할이 유지되어 도착지가 같고 보던 자리로 이어진다",
          "역할에 따른 도착지 판정은 그 재확보가 끝난 뒤에 내린다 — 확보되기 전에 판정하면 아직 확인하지 못한 상태가 역할 없음으로 읽혀, 이어 보던 자리 대신 역할 부여를 요청하라는 안내로 잘못 보내진다",
          "이 재확보는 채널을 가리지 않는다 — 내부로 들어왔든 외부로 들어왔든 확인한다",
          "알아낸 역할을 인계 토큰을 두는 자리에 함께 보관하지 않는다 — 함께 보관하면 서버에서 역할이 바뀌거나 회수된 뒤에도 옛 값이 남아 실제 권한과 어긋난다",
          "세션이 끊긴 뒤 같은 인계 토큰으로 다시 서도 역할 보유자가 자기 도착지에 이른다 — 앞서 확보한 결과를 다시 쓰더라도 그 결과가 지금 상태에 실제로 반영돼 있어야 하며, 역할이 비어 있는 채로 판정하지 않는다. 같은 인계 토큰이라는 이유만으로 다시 묻기를 건너뛰면 세션이 갈린 뒤에는 역할이 상태에 앉아 있지 않아 도착지가 어긋난다",
          "확보 상태에 따라 판정이 갈린다 — 세션이 없으면 역할 판정을 통과시키고, 세션은 있는데 아직 확보하지 않았으면 역할 판정에 도달하지 않는다. 두 상태를 한 상태로 묶으면 미확보가 역할 없음으로 읽혀 이 단언이 무의미해진다",
          "종류가 refresh 로 명시된 토큰을 인증에 쓰지 않게 된 뒤에도, 토큰 종류 클레임이 없는 토큰(개발용 로그인 토큰·포털 인계 토큰 등)과 종류가 access 인 관제 토큰은 종전대로 수용되어 인계·채널·역할 해석과 인가가 그대로 이어진다 — 거부 대상은 종류가 refresh 로 명시된 토큰 하나뿐이며, 종류 클레임이 없다는 이유로 막히는 과잉 차단이 없다"
        ],
        "when": [
          "상위 시스템이 발급한 유효한 토큰을 채널 수단으로 인계받아 진입한다",
          "진입한 뒤 화면을 다시 불러온다",
          "세션이 끊긴 뒤 같은 인계 토큰으로 다시 선다",
          "토큰 종류 클레임이 없는 토큰이나 종류가 access 인 관제 토큰으로 인계받아 진입한다"
        ],
        "given": [
          "상위 시스템이 저작도구와 같은 발급처로 토큰을 발급한다",
          "저작도구는 세션 토큰을 스스로 발급하지 않는다"
        ]
      },
      "statement": "상위 시스템(관제서버/포털)이 발급한 토큰을 저작도구 프론트가 채널별 수단으로 인계받으면, 시스템이 발급처(iss)를 검증하고(iss 클레임이 없는 경우를 통과시킨다. iss 가 있는데 허용목록 밖이면 여전히 거부한다. 시크릿 서명검증과 exp(만료) 필수는 그대로 유지한다) 채널을 배포 향으로 정한 뒤(포털 향 배포본은 토큰의 채널 값을 읽지 않고 포털로 확정하고, 관제 향은 값이 있으면 그 값을 쓰고 없으면 내부로 본다. 배포 향이 선언되지 않았거나 값역 밖이면 내부로 닫는다), 내부 채널이면 저작도구가 보관한 역할로 인가를 해석하고(userId 클레임으로 저작도구 마스터 LS_ACNT_USER.USER_ID 로 조회한다. 다중 매칭이면 fail-closed 로 무권한 처리한다(CWE-639). 0건 매칭이면 인가 계층은 무권한(role=null)으로 남되 식별 계층은 진입 순간 로컬 userNo 를 발급한다 — 0건이 곧 아무것도 안 만듦은 아니다. 기존 숫자 sub(USER_NO) 식별 경로는 그대로 두고 바꾸지 않는다), 역할이 없는 내부 진입자는 그 시점에 작업자로 자동 등록한다(이미 부여된 역할은 덮지 않음). 단 개발용 로그인이 발급하는 표준 계정은 이 자동 등록 대상이 아니며, 역할 없는 상태로 남는다. 채널과 역할로 각 창구 접근을 판정하며 관리자는 검수자 자리를 계층으로 물려받되 작업자 전용 자리는 열리지 않는다 내부 채널에서 비숫자 sub 를 userId 로 유일 해석한 경우 principal 의 주체 식별자(sub)가 그 userNo(문자열)로 정규화되어, 이후 per-user 스코핑·소유권·감사·작업 잠금이 raw 로그인 ID 가 아니라 실제 userNo 를 본다(숫자 sub 는 정규화 전후 값이 같아 동작이 동일하다). 미등록 관제 진입자(비숫자 sub·INTERNAL 채널)가 조회 0건이면 진입 순간 로컬 userNo 를 disjoint 상위 시퀀스로 발급하고 USER_ID 안정키로 원자 등록하며(role=null), 재진입·동시진입에 같은 USER_ID 는 같은 userNo 로 수렴하고, 발급된 userNo 로 principal 정규화가 이어져 role-claim 이 성립한다(첫 진입자는 관리자 비밀번호로 ADMIN 부트스트랩에 성공). 신설 프로비저닝은 INTERNAL 채널 + 비숫자 sub 에만 적용되며 숫자 sub(내부·포털)·PORTAL 채널 경로는 무영향이다. 화면을 다시 불러올 때마다 인가 역할을 저작도구에 다시 물어 확보하며(서버가 알려 준 역할은 그 화면이 살아 있는 동안만 유효한 값이고 인계 토큰과 함께 보존되지 않는다), 역할에 따른 도착지 판정은 그 재확보가 끝난 뒤에 내린다 — 확보되기 전에 판정하면 아직 확인하지 못한 상태가 역할 없음으로 읽혀 이어 보던 자리 대신 역할 부여를 요청하라는 안내로 잘못 보내지기 때문이다. 그래서 화면을 다시 불러와도 역할이 유지되어 도착지가 같고 보던 자리로 이어진다. 이 재확보는 채널을 가리지 않으며, 알아낸 역할은 인계 토큰을 두는 자리에 함께 보관하지 않는다. 세션이 끊긴 뒤 같은 인계 토큰으로 다시 서는 경우에도 역할 보유자는 자기 도착지에 이른다 — 앞서 확보한 결과를 다시 쓰더라도 그 결과가 지금 상태에 실제로 반영돼 있어야 하며, 역할이 비어 있는 채로 판정하지 않는다(같은 인계 토큰이라는 이유만으로 다시 묻기를 건너뛰면 세션이 갈린 뒤에는 역할이 상태에 앉아 있지 않다). 확보 상태에 따라 판정이 갈린다 — 세션이 없으면 역할 판정을 통과시키고, 세션은 있는데 아직 확보하지 않았으면 역할 판정에 도달하지 않으며, 두 상태를 한 상태로 묶으면 미확보가 역할 없음으로 읽혀 이 단언이 무의미해진다. 종류 클레임이 refresh 로 명시된 토큰은 인증에 쓰지 않지만, 종류 클레임이 없는 토큰(개발용 로그인 토큰·포털 인계 토큰 등)과 종류가 access 인 관제 토큰은 종전대로 수용되어 위 인계·인가 해석이 그대로 이어진다 — 거부 대상은 종류가 refresh 로 명시된 토큰 하나뿐이며 종류 클레임이 없다는 이유로 막히는 과잉 차단이 없다([[ADR-063]]).",
      "brownfield": {
        "status": "new",
        "decided_by": "ADR-063"
      },
      "is_negative": false,
      "attached_files": [],
      "implementation": {
        "status": "implemented",
        "modules": [],
        "records": [
          "IMPREC-392",
          "IMPREC-437"
        ],
        "progress": 100,
        "subtasks": [],
        "last_updated": "2026-09-08T12:03:41.782Z",
        "module_paths": []
      },
      "derived_domain_ids": [
        "DOMAIN-001"
      ],
      "verification_method": "automated_test",
      "verification_status": "unverified",
      "derived_from_use_cases": [
        "UC-041"
      ]
    },
    "data_projected_to": null,
    "schema_version": 1,
    "current_version": 17,
    "stale": true,
    "stale_reason": "UC-041의 2개 필드 변경 — derived_from",
    "change_summary": "채널 판정 서술 1건을 ADR-012 「채널 판정 축 — 배포 향이 정한다」(2026-09-09 확정)에 맞춘다 — cascade 출처 ADR-012 · 같은 드리프트를 먼저 정정한 SEQ-034(CO-20260910-관제향-토큰갱신-세션연장 라운드). 결과 두 번째 단언의 구 문장 「채널 표식으로 내부/포털이 갈린다(표식 없으면 내부)」와 본문의 같은 서술 「채널 표식으로 내부/포털을 가른 뒤(표식 없으면 내부)」를 현재 규칙으로 교체했다 — 채널은 배포 향이 정하며 포털 향 배포본은 토큰의 채널 값을 읽지 않고 포털로 확정하고, 관제 향은 값이 있으면 그 값을 쓰고 없으면 내부로 보며, 배포 향이 선언되지 않았거나 값역 밖이면 내부로 닫는다. 문구는 SEQ-034 채널 판정 단계와 글자 그대로 맞췄다(층마다 갈리지 않게). 두 자리를 함께 고쳤다 — 한쪽만 고치면 같은 기준 안에서 앞뒤가 어긋난다. 원소 안 문장만 교체했고 원소 추가·삭제는 없다. 그 밖의 단언(결정 ⑦ 회귀 가드 포함)·조건·계기·증빙·마지막 실행 기록·검증 수단·검증 상태·구현 상태·상태는 건드리지 않았다. 증빙의 「채널로 내부/포털 구분」 서술은 2026-08-31 시점 기록이라 소급 수정하지 않았다. 쓰기 전 stale=true(사유: UC-041 의 data.tech_considerations 변경 — derived_from)였고 이 쓰기는 그 전파 내용을 대조하지 않았다 — 그 정합은 별도 확인 대상이다.",
    "last_updated_by": "claude",
    "last_updated_at": "2026-09-10T10:20:43.915Z",
    "created_at": "2026-08-31T02:10:30.533Z"
  }
}