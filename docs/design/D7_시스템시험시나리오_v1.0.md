# D7. 시스템시험 시나리오

## ▣ 제·개정 이력

| 날짜 | 버전 | 작성자 | 승인자 | 내용 |
|---|---|---|---|---|
| 2026-05-21 | 1.0 | 박찬기 | - | 최초 작성 — 비기능 요구사항 시험(성능/부하/볼륨/보안/장애복구/관측성) |

---

| 시스템명 | 기반 지방정부 관제지원시스템 구축(2차) AI CCTV | 서브시스템명 | AI CCTV 학습데이터 저작도구 (KLID-AT-SS-001) |
|---|---|---|---|
| **단계명** | 설계 | **작성일자 / 버전** | 2026-05-21 / 1.0 |

> 본 시나리오는 D5 § 3.1 의 비기능 요구사항(RQ-NFR-*)을 1:1 검증한다. 시험 시나리오 ID 는 `KLID-ST-NNN` 규칙을 따른다.

---

## 시험 시나리오 목록

| 시험 유형 | 관련 요구사항 ID | 시험 시나리오 ID | 시험 시나리오 | 시험 절차 | 만족기준 | 시험결과 | 비고 |
|---|---|---|---|---|---|---|---|
| 성능 | RQ-NFR-PRF-01 | KLID-ST-001 | 라벨 저장 응답시간 | k6 30 VU × 5분, PUT `/v1/frames/{srcSn}/labels` (100점 라벨 5개) | p95 ≤ 1000ms, p99 ≤ 2000ms, 오류율 0% | (실행 시 기록) | k6 |
| 성능 | RQ-NFR-PRF-01 | KLID-ST-002 | 영상 목록 조회 응답시간 | k6 50 VU × 5분, GET `/v1/videos?page=0&size=20` | p95 ≤ 2000ms, 오류율 0% | |
| 성능 | RQ-NFR-PRF-01 | KLID-ST-003 | 대시보드 요약 응답시간 | 100 VU × 3분 GET `/v1/stats/summary` | p95 ≤ 800ms, polling 30s 정상 | |
| 성능 | RQ-NFR-PRF-02 | KLID-ST-004 | 자동 파이프라인 처리량 | LS_DATA_RAW 1000건 PENDING 적재 → Quartz 1분 간격 1시간 가동 → 처리율 측정 | ≥ 50건/시간, 실패 ≤ 2% | |
| 부하 | RQ-NFR-PRF-02 | KLID-ST-005 | 동시 다중 영상 파이프라인 | Worker Pod 2개 → 1000건 처리 → SKIP LOCKED 중복 처리 0건 검증 | 중복 처리 0건, 처리율 선형 증가 | |
| 부하 | RQ-NFR-PRF-01 | KLID-ST-006 | 라벨링 캔버스 사용성 부하 | Playwright 30 동시 세션 × 라벨 100점/3초 저장 × 30분 | 모든 저장 성공, 504/502 0건 | |
| 볼륨 | RQ-NFR-DAT-01 | KLID-ST-007 | Gitea 커밋 누적 볼륨 | 1영상 × 200회 라벨 수정 → Gitea 커밋 200건 → diff 조회 응답시간 | listVersions p95 ≤ 3초, diff p95 ≤ 2초 | |
| 볼륨 | RQ-NFR-DAT-02 | KLID-ST-008 | Object Storage 영상 저장 볼륨 | 영상 100건(평균 500MB) 원본+비식별 적재 → 100GB | 저장 성공, 별도 prefix 분리 검증 | |
| 보안 | RQ-NFR-SEC-01 | KLID-ST-009 | JWT 알고리즘 강제 (alg=none, alg=RS256 거부) | curl로 `alg=none` JWT, `alg=RS256` JWT 시도 | 둘 다 401, 본 도구 로그에 alg violation 기록 | | OWASP A05 |
| 보안 | RQ-NFR-SEC-01 | KLID-ST-010 | JWT 만료·재사용 차단 | exp 초과 JWT, 동일 jti 재사용 시도 | 둘 다 401 | |
| 보안 | RQ-NFR-SEC-02 | KLID-ST-011 | 채널·역할 분리 (PORTAL → INTERNAL 접근) | PORTAL_USER JWT 로 `/v1/reviews` 호출 | 403 Forbidden, `/forbidden` redirect | | ChannelGuard |
| 보안 | RQ-NFR-SEC-03 | KLID-ST-012 | 웹훅 HMAC 서명 위조 차단 | 위조된 X-Signature 로 `/v1/deidentify/result` POST | 401, body 처리 안됨 | | HmacWebhookFilter |
| 보안 | RQ-NFR-SEC-04 | KLID-ST-013 | IDOR 차단 (타인 프레임 라벨 조회) | WORKER A 의 JWT 로 WORKER B 의 srcSn 조회 | 403 또는 404, 로그에 IDOR 시도 기록 | | LabelAccessGuard |
| 보안 | RQ-NFR-SEC-05 | KLID-ST-014 | 좌표 점 1001개 라벨 저장 시도 | PUT `/v1/frames/{srcSn}/labels` body에 점 1001개 | 400 Bad Request, 메시지에 MAX_POINTS_PER_LABEL 안내 | | CWE-770 |
| 보안 | RQ-NFR-SEC-05 | KLID-ST-015 | TASK_MODIFIED 페이로드 64KB 초과 분할 | 단일 통지 페이로드 100KB 입력 → 송신 검증 | 2개 청크로 분할, 동일 requestId + chunkIndex/Total | |
| 보안 | RQ-NFR-SEC-06 | KLID-ST-016 | 로그 인젝션 차단 | reason 에 `\r\n[FAKE LOG]...` 포함 신고 등록 | 저장은 성공, 로그에 개행 제거됨 (sanitize) | | CWE-117 |
| 보안 | RQ-NFR-SEC-06 | KLID-ST-017 | 200자 초과 reason 로그 절단 | reason 300자 신고 → 로그 확인 | 로그에 200자 + `...(truncated)` | | CWE-359 |
| 보안 | OWASP A03 | KLID-ST-018 | 의존성 잠금 무결성 | gradle.lockfile 무결성 검증 (`./gradlew dependencies --no-write-locks`) | 변경 0건, 락 위반 시 빌드 실패 | | SCA |
| 보안 | OWASP A06 | KLID-ST-019 | OWASP ZAP DAST 스캔 | 주요 엔드포인트 100개 ZAP 자동 스캔 | High 0건, Medium 5건 이하 (이슈별 조치 계획) | |
| 보안 | RQ-NFR-SEC-02 | KLID-ST-020 | 완료 작업 재배정 이중 차단 | REVIEWER A 가 COMPLETED 영상 재배정 요청 | UI 비활성 + 서버 400/409 | |
| 장애복구 | RQ-NFR-REL-01 | KLID-ST-021 | Gitea 장애 시 폴백 큐 적재 | Gitea 컨테이너 정지 → 라벨 저장 10건 | 라벨 저장 200, 폴백 큐 깊이=10, Gitea 복구 후 자동 커밋 100% | |
| 장애복구 | RQ-NFR-REL-01 | KLID-ST-022 | 외부 비식별 솔루션 타임아웃 시 재시도 | WireMock 30s 지연 → DeidentifyClient 호출 | Retry 3회 → BatchRetryQueue.enqueue, 원본 영상 보존 | |
| 장애복구 | RQ-NFR-REL-02 | KLID-ST-023 | 중복 콜백 멱등성 | 동일 requestId 로 /v1/deidentify/result 2회 POST | 1회만 처리, 2회차는 200 ok + no-op, 멱등성 카운터 +1 | |
| 장애복구 | RQ-NFR-REL-03 | KLID-ST-024 | 비식별 누락 0건 정책 검증 | 영상 1000건 자동 파이프라인 → 비식별 단계 누락 검사 | LsDeidentProcLog 1000건, 누락 0건 | |
| 장애복구 | RQ-NFR-REL-04 | KLID-ST-025 | 라벨 우선 저장 원칙 | Gitea 차단 상태에서 라벨 200건 저장 | LS_DATA_LBL 200건, LS_LABEL_VERSION 0건, 폴백 큐 200건 | |
| 장애복구 | RQ-NFR-AVL-01 | KLID-ST-026 | Rolling Update 무중단 | App Tier Pod 1개씩 순차 재시작 (총 4분) | 진행 중 트래픽 5xx 0건, 라벨 저장 진행 중 사용자 영향 0 | |
| 장애복구 | RQ-NFR-AVL-02 | KLID-ST-027 | DB Primary 장애 → Replica 승격 | MariaDB Primary 강제 종료 | RTO ≤ 30초, 읽기 트래픽 지속, 쓰기는 승격 후 재개 | |
| 관측성 | RQ-NFR-OBS-01 | KLID-ST-028 | 요청 추적 (X-Request-ID) | 클라이언트가 X-Request-ID 헤더 동봉 → 응답 + 모든 로그에 동일 ID | 모든 로그 라인에 동일 requestId 출현 | | RequestIdFilter |
| 관측성 | RQ-NFR-OBS-02 | KLID-ST-029 | 메트릭 노출 (Prometheus) | `/actuator/prometheus` GET | 핵심 메트릭 8종 노출: batch_completed_total, label_save_p95, webhook_duplicate_count, cb_state, gitea_fallback_queue_depth, control_notify_inflight, control_notify_dead, jvm_memory_used | |
| 관측성 | RQ-NFR-OBS-02 | KLID-ST-030 | Grafana 대시보드 패널 가동 | 8개 패널 시각화 확인 | 패널 100% 데이터 수신, 알람 룰 동작 | |
| 통신 | RQ-NFR-PRF-01 | KLID-ST-031 | TUS 청크 업로드 재개 | 2GB 영상 50% 업로드 후 네트워크 단절 → 재개 | 끊긴 지점부터 재개, 무결성 해시 일치 | | tus.io |
| 통신 | RQ-NFR-PRF-01 | KLID-ST-032 | WebSocket 연결 안정성 | 30분간 100개 동시 WebSocket 세션 유지 | 연결 끊김 0건, 메시지 손실 0건 | |
| 호환성 | RQ-NFR-MAI-02 | KLID-ST-033 | OpenAPI 스키마 ↔ FE 타입 정합성 | `npm run gen-api` 실행 후 FE 빌드 | 빌드 성공, 타입 에러 0건 | |
| 마이그레이션 | RQ-NFR-MAI-01 | KLID-ST-034 | Flyway 마이그레이션 무결성 | 신규 환경에 Flyway 전체 적용 → 운영 백업과 schema diff | diff 0건, 모든 V*__*.sql 적용 성공 | |
| 통지 | RQ-NFR-SEC-05 / SEC-06 | KLID-ST-035 | TASK_COMPLETED 즉시 통지 | REVIEWER 승인 1회 | 이벤트 발행 → 1초 내 ControlNotifyClient 호출 | |
| 통지 | UC-015 | KLID-ST-036 | TASK_MODIFIED 디바운스 | 60s 윈도우 내 동일 영상 10회 라벨 수정 | 60s 후 1회만 송신, frameIds 10개 누적 | |
| 통지 | RQ-NFR-REL-01 | KLID-ST-037 | TASK_* 통지 실패 → 폴백 | 관제서버 5xx → 통지 5건 시도 | 폴백 큐 적재 5건, 재시도 스케줄러 가동, 복구 후 SUCCESS 전이 | |
---

## 시험 시나리오 상세 (대표 12건)

### KLID-ST-001  라벨 저장 응답시간

| 항목 | 내용 |
|---|---|
| 시험 유형 | 성능 |
| 관련 요구사항 ID | RQ-NFR-PRF-01 |
| 시험 시나리오 | 동시 30명 WORKER 가 5분간 지속적으로 라벨을 저장할 때, 서버 응답 시간이 SLA 를 만족하는지 검증 |
| 시험 절차 | (1) stg 환경 활성, (2) `/v1/dev/tokens`로 30개 WORKER JWT 발급, (3) k6 스크립트 30 VU × 5분, PUT `/v1/frames/{srcSn}/labels` payload (5 라벨 × 100점) 반복, (4) Prometheus에서 `label_save_p95` 측정 |
| 만족기준 | p95 ≤ 1000ms, p99 ≤ 2000ms, 오류율 0%, CPU ≤ 70%, Heap ≤ 70% |
| 시험결과 | (시험 실행 후 기록) |

### KLID-ST-004  자동 파이프라인 처리량

| 항목 | 내용 |
|---|---|
| 시험 유형 | 성능 |
| 관련 요구사항 ID | RQ-NFR-PRF-02 |
| 시험 시나리오 | Quartz 스케줄러가 1분 간격으로 1건씩 자동 파이프라인 처리 시 시간당 처리량 검증 |
| 시험 절차 | (1) LS_DATA_RAW 1000건 PENDING 적재, (2) Worker Pod 1개 가동, (3) 1시간 후 LS_RAW_DATA_STATUS COMPLETED 건수 카운트 |
| 만족기준 | ≥ 50건/시간, 실패 ≤ 2% (FAILED 상태) |
| 시험결과 |
### KLID-ST-009  JWT 알고리즘 강제

| 항목 | 내용 |
|---|---|
| 시험 유형 | 보안 |
| 관련 요구사항 ID | RQ-NFR-SEC-01 |
| 시험 시나리오 | 본 도구는 HS256 만 허용하며, alg=none / alg=RS256 등 다른 알고리즘으로 위조한 JWT 가 거부되는지 검증 (OWASP A05 보안 설정 오류) |
| 시험 절차 | (1) `header.alg=none` JWT 생성 (서명 없음) → Authorization 헤더로 GET `/v1/me` 호출, (2) `header.alg=RS256` 임의 키로 서명한 JWT 호출, (3) 정상 HS256 JWT 호출 |
| 만족기준 | (1) 401, (2) 401, (3) 200. 로그에 alg violation 기록 |
| 시험결과 |
### KLID-ST-013  IDOR 차단

| 항목 | 내용 |
|---|---|
| 시험 유형 | 보안 |
| 관련 요구사항 ID | RQ-NFR-SEC-04 |
| 시험 시나리오 | WORKER 가 본인 배정이 아닌 타인의 프레임 라벨에 접근 시도할 때 차단 검증 (CWE-639) |
| 시험 절차 | (1) WORKER A 에게 srcSn=1001 배정, WORKER B 에게 srcSn=2001 배정, (2) A 의 JWT 로 GET `/v1/frames/2001/labels` 시도, (3) PUT `/v1/frames/2001/labels` 시도, (4) POST `/v1/frames/2001/sam2-track` 시도 |
| 만족기준 | 모든 시도 403, LabelAccessGuard 로그에 IDOR 시도 ID 기록, B 의 라벨 무변경 |
| 시험결과 |
### KLID-ST-014  좌표 점 1001개 라벨 저장 시도

| 항목 | 내용 |
|---|---|
| 시험 유형 | 보안 |
| 관련 요구사항 ID | RQ-NFR-SEC-05 |
| 시험 시나리오 | 단일 라벨에 좌표 점 1001개를 포함한 저장 요청이 거부되는지 검증 (CWE-770 DoS) |
| 시험 절차 | (1) WORKER JWT 로 PUT `/v1/frames/{srcSn}/labels` body 에 points[1001] 포함, (2) 점 1000개는 정상 저장 확인 |
| 만족기준 | 1001개 → 400 Bad Request, 메시지에 `MAX_POINTS_PER_LABEL=1000` 안내. 1000개는 200 OK |
| 시험결과 |
### KLID-ST-021  Gitea 장애 시 폴백 큐 적재

| 항목 | 내용 |
|---|---|
| 시험 유형 | 장애복구 |
| 관련 요구사항 ID | RQ-NFR-REL-01 |
| 시험 시나리오 | Gitea 가 다운된 상태에서 라벨 저장 시 데이터 유실 없이 폴백 큐에 적재되고, Gitea 복구 후 자동 커밋 완료되는지 검증 |
| 시험 절차 | (1) Gitea 컨테이너 중지, (2) WORKER JWT 로 라벨 10건 저장(PUT 10회), (3) GiteaCommitFallbackQueue 깊이 확인, (4) LS_GITEA_FALLBACK_QUEUE 깊이 확인 (30초 후 영속화), (5) Gitea 컨테이너 재가동, (6) 5분 후 폴백 큐 깊이 0 + LS_LABEL_VERSION 10건 확인 |
| 만족기준 | (2) 모든 저장 200 OK, (3) 큐 깊이=10 (또는 영속큐로 이전), (5)(6) 자동 커밋 100%, 라벨 무결성 유지 |
| 시험결과 |
### KLID-ST-023  중복 콜백 멱등성

| 항목 | 내용 |
|---|---|
| 시험 유형 | 장애복구 |
| 관련 요구사항 ID | RQ-NFR-REL-02 |
| 시험 시나리오 | 외부 비식별 솔루션이 동일 requestId 로 결과 콜백을 2회 송신해도 1회만 처리되는지 검증 |
| 시험 절차 | (1) 정상 콜백 1회 송신(HMAC 정상), (2) 동일 requestId 로 콜백 재송신, (3) LS_WEBHOOK_IDEMPOTENCY 카운트 확인, (4) 영상 상태/비식별본 경로 확인 |
| 만족기준 | (2) 200 OK + 처리 X, (3) 1건만 INSERT (2회차 no-op), (4) 결과 변경 없음 |
| 시험결과 |
### KLID-ST-024  비식별 누락 0건 정책

| 항목 | 내용 |
|---|---|
| 시험 유형 | 장애복구 |
| 관련 요구사항 ID | RQ-NFR-REL-03 |
| 시험 시나리오 | 자동 파이프라인이 비식별 단계를 건너뛰지 않는지 검증 (모든 영상에 무조건 적용) |
| 시험 절차 | (1) 1000건 영상 자동 파이프라인 처리, (2) `LsDeidentProcLog` 건수 vs `LS_DATA_RAW` COMPLETED 건수 비교, (3) BatchOrchestrator 코드에 비식별 분기 조건이 없는지 정적 분석 |
| 만족기준 | 비식별 로그 = COMPLETED 건수, 누락 0건 |
| 시험결과 |
### KLID-ST-026  Rolling Update 무중단

| 항목 | 내용 |
|---|---|
| 시험 유형 | 장애복구 |
| 관련 요구사항 ID | RQ-NFR-AVL-01 |
| 시험 시나리오 | App Tier Pod 를 1개씩 순차 재시작하는 동안 사용자 트래픽이 5xx 없이 처리되는지 검증 |
| 시험 절차 | (1) k6 50 VU 라벨 저장·조회 트래픽 시작, (2) Pod 1번 재시작 → 30초 대기 → Pod 2번 재시작, (3) k6 결과 보고서 |
| 만족기준 | 5xx 0건, 평균 응답시간 변동 ≤ 20%, 데이터 유실 0건 |
| 시험결과 |
### KLID-ST-029  메트릭 노출

| 항목 | 내용 |
|---|---|
| 시험 유형 | 관측성 |
| 관련 요구사항 ID | RQ-NFR-OBS-02 |
| 시험 시나리오 | Prometheus가 핵심 메트릭 8종을 수집할 수 있는지 검증 |
| 시험 절차 | (1) curl `/actuator/prometheus` 응답에 metric 이름 포함 확인, (2) Prometheus 쿼리로 각 메트릭 시계열 조회 |
| 만족기준 | 8종 메트릭 모두 노출, 측정값 변화 확인 |
| 시험결과 |
### KLID-ST-035  TASK_COMPLETED 즉시 통지

| 항목 | 내용 |
|---|---|
| 시험 유형 | 통지 |
| 관련 요구사항 ID | UC-014 |
| 시험 시나리오 | REVIEWER 승인 시 1초 내에 관제서버로 TASK_COMPLETED 통지가 송신되는지 검증 |
| 시험 절차 | (1) WireMock 으로 관제서버 stub (200 OK), (2) REVIEWER 가 POST `/v1/reviews/{videoId}/approve` 호출, (3) WireMock 의 요청 수신 시각과 approve 응답 시각 비교 |
| 만족기준 | 통지 도착 ≤ 1초, payload eventType=TASK_COMPLETED, rawSn 일치, X-Request-ID 헤더 존재 |
| 시험결과 |
| 비고 | 본 시나리오 적용 |

### KLID-ST-036  TASK_MODIFIED 디바운스

| 항목 | 내용 |
|---|---|
| 시험 유형 | 통지 |
| 관련 요구사항 ID | UC-015 |
| 시험 시나리오 | 60초 윈도우 내 동일 영상에 10회 라벨 수정 시 단 1회만 통지가 전송되며, frameIds 가 10개 누적되는지 검증 |
| 시험 절차 | (1) COMPLETED 영상 1건 준비, (2) WORKER 가 5초 간격으로 10회 라벨 수정 (서로 다른 frameId), (3) 60초 경과 대기, (4) WireMock 수신 요청 수 / payload.frameIds 길이 검증 |
| 만족기준 | 통지 요청 수 = 1, payload.frameIds.length = 10, 60s ± 2s 후 송신, 페이로드에 PII 미포함 |
| 시험결과 |
| 비고 | — ControlNotifyDebouncer 동작 검증 |

### KLID-ST-037  TASK_* 통지 실패 → 폴백

| 항목 | 내용 |
|---|---|
| 시험 유형 | 통지 |
| 관련 요구사항 ID | RQ-NFR-REL-01 |
| 시험 시나리오 | 관제서버가 5xx 응답 시 폴백 큐에 적재되고, 복구 후 재시도되는지 검증 |
| 시험 절차 | (1) WireMock 으로 5xx stub, (2) approve 5회 호출, (3) `LS_CONTROL_NOTIFY_FALLBACK` 5건 INSERT 확인, (4) WireMock 200 으로 전환, (5) 30초 후 STATUS=SUCCESS 전이 확인, (6) 실패 6회 누적 시 STATUS=DEAD 검증 |
| 만족기준 | (3) 5건 PENDING, (5) SUCCESS 5건, (6) 6회 실패 시 DEAD 자동 전이 + dead 메트릭 +1 |
| 시험결과 |
| 비고 | — ControlNotifyFallbackQueue 동작 검증 |
