# D2 사용자 인터페이스 설계서 (CBD 양식)

## 제·개정 이력

| 날짜 | 버전 | 작성자 | 승인자 | 내용 |
|------|------|--------|--------|------|
| 2026-05-21 | 1.0 | 박찬기 | - | 최초 작성 (전체 26화면) |
| 2026-05-26 | 1.1 | ck.park | - | 마킹 화면(SC-027) 추가, CBD 양식 대조 |
| 2026-05-26 | 2.0 | ck.park | - | CBD 양식 전면 재작성 — PlantUML Salt 와이어프레임 전체 27화면 추가 |

---

| D2 | 사용자 인터페이스 설계서 |
|----|------------------------|
| 시스템명 | AI 기반 지방정부 CCTV 관제지원시스템 구축(2차) | 서브시스템명 | AI CCTV 학습데이터 저작도구 (KLID-AT-SS-001~010, 전체) |
| 단계명   | 설계 | 작성일자 | 2026-05-26 | 버전 | 2.0 |

---

## 1. 사용자 인터페이스 구조도

본 도구는 **2개 채널 + 1개 공통 진입점**으로 구성된다.
- **INTERNAL 채널**: 저작도구 내부 사용자(REVIEWER / WORKER)용. `AppLayout`(LNB + GNB) 적용
- **PORTAL 채널**: 외부 포털사용자(PORTAL_USER)용. `PortalLayout`(모바일 친화, LNB 없음)
- **공통**: `/ingress` (관제·포털서버 JWT 인계), `/forbidden`, `/role-claim` (role 자가 부여)

| 업무 영역 (Level 1) | Level 2 | Level 3 | Level 4 |
|-------------------|---------|---------|---------|
| **공통 (Public)** | | | |
| | 세션 진입 | | |
| | 권한 자가 부여 | | |
| **내부채널 (INTERNAL)** | | | |
| | 대시보드 | | |
| | 영상 관리 | 영상 목록 (완료) | |
| | | 영상 처리 상태 | |
| | | 영상 상세 | |
| | 작업 관리 | 내 작업 목록 | |
| | | 작업 배정 | |
| | 마킹 | 마킹 화면 | |
| | 라벨링 | 라벨링 캔버스 | |
| | 오토라벨 결과 | 오토라벨 요약 | |
| | | 시계열 메타 검토 | |
| | 비식별화 | 비식별 목록 | |
| | | 비식별 상세 | |
| | 검수 | 검수 대기 목록 | |
| | | 검수 상세 | |
| | 데이터 증강 | 증강 요청 | |
| | | 증강 결과 | |
| | 통계 | 작업자 통계 | |
| | | 전체 통계 | |
| | 버전 이력 | 이력 조회 | |
| | 관리 | 사용자 관리 | |
| | | 시스템 설정 | |
| | | 프리셋 관리 | |
| | 개발 전용 | 오토라벨 테스트 | |
| **포털채널 (PORTAL)** | | | |
| | 포털 홈 | | |
| | 포털 라벨링 | | |

---

## 2. 사용자 인터페이스 목록

### 2.1 화면

| 화면 ID | 화면명 | 관련 유스케이스 ID |
|---|---|---|
| KLID-AT-SC-001 | 세션 진입(SessionIngress) | KLID-AT-UC-013 |
| KLID-AT-SC-002 | 권한 자가 부여(RoleClaim) | KLID-AT-UC-013 |
| KLID-AT-SC-003 | 대시보드(Dashboard) | KLID-AT-UC-001 |
| KLID-AT-SC-004 | 완료 영상 목록(VideoList) | KLID-AT-UC-001 |
| KLID-AT-SC-005 | 영상 처리 상태(VideoStatus) | KLID-AT-UC-001 |
| KLID-AT-SC-006 | 영상 상세(VideoDetail) | KLID-AT-UC-001 |
| KLID-AT-SC-007 | 내 작업 목록(TaskList) | KLID-AT-UC-008 |
| KLID-AT-SC-008 | 라벨링 캔버스(Labeling) | KLID-AT-UC-006, KLID-AT-UC-010, KLID-AT-UC-003 |
| KLID-AT-SC-009 | 오토라벨 요약(AutoLabelSummary) | KLID-AT-UC-001 |
| KLID-AT-SC-010 | 시계열 메타 검토(MetaReview) | KLID-AT-UC-005 |
| KLID-AT-SC-011 | 비식별 영상 목록(DeidentList) | KLID-AT-UC-003 |
| KLID-AT-SC-012 | 비식별 영상 상세(DeidentDetail) | KLID-AT-UC-003 |
| KLID-AT-SC-013 | 검수 목록(ReviewList) | KLID-AT-UC-009 |
| KLID-AT-SC-014 | 검수 상세(ReviewDetail) | KLID-AT-UC-009, KLID-AT-UC-014 |
| KLID-AT-SC-015 | 버전 이력(History) | KLID-AT-UC-010 |
| KLID-AT-SC-016 | 워커 통계(WorkerStat) | - |
| KLID-AT-SC-017 | 전체 통계(OverallStat) | - |
| KLID-AT-SC-018 | 증강 요청(AugmentRequest) | KLID-AT-UC-011 |
| KLID-AT-SC-019 | 증강 결과(AugmentResult) | KLID-AT-UC-011 |
| KLID-AT-SC-020 | 사용자 관리(UserManage) | - |
| KLID-AT-SC-021 | 시스템 설정(SystemSettings) | - |
| KLID-AT-SC-022 | 라벨 프리셋(PresetList) | KLID-AT-UC-007 |
| KLID-AT-SC-023 | 포털 홈(PortalHome) | KLID-AT-UC-012 |
| KLID-AT-SC-024 | 포털 라벨링(PortalLabeling) | KLID-AT-UC-012 |
| KLID-AT-SC-025 | 작업 배정(TaskAssign) | KLID-AT-UC-008 |
| KLID-AT-SC-026 | 개발자 오토라벨 테스트(DevAutolabelTest) | - |
| KLID-AT-SC-027 | 마킹(Marking) | KLID-AT-UC-001 |

### 2.2 출력물

| 출력물 ID | 출력물명 | 관련 유스케이스 ID |
|---|---|---|
| KLID-AT-PR-001 | 통계 리포트 CSV(`/v1/stats/report`) | - |
| KLID-AT-PR-002 | Gitea Diff Patch(`/v1/versions/{commit}/diff`) | KLID-AT-UC-010 |
| KLID-AT-PR-003 | 검수 결과 요약 화면 인쇄(브라우저 print) | KLID-AT-UC-009 |
| KLID-AT-PR-004 | 증강 결과 미리보기 이미지 묶음 | KLID-AT-UC-011 |

---

## 3. 화면 상세 설계

### 3.1 KLID-AT-SC-001 세션 진입(SessionIngress)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-001</td><td>화면명</td><td>세션 진입(SessionIngress)</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-013</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">KLID-AT-SD-007</td></tr>
<tr><td>화면유형</td><td>입력(쿼리 파라미터 처리)</td><td>메뉴경로</td><td>`/ingress` (외부 진입점)</td></tr>
<tr><td>화면개요</td><td colspan="3">관제서버·포털서버와 동일 도메인 운영. 브라우저 스토리지(localStorage/sessionStorage) 공유를 통해 JWT를 전달. 토큰을 SecurityContext에 등록하고 채널·역할에 따라 적합한 시작 화면으로 자동 이동.</td></tr>
</table>

**화면 레이아웃**

라우터에 별도 페이지 컴포넌트 없음 — 토큰 인계 처리 흐름(JwtAuthenticationFilter + 채널/역할 분기 navigate).
시각 노출은 짧은 리다이렉트 구간뿐이며 사용자 입력이 없는 transit 화면이다.

```
┌─ 흐름도 ────────────────────────────────────────────────────────────────────────┐
│                                                                                  │
│  [상위 시스템(관제/포털)]                                                          │
│         │                                                                        │
│         │ 동일 도메인 — 브라우저 스토리지(localStorage/sessionStorage)에 JWT 저장 │
│         ▼                                                                        │
│  [/ingress 진입]  ── (URL 쿼리 ?token= 미사용)                                    │
│         │                                                                        │
│         ▼                                                                        │
│  JwtAuthenticationFilter — token 검증 (issuer/exp/HS256)                          │
│         │                                                                        │
│         ├─ 통과 ─┐                                                                │
│         │       ▼                                                                 │
│         │   channel × role 분기 navigate                                          │
│         │     ├─ INTERNAL + REVIEWER → /dashboard                                 │
│         │     ├─ INTERNAL + WORKER   → /task                                      │
│         │     └─ PORTAL_USER         → /portal                                    │
│         │                                                                        │
│         └─ 실패 → /forbidden (발급자 로그인 URL 안내)                              │
│                                                                                  │
└──────────────────────────────────────────────────────────────────────────────────┘
```

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| token | (브라우저 스토리지 키) | String(<=4096) | I/H | 필수, JWT 형식, HS256 |
| issuer | (JWT 클레임) | String | I/H | 허용 issuer만 |
| 분기 결과 | navigate | - | O | 채널·역할 기준 라우트 결정 |
| 오류 처리 | redirect → /forbidden | - | O | 401/검증 실패 시 |

**처리 내용**

1. 브라우저 스토리지에서 `token` 읽기 → `JwtAuthenticationFilter` 사전 검증 (KLID-AT-SD-007)
2. 통과 시 `channel`(INTERNAL/PORTAL) + `role` 분기 → INTERNAL+REVIEWER → `/dashboard`, INTERNAL+WORKER → `/task`, PORTAL_USER → `/portal`
3. 실패 시 `/forbidden` 으로 redirect + 발급자 로그인 URL 안내

**기술적 고려사항**

동일 도메인 브라우저 스토리지 공유 방식이므로 URL 쿼리 파라미터 미사용. CSP `default-src 'self'`.

---

### 3.2 KLID-AT-SC-002 권한 자가 부여(RoleClaim)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-002</td><td>화면명</td><td>권한 자가 부여(RoleClaim)</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-013</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">KLID-AT-SD-007</td></tr>
<tr><td>화면유형</td><td>입력</td><td>메뉴경로</td><td>`/role-claim`</td></tr>
<tr><td>화면개요</td><td colspan="3">인증은 완료되었으나 role 클레임이 없는 사용자가 관리자 공유 패스워드를 입력하여 WORKER 또는 REVIEWER 역할을 자가 부여하는 화면.</td></tr>
</table>

**화면 레이아웃**

독립 라우트 `/role-claim` — AppLayout 밖. 화면 전체를 회색 배경 + 중앙 정렬 Card 단일로 구성.

```
┌─ <main> (min-h-screen, bg-gray-50, flex items-center justify-center, px-4 py-10) ──┐
│                                                                                    │
│                ┌─ Card (w-full max-w-md, padding="lg") ─────────────────┐           │
│                │ ┌─ <header> ────────────────────────────────────────┐  │           │
│                │ │ 권한 부여 필요                          (h1)      │  │           │
│                │ │ 관리자에게 받은 패스워드로 역할을 부여받으세요.   │  │           │
│                │ └───────────────────────────────────────────────────┘  │           │
│                │                                                        │           │
│                │ ┌─ <form> (space-y-5) ───────────────────────────────┐ │           │
│                │ │ ┌─ <fieldset> ───────────────────────────────────┐ │ │           │
│                │ │ │ <legend> 역할 선택                              │ │ │           │
│                │ │ │ ┌─ role="radiogroup" (border, rounded-lg) ───┐ │ │ │           │
│                │ │ │ │ (•) 작업자 (WORKER)        Radio          │ │ │ │           │
│                │ │ │ │ ( ) 검수자 (REVIEWER)      Radio          │ │ │ │           │
│                │ │ │ └────────────────────────────────────────────┘ │ │ │           │
│                │ │ └────────────────────────────────────────────────┘ │ │           │
│                │ │                                                    │ │           │
│                │ │  Input  label="관리자 패스워드"                     │ │           │
│                │ │         type="password"                            │ │           │
│                │ │         autoComplete="new-password"                │ │           │
│                │ │         placeholder="관리자에게 받은 패스워드..."   │ │           │
│                │ │                                                    │ │           │
│                │ │  [조건부] <p role="alert" text-danger>              │ │           │
│                │ │           {errorMessage}                           │ │           │
│                │ │                                                    │ │           │
│                │ │  ┌─────────────────────────────────────────────┐   │ │           │
│                │ │  │           권한 부여 확인  (Button fullWidth) │   │ │           │
│                │ │  └─────────────────────────────────────────────┘   │ │           │
│                │ └────────────────────────────────────────────────────┘ │           │
│                └────────────────────────────────────────────────────────┘           │
│                                                                                    │
└────────────────────────────────────────────────────────────────────────────────────┘
```

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 역할 선택 | Radio(radiogroup) | enum | I | WORKER / REVIEWER 중 선택 필수 |
| 관리자 패스워드 | Input(password) | String | I | 필수, autoComplete="new-password", 평문 로그 금지 |
| 오류 안내 | label(role=alert) | - | O | 401·409·429 별 사용자 메시지 |
| 권한 부여 확인 | Button(submit) | - | I | 역할 미선택 또는 패스워드 공백 시 disabled |

**처리 내용**

1. 폼 제출 → `POST /v1/auth/role-claim { role, adminPassword }`
2. 성공 시 채널·역할에 따라 자동 이동 (REVIEWER → `/dashboard`, WORKER → `/task`)
3. 401: 패스워드 불일치 안내, 409: 이미 역할 보유 안내, 429: rate limit 초과 안내

**기술적 고려사항**

패스워드는 `type="password"` + `autoComplete="new-password"`로 단말 캐싱 방지. `console.log` 및 sessionStorage 저장 금지(보안정책).

---

### 3.3 KLID-AT-SC-003 대시보드(Dashboard)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-003</td><td>화면명</td><td>대시보드</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-001</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">KLID-AT-SD-001</td></tr>
<tr><td>화면유형</td><td>조회</td><td>메뉴경로</td><td>`홈 > 대시보드`</td></tr>
<tr><td>화면개요</td><td colspan="3">처리 대기/완료/반려 영상 건수 KPI, 이벤트별 이미지·영상 데이터 분포, 최근 완료 영상 목록, WORKER 한정 내 작업 현황.</td></tr>
</table>

**화면 레이아웃**

AppLayout 내부 (LNB w-60 + 상단 헤더 h-14). 페이지 본문은 `space-y-6`.

```
AppLayout (LNB w-60 / 상단 h-14)
└── 페이지 본문 (space-y-6)
    ├── Header (flex items-center justify-between)
    │     ├── h1: "대시보드"
    │     └── 우측: NowClock {YYYY-MM-DD HH:mm:ss} + [↻ 새로고침] Button(secondary,sm)
    │
    ├── [조건부] ErrorState "대시보드 정보를 불러올 수 없습니다"
    │
    ├── KPI 그리드 (data-testid="dashboard-kpi-grid")
    │     · grid-cols-2 / xl:grid-cols-4 (WORKER) | xl:grid-cols-3 (REVIEWER)
    │     ├── KpiCard "처리 대기" {pendingCount} 건  (Film, bg-yellow-50)
    │     ├── KpiCard "처리 완료" {completedCount} 건 (CheckCircle2, bg-green-50)
    │     ├── KpiCard "내 작업" {myTaskCount} 건  [WORKER 한정] (ClipboardList, bg-blue-50)
    │     └── KpiCard "반려 건수" {rejectedCount} 건 (XCircle, bg-red-50)
    │
    ├── 이미지/영상 분포 (grid-cols-1 md:grid-cols-2 gap-4)
    │     ├── Card title="이미지 데이터 개수"
    │     │     ├── {cumulativeImageCount}장 (text-2xl, text-primary-600)
    │     │     └── border-t · grid-cols-2 · 이벤트 6종 (쓰러짐/폭력/교통사고/이상행동(유괴)/침수/산불)
    │     └── Card title="영상 데이터 개수"
    │           ├── {cumulativeVideoCount}건 (text-2xl, text-primary-600)
    │           └── border-t · grid-cols-2 · 이벤트 6종
    │
    └── 최근 완료 / 내 작업 영역 (grid-cols-1 [WORKER: xl:grid-cols-2] gap-4)
          ├── Card title="최근 완료 영상"
          │     └── <table>: CCTV명 / 이벤트(EventTypeBadge) / 길이 / 완료일 (size=5)
          └── [WORKER 한정] Card title="내 작업 현황"
                └── <table>: 영상 / 상태(StatusBadge) / 진행률(ProgressBar) (size=5)
```

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| KPI 카드(처리 대기/완료/내 작업/반려) | KpiCard | int | O | WORKER 4카드, REVIEWER 3카드 |
| 이미지 데이터 분포 | Card + 이벤트 6종 | - | O | 이벤트별 건수 |
| 영상 데이터 분포 | Card + 이벤트 6종 | - | O | 이벤트별 건수 |
| 최근 완료 영상 | DataTable | - | O | CCTV명/이벤트/길이/완료일 |
| 내 작업 현황 | DataTable | - | O | WORKER 전용 |
| 새로고침 | Button | - | I | 30초 자동 polling |

**처리 내용**

`GET /v1/stats/summary` → `DashboardSummaryResponse` KPI·분포 카드 렌더. `GET /v1/videos?size=5&sort=capturedAt,desc` → 최근 완료 영상. WORKER: `GET /v1/tasks/board?workerId={myId}&size=5` → 내 작업 현황. polling 30s.

**기술적 고려사항**

react-query staleTime 30s, GET 401 → `/ingress` 재진입.

---

### 3.4 KLID-AT-SC-004 완료 영상 목록(VideoList)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-004</td><td>화면명</td><td>완료 영상 목록</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-001</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">-</td></tr>
<tr><td>화면유형</td><td>조회</td><td>메뉴경로</td><td>`영상 > 완료 영상`</td></tr>
<tr><td>화면개요</td><td colspan="3">관제서버에서 인계받은 영상의 배치 처리 상태와 단계를 확인하는 테이블 뷰. 필터, 다중 선택, 페이지네이션 제공.</td></tr>
</table>

**화면 레이아웃**

AppLayout 내부 (LNB w-60 + 상단 h-14). 페이지 본문은 `space-y-4`.

```
AppLayout (LNB w-60 / 상단 h-14)
└── 페이지 본문 (space-y-4)
    ├── Header (flex items-center justify-between)
    │     ├── 좌측: h1 "영상 처리 현황" + 부제 "관제서버에서 인계받은 영상의 배치 처리 상태와 단계를 확인합니다."
    │     └── 우측: [↻ 새로고침] Button(secondary,sm)
    │
    ├── VideoFilters (검색어 / 상태 Select / 이벤트 Select / [적용] [초기화])
    │     · searchParams 기반 URL 동기화. 필터 변경 시 page=0 리셋
    │
    ├── [조건부] ErrorState "영상 목록을 불러올 수 없습니다"
    │
    ├── [선택>=1건] Bulk action bar (bg-primary-50 border-primary-200 rounded-lg)
    │     └── "선택 {N}건" (text-primary-700)
    │
    ├── Table 컨테이너 (bg-white border rounded-lg shadow-sm)
    │     ├── 헤더 바 (px-4 py-2, bg-gray-50)
    │     │     ├── [☐] 전체 선택 checkbox (accent-primary-600)
    │     │     └── "전체 {totalElements}건 ({currentPage+1}/{totalPages} 페이지)"
    │     └── <table> (text-sm)
    │           ├── thead: [40px] / CCTV명 / 이벤트 / 녹화일 / 길이 / 개인정보 / 처리 단계(120px) / 액션
    │           └── tbody: 행 클릭 시 navigate(`/video/{id}`)
    │                 ├── [☐] 행 체크박스 (stopPropagation)
    │                 ├── CCTV명 (font-medium, text-xs)
    │                 ├── EventTypeBadge
    │                 ├── capturedAt slice(0,10) (text-xs text-gray-500)
    │                 ├── formatDuration(durationSec)
    │                 ├── PrivacyBadge (size="sm")
    │                 ├── 처리 단계: StageBadge(COMPLETED/FAILED) 또는 StatusBadge(그 외)
    │                 └── 액션: [상세 >] Button(ghost, sm) (stopPropagation)
    │           · Loading: Skeleton 5행 × 8열
    │           · Empty: EmptyState "해당하는 영상이 없습니다."
    │
    └── [조건부 totalPages>1] Pagination (flex justify-center gap-1)
          [< 이전] · [1][2]…[7] (최대 7페이지 버튼, 현재 페이지 bg-primary-600 text-white) · [> 다음]
```

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 검색어 | input text | String(<=100) | I/E | XSS sanitize |
| 상태 필터 | Select | enum | I/E | 전체/PENDING/IN_PROGRESS/COMPLETED/FAILED |
| 이벤트 필터 | Select | enum | I/E | 6종 이벤트 |
| 전체 선택 | checkbox | bool | I/E | 현재 페이지 행만 |
| 결과 그리드 | DataTable | - | O | CCTV명/이벤트/녹화일/길이/개인정보/처리단계/액션 |
| 상세 보기 | Button(ghost) | - | I | → `/video/:id` |
| 페이지 | Pagination | int | I/E | size=20, 최대 7페이지 표시 |

**처리 내용**

`GET /v1/videos?page=&size=&sort=&q=&eventTypeCd=&dataSttsCd=` → 페이지 렌더. 행 클릭 시 `/video/:id` 이동.

**기술적 고려사항**

searchParams 기반 URL 동기화. 필터 변경 시 page=0 리셋. VideoFilters 컴포넌트 분리.

---

### 3.5 KLID-AT-SC-005 영상 처리 상태(VideoStatus)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-005</td><td>화면명</td><td>영상 처리 상태(VideoStatus)</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-001</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">-</td></tr>
<tr><td>화면유형</td><td>조회</td><td>메뉴경로</td><td>`영상 > 처리 현황`</td></tr>
<tr><td>화면개요</td><td colspan="3">배치 파이프라인에서 처리 중인 영상의 실시간 처리 현황을 표 형식으로 표시. 5초 자동 폴링. KPI 3개(처리 중/완료/실패) + 전체 목록 테이블 제공.</td></tr>
</table>

**화면 레이아웃**

AppLayout 내부 (LNB w-60 + 상단 h-14). 페이지 본문은 `space-y-4`.

```
AppLayout (LNB w-60 / 상단 h-14)
└── 페이지 본문 (space-y-4)
    ├── Header (flex items-center justify-between)
    │     ├── 좌측: h1 "처리 현황" + 부제 "배치 파이프라인의 실시간 처리 현황 (5초마다 자동 갱신)"
    │     └── 우측: [↻ 새로고침] Button(secondary,sm)
    │
    ├── KPI 3카드 (grid-cols-1 sm:grid-cols-3 gap-3)
    │     ├── KpiCard "처리 중" {totalProcessing}건  (stage !∈ COMPLETED/FAILED/PENDING)
    │     ├── KpiCard "완료" {totalCompleted}건
    │     └── KpiCard "실패" {totalFailed}건
    │
    ├── [조건부] ErrorState "배치 현황을 불러올 수 없습니다"
    │
    ├── [선택>=1건] Bulk action bar (bg-primary-50 border-primary-200)
    │     └── "선택 {N}건"
    │
    └── Table 컨테이너 (bg-white border rounded-lg shadow-sm)
          ├── 헤더 바 (px-4 py-2, bg-gray-50)
          │     ├── [☐] 전체 선택 checkbox
          │     └── "전체 {items.length}건"
          └── <table data-testid="batch-video-list">
                ├── thead: [40px] / 원본 번호 / 처리 단계(120px) / 시작 시각 / 마지막 갱신 / 재시도 / 오류
                └── tbody:
                      ├── [☐] 행 체크박스 (선택 시 bg-primary-50)
                      ├── #{rawSn} (font-medium, text-xs)
                      ├── StageBadge(stage, status) — status 매핑:
                      │     COMPLETED → 'COMPLETED'
                      │     FAILED    → 'FAILED'
                      │     PENDING   → 'PENDING'
                      │     그 외     → 'PROGRESS'
                      ├── startedAt (formatDateTime, text-xs)
                      ├── lastUpdatedAt (formatDateTime, text-xs)
                      ├── retryCount > 0 ? "{N}회" : "-"
                      └── errorMessage (text-red-600, 30자 초과 시 "..." 말줄임, title 속성으로 풀텍스트)
                · Loading: Skeleton 5행 × 7열
                · Empty: EmptyState "처리 중인 영상이 없습니다 / 배치 처리 대기 중이거나 모두 완료되었습니다."
```

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 처리 중 건수 | KpiCard | int | O | - |
| 완료 건수 | KpiCard | int | O | - |
| 실패 건수 | KpiCard | int | O | - |
| 전체 선택 체크박스 | checkbox | bool | I/E | - |
| 원본 번호 | text | int | O | - |
| 처리 단계 | StageBadge | enum | O | PENDING/VLM/DEID/FRAME/YOLO/SAM2/COMPLETED/FAILED |
| 시작 시각 | text | datetime | O | - |
| 마지막 갱신 시각 | text | datetime | O | - |
| 재시도 횟수 | text | int | O | 0이면 '-' 표시 |
| 오류 메시지 | text | String(<=30) | O | 30자 초과 시 말줄임 처리 |
| 새로고침 | Button | - | I | refetch + videos 캐시 무효화 |

**처리 내용**

`GET /v1/batch/status` → `BatchStageProgress[]` → KPI 집계 + 테이블 렌더. 5초 폴링.

**기술적 고려사항**

react-query refetchInterval=5000. 오류 메시지 full-text는 title 속성으로 hover 시 표시.

---

### 3.6 KLID-AT-SC-006 영상 상세(VideoDetail)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-006</td><td>화면명</td><td>영상 상세(VideoDetail)</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-001</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">-</td></tr>
<tr><td>화면유형</td><td>조회</td><td>메뉴경로</td><td>`/video/:id`</td></tr>
<tr><td>화면개요</td><td colspan="3">개별 영상의 상세 정보를 3개 탭(기본 정보 / 프레임 미리보기 / 오토라벨 결과)으로 구성. 헤더 카드에서 CCTV명, 이벤트 유형, 처리 상태, 버전관리 이동 버튼 제공.</td></tr>
</table>

**화면 레이아웃**

AppLayout 내부 (LNB w-60 + 상단 h-14). 페이지 본문은 `space-y-4`.

```
AppLayout (LNB w-60 / 상단 h-14)
└── 페이지 본문 (space-y-4)
    ├── [← 뒤로가기] 버튼 (text-sm text-gray-500, navigate(-1))
    │
    ├── [로딩] Skeleton h=160 + Skeleton h=300
    ├── [에러] ErrorState "영상 정보를 불러올 수 없습니다"
    │
    ├── Card (헤더 카드, flex flex-wrap items-start gap-3)
    │     ├── 썸네일 영역 (w-32 h-20, shrink-0)
    │     │     ├── thumbnailSrcSn 있음 → AuthImage(srcSn, w=128 h=80)
    │     │     └── 없음 → bg-gray-200 placeholder "미리보기 없음"
    │     └── 우측 (flex-1)
    │           ├── 상단 row (flex items-start justify-between)
    │           │     ├── h2 {cctvName} (text-lg font-bold)
    │           │     └── [⎇ 버전관리로 이동] Button(secondary,sm) → /history/{id}
    │           ├── 뱃지 줄 (flex flex-wrap gap-2)
    │           │     ├── EventTypeBadge (size="md")
    │           │     └── StatusBadge
    │           └── 메타 줄 (flex flex-wrap gap-4, text-sm text-gray-500)
    │                 · "길이: {formatDuration}"
    │                 · "녹화일: {capturedAt.slice(0,10)}"
    │
    └── 탭 컨테이너 (bg-white border rounded-lg shadow-sm)
          ├── Tabs (px-4 pt-4) — items: 기본 정보 / 프레임 미리보기 / 오토라벨 결과
          └── 탭 콘텐츠 (px-6 pb-6)
                ├── [tab=info] InfoTab
                │     · grid-cols-1 md:grid-cols-2 gap-3
                │     · 메타 카드 (bg-gray-50 rounded-lg) 8개:
                │       CCTV ID / 해상도 / 길이 / 녹화 시각 / 처리 단계(BatchStageIndicator)
                │       / 개인정보 분류(PrivacyBadge) / 생성일 / 수정일
                │
                ├── [tab=frames] FramePreviewTab
                │     · grid-cols-3 sm:grid-cols-6 gap-2
                │     · 각 셀: <button> + AuthImage(aspect-video, w=160 h=90)
                │             · 우상단 빨간 점 (hasIssue 시)
                │             · 하단 오버레이 "#{frameNo}"
                │     · 클릭 → Modal(size="xl") 라이트박스
                │             · 큰 이미지(max-h-80) + frameNo/timestampMs/이슈 표시
                │             · 푸터: [닫기] / [라벨링 편집] → navigate(`/label/{srcSn}`)
                │     · Empty: "프레임 데이터가 없습니다."
                │
                └── [tab=autolabel] AutoLabelTab — flex flex-col gap-6
                      ├── 처리 정보 (grid-cols-2 sm:grid-cols-3 gap-3, bg-gray-50)
                      │     · 총 라벨 수 / 오토라벨 수 / 오토라벨 비율 / 처리 상태
                      ├── 신뢰도 분포 (오토라벨) — 막대 차트 (h-24)
                      │     · 버킷 3개: 0.9+(bg-green-500), 0.7~0.9(bg-yellow-400), <0.7(bg-red-400)
                      └── 라벨별 분포 — 가로 막대 목록 (상위 10개)
                            · 라벨명(w-20) / 배경막대(bg-gray-100 h-3) / 카운트
```

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 썸네일 이미지 | AuthImage | - | O | 프레임 없으면 "미리보기 없음" placeholder |
| CCTV명 | text | String | O | - |
| 이벤트 유형 | EventTypeBadge | enum | O | - |
| 처리 상태 | StatusBadge | enum | O | - |
| 버전관리로 이동 | Button | - | I | → `/history/{id}` |
| 탭 선택 | Tabs | enum | I/E | 기본 정보 / 프레임 미리보기 / 오토라벨 결과 |
| [기본 정보] 메타 카드 | Card Grid | - | O | CCTV ID/해상도/길이/녹화 시각/처리 단계/개인정보 |
| [프레임] 프레임 그리드 | AuthImage Grid | - | O | 클릭 시 Lightbox 모달 |
| [오토라벨] 처리 정보 | Card x4 | - | O | 총 라벨/오토라벨 수/비율/상태 |
| [오토라벨] 신뢰도 분포 | 막대 차트 | - | O | 0.9+/0.7~0.9/<0.7 구간 |
| [오토라벨] 라벨별 분포 | 가로 막대 | - | O | 상위 10개 표시 |
| 뒤로가기 | Button | - | I | navigate(-1) |

**처리 내용**

`GET /v1/videos/{id}` → 헤더·기본정보 렌더. 오토라벨 탭: `GET /v1/videos/{id}/labels/auto`. id가 숫자가 아니면 ErrorState 표시.

**기술적 고려사항**

id는 Number.parseInt + isFinite 검증. AuthImage는 인증 헤더 포함 요청으로 프레임 이미지 보호.

---

### 3.7 KLID-AT-SC-007 내 작업 목록(TaskList)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-007</td><td>화면명</td><td>내 작업 목록(TaskList)</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-008</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">KLID-AT-SD-005</td></tr>
<tr><td>화면유형</td><td>조회 + 입력(배정)</td><td>메뉴경로</td><td>`작업 > 내 작업`</td></tr>
<tr><td>화면개요</td><td colspan="3">REVIEWER: 처리 완료 영상에 대한 작업자 배정·재배정·일괄배정. WORKER: 본인에게 배정된 작업 목록 조회 및 작업 시작 진입. KPI 4카드 + 필터 + 다중 선택 테이블.</td></tr>
</table>

**화면 레이아웃**

AppLayout 내부 (LNB w-60 + 상단 h-14). 페이지 본문은 `space-y-4`.

```
AppLayout (LNB w-60 / 상단 h-14)
└── 페이지 본문 (space-y-4)
    ├── Header (flex items-center justify-between)
    │     ├── 좌측 (flex items-center gap-3)
    │     │     ├── h1 "작업 목록"
    │     │     ├── 청록 뱃지 (bg-cyan-100 text-cyan-700, rounded-full)
    │     │     │     "현재 역할: {검수자|작업자}"
    │     │     └── "처리 완료된 영상만 표시" (text-xs text-gray-500)
    │     └── 우측: [↻ 새로고침] Button(secondary,sm)
    │
    ├── TaskFilters (검색폼)
    │     · 영상명/작업자명 input
    │     · 이벤트 Select (목록 unique 수집)
    │     · 상태 Select
    │     · 작업자 Select  [REVIEWER 한정 showAssigneeSelect]
    │     · [초기화]
    │
    ├── [조건부] ErrorState "작업 목록을 불러올 수 없습니다"
    │
    ├── KPI 4카드 (grid-cols-4 gap-4)
    │     ├── KpiCard "전체 작업" {total} (ListTodo, bg-primary-50)
    │     ├── KpiCard "진행중" {inProgress} (Play, bg-green-50)
    │     ├── KpiCard "검수대기" {reviewPending} (Flame, bg-yellow-50)
    │     └── KpiCard "반려" {rejected} (ArrowDown, bg-red-50)
    │
    ├── [REVIEWER + 선택>=1건] Bulk 배정 액션바 (data-testid="bulk-assign-bar", bg-primary-50)
    │     ├── 좌: "{N}개 선택됨" (bg-blue-100 text-blue-700 rounded-full) + [선택 해제] 텍스트버튼
    │     └── 우: [👥 {N}개 일괄 배정] Button(primary, sm) → AssignModal(mode="bulk")
    │
    ├── Table 컨테이너 (bg-white border rounded-lg shadow-sm)
    │     ├── 헤더 바 (px-4 py-2, bg-gray-50, flex items-center gap-3)
    │     │     ├── [REVIEWER] [☐] "현재 페이지 전체 선택" label + checkbox (indeterminate 지원)
    │     │     └── 우측 "전체 {visibleRows.length}건 ({safePage+1}/{totalPages} 페이지)" (ml-auto, text-xs)
    │     └── <table> (text-sm)
    │           ├── thead: [REVIEWER만 ☐(w-10)] / 영상명 / 이벤트 / 상태 / 작업자 / 검수자 / 액션
    │           └── tbody:
    │                 ├── [REVIEWER] 행 체크박스
    │                 ├── 영상명 (truncate, max-w-200) + 부제 "video-{id.padStart(4,'0')}"
    │                 ├── EventTypeBadge or "-"
    │                 ├── StatusBadge (STATUS_BADGE_MAP) + label(STATUS_LABEL: 미배정/대기/진행중/검수대기/완료/반려)
    │                 ├── 작업자: workerName 또는 "미배정" (italic, text-gray-400)
    │                 ├── 검수자: reviewerName → reviewerMap fallback → "미등록" (italic)
    │                 └── 액션 (flex flex-nowrap gap-1, stopPropagation):
    │                       · [REVIEWER, COMPLETED 아닐 때]
    │                           workerId 있음 → [↻ 재배정] Button(ghost,sm) → AssignModal(mode="reassign")
    │                           workerId 없음 → [➕ 배정] Button(ghost,sm) → AssignModal(mode="assign")
    │                       · [REVIEWER + task 있음] [⏱ 이력] → HistoryDrawer 오픈
    │                       · [WORKER + task 있음]
    │                           firstSrcSn 있음 → [▶ 작업] → navigate(`/label/{firstSrcSn}`)
    │                           firstSrcSn 없음 → [☰ 마킹] → navigate(`/marking/{videoId}`)
    │                           + [⏱ 이력] HistoryDrawer
    │           · Loading: Skeleton (REVIEWER 7열 / WORKER 6열)
    │           · Empty: EmptyState "배정된 작업이 없습니다"
    │
    ├── [totalPages>1] Pagination (flex justify-center gap-1)
    │     [<] [1][2]…[7] (현재 페이지 bg-primary-600 text-white) [>]
    │
    ├── AssignModal (mode: assign / reassign / bulk)
    └── HistoryDrawer (우측 슬라이드, 배정 이력)
```

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 검색어 | Input text | String | I/E | 영상명/작업자명 필터 |
| 이벤트 유형 필터 | Select | String | I/E | 목록에서 unique 수집 |
| 상태 필터 | Select | enum | I/E | 전체/미배정/대기/진행중/검수대기/완료/반려 |
| 작업자 필터 | Select | int | I/E | REVIEWER만 표시 |
| 행 체크박스 | checkbox | bool | I/E | 다중 선택용 |
| 일괄 배정 바 | 액션 바 | - | O/I | 1건 이상 선택 시 노출 |
| 영상명 | DataTable 컬럼 | String | O | - |
| 이벤트 유형 | EventTypeBadge | enum | O | - |
| 상태 | StatusBadge | enum | O | - |
| 작업자·검수자 | text | String | O | 미배정이면 italic |
| 배정/재배정 | Button(ghost) | - | I | COMPLETED 행은 미노출 |
| 이력 | Button(ghost) | - | I | HistoryDrawer 오픈 |
| WORKER: 작업 | Button(ghost) | - | I | → `/label/{firstSrcSn}` |

**처리 내용**

REVIEWER: `GET /v1/tasks/board?status=COMPLETED&page=&size=` → 목록 렌더. 배정: `POST /v1/assignments { workerId, rawDataIds }`. 재배정: `PATCH /v1/assignments/{id} { workerId }`. WORKER: `GET /v1/tasks?page=&size=` → 본인 배정 작업만.

**기술적 고려사항**

COMPLETED(검수 승인 완료) 행은 재배정 버튼 미노출. `/users` API는 REVIEWER만 호출(enabled 가드).

---

### 3.8 KLID-AT-SC-008 라벨링 캔버스(Labeling)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-008</td><td>화면명</td><td>라벨링 캔버스</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-006, KLID-AT-UC-010, KLID-AT-UC-003</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">KLID-AT-SD-004</td></tr>
<tr><td>화면유형</td><td>입력·갱신·삭제</td><td>메뉴경로</td><td>`/label/{srcSn}` (풀스크린)</td></tr>
<tr><td>화면개요</td><td colspan="3">konva 기반 라벨링 캔버스. BBox/Polygon/Mask 도구, 프레임 타임라인, 좌측 객체 리스트, 우측 객체 트리·속성·시계열 메타 패널, 단축키, 자동 저장. 우측 패널 하단에 시계열 메타(VLM 자연어) 접이식 사이드 패널 — 프레임 전환 시 srcSn 기반 자동 갱신.</td></tr>
</table>

**화면 레이아웃**

풀스크린 다크 UI (AppLayout 밖, `fixed inset-0 bg-gray-900`). LNB/GNB 없음.

```
┌─ LabelHeader (h-14, bg-gray-800) ──────────────────────────────────────────────────────────┐
│ [X 닫기]  프레임 #{srcSn}      Frame N/총   {N}개 객체  [DEID|RAW]                          │
│                                ● 편집중/✓                                                   │
│                                저장됨        [비식별누락신고] [히스토리] [저장] [검수제출]   │
├─ (조건부) 비식별 잠금 배너 (bg-amber-900/60) ────────────────────────────────────────────────┤
├─ flex-1 본문 ───────────────────────────────────────────────────────────────────────────────┤
│ ┌──DarkToolbar──┐┌─LabelSidebar──┐┌─ 캔버스 (flex-1, Konva Stage) ─┐┌──RightPanel──┐┌HistoryPanel ┐│
│ │ (w-14, 다크)  ││ (w-56, 다크)  ││                                ││ (w-72, 다크) ││ (w-80, 다크) ││
│ │ [선택  V]    ││ 라벨 클래스   ││                                ││ ─객체 목록─  ││ (인라인 토글)││
│ │ [BBox  B]    ││ 트리/색상     ││   프레임 이미지 + 라벨 오버레이 ││ ObjectClass  ││ HistoryPanel ││
│ │ [폴리곤 P]   ││ ■ person      ││                                ││ Tree         ││ Gitea 커밋   ││
│ │ ─────        ││ ■ car         ││                                ││ ─속성─       ││ 목록/Diff    ││
│ │ [삭제 Del]   ││ ■ ...         ││                                ││ ObjectAttr   ││              ││
│ │ [실행취소]   ││               ││                                ││ Panel        ││              ││
│ │ ─────        ││               ││                                ││ ─시계열메타─ ││              ││
│ │ [저장 Ctrl+S]││               ││                                ││ Timeseries   ││              ││
│ │              ││               ││                                ││ SidePanel    ││              ││
│ │              ││               ││                                ││ (textarea+   ││              ││
│ │              ││               ││                                ││  글자수)     ││              ││
│ └──────────────┘└───────────────┘└────────────────────────────────┘└──────────────┘└──────────────┘│
├─ 하단 (h-120, border-t) ────────────────────────────────────────────────────────────────────┤
│ DarkFrameStrip (h-60) — 썸네일: [1][2][3][4][5][6]...[120]                                  │
│ DarkFrameSlider (h-60) — [<<] |---------O---------------| [>>]                              │
└─────────────────────────────────────────────────────────────────────────────────────────────┘
```

- HistoryPanel(w-80)은 헤더의 [히스토리] 버튼으로 인라인 토글 (INTERNAL 채널 + showHistory 가드 통과 시).
- 미저장 변경(dirty>0) 상태에서 X 닫기 클릭 시 3-옵션 Modal: [저장 후 닫기]/[저장 없이 닫기]/[취소].

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 프레임 이미지 | Konva.Image | - | O | resolveFrameImageType(RAW/DEID) |
| 라벨 오브젝트 | Konva.Shape | - | I/O/E | 좌표 점 <=1000개 (CWE-770) |
| 도구 선택 | Toolbar | enum | I/E | BBox/Polygon/Mask/Select |
| 라벨 마스터 | Dropdown | string | I/E | LabelMasterService.list() |
| 속성 입력 | Form | - | I/E | LabelAttrService.list() |
| 시계열 메타 사이드 패널 | TimeseriesSidePanel | - | I/O/E | 접이식. srcSn 변경 시 `useMeta(srcSn)` 로 자동 재조회. `PUT /v1/frames/{srcSn}/meta` 저장 |
| 비식별 오류 신고 | Button | - | I | UC-003 트리거 |
| SAM2 N프레임 전파 | Button | - | I | POST /v1/frames/{srcSn}/sam2-track |
| 저장 | Button(Ctrl+S) | - | I | 좌표 검증 후 bulkUpsert |
| 이전·다음 프레임 | Button(←→) | - | I | 키보드 단축키 |
| 검수 제출 | Button | - | I | WORKER 전용 |

**처리 내용**

- GET `/v1/frames/{srcSn}/labels` → 캔버스 렌더 (KLID-AT-SD-004)
- 편집 후 Ctrl+S → PUT `/v1/frames/{srcSn}/labels` (좌표 검증 + Gitea 자동 커밋)
- 비식별 오류 신고 → POST `/v1/labels/{srcSn}/deident-report` (KLID-AT-SD-006)
- 시계열 메타: 우측 패널 하단 TimeseriesSidePanel — 프레임 전환 시 srcSn 기반 자동 재조회. dirty 시만 저장 활성. `PUT /v1/frames/{srcSn}/meta { vlmText }`

**기술적 고려사항**

konva 별도 청크, useReducer로 변경 이력 관리, dirty 상태 페이지 이탈 가드. 풀스크린 다크 UI (AppLayout 밖).

---

### 3.9 KLID-AT-SC-009 오토라벨 요약(AutoLabelSummary)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-009</td><td>화면명</td><td>오토라벨 요약(AutoLabelSummary)</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-001</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">-</td></tr>
<tr><td>화면유형</td><td>조회</td><td>메뉴경로</td><td>`/auto/{videoId}`</td></tr>
<tr><td>화면개요</td><td colspan="3">YOLO/SAM2 오토라벨링 결과 요약. 처리 정보, 신뢰도 구간 분포, 라벨별 분포, 낮은 신뢰도 프레임 목록 제공.</td></tr>
</table>

**화면 레이아웃**

AppLayout 내부 (LNB w-60 + 상단 h-14). 페이지 본문은 `<section>` + `flex flex-col gap-4`.

```
AppLayout (LNB w-60 / 상단 h-14)
└── <section> (flex flex-col gap-4)
    ├── PageHeader title="오토라벨링 결과 요약"
    │     ├── breadcrumb: [영상 (/video/completed)] > [#{validId} (/video/{validId})] > 오토라벨
    │     └── actions: [data 있고 placeholder 아님]
    │           [시계열 메타 검토] Button(primary,md) → navigate(`/auto/{validId}/meta`)
    │
    ├── [잘못된 ID] ErrorState "잘못된 영상 ID / 유효한 영상 ID가 필요합니다."
    ├── [로딩] rounded border bg-white p-4 + Skeleton 2개
    ├── [에러] ErrorState "요약 정보를 불러올 수 없습니다"
    │
    ├── [placeholder (buckets/classDistribution 없음)]
    │     rounded border bg-white p-6 text-center
    │     · "배치 처리 대기 중" (text-section-title)
    │     · "YOLO/SAM2 오토라벨링 완료 후 요약 정보가 표시됩니다." (text-body)
    │
    └── [data 있고 placeholder 아님] — 4개 섹션
          ├── 처리 정보 (data-testid="auto-summary-info")
          │     · rounded border bg-white p-4, grid-cols-2 sm:grid-cols-5 gap-3
          │     · Field "총 프레임"     {totalFrames.toLocaleString}
          │     · Field "총 라벨"        {totalLabels.toLocaleString}
          │     · Field "평균 신뢰도"   {(averageConfidence*100).toFixed(1)}%
          │     · Field "VLM 동의"      {vlmVerifiedCount.toLocaleString}
          │     · Field "VLM 거부"      {vlmRejectedCount.toLocaleString}
          │
          ├── ConfidenceDistribution buckets={data.buckets}
          │     (외부 컴포넌트 — 0.9+/0.7~0.9/<0.7 막대 차트)
          │
          ├── <section data-testid="class-distribution"> aria-label="라벨별 분포"
          │     · rounded border bg-white p-4
          │     · h3 "라벨별 분포"
          │     · <ul flex flex-col gap-2> — 상위 10개
          │         · 라벨명(w-20 shrink-0 truncate, text-xs)
          │         · 배경막대 (bg-gray-100 h-3 rounded-full) + 채움막대(bg-blue-500)
          │         · 카운트 (w-8 text-right tabular-nums)
          │
          └── <section data-testid="low-confidence-section"> aria-label="낮은 신뢰도 프레임"
                · rounded border bg-white p-4
                ├── 헤더 줄 (flex justify-between)
                │     ├── h3 "신뢰도 낮은 프레임"
                │     └── [필터 토글] Button(sm, variant=lowOnly?'primary':'outline')
                │           라벨: lowOnly=false 시 "낮은 신뢰도 프레임만 보기 (70% 미만)"
                │                 lowOnly=true  시 "전체 보기"
                └── <ul data-testid="low-confidence-list"> grid-cols-3 sm:grid-cols-6 gap-2
                      · <li data-testid="low-frame-{srcSn}">
                          · <img src={thumbnailUrl} loading="lazy" w=120 h=68> (BE 응답 URL 그대로)
                          · "#{frameNo} · {(confidence*100).toFixed(0)}%" (text-sub text-neutral)
                · Empty: "표시할 프레임이 없습니다."
```

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 총 프레임 수 | Field | int | O | - |
| 총 라벨 수 | Field | int | O | - |
| 평균 신뢰도 | Field | float | O | % 변환 표시 |
| VLM 동의/거부 수 | Field | int | O | - |
| 신뢰도 분포 | ConfidenceDistribution | - | O | buckets 기반 막대 렌더 |
| 라벨별 분포 | 가로 막대 목록 | - | O | 상위 10개 |
| 낮은 신뢰도 프레임 | 썸네일 그리드 | - | O | frameNo + confidence% 표시 |
| 필터 토글 | Button | bool | I/E | 70% 미만 프레임만 / 전체 토글 |
| 시계열 메타 검토 | Button | - | I | → `/auto/{videoId}/meta` |

**처리 내용**

`GET /v1/videos/{videoId}/auto-summary` → 요약 렌더. BE placeholder(status=PENDING, buckets 없음) 감지 시 "배치 처리 대기 중" 안내 표시.

**기술적 고려사항**

videoId는 number 검증. 이미지 URL은 BE 응답값 그대로 사용(사용자 입력 반영 X).

---

### 3.10 KLID-AT-SC-010 시계열 메타 검토(MetaReview)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-010</td><td>화면명</td><td>시계열 메타 검토</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-005</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">KLID-AT-SD-010</td></tr>
<tr><td>화면유형</td><td>조회·갱신</td><td>메뉴경로</td><td>`/auto/{videoId}/meta`</td></tr>
<tr><td>화면개요</td><td colspan="3">VLM 시계열 결과 검토·수정. 좌측: 프레임 미리보기 + 상태 변화 타임라인. 우측: VLM 시계열 자연어 텍스트 패널(단일). (V2.0 — VLM 자연어 응답 전환: 환경/이벤트 enum 폼·VLM 객체 검증 카드 제거. 외부 VLM 이 자유 텍스트로 시계열 정보를 반환하므로 textarea 단일 입력으로 검토·수정)</td></tr>
</table>

**화면 레이아웃**

```
┌─ PageHeader ────────────────────────────────────────────────────────────────────────┐
│ 시계열 메타 검토·수정                                                 [저장]         │
│ breadcrumb: 영상 > #{videoId} > 오토라벨 > 메타 검토                                 │
├─ grid 2열 (lg:grid-cols-2, gap-4) ──────────────────────────────────────────────────┤
│ ┌─ 좌(meta-left-panel) ─────────────┐  ┌─ 우(meta-right-panel) ───────────────────┐ │
│ │ ┌─ section: 프레임 미리보기 ───┐  │  │ ┌─ TimeseriesTextPanel(fieldset) ─────┐ │ │
│ │ │ 프레임 (h3)                  │  │  │ │ VLM 시계열 메타 (legend)             │ │ │
│ │ │ ┌──────────────────────────┐ │  │  │ │ 외부 VLM 이 자동 생성한 시계열 정보  │ │ │
│ │ │ │  <img> 프레임 이미지      │ │  │  │ │ 입니다. 검토 후 수정할 수 있습니다.  │ │ │
│ │ │ │  (object-contain)         │ │  │  │ │ ┌──────────────────────────────┐    │ │ │
│ │ │ │                           │ │  │  │ │ │ <textarea rows=8>            │    │ │ │
│ │ │ └──────────────────────────┘ │  │  │ │ │  자유 텍스트 (최대 5000자)    │    │ │ │
│ │ │ #{frameNo} (srcSn: {srcSn})  │  │  │ │ │                              │    │ │ │
│ │ └──────────────────────────────┘  │  │ │ └──────────────────────────────┘    │ │ │
│ │ ┌─ StateChangeTimeline ────────┐  │  │ │              hint: {N}/5000          │ │ │
│ │ │ 상태 변화 (h3)               │  │  │ └──────────────────────────────────────┘ │ │
│ │ │ ┌───┐                        │  │  │                                          │ │
│ │ │ │#N │ fromState → toState    │  │  │                                          │ │
│ │ │ ├───┤                        │  │  │                                          │ │
│ │ │ │#N │ fromState → toState    │  │  │                                          │ │
│ │ │ └───┘                        │  │  │                                          │ │
│ │ └──────────────────────────────┘  │  │                                          │ │
│ └───────────────────────────────────┘  └──────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

- 좌측: 프레임 카드 + StateChangeTimeline 카드 (`flex flex-col gap-4`).
- 우측: TimeseriesTextPanel 단일 카드 (fieldset + textarea + 글자수 hint).
- 진입: `/auto/{videoId}/meta?srcSn=N` — srcSn 미지정 시 ErrorState("프레임이 선택되지 않았습니다").

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 프레임 이미지 | img | - | O | BE 응답 URL |
| 상태 변화 타임라인 | StateChangeTimeline | - | O | 시간순 이벤트 |
| VLM 시계열 텍스트 | TimeseriesTextPanel(Textarea) | String(<=5000) | I/E | zod max(5000). React 자동 escape (XSS 방어) |
| 글자수 카운터 | hint | - | O | `{현재}/5000` 실시간 표시 |
| 저장 | Button | - | I | PUT /v1/frames/{srcSn}/meta (dirty 시만 활성) |

**처리 내용**

`GET /v1/frames/{srcSn}/meta` → 좌(프레임 + StateChangeTimeline) / 우(TimeseriesTextPanel 단일) 렌더. 수정 후 저장 → `PUT /v1/frames/{srcSn}/meta { vlmText }` (zod `string().max(5000)` 검증 후 호출).

**기술적 고려사항**

dirty 상태 추적으로 변경 없으면 저장 비활성. srcSn은 쿼리파라미터 `?srcSn=N`으로 전달.

---

### 3.11 KLID-AT-SC-011 비식별 영상 목록(DeidentList)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-011</td><td>화면명</td><td>비식별 영상 목록(DeidentList)</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-003</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">-</td></tr>
<tr><td>화면유형</td><td>조회</td><td>메뉴경로</td><td>`비식별 > 비식별 결과`</td></tr>
<tr><td>화면개요</td><td colspan="3">비식별화 처리가 완료되었거나 실패한 영상 목록. 비식별 대상 여부·처리 상태 필터 및 진행 프레임 수 표시.</td></tr>
</table>

**화면 레이아웃**

```
┌─ section (flex flex-col gap-4) ─────────────────────────────────────────────────────┐
│ ┌─ PageHeader ──────────────────────────────────────────────────────────────────┐  │
│ │ 비식별화 결과                                                                  │  │
│ │ 비식별화 처리가 완료되었거나 실패한 영상 목록입니다.                            │  │
│ └────────────────────────────────────────────────────────────────────────────────┘  │
│ ┌─ 필터 박스 (grid sm:grid-cols-3, rounded border bg-white p-4) ────────────────┐  │
│ │  [Select: 비식별 대상 (PRVC_YN) ▼]   [Select: 처리 상태 ▼]                    │  │
│ │   전체 / 대상(Y) / 비대상(N)          전체/PENDING/IN_PROGRESS/COMPLETED/FAILED │  │
│ └────────────────────────────────────────────────────────────────────────────────┘  │
│ ┌─ DataTable<DeidentListRow> ──────────────────────────────────────────────────┐   │
│ │ CCTV명 │ Clip ID │ 개인정보 유형 │ 비식별 대상 │ 상태(StatusBadge) │ 진행 │액션 │   │
│ │ cctv01 │ clip-001│ 얼굴         │ 대상       │ [COMPLETED]      │120/120│ ▶ │   │
│ │ cctv02 │ clip-002│ 차량번호      │ 대상       │ [BATCH_FAILED]  │80/120 │ ▶ │   │
│ │        │         │              │            │                  │(실패40)│   │   │
│ │ cctv03 │ clip-003│ -            │ 비대상      │ [COMPLETED]      │100/100│ ▶ │   │
│ │                                                                                │   │
│ │ 액션 셀: [상세](ghost) [재처리](outline, REVIEWER+prvcYn=Y 만 노출)            │   │
│ │ 페이징: page/size — searchParams 동기화                                         │   │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

- 컴포넌트: `PageHeader`, `Select`(label/options/onChange), `DataTable<DeidentListRow>`, `StatusBadge`, `Button`(ghost/outline).
- URL `?page=&size=&prvcYn=&status=` 동기화 (`useSearchParams`).
- 진입: `/deident`. 상세 클릭 → `/deident/{videoId}`. 재처리는 REVIEWER 역할 + `prvcYn === 'Y'` 행에만 노출.

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 비식별 대상 필터 | Select | enum | I/E | 전체/대상(Y)/비대상(N) |
| 처리 상태 필터 | Select | enum | I/E | 전체/PENDING/IN_PROGRESS/COMPLETED/FAILED |
| DataTable 컬럼 | 각종 | - | O | CCTV명/Clip ID/개인정보유형/대상/상태/진행/액션 |
| 상세 버튼 | Button(ghost) | - | I | → `/deident/{videoId}` |
| 재처리 버튼 | Button(outline) | - | I | REVIEWER + prvcYn=Y 행에만 노출 |

**처리 내용**

`GET /v1/deident?prvcYn=&status=&page=&size=` → DataTable 렌더. 재처리 클릭 → `POST /v1/deident/{videoId}/reprocess` → 성공 토스트 + 목록 재조회.

**기술적 고려사항**

검색 입력은 axios params로 전달(XSS 방지). IDOR 방어는 BE에서 수행.

---

### 3.12 KLID-AT-SC-012 비식별 영상 상세(DeidentDetail)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-012</td><td>화면명</td><td>비식별 영상 상세</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-003</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">KLID-AT-SD-006</td></tr>
<tr><td>화면유형</td><td>조회·갱신</td><td>메뉴경로</td><td>`/deident/{videoId}`</td></tr>
<tr><td>화면개요</td><td colspan="3">비식별화 영상 단위 비교. 처리 정보 5컬럼 + 12프레임 그리드 + 좌우(원본/비식별) 비교 + 처리 이력.</td></tr>
</table>

**화면 레이아웃**

```
┌─ section (flex flex-col gap-4) ─────────────────────────────────────────────────────┐
│ ┌─ PageHeader (breadcrumb: 비식별화 > #{videoId}) ──────────────────────────────┐  │
│ │ 비식별화 영상 비교                                                              │  │
│ └────────────────────────────────────────────────────────────────────────────────┘  │
│ ┌─ 처리 정보 5컬럼 (grid sm:grid-cols-5, rounded border bg-white p-4) ─────────┐   │
│ │ Field: CCTV명 │ Clip ID │ 개인정보 유형 │ 상태(StatusBadge) │ 처리/실패/총     │   │
│ │   cctv-01     │ clip-001│ 얼굴          │ [COMPLETED]        │ 120 / 0 / 120  │   │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
│ ┌─ section: 프레임 페어 (최대 12) (rounded border bg-white p-4) ───────────────┐   │
│ │ <h3> 프레임 페어 (최대 12)                                                     │   │
│ │ ┌─ FrameGrid12 (선택된 srcSn 강조) ─────────────────────────────────────────┐ │   │
│ │ │ [img1] [img2] [img3] [img4] [img5] [img6]                                  │ │   │
│ │ │ [img7] [img8] [img9] [img10][img11][img12]                                 │ │   │
│ │ └────────────────────────────────────────────────────────────────────────────┘ │   │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
│ ┌─ section: 선택 프레임 비교 (selectedPair 존재 시) ───────────────────────────┐   │
│ │ <h3> 선택 프레임 비교 (#{frameNo ?? srcSn})                                     │   │
│ │ ┌─ SideBySideCompare (50:50) ──────────────────────────────────────────────┐  │   │
│ │ │  [원본]                       │                       [비식별]           │  │   │
│ │ │  leftImage=originalUrl        │              rightImage=processedUrl    │  │   │
│ │ │                                │              (없으면 "비식별 이미지 없음")│  │   │
│ │ └───────────────────────────────────────────────────────────────────────────┘  │   │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
│ ┌─ ProcessHistoryList ─────────────────────────────────────────────────────────┐   │
│ │ 처리 이력                                                                      │   │
│ │  2026-05-26 14:00 │ COMPLETED   │ 전체 프레임 비식별 완료                       │   │
│ │  2026-05-26 13:50 │ IN_PROGRESS │ 비식별 처리 시작                              │   │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

- 컴포넌트: `PageHeader`(breadcrumb), `Field`(내부 dt/dd), `StatusBadge`, `FrameGrid12`(`selectedSrcSn`+`onSelect`), `SideBySideCompare`, `ProcessHistoryList`, `Skeleton`/`ErrorState`.
- 진입: `/deident/{videoId}` — videoId NaN/0 이하면 `ErrorState("잘못된 영상 ID")`. 첫 진입 시 `framePairs[0].srcSn`로 자동 선택.

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 처리 정보 | Field x5 | - | O | CCTV명/Clip ID/개인정보유형/상태/프레임수 |
| 프레임 그리드 | FrameGrid12 | - | I/O | 클릭 시 좌우 비교 갱신 |
| 좌우 비교 | SideBySideCompare | - | O | 원본/비식별 50:50 |
| 처리 이력 | ProcessHistoryList | - | O | 시각/상태/메시지 |

**처리 내용**

`GET /v1/deident/{videoId}/detail` → 처리 정보·프레임·이력 렌더. REVIEWER가 재처리 클릭 → `POST /v1/deident/{videoId}/reprocess`.

**기술적 고려사항**

videoId number 검증. 이미지 URL은 BE 응답값만 사용.

---

### 3.13 KLID-AT-SC-013 검수 목록(ReviewList)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-013</td><td>화면명</td><td>검수 목록</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-009</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">KLID-AT-SD-005</td></tr>
<tr><td>화면유형</td><td>조회</td><td>메뉴경로</td><td>`검수 > 검수 대기`</td></tr>
<tr><td>화면개요</td><td colspan="3">작업자가 제출한 라벨링 결과를 검수하는 REVIEWER 전용 목록. KPI 4종 + 검색·상태 필터 + DataTable.</td></tr>
</table>

**화면 레이아웃**

```
┌─ section (flex flex-col gap-4, data-testid="review-list-page") ──────────────────────┐
│ ┌─ PageHeader ──────────────────────────────────────────────────────────────────┐   │
│ │ 검수 목록                                                                      │   │
│ │ 작업자가 제출한 라벨링 결과를 검수합니다.                                       │   │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
│ ┌─ KPI 4종 (grid grid-cols-4 gap-4) ───────────────────────────────────────────┐   │
│ │ ┌─ KpiCard ─────┐ ┌─ KpiCard ─────┐ ┌─ KpiCard ─────┐ ┌─ KpiCard ─────┐    │   │
│ │ │ Hourglass 🟡  │ │ ClipboardCheck│ │ CheckCircle2 🟢│ │ XCircle 🔴    │    │   │
│ │ │ 검수 대기 {N} │ │ 검수중 {N}    │ │ 승인 {N}      │ │ 반려 {N}      │    │   │
│ │ └───────────────┘ └───────────────┘ └───────────────┘ └───────────────┘    │   │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
│ ┌─ 필터 박스 (flex flex-wrap items-end gap-3, rounded border bg-white p-3) ────┐   │
│ │  [영상명 / 작업자명 input "검색어를 입력하세요"] [상태 ▼ 전체/PENDING/.../REJECTED] │
│ │  [조회](primary)  [초기화](ghost, disabled when !isFilterActive)              │   │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
│ ┌─ DataTable<Review> ──────────────────────────────────────────────────────────┐   │
│ │ 영상명      │ 이벤트          │ 작업자 │ 제출일(sort) │ 라벨수 │ 상태  │ 액션 │   │
│ │ cctv-test-1 │ [EventTypeBadge]│ 홍길동 │ 2026-05-26   │ 45    │[PEND] │ ▶   │   │
│ │  video-0001 │ 쓰러짐          │       │ 14:00        │       │       │검수시작▶│  │
│ │ cctv-test-2 │ [EventTypeBadge]│ 박작업 │ 2026-05-25   │ 38    │[REV]  │이어서검수│ │
│ │ cctv-test-3 │ [EventTypeBadge]│ 이작업 │ 2026-05-24   │ 52    │[COMP] │결과보기▶│ │
│ │                                                                                │   │
│ │ 페이징/정렬: searchParams 동기화 (page/size/sort=submittedAt,desc)             │   │
│ │ ASSIGNED 상태(라벨링 진행 중)는 클라이언트에서 제외                              │   │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

- 컴포넌트: `PageHeader`, `KpiCard`×4(아이콘+iconBg), `Button`(primary/ghost/secondary), `DataTable<Review>`, `StatusBadge`, `EventTypeBadge`.
- 액션 라벨: `REVIEW_PENDING` → "검수시작 ▶"(primary), `REVIEWING` → "이어서 검수"(secondary), 그 외 → "결과보기 ▶"(secondary).
- 클라이언트 검색/상태 필터 — 영상명·작업자명 부분일치(toLowerCase).
- 진입: `/review` (REVIEWER 라우터 RoleGuard). 클릭 시 `navigate('/review/{id}')`.

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| KPI 카드 | KpiCard x4 | int | O | 검수 대기/검수중/승인/반려 |
| 검색어 | input text | String | I/E | 영상명/작업자명 |
| 상태 필터 | Select | enum | I/E | 전체/REVIEW_PENDING/REVIEWING/COMPLETED/REJECTED |
| DataTable | 각종 컬럼 | - | O | 영상명/이벤트/작업자/제출일/라벨수/상태/액션 |
| 검수 시작 | Button | - | I | → `/review/{id}` |

**처리 내용**

`GET /v1/reviews?page=&size=&sort=` → DataTable 렌더. 검수 시작 클릭 → `/review/{id}` 이동.

**기술적 고려사항**

URL searchParams로 페이지 파라미터만 전달 (axios 자동 URL 인코딩 — XSS 방어). REVIEWER 역할 검증은 라우터 RoleGuard.

---

### 3.14 KLID-AT-SC-014 검수 상세(ReviewDetail)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-014</td><td>화면명</td><td>검수 상세</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-009, KLID-AT-UC-014</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">KLID-AT-SD-005, KLID-AT-SD-011</td></tr>
<tr><td>화면유형</td><td>조회·갱신</td><td>메뉴경로</td><td>`/review/{id}`</td></tr>
<tr><td>화면개요</td><td colspan="3">3분할 풀스크린 검수 화면. 좌측: 읽기 전용 Konva 캔버스. 우측: 객체 목록/속성/검수 메모. 하단: 프레임 타임라인 + 승인/반려 액션 바.</td></tr>
</table>

**화면 레이아웃**

```
┌─ fixed inset-0 z-50 grid (다크 bg-gray-900) ─────────────────────────────────────────┐
│  gridTemplateColumns: 1fr 360px   /   gridTemplateRows: 64px 1fr auto                │
│ ┌─ ReviewHeader (col-span-2, h-16, 다크) ──────────────────────────────────────┐    │
│ │  검수 │ cctv-001 │ 홍길동 │ 제출: 2026-05-26 │ 프레임 {curr+1}/{total} │       │    │
│ │  [상태 배지 REVIEWING]                                          [X 닫기]      │    │
│ └────────────────────────────────────────────────────────────────────────────────┘    │
│ ┌─ main canvas (relative, 다크 bg-gray-900) ──┐ ┌─ aside (w-360px, 다크) ────┐   │
│ │  [읽기 전용] 뱃지 (left-3 top-3, yellow-600) │ │ ┌─ ObjectListPanel ──────┐ │   │
│ │                                              │ │ │ <h2> 객체 목록         │ │   │
│ │ ┌─ LabelCanvas (Konva read-only) ──────────┐ │ │ │ person-1 person-2 car-1│ │   │
│ │ │  frame={frames[currentFrameIdx]}         │ │ │ └────────────────────────┘ │   │
│ │ │  라벨 오버레이 표시                       │ │ │ ┌─ ObjectAttributesPanel┐ │   │
│ │ │                                          │ │ │ │ <h2> 속성              │ │   │
│ │ │                                          │ │ │ │ 라벨: person 색상: ... │ │   │
│ │ │                                          │ │ │ └────────────────────────┘ │   │
│ │ └──────────────────────────────────────────┘ │ │ ┌─ ReviewMemoPanel ──────┐ │   │
│ │                                              │ │ │ 검수 메모(Textarea<=2K)│ │   │
│ │                                              │ │ │ 이슈 목록 (issues[])   │ │   │
│ │                                              │ │ └────────────────────────┘ │   │
│ └──────────────────────────────────────────────┘ └────────────────────────────┘   │
│ ┌─ Footer (col-span-2) ──────────────────────────────────────────────────────────┐ │
│ │ ┌─ FrameTimeline ────────────────────────────────────────────────────────────┐ │ │
│ │ │ 1 | 2 | 3 | ... | {totalFrames}   (현재 프레임 강조)                         │ │ │
│ │ └────────────────────────────────────────────────────────────────────────────┘ │ │
│ │ ┌─ ReviewActionBar ──────────────────────────────────────────────────────────┐ │ │
│ │ │                                              [반려](RejectModal) [승인]    │ │ │
│ │ └────────────────────────────────────────────────────────────────────────────┘ │ │
│ └────────────────────────────────────────────────────────────────────────────────┘ │
│  ConfirmDialog(승인 확정), RejectModal(reason+composeRejectReason)                  │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

- 풀스크린 3분할 그리드 (`gridTemplateColumns: '1fr 360px'`, `gridTemplateRows: '64px 1fr auto'`).
- 진입 시 자동 `startReview` (REVIEW_PENDING → REVIEWING).
- 승인 → `ConfirmDialog` → `useApproveReview` → 토스트 + `/review` 복귀.
- 반려 → `RejectModal` → `composeRejectReason(userReason, reviewComment, pendingIssues)` 로 사유 합성.
- 다크 테마 고정 (`bg-gray-900 text-white`).

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 영상 메타 | Header | - | O | 영상명·CCTV·작업자·제출일 |
| 프레임 타임라인 | FrameTimeline | - | I/E | 프레임 선택 |
| 라벨 캔버스 | LabelCanvas | - | O | konva read-only |
| 객체 리스트 | ObjectListPanel | - | O | 클릭 시 캔버스 포커스 |
| 객체 속성 | ObjectAttributesPanel | - | O | 라벨 속성 표시 |
| 검수 메모 | ReviewMemoPanel | Textarea | I/E | <=2000자 |
| 이슈 목록 | IssueSidebar | - | I/E | 코멘트·심각도 |
| 승인 | Button | - | I | ConfirmDialog 경유 → POST `/v1/reviews/{videoId}/approve` |
| 반려 | Button(+사유) | - | I | RejectModal → reason 필수 |

**처리 내용**

승인 시 `ReviewService.approve()` → 상태 전이(COMPLETED) → `ReviewApprovedEvent` 발행(비동기) → 관제서버 TASK_COMPLETED 통지(KLID-AT-SD-011).

**기술적 고려사항**

풀스크린 3분할 그리드(1fr 360px / 64px 1fr auto). 낙관적 잠금(409) 시 재조회 안내 토스트.

---

### 3.15 KLID-AT-SC-015 버전 이력(History)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-015</td><td>화면명</td><td>버전 이력(Gitea Diff)</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-010</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">KLID-AT-SD-004</td></tr>
<tr><td>화면유형</td><td>조회</td><td>메뉴경로</td><td>`/history/{videoId}`</td></tr>
<tr><td>화면개요</td><td colspan="3">Gitea 기반 라벨 버전 관리. 커밋 타임라인 + Diff 뷰어 + 롤백 기능.</td></tr>
</table>

**화면 레이아웃**

```
┌─ div (space-y-5 p-6, data-testid="history-page") ────────────────────────────────────┐
│ ┌─ 헤더 (flex items-center gap-3) ──────────────────────────────────────────────┐  │
│ │  [ArrowLeft 뒤로가기 버튼]   버전 관리                                          │  │
│ │                              {videoId}                                          │  │
│ └────────────────────────────────────────────────────────────────────────────────┘  │
│ ┌─ HistoryPanel (rounded-lg border bg-white, srcSn={videoId}) ────────────────┐    │
│ │ ┌─ Header ────────────────────────────────────────────────────────────────┐ │    │
│ │ │ [GitCommit] 커밋 목록   [N]                                  (onClose 없음) │ │    │
│ │ └─────────────────────────────────────────────────────────────────────────┘ │    │
│ │ ┌─ CommitList (ul max-h-[40vh] overflow-y-auto, divide-y) ──────────────┐  │    │
│ │ │ ☐ [shortHash] [최신]                                                    │  │    │
│ │ │   라벨 45건 수정                                                         │  │    │
│ │ │   홍길동 · 2026-05-26 14:30                                              │  │    │
│ │ │ ─────────                                                                │  │    │
│ │ │ ☐ [shortHash]                                                            │  │    │
│ │ │   오토라벨 초기 등록                                                      │  │    │
│ │ │   시스템 · 2026-05-26 10:00                                              │  │    │
│ │ └─────────────────────────────────────────────────────────────────────────┘  │    │
│ │ ┌─ Diff 영역 (border-t, flex-1 overflow-y-auto p-4) ────────────────────┐    │    │
│ │ │ <h3> 변경 내용 (Diff)                          [롤백](secondary, REVIEWER│    │
│ │ │                                                +WORKER, 최신 아닌 경우)│    │    │
│ │ │ ┌─ DiffViewer (diffs[]) ─────────────────────────────────────────────┐ │    │    │
│ │ │ │  - person-1: bbox(10,20,100,200)                                    │ │    │    │
│ │ │ │  + person-1: bbox(12,22,102,202)                                    │ │    │    │
│ │ │ │  + car-2: bbox(300,150,400,300) [신규]                              │ │    │    │
│ │ │ └─────────────────────────────────────────────────────────────────────┘ │    │    │
│ │ │ (커밋 미선택 시: "커밋을 선택하거나 두 커밋을 체크하여 diff 를 확인하세요.")│   │    │
│ │ └─────────────────────────────────────────────────────────────────────────┘  │    │
│ │ RollbackConfirmModal (롤백 클릭 시 commitSha 전달)                            │    │
│ └──────────────────────────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

- 컴포넌트: `HistoryPanel`(`srcSn`/`onClose?`/`dark?` props) — 라우트 페이지와 라벨링 화면 우측 패널이 공유.
- 체크박스 2개 선택 시 → 두 커밋 간 diff. 단일 클릭 시 → 해당 커밋과 다음(이전) 커밋 간 diff.
- 롤백 버튼 노출 조건: 단일 선택 + 최신이 아닌 커밋 + 권한(REVIEWER/WORKER).
- ESC 키 닫기는 `onClose` 가 있을 때만 (라우트 페이지에선 미적용).

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 커밋 타임라인 | Timeline | - | O | 작성자·시각·메시지 |
| Diff 뷰어 | DiffViewer | - | O | 라벨 add/mod/del 색상 |
| 롤백 | Button(+확인) | - | I | POST `/v1/versions/{commit}/rollback` |

**처리 내용**

`GET /v1/videos/{videoId}/versions` → 커밋 타임라인. `GET /v1/versions/{commit}/diff` → Diff 뷰어. 롤백: ConfirmDialog → POST.

**기술적 고려사항**

commit hash는 BE에서 SHA hex 검증. FE는 단순 전달.

---

### 3.16 KLID-AT-SC-016 워커 통계(WorkerStat)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-016</td><td>화면명</td><td>워커 통계(WorkerStat)</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">-</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">-</td></tr>
<tr><td>화면유형</td><td>조회</td><td>메뉴경로</td><td>`통계 > 작업자 통계`</td></tr>
<tr><td>화면개요</td><td colspan="3">작업자별 작업 통계. REVIEWER는 작업자 선택 가능, WORKER는 본인 고정. KPI 4 + Secondary 2 + 일별 차트 + 월별 표.</td></tr>
</table>

**화면 레이아웃**

```
┌─ section (flex flex-col gap-6, data-testid="worker-stat-page") ──────────────────────┐
│ ┌─ 헤더 (flex items-center justify-between) ────────────────────────────────────┐   │
│ │  ┌─ 좌 ────────────────────────────────────┐  ┌─ 우 (REVIEWER 만) ────────┐  │   │
│ │  │ [BarChart2 아이콘, 9x9 bg-blue-50]      │  │ <select> 작업자 선택       │  │   │
│ │  │  작업자 통계                              │  │   홍길동 / 박작업 / ...    │  │   │
│ │  │  {workerName} (서브)                     │  └────────────────────────────┘  │   │
│ │  └──────────────────────────────────────────┘                                 │   │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
│ ┌─ KPI 4 (grid grid-cols-2 md:grid-cols-4, data-testid="worker-kpi-grid") ────┐    │
│ │ KpiCard(CheckCircle 🟢)  KpiCard(Clock 🟡)  KpiCard(XCircle 🔴) KpiCard(Tag) │    │
│ │ 완료 작업 {N}            진행 중 {N}        반려 {N}             총 라벨 {N}  │    │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
│ ┌─ Secondary 카드 2 (grid grid-cols-2) ───────────────────────────────────────┐   │
│ │ ┌─ 카드 ───────────────┐  ┌─ 카드 ─────────────────────────────────────┐   │   │
│ │ │ 오토라벨 비율          │  │ 반려율 (rejectPct > 10 ? text-red-600)      │   │   │
│ │ │ 78.5% (또는 "—")      │  │ 6.7%                                       │   │   │
│ │ └────────────────────────┘  └────────────────────────────────────────────┘   │   │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
│ ┌─ section: 일별 작업량 (rounded-lg border bg-white p-5) ─────────────────────┐    │
│ │ <h2> 일별 작업량 (최근 30일)                                                  │    │
│ │ ┌─ DailyCompletionChart (data=dailyCompletion[]) ──────────────────────────┐ │    │
│ │ │  [막대 차트 영역]                                                          │ │    │
│ │ └──────────────────────────────────────────────────────────────────────────┘ │    │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
│ ┌─ section: 월별 통계 (rounded-lg border bg-white overflow-hidden) ──────────┐    │
│ │ <h2> 월별 통계 (최근 12개월)                                                  │    │
│ │ ┌─ table (data-testid="worker-monthly-table") ─────────────────────────────┐ │    │
│ │ │ 월       │ 완료(우) │ 반려(우, red) │ 라벨 수(우)                            │ │    │
│ │ │ 2026-05  │     42   │       3      │ 1,250                                 │ │    │
│ │ │ 2026-04  │     38   │       2      │ 1,100                                 │ │    │
│ │ │ 2026-03  │     35   │       4      │   980                                 │ │    │
│ │ │ (empty) → "월별 데이터가 없습니다" colSpan=4                                 │ │    │
│ │ └──────────────────────────────────────────────────────────────────────────┘ │    │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

- 컴포넌트: `KpiCard`×4, `Skeleton`(로딩), `DailyCompletionChart`, `ErrorState`.
- REVIEWER 전용 작업자 `<select>` — `useUsers({role: WORKER}, {enabled: isReviewer})`.
- WORKER 진입 시 `targetWorkerId = claims.sub` 고정, `/users` API 호출 차단 (403 방지).
- `autoLabelRate`/`rejectRate` 는 `Number.isFinite` 검증 후 `× 100` 표시.

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 작업자 선택 | Select | int | I/E | REVIEWER 전용. WORKER는 본인 고정 |
| KPI 카드 x4 | KpiCard | int | O | 완료/진행중/반려/총 라벨 수 |
| 오토라벨 비율 | 보조 카드 | float | O | % 변환. isFinite 검증 |
| 반려율 | 보조 카드 | float | O | 10% 초과 시 빨간색 강조 |
| 일별 작업량 차트 | DailyCompletionChart | - | O | 최근 30일 |
| 월별 통계 표 | table | - | O | 월/완료/반려/라벨 수 |

**처리 내용**

REVIEWER: `GET /v1/stats/workers/{workerId}`. WORKER: 본인 ID 고정 → 동일 API 호출. `/users?role=WORKER`는 REVIEWER만 호출.

**기술적 고려사항**

WORKER 화면에서 `/users` API 호출 차단(403 방지). autoLabelRate/rejectRate는 Number.isFinite 검증 후 렌더.

---

### 3.17 KLID-AT-SC-017 전체 통계(OverallStat)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-017</td><td>화면명</td><td>전체 구축 현황(OverallStat)</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">-</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">-</td></tr>
<tr><td>화면유형</td><td>조회</td><td>메뉴경로</td><td>`통계 > 전체 통계` (REVIEWER 전용)</td></tr>
<tr><td>화면개요</td><td colspan="3">시스템 전체 학습 데이터 구축 현황. 누적 이미지·영상, 처리 현황 5카드, 일별 작업량, 이벤트 분포, 작업자별 현황. CSV 다운로드.</td></tr>
</table>

**화면 레이아웃**

```
┌─ div (p-6 space-y-6) ────────────────────────────────────────────────────────────────┐
│ ┌─ 헤더 (flex items-center justify-between) ────────────────────────────────────┐   │
│ │  [TrendingUp 아이콘, 9x9 bg-indigo-50]  전체 구축 현황     [📥 리포트 다운로드] │   │
│ │                                                              (Button primary)  │   │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
│ ┌─ 누적 2카드 (grid md:grid-cols-2, data-testid="cumulative-cards") ───────────┐   │
│ │ ┌─ 카드 (bg-white border p-6) ──────┐  ┌─ 카드 (bg-white border p-6) ──────┐ │   │
│ │ │ [Image 🔵] 이미지 학습데이터       │  │ [Film 🟣] 영상 학습데이터          │ │   │
│ │ │ 24,500 장 (text-3xl font-black)   │  │ 350 건                            │ │   │
│ │ │ sr-only: "누적 이미지" "누적 영상" │  │                                    │ │   │
│ │ └────────────────────────────────────┘  └────────────────────────────────────┘ │   │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
│ ┌─ 처리 현황 (bg-white border p-5) ─────────────────────────────────────────────┐  │
│ │ [Activity] 처리 현황                                                            │  │
│ │ ┌─ grid grid-cols-5 (data-testid="processing-cards") ──────────────────────┐  │  │
│ │ │ 전체 350 │ 완료(녹) 280 │ 처리중(청) 45 │ 실패(적) 10 │ 대기(황) 15        │  │  │
│ │ └──────────────────────────────────────────────────────────────────────────┘  │  │
│ └────────────────────────────────────────────────────────────────────────────────┘  │
│ ┌─ 일별 작업량 (bg-white border p-5) ───────────────────────────────────────────┐  │
│ │ <h2> 일별 전체 작업량 (최근 30일)                                                │  │
│ │ ┌─ SimpleBarChart (data-testid="daily-trend-chart", height=200, #6366f1) ─┐  │  │
│ │ │  [막대 차트 영역]                                                         │  │  │
│ │ └────────────────────────────────────────────────────────────────────────┘  │  │
│ └────────────────────────────────────────────────────────────────────────────────┘  │
│ ┌─ 이벤트 분포 (bg-white border p-5) ───────────────────────────────────────────┐  │
│ │ <h2> 이벤트 유형 분포                                                            │  │
│ │ ┌─ flex items-center gap-8 ────────────────────────────────────────────────┐  │  │
│ │ │ [SimplePieChart size=160 showLegend] │ <ul 6종 고정 슬롯>                  │  │  │
│ │ │                                       │  쓰러짐 (FALL)  [▓▓▓▓▓▓░░░░] 60   │  │  │
│ │ │                                       │  폭력           [▓▓▓▓▓░░░░░] 55   │  │  │
│ │ │                                       │  교통사고        [▓▓▓▓▓▓▓░░░] 70   │  │  │
│ │ │                                       │  이상행동        [▓▓▓▓░░░░░░] 50   │  │  │
│ │ │                                       │  침수           [▓▓▓▓▓▓░░░░] 60   │  │  │
│ │ │                                       │  산불           [▓▓▓▓▓░░░░░] 55   │  │  │
│ │ └────────────────────────────────────────────────────────────────────────────┘  │  │
│ └────────────────────────────────────────────────────────────────────────────────┘  │
│ ┌─ 작업자별 현황 (bg-white border overflow-hidden) ────────────────────────────┐  │
│ │ <h2> 작업자별 현황   <p> 컬럼 헤더 클릭으로 정렬                                  │  │
│ │ ┌─ WorkerStatsTable (rows=workers[]) ──────────────────────────────────────┐  │  │
│ │ │  작업자 │ 완료 │ 반려 │ 라벨 수 │ 반려율 (정렬 가능)                          │  │  │
│ │ │  홍길동 │  42  │  3   │ 1,250  │  6.7%                                      │  │  │
│ │ │  박작업 │  38  │  2   │ 1,100  │  5.0%                                      │  │  │
│ │ └──────────────────────────────────────────────────────────────────────────┘  │  │
│ └────────────────────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

- 컴포넌트: `Button`(primary), `SimpleBarChart`, `SimplePieChart`, `WorkerStatsTable`, `ErrorState`.
- 누적 카드는 KpiCard 미사용 — `<p text-3xl font-black>` + `<span sr-only>` (UI/UX §4-11 회귀 방지: ProgressBar/`%`/`<progress>` 절대 미노출).
- 이벤트 분포는 `FIXED_EVENT_TYPES` 6종 고정 슬롯 — 데이터 누락 시에도 6개 `<li>` 렌더.
- 다운로드: `downloadReport('MONTH')` → Blob → `overall-stat-{ISO}.csv` (파일명 사용자 입력 미반영).

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 누적 이미지 수 | 카드 | int | O | - |
| 누적 영상 수 | 카드 | int | O | - |
| 처리 현황 5카드 | 카드 그리드 | int x5 | O | 전체/완료/처리중/실패/대기 |
| 일별 작업량 | SimpleBarChart | - | O | 최근 30일 |
| 이벤트 분포 | SimplePieChart + 가로막대 | - | O | 6종 고정 슬롯 |
| 작업자별 현황 | WorkerStatsTable | - | O | 컬럼 헤더 클릭 정렬 |
| 리포트 다운로드 | Button | - | I | GET /v1/stats/report?type=MONTH → CSV |

**처리 내용**

`GET /v1/stats/overall` → 각 섹션 렌더. 다운로드: `downloadReport('MONTH')` → Blob → `<a>` 자동 클릭.

**기술적 고려사항**

이벤트 분포는 6종 고정 슬롯으로 데이터 누락 시에도 6개 항목 렌더. 파일명은 고정 prefix + ISO 날짜(사용자 입력 미반영).

---

### 3.18 KLID-AT-SC-018 증강 요청(AugmentRequest)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-018</td><td>화면명</td><td>증강 요청</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-011</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">KLID-AT-SD-008</td></tr>
<tr><td>화면유형</td><td>입력</td><td>메뉴경로</td><td>`/augment`</td></tr>
<tr><td>화면개요</td><td colspan="3">검수 완료(승인) 영상에 4종 증강(겨울/야간/비/해상도)을 요청. 2단계: 유형 선택 → 영상 선택 + 최근 이력 잡 카드.</td></tr>
</table>

**화면 레이아웃**

```
┌─ section (flex flex-col gap-6 pb-28, data-testid="augment-request-page") ────────────┐
│ ┌─ PageHeader ──────────────────────────────────────────────────────────────────┐   │
│ │ 데이터 증강 요청                                                                │   │
│ │ 검수 완료(승인) 영상에 4종(겨울/야간/비/해상도) 증강을 요청합니다.                │   │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
│ ┌─ SFR-07 안내 배너 (bg-blue-50 border-blue-200, [Info]) ──────────────────────┐   │
│ │ 증강 요청은 검수 완료(승인)된 영상만 가능합니다. 미승인 영상은 표시되지 않습니다.  │   │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
│ ┌─ Step 1: 증강 유형 선택 ─────────────────────────────────────────────────────┐    │
│ │ [①] 증강 유형 선택   [N종 선택 배지]                                            │    │
│ │ ┌─ grid grid-cols-2 md:grid-cols-4 (data-testid="augment-type-list") ─────┐  │    │
│ │ │ [AugmentTypeCard WINTER]  [NIGHT]  [RAIN]  [RESOLUTION]                  │  │    │
│ │ └──────────────────────────────────────────────────────────────────────────┘  │    │
│ │ (미선택 시: [AlertCircle] "증강 유형을 하나 이상 선택하세요.")                    │    │
│ └────────────────────────────────────────────────────────────────────────────────┘    │
│ ┌─ Step 2: 대상 영상 선택 ─────────────────────────────────────────────────────┐    │
│ │ [②] 대상 영상 선택  [검수 완료 {totalElements}건] [{N}건 선택]                  │    │
│ │ ┌─ 필터 form (flex flex-wrap, data-testid="augment-video-filters") ────────┐ │    │
│ │ │  [Search 🔍 영상명/CCTV/ID input] [이벤트 ▼ 전체/FIXED_EVENT_TYPES]      │ │    │
│ │ │  [Search 조회](primary) [RotateCcw 초기화](secondary)                    │ │    │
│ │ └──────────────────────────────────────────────────────────────────────────┘ │    │
│ │ ┌─ table (data-testid="augment-video-table") ─────────────────────────────┐  │    │
│ │ │ ☐(indet) │ 영상명 / CCTV   │ 이벤트(Badge) │ 녹화일     │ 검수 완료 일시 │  │    │
│ │ │ ☑        │ cctv-test-001   │ 쓰러짐         │ 2026-05-20 │ 2026-05-25 16:00 │ │    │
│ │ │          │ #1              │               │            │                │  │    │
│ │ │ ☑        │ cctv-test-002   │ 폭력           │ 2026-05-19 │ 2026-05-25 14:30 │ │    │
│ │ │ ☐        │ cctv-test-003   │ 교통사고       │ 2026-05-18 │ 2026-05-24 10:00 │ │    │
│ │ │ ──────────────────────────────────────────────────────────────────────── │  │    │
│ │ │  전체 85건 (1/5 페이지)                                  [이전] [다음]  │  │    │
│ │ │ (loading: Skeleton×5, empty: EmptyState)                                │  │    │
│ │ └──────────────────────────────────────────────────────────────────────────┘ │    │
│ │ ┌─ 선택 액션 배지 (선택>0일 때, bg-primary-50 border-primary-200) ────────┐ │    │
│ │ │  [{N}개 영상 선택됨]  [X 선택 해제]                                       │ │    │
│ │ └──────────────────────────────────────────────────────────────────────────┘ │    │
│ └────────────────────────────────────────────────────────────────────────────────┘    │
│ ┌─ 최근 요청 이력 ──────────────────────────────────────────────────────────────┐   │
│ │ [History 아이콘] 최근 요청 이력          [{totalElements}건]                      │   │
│ │ ┌─ grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 (data-testid="job-card-grid")┐│   │
│ │ │ [JobCard #1 겨울 3건 COMPLETED] [JobCard #2 야간 PROCESSING] [JobCard #3 FAILED]│ │   │
│ │ │ ... 최대 6건 slice                                                          │ │   │
│ │ └──────────────────────────────────────────────────────────────────────────────┘│   │
│ │ (loading: Skeleton×6, empty: EmptyState "등록된 증강 요청이 없습니다")          │   │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
│ ┌─ 고정 하단 액션 바 (fixed bottom-0 left-60 right-0 z-20, bg-white border-t) ─┐  │
│ │  선택: {N}종 × {M}건 = 예상 {N*M}건         [취소](secondary) [Wand2 증강 요청] │  │
│ └────────────────────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

- 컴포넌트: `PageHeader`, `AugmentTypeCard`×4(`WINTER/NIGHT/RAIN/RESOLUTION`), `JobCard`, `Button`, `EmptyState`, `ErrorState`, `Skeleton`, `EventTypeBadge`.
- BE 파라미터: `useVideos({page, size:20, dataSttsCd:'COMPLETED', reviewStatusCd:'APPROVED'})` — SFR-07 가드(클라이언트에서도 `v.status === 'COMPLETED'` 안전 필터).
- 클라이언트 필터(현재 페이지 한정): 영상명/CCTV/ID 부분일치 + 이벤트 enum allowlist.
- `canSubmit = videoIds.length > 0 && types.size > 0 && !isPending`. 성공 시 `/augment/result/{jobId}` 이동.

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 증강 유형 카드 | AugmentTypeCard x4 | enum | I/E | WINTER/NIGHT/RAIN/RESOLUTION |
| 영상 검색 | Input + Select | String | I/E | 영상명/CCTV/ID + 이벤트 필터 |
| 영상 테이블 | DataTable | - | I/O | 체크박스 다중 선택 |
| 최근 이력 | JobCard x6 | - | O | jobId/유형/건수/상태 |
| 증강 요청 | Button | - | I | POST `/v1/augments/request` |

**처리 내용**

영상 목록: `GET /v1/videos?dataSttsCd=COMPLETED&reviewStatusCd=APPROVED&page=&size=20`. 증강 요청: `POST /v1/augments/request { videoIds, types }`. 성공 시 → `/augment/result/{jobId}`.

**기술적 고려사항**

REVIEWER만 진입. videoIds는 number[]로 변환. types는 enum allowlist 강제.

---

### 3.19 KLID-AT-SC-019 증강 결과(AugmentResult)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-019</td><td>화면명</td><td>증강 결과(AugmentResult)</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-011</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">KLID-AT-SD-008</td></tr>
<tr><td>화면유형</td><td>조회·갱신</td><td>메뉴경로</td><td>`/augment/result/:jobId`</td></tr>
<tr><td>화면개요</td><td colspan="3">데이터 증강 작업 결과 확인. 작업 요약 카드 + 라벨 무결성 + 영상별 증강 유형 탭 + 프레임 그리드 + 좌우 비교 + 채택/거부.</td></tr>
</table>

**화면 레이아웃**

```
┌─ <section data-testid="augment-result-page"> (flex flex-col gap-4) ─────────────┐
│ ┌─ PageHeader ────────────────────────────────────────────────────────────────┐ │
│ │ 증강 결과 확인                                              [StatusBadge]    │ │
│ │ breadcrumb: 데이터 증강 > 잡 #{jobId}                                        │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─ 작업 요약 (rounded border bg-white p-4) data-testid=augment-result-summary┐ │
│ │ 작업 요약 (h2 text-section-title)                                            │ │
│ │ ┌─ dl (grid grid-cols-2 md:grid-cols-5 gap-x-6 gap-y-3) ────────────────┐  │ │
│ │ │ 작업 ID   │ 증강 유형     │ 대상 영상 │ 총 처리 이미지 │ 생성일         │  │ │
│ │ │ #{jobId}  │ 겨울 · 야간   │ 3건       │ 360장          │ -              │  │ │
│ │ └─────────────────────────────────────────────────────────────────────────┘  │ │
│ │ (status !== COMPLETED 일 때만)                                                │ │
│ │   진행률                                                          50%         │ │
│ │   <ProgressBar value={progress} tone={primary|danger} size="md" />            │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─ PROCESSING 배너 (border-blue-200 bg-blue-50) role=status (조건부) ────────┐ │
│ │ [Spinner sm]  증강 처리 중입니다...                                           │ │
│ │               잠시 후 자동으로 결과가 표시됩니다.                              │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─ FAILED 배너 (border-red-200 bg-red-50) role=alert (조건부) ───────────────┐ │
│ │ 증강 처리 실패                                              [재시도]          │ │
│ │ AI 서버 응답 오류 또는 리소스 부족으로 처리가 중단되었습니다.                  │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─ COMPLETED — 라벨 무결성 카드 (rounded border bg-white p-6, 중앙정렬) ─────┐ │
│ │                          라벨 무결성                                          │ │
│ │                  97% (text-5xl, 95%+ green / 85%+ yellow / 미만 red)          │ │
│ │                  우수 — 라벨 정합성이 매우 높습니다                            │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─ VideoSection (rounded border bg-white p-4) data-testid=video-{id} ────────┐ │
│ │ {cctvName} (h2 text-section-title)                                           │ │
│ │ ┌─ Tabs (영상별 증강 유형 — WINTER/NIGHT/RAIN/RESOLUTION) ────────────────┐ │ │
│ │ │ [겨울][야간*][비][해상도]                                                │ │ │
│ │ │ ┌─ ResultPanel (flex flex-col gap-3) ────────────────────────────────┐  │ │ │
│ │ │ │ <FrameGrid12 frames pairLabel={top:'원본', bottom:유형}             │  │ │ │
│ │ │ │              selectedSrcSn onSelect />                              │  │ │ │
│ │ │ │ <SideBySideCompare leftImage={원본} rightImage={증강}               │  │ │ │
│ │ │ │                    leftLabel="원본" rightLabel={유형} />            │  │ │ │
│ │ │ │ <DecisionCard status decidedAt rejectReason loading                 │  │ │ │
│ │ │ │               onAccept onReject(사유) />                            │  │ │ │
│ │ │ └─────────────────────────────────────────────────────────────────────┘  │ │ │
│ │ └─────────────────────────────────────────────────────────────────────────┘ │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ (groupedByVideo.size > 2 && !showAll)                                            │
│ [더 보기 ({남은개수}건)] data-testid=augment-result-show-more (text-accent 밑줄) │
└──────────────────────────────────────────────────────────────────────────────────┘
```

- VISIBLE_INITIAL=2 — 영상 그룹 2건 초과 시 "더 보기" 버튼으로 펼침.
- 잡 status 파생: results 비어있으면 PROCESSING / 무결성 0%면 FAILED / 그 외 COMPLETED.

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 작업 요약 | dl 필드 | - | O | jobId/증강유형/영상수/이미지수 |
| 라벨 무결성 | text(%) | float | O | 95%+ 녹색/85%+ 노란색/미만 빨간색 |
| 진행률 바 | ProgressBar | int | O | COMPLETED가 아닐 때만 노출 |
| 영상별 섹션 | VideoSection | - | O | 2건 초과 시 "더 보기" 버튼 |
| 증강 유형 탭 | Tabs | enum | I/E | 영상 내 증강 유형별 탭 |
| 프레임 그리드 | FrameGrid12 | - | O | 원본/증강 이미지 쌍 |
| 좌우 비교 | SideBySideCompare | - | I/O | 원본/증강 |
| 채택 | Button | - | I | POST `/v1/augments/{id}/accept` |
| 거부 | Button(+사유) | - | I | POST `/v1/augments/{id}/reject` |

**처리 내용**

`GET /v1/augments/result/{jobId}` → 영상별 그룹핑 → 섹션 렌더. 채택/거부 각각 mutation 후 성공 토스트.

**기술적 고려사항**

jobId는 number 검증. 이미지 URL은 BE 응답값만 사용. 결과 비어있으면 PROCESSING, 무결성 0이면 FAILED로 status 파생.

---

### 3.20 KLID-AT-SC-020 사용자 관리(UserManage)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-020</td><td>화면명</td><td>사용자 관리(UserManage)</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">-</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">-</td></tr>
<tr><td>화면유형</td><td>조회·갱신</td><td>메뉴경로</td><td>`/manage/users` (REVIEWER 전용)</td></tr>
<tr><td>화면개요</td><td colspan="3">시스템 사용자 목록 조회·관리. 검색, 역할·상태 필터, 활성/비활성 토글, 역할 변경.</td></tr>
</table>

**화면 레이아웃**

```
┌─ <section> (flex flex-col gap-4) ───────────────────────────────────────────────┐
│ ┌─ PageHeader ────────────────────────────────────────────────────────────────┐ │
│ │ [👥 Users icon (bg-gray-100 p-2)] 사용자 관리                                 │ │
│ │ description: 전체 {totalElements.toLocaleString()}명                          │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─ 검색·필터 바 (flex flex-wrap items-end gap-2 rounded-lg border bg-white p-3)┐│
│ │ ┌─ 검색 (flex max-w-md flex-1 items-end gap-2) ──────────────────────────┐  │ │
│ │ │ <Input label="검색" placeholder="이름 또는 이메일 검색"                  │  │ │
│ │ │        onKeyDown=Enter→handleSearch />     [검색] (Button primary)       │  │ │
│ │ └─────────────────────────────────────────────────────────────────────────┘  │ │
│ │ ┌─ 역할 ─────────────┐ ┌─ 상태 ─────────────┐ (isFilterActive 일 때만)      │ │
│ │ │ <select id=        │ │ <select id=        │ [필터 초기화]                  │ │
│ │ │   user-role-filter>│ │   user-status-     │ (Button ghost sm)              │ │
│ │ │ 전체 역할 / 검수자 │ │   filter>          │                                │ │
│ │ │ / 작업자 / 포털    │ │ 전체 / 활성 / 비활성│                                │ │
│ │ │ </select>          │ │ </select>          │                                │ │
│ │ └────────────────────┘ └────────────────────┘                                │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ {error && <ErrorState title="사용자 목록을 불러올 수 없습니다" />}                │
│                                                                                  │
│ ┌─ <DataTable<User>> (columns + rows + page/size 페이징) ─────────────────────┐ │
│ │ ┌─ 헤더 ────────────────────────────────────────────────────────────────┐  │ │
│ │ │ 이름 │ 이메일 │ 역할 │ 상태 │ 최근 로그인 │ 관리                          │  │ │
│ │ ├──────┼────────┼──────┼──────┼─────────────┼──────────────────────────────│  │ │
│ │ │ [홍]   │ hong@..│ {검수자  │ {활성 │ 2026-05-26 │ [수정][비활성화]            │  │ │
│ │ │ 홍길동 │        │ amber}  │ green}│             │ (text-red-500)              │  │ │
│ │ │ [박]   │ park@..│ {작업자  │ {활성 │ 2026-05-25 │ [수정][비활성화]            │  │ │
│ │ │ 박작업 │        │ blue}   │ green}│             │                             │  │ │
│ │ │ [이]   │ lee@.. │ {포털    │ {비활성│ 2026-05-20 │ [수정][활성화]              │  │ │
│ │ │ 이포털 │        │ gray}   │ gray} │             │ (text-green-600)            │  │ │
│ │ │  ↑ 7x7 rounded-full avatar (bg-primary-100, 첫 글자)                       │  │ │
│ │ └─────────────────────────────────────────────────────────────────────────┘  │ │
│ │ Pagination: onPageChange(p) → updateParams({page:p})                          │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─ <ConfirmDialog> (활성/비활성 토글) ───────────────────────────────────────┐ │
│ │ title: "사용자 비활성화" / "사용자 활성화"                                    │ │
│ │ description: "{name}({loginId})을(를) {활성|비활성}화하시겠습니까?"           │ │
│ │ variant: danger | primary  [취소] [비활성화|활성화]                            │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─ <Modal size="sm"> 사용자 정보 수정 ───────────────────────────────────────┐ │
│ │ title: "사용자 정보 수정"  description: "{name}({loginId})"                  │ │
│ │ ┌─ 역할 (select) ─────────────────────────────────────────────────────────┐│ │
│ │ │ 검수자 / 작업자 / 포털                                                    ││ │
│ │ └─────────────────────────────────────────────────────────────────────────┘│ │
│ │ ┌─ 상태 (select) ─────────────────────────────────────────────────────────┐│ │
│ │ │ 활성 / 비활성                                                              ││ │
│ │ └─────────────────────────────────────────────────────────────────────────┘│ │
│ │ footer: [취소] [저장]                                                         │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────────────┘
```

- 역할 컬러 배지: 검수자=amber / 작업자=blue / 포털=gray. 상태 배지: 활성=green / 비활성=gray.
- 활성/비활성화 버튼은 현재 상태에 따라 텍스트·tone 토글 (red ↔ green).

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 검색어 | Input text | String | I/E | Enter 키로도 검색 |
| 역할 필터 | Select | enum | I/E | 전체/검수자/작업자/포털 |
| 상태 필터 | Select | enum | I/E | 전체/활성/비활성 |
| DataTable | 각종 컬럼 | - | O | 이름(아바타)/이메일/역할(컬러배지)/상태/최근로그인/관리 |
| 수정 | Button(ghost) | - | I | 역할·상태 수정 Modal 오픈 |
| 활성화/비활성화 | Button(ghost) | - | I | ConfirmDialog 경유 후 PATCH |
| [수정 Modal] 역할 | Select | enum | I/E | REVIEWER/WORKER/PORTAL_USER |
| [수정 Modal] 상태 | Select | enum | I/E | 활성/비활성 |

**처리 내용**

`GET /v1/users?keyword=&page=&size=` → DataTable. PATCH `/v1/users/{userNo} { useYn, role }` → 캐시 무효화 + 성공 토스트.

**기술적 고려사항**

useYn/role은 TypeScript 리터럴 유니온 + BE @Pattern 화이트리스트 이중 검증. 역할·상태 필터는 클라이언트 사이드 추가 필터.

---

### 3.21 KLID-AT-SC-021 시스템 설정(SystemSettings)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-021</td><td>화면명</td><td>시스템 설정(SystemSettings)</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">-</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">-</td></tr>
<tr><td>화면유형</td><td>조회·갱신</td><td>메뉴경로</td><td>`/manage/settings` (REVIEWER 전용)</td></tr>
<tr><td>화면개요</td><td colspan="3">시스템 설정을 3개 영역으로 구분: 편집 가능(Batch/YOLO), 실시간 모니터링(Health), 위험 액션(placeholder). (V2.0 — FFmpeg 프레임 간격 설정 제거: 마킹 위치 기반 프레임 추출로 전환되어 전역 설정 불필요)</td></tr>
</table>

**화면 레이아웃**

```
┌─ PageHeader ───────────────────────────────────────────────────────────────────┐
│ [⚙ 아이콘] 시스템 설정                                                          │
│ description: 배치 파라미터 · 외부 연동 헬스 · 위험 구역                          │
├─ ① 편집 가능 — DB 영속화 (h2, 섹션 라벨) ────────────────────────────────────────┤
│ ┌─ grid 2열 (md:grid-cols-2, gap-4) ─────────────────────────────────────────┐ │
│ │ ┌─ BatchConfigCard ─────────┐  ┌─ YoloConfigCard ─────────────────────┐    │ │
│ │ │ 배치 처리       [Save 저장]│  │ YOLO 추론 파라미터     [Save 저장]    │    │ │
│ │ │ ─────────────────────────  │  │ ────────────────────────────────────  │    │ │
│ │ │ 처리 주기 (초)      60s   │  │ Confidence Threshold        0.40      │    │ │
│ │ │ [──────●────────]          │  │ [──────●────────]                     │    │ │
│ │ │ 10s            300s        │  │ 0.25            0.80                  │    │ │
│ │ │                            │  │ 이미지 크기 (imgsz)         1280px    │    │ │
│ │ │ 동시 처리 수          3   │  │ [ <number input 320~1920, step 32> ]  │    │ │
│ │ │ [──●──────────]            │  │ IoU 임계값                  0.50      │    │ │
│ │ │ 1               8          │  │ [──────●────────]                     │    │ │
│ │ │                            │  │ 0.25            0.80                  │    │ │
│ │ └────────────────────────────┘  └───────────────────────────────────────┘    │ │
│ └────────────────────────────────────────────────────────────────────────────┘ │
├─ ② 실시간 모니터링 (h2, 섹션 라벨) ──────────────────────────────────────────────┤
│ ┌─ HealthStatusList (Card) ─────────────────────────────────────────────────┐ │
│ │ 외부 연동 상태                       [👁 실시간 모니터링]                  │ │
│ │ ────────────────────────────────────────────────────────────────────────── │ │
│ │ [📶] DB              45ms    [정상]                                        │ │
│ │ [📶] 디스크          12ms    [정상]                                        │ │
│ │ [📶] AI 서버         60ms    [정상]                                        │ │
│ │ [📵] Gitea           timeout [연결 끊김]                                   │ │
│ │ [📶] 비식별 서버     80ms    [정상]                                        │ │
│ │ ────────────────────────────────────────────────────────────────────────── │ │
│ │ 👁 이 항목은 actuator/health에서 실시간 조회되며 편집할 수 없습니다.       │ │
│ └────────────────────────────────────────────────────────────────────────────┘ │
├─ ③ 위험 액션 (h2, 섹션 라벨) ────────────────────────────────────────────────────┤
│ ┌─ DangerActions (border-red-200) ──────────────────────────────────────────┐ │
│ │ ⚠ 위험 액션은 별도 운영 도구로 이관 예정입니다. (yellow 배너)              │ │
│ │ ⚠ 위험 구역                                                                │ │
│ │ 아래 작업은 되돌릴 수 없습니다. 신중하게 진행하세요.                       │ │
│ │ [시스템 초기화 (개발 전용, danger)] [배치 큐 초기화] [캐시 삭제]           │ │
│ └────────────────────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────────────────────┘
```

- 카드 2종: BatchConfigCard / YoloConfigCard (각 카드 우상단에 독립 [저장] 버튼, dirty 시 활성).
- FFmpeg 카드 없음 (V2.0 — 마킹 위치 기반 프레임 추출로 전환되어 전역 설정 불필요).
- 헬스: 5초 폴링 read-only. 각 컴포넌트별 latencyMs + 상태 배지.
- 위험 액션 3종: 시스템 초기화(danger 변형) / 배치 큐 초기화 / 캐시 삭제 — 모두 ConfirmDialog + toast placeholder.

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 배치 설정 | BatchConfigCard | - | I/E | 독립 저장 PATCH /v1/sysconfig/batch |
| YOLO 설정 | YoloConfigCard | - | I/E | 독립 저장 PATCH /v1/sysconfig/yolo |
| 헬스 상태 | HealthStatusList | - | O | 5초 폴링. read-only |
| 위험 액션 | DangerActions | - | I | placeholder. 확인 절차 필수 |

**처리 내용**

`GET /v1/sysconfig` → 카드 초기값 주입. 각 카드 독립 저장(PATCH). HealthStatusList는 `GET /v1/health/services` 5초 폴링.

**기술적 고려사항**

REVIEWER만 진입(RoleGuard). 각 설정 카드는 독립 저장으로 의도치 않은 일괄 덮어쓰기 방지.

---

### 3.22 KLID-AT-SC-022 라벨 프리셋(PresetList)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-022</td><td>화면명</td><td>라벨 프리셋</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-007</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">-</td></tr>
<tr><td>화면유형</td><td>입력·갱신·삭제</td><td>메뉴경로</td><td>`/manage/presets`</td></tr>
<tr><td>화면개요</td><td colspan="3">라벨 코드 프리셋 관리. 2열 카드 그리드 + 클라이언트 페이지네이션. CRUD + 이벤트 타입 매핑.</td></tr>
</table>

**화면 레이아웃**

```
┌─ <section> (flex flex-col gap-4) ───────────────────────────────────────────────┐
│ ┌─ PageHeader ────────────────────────────────────────────────────────────────┐ │
│ │ [📚 Layers icon (bg-orange-50 p-2)] 프리셋 관리        [+ 프리셋 추가]        │ │
│ │ description: 라벨 코드 프리셋 관리 — 전체 {totalElements}개                   │ │
│ │ (추가 버튼은 isReviewer 일 때만 노출 — Plus icon + Button primary)             │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ {error && <ErrorState title="프리셋 목록을 불러올 수 없습니다" />}                │
│                                                                                  │
│ (isLoading 일 때: Skeleton x4 in grid)                                           │
│ (totalElements===0 일 때: 빈 상태 카드 — Layers 40px + "등록된 프리셋이 없습니다" │
│  + [새 프리셋 만들기] 버튼)                                                       │
│                                                                                  │
│ ┌─ 카드 그리드 (grid grid-cols-1 md:grid-cols-2 gap-4) ──────────────────────┐ │
│ │ ┌─ Preset Card (rounded-lg border bg-white p-5 hover:border-primary-300)─┐ │ │
│ │ │ ┌─ 헤더 (flex items-start justify-between) ──────────────────────────┐│ │ │
│ │ │ │ 사람 기본 (h3 text-sm font-semibold)   [쓰러짐 배지 orange-50]    [수정][삭제]││ │ │
│ │ │ │ description (text-xs text-gray-400 truncate, 옵션)                  ││ │ │
│ │ │ │ (수정/삭제는 isReviewer 일 때만, Pencil/Trash2 아이콘 + Button ghost)││ │ │
│ │ │ └─────────────────────────────────────────────────────────────────────┘│ │ │
│ │ │ ┌─ Label codes (flex flex-wrap gap-1.5) ─────────────────────────────┐│ │ │
│ │ │ │ [person][face][body]                                  [3개]         ││ │ │
│ │ │ │ (각 코드: rounded-full bg-primary-50 text-primary-700)                ││ │ │
│ │ │ │ (개수: bg-gray-100 text-gray-600 tabular-nums)                        ││ │ │
│ │ │ └─────────────────────────────────────────────────────────────────────┘│ │ │
│ │ │ ┌─ Dates (border-t pt-2 text-xs text-gray-400) ──────────────────────┐│ │ │
│ │ │ │ 생성: 2026-05-20                                  수정: 2026-05-25  ││ │ │
│ │ │ └─────────────────────────────────────────────────────────────────────┘│ │ │
│ │ └─────────────────────────────────────────────────────────────────────────┘ │ │
│ │ ┌─ Preset Card: 차량 기본 [교통사고] ─ {car,truck,bus,plate} 4개 ────────┐ │ │
│ │ ...                                                                          │ │
│ │ (페이지당 10건 — PAGE_SIZE 클라이언트 페이지네이션)                            │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ <Pagination page={safePage} size={10} totalElements onPageChange />              │
│                                                                                  │
│ <PresetEditModal open onClose initial={editing} onSubmit submitting />           │
│ <ConfirmDialog open title="프리셋 삭제"                                          │
│                description='"{name}" 프리셋을 삭제하시겠습니까?'                  │
│                variant="danger" confirmLabel="삭제" />                           │
└──────────────────────────────────────────────────────────────────────────────────┘
```

- 이벤트 미매핑 프리셋은 회색 "미매핑" 배지로 표시.
- 라벨 항목 최대 6종 제한 (zod presetSchema). 409 응답 시 BE 메시지 그대로 토스트.

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 프리셋 카드 그리드 | Card Grid | - | O | 이름·이벤트타입·라벨코드·생성/수정일 |
| 프리셋 추가 | Button | - | I | 빈 프리셋 생성 |
| 이벤트 타입 | Select | enum | I/E | 1:1 매핑 검증(중복 시 409) |
| 라벨 코드 | Chip[] | string[] | I/E | 최대 6종 |
| 수정 | Button(ghost) | - | I | PresetEditModal 오픈 |
| 삭제 | Button(ghost) | - | I | ConfirmDialog 경유 |

**처리 내용**

`GET /v1/manage/presets` → 카드 그리드. `POST /v1/manage/presets` → 추가. `PUT /v1/manage/presets/{id}` → 수정. `DELETE /v1/manage/presets/{id}` → 삭제. 409 CONFLICT(이벤트 중복) 시 BE 메시지 표시.

**기술적 고려사항**

라벨 항목 최대 6종 제한(zod presetSchema). 클라이언트 페이지네이션(10건/페이지).

---

### 3.23 KLID-AT-SC-023 포털 홈(PortalHome)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-023</td><td>화면명</td><td>포털 홈</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-012</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">KLID-AT-SD-009</td></tr>
<tr><td>화면유형</td><td>조회·입력</td><td>메뉴경로</td><td>`/portal`</td></tr>
<tr><td>화면개요</td><td colspan="3">포털 메인 페이지. Hero 섹션 + KPI 2 + 2단계 카드(업로드/라벨링) + 내 업로드 목록. TUS 재개 업로드 지원.</td></tr>
</table>

**화면 레이아웃**

```
┌─ <div flex flex-col> ───────────────────────────────────────────────────────────┐
│ ┌─ Hero (bg-gradient-to-br from-orange-500 to-orange-600 px-6 py-10 text-white)┐│
│ │ <div mx-auto max-w-4xl>                                                       ││
│ │   AI 학습데이터 작성 포털 (h1 text-page-title)                                ││
│ │   영상/이미지 업로드, 간편 라벨링, 오토라벨링 체험 (text-orange-50)            ││
│ └──────────────────────────────────────────────────────────────────────────────┘│
│                                                                                  │
│ ┌─ 본문 (mx-auto max-w-4xl flex flex-col gap-6 px-4 py-6) ────────────────────┐ │
│ │ ┌─ KPI (grid-cols-1 md:grid-cols-2 gap-3) ────────────────────────────────┐│ │
│ │ │ <KpiCard label="업로드 수"    value={totalUploads}  unit="건" />        ││ │
│ │ │ <KpiCard label="라벨링 완료"  value={labeledCount}  unit="건" />        ││ │
│ │ └─────────────────────────────────────────────────────────────────────────┘│ │
│ │                                                                              │ │
│ │ <h2 uppercase tracking-wide text-gray-500> 이용 방법                          │ │
│ │ ┌─ 2단계 카드 (grid grid-cols-1 md:grid-cols-2 gap-3) ────────────────────┐│ │
│ │ │ ┌─ Step 1: 업로드 (rounded-xl border bg-white p-5 shadow-sm) ────────┐ ││ │
│ │ │ │ [①  bg-orange-100][UploadIcon orange] 1. 업로드                     │ ││ │
│ │ │ │ 영상 또는 이미지를 업로드하세요                                       │ ││ │
│ │ │ │ ┌─ <UploadDropzone onAccept onReject /> ─────────────────────────┐ │ ││ │
│ │ │ │ │ [ 파일을 드래그하거나 클릭하여 선택 ]                            │ │ ││ │
│ │ │ │ └─────────────────────────────────────────────────────────────────┘ │ ││ │
│ │ │ │ (rejectionMsg && <p role=alert text-danger>{메시지}</p>)             │ ││ │
│ │ │ │ (activeFile && status !== IDLE)                                       │ ││ │
│ │ │ │  ┌─ <UploadProgressItem> ─────────────────────────────────────────┐  │ ││ │
│ │ │ │  │ 파일명.mp4 [####------] 45%  [일시정지] [재개] [취소]            │  │ ││ │
│ │ │ │  └─────────────────────────────────────────────────────────────────┘  │ ││ │
│ │ │ └─────────────────────────────────────────────────────────────────────┘ ││ │
│ │ │ ┌─ Step 2: 라벨링 (rounded-xl border bg-white p-5 shadow-sm) ────────┐ ││ │
│ │ │ │ [②  bg-orange-100][Tag orange] 2. 라벨링                            │ ││ │
│ │ │ │ 업로드한 데이터에 라벨을 추가하세요                                   │ ││ │
│ │ │ │ 라벨링 필요 <strong text-orange-600>{pendingCount}건</strong>         │ ││ │
│ │ │ │ [▶ 시작하기 ▶] (bg-orange-500, mt-auto, full width)                  │ ││ │
│ │ │ │ (firstPendingSrcSn===null 일 때 disabled)                            │ ││ │
│ │ │ │ onClick → navigate(`/portal/label/{firstPendingSrcSn}`)              │ ││ │
│ │ │ └─────────────────────────────────────────────────────────────────────┘ ││ │
│ │ └─────────────────────────────────────────────────────────────────────────┘│ │
│ │                                                                              │ │
│ │ ┌─ 내 업로드 (flex flex-col gap-2) ──────────────────────────────────────┐│ │
│ │ │ <h2 text-section-title> 내 업로드                                        ││ │
│ │ │ {isLoading ? <Spinner label="목록 로딩" /> : <MyUploadList uploads />}   ││ │
│ │ │  ┌─ MyUploadList: 파일명 | 상태 | 업로드일 ──────────────────────────┐│ ││ │
│ │ │  │ video01.mp4 │ AUTOLABEL_DONE    │ 2026-05-26                       ││ ││ │
│ │ │  │ video02.mp4 │ COMPLETED         │ 2026-05-25                       ││ ││ │
│ │ │  │ video03.mov │ AUTOLABEL_PENDING │ 2026-05-24                       ││ ││ │
│ │ │  └─────────────────────────────────────────────────────────────────────┘│ ││ │
│ │ └─────────────────────────────────────────────────────────────────────────┘│ │
│ │                                                                              │ │
│ │ ※ 업로드한 영상은 본인만 조회/라벨링할 수 있으며, 결과 파일 제공은 포털       │ │
│ │   시스템에서 별도로 안내됩니다. (text-sub text-gray-500)                       │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────────────┘
```

- 토큰 ingress race 방어: useAuthStore.token 존재할 때만 listMyUploads 호출.
- 다운로드 UI 미제공 — V1.5 포털 자체 책임으로 이관.

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| KPI 카드 | KpiCard x2 | int | O | 업로드 수/라벨링 완료 |
| TUS 업로드 | UploadDropzone | file | I | <=5GB, mp4/mov/avi |
| 업로드 진행 | UploadProgressItem | - | O | 상태/진행률/일시정지/재개/취소 |
| 라벨링 시작 | Button | - | I | → `/portal/label/{srcSn}` |
| 내 업로드 목록 | MyUploadList | - | O | 파일명/상태/업로드일 |

**처리 내용**

`GET /v1/portal/uploads` → 내 업로드 목록. TUS: `OPTIONS/POST /v1/portal/uploads` (Tus 프로토콜). 라벨링 진입: 첫 번째 미완료 건 srcSn으로 이동.

**기술적 고려사항**

TUS(Tus.io) 청크 업로드. 1MB 단위 chunk + 재개 가능. 모바일 친화(PortalLayout). 사용자 작업 데이터는 `LS_PORTAL_USER_LABEL`에 별도 적재(원본 미수정).

---

### 3.24 KLID-AT-SC-024 포털 라벨링(PortalLabeling)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-024</td><td>화면명</td><td>포털 간편 라벨링</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-012</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">KLID-AT-SD-009</td></tr>
<tr><td>화면유형</td><td>입력·갱신</td><td>메뉴경로</td><td>`/portal/label/{id}`</td></tr>
<tr><td>화면개요</td><td colspan="3">LabelingPage 재사용(포털 모드). 히스토리/검수제출/VLM/메타 탭 미노출. 오토라벨 버튼 추가.</td></tr>
</table>

**화면 레이아웃**

```
┌─ <div flex flex-col gap-3> ─────────────────────────────────────────────────────┐
│ (Number.isFinite(numericId) 일 때만 — 오토라벨 헤더)                              │
│ ┌─ <div flex items-center justify-end> ──────────────────────────────────────┐ │
│ │                                              <AutolabelButton                │ │
│ │                                                portalVideoSn={numericId}     │ │
│ │                                                onSuccess(r) → toast          │ │
│ │                                                onError(err) → toast />       │ │
│ │  ※ imageB64 미제공으로 현재 disabled — LabelingPage 캔버스 ref 확보 후 활성  │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─ <LabelingPage key={autolabelKey} /> 재사용 ────────────────────────────────┐ │
│ │ (portalMode: useAuthStore.channel === 'PORTAL' 에서 자동 분기)                │ │
│ │                                                                                │ │
│ │ ┌─ Header (라벨링 | 포털 모드 | 프레임 1/30 | [저장 Ctrl+S] [X 닫기]) ────┐ │ │
│ │ │  포털 채널에서는 [히스토리] / [검수 제출] / VLM·메타 탭 미노출            │ │ │
│ │ └─────────────────────────────────────────────────────────────────────────┘ │ │
│ │ ┌─ 본문 (flex) ────────────────────────────────────────────────────────────┐│ │
│ │ │ ┌─ 좌측 툴바 ─┐ ┌─ 캔버스 영역 (Konva, 포털 간소화) ─┐ ┌─ 우측 패널 ──┐ ││ │
│ │ │ │ [BBox]      │ │                                      │ │ 객체 목록   │ ││ │
│ │ │ │ [Poly]      │ │     [   캔버스   ]                    │ │ ──────────  │ ││ │
│ │ │ │ [선택]      │ │                                      │ │ person-1    │ ││ │
│ │ │ │             │ │                                      │ │ car-1       │ ││ │
│ │ │ │             │ │                                      │ │ ━━━━━━━━━   │ ││ │
│ │ │ │             │ │                                      │ │ 속성        │ ││ │
│ │ │ │             │ │                                      │ │ 라벨: person│ ││ │
│ │ │ └─────────────┘ └──────────────────────────────────────┘ └─────────────┘ ││ │
│ │ └─────────────────────────────────────────────────────────────────────────────┘│ │
│ │ ┌─ Frame Strip ────────────────────────────────────────────────────────────┐ │ │
│ │ │ [1] [2] [3] ... [30]                                                       │ │ │
│ │ └─────────────────────────────────────────────────────────────────────────────┘ │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────────────┘
```

- 오토라벨 성공 시 setAutolabelKey 증가 → LabelingPage 강제 리렌더로 라벨 결과 반영.
- 모바일 친화 — PortalLayout 사용, 768px 이하 단축키 미표시, 캔버스 핀치 줌 지원.

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 본인 영상 캔버스 | LabelCanvas | - | I/O/E | 소유자 검증 통과만 |
| 오토라벨 | Button | - | I | POST `/v1/portal/autolabel` (YOLO) |
| 라벨 수락 | Button | - | I | POST `/v1/portal/labels` |
| 메뉴 단순화 | - | - | - | 검수/버전/메타 노출 X |

**처리 내용**

LabelingPage 재사용. 포털 채널은 `useAuthStore.channel='PORTAL'`에서 자동 분기. 오토라벨: POST → 결과 반영 후 key 갱신으로 리렌더.

**기술적 고려사항**

모바일 친화 — `PortalLayout` 사용, 768px 이하 단축키 미표시, 캔버스 핀치 줌 지원.

---

### 3.25 KLID-AT-SC-025 작업 배정(TaskAssign)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-025</td><td>화면명</td><td>작업 배정(TaskAssign)</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-008</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">KLID-AT-SD-005</td></tr>
<tr><td>화면유형</td><td>조회·입력·갱신</td><td>메뉴경로</td><td>`/task/assign` (REVIEWER 전용)</td></tr>
<tr><td>화면개요</td><td colspan="3">REVIEWER 전용 작업 배정 화면. 미배정 영상 목록(`status=UNASSIGNED`)을 표시하고 단건 또는 일괄로 WORKER 에게 배정한다. 체크박스로 다중 선택 → 일괄 배정 모달 / 행 단위 [배정] 버튼 → 단건 배정 모달. (V2.0 — Placeholder 제거: TaskBoard API 기반 실제 배정 UI 구현 완료)</td></tr>
</table>

**화면 레이아웃**

```
┌─ 헤더 (flex items-center justify-between) ─────────────────────────────────────┐
│ 작업 배정 (h1)                                  [N건 일괄 배정] (선택>0 시 노출)│
├─ 본문 ─────────────────────────────────────────────────────────────────────────┤
│ ┌─ <table> (rounded-lg border bg-white) ──────────────────────────────────────┐│
│ │ ┌─ <thead> (border-b bg-gray-50) ─────────────────────────────────────────┐ ││
│ │ │ [☐ 전체선택] │ 영상명 │ 이벤트 │ 프레임 수 │ 촬영일시 │ 액션              │ ││
│ │ └─────────────────────────────────────────────────────────────────────────┘ ││
│ │ ┌─ <tbody> ───────────────────────────────────────────────────────────────┐ ││
│ │ │ [☑] cctv-001     쓰러짐   120        2026-05-26   [배정 (ghost)]         │ ││
│ │ │ [☑] cctv-002     폭력     90         2026-05-26   [배정 (ghost)]         │ ││
│ │ │ [☐] cctv-003     -        60         2026-05-25   [배정 (ghost)]         │ ││
│ │ │ ...                                                                       │ ││
│ │ └───────────────────────────────────────────────────────────────────────────┘ ││
│ └────────────────────────────────────────────────────────────────────────────────┘│
├─ 페이지네이션 (totalPages>1, justify-center) ───────────────────────────────────┤
│           [이전 (outline)]   {page+1} / {totalPages}   [다음 (outline)]          │
├─ 모달 (조건부, 컴포넌트 외부에 mount) ──────────────────────────────────────────┤
│ ┌─ AssignModal mode='assign' (단건) ──────────────────────────────────────────┐ │
│ │ {videoName} 에 작업자 배정                                                   │ │
│ │ 작업자 select (GET /v1/users?role=WORKER) → [배정 제출]                       │ │
│ └──────────────────────────────────────────────────────────────────────────────┘ │
│ ┌─ AssignModal mode='bulk' (다건) ────────────────────────────────────────────┐ │
│ │ {N}건 일괄 배정 (videoIds[], videoNameById)                                   │ │
│ │ 작업자 select → 일괄 [배정 제출]                                              │ │
│ └──────────────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────────────┘
```

- 미배정 영상 0건: `bg-gray-50 dashed` empty-state 카드 ("미배정 영상이 없습니다").
- 선택 1건 이상일 때만 헤더 우측에 [N건 일괄 배정] 노출.
- 행 단위 [배정] 클릭 → 단건 모달. 헤더 [N건 일괄 배정] 클릭 → 일괄 모달.

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 미배정 영상 목록 | Table | - | O | `GET /v1/tasks/board?status=UNASSIGNED&page=N&size=20` |
| 전체 선택 | Checkbox | bool | I | 페이지 내 전체 토글 |
| 행 선택 | Checkbox | bool | I | videoId 다중 선택 — Set 관리 |
| 영상명/이벤트/프레임수/촬영일시 | Cell | - | O | cctvName / eventName / frameCount / capturedAt |
| 단건 배정 | Button(행) | - | I | AssignModal mode='assign' open |
| 일괄 배정 | Button(헤더) | - | I | 선택 1건 이상일 때 노출. AssignModal mode='bulk' open |
| 페이지네이션 | Button(이전/다음) | - | I | totalPages > 1 일 때 노출 |
| 작업자 선택 | Select(모달 내부) | userId | I | `GET /v1/users?role=WORKER` |
| 배정 제출 | Button(모달 내부) | - | I | `POST /v1/assignments` 또는 일괄 호출 |

**처리 내용**

- 진입: `GET /v1/tasks/board?status=UNASSIGNED&page=0&size=20` → 페이징 테이블 렌더
- 단건 배정: 행 [배정] 클릭 → AssignModal(mode='assign', videoId) → 작업자 선택 → `POST /v1/assignments { videoId, workerId }` → 성공 시 목록 invalidate
- 일괄 배정: 체크박스로 다중 선택 → 헤더 [N건 일괄 배정] 클릭 → AssignModal(mode='bulk', videoIds[]) → 작업자 선택 → 다건 `POST /v1/assignments` → 선택 초기화

**기술적 고려사항**

- 라우터 RoleGuard 에서 REVIEWER 만 진입 허용 (router/index.tsx)
- 작업자 select 는 AssignModal 내부에서 `@PreAuthorize` 보호 API 호출
- 입력값은 숫자 ID 만 전달 — axios URL 인코딩으로 SQL Injection / XSS 위험 없음
- 페이지 변경 시 선택 상태는 유지하지 않음(혼동 방지)

---

### 3.26 KLID-AT-SC-026 개발자 오토라벨 테스트(DevAutolabelTest)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-026</td><td>화면명</td><td>영상 업로드(DevAutolabelTest)</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">-</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">-</td></tr>
<tr><td>화면유형</td><td>입력</td><td>메뉴경로</td><td>`/dev/autolabel-test` (REVIEWER 전용, 개발/검수용)</td></tr>
<tr><td>화면개요</td><td colspan="3">영상 파일과 메타데이터를 입력하여 배치 파이프라인을 수동 실행하는 개발/검수 전용 화면. 실행 후 2초 폴링으로 파이프라인 진행 상황 가시화.</td></tr>
</table>

**화면 레이아웃**

```
┌─ <main className="space-y-6"> ──────────────────────────────────────────────────┐
│ ┌─ <PageHeader> ──────────────────────────────────────────────────────────────┐ │
│ │ 영상 업로드                                                                   │ │
│ │ description: 영상 파일과 메타데이터를 입력해 프레임 추출 + 오토라벨링         │ │
│ │              파이프라인을 실행합니다. (개발/검수 전용)                         │ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─ <Card title="파일 + 메타 입력" padding="lg"> (form noValidate space-y-5) ─┐ │
│ │ ┌─ 영상 파일 * ────────────────────────────────────────────────────────────┐│ │
│ │ │ <input type="file" accept="video/mp4,webm,quicktime,x-msvideo,.mp4..." />││ │
│ │ │ 허용 확장자: mp4 / webm / mov / avi · 최대 500MB                          ││ │
│ │ │ (file 선택 후) 선택: {file.name} ({size MB}) data-testid=selected-file    ││ │
│ │ └─────────────────────────────────────────────────────────────────────────┘│ │
│ │ ┌─ grid grid-cols-1 sm:grid-cols-2 gap-4 ────────────────────────────────┐│ │
│ │ │ <Input label="vmsClipId"  placeholder="test-xxxx" hint="영문/숫자/-/_ 1~64자" required /> ││ │
│ │ │ <Input label="cctvId"     placeholder="CCTV-001" hint="MNG_RESOURCE_CCTV.VMS_CCTV_ID" required /> ││ │
│ │ └─────────────────────────────────────────────────────────────────────────┘│ │
│ │ ┌─ grid grid-cols-1 sm:grid-cols-2 gap-4 ────────────────────────────────┐│ │
│ │ │ eventTypeCd * <select> SFR 6종 옵션 (코드+한글)                          ││ │
│ │ │ <Input label="localGovCd" placeholder="11680" inputMode="numeric" required /> ││ │
│ │ └─────────────────────────────────────────────────────────────────────────┘│ │
│ │ ┌─ <fieldset> prvcTypeCd * (role=radiogroup, rounded-lg border p-3) ─────┐│ │
│ │ │ ◉ ANONY (비식별 대상 아님) — 원본만 저장                                  ││ │
│ │ │ ○ PRVC  (개인정보 포함)    — 비식별 처리 대상                              ││ │
│ │ │ ○ PSDO  (가명 정보)        — 비식별 처리 대상                              ││ │
│ │ └─────────────────────────────────────────────────────────────────────────┘│ │
│ │ ┌─ grid grid-cols-1 sm:grid-cols-2 gap-4 ────────────────────────────────┐│ │
│ │ │ ┌─ capturedAt * ────────────┐  ┌─ durationSec (자동) ─────────────┐    ││ │
│ │ │ │ <input datetime-local />   │  │ 업로드 후 ffprobe 로 자동 추출됩니다│   ││ │
│ │ │ │ 촬영 시각 (브라우저 로컬 → │  │ (border-dashed bg-gray-50)         │    ││ │
│ │ │ │  BE 전송 시 ISO-8601 UTC)  │  │ 영상 파일에서 BE 가 자동 산출       │    ││ │
│ │ │ └────────────────────────────┘  └─────────────────────────────────────┘    ││ │
│ │ └─────────────────────────────────────────────────────────────────────────┘│ │
│ │ ┌─ <fieldset> 실행 단계 선택 (role=group, grid sm:grid-cols-2 gap-2) ────┐│ │
│ │ │ [X] 프레임 추출 — FFmpeg 로 영상에서 1초 단위 프레임 추출 (FRAME_EXTRACT) ││ │
│ │ │ [X] 비식별     — 외부 비식별 API 호출 (V2: 무조건) (DEIDENTIFY)            ││ │
│ │ │ [X] YOLO       — 객체 탐지 + 트래킹 (YOLO)                                ││ │
│ │ │ [X] SAM2       — YOLO bbox 힌트 기반 세그 (SAM2)                          ││ │
│ │ │ data-testid=autolabel-stage-toggle-{key}                                  ││ │
│ │ └─────────────────────────────────────────────────────────────────────────┘│ │
│ │ (errorMessage 있을 때) ┌─ role=alert data-testid=autolabel-error ────────┐│ │
│ │                        │ {extractBeMessage(err)} (border-danger bg-red-50)││ │
│ │                        └────────────────────────────────────────────────────┘│ │
│ │ ┌─ 액션 (flex items-center gap-2) ───────────────────────────────────────┐│ │
│ │ │ [실행 (Button primary, loading)]  [초기화 (Button secondary)]           ││ │
│ │ │ (!isValid || isPending 일 때 실행 비활성)                                ││ │
│ │ └─────────────────────────────────────────────────────────────────────────┘│ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ (result 가 있을 때만)                                                            │
│ ┌─ <Card title="실행 결과" padding="lg"> (space-y-3 text-sm) ─────────────────┐ │
│ │ ┌─ flex items-center gap-2 ─────────────────────────────────────────────┐  │ │
│ │ │ [rawSn = {N}] (bg-primary-50 font-mono) data-testid=autolabel-raw-sn     │  │ │
│ │ │ 파이프라인 상태: {COMPLETED|FAILED|PROCESSING}  [Spinner sm]             │  │ │
│ │ └─────────────────────────────────────────────────────────────────────────┘  │ │
│ │ (status==='FAILED') role=alert data-testid=autolabel-pipeline-failed         │ │
│ │   파이프라인 실행에 실패했습니다. BE 로그를 확인해주세요. (rawSn = N)         │ │
│ │ ┌─ grid sm:grid-cols-3 ──────────────────────────────────────────────────┐  │ │
│ │ │ 파일 경로     │ 프레임 수            │ 트리거 시각                       │  │ │
│ │ │ /storage/...  │ 120 (text-base bold)│ 2026-05-26 14:05                  │  │ │
│ │ └─────────────────────────────────────────────────────────────────────────┘  │ │
│ │ ┌─ Stages (ul flex flex-wrap gap-2) ─────────────────────────────────────┐  │ │
│ │ │ [FRAME · COMPLETED (100%)] [DEID · IN_PROGRESS (50%)]                    │  │ │
│ │ │ [YOLO · PENDING (0%)] [SAM2 · PENDING (0%)] (rounded-full bg-gray-100)   │  │ │
│ │ └─────────────────────────────────────────────────────────────────────────┘  │ │
│ │ <Link to="/video/{rawSn}"> 영상 상세 보기 → (text-primary-600 hover:underline)│ │
│ └─────────────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────────────┘
```

- result 수신 후 2초 폴링 (useAutolabelStatus). status 가 COMPLETED|FAILED 도달 시 polling 중단.
- 사용자 입력은 평문 console.log 금지. 에러는 BE 텍스트를 JSX 자동 이스케이프로 표시.

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 영상 파일 | input[file] | File | I | 필수. accept: mp4/webm/mov/avi. 최대 500MB |
| vmsClipId | Input text | String(1~64) | I | 필수. 영문/숫자/-/_ |
| cctvId | Input text | String | I | 필수 |
| eventTypeCd | Select | enum | I | 필수. 6종 |
| localGovCd | Input text | String(1~10) | I | 필수. 숫자만. 기본 11680 |
| prvcTypeCd | Radio | enum | I | 필수. ANONY/PRVC/PSDO |
| capturedAt | input[datetime-local] | datetime | I | 필수 |
| 실행 단계 선택 | checkbox x4 | bool | I/E | 기본 모두 ON |
| 실행 | Button(submit) | - | I | 필수 항목 미입력 시 disabled |
| [결과] rawSn | Badge | int | O | 제출 성공 후 표시 |
| [결과] 파이프라인 상태 | text + Spinner | String | O | COMPLETED/FAILED 도달 전까지 폴링 |
| [결과] 단계별 상태 | Badge 목록 | - | O | name·status·progress(%) |
| [결과] 영상 상세 보기 | Link | - | I | → `/video/{rawSn}` |

**처리 내용**

`POST /api/v1/dev/autolabel-test` (multipart: file + meta JSON) → `{ rawSn, savedFilePath, pipelineStatus, startedAt }`. 이후 `GET /v1/videos/{rawSn}` 2초 폴링 → stages 표시. COMPLETED/FAILED 도달 시 폴링 중단.

**기술적 고려사항**

file input accept로 확장자 1차 가드, BE가 본 검증. 사용자 입력 `console.log` 금지. durationSec은 BE ffprobe 자동 추출(1~7200초 검증).

---

### 3.27 KLID-AT-SC-027 마킹(Marking)

<table>
<tr><td>화면 ID</td><td>KLID-AT-SC-027</td><td>화면명</td><td>마킹(Marking)</td></tr>
<tr><td>관련 유스케이스 ID</td><td colspan="3">KLID-AT-UC-001</td></tr>
<tr><td>관련 시퀀스도 ID</td><td colspan="3">-</td></tr>
<tr><td>화면유형</td><td>입력·갱신·삭제</td><td>메뉴경로</td><td>`/marking/{rawSn}`</td></tr>
<tr><td>화면개요</td><td colspan="3">영상 재생 + 자동/수동 모드로 이벤트 시점 마킹. 수동 모드: Space로 현재 프레임 마킹. 자동 모드: 프레임 간격(프레임 단위) 기반 마킹. 영상 플레이어는 배속(0.25x/0.5x/1x/1.5x/2x/4x) 설정 지원. 마킹 결과를 VLM에 콜백으로 전달. (V2.0 — 시간(초) 단위 → 프레임 단위로 전환: 마킹 위치 기반 프레임 추출 정확도 확보)</td></tr>
</table>

**화면 레이아웃**

```
┌─ 컨테이너 (max-w-5xl mx-auto p-4 space-y-4) ────────────────────────────────────┐
│ ┌─ h1 ───────────────────────────────────────────────────────────────────────┐ │
│ │ 마킹 — 영상 #{rawSn}                                                         │ │
│ └────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─ VideoPlayer ──────────────────────────────────────────────────────────────┐ │
│ │ ┌──────────────────────────────────────────────────────────────────────┐    │ │
│ │ │           <video src="/api/v1/videos/{rawSn}/stream">                 │    │ │
│ │ │           (w-full rounded-lg bg-black)                                │    │ │
│ │ └──────────────────────────────────────────────────────────────────────┘    │ │
│ │ ┌─ 컨트롤 바 (flex items-center gap-3) ──────────────────────────────────┐  │ │
│ │ │ [재생/일시정지]  [0.25x][0.5x][1x*][1.5x][2x][4x]                       │  │ │
│ │ │ |───────●──────────────────| (range, flex-1)    00:12 / 02:30           │  │ │
│ │ │                                                  (tabular-nums)         │  │ │
│ │ └─────────────────────────────────────────────────────────────────────────┘  │ │
│ └────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─ MarkingTimeline (h-8 bg-gray-100 rounded) ────────────────────────────────┐ │
│ │ |--*----*--*------------------|  (마크 위치 표시, 클릭 선택)                  │ │
│ └────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─ MarkingToolbar (flex flex-wrap gap-3 p-3 border) ─────────────────────────┐ │
│ │ [자동][수동*]  [이벤트명 input w-40]  간격(프레임) [30] (AUTO 전용)          │ │
│ │ (MANUAL 시) "Space: 마킹 | Del: 삭제 | Enter: 완료"                          │ │
│ │                              [N건]  [초기화]  [마킹 완료]                    │ │
│ └────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─ 현재 마킹 (markCount>0 시) ────────────────────────────────────────────────┐ │
│ │ 현재 마킹 (3건)                                                              │ │
│ │ [F12 (00:12)] [F45 (00:45)] [F78 (01:18)] (chip 버튼, 선택 시 blue-600)      │ │
│ └────────────────────────────────────────────────────────────────────────────┘ │
│                                                                                  │
│ ┌─ MarkingList (저장된 마킹) ─────────────────────────────────────────────────┐ │
│ │ 저장된 마킹 (h3)                                                              │ │
│ │ ┌────────────────────────────────────────────────────────────────────────┐ │ │
│ │ │ 쓰러짐 [자동(green)] 5건 [VLM_COMPLETED]                       [삭제]    │ │ │
│ │ ├────────────────────────────────────────────────────────────────────────┤ │ │
│ │ │ 폭력   [수동(purple)] 15건 [VLM_REQUESTED]                     [삭제]    │ │ │
│ │ └────────────────────────────────────────────────────────────────────────┘ │ │
│ └────────────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────────────┘
```

- 컨트롤 바 배속 옵션: 0.25 / 0.5 / 1 / 1.5 / 2 / 4 (선택 시 bg-blue-600).
- 단축키 (MANUAL 모드 한정): Space → 현재 프레임 마크 추가, Delete/Backspace → 선택 마크 삭제, Enter → 제출.
- 모드 토글 [자동][수동] / 이벤트명 input(w-40) / AUTO 전용 간격 number input / [초기화] / [마킹 완료].
- MarkingList 항목: 이벤트명 / 모드 배지(AUTO=green, MANUAL=purple) / 마크 수 / 상태 배지(VLM_COMPLETED=blue, VLM_REQUESTED=yellow) / [삭제].

**입출력 항목**

| 항목명 | 컨트롤명 | 타입 및 길이 | 속성 | Validation Check |
|---|---|---|---|---|
| 영상 플레이어 | VideoPlayer | - | O | /api/v1/videos/{rawSn}/stream |
| 배속 선택 | SpeedButton | float | I | 0.25/0.5/1/1.5/2/4. 기본 1x |
| 마킹 타임라인 | MarkingTimeline | - | O | 마크 위치 시각화 |
| 모드 선택 | Radio | enum | I | MANUAL/AUTO |
| 이벤트명 | Input text | String | I | 필수. XSS sanitize |
| 간격(프레임) | Input number | int(1~3600) | I/E | AUTO 모드 전용. 기본 30프레임 |
| 마크 추가 | 키보드(Space) | - | I | MANUAL 모드에서 현재 프레임 추가 |
| 마크 삭제 | 키보드(Delete) | - | I | 선택된 마크 삭제 |
| 제출 | Button / Enter | - | I | POST /v1/markings |
| 현재 마킹 | Chip 목록 | - | O | frameIndex + timestamp 표시 |
| 저장된 마킹 목록 | MarkingList | - | O | SN/이벤트명/모드/마크수/삭제 |

**처리 내용**

- MANUAL: 사용자가 Space 키로 현재 프레임 마크 → 제출 시 `POST /v1/markings { eventName, markingMode: 'MANUAL', marks: [...] }`
- AUTO: 프레임 간격 기반 → 제출 시 `POST /v1/markings { eventName, markingMode: 'AUTO', intervalFrames }`
- 배속 조절: 영상 재생 중 0.25x~4x 버튼으로 `video.playbackRate` 변경 — 빠른 영상 정밀 마킹 / 느린 영상 빠른 스캔 지원
- 저장된 마킹 삭제: `DELETE /v1/markings/{sn}`
- 마킹 완료 후 배치 파이프라인에서 VLM 콜백으로 시계열 결과 수신

**기술적 고려사항**

단축키: Space(마크 추가), Delete(마크 삭제), Enter(제출). input/textarea/select 포커스 시 단축키 비활성. rawSn number 검증.

---

## 부록 — 화면-API 매핑 요약

| 화면 ID | 주요 호출 API |
|---|---|
| KLID-AT-SC-001 | (브라우저 스토리지 읽기 — JWT 인계) |
| KLID-AT-SC-002 | POST /v1/auth/role-claim |
| KLID-AT-SC-003 | GET /v1/stats/summary, GET /v1/videos, GET /v1/tasks/board |
| KLID-AT-SC-004 | GET /v1/videos |
| KLID-AT-SC-005 | GET /v1/batch/status |
| KLID-AT-SC-006 | GET /v1/videos/{id}, GET /v1/videos/{id}/labels/auto |
| KLID-AT-SC-007 | GET /v1/tasks/board, POST /v1/assignments, PATCH /v1/assignments/{id} |
| KLID-AT-SC-008 | GET·PUT /v1/frames/{srcSn}/labels, POST /v1/frames/{srcSn}/sam2-track |
| KLID-AT-SC-009 | GET /v1/videos/{videoId}/auto-summary |
| KLID-AT-SC-010 | GET·PUT /v1/frames/{srcSn}/meta |
| KLID-AT-SC-011 | GET /v1/deident, POST /v1/deident/{videoId}/reprocess |
| KLID-AT-SC-012 | GET /v1/deident/{videoId}/detail |
| KLID-AT-SC-013 | GET /v1/reviews |
| KLID-AT-SC-014 | GET /v1/reviews/{id}, POST approve/reject/start |
| KLID-AT-SC-015 | GET /v1/videos/{videoId}/versions, GET /v1/versions/{commit}/diff |
| KLID-AT-SC-016 | GET /v1/stats/workers/{workerId}, GET /v1/users?role=WORKER |
| KLID-AT-SC-017 | GET /v1/stats/overall, GET /v1/stats/report?type=MONTH |
| KLID-AT-SC-018 | GET /v1/videos, POST /v1/augments/request |
| KLID-AT-SC-019 | GET /v1/augments/result/{jobId}, POST /v1/augments/{id}/accept\|reject |
| KLID-AT-SC-020 | GET /v1/users, PATCH /v1/users/{userNo} |
| KLID-AT-SC-021 | GET /v1/sysconfig, PATCH /v1/sysconfig/*, GET /v1/health/services |
| KLID-AT-SC-022 | GET·POST·PUT·DELETE /v1/manage/presets |
| KLID-AT-SC-023 | GET /v1/portal/uploads, OPTIONS·POST /v1/portal/uploads (TUS) |
| KLID-AT-SC-024 | POST /v1/portal/autolabel, POST /v1/portal/labels |
| KLID-AT-SC-025 | (KLID-AT-SC-007 REVIEWER 시각과 동일) |
| KLID-AT-SC-026 | POST /api/v1/dev/autolabel-test, GET /v1/videos/{rawSn} |
| KLID-AT-SC-027 | GET /v1/markings, POST /v1/markings, DELETE /v1/markings/{sn} |
