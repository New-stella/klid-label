# D11-R1 단위시험 절차서 (기능 정상동작 한정 · 컴포넌트 기준 · 상세)

## 작성 목적
> 본 절차서는 `D11-R1-단위시험절차서.md`(전체)에서 **예외·경계·권한·검증 케이스를 제외하고 각 기능의 정상 동작 케이스만** 추린 기능 검증용 변형본이다. 혼합 케이스는 정상 절만 추출했다(원문 verbatim 아님). 활성 R1 기능 유스케이스 18종은 모두 정상 동작 케이스를 1건 이상 보유한다. 본 판본은 단위시험을 **컴포넌트(CO) 단위 블록**으로 재편하고, 케이스 ID를 `KLID-AT-UT-{컴포넌트번호}-NN`로 부여한 컴포넌트 기준본이다. 본 상세본은 시험항목 및 처리절차·예상결과 및 검증방법을 실제 코드·DB 설계(D8·D9) 기준으로 다단계 절차/테이블·컬럼 검증 수준까지 상세화한 판본이다.

### 제·개정 이력

| 날짜 | 버전 | 제·개정 내역 | 작성자 | 검토자 |
|------|------|------------|--------|--------|
| 2026-06-30 | 1.0 | 전체 단위시험 절차서에서 기능 정상동작만 추출 | - | - |
| 2026-07-01 | 1.1 | CBD 표준 단위시험 케이스 구조로 블록 머리표(단위시험ID·설명·관련 컴포넌트/프로그램 ID) 추가, 케이스표 헤더 표준화 | - | - |
| 2026-07-01 | 2.0 | 단위시험을 컴포넌트(CO) 단위로 재편, 단위시험ID=KLID-AT-UT-{컴포넌트번호}, 케이스ID를 블록별 01부터 순번 재부여(원본 D11 TC 번호 제거), 다중 CO 블록 분리 | - | - |
| 2026-07-01 | 3.0 | 시험항목 및 처리절차·예상결과 및 검증방법을 코드·D8/D9 기반 다단계 절차·테이블/컬럼/상태 검증 수준으로 상세화 | - | - |

### 헤더

| D11-R1 | 단위시험 절차서 (R1 한정) |
|-------|---------|
| 시스템명 | AI 기반 지방정부 CCTV 관제지원시스템 구축(2차) | 서브시스템명 | 학습데이터 저작도구 (AT) |
| 단계명 | 설계 | 작성일자 | 2026-06-30 | 버전 | 1.0 |

> 각 블록은 컴포넌트(CO) 1:1 단위시험으로, [단위시험ID(=관련 컴포넌트 번호) · 설명 · 관련 컴포넌트 ID · 관련 프로그램 ID] 머리표로 시작한다(설명 칸에 R1 요구사항 ID·UC 보존, 관련 프로그램 ID 칸에 클래스명). 케이스 ID는 `KLID-AT-UT-{컴포넌트번호}-NN`(블록마다 01부터 순번). 케이스표 컬럼: 케이스 ID · 케이스 명 · 작업 권한 · 시험 데이터 · 시험항목 및 처리절차 · 예상결과 및 검증방법 · 시험 결과(공란).

---

## 단위시험 KLID-AT-UT-001

| 단위시험ID | KLID-AT-UT-001 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-07-01 / RQ-SFR-11-05 · UC KLID-AT-UC-001] 생성형 AI 학습데이터 자동생성(증강 요청) | | |
| 관련 컴포넌트 ID | KLID-AT-CO-001 | 관련 프로그램 ID | AugmentRequestController, AugmentRequestService |
> 출처: D11 UT-03(외부 증강). 증강 요청·결과 묶음 조회 케이스. RQ-SFR-11-05는 07-01과 동일 기능이라 통합.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-001-01 | 증강요청 WINTER 단일 정상 200 | REVIEWER | 검수완료 영상·WINTER | 1) REVIEWER 토큰으로 `POST /v1/augments/request` 에 `videoIds=[검수완료 RAW_SN 1건]`·`types=["WINTER"]`(각 @Size(max=1)) 전송 <br>2) AugmentRequestService.request → LS_RAW_DATA_STATUS.DATA_STTS_CD='APPROVED' 검증 통과(미검수면 400 NOT_REVIEWED+blockedVideoIds) <br>3) 영상 대표 프레임(MIN SRC_SN) 조회 후 idempotencyKey(AUG-UUID·≤64)·externalJobId(JOB-UUID·≤128) 발급 <br>4) LsDataAug.createRequested 로 PENDING 행 단일 INSERT → AFTER_COMMIT 이벤트로 멱등키 등록·외부 콜백 위임 | · LS_DATA_AUG에 AUG_TYPE_CD='WINTER'·AUG_PROC_STTS_CD='PENDING'·IDMP_KEY·OTSD_JOB_ID INSERT <br>· AugmentRequestResponse(jobId·requestedAt·videoCount=1·typeCount=1) 200 반환 <br>· 검증: HTTP 200·응답 jobId 존재·DB LS_DATA_AUG 신규 row 조회 | |

## 단위시험 KLID-AT-UT-002

| 단위시험ID | KLID-AT-UT-002 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-07-02 · UC KLID-AT-UC-002] 생성 라벨 무결성 유지·증강 결과 수신 | | |
| 관련 컴포넌트 ID | KLID-AT-CO-002 | 관련 프로그램 ID | LabelIntegrityCalculator |
> 출처: D11 UT-03(외부 증강) 중 라벨 보존율 계산 케이스. + UT-20(외부 콜백 웹훅, UC-002 증강 결과 수신·등록) 새영상 생성·라벨 복사 케이스 추가(UC-002 수신·등록 직접 검증).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-002-01 | LabelIntegrity 완전 동일 라벨 보존율 100 | - | 원본·증강 라벨쌍 | 1) 원본 라벨 리스트와 좌표·label 이 완전 동일한 증강 라벨 리스트를 LabelIntegrityCalculator.calculate(original, augmented) 에 전달 <br>2) 좌표 보존(원본별 IoU>0.9 매칭 비율, 가중 0.7)·카테고리 보존(label 텍스트 동일 비율, 가중 0.3) 산출 후 100 스케일 | · 전 원본 라벨이 IoU>0.9·label 일치 → coordPreserved=1.0·categoryPreserved=1.0 <br>· (1.0×0.7 + 1.0×0.3)×100 = 100.00 (소수 2자리 HALF_UP 반올림) <br>· 검증: 반환 BigDecimal == 100.00 | |
| KLID-AT-UT-002-02 | 증강 SUCCESS 시 새 RAW_SN 생성 ORGNL_RAW_SN 참조 + 원본 프레임 라벨·메타 복사 | - | 증강 SUCCESS | 1) PENDING LsDataAug 행에 대해 `POST /v1/augments/result`(HMAC 웹훅) 로 `status="SUCCESS"`·`originAugSn`·`augType`(WINTER/NIGHT/RAIN)·`idempotencyKey`·`resultFilePath` 전송 <br>2) AugmentResultService.handle → 멱등키 allowlist·중복 검증 통과 → AUG_PROC_STTS_CD='ACCEPTED' 전이 <br>3) createAugmentedVideo → LsDataRaw.createFromAugment 로 신규 영상 INSERT | · LS_DATA_RAW에 새 RAW_SN INSERT(ORGNL_RAW_SN=원본 RAW_SN·DATA_STTS_CD='PENDING'·DE_IDNTF_YN='N') <br>· 원본 프레임(LS_DATA_SRC)·라벨(LS_DATA_LBL)·메타(LS_DATA_META) 를 새 RAW_SN 하위로 saveAll 복사 <br>· 검증: DB LS_DATA_RAW.ORGNL_RAW_SN 조회·프레임/라벨/메타 복사 건수 원본과 일치 | |
| KLID-AT-UT-002-03 | 증강 성공시 원본 라벨/메타가 새 프레임에 정확히 복사·복수 프레임 매핑 | - | 원본 라벨·메타 | 1) 복수 프레임·라벨을 가진 원본 영상에 대해 SUCCESS 콜백 수신 <br>2) createAugmentedVideo 가 원본 프레임별 신규 SRC_SN 매핑(srcSnMap) 생성 → LsDataLbl.copyForNewSrc 로 신규 SRC_SN 에 라벨 복사 <br>3) 원본↔복사 라벨을 zip 하여 LS_DATA_AUG_LBL_MAP 기록 | · 각 원본 SRC_SN → 신규 SRC_SN 매핑 정확·라벨이 대응 프레임에 복사 <br>· LS_DATA_AUG_LBL_MAP에 ORGNL_DATA_LBL_SN·DATA_LBL_SN·COORD_RECALC_YN='N'(해상도 동일, SCALE_X/SCALE_Y=null) INSERT <br>· 검증: DB 매핑 row 수·프레임별 라벨 좌표 원본 일치 | |

## 단위시험 KLID-AT-UT-003

| 단위시험ID | KLID-AT-UT-003 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-06-03 · UC KLID-AT-UC-003] 이미지 확대·축소·해상도 변경 | | |
| 관련 컴포넌트 ID | KLID-AT-CO-003 | 관련 프로그램 ID | VideoResolutionService, VideoResolutionController, Java2DImageResizer |
> 출처: D11 UT-22(영상·스트리밍·해상도). 해상도 변경(다운스케일 이미지셋, SFR-06-03) 케이스만 추출(영상 목록·스트리밍·서명 URL은 인프라/공통으로 제외).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-003-01 | 다운스케일 성공시 이미지셋 생성·EXPORT 1행 기록 | REVIEWER | 해상도 preset | 1) `VideoResolutionService.changeResolution(rawSn, {preset:RES_720P}, regId)` 호출 <br>2) `loadAndValidate` — APPROVED(LS_RAW_DATA_STATUS) + `ORGNL_RAW_SN` null(비-증강본) 검증 <br>3) `existsByDataRawSnAndTargetResCd` 중복 사전 체크·`countByRawSn>0` 프레임 확인 <br>4) 첫 프레임 `readDimensions` 실측 → 종횡비 보존 targetH 기준 targetW 짝수 산정, `targetH<srcH` 업스케일 거부 <br>5) FRAME_CHUNK_SIZE(500) 청크 순회로 `imageResizer.resize`(트랜잭션 밖·Semaphore 상한) <br>6) 완료 후 `VideoResolutionPersister.persist`(REQUIRES_NEW)로 EXPORT INSERT | · LS_RESOLUTION_EXPORT 1행 INSERT(DATA_RAW_SN·TARGET_RES_CD·ORGNL_W/H·TARGET_W/H·FRAME_CNT·OUTPUT_DIR_PATH·REG_ID) <br>· 다운스케일 이미지셋 `{base}/resolution/{rawSn}/{TARGET_RES_CD}/` 하위 생성 <br>· 신규 LS_DATA_RAW 미생성·라벨 좌표 미복사 <br>· 검증: `exportRepository` 조회 1건 + UK(DATA_RAW_SN,TARGET_RES_CD) 유일 | |
| KLID-AT-UT-003-02 | 정상 해상도 변경 요청시 201 | REVIEWER | preset | 1) `POST /v1/videos/{rawSn}/resolution`(@PreAuthorize REVIEWER)·body `{preset:RES_720P}` <br>2) `@ResponseStatus(CREATED)` 로 성공 응답 반환 <br>3) 응답 매핑 `ApiResponse.ok(ResolutionChangeResponse)` | · HTTP 201 Created <br>· data = `{exportSn, srcW, srcH, targetW, targetH, frameCount}` <br>· OUTPUT_DIR_PATH 등 내부 파일 경로 응답 미포함(CWE-209) <br>· 검증: 응답 상태코드·바디 필드 존재/경로 부재 | |

## 단위시험 KLID-AT-UT-004

| 단위시험ID | KLID-AT-UT-004 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-08-01 · UC KLID-AT-UC-004] 라벨링 정확도(위치·경계) 향상 | | |
| 관련 컴포넌트 ID | KLID-AT-CO-004 | 관련 프로그램 ID | TrackInterpolator·TrackInterpolationStep(UT-11) / YoloAutolabelStep(UT-09) |
> 출처: D11 UT-11(트랙 보간, 직접 UC-004) + UT-09(오토라벨링 YOLO, 간접 UC-004 — SFR-08 라벨링 핵심이므로 포함).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-004-01 | 두 키프레임 5~10 사이 6 7 8 9 프레임 선형 보간 / 세 키프레임 각 구간 독립 보간 | - | 키프레임 2~3개 | 1) 키프레임 목록 구성(frame 5·10 Bbox, `outside=false`) <br>2) `TrackInterpolator.interpolate(keyframes, totalFrames)` 호출 <br>3) 각 사이 프레임 f 에 대해 `t=(f-5)/span`, `Bbox.linearInterpolate(k, next, t)` 계산 <br>4) 세 키프레임 입력 시 각 구간(5~10, 10~N)을 독립 보간 | · 반환 Map(frame→Bbox)에 6/7/8/9 선형 보간값 산출 <br>· 키프레임 자체(5·10) 포함, `outside=true` 이후·마지막 키프레임 이후 propagate 없음 <br>· 검증: 순수 도메인 로직 단위테스트 — 반환 Map 좌표 계산 일치 | |
| KLID-AT-UT-004-02 | 같은 trackId 5~10 프레임 BBOX 2개 있으면 6 7 8 9 프레임 보간 4건 저장 | - | trackId 키프레임 | 1) `TrackInterpolationStep.run(rawSn)`(REQUIRES_NEW) <br>2) `findByRawSnOrderByFrameNoAsc` frame↔srcSn 매핑 캐싱 <br>3) `findAutoBboxWithTrackId`(자동+BBOX+TRACK_ID NOT NULL) 조회 → trackId 그룹 정렬 → Keyframe 변환 <br>4) `INTERPOLATOR.interpolate` 후 키프레임 skip, 사이 프레임만 `LsDataLbl.createAutoInterpolatedBbox` `saveAll` <br>5) 보간 row별 `LsDataLblAiInfo.create(SRC_INTERPOLATE)` `saveAll` | · LS_DATA_LBL 4행 INSERT(LBL_TYPE_CD=BBOX·AUTO_LBL_YN=Y·동일 TRACK_ID·CONF_SCORE 0·POINT_CN nested) <br>· LS_DATA_LBL_AI_INFO 4행(LBL_SRC_CD=INTERPOLATE·DATA_RAW_SN·DATA_SRC_SN) <br>· 검증: DB 조회 4건 + frameNo 6/7/8/9 매핑 | |
| KLID-AT-UT-004-03 | YOLO 검출 결과는 AUTO_LBL_YN Y + BBOX 타입 + score 0~1 저장 | - | 검출 응답 | 1) `YoloAutolabelStep.run(rawSn)`(REQUIRES_NEW) <br>2) 프레임별 `AiServerClient.predictYoloTrack(imageB64, clipId=rawSn, frameIndex, conf, imgsz, iou)` 호출 <br>3) 검출별 좌표 nested `[[x1,y1],[x2,y2]]` 정규화·`score.setScale(4)` <br>4) `LsDataLbl.createAutoBbox`(BBOX 토글 시) 저장 + `LsDataLblAiInfo.create(SRC_YOLO)` 저장 | · LS_DATA_LBL INSERT: AUTO_LBL_YN=Y·LBL_TYPE_CD=BBOX·CONF_SCORE 0.0~1.0 <br>· LS_DATA_LBL_AI_INFO INSERT: LBL_SRC_CD=YOLO·CONF_SCORE <br>· 검증: DB 조회 — 저장 행 필드 값 일치 | |

## 단위시험 KLID-AT-UT-005

| 단위시험ID | KLID-AT-UT-005 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-08-02 · UC KLID-AT-UC-005] 객체 외곽경계 자동밀착 | | |
| 관련 컴포넌트 ID | KLID-AT-CO-005 | 관련 프로그램 ID | Sam2SegmentStep, BbHint |
> 출처: D11 UT-10(배치 단계 — SAM2 분할).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-005-01 | DB BBOX 있으면 SAM2 호출 후 POLYGON 저장 | - | BBOX 있음 | 1) `Sam2SegmentStep.run(rawSn, upstreamHints)`(REQUIRES_NEW) <br>2) 프레임별 `findBySrcSnAndAutoLblYn(AUTO_YES)` + `TYPE_BBOX` 필터 → `(srcSn,label,trackId)` dedup `SegmentJob` 생성 <br>3) `parseBbox(POINT_CN)` 평탄화 → `AiServerClient.segment(Sam2Request(imageB64, null, box))` 호출 <br>4) `resp.polygon` → `capPolygon`(MAX_POINTS cap) → `LsDataLbl.createAutoPolygon` 저장 | · `AiServerClient.segment` 호출 발생 <br>· LS_DATA_LBL INSERT: LBL_TYPE_CD=POLYGON·AUTO_LBL_YN=Y·POINT_CN=폴리곤 JSON·CONF_SCORE(setScale 4) <br>· 검증: DB 조회 — POLYGON row 저장 확인 | |
| KLID-AT-UT-005-02 | POLYGON 저장 시 LsDataLblAiInfo SRC SAM2도 동시 저장 | - | 저장 결과 | 1) POLYGON 저장(`createAutoPolygon`) 직후 동일 트랜잭션에서 <br>2) `LsDataLblAiInfo.create(savedLabel.lblSn, rawSn, srcSn, SRC_SAM2, score, "batch")` 저장 | · LS_DATA_LBL_AI_INFO 1행 INSERT: LBL_SRC_CD=SAM2·DATA_LBL_SN=저장 라벨 PK·DATA_RAW_SN·DATA_SRC_SN·CONF_SCORE <br>· POLYGON 라벨 1건당 AiInfo 1건 매칭 <br>· 검증: `aiInfoRepository` 조회 — LBL_SRC_CD=SAM2 행 존재 | |

## 단위시험 KLID-AT-UT-006

| 단위시험ID | KLID-AT-UT-006 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-08-03 · UC KLID-AT-UC-006] 라벨링 정밀도 조절 | | |
| 관련 컴포넌트 ID | KLID-AT-CO-006 | 관련 프로그램 ID | SystemConfigController, ConfigKeys, PolygonSimplifyConfig |
> 출처: D11 UT-26(시스템 설정) 중 POLYGON_SIMPLIFY_TOLERANCE(정밀도 설정값) 케이스만 추출(YOLO 파라미터·배치 주기 설정은 인프라/공통으로 제외).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-006-01 | POLYGON_SIMPLIFY_TOLERANCE 기본값 1.0·REVIEWER 2.5 저장 반영 | REVIEWER | 허용오차 | 1) `PUT /v1/manage/configs/POLYGON_SIMPLIFY_TOLERANCE`(@PreAuthorize REVIEWER)·body `{value:"2.5"}` <br>2) `SystemConfigService.update` — `verifyReviewer` + `ConfigKeys.ALLOWED` 포함 검증 <br>3) CONFIG_TYPE_CD=DECIMAL → `validateDecimalRange`(0.0~50.0) 통과 <br>4) `cfg.updateValue("2.5", sub)` + `@CacheEvict(sysconfig)` 전체 무효화 | · LS_SYSTEM_CONFIG.CONFIG_VL '1.0'(기본)→'2.5' UPDATE <br>· 캐시 무효화 → 다음 `getDouble` 호출에서 2.5 반영 <br>· 검증: 응답 ConfigResponse.value='2.5' + DB 조회 | |
| KLID-AT-UT-006-02 | ConfigKeys에 YOLO CONF IMGSZ IOU·POLYGON_SIMPLIFY_TOLERANCE 화이트리스트 포함·NUMBER/DECIMAL RANGE 유효·기존 Batch 키 유지 | - | 설정 키 | 1) `ConfigKeys.ALLOWED` 화이트리스트 상수 검증 <br>2) `NUMBER_RANGE`(정수 키별 [min,max]) 검증 <br>3) `DECIMAL_RANGE`(소수 키별 [min,max]) 검증 | · ALLOWED 포함: YOLO_CONF_THRESHOLD·YOLO_IMGSZ·YOLO_IOU·POLYGON_SIMPLIFY_TOLERANCE + 기존 BATCH_INTERVAL_SEC·BATCH_CONCURRENCY 유지 <br>· NUMBER_RANGE: YOLO_CONF_THRESHOLD 25~80·YOLO_IMGSZ 320~1920·YOLO_IOU 30~80 <br>· DECIMAL_RANGE: POLYGON_SIMPLIFY_TOLERANCE 0.0~50.0 <br>· 검증: 상수 단위테스트 — 집합·범위 정확 | |

## 단위시험 KLID-AT-UT-007

| 단위시험ID | KLID-AT-UT-007 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-08-04 · UC KLID-AT-UC-007·009] 버전관리·변경이력 추적 | | |
| 관련 컴포넌트 ID | KLID-AT-CO-007 | 관련 프로그램 ID | VersionService, LsDataLblHstryRepository |
> 출처: D11 UT-18(버전관리) 중 검수 승인 스냅샷·이력 케이스. + UT-19(관제 통지, UC-009 검수 완료·수정 통지) 통지 케이스 추가. UC-009는 08-04·08-05 공통이며 KLID-AT-UT-008 블록도 동일 UC-009를 실현한다(케이스는 본 블록에 집약).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-007-01 | 검수 승인시 영상 프레임의 현재 라벨로 DB 스냅샷 APPROVED 버전 생성 / 다중 프레임 각 스냅샷 | REVIEWER | 라벨 보유 영상 | 1) 검수 승인 트랜잭션 내부에서 `commitApproved(rawSn, actor)` 호출 <br>2) 영상 프레임을 `findByRawSnOrderByFrameNoAsc` 로 조회하고, 프레임 라벨을 `findBySrcSnIn` 단일 IN 쿼리로 일괄 조회(N+1 회피) 후 srcSn 단위 분류 <br>3) 프레임별 현재 라벨을 `LabelResponse` 스냅샷(JSON)으로 직렬화 → SHA-256 `versionHash` 산출 <br>4) `findActiveForUpdate` 로 ACTIVE 행 비관적 잠금 획득 후, 기존 active 와 동일 `versionHash` 면 멱등 스킵·아니면 `saveActiveVersion(SAVE_REASON_APPROVED)` <br>5) 다중 프레임이면 각 프레임을 순회하며 개별 스냅샷 생성 | · LS_LABEL_VERSION 신규 행(프레임당 1): SAVE_REASON_CD='APPROVED', ACTVTN_YN='Y', VERSION_HASH=payload SHA-256, LBL_PAYLOAD=라벨 전체 JSON, VERSION_NO 증가 <br>· 동일 프레임 기존 active 는 ACTVTN_YN='N' 으로 deactivate <br>· 라벨 0건 프레임은 스냅샷 미생성(스킵), 반환값 `CommitResult(created, skipped)` <br>· 검증: LS_LABEL_VERSION DB 조회(프레임 수 = 생성 버전 수, SAVE_REASON_CD/ACTVTN_YN/VERSION_HASH 확인) | |
| KLID-AT-UT-007-02 | LsDataLblHstry 삭제 이력 저장·PK 채번 / 프레임 단위 최신순 조회 | - | 이력 데이터 | 1) `LsDataLblHstry.recordDeletion(lblSn, srcSn)` 로 삭제 이력 엔티티 생성(REGISTERED_AT=now) <br>2) `LsDataLblHstryRepository.save(...)` 로 적재(LBL_HSTRY_SN IDENTITY 채번) <br>3) 같은 srcSn 에 다건 적재 후 `findBySrcSnOrderByRegisteredAtDesc(srcSn)` 로 조회 | · LS_DATA_LBL_HSTRY 행: LBL_HSTRY_SN(IDENTITY 자동 채번), LBL_SN·SRC_SN·REGISTERED_AT 기록(PII·사유 미저장) <br>· 조회 결과가 REGISTERED_AT DESC(최신순) 정렬 <br>· 검증: 저장 후 PK not-null + 조회 순서 DB 확인 | |
| KLID-AT-UT-007-03 | TASK_COMPLETED/TASK_MODIFIED 통지 정상 송신시 200 | - | 통지 페이로드 | 1) `ControlNotifyService.sendCompleted` → `ControlNotifyClient.sendTaskCompleted(payload).block(BLOCK_TIMEOUT)` <br>2) `sendModified` → `client.sendTaskModified(payload).block(BLOCK_TIMEOUT)` <br>3) 관제 inbound SPI(mock)가 200 응답 | · 예외 없이 정상 반환, `metrics.incrementCompletedSuccess()`/`incrementModifiedSuccess()` 증가 <br>· 실패 시에만 `fallbackService.enqueuePending(requestId, eventType, rawSn, payload)` 로 폴백 큐 적재(정상 200 이면 미적재) <br>· 검증: 응답 200 + 폴백 큐 미적재 확인 | |
| KLID-AT-UT-007-04 | TaskCompletedPayload/TaskModifiedPayload record 정상 생성 | - | 페이로드 | 1) `new TaskCompletedPayload("TASK_COMPLETED", rawSn, reviewerName, approvedAt, totalFrames, labeledFrames, requestId)` 생성 <br>2) `new TaskModifiedPayload("TASK_MODIFIED", rawSn, lastModifiedAt, frameIds, changeTypes, requestId)` 생성 <br>3) 각 record 필드 접근·구조 검증 | · eventType 고정값("TASK_COMPLETED"/"TASK_MODIFIED"), requestId=UUID(idempotency) <br>· TaskModifiedPayload.changeTypes ⊆ {LABEL_ADDED, LABEL_UPDATED, LABEL_DELETED, META_UPDATED}(ChangeType.ALL), frameIds=SRC_SN 목록 <br>· PII·토큰·라벨/메타 본문·원본 이미지 경로 미포함(CWE-359) <br>· 검증: record 필드 값 assert | |
| KLID-AT-UT-007-05 | ReviewApprovedEvent 수신시 sendCompleted 호출 / TaskModifiedEvent 수신시 debouncer accumulate 호출 | - | 이벤트 | 1) `ControlNotifyEventListener.onReviewApproved`(@TransactionalEventListener AFTER_COMMIT) → `notifyService.sendCompleted(event)` <br>2) `onTaskModified`(AFTER_COMMIT) → `debouncer.accumulate(event)` → `windows.compute(rawSn)` 윈도우에 `addFrame(srcSn, changeType)` 축적 <br>3) `flushExpiredWindows`(10초 fixedDelay)가 60초 만료 윈도우 스캔 → `sendModified(rawSn, frameIds, changeTypes)` | · ReviewApprovedEvent 수신 시 sendCompleted 1회 호출 <br>· TaskModifiedEvent 수신 시 rawSn 윈도우에 frameIds/changeTypes 누적(중복 srcSn 합쳐짐), 윈도우 만료 후 sendModified 1회 flush <br>· 롤백 트랜잭션에서는 AFTER_COMMIT 미호출 <br>· 검증: mock notifyService/debouncer 호출 횟수·인자 검증 | |

## 단위시험 KLID-AT-UT-008

| 단위시험ID | KLID-AT-UT-008 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-08-05 · UC KLID-AT-UC-008·009] 버전 비교·복구 | | |
| 관련 컴포넌트 ID | KLID-AT-CO-008 | 관련 프로그램 ID | VersionController, VersionService |
> 출처: D11 UT-18(버전관리) 중 diff/롤백 케이스. (UC-009 검수 완료·수정 통지 케이스는 KLID-AT-UT-007 블록에 집약 — 중복 회피.)

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-008-01 | diff 두 스냅샷 labels JSON 파싱하여 라벨 단위 ADDED REMOVED MODIFIED 반환 / shape 변경 before·after | WORKER | 스냅샷쌍 | 1) `GET /v1/versions/{version}/diff?compareWith={fromHash}` → `VersionService.diff(fromHash, toHash, actor)` <br>2) `validateHash`(hex, 64자 이하)로 두 해시 형식 검증 → `findByHashOrThrow` 로 각 버전 조회 → `accessGuard.verifyAccess(dataSrcSn)`(IDOR 방어) <br>3) 두 버전이 서로 다른 srcSn 이면 빈 결과 반환 <br>4) 두 LBL_PAYLOAD 를 `computeLabelDiffs` 로 파싱, items[].id(=LS_DATA_LBL.LBL_SN) 기준 맵 비교 | · from 에만 존재=REMOVED(before만), to 에만 존재=ADDED(after만), 양쪽 존재+좌표/lblTypeCd/label 차이=MODIFIED(before+after) <br>· ShapeDto: 2점=BBOX(left/top/right/bottom), 3점+=POLYGON(flat points 배열) <br>· 동일 내용은 결과 제외, JSON 파싱 실패 시 빈 리스트(장애 격리) <br>· 검증: 응답 배열의 type·objectId·before/after shape 확인 | |
| KLID-AT-UT-008-02 | 배정된 WORKER 본인 프레임 rollback시 대상 스냅샷 복원 새 ROLLBACK 버전 / 복원 후 LS_DATA_LBL이 스냅샷과 일치 | WORKER | 스냅샷·본인 프레임 | 1) `POST /v1/versions/{version}/rollback`(body srcSn) → `VersionService.rollback(versionHash, srcSn, actor)` <br>2) `accessGuard.verifyAndGet(srcSn, actor)`(WORKER 본인 LABELER 배정만 통과, CWE-639) → `findByDataSrcSnAndVersionHash` 로 대상 스냅샷 조회 <br>3) 작업락(LS_AUTH_WORK_LOCK) 잠긴 영상이면 CONFLICT 거부 <br>4) `parseSnapshotLabels`(items[] 파싱, 손상 시 INVALID_INPUT 전체 롤백) → `findActiveForUpdate` ACTIVE 비관 잠금 → `replaceFrameLabels`(기존 LS_DATA_LBL 삭제+flush 후 스냅샷 라벨 재생성, lbl_sn 재발급) <br>5) `upsertRollbackVersion`(멱등/UNIQUE 충돌 시 기존 행 재활성·아니면 새 active SAVE_REASON_ROLLBACK) <br>6) APPROVED(검수 완료) 영상이면 `TaskModifiedEvent(rawSn, srcSn, LABEL_UPDATED)` 발행 | · LS_LABEL_VERSION 새 active: SAVE_REASON_CD='ROLLBACK', ACTVTN_YN='Y'(동일 해시면 기존 행 재활성, 이전 active 'N') <br>· LS_DATA_LBL 이 대상 스냅샷 라벨과 일치(lblTypeCd·label·좌표) — 라벨링 캔버스 / 검수완료 export 폴더 JSON 즉시 반영 <br>· 라벨 교체·버전 전환·통지가 동일 트랜잭션에 묶여 원자적 커밋/롤백 <br>· APPROVED 영상은 TASK_MODIFIED(LABEL_UPDATED) 발행 <br>· 검증: DB에서 LS_DATA_LBL vs 스냅샷 items 비교 + LS_LABEL_VERSION active 행(SAVE_REASON_CD/ACTVTN_YN) 확인 | |

## 단위시험 KLID-AT-UT-010

| 단위시험ID | KLID-AT-UT-010 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-07-03 · UC KLID-AT-UC-010] 생성 데이터 활용여부 선택(증강 검수) | | |
| 관련 컴포넌트 ID | KLID-AT-CO-010 | 관련 프로그램 ID | AugmentReviewService, AugmentController |
> 출처: D11 UT-03(외부 증강) 중 수락/반려 검수 케이스.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-010-01 | REVIEWER accept시 AUG_PROC_STTS_CD ACCEPTED + LS_DATA_AUG_RVW row INSERT | REVIEWER | 증강 결과 | 1) REVIEWER 토큰으로 PENDING 증강 결과 PK 에 `POST /v1/augments/{id}/accept` 호출 <br>2) AugmentReviewService.accept → LsDataAug.applyReviewStatus('ACCEPTED')(PENDING 이외면 409 CONFLICT) <br>3) LS_DATA_AUG_RVW row 로드/생성 후 accept(rvwId·rvwDt) | · LS_DATA_AUG.AUG_PROC_STTS_CD 'PENDING'→'ACCEPTED' UPDATE <br>· LS_DATA_AUG_RVW에 RVW_STTS_CD='ACCEPTED'·RVW_ID·RVW_DT INSERT/갱신 <br>· 검증: HTTP 200·DB 두 테이블 상태 조회 | |
| KLID-AT-UT-010-02 | REJECTED 시 사유 LS_DATA_AUG_RVW REJECT_RSN 저장 | REVIEWER | 반려 사유 | 1) REVIEWER 토큰으로 `POST /v1/augments/{id}/reject` 에 `reason`(RejectRequest, @NotBlank·≤500자) 전송 <br>2) AugmentReviewService.reject → 사유 공백 시 400 INVALID_INPUT <br>3) applyReviewStatus('REJECTED') → LS_DATA_AUG_RVW row reject(reason·rvwId·rvwDt) | · LS_DATA_AUG.AUG_PROC_STTS_CD 'PENDING'→'REJECTED' UPDATE <br>· LS_DATA_AUG_RVW에 RVW_STTS_CD='REJECTED'·REJECT_RSN=입력 사유 저장 <br>· 검증: HTTP 200·DB LS_DATA_AUG_RVW.REJECT_RSN 값 조회 | |

## 단위시험 KLID-AT-UT-011

| 단위시험ID | KLID-AT-UT-011 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-09-01 / RQ-SFR-09-02 / RQ-SFR-11-06 · UC KLID-AT-UC-011] 비식별 처리·API 연동 | | |
| 관련 컴포넌트 ID | KLID-AT-CO-011 | 관련 프로그램 ID | DeidentifyStep, DeidentifyClient |
> 출처: D11 UT-07(배치 단계 — 비식별). RQ-SFR-11-06은 09-01과 동일 기능이라 통합.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-011-01 | 정상 경로 응답이면 LS_DEIDENT_REPORT SUCCEEDED + DE_IDNTF Y + 결과경로 반환 | - | 정상 응답 | 1) 원본 영상 1건(LS_DATA_RAW, RAW_FILE_PATH_NM 존재)을 준비하고 DeidentifyStep.execute(BatchContext) 실행 <br>2) 처리 시작 시 LS_DEIDENT_PROC_LOG 를 PROC_STTS_CD=REQUESTED 로 적재(ORGNL_FILE_PATH_NM=원본경로) <br>3) 정상 경로(mock 완료 응답)면 원본을 비식별 경로로 복사(임시파일→atomic move) 후 결과경로 산출 <br>4) LS_DATA_RAW.DE_IDNTF_YN='Y' 전이 + LS_DEIDENT_PROC_LOG.succeed(결과경로) 호출 <br>5) DeidentResult.completed 반환 → 호출자가 LS_DATA_RAW.DATA_STTS_CD=MARKING_READY 전이 | · LS_DEIDENT_PROC_LOG.PROC_STTS_CD REQUESTED→SUCCEEDED 전이 <br>· LS_DEIDENT_PROC_LOG.DE_IDNTF_FILE_PATH_NM = 산출 비식별 영상 경로 <br>· LS_DATA_RAW.DE_IDNTF_YN='Y' <br>· LS_DATA_RAW.DATA_STTS_CD='MARKING_READY' <br>· 검증: 해당 RAW_SN 으로 두 테이블 SELECT 조회 | |
| KLID-AT-UT-011-02 | 위탁 시 LS_DEIDENT_PROC_LOG REQUESTED 상태로 적재 / idempotencyKey 헤더 포함 | - | 위탁 요청 | 1) KPST 위탁 경로(kpst.deid.enabled=true)로 DeidentifyStep.run(raw) 실행 <br>2) LS_DEIDENT_PROC_LOG 를 PROC_STTS_CD=REQUESTED 로 적재(REQ_ID = 요청 idempotency 식별자, ORGNL_FILE_PATH_NM=원본경로) <br>3) KpstDeidentService.submit 이 createProject 위탁 후 markKpstSubmitted(prjId) 호출 → KPST_PRJ_ID 기록 + POLL_STTS_CD=WAITING <br>4) 위탁 직후에는 DE_IDNTF_YN 미전이(완료 대기) → DeidentResult.deferred 반환(MARKING_READY 미전이) | · LS_DEIDENT_PROC_LOG.PROC_STTS_CD='REQUESTED' (위탁 직후 유지) <br>· LS_DEIDENT_PROC_LOG.REQ_ID 기록(요청 idempotency 식별자) + KPST_PRJ_ID 기록 + POLL_STTS_CD='WAITING' <br>· LS_DATA_RAW.DE_IDNTF_YN 미전이(완료는 폴링 잡이 담당) <br>· 검증: RAW_SN 으로 LS_DEIDENT_PROC_LOG SELECT — REQUESTED·REQ_ID·POLL_STTS 확인 | |

## 단위시험 KLID-AT-UT-013

| 단위시험ID | KLID-AT-UT-013 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-09-04 · UC KLID-AT-UC-013] 비식별 옵션 설정 | | |
| 관련 컴포넌트 ID | KLID-AT-CO-013 | 관련 프로그램 ID | SystemConfigController, SystemConfigService, LsSystemConfig (D3 CO-013) |
> **⚠ 설계 케이스(구현·테스트 후속)** — D3 CO-013은 비식별 옵션 설정을 **SystemConfig 메커니즘**(SystemConfigController/Service·LsSystemConfig, IF-008 비식별 옵션 설정 변경)으로 실현하도록 설계돼 있다. SystemConfig 의 설정 저장·화이트리스트 검증·권한·캐시 메커니즘 자체는 **실코드로 구현·검증됨**(KLID-AT-UT-006 블록 `KLID-AT-UT-006-01` POLYGON_SIMPLIFY_TOLERANCE 등 UT-26 실케이스). 다만 **비식별 마스킹 옵션 키(마스킹 타입·범위·출력 화질·포맷)는 현재 `ConfigKeys` 미등록**이고, 비식별 옵션은 `KpstProjectRequest.withDefaults()` 기본값(masking_type=0·db_save=0·masking_range=1·exp_quality=0·exp_format=1)으로 고정돼 위탁된다(사용자 설정 미연동). 따라서 아래는 **D11 실코드 추출이 아닌 설계 케이스**이며, 비식별 옵션 키 등록 시 UT-26 패턴과 동일하게 실테스트로 승격한다. (케이스 ID는 본 절차서 자체 채번 — D11 UT 출처 없음.)

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-013-01 | [설계] REVIEWER가 비식별 마스킹 옵션(타입·범위·화질·포맷) 설정값 저장 시 SystemConfig 반영 + 이후 비식별 위탁에 적용 | REVIEWER | 허용 옵션값 | [설계] 1) REVIEWER 권한으로 SystemConfigController 를 통해 비식별 마스킹 옵션(마스킹 타입·범위·출력 화질·포맷) 설정값 저장 요청 <br>2) [설계] SystemConfigService 가 화이트리스트(허용 옵션값) 검증 후 LS_SYSTEM_CONFIG 에 upsert + 캐시 무효화 <br>3) [설계] 이후 비식별 위탁 시 KpstProjectRequest 가 저장된 설정값(masking_type·masking_range·exp_quality·exp_format)으로 구성되어 위탁 <br>※ 현행: 옵션 키 ConfigKeys 미등록 → withDefaults() 기본값(masking_type=0·db_save=0·masking_range=1·exp_quality=0·exp_format=1) 고정 위탁 | · [설계] LS_SYSTEM_CONFIG 에 비식별 옵션 키/값 저장(허용 옵션값만 통과, 그 외 400) <br>· [설계] 저장 후 조회 시 반영된 옵션값 확인 <br>· [설계] 위탁 요청의 KpstProjectRequest 필드가 저장 옵션값과 일치 <br>· 검증: LS_SYSTEM_CONFIG SELECT + KpstProjectRequest 필드 확인(옵션 키 등록 시 UT-26 패턴 실테스트로 승격) | |

## 단위시험 KLID-AT-UT-016

| 단위시험ID | KLID-AT-UT-016 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-09-03 / RQ-SFR-09-05 · UC KLID-AT-UC-016] 비식별 결과 검토·연동확인 | | |
| 관련 컴포넌트 ID | KLID-AT-CO-016 | 관련 프로그램 ID | DeidentReportController, DeidentReportService |
> 출처: D11 UT-15(비식별 누락 신고). 신고→락+라벨 스냅샷 후 삭제, 수동 비식별 후 resolve 케이스.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-016-01 | WORKER 본인 배정 영상 신고 201 + REPORT_STTS OPEN + LS_AUTH_WORK_LOCK LOCKED + DE_IDNTF F | WORKER | 본인 배정 영상 | 1) WORKER 본인 배정 프레임(srcSn)에 대해 POST /v1/labels/{srcSn}/deident-report (reason 1~1000자) 호출 <br>2) LabelAccessGuard 가 srcSn→rawSn 획득 + WORKER 본인 배정 검증(CWE-639), 이미 잠금이면 409 <br>3) LsDeidentReport.createReport 로 신고 row 저장(REPORT_STTS_CD=OPEN) <br>4) 해당 영상 전체 라벨 스냅샷(LS_LABEL_VERSION, SAVE_REASON='DEIDENT_REPORT') 후 일괄 삭제(ATTR_VAL→AI_INFO→LBL, LS_DATA_LBL_HSTRY 이력 기록) <br>5) workLockService.lockRawForRedeident 로 영상 잠금 + LS_DATA_RAW.markDeidentified('F') <br>6) REVIEWER 알림 → 201 CREATED(신규 RPRT_SN 반환) | · HTTP 201 Created (body=생성된 DEIDENT_REPORT_SN) <br>· LS_DEIDENT_REPORT INSERT: REPORT_STTS_CD='OPEN' (DATA_RAW_SN·REPORTER_NO·RSN) <br>· LS_AUTH_WORK_LOCK INSERT: LOCK_TARGET_CD='RAW' + LOCK_STTS_CD='LOCKED' <br>· LS_DATA_RAW.DE_IDNTF_YN='F' <br>· 검증: rawSn 으로 LS_DEIDENT_REPORT·LS_AUTH_WORK_LOCK·LS_DATA_RAW SELECT 조회 | |
| KLID-AT-UT-016-02 | 수동 비식별화 완료 resolve 200 + RESOLVED 전이 + LOCK 해제 | WORKER | OPEN 신고 | 1) 외부 솔루션으로 수동 비식별화 완료 후 POST /v1/deident-reports/{rprtSn}/resolve 호출 <br>2) 신고 로드(없으면 404) + accessGuard.verifyRawAccess(rawSn) 로 WORKER 본인 배정 검증(REVIEWER 전체 허용) <br>3) 신고가 OPEN 아니면 409(이미 RESOLVED/DISMISSED 재-resolve 차단) <br>4) report.resolve() 로 OPEN→RESOLVED 전이(RESOLVED_DT 기록) + workLockService.releaseRaw 로 작업락 해제(동일 트랜잭션·원자성) <br>5) 200 OK 반환 | · HTTP 200 OK <br>· LS_DEIDENT_REPORT.REPORT_STTS_CD 'OPEN'→'RESOLVED' + RESOLVED_DT 기록 <br>· LS_AUTH_WORK_LOCK.LOCK_STTS_CD 'LOCKED'→'RELEASED' (RELEASE_DT·RELEASE_RSN='MANUAL_DEIDENT_DONE') <br>· 검증: rprtSn/rawSn 으로 LS_DEIDENT_REPORT·LS_AUTH_WORK_LOCK SELECT 조회 | |

## 단위시험 KLID-AT-UT-018

| 단위시험ID | KLID-AT-UT-018 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-11-04 · UC KLID-AT-UC-018] 영상 적재(관제 학습용 설정) | | |
| 관련 컴포넌트 ID | KLID-AT-CO-018 | 관련 프로그램 ID | TrainingVideoIngestService, ControlTrainingVideoScanJob |
> 출처: UC-018(영상 적재 관제 학습용 설정)은 D11에 별도 UT 블록이 없으나 백엔드 실테스트(TrainingVideoIngestServiceTest·ControlTrainingVideoScanJobTest)가 존재하여 적재 케이스(실코드 출처·D11 미수록)를 추가했다. 적재(TUS 업로드, D11 UT-23)는 관제 학습용 설정 기반 적재로 대체되는 폐지 예정 경로(CLAUDE.md)라 TUS 업로드(UT-23)는 제외.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-018-01 | 관제 작업수요 설정 클립을 픽업해 적재 위임한다 | - | 작업수요 설정 클립 | 1) 관제 공유 DB에서 `MNG_CLIP_MASTER.JOB_DMND_YN='Y'` 학습용 지정 클립을 조회한다(readOnly, controlTransactionManager). <br>2) `ControlTrainingVideoScanJob.execute` → `TrainingVideoIngestService.scanAndIngest()` 호출(1건/분 주기, DisallowConcurrentExecution). <br>3) 클립별 `TrainingVideoIngestTx.ingestOne`(REQUIRES_NEW 독립 트랜잭션)으로 적재 — `CLIP_ID`/`VMS_CCTV_ID`/`FILE_PATH` blank 가드 + `findByVmsClipId` 멱등 skip. <br>4) `EVNT_ID`로 `MNG_CLIP_EVNT_LST` 조인해 `EVNT_TYPE_CD`·`SHT_DT` 도출(미매칭 시 `CRT_DT` 폴백), `VDO_LEN_SEC`(ms)→초 변환. <br>5) `LsDataRaw.createFromIngest`로 `LS_DATA_RAW` 적재 후 `VideoIngestedEvent`(AFTER_COMMIT) 발행 → 비식별 선두 파이프라인에 위임한다. | · LS_DATA_RAW INSERT (VMS_CLIP_ID=CLIP_ID(UK), RAW_FILE_PATH_NM=FILE_PATH, PRVC_TYPE_CD=ANONY) <br>· LS_DATA_RAW.DATA_STTS_CD=PENDING (적재 초기 단계) <br>· VideoIngestedEvent 발행 (비식별 트리거) <br>· 중복/blank 식별자 클립은 skip → 신규 적재 건수 미포함 <br>· 검증: DB 조회(LS_DATA_RAW.VMS_CLIP_ID 존재) + scanAndIngest() 반환 ingested 건수 | |

## 단위시험 KLID-AT-UT-019

| 단위시험ID | KLID-AT-UT-019 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-11-04 · UC KLID-AT-UC-019] 이벤트 마킹(자동/수동) | | |
| 관련 컴포넌트 ID | KLID-AT-CO-019 | 관련 프로그램 ID | MarkingController, MarkingService, LsMarking |
> 출처: D11 UT-13(마킹). 비식별 영상 자동(프레임간격)/수동(이벤트시점) 마킹 케이스.

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-019-01 | 마킹 자동모드/수동모드 생성 201 | WORKER | 자동·수동 요청 | 1) `POST /v1/videos/{rawSn}/markings` 호출(mode=AUTO 또는 MANUAL). <br>2) 본인 배정 검증(`LS_TASK_ASSIGNMENT` TASK_TYPE_CD='LABELER' 존재) — REVIEWER는 전체 허용, 미배정 WORKER는 403. <br>3) 영상 조회 + 비식별 완료 가드(`LS_DATA_RAW.DE_IDENT_YN='Y'`) + 배치 단계 가드(`DATA_STTS_CD=MARKING_READY`). <br>4) 이벤트명은 `LS_DATA_RAW.EVNT_TYPE_CD`에서 자동 소싱, AUTO=`generateAutoMarks`·MANUAL=요청 marks 직렬화. <br>5) `LsMarking.createAuto/createManual`로 `LS_MARKING` 저장 + `MarkingCompletedEvent` 발행. | · HTTP 201 Created <br>· LS_MARKING INSERT (RAW_SN, EVNT_NM, MARK_MODE_CD, MARK_CN=marks JSON, STTS_CD) <br>· AUTO 시 FRME_INTV_NOCS=intervalFrames 저장 <br>· MarkingCompletedEvent 발행 (잔여 배치 VLM→프레임추출→오토라벨링 트리거) <br>· 검증: 응답 201 + DB 조회(LS_MARKING.MARKING_SN 존재) | |
| KLID-AT-UT-019-02 | 자동모드 마킹 생성 intervalFrames 기반 marks 자동생성 (30/60프레임 120초) | WORKER | intervalFrames·duration | 1) AUTO 모드로 `durationSec`·`intervalFrames`(1 이상) 지정 호출. <br>2) `generateAutoMarks`: totalFrames = durationSec × 30(NATIVE_FPS 고정 가정) 계산. <br>3) frameIndex=0부터 intervalFrames 간격으로 `frameIndex < totalFrames`까지 MarkItem 생성(frameIndex, timestamp=frameIndex/30 → "mm:ss"). <br>4) durationSec null/0 이하면 INVALID_INPUT 거부(퇴화 방지), 끝 경계 totalFrames는 미포함(off-by-one 수정). | · marks 개수 = totalFrames를 intervalFrames로 나눈 스텝 수(끝 경계 totalFrames 제외) <br>· 예: 120초×30fps=3600프레임 → interval=30 시 120건, interval=60 시 60건 <br>· MARK_CN에 marks JSON 직렬화 저장 <br>· 검증: DB 조회(LS_MARKING.MARK_CN 파싱 marks 배열 개수) | |

## 단위시험 KLID-AT-UT-021

| 단위시험ID | KLID-AT-UT-021 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-11-08 / RQ-SFR-16 / RQ-SFR-17 · UC KLID-AT-UC-021] 라벨링·이미지/영상 가공(라벨 편집) | | |
| 관련 컴포넌트 ID | KLID-AT-CO-021 | 관련 프로그램 ID | LabelController, LabelService, LsDataLbl |
> 출처: D11 UT-14(라벨링) 중 프레임 라벨 편집(임시저장 upsert)·검증 케이스(SAM2 분할/Track은 08-01·08-02 블록에서 다룸).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-021-01 | bbox 좌표 저장시 AUTO_LBL_YN N 저장(수동) / 오토 라벨 수정시 AUTO_LBL_YN은 Y 유지 | WORKER | 수동·오토 라벨 | 1) 프레임 라벨 bulk upsert 호출(`LabelController` → `LabelService.bulkUpsert`). <br>2) 본인 배정 검증(`LabelAccessGuard`, WORKER는 본인 배정 프레임만) + 재비식별 락(LOCKED) 가드 + 좌표 검증(음수 차단·신규 최대 1000점). <br>3) item.id==null → `LsDataLbl.createManual` 신규 INSERT (AUTO_LBL_YN='N' 강제, 수동 라벨). <br>4) item.id!=null → `updateUserContent`로 좌표/라벨만 UPDATE, AUTO_LBL_YN은 미변경(오토 라벨은 수정해도 'Y' 유지 — Mass Assignment 요청값 무시). <br>5) 검수 완료(APPROVED) 영상 수정 시에만 `TaskModifiedEvent`(LABEL_UPDATED) 발행. | · 신규 라벨 LS_DATA_LBL INSERT + AUTO_LBL_YN='N' <br>· 오토 라벨 수정 시 LS_DATA_LBL.AUTO_LBL_YN='Y' 유지(요청의 autoLblYn 무시) <br>· POINT_CN=좌표 JSON, 1000점 초과 시 Douglas-Peucker simplify 후 저장 <br>· 검수 전 저장은 통지 미발행(TASK_MODIFIED는 APPROVED 후 수정만) <br>· 검증: DB 조회(LS_DATA_LBL.AUTO_LBL_YN 값 N/Y) | |

## 단위시험 KLID-AT-UT-022

| 단위시험ID | KLID-AT-UT-022 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-11-10 · UC KLID-AT-UC-022] VLM 시계열 메타 검토 | | |
| 관련 컴포넌트 ID | KLID-AT-CO-022 | 관련 프로그램 ID | MetaService |
> 출처: D11 UT-16(외부 VLM 메타 검토 승인/반려).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-022-01 | 검토 승인 REVIEWER 정상 200 | REVIEWER | 승인 요청 | 1) VLM/외부 생성 메타 검토 승인 호출(`MetaService.approveReview(metaReviewSn)`). <br>2) REVIEWER 권한 검증(`ensureReviewer`) — 미인증 401, 비-REVIEWER 403. <br>3) `LS_DATA_META_REVIEW` 조회(미존재 시 404). <br>4) `review.approve(reviewerId, now)` 호출 → RVW_STTS_CD를 APPROVED로 전이하고 RVW_ID·RVW_DT 기록. | · HTTP 200 <br>· LS_DATA_META_REVIEW.RVW_STTS_CD PENDING→APPROVED 전이 <br>· RVW_ID=검토자 sub, RVW_DT=처리 일시 기록 <br>· 검증: 응답 200 + DB 조회(LS_DATA_META_REVIEW.RVW_STTS_CD=APPROVED) | |

## 단위시험 KLID-AT-UT-023

| 단위시험ID | KLID-AT-UT-023 | | |
|---|---|---|---|
| 설명 | [RQ-SFR-11-10 · UC KLID-AT-UC-023] 검수 승인·반려 | | |
| 관련 컴포넌트 ID | KLID-AT-CO-023 | 관련 프로그램 ID | ReviewService |
> 출처: D11 UT-17(검수 승인/반려).

| 케이스 ID | 케이스 명 | 작업 권한 | 시험 데이터 | 시험항목 및 처리절차 | 예상결과 및 검증방법 | 시험 결과 |
|---------|---------|---------|-----------|---------|--------------|------|
| KLID-AT-UT-023-01 | 승인시 LS_RAW_DATA_STATUS APPROVED | REVIEWER | IN_REVIEW 영상 | 1) 검수 승인 호출(`ReviewService.approve(videoId)`). <br>2) REVIEWER 권한 검증 + `LS_RAW_DATA_STATUS` 조회(미존재 404). <br>3) `ReviewStateMachine.verify`(IN_REVIEW→APPROVED) 통과 후 `transitionTo(APPROVED)`. <br>4) `LsTaskEventLog.approve` 기록 + flush — `@Version` 낙관적 락 충돌 시 409. <br>5) `VersionService.commitApproved`로 영상 전체 라벨 스냅샷(`LS_LABEL_VERSION`, SAVE_REASON=APPROVED) 생성 + `ReviewApprovedEvent` 발행(동일 트랜잭션). | · LS_RAW_DATA_STATUS.DATA_STTS_CD IN_REVIEW→APPROVED 전이 <br>· LS_LABEL_VERSION 스냅샷 생성(검수 확정 학습데이터 버전) <br>· ReviewApprovedEvent 발행(TASK_COMPLETED 통지 연계) <br>· 동시 승인 시 1건만 성공·나머지 409 CONFLICT <br>· 검증: DB 조회(LS_RAW_DATA_STATUS.DATA_STTS_CD=APPROVED) | |
| KLID-AT-UT-023-02 | 반려시 LS_DATA_ISSUE 생성 + 상태 REJECTED + DATA_STTS_CD 업데이트 | REVIEWER | 반려 사유 | 1) 검수 반려 호출(`ReviewService.reject(videoId, reason)`). <br>2) REVIEWER 권한 검증 + `ReviewStateMachine.verify`(→REJECTED). <br>3) 직전 반려 이슈 조회해 계층 연결(UP_DATA_ISSUE_SN), `LsDataIssue.create/createWithParent`로 `LS_DATA_ISSUE` INSERT(ISSUE_TYPE_CD=REJECTION, ISSUE_STTS_CD=RESOLVED, ISSUE_RSN=사유). <br>4) `transitionTo(REJECTED)` + `LsTaskEventLog.reject` 기록 + flush(락 충돌 시 409). | · LS_DATA_ISSUE INSERT (ISSUE_TYPE_CD=REJECTION, ISSUE_STTS_CD=RESOLVED, ISSUE_RSN=반려사유) <br>· LS_RAW_DATA_STATUS.DATA_STTS_CD IN_REVIEW→REJECTED 전이 <br>· 직전 반려 존재 시 UP_DATA_ISSUE_SN으로 이력 계층 연결 <br>· 검증: DB 조회(LS_DATA_ISSUE 생성 + LS_RAW_DATA_STATUS.DATA_STTS_CD=REJECTED) | |

---

유스케이스 커버리지: 활성 R1 UC 18종 전부 정상 케이스 보유
