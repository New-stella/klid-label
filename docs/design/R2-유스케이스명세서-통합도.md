# R2 유스케이스 명세서 — 통합 UCD (A+B)

> **목적**: 기존 `R2-유스케이스명세서.md`는 UCD를 흐름별 2개(가공·검수 14 UC / 데이터 증강 4 UC)로 분리하고, 증강 UCD는 노드가 적어 페이지에서 작게 렌더된다.
> 본 문서는 **(A) 전 액터(8) · 전 유스케이스(18)를 한 장에 모은 단일 통합 UCD** + **(B) `left to right direction` + `scale` 상향으로 페이지 폭을 채우는 와이드 렌더**를 적용한 보충 산출물이다. 유스케이스 목록·기술서(시나리오·전제/종료조건 등)는 기존 `R2-유스케이스명세서.md`를 그대로 따른다.
>
> **렌더 권장**: PlantUML `scale`/`dpi` 적용본. 폭 ≥ 1200px 기준. (plantuml.jar 또는 동등 렌더러 사용)

## 통합 유스케이스 다이어그램 (전 액터 · 전 UC)

| UCD ID | KLID-AT-UCD-ALL | UCD 명 | 학습데이터 저작도구 통합 유스케이스 다이어그램 |
|--------|---|-------|---|
| 관련 서브시스템 | KLID-AT-SS-002~009 | 포함 | 가공·검수 핵심 흐름 + 데이터 증강 흐름 (UCD-001 + UCD-002 통합) |

```plantuml
@startuml
scale 1.2
left to right direction
skinparam dpi 150
skinparam packageStyle rectangle
skinparam shadowing false

actor "라벨링 작업자\n(WORKER)" as WORKER
actor "검수자\n(REVIEWER)" as REVIEWER
actor "관제서버 [외부]" as CTRL
actor "비식별 솔루션 [외부]" as DEID
actor "시계열 분석 서비스 [외부]" as TSVC
actor "AI 추론 서버 [내부]" as AISRV
actor "생성형 AI 서비스 [외부]" as GENAI

rectangle "학습데이터 저작도구 (가공·검수)" {
  usecase "UC-018.영상 적재" as U18
  usecase "UC-011.비식별 처리 요청" as U11
  usecase "UC-013.비식별 옵션 설정" as U13
  usecase "UC-016.비식별 처리 상태·이력 확인" as U16
  usecase "UC-019.이벤트 마킹" as U19
  usecase "UC-022.시계열 메타 검토" as U22
  usecase "UC-021.라벨 편집·임시저장" as U21
  usecase "UC-004.객체 자동 추적" as U4
  usecase "UC-005.객체 외곽 경계 자동 밀착" as U5
  usecase "UC-006.라벨링 정밀도 조절" as U6
  usecase "UC-023.검수 승인·반려" as U23
  usecase "UC-007.라벨 버전 저장·이력 추적" as U7
  usecase "UC-008.버전 비교·복구" as U8
  usecase "UC-009.검수 완료·수정 통지" as U9
}

rectangle "데이터 증강" {
  usecase "UC-001.증강 영상 생성 요청" as U1
  usecase "UC-002.증강 결과 수신·등록" as U2
  usecase "UC-003.해상도 변경 수행" as U3
  usecase "UC-010.증강 영상 활용 여부 검수" as U10
}

CTRL --> U18
REVIEWER --> U11
U11 --> DEID
REVIEWER --> U13
REVIEWER --> U16
WORKER --> U16
WORKER --> U19
REVIEWER --> U22
TSVC --> U22
WORKER --> U21
WORKER --> U4
WORKER --> U5
U4 --> AISRV
U5 --> AISRV
REVIEWER --> U6
REVIEWER --> U23
U23 ..> U7 : include
REVIEWER --> U8
WORKER --> U8
U23 --> U9
U9 --> CTRL
REVIEWER --> U1
U1 --> GENAI
GENAI --> U2
REVIEWER --> U3
REVIEWER --> U10
@enduml
```

> 본 통합 UCD는 액터 8종과 유스케이스 18종의 전 관계를 한 장으로 조망한다. 유스케이스별 상세 기술서(주요 액터·이해관계자·전제/종료조건·기본/대안 시나리오·발생 빈도)는 원본 `R2-유스케이스명세서.md`의 "5. 유스케이스 기술서" 절을 그대로 참조한다.
