{
  "item": {
    "id": "EXTSYS-003",
    "project_id": "4ece2c3f-8e99-46f5-9580-71108a76e578",
    "type": "external_system",
    "title": "KPST (외부 비식별 솔루션)",
    "slug": "외부-비식별-솔루션",
    "sub_id": null,
    "status": "approved",
    "data": {
      "kind": "regulatory",
      "status": "active",
      "vendor": "발주기관 제공",
      "brownfield": {
        "status": "new",
        "decided_by": "ADR-006",
        "change_kind": [
          "component-replace"
        ],
        "diff_summary": "캔버스 수동 블러 → 외부 비식별 솔루션 연동"
      },
      "owner_team": "저작도구 연동팀",
      "criticality": "critical",
      "description": "영상 비식별화(개인정보 마스킹)를 수행하는 외부 솔루션. 비식별화는 **파이프라인 선두 단계**로 영상 적재 직후 자동 트리거되며, 성공해야 마킹·라벨링 단계로 진입한다(전체 영상 비식별 정책).\n\n## 연동 2종 (상세는 INT-004 / INT-005)\n| 구분 | 경로 | 비고 |\n|---|---|---|\n| 위탁 (INT-004) | `POST /project` | 응답 `prj_id` 가 이후 폴링 키 |\n| 폴링 (INT-005) | `GET /retrieve_progress` (**GET 이면서 바디에 JSON**) | KPST 는 콜백을 제공하지 않아 **폴링이 유일한 완료 감지 수단** |\n| 정리 | `POST /delete_project_id` | 재위탁 전 stale export 제거 |\n\n## 인증\n**없음**. `https` 스킴일 때만 자체 CA(`ca.crt`)로 서버 인증서를 검증하며(클라이언트 인증서 없음), `http` 내부망 운영 시엔 평문이다 — 망 분리가 사실상의 방어선.\n\n## 활성화 실태 (2026-07-27 실측)\n`kpst.deid.enabled` 기본 true. 다만 **mock-mode**(`authoring.integration.deidentify.mock-mode`)가 갈라다:\n- ⚠ **[폐기]** **local·dev**: mock-mode `true` → KPST 실사용 안 함\n- **stg·prd**: mock-mode `false` → **실제 KPST 폴링 활성**\n- ⚠ **[폐기]** **prd 에서 mock-mode=true 면 부트 거부**(`assertMockAllowedProfile()`, allowlist=local/dev/stg) — 운영에서 가짜 비식별로 넣는 사고를 fail-closed 로 차단\n\n⚠ **[폐기] 위 「local·dev 는 mock-mode 가 켜져 있다」** — 실측 정정. 이 모드는 **어느 프로파일에서도 켜져 있지 않다**(전 프로파일 미설정 = 꺼짐). 따라서 이 모드로 갈리는 환경은 실제로 없으며, 전 환경이 실제 위탁 경로를 탄다. 구 서술을 지우지 않고 무엇이 사실인지를 함께 남긴다.\n\n⚠ **[폐기] 위 「부트 거부」 규칙** — 판정하는 자리가 옮겨졌다. `ADR-062`(2026-09-03 승인) 「외부 연동 판정을 기동 시점에서 사용 시점으로 옮긴다」가 이 판정을 **비식별 산출 시점**으로 옮겼다. 문장을 지우지 않고 무엇으로 대체됐는지를 함께 남긴다.\n\n**[폐기] 대체 규칙 — 산출 시점 거부.** 운영 프로파일에서 이 모드가 켜져 있어도 **기동은 통과**하며, 기동 시점에는 예외 대신 **오류 수준 기록**을 남긴다. 대신 **비식별 산출이 일어나는 순간 거부**한다.\n\n★ **[폐기] 거부하는 자리를 「위탁 요청」으로 적지 말 것.** 이 모드는 외부로 요청을 보내지 않고 **원본을 그대로 복사**할 뿐이다. 위탁 요청 차단으로 적으면 이 판정이 지키는 것 — **비식별되지 않은 영상이 비식별본 행세를 하고 산출물로 나가는 것** — 을 막는 보호가 통째로 사라진다. 막는 자리는 **산출물이 만들어지는 지점**이다.\n\n**프로파일별 강도는 그대로다** — 개발 계열(local · dev · stg)에서는 이 모드가 정상 동작하고, 운영 프로파일(prd)에서만 거부된다.\n\n⚠ **[폐기] 위 ★ 문단의 전제와 「막는 자리」 지시** — 실측 정정. ① **「원본을 그대로 복사한다」는 더 이상 사실이 아니다.** 이 모드에는 **자체 산출 경로가 없다** — 그 경로는 폐지됐고, 지금 이 모드에 남은 것은 **판정뿐**이다(그 귀결로 바로 위 「개발 계열에서는 이 모드가 정상 동작한다」도 성립하지 않는다). ② 그래서 **「막는 자리는 산출물이 만들어지는 지점이다」도 성립하지 않는다** — 막을 산출 지점 자체가 없다. 두 문장을 지우지 않고 무엇으로 대체됐는지를 함께 남긴다.\n\n**대체 규칙 — 위탁 요청층 거부.** 막을 산출 지점이 실재하지 않으므로, 남은 토글은 **위탁 요청층에서 거부**한다. 그 형상에서 비식별은 **전건 실패**하며 그 사실이 기동 기록과 상태로 드러난다.\n\n★ **되살아날 때를 위한 경고.** 자체 복사 산출 경로가 다시 생기면 **이 배선으로는 막히지 않는다**(그 경로는 외부 요청을 타지 않는다). 그런 경로를 다시 만든다면 **산출 지점에 별도 차단을 함께** 넣어야 한다.\n\n⚠ **대가(인지·수용)**: 운영에서 이 모드가 켜진 채 배포되면 **앱은 정상으로 떠 있는데 비식별만 전건 실패**한다. 조용한 실패가 이 결정의 주된 위험이므로 그 사실이 반드시 **기록과 상태로 드러나야 한다** — 기동 시 오류 수준 기록, 그리고 산출을 시도할 때마다 남는 실패 기록과 그 영상의 비식별 실패 상태.\n\n※ 로컬 검증은 내부 mock 모드가 아니라 **목 서버(:9400)를 바라보게 배선**하는 것이 운영 원칙이다.\n\n## ★ 운영상 중요 사실\n1. **결과 파일명 규칙** — 응답 `fileName` 은 산출물명이 아니라 **원본 입력파일의 절대경로**이고, 실제 결과물은 `{stem}-mask{ext}`. 2026-07-21 실서버 curl 실측으로 확정됐으며, 과거 이를 오해해 완료 건을 'F' 로 오종결하던 결함이 수정됐다\n2. **완료 판정 가드** — 도착 파일이 존재하고 0바이트 초과해야만 `deIdntfYn='Y'`. 빈 파일을 성공으로 오판하면 비식별 안 된 영상이 라벨링으로 흘러들어가므로 **PII 방어선**\n3. **procState 해석** — `2`=완료, `3/4/99`=터미널 실패 즉시 'F', 그 외는 진행중\n4. **dead-letter 없음** — 타임아웃(180분)·터미널 실패는 'F' 마킹만 하고 자동 재시도가 없다. **외부 솔루션으로 수동 재비식별 후 resolve** 하는 것이 확정된 운영 정책(자동 재비식별 큐는 폐기됨)",
      "environments": [],
      "attached_files": [],
      "implementation": {
        "status": "implemented",
        "modules": [],
        "records": [],
        "progress": 100,
        "subtasks": [],
        "module_paths": []
      },
      "compliance_tags": [
        "isms"
      ],
      "used_by_domains": [
        "DOMAIN-012"
      ],
      "data_sensitivity": "pii",
      "shared_with_projects": []
    },
    "data_projected_to": null,
    "schema_version": 1,
    "current_version": 10,
    "stale": false,
    "stale_reason": null,
    "change_summary": "실측 정정 — 「이 모드는 원본을 그대로 복사한다」 전제와 그 위에 선 「막는 자리는 산출물이 만들어지는 지점이다」 지시를 폐기 표기하고, 대체 규칙(위탁 요청층 거부·전건 실패)과 「자체 복사 산출 경로가 되살아나면 이 배선으로는 막히지 않는다」 경고를 신설. 활성화 실태(local·dev 가 mock-mode 켜짐)도 전 프로파일 미설정으로 정정. 구 문장은 원문 그대로 보존(삽입만, 삭제·치환 0).",
    "last_updated_by": "claude",
    "last_updated_at": "2026-09-03T06:03:29.659Z",
    "created_at": "2026-06-01T06:31:28.887Z"
  }
}