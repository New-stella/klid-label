{
  "item": {
    "id": "SEQ-012",
    "project_id": "4ece2c3f-8e99-46f5-9580-71108a76e578",
    "type": "diagram_sequence",
    "title": "비식별 처리 요청 — 선두 자동 위탁부터 KPST 폴링 완료까지",
    "slug": "비식별-처리-요청-재처리-요청부터-외부-솔루션-결과-콜백까지",
    "sub_id": null,
    "status": "draft",
    "data": {
      "theme": "neutral",
      "title": "비식별 처리 요청 — 선두 자동 위탁부터 KPST 폴링 완료까지",
      "source": "sequenceDiagram\n    autonumber\n    participant IG as 적재 (TrainingVideoIngestService)\n    participant BR as IngestDeidentifyBridge → AsyncDeidentifyRunner\n    participant DS as DeidentifyStep\n    participant KP as KpstDeidentService\n    participant EX as 외부 KPST 비식별 솔루션\n    participant PJ as KpstDeidentPollJob (Quartz)\n    participant DB as PostgreSQL (LS_DATA_RAW / LS_DEIDENT_PROC_LOG)\n    participant FS as 공유 마운트(NAS)\n\n    Note over IG,DS: 비식별은 파이프라인 <선두> 단계 — 적재 직후 자동 트리거\n    IG->>BR: VideoIngestedEvent (AFTER_COMMIT)\n    BR->>DS: @Async run(rawSn)\n    Note over DS,DB: ★게이팅 폐지 — PRVC_TYPE_CD 와 무관하게 <전체 영상>이 대상이다(ANONY 포함). 구 'PRVC/PSDO 만 호출' 규칙은 폐기\n\n    alt local mock 모드\n        DS->>FS: 원본을 비식별 경로로 복사 (외부 미접촉) → 동기 완료\n        DS->>DB: DE_IDNTF_YN='Y' + procLog SUCCEEDED(DE_IDNTF_FILE_PATH_NM)\n    else KPST 위탁 (기본 경로)\n        DS->>KP: submit(raw)\n        KP->>DB: 원장 선커밋 — LS_DEIDENT_PROC_LOG (POLL_STTS_CD=WAITING, prjId 미정)\n        KP--)EX: createProject 논블로킹 제출 (공유 마운트 원본 경로 참조)\n        Note over KP,EX: 스텝이 확정적으로 말하는 사실은 “제출을 개시했다” 뿐이다\n        EX--)KP: ACK (프로젝트 ID)\n        KP->>DB: 완료 핸들러가 prjId 기록 — SubmitSignalDispatch 전용 풀 (publishOn 금지: reactor-netty 이벤트루프에서 JPA 쓰기 위험)\n        Note over KP,DB: 아무 신호도 없으면 미결 스위퍼가 유일한 회수 경로 — 조건부 UPDATE 원자 클레임으로 2노드 중복 재위탁 차단\n        DS-->>BR: deferred (파이프라인 스레드 미점유)\n\n        loop 주기 폴링 (별도 결과 콜백 없음)\n            PJ->>EX: retrieveProgress(prjId)\n            EX-->>PJ: procState\n            alt COMPLETED\n                PJ->>FS: 결과 다운로드 / 공유 마운트 산출물 확인\n                PJ->>DB: DE_IDNTF_YN='Y' + procLog SUCCEEDED + DE_IDNTF_FILE_PATH_NM 적재\n                PJ->>DB: LS_DATA_RAW.DATA_STTS_CD=MARKING_READY (마킹 진입 허용)\n            else 실패 / 폴링 상한·타임아웃 초과\n                PJ->>DB: DE_IDNTF_YN='F' + procLog FAILED (원본 절대 삭제 금지)\n                Note over PJ,DB: 자동 재비식별 큐는 없다 — 외부 솔루션 수동 비식별 후 resolve 로 해소한다\n            end\n        end\n    end\n\n    Note over DS,FS: ⚠ 비식별본 파일명은 고정이 아니다 (mock=deidentified.mp4 / KPST={원본stem}-mask{ext})\n    Note over DS,FS: 소비자는 경로를 조합하지 말고 LS_DEIDENT_PROC_LOG.DE_IDNTF_FILE_PATH_NM <값>을 읽는다\n\n    Note over DS,DB: 재비식별 진입점 — POST /v1/videos/{rawSn}/redeident (ApprovedRedeidentController) 하나뿐. 구 재처리 placeholder 컨트롤러(POST /v1/deident/{videoId}/reprocess)는 두지 않는다",
      "messages": [],
      "fragments": [],
      "description": "비식별 처리 위탁·수신 흐름. ★대상 게이팅은 폐지됐다 — PRVC_TYPE_CD(PRVC/PSDO/ANONY) 구분 없이 모든 영상을 비식별하며, 비식별은 파이프라인 <선두> 단계로 적재 직후 자동 트리거된다(VideoIngestedEvent → IngestDeidentifyBridge(AFTER_COMMIT) → AsyncDeidentifyRunner(@Async) → DeidentifyStep). 경로는 KPST 로 단일화됐다 — 레거시 동기 SPI(DeidentifyClient)는 제거됐고 local mock(원본을 비식별 경로로 복사해 동기 완료)만 남아 있으며, mock 도 아니고 KPST 서비스도 주입되지 않은 형상은 설정 오류로 거부한다(폴백 없음). ★KPST 제출은 논블로킹이다 — LS_DEIDENT_PROC_LOG 원장을 POLL_STTS_CD=WAITING + prjId null 로 <선커밋> 한 뒤 createProject 를 비동기 디스패치하므로, 스텝이 확정적으로 말하는 사실은 “제출을 개시했다” 뿐이다(VLM 위탁과 대칭). ACK 수신 시 prjId 기록은 publishOn 이 아니라 명시적 디스패치(SubmitSignalDispatch)로 전용 풀에서만 실행한다 — publishOn 은 풀 포화 시 거부가 시그널을 나른 reactor-netty 이벤트 루프로 흘러 JPA 쓰기를 이벤트 루프에서 실행시킨다. 아무 신호도 오지 않으면 in-memory subscription 은 노드 종료와 함께 사라져 실패 행조차 없으므로 <미결 스위퍼가 유일한 회수 경로>이며, 2노드 Active-Active 에서 같은 후보를 두 번 재위탁하지 않도록 처리 전 조건부 UPDATE 로 원자 클레임한다. 수신은 <주기 폴링>이다 — KpstDeidentPollJob 이 retrieveProgress 로 procState 를 판정해 완료 시 결과를 다운로드/확인하고 DE_IDNTF_YN='Y' + procLog SUCCEEDED + DE_IDNTF_FILE_PATH_NM 을 적재한 뒤 DATA_STTS_CD 를 MARKING_READY 로 전이시킨다. <별도 결과 콜백은 없다> — 구 수신 경로 POST /v1/deidentify/result(HMAC 웹훅)은 폐기됐다. 실패나 폴링 상한·타임아웃 초과 시 DE_IDNTF_YN='F' 로 마킹하고 원본은 절대 삭제하지 않으며, <자동 재비식별 큐는 없다> — 작업자/검수자가 외부 솔루션으로 수동 비식별한 뒤 resolve 로 해소한다(UC-016). ⚠ 비식별본 파일명은 고정이 아니다 — 우리가 지정하는 것은 디렉터리(export_path)까지이고 파일명은 외부 솔루션이 정한다(mock=deidentified.mp4 / KPST={원본stem}-mask{ext}). 따라서 모든 소비자는 경로를 조합·추측하지 말고 LS_DEIDENT_PROC_LOG.DE_IDNTF_FILE_PATH_NM 적재값을 읽어야 한다. 재비식별 진입점은 POST /v1/videos/{rawSn}/redeident(ApprovedRedeidentController) 하나뿐이다 — 아무 일도 하지 않던 구 재처리 placeholder 엔드포인트(POST /v1/deident/{videoId}/reprocess, DeidentController)는 두지 않는다.",
      "invokes_apis": [
        "API-112"
      ],
      "participants": [],
      "scenario_type": "happy_path",
      "implementation": {
        "status": "implemented",
        "modules": [],
        "records": [],
        "progress": 100,
        "subtasks": [],
        "module_paths": []
      },
      "publishes_events": [],
      "referenced_items": [],
      "realizes_use_cases": [
        "UC-011"
      ],
      "related_acceptances": [],
      "related_requirements": [],
      "referenced_integrations": [
        "INT-004",
        "INT-005"
      ]
    },
    "data_projected_to": null,
    "schema_version": 1,
    "current_version": 12,
    "stale": false,
    "stale_reason": null,
    "change_summary": "진행률 미설정 정정 — status 는 implemented 인데 progress 가 0 이라 ITEM 안에서 앞뒤가 맞지 않았다. 하위작업이 하나도 없어 «부분 완료» 를 나타낼 대상이 없으므로 0 은 값을 넣지 않은 잔재이지 판단이 아니다. 이미 선언된 status 에 맞춰 100 으로 맞춘다. ⚠ status 자체를 새로 판정한 것은 아니다 — 구현 실재는 코드로 표본 확인했고(controlnotify · TaskQueryService · AugmentRequestService · VlmClient), 하위작업이 있어 진행률이 100 미만인 항목(FEAT-006 · NFR-022 · AC-120)은 그 미완이 의도된 것이라 건드리지 않았다.",
    "last_updated_by": "claude",
    "last_updated_at": "2026-08-29T01:15:02.129Z",
    "created_at": "2026-06-02T13:14:34.920Z"
  }
}