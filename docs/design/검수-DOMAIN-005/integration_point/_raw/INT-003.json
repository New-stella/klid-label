{
  "item": {
    "id": "INT-003",
    "project_id": "4ece2c3f-8e99-46f5-9580-71108a76e578",
    "type": "integration_point",
    "title": "VLM 시계열 결과 콜백 수신",
    "slug": "vlm-시계열-결과-콜백-수신",
    "sub_id": null,
    "status": "approved",
    "data": {
      "status": "draft",
      "protocol": "webhook",
      "auth_type": "none",
      "direction": "inbound",
      "brownfield": {
        "status": "new",
        "change_kind": [
          "component-add"
        ],
        "diff_summary": "고도화 신규 — VLM 결과 콜백 수신"
      },
      "auth_detail": {
        "notes": "서명·토큰 인증 없음 — 벤더 계약(KLID 연동 API v1.2.0)이 **무서명 콜백** 규격이라 HMAC 을 적용할 수 없다(2026-07-07 확정). 대신 3계층 방어로 대체: ① IP allowlist `webhook.vlm.allowed-ip-cidrs` ② rate limit + 본문 크기 상한 ③ `request_id` 발급 게이트(우리가 발급하지 않은 request_id 는 거부). 증강 콜백(INT-006)이 HMAC 필수인 것과 대비되는 지점이며, 벤더 규격 차이에 따른 의도된 설계다. ⚠ v1.2.0 이 신설한 `X-API-Key` 는 우리가 나가는 위탁 요청 방향의 인증이고 콜백 수신 방향에는 인증 헤더가 없다 — 이 연동의 무서명 전제와 3계층 방어는 그대로다."
      },
      "data_format": "json",
      "description": "외부 VLM 서비스가 시계열 메타 생성 결과를 저작도구로 push 하는 inbound 콜백.\n\n## 연동 계약 (KLID 연동 API v1.2.0, 2026-09-07)\n- 규격서 파일명 접미가 `_pre` 라 사전배포판일 수 있다 — 벤더 확답 대기. 판번호는 그 전제 아래 적는다.\n- 수신 엔드포인트 `POST /v1/vlm/callback` — 콜백 본문을 검증하고 결과를 적재한다(우리 쪽 경로).\n- 연동 대상은 묘사(describe)·추가 질문(custom) 두 축이며 verify 는 미연동이다. 추가 질문 축의 위탁 창구가 describe-sub 에서 custom 으로 교체됐다.\n- [폐기] describe-sub 창구 — 벤더는 그 창구를 계속 제공하나 우리는 두지 않는다(설계 결정). 이미 그 창구로 받아 적재된 결과는 작업 결과라 보존하며 화면·산출물이 계속 읽는다.\n- 요청 `VlmResultRequest`: `request_id` · `status`(`completed` | `failed`) · `results{description}`(단일 서술) · `error`(문자열)\n- [폐기] `code`·`message` — 두지 않는다(설계 결정). 객체면 실패 콜백이 전량 거부된다.\n- `description`(분석 결과 서술)은 우리가 쓰는 두 축이 받는 유일한 결과 항목이다. `detected`·`accuracy` 는 verify 창구에서만 제공되므로(§2.8) 두 축의 콜백에는 오지 않는다. 저작도구는 verify 를 연동하지 않아 그 두 값이 우리 경로에 생기지 않는다 — 산출물에 일치도가 없는 것은 결함이 아니라 이 연동 범위의 귀결이다.\n- 콜백 본문에는 창구 구분자가 없다. 그래서 창구마다 별개 `request_id` 를 발급하고 원장 채널로 역조회해 어느 축의 결과인지 판정한다 — 이 구조는 창구가 바뀌어도 그대로이며, 바뀐 것은 그 채널이 가리키는 창구 이름뿐이다(추가 질문 채널이 이제 custom 창구의 결과를 받는다).\n- ⚠ 원장 채널 값(식별자 문자열) 자체는 바꾸지 않는다. 창구가 바뀐 것이지 축이 바뀐 것이 아니며, 그 값은 이미 적재된 행의 역조회 키이자 미결 회수의 대상 목록이다. 이름이 새 창구명과 달라 보여도 유지한다.\n- `description` 은 자연어 평문이라 결과 처리는 문자열 파싱에 의존하지 않는다. 예외는 묘사 축의 「상황」 항목 하나뿐이며, 그 줄 단위 형식은 연동 규격 콜백 예시에 명시돼 있다.\n\n## 성공 시 적재·검수 흐름 — 어노테이션 적재 축 (2026-08-25 확정)\n- 성공 시 시계열 메타 적재 + 검수큐 진입을 유지한다.\n- 두 창구의 결과가 이벤트 어노테이션의 어느 칸으로 가는지는 아래로 확정한다.\n  · `video.vd_description` ← 묘사(describe) 전문(장소·날씨·상황·환경·심각성). 종전과 같다.\n  · `event.question` ← 저작도구가 보관하는 검증 이벤트 유형별 질문 문구 목록(마킹에서 고른 값, 없으면 그 유형의 첫 번째). 위탁 응답에서 오지 않는다.\n  · `event.caption.c1.caption_text` ← 추가 질문(custom)의 답변 서술. 창구 교체 이전에 describe-sub 로 받아 적재된 값도 같은 칸에 남아 있으며 지우지 않는다.\n  · `event.caption.c1.cot[\"1단계\"]` ← 묘사 전문 중 「상황」 항목만 파싱한 값.\n- 묘사 축은 적재처가 하나 늘었다 — 시계열 서술 전문에 더해 CoT 1단계로도 흐른다.\n- 「상황」 파싱은 `- 라벨: 값` 줄 단위 형식이 연동 규격 콜백 예시에 명시된 묘사 축에만 적용한다. 「상황」 라벨 줄을 찾아 그 줄의 줄바꿈까지를 값으로 쓰고 다음 라벨 줄로 이어붙이지 않는다.\n- 라벨 목록에 의존하지 않는다 — 규격이 전체 목록·순서·필수 여부를 정의하지 않으므로, 「상황」 줄 하나만 찾는 방식이라야 항목이 늘거나 순서가 바뀌어도 성립한다.\n- 「상황」 줄이 없으면 채우지 않는다(빈 값을 넣지 않는다).\n- CoT 는 1단계 키만 만든다. 2단계 이후 키를 만들지 않고 공란으로 두어 사람이 입력한다.\n- 질문 문장은 이벤트별로 외부 서버가 관리하며 연동 시스템이 지정하지도 응답으로 받지도 않는다. 따라서 이벤트유형별 문구를 지어내지 않고, 저작도구가 보관하는 검증 이벤트 유형별 질문 문구 목록에서 <위탁 시점에> 조달해 요청 본문에 싣고 그 전문을 원장에 보관하며 결과 수신 시 재조달하지 않는다 — 마킹에서 고른 질문이 있으면 그 값, 없으면 그 유형의 첫 번째 질문을 쓴다 — 유형이 미수신이어도 마킹에서 고른 질문이 있으면 그 질문을 쓰고, 실을 문구를 하나도 얻지 못할 때만 비워 둔다.\n- 답변(answer) 칸은 자동으로 채우지 않는다 — 사람이 확정한다.\n- 근거 서술(evidence)은 채우지 않는다 — 자유 서술이라 프레임·객체 지시로 쪼개려면 문자열 파싱이 필요한데, 규격이 그 서술은 형식이 고정돼 있지 않으니 파싱에 의존하지 말라고 못 박는다.\n- 두 창구는 같은 캡션 후보 `c1` 을 공유하고 도착 순서가 보장되지 않는다. 나중에 도착한 축이 먼저 도착한 축의 값을 지우지 않도록, 후보를 통째로 교체하지 않고 자기 칸만 채운다.\n- 기적재분은 백필하지 않는다 — 신규 수신분부터 적용한다.\n- 자동채움은 신규 적재에만 적용하며 기존 보호 경계를 모두 유지한다 — 승인 이력이 있으면 자동채움하지 않고, 값이 이미 있으면 덮지 않으며, 어노테이션 검토가 종결됐으면 하지 않고, 중복 콜백은 멱등 처리하며, 자동채움은 재검수를 발화시키지 않는다.\n- REVIEWER 가 라벨링 캔버스 화면(SCREEN-005)에서 검토·수정·승인한다.\n\n## ⚠ 적재 축은 이틀 만에 두 번 뒤집혔다 — 세 번째로 되돌리지 말 것\n2026-08-24 확정은 「추가 질문 결과는 어노테이션의 답변(answer) 축 초안으로 간다」였고, 그때 그보다 앞선 「묘사 → caption/cot 축」 지시를 명시적으로 폐기했다. 2026-08-25 사업 담당 회신이 그것을 다시 caption/cot 축으로 되돌렸다 — 폐기됐던 앞선 지시가 결과적으로 맞았다. 위 적재 축이 현재 확정이며, 답변(answer) 축 자동채움으로 되돌리지 않는다.\n\n## 인증 — 무서명(벤더 규격) + 3계층 대체 방어\n콜백 규격이 서명 헤더를 제공하지 않으므로 HMAC 미적용(증강 콜백 INT-006 과 다른 지점). 대신:\n1. IP allowlist `webhook.vlm.allowed-ip-cidrs`\n2. rate limit + 본문 크기 상한\n3. `request_id` 발급 게이트 — 저작도구가 발급한 적 없는 request_id 는 거부\n\n## ⚠ 콜백이 아예 오지 않는 사유 — 인증 실패 (v1.2.0 §2.1 신설)\n분석 요청에 `X-API-Key` 가 없거나 값이 다르면 401 이고 그때는 콜백이 전송되지 않는다. 결과가 아무 신호 없이 사라지므로 콜백 미도착을 분석 지연으로만 해석하면 안 되며, 미도착 판정에 인증 실패 사유를 함께 둔다. 키 값과 사용 여부는 규격 밖 별도 전달이라 확인 대기 상태다.\n\n## 멱등·동시성·재전송 (KLID §5.1)\n- 콜백 수신 후 2xx 응답을 5초 내 반환한다(무거운 후속 처리는 반환 후 수행).\n- 벤더는 실패 시 최대 3회·각 5초 간격으로 재전송하며, `request_id` 기준 멱등 처리로 재수신을 흡수한다.\n- 웹훅 멱등 원장으로 `request_id` 기준 멱등 — 이미 PROCESSED 면 재수신 skip.\n- 요청과 콜백의 순서가 일치하지 않을 수 있어 `request_id` 로 매칭한다.\n- 동시 콜백은 비관적 락으로 직렬화한다.\n- 트랜잭션 롤백 시 PROCESSED 마킹도 함께 롤백되어 벤더 재전송을 유도하는 구조다.\n\n## ⚠ dead-letter 부재\n실패는 `VLM_FAILED` 로 종결 실패 상태를 마킹할 뿐이고 자동 재요청 잡을 두지 않는다. 관제 통지(INT-007)가 dead-letter + 백오프 재시도 큐를 갖춘 것과 대비되는 미비점 — 운영 시 수동 재처리 경로가 필요하다.",
      "idempotency": true,
      "triggers_apis": [
        "API-065"
      ],
      "attached_files": [
        "FILE-041"
      ],
      "implementation": {
        "status": "implemented",
        "modules": [],
        "records": [],
        "progress": 100,
        "subtasks": [],
        "module_paths": []
      },
      "pii_in_payload": false,
      "related_events": [],
      "spec_artifacts": [
        "INTSPEC-002"
      ],
      "transport_meta": {
        "path": "/v1/vlm/callback",
        "method": "POST"
      },
      "external_system": "EXTSYS-002",
      "invoked_by_apis": [],
      "used_by_domains": [
        "DOMAIN-005"
      ],
      "endpoints_per_env": [],
      "referenced_in_sequences": [
        "SEQ-023"
      ]
    },
    "data_projected_to": null,
    "schema_version": 1,
    "current_version": 23,
    "stale": false,
    "stale_reason": null,
    "change_summary": "실패 콜백의 `error` 를 객체 `{code, message}` 에서 규격대로 문자열로 정정했다 — 벤더 개발 서버로 실제 위탁해 받은 실패 콜백이 문자열 한 줄로 왔고(실서버 응답으로 문자열임이 확인됨) 연동 규격 §2.7 도 같다. 같은 정정이 콜백 수신 규격(INTSPEC-002)에는 이미 반영돼 있었는데 이 연동 포인트만 옛 서술로 남아, 한 계약이 두 층에 다르게 적혀 있던 드리프트를 닫았다. 구 객체 서술은 지우지 않고 [폐기] 로 남겨 `code`·`message` 칸을 두지 않는다는 설계 결정과 그 근거(객체로 검증하면 실패 콜백이 전량 거부된다)를 적었다. ⚠ 설명 필드가 4000자 상한이고 기준선이 이미 3948자라, 근거의 나머지(실패 사실 자체를 잃는다 · 벤더 재전송 3회 뒤에는 되받을 조회 창구가 없어 영영 유실된다 · 되받을 수 없는 입구에서의 엄격함은 곧 손실이다)는 본문에 싣지 못했다 — 그 전문은 INTSPEC-002 에 이미 그대로 있다. 성공 콜백의 결과 항목 구조, 재전송 최대 3회·각 5초, 멱등 요구, 창구별 별개 요청 식별자와 원장 채널 역조회, verify 미연동 서술은 바이트 그대로 두었고 삭제한 줄은 0건이다.",
    "last_updated_by": "claude",
    "last_updated_at": "2026-09-07T14:26:21.546Z",
    "created_at": "2026-06-01T06:33:52.543Z"
  }
}