---
logicraft_item: ADR-034
type: adr
version: 4
domain: DOMAIN-010
project_id: 4ece2c3f-8e99-46f5-9580-71108a76e578
synced_at: 2026-08-25T10:52:09.960Z
status: CHANGED
prev_version: 3
content_hash: 372f4531706c05e6b68b0247854256e9c8491724a36ade7afb8680ccffe9daef
stale: false
raw: ./_raw/ADR-034.json
links:
  belongs_to_domain: ["[[DOMAIN-010]]"]
  based_on_backward: ["[[API-037]]", "[[API-038]]", "[[SCREEN-026]]"]
  references_backward: ["[[ADR-054]]"]
---

# 라벨 프리셋 = 라벨 마스터 단일 진실원(LBL_ID FK)

## context

프리셋 코드(LS_LABEL_PRESET_CODE)가 라벨명·형태를 스냅샷 저장해 마스터 변경이 기존 프리셋에 반영 안 되던 구조였다.

## decision

### chosen_option

LBL_ID FK로 마스터 실시간 join(채택)

### justification

프리셋 코드는 LBL_ID(FK→LS_LABEL.LBL_ID, nullable)로 마스터를 실시간 join한다. 형태는 마스터 LBL_TYPE_CD가 강제 파생하며(프리셋에서 개별 토글 불가, 구 BBOX_ENABLED/POLYGON_ENABLED 컬럼 제거), 라벨명 유일성은 활성행 LOWER(TRIM(LBL_NM)) 부분 유니크로 강제한다. 매칭 안 되는 기존 코드(labelId null/비활성)는 오류 없이 '미연결'로 표시한다(자동 생성/삭제 없음).

## brownfield

### notes

V117~V119, 라벨명 정규화는 V120/V121.

### status

modified

### change_kind

- decision-record
- data-model

### diff_summary

라벨 프리셋: 라벨명·형태 스냅샷 저장 → LBL_ID FK 실시간 join 단일 진실원

## references

### 커밋 db5afd27(V117~V119)

- **url**: https://example.invalid/commit/db5afd27

### 커밋 3192143f(V120/V121)

- **url**: https://example.invalid/commit/3192143f

## consequences

### neutral

- 프리셋의 실효 여부를 좌우하는 AI 검출 클래스 매핑은 프리셋이 아니라 라벨 마스터가 소유한다. 매칭축을 AI 검출 클래스로 두는 결정 자체는 ADR-019, 매핑이 없거나 무효인 프리셋을 만났을 때의 동작은 ADR-054 가 소유하므로 여기서 다시 정의하지 않는다.

### negative

- 매칭 안 되는 기존 코드는 '미연결' 상태로 남아 별도 정리 작업 필요
- 반대로 라벨 마스터에서 AI 검출 클래스 매핑을 거두면 그 라벨에 기대던 프리셋이 무효가 될 수 있다. 프리셋이 담은 라벨 중 매핑을 가진 것이 하나도 없으면 그 프리셋은 등록돼 있어도 거를 대상을 갖지 못한다.

### positive

- 마스터 변경이 신규·기존 프리셋 모두에 즉시 반영
- 형태(BBOX/POLYGON)가 마스터와 항상 일치
- 라벨 마스터에서 AI 검출 클래스 매핑을 지정하면 그 라벨을 담은 기존 프리셋들이 별도 조작 없이 함께 실효해진다 — 스냅샷이 아니라 실시간 참조 구조라서 그것이 보장된다.

## decision_date

2026-07-21

## decision_drivers

_(empty)_

## considered_options

### 프리셋에 라벨명·형태 스냅샷 저장(구 구조)

**cons**:

- 마스터 변경이 기존 프리셋에 반영되지 않아 드리프트 발생
- 형태 개별 토글 컬럼(BBOX_ENABLED/POLYGON_ENABLED)이 마스터와 불일치 가능

**pros**:

- 조회 시 join 불필요

- **description**: 프리셋 코드가 라벨명·형태를 자체 컬럼에 스냅샷 저장

### LBL_ID FK로 마스터 실시간 join(채택)

**cons**:

- 매칭 안 되는 기존 코드(labelId null/비활성)는 '미연결' 표시 필요

**pros**:

- 마스터 변경이 모든 프리셋에 즉시 반영
- 형태 불일치 원천 차단

- **description**: 프리셋 코드는 LBL_ID(FK→LS_LABEL.LBL_ID, nullable)로 마스터를 실시간 join. 형태는 마스터 LBL_TYPE_CD가 강제 파생(프리셋 개별 토글 불가, 구 컬럼 제거). 라벨명 유일성은 활성행 LOWER(TRIM(LBL_NM)) 부분 유니크로 강제
