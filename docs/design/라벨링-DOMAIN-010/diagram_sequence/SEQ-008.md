---
logicraft_item: SEQ-008
type: diagram_sequence
version: 5
domain: null
project_id: 4ece2c3f-8e99-46f5-9580-71108a76e578
synced_at: 2026-08-16T14:48:54.244Z
status: NEW
prev_version: null
content_hash: afd43ab631ce01c6375f3ba63aa2359203932df634a651901bc018f008ec495e
stale: false
raw: ./_raw/SEQ-008.json
links:
  consumes: ["[[API-014]]"]
  realizes: ["[[UC-007]]"]
---

# 라벨 버전 저장·이력 추적 — 검수 승인 시점 스냅샷 생성

## theme

neutral

## title

라벨 버전 저장·이력 추적 — 검수 승인 시점 스냅샷 생성

## source

sequenceDiagram
    autonumber
    actor RV as 검수자(REVIEWER)
    participant RC as ReviewController
    participant RS as ReviewService
    participant VS as VersionService
    participant DB as PostgreSQL (LS_RAW_DATA_STATUS / LS_LABEL_VERSION)
    participant EP as ApplicationEventPublisher

    RV->>RC: POST /v1/reviews/{videoId}/approve
    RC->>RS: approve(videoId, actor)
    RS->>DB: loadByVideoId + 상태머신 verify(→APPROVED)
    RS->>DB: transitionTo(APPROVED) + TaskEventLog.approve save
    RS->>DB: flush()
    alt OptimisticLock 충돌
        RS-->>RC: 409 CONFLICT
        RC-->>RV: 409
    else 승인 확정
        RS->>VS: commitApproved(rawSn, actor)
        loop 영상 프레임별
            VS->>DB: findBySrcSn (라벨)
            opt 라벨 있음
                VS->>VS: LabelResponse JSON 직렬화 + SHA-256(VERSION_HASH)
                VS->>DB: findActiveForUpdate (비관적 잠금)
                alt 동일 해시(멱등)
                    VS->>VS: 스킵
                else 변경됨
                    VS->>DB: 새 active 버전 save (SAVE_REASON_CD=APPROVED)
                end
            end
        end
        RS->>EP: publish ReviewApprovedEvent(rawSn)
        RS-->>RC: ReviewResponse(APPROVED)
        RC-->>RV: 200 OK
    end

## messages

_(empty)_

## fragments

_(empty)_

## description

ReviewController.approve(POST /v1/reviews/{videoId}/approve, REVIEWER) → ReviewService.approve: IN_REVIEW→APPROVED 전이 + 낙관적 잠금 flush(충돌 시 409) → 같은 트랜잭션에서 VersionService.commitApproved: 영상 프레임 순회, 프레임별 현재 라벨을 LabelResponse JSON 으로 직렬화 → SHA-256 VERSION_HASH → ACTIVE 행 비관적 잠금 후 동일 해시면 멱등 스킵, 아니면 SAVE_REASON_CD=APPROVED 새 active 저장. 끝으로 ReviewApprovedEvent 발행.

## invokes_apis

- API-014

## participants

_(empty)_

## scenario_type

happy_path

## implementation

### status

implemented

### modules

_(empty)_

### records

_(empty)_

### progress

0

### subtasks

_(empty)_

## publishes_events

_(empty)_

## referenced_items

_(empty)_

## realizes_use_cases

- UC-007

## related_acceptances

_(empty)_

## related_requirements

_(empty)_

## referenced_integrations

_(empty)_
