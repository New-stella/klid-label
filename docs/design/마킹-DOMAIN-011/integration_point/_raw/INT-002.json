{
  "item": {
    "id": "INT-002",
    "project_id": "4ece2c3f-8e99-46f5-9580-71108a76e578",
    "type": "integration_point",
    "title": "VLM 시계열 위탁 요청",
    "slug": "vlm-시계열-위탁-요청",
    "sub_id": null,
    "status": "approved",
    "data": {
      "status": "draft",
      "protocol": "rest",
      "auth_type": "bearer",
      "direction": "outbound",
      "brownfield": {
        "status": "new",
        "decided_by": "ADR-046",
        "change_kind": [
          "component-add"
        ],
        "diff_summary": "고도화 신규 — VLM 시계열 위탁 요청"
      },
      "auth_detail": {
        "notes": "`vlm.client.token` 프로퍼티가 설정된 경우에만 `Authorization: Bearer <token>` 헤더를 부착하고, 미설정이면 헤더 없이 호출한다(HTTP 클라이언트 설정에서 조건부로 부착). 즉 인증은 조건부이며 현재 전 프로파일에서 연동 자체가 비활성이라 실사용 이력 없음.",
        "scheme": "Bearer",
        "header_name": "X-Idempotency-Key",
        "token_location": "header"
      },
      "data_format": "json",
      "description": "저작도구가 외부 VLM 서비스에 영상 시계열 메타(설명) 생성을 위탁하는 outbound 호출. 결과 상세는 콜백(INT-003)으로 비동기 수신한다.\n\n## 위탁 대상 (KLID 연동 API v1.1.0)\n저작도구가 연동하는 외부 VLM 위탁 endpoint 는 둘이며, 한 영상에 대해 이중으로 위탁한다.\n- `POST {vlm.client.url}/v1/videovlm-klid/describe` — 묘사(CoT). 이벤트 관점에서 영상 상황(장소·날씨·상황)을 서술한다.\n- `POST {vlm.client.url}/v1/videovlm-klid/describe-sub` — 추가 질문(VQA). 이벤트 발생 여부와 근거를 서술한다.\n- 두 호출은 각각 별도 `request_id` 를 발급하고, 콜백은 그 `request_id` 로 역조회해 어느 위탁의 결과인지 구분한다.\n- 이벤트 판정(detected 반환) endpoint 는 연동하지 않는다.\n\n## 요청 계약\n- 요청 바디: `request_id` · `event_type` · `media{type, source_type, path, frame_policy}` · `callback_url`\n- `frame_policy` 는 `mode` 와 `selected_frames` 로 프레임을 지정한다. 우리는 항상 `frame_selected` mode + `selected_frames` 를 실어 보낸다 — 마킹이 있으면 사용자가 마킹한 프레임 인덱스, 마킹이 없으면 자동 선택된 프레임 리스트. `selected_frames` 상한은 **600**.\n- 프레임 추출 간격·장수 세부값은 연동 시스템이 지정하지 않고 벤더 서버가 관리한다(`frame_policy` 에 추출 간격 필드를 두지 않는다).\n- 동기 응답: `{request_id, status}` — 정상값은 `accepted` 만 인정한다. 수락되면 즉시 파이프라인 다음 단계로 진행하고 결과 상세는 콜백으로 받는다.\n- 호출 주체: 배치 파이프라인의 시계열 위탁 단계\n- 인증: `vlm.client.token` 설정 시에만 `Authorization: Bearer` 부착(조건부)\n- 파일 본문 미전송 — `media.path` 로 경로만 전달\n\n## 서버 상태·이벤트 사전 확인\n- `GET /v1/videovlm-klid/status` 로 서버 처리 가능 여부(ready/busy/loading)를 확인한다.\n- `GET /v1/videovlm-klid/events` 로 지원 이벤트(`describe_events`/`describe_sub_events`) 가용성을 확인한 뒤 위탁한다.\n\n## 보류·재개\n- 신고 구간(`DE_IDNTF_YN='F'`) 위탁은 스텝 내부에서 **보류(SKIPPED)** — 실패가 아니라 보류이며, 해소 시 자동으로 재위탁한다(멱등 조건: 시계열 메타 0건).\n- 응답 신호가 없어도 미결 스위퍼가 회수하고, 검수자가 사유를 남기고 누르는 수동 스킵 게이트로 미연동 구간의 재수행을 통제한다.\n\n## 신뢰성 정책 (실측값)\n- HTTP 클라이언트 타임아웃 `vlm.client.timeout-seconds` 기본 **10s**. 분석 제한 **900s**.\n- Retry `vlmClient`: 3회, 1s, ×2 backoff — **429(동시 처리 한도 32건 초과)는 재시도 대상**이며, 그 외 4xx 는 비재시도 오류로 분류해 재시도·서킷 집계에서 제외한다. `503`(서버 미준비)·`415`(Content-Type)도 처리한다.\n- CircuitBreaker `vlmClient`: failure-rate 50% / sliding-window 10 / min-calls 5 / open 30s\n- 배치는 응답을 기다리지 않고 제출만 개시한다(논블로킹 제출).\n\n## ★★ 활성화 상태 — 설정 토글은 폐지됐다 (ADR-049)\n연동 여부를 설정으로 켜고 끄던 토글은 폐지됐다. 연동 주소가 주입되어 있지 않으면 위탁은 **실패**하며 조용히 건너뛰지 않는다. 구 동작은 토글이 꺼져 있으면 외부 호출 없이 즉시 건너뛴 것으로 처리하고 아무 기록도 남기지 않았는데, 그 기본값이 비활성이라 시계열이 꺼진 채로 납품될 수 있었다.\n\n벤더 미연동 구간의 운영은 **검수자가 사유를 남기고 누르는 시계열 묶음 스킵**으로 처리한다(단건·일괄 모두 제공). 설정으로 끄는 것과 달리 누가 언제 왜 건너뛰었는지가 배치 이력에 남고, 벤더 연동 시점에 해제·재수행으로 되돌릴 수 있다.\n\n⚠ **미연동 구간에는 먼저 스킵한다** — 스킵 없이 돌려 실패시키면 마킹이 위탁 실패 상태로 종결되고, 마킹 상태 전이는 위탁 대기 상태에서만 일어나므로 이후 재수행해도 마킹 표시가 실패로 남는다(시계열 적재·검수 진입·산출물 재생성은 정상 동작한다).\n\n| 환경 | 연동 대상 |\n|---|---|\n| local | 목업 벤더 서버 |\n| dev | 목업 벤더 서버 |\n| stg | 실제 연동 주소 주입 필요 |\n| prd | 실제 연동 주소 주입 필요 — 콜백 허용 출처 설정이 선행돼야 한다 |",
      "idempotency": true,
      "retry_policy": "Resilience4j Retry `vlmClient` — max-attempts 3, wait 1s, 지수 backoff ×2. 429(동시 처리 한도 32건 초과)는 재시도 대상, 그 외 4xx 는 비재시도 오류로 분류해 재시도·서킷 집계에서 제외. 503(서버 미준비)·415(Content-Type)도 처리. CircuitBreaker `vlmClient` — failure-rate 50%, sliding-window 10, min-calls 5, open 30s. 타임아웃: `vlm.client.timeout-seconds` 기본 10s, 분석 제한 900s. 배치는 응답을 기다리지 않고 제출만 개시한다(논블로킹).",
      "triggers_apis": [],
      "implementation": {
        "status": "implemented",
        "modules": [],
        "records": [],
        "progress": 0,
        "subtasks": []
      },
      "pii_in_payload": false,
      "related_events": [],
      "spec_artifacts": [
        "INTSPEC-003"
      ],
      "transport_meta": {
        "path": "/v1/videovlm-klid/describe",
        "notes": "벤더(IntelliVIX Video VLM · KLID 연동 API v1.1.0) 제공 endpoint. 위탁은 묘사(CoT) `POST /v1/videovlm-klid/describe` 와 추가 질문(VQA) `POST /v1/videovlm-klid/describe-sub` 이중 호출이며 각각 별도 request_id 를 발급한다. 위탁 전 `GET /v1/videovlm-klid/status`(ready/busy/loading)·`GET /v1/videovlm-klid/events`(지원 이벤트) 로 처리 가능 여부를 확인한다. base-url 은 `vlm.client.url`.",
        "method": "POST"
      },
      "external_system": "EXTSYS-002",
      "invoked_by_apis": [],
      "used_by_domains": [
        "DOMAIN-005"
      ],
      "endpoints_per_env": [],
      "referenced_in_sequences": []
    },
    "data_projected_to": null,
    "schema_version": 1,
    "current_version": 16,
    "stale": true,
    "stale_reason": "INTSPEC-003의 data.effective_date 변경 — references",
    "change_summary": "위탁 대상 API를 verify 단일에서 describe(CoT)+describe-sub(VQA) 이중 위탁으로 교체한다(KLID 연동 API v1.1.0 정합). 경로를 /v1/videovlm/verify 에서 /v1/videovlm-klid/describe·/v1/videovlm-klid/describe-sub 로 바꾸고, 각각 별도 request_id 발급·콜백 역조회로 구분한다. frame_policy 에서 추출 간격(framerate) 필드를 제거하고 항상 frame_selected mode + selected_frames(상한 600, 구 8 폐기)로 프레임을 지정한다. 위탁 전 status(ready/busy/loading)·events 사전 확인, 429(동시 처리 한도 32건 초과) 재시도 대상 분류, 503·415 처리, 분석 제한 900s 를 추가한다. 동기 응답 형식·신고구간 보류(SKIPPED)·미결 스위퍼·수동 스킵 게이트·논블로킹 제출·환경별 표는 보존한다. 벤더 표기를 KLID 연동 API v1.1.0 으로 정합한다.",
    "last_updated_by": "claude",
    "last_updated_at": "2026-08-24T07:45:11.619Z",
    "created_at": "2026-06-01T06:33:45.748Z"
  }
}