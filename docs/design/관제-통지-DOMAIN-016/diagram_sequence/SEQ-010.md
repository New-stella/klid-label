---
logicraft_item: SEQ-010
type: diagram_sequence
version: 19
domain: null
project_id: 4ece2c3f-8e99-46f5-9580-71108a76e578
synced_at: 2026-09-15T13:21:53.429Z
status: CHANGED
prev_version: 18
content_hash: 24a0f6b258c5bdffa7b69d97a1d77eb9078ce4ec0f890e8d7616e7e34375ae1c
stale: false
raw: ./_raw/SEQ-010.json
links:
  consumes: ["[[API-014]]", "[[API-019]]"]
  implements: ["[[IMPREC-381]]"]
  realizes: ["[[UC-009]]"]
  references: ["[[INT-007]]", "[[INT-010]]"]
  triggers: ["[[EVT-004]]", "[[EVT-009]]"]
---

# 데이터마트 수정 통지 — 검수완료 후 수정부터 TASK_MODIFIED push까지

## theme

neutral

## title

데이터마트 수정 통지 — 검수완료 후 수정부터 TASK_MODIFIED push까지

## source

sequenceDiagram
    actor reviewerWorker as 검수자/작업자
    participant labelCtrl as LabelController
    participant labelSvc as LabelService
    participant deidentGate as DeidentReportGate
    participant eventPublisher as ApplicationEventPublisher
    participant accumulateListener as TaskModifiedAccumulateListener
    participant recheckMarkListener as ReviewRecheckMarkListener
    participant reviewSvc as ReviewService
    participant notifyDebouncer as ControlNotifyDebouncer
    participant exportRunner as AsyncDatasetExportRunner
    participant outputFolder as 산출 폴더 v{n+1}
    participant notifySvc as ControlNotifyService
    participant notifyClient as ControlNotifyClient
    participant controlServer as 관제서버[외부]

    reviewerWorker->>labelCtrl: PUT /v1/frames/{srcSn}/labels (검수완료 후 수정)
    labelCtrl->>labelSvc: bulkUpsert(srcSn, req, actor)
    labelSvc->>deidentGate: isUnderDeidentReport(rawSn) — 락 검사보다 <먼저>
    alt DE_IDNTF_YN='F' (비식별 재처리 대기)
        deidentGate-->>labelCtrl: 412 — 락 유무와 무관하게 통일(409 로 갈리면 응답이 잠금 상태 오라클)
    end
    alt 정상
        labelSvc->>labelSvc: 라벨 full-replace upsert
        alt isReviewApproved(rawSn)=true
            labelSvc->>eventPublisher: TaskModifiedEvent(rawSn, srcSn, LABEL_UPDATED, exportRegenerated=true)
        end
        alt 검수 전
            labelSvc->>labelSvc: 통지 미발행
        end
        labelCtrl-->>reviewerWorker: 200 OK
    end
    Note over eventPublisher: 트랜잭션 커밋 이후 — 수정 축적은 통지 토글과 무관하게 항상 돌아간다
    eventPublisher->>accumulateListener: onTaskModified(event) — 영상 1건 단위 디바운스 축적
    eventPublisher->>recheckMarkListener: onTaskModified(needsRecheck) → 재검토 표시 REVLT_YN='Y'
    Note over accumulateListener: 표시가 서 있는 동안 만료 flush <보류> — flush 후보 조회와 클레임 UPDATE 두 곳에서 축적분 제외
    reviewerWorker->>reviewSvc: POST /v1/reviews/{videoId}/approve — 그 수정을 다시 검수해 승인. 일괄 승인 창구로 여러 건을 한 번에 승인해도 그 건은 여기와 똑같이 재승인으로 처리된다
    reviewSvc->>recheckMarkListener: clearNeedsRecheck() — 표시 해제, 축적 윈도우가 풀린다
    Note over reviewSvc: 재승인은 ReviewApprovedEvent 를 발행하지 않는다(중복 방지) — 재생성·통지는 디바운서 flush 가 직접 낸다(승인 경로의 DatasetExportBridge·DatasetExportCompletedEvent·ControlNotifyEventListener 를 거치지 않는다)
    notifyDebouncer->>notifyDebouncer: 표시 해제 후 다음 flush tick — 풀린 윈도우를 원자 클레임(조건부 UPDATE)
    notifyDebouncer->>exportRunner: runReExportThenNotify(rawSn, force=true, afterExport=sendModified 콜백) — 재생성 동반 윈도우는 export 를 먼저 마치게 위임
    exportRunner->>outputFolder: 새 버전 폴더 v{n+1} 으로 <전량 재생성> (이미지 2벌 포함, 승인 경로와 동일 산출)
    Note over exportRunner: 버전마다 전체 자기완결 + 전 버전 보존(retention 정리 없음) — "복구" 요구 충족
    alt export SUCCEEDED
        exportRunner->>notifySvc: afterExport.run() = sendModified(rawSn, 축적된 프레임 변경 · 영상 단위 변경 종류, true) — 같은 @Async 스레드에서 export 종결 이후 실행(순서 보장)
        notifySvc->>notifySvc: VersionExplanationPolicy 로 ver_expln 판정 (단일 원천 — 복제 금지)
        Note over notifySvc: ver_expln 은 optional — null 이면 <키를 생략>한다(NON_NULL). 완료 통지의 "required 는 null 이어도 키를 남긴다"와 의도적 비대칭
        notifySvc->>notifyClient: sendTaskModified(수정 통지 본문)
        notifyClient->>controlServer: TASK_MODIFIED (job_id · changed_items(images·jsons = 산출 폴더 파일명) · ver_expln(선택) · output_ver_no(선택))
        alt 전송 성공
            controlServer->>controlServer: 저작도구 조회 API 호출 → 1행 UPSERT (산출 폴더는 OUTPUT_PATH_NM 으로 픽업)
        end
        alt 전송 실패
            notifySvc->>notifySvc: fallbackService.enqueuePending (dead-letter / 재등록 큐)
        end
    end
    alt export 실패
        Note over exportRunner: afterExport 콜백을 실행하지 않아 통지 <보류> — DatasetExportFailureRecoverer 가 재산출 성공 시 runApprovalAsync 로 DatasetExportCompletedEvent 를 재발행해 재개(sendCompleted 409 자기치유로 정합, 유실이 아니라 지연)
    end
    Note over labelSvc: 수정 횟수와 무관하게 재승인 1건 = 새 버전 폴더 1개 = 통지 1건
    Note over labelSvc: 같은 재-export·재통지 경로를 타는 수정은 사람이 콘텐츠를 고치는 모든 경로다 (예 — 라벨 · 트랙 편집 · 트랙 병합 · 버전 롤백 · 촬영환경 · 프레임 설명 · 개인정보 메타 · 메타 수동 편집 · 이벤트 어노테이션)
    Note over reviewSvc: [일괄 승인 경로도 같다] 일괄 승인으로 들어온 재검수 건도 재승인 분기를 건너뛰지 않는다 — 상태 전이 없이 표시가 해제되고 축적된 변경분으로 새 버전이 재생성된 뒤 수정 통지가 나간다. 연쇄는 뒤에서 줄 세운다


## messages

### [1]

- **to**: labelCtrl
- **from**: reviewerWorker
- **kind**: sync
- **label**: PUT /v1/frames/{srcSn}/labels (검수완료 후 수정)

### [2]

- **to**: labelSvc
- **from**: labelCtrl
- **kind**: sync
- **label**: bulkUpsert(srcSn, req, actor)

### [3]

- **to**: deidentGate
- **from**: labelSvc
- **kind**: sync
- **label**: isUnderDeidentReport(rawSn) — 락 검사보다 <먼저>

### [4]

- **to**: labelCtrl
- **from**: deidentGate
- **kind**: return
- **label**: 412 — 락 유무와 무관하게 통일(409 로 갈리면 응답이 잠금 상태 오라클)

### [5]

- **to**: labelSvc
- **from**: labelSvc
- **kind**: self
- **label**: 라벨 full-replace upsert

### [6]

- **to**: eventPublisher
- **from**: labelSvc
- **kind**: sync
- **label**: TaskModifiedEvent(rawSn, srcSn, LABEL_UPDATED, exportRegenerated=true)

### [7]

- **to**: labelSvc
- **from**: labelSvc
- **kind**: self
- **label**: 통지 미발행

### [8]

- **to**: reviewerWorker
- **from**: labelCtrl
- **kind**: return
- **label**: 200 OK

### [9]

- **to**: eventPublisher
- **from**: eventPublisher
- **kind**: note
- **label**: 트랜잭션 커밋 이후 — 수정 축적은 통지 토글과 무관하게 항상 돌아간다

### [10]

- **to**: accumulateListener
- **from**: eventPublisher
- **kind**: sync
- **label**: onTaskModified(event) — 영상 1건 단위 디바운스 축적

### [11]

- **to**: recheckMarkListener
- **from**: eventPublisher
- **kind**: sync
- **label**: onTaskModified(needsRecheck) → 재검토 표시 REVLT_YN='Y'

### [12]

- **to**: accumulateListener
- **from**: accumulateListener
- **kind**: note
- **label**: 표시가 서 있는 동안 만료 flush <보류> — flush 후보 조회와 클레임 UPDATE 두 곳에서 축적분 제외

### [13]

- **to**: reviewSvc
- **from**: reviewerWorker
- **kind**: sync
- **label**: POST /v1/reviews/{videoId}/approve — 그 수정을 다시 검수해 승인. 일괄 승인 창구로 여러 건을 한 번에 승인해도 그 건은 여기와 똑같이 재승인으로 처리된다

### [14]

- **to**: recheckMarkListener
- **from**: reviewSvc
- **kind**: sync
- **label**: clearNeedsRecheck() — 표시 해제, 축적 윈도우가 풀린다

### [15]

- **to**: reviewSvc
- **from**: reviewSvc
- **kind**: note
- **label**: 재승인은 ReviewApprovedEvent 를 발행하지 않는다(중복 방지) — 재생성·통지는 디바운서 flush 가 직접 낸다(승인 경로의 DatasetExportBridge·DatasetExportCompletedEvent·ControlNotifyEventListener 를 거치지 않는다)

### [16]

- **to**: notifyDebouncer
- **from**: notifyDebouncer
- **kind**: self
- **label**: 표시 해제 후 다음 flush tick — 풀린 윈도우를 원자 클레임(조건부 UPDATE)

### [17]

- **to**: exportRunner
- **from**: notifyDebouncer
- **kind**: sync
- **label**: runReExportThenNotify(rawSn, force=true, afterExport=sendModified 콜백) — 재생성 동반 윈도우는 export 를 먼저 마치게 위임

### [18]

- **to**: outputFolder
- **from**: exportRunner
- **kind**: sync
- **label**: 새 버전 폴더 v{n+1} 으로 <전량 재생성> (이미지 2벌 포함, 승인 경로와 동일 산출)

### [19]

- **to**: exportRunner
- **from**: exportRunner
- **kind**: note
- **label**: 버전마다 전체 자기완결 + 전 버전 보존(retention 정리 없음) — "복구" 요구 충족

### [20]

- **to**: notifySvc
- **from**: exportRunner
- **kind**: sync
- **label**: afterExport.run() = sendModified(rawSn, 축적된 프레임 변경 · 영상 단위 변경 종류, true) — 같은 @Async 스레드에서 export 종결 이후 실행(순서 보장)

### [21]

- **to**: notifySvc
- **from**: notifySvc
- **kind**: self
- **label**: VersionExplanationPolicy 로 ver_expln 판정 (단일 원천 — 복제 금지)

### [22]

- **to**: notifySvc
- **from**: notifySvc
- **kind**: note
- **label**: ver_expln 은 optional — null 이면 <키를 생략>한다(NON_NULL). 완료 통지의 "required 는 null 이어도 키를 남긴다"와 의도적 비대칭

### [23]

- **to**: notifyClient
- **from**: notifySvc
- **kind**: sync
- **label**: sendTaskModified(수정 통지 본문)

### [24]

- **to**: controlServer
- **from**: notifyClient
- **kind**: sync
- **label**: TASK_MODIFIED (job_id · changed_items(images·jsons = 산출 폴더 파일명) · ver_expln(선택) · output_ver_no(선택))

### [25]

- **to**: controlServer
- **from**: controlServer
- **kind**: self
- **label**: 저작도구 조회 API 호출 → 1행 UPSERT (산출 폴더는 OUTPUT_PATH_NM 으로 픽업)

### [26]

- **to**: notifySvc
- **from**: notifySvc
- **kind**: self
- **label**: fallbackService.enqueuePending (dead-letter / 재등록 큐)

### [27]

- **to**: exportRunner
- **from**: exportRunner
- **kind**: note
- **label**: afterExport 콜백을 실행하지 않아 통지 <보류> — DatasetExportFailureRecoverer 가 재산출 성공 시 runApprovalAsync 로 DatasetExportCompletedEvent 를 재발행해 재개(sendCompleted 409 자기치유로 정합, 유실이 아니라 지연)

### [28]

- **to**: labelSvc
- **from**: labelSvc
- **kind**: note
- **label**: 수정 횟수와 무관하게 재승인 1건 = 새 버전 폴더 1개 = 통지 1건

### [29]

- **to**: labelSvc
- **from**: labelSvc
- **kind**: note
- **label**: 같은 재-export·재통지 경로를 타는 수정은 사람이 콘텐츠를 고치는 모든 경로다 (예 — 라벨 · 트랙 편집 · 트랙 병합 · 버전 롤백 · 촬영환경 · 프레임 설명 · 개인정보 메타 · 메타 수동 편집 · 이벤트 어노테이션)

### [30]

- **to**: reviewSvc
- **from**: reviewSvc
- **kind**: note
- **label**: [일괄 승인 경로도 같다] 일괄 승인으로 들어온 재검수 건도 재승인 분기를 건너뛰지 않는다 — 상태 전이 없이 표시가 해제되고 축적된 변경분으로 새 버전이 재생성된 뒤 수정 통지가 나간다. 연쇄는 뒤에서 줄 세운다

## fragments

### [1]

- **kind**: alt
- **label**: DE_IDNTF_YN='F' (비식별 재처리 대기)
- **end_index**: 3
- **start_index**: 3

### [2]

- **kind**: alt
- **label**: 정상
- **end_index**: 7
- **start_index**: 4

### [3]

- **kind**: alt
- **label**: isReviewApproved(rawSn)=true
- **end_index**: 5
- **start_index**: 5

### [4]

- **kind**: alt
- **label**: 검수 전
- **end_index**: 6
- **start_index**: 6

### [5]

- **kind**: alt
- **label**: export SUCCEEDED
- **end_index**: 25
- **start_index**: 19

### [6]

- **kind**: alt
- **label**: 전송 성공
- **end_index**: 24
- **start_index**: 24

### [7]

- **kind**: alt
- **label**: 전송 실패
- **end_index**: 25
- **start_index**: 25

### [8]

- **kind**: alt
- **label**: export 실패
- **end_index**: 26
- **start_index**: 26

## description

검수 완료(APPROVED) 영상을 수정했을 때 관제서버로 TASK_MODIFIED 를 push 하는 흐름. LabelService.bulkUpsert 는 먼저 비식별 신고 게이트를 평가해 DE_IDNTF_YN='F' 면 412 로 차단하고(락 검사보다 먼저 — 응답 코드가 잠금 상태 오라클이 되지 않게 409/412 로 갈리지 않게 한다), 정상이면 full-replace upsert 후 isReviewApproved 가 true 일 때만 TaskModifiedEvent(exportRegenerated=true)를 발행한다. ★재생성·통지의 트리거 = 재검수 승인이고 발송 시점 = export 성공 이후다(2026-08-07 확정, 구속) — 수정 이벤트는 항상 활성인 TaskModifiedAccumulateListener(AFTER_COMMIT)가 영상 1건 단위로 디바운스 축적하고, 같은 이벤트가 재검토 표시(LS_RAW_DATA_STATUS.REVLT_YN='Y')를 세워 그 표시가 서 있는 동안 만료 flush 를 보류시킨다(LsMonNotiAcmlRepository 가 flush 후보 조회와 클레임 UPDATE 두 곳에서 축적분을 제외한다). 검수자가 그 수정을 다시 검수해 승인하고 ReviewService.approve 가 표시를 해제하면, 풀린 축적분을 다음 flush tick(ControlNotifyDebouncer)이 AsyncDatasetExportRunner.runReExportThenNotify 로 직접 위임해(승인 경로의 DatasetExportBridge 를 거치지 않는다) 산출물을 <새 버전 폴더 v{n+1} 으로 전량 재생성>한 뒤, SUCCEEDED 가 되면 그 러너가 afterExport 콜백으로 ControlNotifyService.sendModified 를 같은 스레드에서 실행해 비로소 통지를 보낸다(승인 경로처럼 DatasetExportCompletedEvent → ControlNotifyEventListener 를 거치지 않는다). 재승인은 ReviewApprovedEvent 를 발행하지 않으므로(중복 방지) 재생성과 TASK_MODIFIED 는 디바운서가 내고, 최초 승인만 그 이벤트로 강제 재생성 후 TASK_COMPLETED 를 낸다. 수정 횟수와 무관하게 재승인 1건이 새 버전 폴더 1개와 통지 1건에 대응한다. 수정 후 재승인 전까지 관제는 직전 승인본을 본다 — 학습데이터는 검수를 통과한 것만 확정이므로 인지하고 수용한 선택이다. ⚠ 구 정책 — 디바운스 창이 만료되는 것만으로 재생성·통지가 나간다 — 는 폐기됐다. export 가 @Async 라 통지가 앞서면 관제가 V_COMPLETED_VIDEO.OUTPUT_PATH_NM 을 조회할 때 <구 버전 폴더>를 픽업하게 되기 때문이다. export 가 실패하면 통지를 <보류>하고 DatasetExportFailureRecoverer 가 재산출 성공 후 재개한다(통지 유실이 아니라 성공 시점으로 지연). ⚠ 재-export 트리거(DatasetExportBridge·수정 축적·디바운스 flush)는 authoring.control-notify.enabled 토글과 <무관하게 항상 동작>한다 — 그 토글은 발송(sendCompleted/sendModified)만 게이팅하며, 그래서 수정 이벤트 소비가 ControlNotifyEventListener 에서 분리된 것이다(구 구조에선 토글 off 시 수정 이벤트가 소비자 없이 드롭돼 export 가 재생성되지 않았다). 페이로드는 다음으로 구성된다 — job_id(RAW_SN) + changed_items(images·jsons = 산출 폴더의 실제 파일명 {FRM_NO 4자리 zero-pad}.jpg/.json) + ver_expln(선택) + output_ver_no(선택). changed_items 의 범위는 export 재생성을 동반했는지로 갈린다 — 동반이면 전 프레임 이미지·JSON 을 싣고(비우면 관제가 재픽업을 안 해 보유본이 stale 로 고착된다), 재생성이 없으면 빈 리스트가 정상이며(없는 파일을 실으면 관제가 404 를 맞는다) 어느 경우든 통지 자체는 발송된다. output_ver_no 는 이 통지가 대응하는 산출 폴더 버전 번호이고 ver_expln 과 같은 규약으로 null 이면 키를 생략하며, 관제는 키 없음을 "산출물 변경 없음 — 재픽업 불요" 로 읽는다. ★ 마지막 수정 일시 · 변경 종류(LABEL_ADDED|LABEL_UPDATED|LABEL_DELETED|META_UPDATED) · 변경 요약 카운트 · 요청 ID 는 지금은 관제로 보내지 않는다 — 변경 종류는 내부 디바운스 축적 키와 감사 기록으로만 쓰고, 요청 식별자(UUID)는 폴백·재등록 큐의 내부 키다. data_info 도 싣지 않는다(관제 명세에 키 스키마가 없어 추정 스키마로 만들면 422 로 거부되거나 잘못된 값이 적재된다). 라벨/메타 본문 데이터는 포함하지 않으며 관제는 수신 후 저작도구 조회 API 를 호출해 상세를 가져간다. ver_expln 은 관제 dataset_versions.ver_expln(NOT NULL) 조달용이고 판정 단일 원천은 VersionExplanationPolicy 다(복제 금지). optional 이라 null 이면 키를 생략하는데(@JsonInclude NON_NULL) 이는 NOT NULL 컬럼에 명시적 null 을 보내는 것보다 안전하기 때문이며, 완료 통지(TASK_COMPLETED)의 "required 는 null 이어도 키를 남긴다"와 정반대인 <의도된 비대칭>이다. 전송 실패 시 fallbackService.enqueuePending 으로 dead-letter·재등록 큐에 쌓는다. 같은 재-export·재통지 경로를 타는 수정은 개수가 아니라 성질로 정해진다 — 사람이 콘텐츠를 고치는 모든 경로가 대상이고 제외는 넷뿐이다(외부 시계열 콜백 · 비식별 누락 신고 접수 · 운영자 정정 배치 · 이벤트 어노테이션 지연 승인). 판정 기준은 사람이 산출물에 들어가는 내용을 바꿨는가 하나다. 예시 — 라벨 수정(LabelService) · 트랙 편집(TrackEditService) · 트랙 병합(TrackMergeService) · 버전 롤백(VersionService) · 촬영환경 수정(EnvironmentMetaService) · 프레임 설명 수정(FrameDescriptionService) · 개인정보 메타 수정(영상 축 VideoPrivacyMetaService · 프레임 축 FramePrivacyMetaService) · 메타 수동 편집(MetaService) · 이벤트 어노테이션 저장(EvntAnnoService). 산출물은 버전마다 전체 자기완결이고 전 버전을 보존한다(retention 정리 로직을 만들지 않는다) — 델타만 두면 사업 요구의 '복구(rollback)' 가 성립하지 않기 때문이며, OUTPUT_PATH_NM 은 버전 루트가 아니라 영상 루트({rawSn})를 가리켜 관제가 v1·v2 를 한 경로 아래에서 보고 고를 수 있게 한다. 경로·필드명은 관제 정본과 정합 완료이며, 인증만 미확정으로 남아 관제 회신 대기 중이다 — 관제 정본은 x-access-token 헤더를 요구하나 아웃바운드 통지는 인증 헤더를 부착하지 않는다. 일괄 승인 창구로 들어온 재검수 건도 이 흐름을 그대로 탄다 — 재승인 분기를 건너뛰지 않으므로 상태 전이 없이 재검토 필요 표시가 해제되고, 그 사이 축적된 변경분으로 산출 폴더가 새 버전으로 전량 재생성된 뒤 수정 통지가 나간다. 여러 건이 한꺼번에 승인되면 그 연쇄가 몰리지 않도록 뒤에서 줄 세우며, 줄을 세워도 건별 결과는 단건 승인과 완전히 같다.

## invokes_apis

- API-019
- API-014

## participants

### 검수자/작업자

- **id**: reviewerWorker
- **kind**: actor

### LabelController

- **id**: labelCtrl
- **kind**: service

### LabelService

- **id**: labelSvc
- **kind**: service

### DeidentReportGate

- **id**: deidentGate
- **kind**: service

### ApplicationEventPublisher

- **id**: eventPublisher
- **kind**: system

### TaskModifiedAccumulateListener

- **id**: accumulateListener
- **kind**: service

### ReviewRecheckMarkListener

- **id**: recheckMarkListener
- **kind**: service

### ReviewService

- **id**: reviewSvc
- **kind**: service

### ControlNotifyDebouncer

- **id**: notifyDebouncer
- **kind**: service

### AsyncDatasetExportRunner

- **id**: exportRunner
- **kind**: service

### 산출 폴더 v{n+1}

- **id**: outputFolder
- **kind**: system

### ControlNotifyService

- **id**: notifySvc
- **kind**: service

### ControlNotifyClient

- **id**: notifyClient
- **kind**: service

### 관제서버[외부]

- **id**: controlServer
- **kind**: external
- **extsys_ref**: EXTSYS-005

## scenario_type

happy_path

## attached_files

_(empty)_

## implementation

### status

implemented

### modules

_(empty)_

### records

- IMPREC-381

### progress

100

### subtasks

_(empty)_

### last_updated

2026-08-29T01:26:56.700Z

### module_paths

_(empty)_

## publishes_events

- EVT-004
- EVT-009

## referenced_items

_(empty)_

## realizes_use_cases

- UC-009

## related_acceptances

_(empty)_

## related_requirements

_(empty)_

## referenced_integrations

- INT-007
- INT-010
