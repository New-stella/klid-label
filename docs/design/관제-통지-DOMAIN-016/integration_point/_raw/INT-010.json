{
  "item": {
    "id": "INT-010",
    "project_id": "4ece2c3f-8e99-46f5-9580-71108a76e578",
    "type": "integration_point",
    "title": "데이터마트 View 제공(관제 픽업 경로)",
    "slug": "데이터마트-view-제공관제-픽업-경로",
    "sub_id": null,
    "status": "draft",
    "data": {
      "status": "draft",
      "protocol": "jdbc",
      "auth_type": "basic",
      "direction": "outbound",
      "brownfield": {
        "status": "new",
        "decided_by": "ADR-037",
        "change_kind": [
          "component-add"
        ],
        "diff_summary": "데이터마트 View 4종 직접 픽업 표면을 별도 연동 포인트로 신규 등록 — 기존에는 EXTSYS-005/INT-007(outbound 통지)·INT-007(inbound 조회 API) 등록에만 있고 이 View 픽업 축 자체가 ITEM 으로 없었다."
      },
      "auth_detail": {
        "notes": "DB 계정 기반 뷰 SELECT — 별도 애플리케이션 레벨 인증 없음(DB 접근제어에 위임)."
      },
      "data_format": "other",
      "description": "관제서버가 검수 완료(APPROVED) 영상의 학습데이터를 데이터마트로 가져가기 위해 픽업하는 경로. 저작도구는 `klid_at` 스키마에 4종 View(`V_COMPLETED_VIDEO`/`V_COMPLETED_FRAME`/`V_COMPLETED_LABEL_CHANGE`/`V_COMPLETED_META`)를 DB 계정으로 노출하고, 관제가 이를 SELECT 한다. 기존 EXTSYS-005/INT-007 등록에는 outbound 통지(INT-007)와 inbound 조회 API만 명시돼 있었고 이 View 픽업 표면 자체가 별도 ITEM 으로 없었다.\n\n## 확정된 부분\n- `V_COMPLETED_VIDEO.OUTPUT_PATH_NM`(개명 전 `EXPORT_PATH_NM`) 이 가리키는 export 산출 루트는 **`{원본영상디렉터리}/{rawSn}/`**(관제 회신, 2026-07-27) — co-locate 모델로 확정.\n- 라벨 본문은 V114(ADR-037) 뷰 슬림화 이후 뷰에 없음 — export 폴더 JSON 이 유일한 출처(`V_COMPLETED_LABEL_CHANGE`는 변경점만).\n- 비식별 영상 경로는 `V_COMPLETED_VIDEO.DE_IDNTF_FILE_PATH_NM`(V138)로 노출.\n- 파생영상(증강·해상도, `ORGNL_RAW_SN` not null)은 `V_COMPLETED_VIDEO.ORGNL_VDO_PATH_NM`(개명 전 `ORIGINAL_VIDEO_PATH`) 가 NULL 로 동결된다 — 파생영상은 원본영상 자체가 없고 비식별 사본만 있기 때문이다. 관제는 파생 비디오를 `DE_IDNTF_FILE_PATH_NM`(V138)으로 픽업해야 한다(관제 협의 대상).\n\n- 프레임만 이관한 원본은 `V_COMPLETED_VIDEO.DE_IDNTF_YN` 이 미수행으로 나가고 `DE_IDNTF_FILE_PATH_NM` 도 빈 값일 수 있다 — 비식별할 영상 자체가 없으므로 그것이 사실이며, 비식별의 실체는 프레임 축에 있다. 승인된 영상이므로 뷰에서 감추거나 비우지 않는다. 관제는 이 조합을 결함으로 보지 않아야 하며, 해석을 바꾸려면 협의가 선행된다(관제 협의 대상). 이 예외의 근거 결정은 ADR-023 이 소유한다.\n- 검수 완료·통지된 영상은 어떤 사유(비식별 신고 등)로도 뷰에서 감추거나 경로를 비우지 않는다(2026-07-28 확정 정책).\n- 완료 영상 뷰의 썸네일파일경로명은 저작도구가 보유한 비식별 프레임 중 첫 번째(프레임 인덱스가 가장 작고 폐기가 아니며 경로가 비어 있지 않고 원본 프레임 경로와 같지 않은 것)의 절대경로다. 마지막 조건은 프레임 페어 뷰가 쓰는 비식별 경로 불변식 게이트와 같은 것이며, 두 뷰가 어긋나면 프레임 페어 뷰에서는 빠진 프레임이 대표 이미지로 나가 마스킹 전 화면의 경로가 노출된다. 관제가 보낸 값을 그대로 넘기던 구 사양은 폐기됐다. 파생영상도 부모가 아니라 자기 프레임에서 조달한다. 프레임이 없으면 비며, 원본 프레임 경로로 폴백하지 않는다 — 폴백하면 마스킹 전 화면이 대표 이미지로 나간다.\n\n## ★ 미확정 — 관제 워커의 실제 픽업 방식 (§6 B-1, 회신 대기)\n관제 워커가 (a) export 루트 폴더를 통짜로 픽업(rsync/cp 류)하는지, (b) DB에서 파일별 경로를 읽어 개별 fetch 하는지 **미회신**이다. 이 프로토콜 상세는 회신 전까지 등록하지 않는다 — 본 ITEM 은 \"View 4종을 통해 관제가 데이터를 가져간다\"는 확정 사실만 담는다.\n\n## ★ 미확정 — 관제 정본 대응 ITEM (§6 B-2/B-3)\n관제 LogiCraft 정본의 `INT-017`(NAS 공유마운트)·`INT-018`(LS_DATA_* JDBC 직접조회)이 이 빈틈에 대응하는 것으로 추정되나 **유효성 미회신**. 우리 실제 `LS_DATA_*` 테이블은 15개(관제 ADR-067의 \"9테이블\" 서술과 불일치)이며, 비식별 영상 경로는 `LS_DEIDENT_PROC_LOG.DE_IDNTF_FILE_PATH_NM`(LS_DATA_* 밖)에 있다.\n\n## 뷰에 일치도 값이 없는 것은 의도다 — 신규로는 값 자체가 생기지 않는다\n우리가 연동하는 서술 생성(CoT)·질의응답(VQA) 호출은 일치도와 판정을 반환하지 않는다 — 그 둘은 이벤트 검증 호출 전용이다. 따라서 신규 영상에서는 일치도(`vlm.accuracy`) 값 자체가 만들어지지 않는다. 이미 적재된 과거 행은 작업 결과라 삭제하지 않고 보존하지만, 검토 행을 내부 조인하는 `V_COMPLETED_META` 에 구조적으로 닿지 않고 학습데이터 산출 JSON 에도 넣지 않는다. 뷰에 나오지 않는 것이 결함이 아니므로 이 값을 노출하려고 뷰를 고치지 않는다.",
      "idempotency": true,
      "triggers_apis": [],
      "implementation": {
        "status": "planned",
        "modules": [],
        "records": [],
        "progress": 0,
        "subtasks": [],
        "module_paths": []
      },
      "pii_in_payload": false,
      "related_events": [],
      "spec_artifacts": [],
      "transport_meta": {
        "path": "klid_at 스키마 — V_COMPLETED_VIDEO / V_COMPLETED_FRAME / V_COMPLETED_LABEL_CHANGE / V_COMPLETED_META (4종)",
        "notes": "재적용해도 결과가 같도록 정의한다 — 출력 컬럼의 타입이 바뀌는 재정의는 기존 뷰를 지우고 다시 만들고, 그렇지 않으면 있는 뷰를 그대로 대체한다. 관제가 이 뷰들을 직접 SELECT 한다.",
        "method": "GET"
      },
      "external_system": "EXTSYS-005",
      "invoked_by_apis": [],
      "used_by_domains": [
        "DOMAIN-016"
      ],
      "endpoints_per_env": [],
      "referenced_in_sequences": []
    },
    "data_projected_to": null,
    "schema_version": 1,
    "current_version": 12,
    "stale": false,
    "stale_reason": null,
    "change_summary": "대표 이미지 선택 조건에 비식별 경로 불변식 게이트를 더한다. 프레임 페어 뷰가 이미 걸러내는 형태(비식별 경로가 원본 경로와 같은 행)를 대표 이미지 선택도 함께 걸러야 두 뷰가 어긋나지 않는다. 어긋나면 프레임 페어 뷰에서는 빠진 프레임이 대표 이미지로 나가 마스킹 전 화면의 경로가 노출된다.",
    "last_updated_by": "claude",
    "last_updated_at": "2026-08-25T06:28:18.234Z",
    "created_at": "2026-07-30T07:07:12.617Z"
  }
}