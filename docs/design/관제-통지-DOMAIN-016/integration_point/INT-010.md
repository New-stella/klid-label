---
logicraft_item: INT-010
type: integration_point
version: 9
domain: DOMAIN-016
project_id: 4ece2c3f-8e99-46f5-9580-71108a76e578
synced_at: 2026-08-21T00:02:56.126Z
status: CHANGED
prev_version: 6
content_hash: 2c6d09be76a73d1cc82d0f96fd13dda801753674ace40a02088411764bb08b20
stale: false
raw: ./_raw/INT-010.json
links:
  belongs_to_domain: ["[[DOMAIN-016]]"]
  provided_by: ["[[EXTSYS-005]]"]
  depends_on_backward: ["[[DOMAIN-016]]"]
  references_backward: ["[[SEQ-010]]"]
---

# 데이터마트 View 제공(관제 픽업 경로)

## status

draft

## protocol

jdbc

## auth_type

basic

## direction

outbound

## brownfield

### status

new

### decided_by

ADR-037

### change_kind

- component-add

### diff_summary

데이터마트 View 4종 직접 픽업 표면을 별도 연동 포인트로 신규 등록 — 기존에는 EXTSYS-005/INT-007(outbound 통지)·INT-007(inbound 조회 API) 등록에만 있고 이 View 픽업 축 자체가 ITEM 으로 없었다.

## auth_detail

### notes

DB 계정 기반 뷰 SELECT — 별도 애플리케이션 레벨 인증 없음(DB 접근제어에 위임).

## data_format

other

## description

관제서버가 검수 완료(APPROVED) 영상의 학습데이터를 데이터마트로 가져가기 위해 픽업하는 경로. 저작도구는 `klid_at` 스키마에 4종 View(`V_COMPLETED_VIDEO`/`V_COMPLETED_FRAME`/`V_COMPLETED_LABEL_CHANGE`/`V_COMPLETED_META`)를 DB 계정으로 노출하고, 관제가 이를 SELECT 한다. 기존 EXTSYS-005/INT-007 등록에는 outbound 통지(INT-007)와 inbound 조회 API만 명시돼 있었고 이 View 픽업 표면 자체가 별도 ITEM 으로 없었다.

## 확정된 부분
- `V_COMPLETED_VIDEO.OUTPUT_PATH_NM`(개명 전 `EXPORT_PATH_NM`) 이 가리키는 export 산출 루트는 **`{원본영상디렉터리}/{rawSn}/`**(관제 회신, 2026-07-27) — co-locate 모델로 확정.
- 라벨 본문은 V114(ADR-037) 뷰 슬림화 이후 뷰에 없음 — export 폴더 JSON 이 유일한 출처(`V_COMPLETED_LABEL_CHANGE`는 변경점만).
- 비식별 영상 경로는 `V_COMPLETED_VIDEO.DE_IDNTF_FILE_PATH_NM`(V138)로 노출.
- 파생영상(증강·해상도, `ORGNL_RAW_SN` not null)은 `V_COMPLETED_VIDEO.ORGNL_VDO_PATH_NM`(개명 전 `ORIGINAL_VIDEO_PATH`) 가 NULL 로 동결된다 — 파생영상은 원본영상 자체가 없고 비식별 사본만 있기 때문이다. 관제는 파생 비디오를 `DE_IDNTF_FILE_PATH_NM`(V138)으로 픽업해야 한다(관제 협의 대상).

- 프레임만 이관한 원본은 `V_COMPLETED_VIDEO.DE_IDNTF_YN` 이 미수행으로 나가고 `DE_IDNTF_FILE_PATH_NM` 도 빈 값일 수 있다 — 비식별할 영상 자체가 없으므로 그것이 사실이며, 비식별의 실체는 프레임 축에 있다. 승인된 영상이므로 뷰에서 감추거나 비우지 않는다. 관제는 이 조합을 결함으로 보지 않아야 하며, 해석을 바꾸려면 협의가 선행된다(관제 협의 대상). 이 예외의 근거 결정은 ADR-023 이 소유한다.
- 검수 완료·통지된 영상은 어떤 사유(비식별 신고 등)로도 뷰에서 감추거나 경로를 비우지 않는다(2026-07-28 확정 정책).

## ★ 미확정 — 관제 워커의 실제 픽업 방식 (§6 B-1, 회신 대기)
관제 워커가 (a) export 루트 폴더를 통짜로 픽업(rsync/cp 류)하는지, (b) DB에서 파일별 경로를 읽어 개별 fetch 하는지 **미회신**이다. 이 프로토콜 상세는 회신 전까지 등록하지 않는다 — 본 ITEM 은 "View 4종을 통해 관제가 데이터를 가져간다"는 확정 사실만 담는다.

## ★ 미확정 — 관제 정본 대응 ITEM (§6 B-2/B-3)
관제 LogiCraft 정본의 `INT-017`(NAS 공유마운트)·`INT-018`(LS_DATA_* JDBC 직접조회)이 이 빈틈에 대응하는 것으로 추정되나 **유효성 미회신**. 우리 실제 `LS_DATA_*` 테이블은 15개(관제 ADR-067의 "9테이블" 서술과 불일치)이며, 비식별 영상 경로는 `LS_DEIDENT_PROC_LOG.DE_IDNTF_FILE_PATH_NM`(LS_DATA_* 밖)에 있다.

## 뷰에 일치도 값이 없는 것은 의도다
시계열 메타 중 일치도(`vlm.accuracy`)는 검수 대기열에 넣지 않으므로 검토 행을 내부 조인하는 `V_COMPLETED_META` 에 구조적으로 닿지 않고, 학습데이터 산출 JSON 에도 넣지 않는다. 뷰에 나오지 않는 것이 결함이 아니므로 이 값을 노출하려고 뷰를 고치지 않는다.

## idempotency

true

## triggers_apis

_(empty)_

## implementation

### status

planned

### modules

_(empty)_

### records

_(empty)_

### progress

0

### subtasks

_(empty)_

## pii_in_payload

false

## related_events

_(empty)_

## spec_artifacts

_(empty)_

## transport_meta

### path

klid_at 스키마 — V_COMPLETED_VIDEO / V_COMPLETED_FRAME / V_COMPLETED_LABEL_CHANGE / V_COMPLETED_META (4종)

### notes

전부 `CREATE OR REPLACE VIEW` — Flyway 재실행해도 멱등. 관제가 이 4종 뷰를 직접 SELECT.

### method

GET

## external_system

EXTSYS-005

## invoked_by_apis

_(empty)_

## used_by_domains

- DOMAIN-016

## endpoints_per_env

_(empty)_

## referenced_in_sequences

_(empty)_
