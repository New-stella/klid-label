---
logicraft_item: UC-006
type: use_case
version: 7
status: CHANGED
prev_version: 6
raw: UC-006.json
---

> ⚠️ **버전 변경 감지 — logicraft v6 → v7**
> change_summary: 라벨링 화면 변경에 따른 재확인. 이번 화면 변경은 저장 충돌 안내와 프레임 폐기 상태 확정 축이고 이 유스케이스의 축은 단순화 허용오차를 영속 설정과 요청 단위로 조절하는 것이라 겹치지 않는다. 폐기된 화면이나 인터페이스를 가리키는 참조도 없어 본문은 바꾸지 않는다.
> ↳ 요약/구현 노트 재검토 후 작성된 코드에 반영. 직전 요약은 git diff 확인.

# 라벨링 정밀도 조절

## goal

폴리곤 단순화 정밀도(epsilon)를 조절하여 밀착/세그멘테이션 결과의 경계 세밀함(점 수)을 제어한다.

## actor

검수자(REVIEWER)

## title

라벨링 정밀도 조절

## status

draft

## priority

should

## main_flow

### [1]

- **step**: 1
- **actor**: 검수자(REVIEWER)
- **action**: PUT /v1/manage/configs/{key}로 POLYGON_SIMPLIFY_TOLERANCE를 조절한다(영속 기본값)

### [2]

- **step**: 2
- **action**: 시스템이 설정값을 저장하고 캐시(TTL 60s)에 반영한다

### [3]

- **step**: 3
- **action**: 이후 SAM2 track/밀착 시 설정 epsilon으로 폴리곤 점 수를 단순화한다

### [4]

- **step**: 4
- **actor**: 라벨링 작업자(WORKER)
- **action**: 라벨링 화면 AI Tool 모달에서 confThreshold/simplifyTolerance를 요청 단위 1회성으로 override한다(비영속)

### [5]

- **step**: 5
- **action**: 설정 조회 실패 시 기본값(1.0px)으로 fail-safe 폴백한다

## brownfield

### notes

2026-07-30: AI Tool 모달의 1회성(비영속) confThreshold/simplifyTolerance 조절 반영 — 영속 시스템 설정(POLYGON_SIMPLIFY_TOLERANCE)과는 별개 메커니즘

### status

new

### change_kind

- capability-add

### diff_summary

폴리곤 단순화 정밀도(epsilon) 설정 기반 제어 신규

## description

REVIEWER가 시스템 설정에서 POLYGON_SIMPLIFY_TOLERANCE(Douglas-Peucker epsilon)를 조절하면 이후 SAM2 track/밀착 시 폴리곤 점 수가 단순화된다(영속 설정, Caffeine 캐시 TTL 60s, 조회 실패 시 기본값 1.0px fail-safe). ★1회성 조절 확장(2026-07-30) — 이와 별개로 라벨링 화면 AI Tool 모달에서 confThreshold(YOLO conf/iou)·simplifyTolerance를 요청마다 1회성(비영속)으로 override할 수 있다(WORKER·REVIEWER 공통, 시스템 설정 변경 없이 해당 요청에만 적용). 두 메커니즘은 별개다 — 시스템 설정은 REVIEWER 전용 영속 기본값, AI Tool 모달 조절은 요청 단위 임시값이다. (R2 UC-006, RQ-SFR-08-03)

## stakeholders

_(empty)_

## preconditions

- REVIEWER로 인증되어 있고 폴리곤 단순화 설정 키가 존재한다

## implementation

### status

planned

### modules

_(empty)_

### records

_(empty)_

### progress

0

### subtasks

_(empty)_

## postconditions

- 조절된 정밀도가 적용된 라벨 결과가 반환된다

## alternate_flows

### 값 범위 초과

**steps**:

- @Valid 검증으로 400 반환·안내

- **trigger**: 허용 형식/범위 초과

### 권한 부족

**steps**:

- 설정 변경은 REVIEWER 전용이므로 403 반환

- **trigger**: WORKER 호출

## realizes_dfeats

_(empty)_

## related_screens

- SCREEN-025
- SCREEN-005

## extension_points

_(empty)_

## secondary_actors

_(empty)_

## extends_use_cases

_(empty)_

## realizes_features

- FEAT-001

## includes_use_cases

_(empty)_

## tech_considerations

_(empty)_

## covered_by_acceptances

- AC-006
