# 디자인 시스템 (verbatim)

<!-- DS-001 v9 -->

# KRDS Public

## name

KRDS Public

## layout

### notes

간격은 4px 배수 토큰(xs 4 / sm 8 / md 16 / lg 24 / xl 40 / 2xl 64)만 쓰고 임의 px 를 직접 입력하지 않는다. 내부 저작도구라 콘텐츠 최대 폭을 제한하지 않고 화면을 채운다(목록·캔버스가 넓을수록 유리). 좌측 LNB 는 240px 고정, 상단 GNB 는 56px 고정.

### gutter_px

16

### base_unit_px

4

### card_padding_px

24

### hero_padding_px

64

### section_padding_px

40

## tokens

### colors

#### neutral

##### hex

#6d7882

##### scale

- #f4f5f6
- #e6e8ea
- #cdd1d5
- #b1b8be
- #8a949e
- #6d7882
- #58616a
- #464c53
- #33363d
- #1e2124
- #131416

#### primary

##### hex

#256ef4

##### scale

- #ecf2fe
- #d8e5fd
- #b1cefb
- #86aff9
- #4c87f6
- #256ef4
- #0b50d0
- #083891
- #052561
- #03163a
- #020f27

#### semantic

##### info

###### hex

#0b78cb

###### scale

- #e7f4fe
- #d3ebfd
- #9ed2fa
- #5fb5f7
- #2098f3
- #0b78cb
- #096ab3
- #085691
- #053961
- #03253f
- #021a2c

##### warn

###### hex

#9e6a00

###### scale

- #fff3db
- #ffe0a3
- #ffc95c
- #ffb114
- #c78500
- #9e6a00
- #8a5c00
- #614100
- #422c00
- #2e1f00
- #241800

##### error

###### hex

#de3412

###### scale

- #fdefec
- #fcdfd9
- #f7afa1
- #f48771
- #f05f42
- #de3412
- #bd2c0f
- #8a240f
- #5c180a
- #390d05
- #260903

##### success

###### hex

#228738

###### scale

- #eaf6ec
- #d8eedd
- #a9dab4
- #7ec88e
- #3fa654
- #228738
- #267337
- #285d33
- #1f4727
- #122b18
- #0e2012

#### secondary

##### hex

#346fb2

##### scale

- #eef2f7
- #d6e0eb
- #bacbde
- #90b0d5
- #6b96c7
- #346fb2
- #1c589c
- #063a74
- #052b57
- #031f3f
- #02162c

### motion

#### easing

##### standard

cubic-bezier(0.2, 0, 0, 1)

##### emphasized

cubic-bezier(0.3, 0, 0, 1)

#### duration

##### base

200ms

##### fast

120ms

##### slow

320ms

### radius

#### lg

8px

#### md

6px

#### sm

4px

#### full

9999px

### shadow

#### lg

0 10px 24px rgba(14,21,40,0.12)

#### md

0 4px 10px rgba(14,21,40,0.08)

#### sm

0 1px 2px rgba(14,21,40,0.06)

### spacing

#### base

4

#### scale

##### lg

24px

##### md

16px

##### sm

8px

##### xl

40px

##### xs

4px

##### 2xl

64px

### typography

#### body

##### scale

1.125

##### family

Pretendard GOV

##### weight

- 400
- 500

##### base_size

17px

#### mono

##### family

D2Coding

##### weight

- 400

##### base_size

14px

#### label

##### family

Pretendard GOV

##### weight

- 600

##### base_size

14px

#### headline

##### scale

1.2

##### family

Pretendard GOV

##### weight

- 600
- 700
- 800

##### base_size

18px

## do_rules

- 본문 17px 이상 + line-height 1.6 이상으로 가독성을 확보한다.
- 모든 인터랙티브 요소에 focus ring 3px + 2px offset 을 명시한다(focus-visible 한정).
- 버튼·링크·입력의 hit area 를 최소 44x44px 로 보장한다.
- 에러는 색만 쓰지 말고 아이콘과 텍스트를 함께 표시한다(색각 이상 배려).
- 상태 배지는 색과 함께 한글 라벨을 항상 표기한다.
- 폼 라벨은 label[for] 로 입력과 명시 연결하고, 도움말은 aria-describedby 로 잇는다.
- 타이포는 typography_use ladder 의 step 을 의미대로 배정한다 — 폼 라벨·배지는 label, 도움말·캡션은 caption, 읽고 편집하는 텍스트는 body 계열이다.
- 간격·radius·shadow 는 토큰 값만 사용하고 임의 px 를 직접 입력하지 않는다.
- 표 헤더 배경은 secondary 스케일의 가장 옅은 단계를 쓴다 — 페이지 배경과 같은 색을 쓰면 열 구조가 먼저 읽히지 않는다.
- 표의 행 hover 표면은 #FFFBEB 를 쓴다 — 긴 표에서 커서가 짚은 행을 확실히 알린다. 회색 계열은 표면 배경과 겹쳐 구분이 약하다.
- 카드 표면에는 shadow.sm 만 쓴다 — md·lg 는 오버레이·팝오버 전용이다.
- 회색 표면(neutral 0~20단) 위의 보조 텍스트는 neutral 50단이 아니라 60단 이상을 쓴다 — 50단은 흰 배경에서만 AA 를 통과하고(4.51) 회색 표면 위에선 미달이다(neutral 0 위 4.13, 보조색 0 위 4.01). 60단은 세 배경 모두 통과한다(5.60~6.30).
- 범주 구분색(이벤트 유형·라벨 형태·역할처럼 우열 없이 서로 대등한 분류)은 semantic 이 선점한 대역을 피해 8슬롯 팔레트에서만 고른다 — 기준색은 순서대로 #2B7FFF · #AD46FF · #00B8DB · #00BBA7 · #615FFF · #F6339A · #62748E · #E12AFB 이고, 배경은 각 색 스케일의 가장 옅은 두 단계(50·100), 전경은 700 단계를 쓴다. 이벤트·라벨 형태·역할 세 축이 이 한 팔레트를 공유하며 축마다 별도 색표를 만들지 않는다. 슬롯 번호는 우열이나 순서를 뜻하지 않는다. 반대로 상태·성패·경고 전달에는 쓰지 않는다 — 그건 semantic 색이 담당하며, semantic 이 선점한 빨강·호박·초록 대역을 범주색으로 끌어오면 error·warn 신호가 무뎌진다.

## archetype

krds

## is_in_use

true

## applies_to

정부·공공기관 웹/모바일 서비스, 민원·신청·고지서 페이지, 법령·정책 안내

## dont_rules

- 장식적 일러스트·그라데이션·neumorphism 을 쓰지 않는다 — 정보 우선.
- Brand 색을 큰 면적 배경으로 채우지 않는다(대비·피로감).
- Pill 버튼을 기본으로 쓰지 않는다 — 격식이 떨어진다. pill 은 status badge 전용.
- 본문에 weight 300 이하를 쓰지 않으며, Pretendard 외 한글 폰트를 혼용하지 않는다.
- 색만으로 상태를 구분하지 않는다 — 항상 아이콘 또는 텍스트를 동반한다.
- Auto-play 영상·캐러셀을 쓰지 않는다(접근성 가이드 위배). 단 라벨링 대상 영상의 사용자 조작 재생은 이에 해당하지 않는다.
- 클릭 가능한 요소를 div 로 만들지 않는다 — button 또는 a 를 쓴다.
- 공통 컴포넌트가 있는 요소를 화면에서 raw 마크업으로 다시 만들지 않는다 — 토큰 갱신이 전파되지 않는다.

## known_gaps

- 색상 팔레트는 KRDS 공식 토큰 CSS 정본(--krds-color-light-*)에서 그대로 가져온 값이다(2026-08-06 교체). 구 팔레트(#0F4C97 계열 6종)는 정본에 한 건도 등장하지 않는 출처 미기록 값이었다. design_system 스키마에 source/reference 필드가 없어 출처는 이 overview_md·known_gaps 에만 기록된다.
- typography_use 의 15단 ladder 수치는 아직 우리 도출값이다. KRDS 원문은 --krds-pc-font-size-* 로 step 별 크기를 실제로 명시하며(display 60/44/36, heading 40/32/24/19/17/15, body 19/17/15/13, label 19/17/15/13 px 상당), tokens.typography.body.base_size 17px 은 KRDS body-medium(1.7rem)과 일치한다. 그러나 label 14px 은 KRDS label 계열(19/17/15/13) 어디와도 맞지 않고 headline 18px x1.2 도 KRDS heading 단계와 다르다. ladder 를 KRDS 단계로 전환하는 것은 tailwind 토큰 재매핑으로 코드 850곳에 파급되므로 별건으로 남긴다.
- button 과 nav-link 의 크기 17px 은 KRDS label-medium(1.7rem=17px)과 일치함이 정본 대조로 확인됐다(구 known_gap 해소). 다만 굵기 500 은 우리 결정이다 — KRDS 는 --krds-typo-font-weight 로 Regular/Bold 2종만 규정한다.
- responsive_breakpoints 는 KRDS 실사용 미디어쿼리에 767 / 1023 / 1280 이 존재함을 확인해 현재 값(tailwind md 768 / xl 1280)과 정합함을 검증했다. 다만 KRDS 공식 문서가 breakpoint 표를 규범으로 명시하는지는 미확인이다.
- KRDS 토큰은 rem 단위이며 1rem=10px(root font-size 62.5%)로 추정된다 — --krds-number-* 스케일(0.2/0.4/0.6/0.8/1rem)이 2/4/6/8/10px 로 떨어지고 body-medium 1.7rem 이 17px 과 맞아떨어지기 때문이다. 그러나 html{font-size:62.5%} 선언 자체는 수신한 CSS 에서 확인하지 못했다. 이 ITEM 이 px 로 적은 수치는 그 전제 위에 있다.
- desktop_grid_cols 와 max_content_width_px 는 근거를 확인하지 못해 비워 뒀다. 내부 저작도구는 콘텐츠 폭을 제한하지 않는 편이 유리하다는 판단만 layout.notes 에 적었다.
- 팔레트 교체로 대비 실측이 전면 재검증 대상이 됐다. 주조색은 정본이 구 값보다 밝아 대비 여유가 줄었다 — #256EF4 는 흰 배경에서 4.55:1 로 AA(4.5:1)를 통과하나 여유가 거의 없고(구 #0F4C97 은 8.40:1), primary-5(#ECF2FE) 위에서는 4.05:1 로 본문 기준 미달이다(UI 요소 3:1 기준은 통과). 흰 글자 on primary 는 4.55:1, hover(#0B50D0)는 6.83:1 로 통과한다. 이는 KRDS 가 정한 값이므로 수용하되, 본문 텍스트를 primary 표면 위에 올리지 않는 것으로 회피한다.
- semantic warn 토큰이 #C25700 에서 KRDS 정본 #9E6A00 으로 바뀌었으나 같은 성격의 한계가 남는다 — 흰 배경 4.65:1 로 AA 를 통과하지만 warn 표면(#FFF3DB) 위 4.23:1, gray-5 위 4.26:1 로 미달이다. 앱의 상태 배지 다수가 이 조합이므로 배지 전경/배경 조합 재점검이 필요하다(별건).
- 코드의 회색은 stock Tailwind gray 이며 tokens.colors.neutral(KRDS gray 계열 #6D7882)과 다르다. 전 화면 파급이라 별건으로 남겨 뒀던 이 전환을 지금 진행한다.
- 행 hover 표면 #FFFBEB 는 KRDS 정본 팔레트에 없는 값이다. 표 안에서 커서가 짚은 행을 알리려면 표면 배경과 구분되는 색이 필요한데 정본에 그 용도의 토큰이 없어 채택했다. 정본에 대응 토큰이 생기면 교체한다.
- 범주 구분색 8슬롯은 KRDS 정본 팔레트에 없는 값이다. 정본은 primary·secondary·neutral·semantic(info·warn·error·success) 만 정의하는데, 이벤트 유형 8종·라벨 형태 5종·역할 3종처럼 우열 없이 대등한 분류를 가를 색이 그 안에 없다. semantic 에 억지로 배정하면 「작업자는 위험」처럼 읽히지 않아야 할 의미가 생기므로 별도 대역을 채택했다. 행 hover 표면(#FFFBEB)과 같은 처리이며, 정본에 데이터 시각화용 팔레트가 생기면 교체한다. 8색 모두 옅은 배경 위에서 WCAG AA(4.5:1)를 통과한다 — 최저 4.71:1. 대체 대상이던 호박(4.52:1)·주황(4.56:1)보다 여유가 넓다.

## color_usage

### [1]

- **token**: primary
- **use_for**: GNB, 주요 액션 버튼, 활성 메뉴, 링크, 포커스 링, 진행 바.
- **avoid_for**: 넓은 면적의 배경 채움. 대비와 눈 피로 때문에 금지한다. 강조는 면적이 아니라 위계로 만든다.

### [2]

- **token**: secondary
- **use_for**: 본문 내 링크, 보조 강조 요소, 표 헤더 배경(가장 옅은 단계).
- **avoid_for**: 주요 액션 버튼. primary 와 경쟁해 어느 쪽이 주행동인지 흐려진다.

### [3]

- **token**: neutral
- **use_for**: 본문·보조 텍스트, 테두리, 구분선, 비활성 상태, 표면 배경.
- **avoid_for**: 성공·실패 같은 의미 전달. 의미는 semantic 색이 담당한다.

### [4]

- **token**: success
- **use_for**: 검수 승인, 배치 완료, 저장 성공.
- **avoid_for**: 단순 강조나 장식. 의미가 맞을 때만 쓴다.

### [5]

- **token**: warn
- **use_for**: 검토 중, 주의가 필요한 상태, 되돌릴 수 있는 경고.
- **avoid_for**: 확정 실패. 그건 error 다.

### [6]

- **token**: error
- **use_for**: 검수 반려, 배치 실패, 입력 검증 실패, 파괴적 액션.
- **avoid_for**: 단순 강조. 빨강의 경고 신호가 무뎌진다.

### [7]

- **token**: info
- **use_for**: 안내 배너, 도움말 강조. primary 와 같은 색이므로 액션이 아님이 문맥으로 분명할 때만 쓴다.
- **avoid_for**: 클릭 가능해 보여야 하는 요소. primary 와 구분되지 않는다.

## description

대한민국 정부 디자인시스템(KRDS) 공식 토큰 기반 공공서비스용 디자인 시스템. KRDS light 팔레트(주조색 #256EF4) + Pretendard GOV, WCAG AA 강제, 큰 hit area 와 명확한 focus ring 으로 고령층·접근성 우선.

## overview_md

대한민국 정부 디자인시스템(KRDS)을 학습데이터 저작도구에 적용한 디자인 시스템. 색상 토큰은 **KRDS 공식 토큰 CSS 정본**(https://www.krds.go.kr/resources/css/token/krds_tokens.css — `--krds-color-light-*`, 2026-08-06 수신)에서 그대로 가져왔으며 주조색 #256EF4 + Pretendard GOV + WCAG AA 강제가 정체성이다. 장식보다 정보 위계와 가독성이 절대 우선한다.

다만 이 시스템의 적용 대상은 민원·고지 페이지가 아니라 **장시간 연속 작업하는 내부 저작도구**다. 라벨링 캔버스·검수 목록·배치 현황처럼 한 화면에 정보 밀도가 높은 영역이 많다. 그래서 KRDS 의 접근성 하한(본문 크기·대비·포커스 링·터치 타깃)은 그대로 지키되, 밀집 패널에서도 크기 예외를 두지 않고 ladder step 을 의미대로 배정한다 — 좁으면 글자를 줄이지 말고 열 수를 줄인다. **앱에 다크 표면은 없다**(2026-08-06 다크 테마 폐지 확정). 유일한 예외는 영상 프레임을 얹는 미디어 매트와 그 위 오버레이(프레임번호 배지·모달 스크림)뿐이다.

이 ITEM 이 진실원이며 코드가 이를 따른다. 같은 사업의 관제서버 프로젝트 DS-001 도 동일한 KRDS 정본 값을 사용하므로, 사업 전체에서 색상 축이 한 값으로 정렬돼 있다.

## preset_source

krds-public

## typography_use

### [1]

- **step**: display-xl
- **weight**: 800
- **size_px**: 45
- **use_for**: 최상위 히어로 제목. 저작도구 내부 화면에는 사용처가 거의 없다.
- **line_height**: 1.15

### [2]

- **step**: display-lg
- **weight**: 800
- **size_px**: 37
- **use_for**: 랜딩·안내 페이지 대제목.
- **line_height**: 1.2

### [3]

- **step**: display-md
- **weight**: 700
- **size_px**: 31
- **use_for**: 통계 대시보드의 핵심 수치 등 시선을 먼저 끌어야 하는 숫자.
- **line_height**: 1.25

### [4]

- **step**: display-sm
- **weight**: 700
- **size_px**: 26
- **use_for**: KPI 카드 수치.
- **line_height**: 1.3

### [5]

- **step**: title-lg
- **weight**: 700
- **size_px**: 22
- **use_for**: 페이지 제목(PageHeader h1).
- **line_height**: 1.4

### [6]

- **step**: title-md
- **weight**: 600
- **size_px**: 18
- **use_for**: 섹션·카드 제목.
- **line_height**: 1.45

### [7]

- **step**: title-sm
- **weight**: 600
- **size_px**: 17
- **use_for**: 패널 소제목, 테이블 헤더 그룹명.
- **line_height**: 1.5

### [8]

- **step**: body-lg
- **weight**: 400
- **size_px**: 19
- **use_for**: 강조 본문, 안내 문단 도입부.
- **line_height**: 1.6

### [9]

- **step**: body-md
- **weight**: 400
- **size_px**: 17
- **use_for**: 기본 본문. 화면의 읽는 텍스트는 원칙적으로 이 단계 이상을 쓴다.
- **line_height**: 1.6

### [10]

- **step**: body-sm
- **weight**: 400
- **size_px**: 15
- **use_for**: 보조 설명·메타 정보(파일 크기, 갱신 일시 등). 본문 대체용이 아니다 — 읽기 위한 본문은 17px 이상.
- **line_height**: 1.6

### [11]

- **step**: label
- **weight**: 600
- **size_px**: 14
- **use_for**: 폼 라벨, 상태 배지, 태그, 칩.
- **line_height**: 1.4

### [12]

- **step**: caption
- **weight**: 400
- **size_px**: 14
- **use_for**: 입력 도움말, 캡션, 각주.
- **line_height**: 1.5

### [13]

- **step**: button
- **weight**: 500
- **size_px**: 17
- **use_for**: 버튼 텍스트. 44px 터치 타깃과 함께 쓴다.
- **line_height**: 1.4

### [14]

- **step**: nav-link
- **weight**: 500
- **size_px**: 17
- **use_for**: GNB·LNB 메뉴 항목.
- **line_height**: 1.4

### [15]

- **step**: mono
- **weight**: 400
- **size_px**: 14
- **use_for**: D2Coding. 식별자(rawSn, 해시), 좌표, 로그 등 자릿수 정렬이 필요한 값.
- **line_height**: 1.5

## iteration_guide

- 새 컴포넌트는 먼저 카탈로그(ui_component)에서 같은 역할이 있는지 찾고, 있으면 재사용한다. 없을 때만 신설한다.
- 색은 tokens 의 base hex 만 쓰고, 톤 변형이 필요하면 같은 scale 안에서만 이동한다. 새 hex 를 도입하지 않는다.
- 타이포는 typography_use ladder 의 step 만 쓴다. 표에 없는 크기를 임의로 만들지 않는다.
- 밀집 작업 패널이라도 크기 예외를 두지 않는다 — 그 요소가 어떤 step 인지 정하고 그 값을 쓴다. 세로 밀도는 44px 터치 타깃이 지배하므로 글자를 줄여도 이득이 작고, 대신 가로 잘림만 심해진다. 좁으면 크기를 낮추지 말고 열 수를 줄인다.
- spacing / radius / shadow / motion 은 tokens 표의 값만 쓴다.
- semantic 색(success/warn/error/info)은 의미가 맞을 때만 쓰고 단순 강조에 쓰지 않는다.
- 화면은 라이트 단일 기준이다 — 다크 변형 컴포넌트를 만들지 않는다. 영상 프레임을 얹는 매트와 그 위 오버레이만 예외로 어두운 색을 쓴다.
- 접근성 속성(role, aria-*, label 연결)은 컴포넌트가 기본 제공하고, 화면이 매번 다시 붙이게 하지 않는다.

## shape_principle

Radius 4-8px 의 단정한 라운드. 카드·입력·버튼 모두 6-8px 로 통일하고, pill(full) 은 status badge 에만 예외적으로 허용한다. 둥근 정도가 클수록 격식이 떨어진다는 공공서비스 통념을 따른다.

## elevation_levels

### sm

- **use**: 카드, 목록 행, 패널 등 배경에서 한 겹 떠 있는 표면.
- **treatment**: 0 1px 2px rgba(14,21,40,0.06)

### md

- **use**: 드롭다운, 팝오버, 툴팁 등 일시적으로 겹치는 표면.
- **treatment**: 0 4px 10px rgba(14,21,40,0.08)

### lg

- **use**: 모달, 드로어 등 화면 전체를 막고 뜨는 표면.
- **treatment**: 0 10px 24px rgba(14,21,40,0.12)

## font_substitutes

본문·제목은 KRDS 지정 **Pretendard GOV**(정부 전용 서브셋, 오픈소스)를 웹폰트로 직접 서빙한다. 코드·수치 병기용 D2Coding 도 오픈소스이며 동일하게 자체 서빙한다(폐쇄망 대응 — CDN 의존 금지). 미탑재 환경 폴백은 Pretendard → 시스템 한글 산세리프(Apple SD Gothic Neo, Noto Sans KR) 순이며, 폴백 상황에서도 한글 본문에 다른 계열 폰트를 혼용하지 않는다.

## key_characteristics

- Brand 블루 #256EF4 — 정부 디자인시스템 정본 주조색. 흰 배경 대비 4.55:1 로 AA 를 통과하나 여유가 없어 본문 텍스트를 primary 표면 위에 올리지 않는다
- Pretendard 본문 17px 이상 + line-height 1.6 이상 — 고령층·장시간 작업 가독성
- WCAG 2.1 AA 이상 대비 강제 (본문 4.5:1, 큰 텍스트 3:1)
- Focus ring 3px outline + 2px offset, focus-visible 한정 — 키보드 접근성
- Hit area 최소 44x44px — 모바일 터치 및 접근성 가이드
- Radius 4-8px 단정한 라운드, pill(full) 은 status badge 전용
- 색만으로 상태를 구분하지 않는다 — 항상 아이콘 또는 텍스트 동반
- 장식·그라데이션·일러스트 최소 — 정보 전달 우선

## whitespace_principle

정보 위계 우선. 섹션 사이 40-64px 여백으로 작업 단계를 구분하고, 카드 안쪽 24px, 폼 필드 사이 16-24px 을 둔다. 답답함보다 명확함이 우선이다. 라벨링·검수의 우측 작업 패널처럼 한 화면에서 많은 항목을 동시에 봐야 하는 영역은 예외적으로 8-16px 까지 좁히되, 터치 타깃 44px 하한은 그 안에서도 유지한다. 밀도는 글자 크기를 낮춰서가 아니라 여백과 레이아웃으로 확보한다.

## responsive_breakpoints

### mobile

- **width**: < 768px
- **key_changes**: LNB 를 숨기고 오버레이로 전환. 표는 카드형으로 스택. 라벨링 캔버스 등 정밀 작업 화면은 이 폭을 지원 대상으로 보지 않는다.

### tablet

- **width**: >= 768px
- **key_changes**: LNB 노출, 2열 레이아웃 허용. 포털 사용자 화면의 주 대상 폭.

### desktop

- **width**: >= 1280px
- **key_changes**: 라벨링·검수의 3분할(사이드바 + 캔버스 + 우측 패널) 레이아웃. 내부 작업자 화면의 기준 폭.


---

<!-- DS-002 v1 -->

# KLID 포털 플랫폼 (포털 채널)

## name

KLID 포털 플랫폼 (포털 채널)

## layout

### notes

콘텐츠 최대폭 1200 + 좌우 24 를 헤더·본문·푸터가 **같은 정렬선**으로 공유한다 (킷 기본 wrap 1248 · 푸터 패딩 리셋을 --krds-contents-wrap-size: 120rem + 푸터 inner 패딩 복원으로 본문 체계에 맞춤). 단 본문의 '맨바닥에 서는 줄'(표면 없이 배경 위에 글자만 얹힌 줄 — 페이지 제목·브레드크럼·리드문·결과 머리·구획 소제목)은 좌우로 8 들여쓴다: 흰 면을 가진 것(카드·표 셸·필터 패널)은 보더 안쪽에서 글이 시작하는데 맨바닥 줄이 0 에서 시작하면 옆 면들보다 바깥으로 튀어나온 것처럼 읽히기 때문이다. **기준선은 8 자리다.** 면을 가진 것과 그것을 담은 줄에는 주지 않는다.

세로 리듬은 3단 사다리 20 / 32 / 48 (비율 1 : 1.6 : 2.4) + 페이지 섹션 80(히어로 96~128). 이 3단 밖의 값(24·28)을 같은 자리에 섞지 않는다. 헤더 다음 첫 요소가 덩어리진 구획(필터바·탭줄·검색 카드)이면 20 대신 32. 결과 머리(건수·정렬 줄)는 위 32 · 아래 12 — 목록을 설명하는 줄이지 별개 구획이 아니라 아래와 붙인다.

그리드 간격: 카드 16 · 컬럼 24 · KPI 세로 32. 표 셀 좌우 12(인접 합 24 = 컬럼 간격과 같은 리듬), 세로 14, 끝 열 20.

헤더: 컨테이너 1200 · 좌우 24 · 상하 12(총 높이 ~64). 결합형(로고·GNB·계정·전체메뉴 한 줄) + GNB 중앙 정렬. 1024 미만은 킷 모바일 드로어. 사이트맵 오버레이는 컨테이너 1200 · 패딩 32/24/40 · 5컬럼 gap 32 · 계정 aside 224.

좌측 LNB: 폭 240, 접으면 64 아이콘 레일, 1024 미만 숨김. 1단 15/500 패딩 10/12 라운드 12 아이콘 18, 2단 14 패딩 8/12 **왼쪽 40**(= 패딩 12 + 아이콘 18 + 간격 10 — 부모 라벨이 시작하는 자리)· 아이콘 없음.

### gutter_px

24

### base_unit_px

4

### card_padding_px

24

### hero_padding_px

96

### section_padding_px

80

### max_content_width_px

1200

## tokens

### colors

#### neutral

##### hex

#64748b

##### scale

- #f8fafc
- #f1f5f9
- #e2e8f0
- #cbd5e1
- #94a3b8
- #64748b
- #475569
- #334155
- #1e293b
- #0f172a
- #020617

#### primary

##### hex

#2e45dc

##### scale

- #eef1fe
- #e0e6fd
- #c4cffb
- #9daef7
- #6e84f1
- #2e45dc
- #2336b8
- #1f2e92
- #1e2b74
- #16205c
- #0f1640

#### semantic

##### info

###### hex

#2e45dc

###### scale

- #eef1fe
- #e0e6fd
- #c4cffb
- #9daef7
- #6e84f1
- #2e45dc
- #2336b8
- #1f2e92
- #1e2b74
- #16205c
- #0f1640

##### warn

###### hex

#d97706

###### scale

- #fffbeb
- #fef3c7
- #fde68a
- #fcd34d
- #fbbf24
- #d97706
- #b45309
- #92400e
- #78350f
- #451a03
- #2e1102

##### error

###### hex

#dc2626

###### scale

- #fef2f2
- #fee2e2
- #fecaca
- #fca5a5
- #f87171
- #dc2626
- #b91c1c
- #991b1b
- #7f1d1d
- #450a0a
- #2c0606

##### success

###### hex

#047857

###### scale

- #ecfdf5
- #d1fae5
- #a7f3d0
- #6ee7b7
- #34d399
- #047857
- #065f46
- #065f46
- #064e3b
- #022c22
- #011a14

#### tertiary

##### hex

#7c3aed

##### scale

- #f5f3ff
- #ddd6fe
- #7c3aed
- #6d28d9

#### secondary

##### hex

#4757a8

##### scale

- #eef1fe
- #dee4fa
- #bcc6f0
- #94a3e2
- #6878c8
- #4757a8
- #36448a
- #2a3670
- #1e2b74
- #16205c
- #0f1640

### motion

#### duration

##### base

150ms

### radius

#### tag

6px

#### pill

9999px

#### tile

12px

#### input

8px

#### panel

24px

#### surface

16px

### shadow

#### none

none

#### dropdown

0 20px 25px -5px rgba(15,23,42,0.10)

#### card-soft

0 1px 2px rgba(15,23,42,0.05), 0 10px 30px -12px rgba(30,43,116,0.16)

### spacing

#### base

4

#### scale

##### block

20px

##### dense

10px

##### group

48px

##### tight

4px

##### column

24px

##### inline

8px

##### section

32px

##### card-gap

16px

##### label-gap

6px

##### in-component

12px

##### page-section

80px

### typography

#### body

##### scale

- 13px
- 14px
- 15px
- 17px

##### family

Roboto, 'Pretendard GOV'

##### weight

- 400
- 500
- 600

##### base_size

15px

#### label

##### family

Roboto, 'Pretendard GOV'

##### weight

- 500
- 600

##### base_size

14px

#### headline

##### scale

- 20px
- 24px
- 30px
- 40px
- 52px

##### family

Roboto, 'Pretendard GOV'

##### weight

- 500
- 600

##### base_size

20px

## do_rules

- 색을 바꿀 때는 **프리미티브만 덮는다.** 시맨틱 190종과 컴포넌트 변수가 3단 참조로 따라온다
- **1단 프리미티브는 :root, 2단 컴포넌트 변수는 반드시 컴포넌트 클래스에** 건다(.krds-btn 등). :root 에 걸면 조용히 무시된다
- KRDS 가 **선언하지 않는 속성만 '추가'** 한다(보더·간격·::before 등). 추가 전에 grep 으로 미선언을 확인한다
- 헤더 계열 오버라이드는 `#krds-header.클래스`(1-2-0)로 조준한다. KRDS 가 ID 스코프로 선언하므로 클래스만으로는 진다 — 전멸이 아니라 **일부만** 져서 더 위험하다
- 불가피하게 선언된 속성을 덮으면 **그 파일에 '예외' 주석으로 근거를 남긴다** (별도 목록을 관리하지 않고 grep 으로 조회한다)
- 같은 값이 두 화면에 필요하면 **공통 층이 갖는다.** 화면 CSS 에는 그 화면에만 해당하는 것만 남긴다 — 클래스 이름이 같은데 선언이 두 화면에 갈려 있으면 SPA 에서 나중에 로드된 쪽이 이겨, 어느 화면을 먼저 들렀는지에 따라 모양이 달라진다
- 높이부터 스케일(36/44/52)에 스냅하고 → 글자를 높이의 1/3 근처(14/15/17)로 → 좌우 패딩을 요소 성격으로 가른다(버튼 누진 / 입력 14 고정 / 캡슐 비율)
- 표 헤더처럼 죽여야 할 요소는 **굵기(500)보다 색(slate-400~500)으로** 죽인다. 굵기를 색보다 먼저 올리지 않는다
- 선택 상태는 **색만으로 구분하지 않는다** — 다중선택 칩은 체크(✓) 표식을 유지하고, 명도차가 작은 토글 버튼에는 aria-pressed 를 함께 둔다
- 도출값이 어색해 보이면 수치를 더 만지지 말고 **실물 렌더에서 원인을 찾는다** (스토리북 6007 · 데모 앱 5174)

## archetype

krds

## is_in_use

true

## applies_to

포털 채널 산출물 전체 — 저작도구 화면이 외부 채널 화면 안에서 실행될 때 쓰는 시스템. 관제 채널 산출물은 이것을 쓰지 않는다(그쪽은 별도 시스템). 한 저장소에서 산출만 채널별로 갈라지므로 어느 시스템으로 뜨는지는 산출 시점에 정해지고 뜬 뒤 갈아끼우지 않는다.

## dont_rules

- 고대비 모드 값을 건드리지 않는다 — 원본 시스템의 검증값이며 불가침이다
- 우리가 이 사본의 값을 고치지 않는다 — 정본은 외부 채널의 정의다. 고칠 것이 있으면 상대에 제기하고, 반영되면 다시 옮겨 온다
- 관제 채널 시스템의 값과 섞지 않는다 — 주색·본문 크기·모서리·굵기 상한이 모두 다르다
- 토큰 이름이 같다고 값을 같은 것으로 다루지 않는다 — 이쪽은 원본 위에 브랜드 색을 덮어썼다
- 부품 마크업을 임의로 바꾸지 않는다 — 상대가 배포 단위로만 제공하므로, 필요한 것은 값과 감싸는 층으로 해결한다

## known_gaps

- 사본이라 낡을 수 있다 — 상대가 값을 바꿔도 우리는 자동으로 알지 못한다. 옮겨 온 시점을 적어 두는 것이 유일한 단서이며, 포털 채널 화면을 크게 손볼 때는 한 번 대조하는 것이 안전하다
- 이 시스템의 스키마가 담지 못하는 도메인 토큰이 있다 — 상대가 자기 도메인용으로 더 둔 값들은 여기 자리가 없다. 그런 값이 필요하면 상대 산출물에서 확인해야 한다
- 부품 카탈로그가 함께 오지 않았다 — 값만 옮겼고 상대의 부품 정의는 우리 쪽에 없다. 포털 채널 부품이 필요하면 별도 판단이 필요하다

## color_usage

### [1]

- **token**: primary
- **use_for**: 브랜드 강조 단일색. 링크·버튼 채움·선택 상태·활성 GNB·포커스 링·진행바·아이콘 포인트. hover 는 팔레트 한 단계(600→700), active 두 단계(→800), 틴트 표면 50, 틴트 보더 200~500. 앵커 #2e45dc = KRDS primary-50 자리
- **avoid_for**: 즐겨찾기 별(코발트가 화면에 이미 많아 '내가 켠 것'이 묻힌다 — 금색 --klid-color-favorite 을 쓴다). 조건 토글 버튼의 켜짐 상태(채움은 한 패널에서 실행 하나의 무게라, 조건이 여덟·열둘 켜지면 검색 버튼이 묻힌다 — 틴트를 쓴다)

### [2]

- **token**: secondary
- **use_for**: 딥 코발트(네이비). 다크 밴드·강조 구간의 어두운 면. 틴트류(secondary-5)는 코발트 틴트라 KRDS 기본 매핑 그대로 쓴다
- **avoid_for**: 선택·활성 상태. KRDS 는 탭·달력 선택·사이드내비 선택·드롭다운 선택 등 14개 컴포넌트의 활성색을 secondary 로 두는데, 우리 secondary 는 다크 밴드용이라 활성 상태가 전부 네이비로 보인다. 진한 활성 2종(action-secondary-active · text-secondary)만 primary 로 재조준했다

### [3]

- **token**: tertiary
- **use_for**: violet 증강 계열. 증강 출처 배지·AI 증강 태그. 재난유형 '유괴'도 이 패밀리를 공유한다 (--klid-color-violet-5/20/50/60)
- **avoid_for**: 일반 강조. 증강이라는 한 가지 뜻에 묶어 두어야 배지 줄에서 계열이 읽힌다

### [4]

- **token**: neutral
- **use_for**: slate 쿨톤. 텍스트 사다리(제목 900 / 본문 800 / 표 셀 700 / 설명 600 / 메타·표 헤더·라벨 500), 보더(카드 200 · 입력 300), 표면(카드 흰색 · 빈 상태 50 · 캔버스는 도메인 토큰 #f4f6fa)
- **avoid_for**: 흰 배경 위 의미 텍스트에 gray-400 이하(4.5:1 미달). 400 은 비활성·placeholder·순수 장식·aria-hidden 아이콘 전용. 틴트 표면(gray-20) 위에는 gray-500 도 미달(3.86:1)이라 600 부터

### [5]

- **token**: success
- **use_for**: 완료·정상 상태 배지, 내부 출처 사각 태그. 앵커 #047857 은 흰 글자 배지와 흰 배경 텍스트 양쪽에서 대비를 확보한 값(5.6:1)
- **avoid_for**: 앵커를 밝은 emerald(#059669 등)로 되돌리는 것. 2026-07-29 대비 감사에서 내린 값이라 올리면 배지 흰 글자가 미달한다

### [6]

- **token**: warn
- **use_for**: 주의·대기 상태 배지, 만료 임박 표식
- **avoid_for**: 즐겨찾기 별. 색은 비슷하지만(#d97706 동일) 즐겨찾기는 경고가 아니라서 도메인 토큰 --klid-color-favorite 을 따로 둔다 — 경고 토큰을 빌려 쓰면 warning 팔레트가 바뀔 때 별도 따라 바뀐다

### [7]

- **token**: error
- **use_for**: 오류·실패·삭제 위험. 재난유형 '화재'가 같은 앵커(#dc2626)를 쓴다. point(강조 적색) 팔레트도 이 값으로 정렬했다
- **avoid_for**: 단순 강조나 배지 다양화. 적색이 화면에 둘 이상 있으면 어느 것이 실제 오류인지 흐려진다

### [8]

- **token**: info
- **use_for**: 안내 배너·외부 출처 사각 태그. **코발트와 같은 값**을 쓴다 — 안내는 브랜드 톤과 같은 계열로 읽히는 편이 낫다는 판단
- **avoid_for**: primary 와 나란히 놓고 둘을 구별시키려는 자리. 같은 색이라 구별되지 않는다

## description

외부 채널(포털)이 쓰는 디자인 시스템의 참조 사본이다. 정부 디자인시스템 위에 포털 브랜드를 얹은 2차 층이며, 포털 채널 산출물은 주색만이 아니라 형태·타이포·간격·그림자·컨트롤 높이까지 이것을 그대로 따른다. 출처는 외부 채널 프로젝트의 디자인 시스템 정의 v9 이고 값을 그대로 옮겨 왔다.

## overview_md

## 왜 우리 저장소에 두는가

포털 채널 화면을 그릴 때마다 상대 산출물에서 값을 캐내야 했다. 그 과정에서 실제로 어긋난 것이 나왔다 — 이동 탭의 밑줄 굵기·글자 굵기·항목 간격·구분선 유무 넷이 상대 규격과 달랐고, 시안 전체가 관제 채널 시스템으로 그려져 있었다. 값이 우리 안에 없으면 같은 어긋남이 화면마다 되풀이된다.

## 이것은 사본이다 — 값을 우리가 고치지 않는다

★ 우리가 값을 바꾸면 두 번째 진실원이 된다. 정본은 외부 채널의 정의이고 여기 있는 것은 그 시점의 사본이다. 바꿔야 할 것이 생기면 상대에 제기하고, 상대가 바꾸면 여기를 다시 옮겨 온다. 옮겨 온 시점을 적어 두는 이유가 이것이다 — 낡았는지를 볼 수 있어야 한다.

## 두 시스템을 섞지 않는다

관제 채널 산출물과 포털 채널 산출물은 주색이 다르고 본문 기준 크기가 다르고 모서리 값이 다르고 굵기 상한이 다르다. 한 화면 안에서 두 시스템의 값을 섞으면 어느 쪽으로도 읽히지 않는다. 채널을 먼저 정하고 그 시스템 하나만 쓴다.

⚠ 토큰 이름이 같아도 값이 다를 수 있다. 두 시스템 모두 같은 정부 디자인시스템에서 출발했으나, 이쪽은 그 위에 브랜드 색을 덮어썼다. 이름이 같다고 값을 같은 것으로 다루면 조용히 다른 색이 뜬다.

## preset_source

custom

## attached_files

_(empty)_

## typography_use

### [1]

- **step**: display-xl
- **weight**: 600
- **size_px**: 52
- **use_for**: 히어로 제목 (메인·소개 페이지)
- **line_height**: 1.15
- **letter_spacing**: -0.5px + tracking-tight

### [2]

- **step**: display-lg
- **weight**: 600
- **size_px**: 40
- **use_for**: KPI 빅넘버(tabular-nums)·메인 섹션 제목
- **line_height**: 1.2
- **letter_spacing**: -0.5px + tracking-tight

### [3]

- **step**: display-md
- **weight**: 600
- **size_px**: 30
- **use_for**: 페이지 제목 (풀폭 화면의 h1)
- **line_height**: 1.3
- **letter_spacing**: -0.5px

### [4]

- **step**: title-lg
- **weight**: 600
- **size_px**: 24
- **use_for**: 페이지 제목 (LNB 안쪽·폼 페이지의 h1) · 폼 모달(512) 제목
- **line_height**: 1.35
- **letter_spacing**: -0.5px

### [5]

- **step**: title-md
- **weight**: 600
- **size_px**: 20
- **use_for**: 상세 구획 제목(소제목) · 작은 창(400) 제목. 소제목은 독립적으로 성립하는 기능 블록이 둘 이상일 때만 단다
- **line_height**: 1.4
- **letter_spacing**: -0.5px

### [6]

- **step**: title-sm
- **weight**: 600
- **size_px**: 17
- **use_for**: 카드 제목 · 큰 본문 · 버튼 lg(52) · 패널 제목(드롭다운·사이트맵)
- **line_height**: 1.55
- **letter_spacing**: -0.5px

### [7]

- **step**: body-md
- **weight**: 400
- **size_px**: 15
- **use_for**: 본문 기본 · 표 셀 · 버튼 md(44) · 검색창 입력
- **line_height**: 1.6
- **letter_spacing**: -0.5px

### [8]

- **step**: body-sm
- **weight**: 400
- **size_px**: 14
- **use_for**: 폼 라벨 · 칩 · 표 헤더 · 보조 본문 · 버튼 sm(36) · 컴팩트 셀렉트 · LNB 2단
- **line_height**: 1.55
- **letter_spacing**: -0.5px

### [9]

- **step**: caption
- **weight**: 600
- **size_px**: 13
- **use_for**: 배지 · 카운터 · 메타 · 사각 태그(500) · 필터 칩 xs. **크기 하한이며 12px 이하를 쓰지 않는다**
- **line_height**: 1.5
- **letter_spacing**: -0.5px

### [10]

- **step**: button
- **weight**: 600
- **size_px**: 15
- **use_for**: 버튼 라벨 기본(md 44 기준). sm 36 은 14, lg 52 는 17 — 컨트롤 글자는 높이의 약 1/3
- **line_height**: 1.6

### [11]

- **step**: label
- **weight**: 600
- **size_px**: 14
- **use_for**: 폼 라벨·강조 라벨. 표 헤더는 같은 14 지만 굵기 500 + 색 slate-400 으로 낮춘다 (굵기를 색보다 먼저 올리지 않는다)
- **line_height**: 1.55

### [12]

- **step**: nav-link
- **weight**: 500
- **size_px**: 15
- **use_for**: GNB 항목(높이 40 · 좌우 패딩 14 · 라운드 8) · LNB 1단 항목. 기본 gray-60, hover gray-5 필, 활성 primary-60 텍스트(밑줄 없음)
- **line_height**: 1.6

## iteration_guide

- 포털 채널 화면을 그릴 때는 이 시스템의 값만 쓴다. 관제 채널 시스템의 토큰을 끌어오지 않는다
- 값이 없으면 지어내지 말고 상대 산출물에서 확인한다. 확인이 안 되면 비워 두고 정보 부족으로 남긴다
- 상대에 없는 화면을 새로 그릴 때는 가장 가까운 상대 화면의 짜임을 먼저 찾아 그 규격을 따른다 — 같은 문제를 상대가 이미 푼 자리가 대개 있다
- 값을 바꿔야 한다고 판단되면 여기를 고치지 말고 상대에 제기한다

## shape_principle

**명령은 둥글게, 내용 상자는 각지게.** 누르는 것(버튼·칩·정보 배지·검색창·채움형 탭)은 캡슐이고, 내용을 담는 것(입력·셀렉트·아이콘 버튼·페이저 셀)은 8, 표면(카드·표 셸·모달·아코디언·빈 상태)은 16, 큰 패널·미디어는 24, 가장 조용한 사각 태그는 6이다.

**하나의 요소에서 바깥 라운드보다 안쪽 라운드가 크면 안 된다.** 카드 16 안에 KPI 칩 12, 그 안에 아이콘 — 안쪽으로 갈수록 작아진다. 카드 안 대표 이미지에 따로 라운드를 주지 않고 카드가 overflow:hidden 으로 잘라내는 것도 이 원칙이 저절로 지켜지게 하려는 것이다.

정사각 아이콘 버튼은 라운드 8 이다 — 버튼과 달리 보조 조작이라 각을 남긴다. **아이콘 ≈ 박스의 절반**(28/16 · 32/18 · 36/18, 비율 0.5~0.57).

캡슐끼리는 **가로:세로 패딩 비율이 역할을 말한다.** 읽기만 하는 정보 배지는 5:1(납작하고 가로로 긴 알약), 클릭하는 칩은 2.3~2.5:1(통통해서 누를 수 있어 보임), 사각 태그는 3:1 + 라운드 6 으로 가장 조용하게. 한 카드에 배지가 여럿 서면 색만 바꾸지 않고 **역할마다 모양을 준다** — 분류는 캡슐 흰 면 + 보더(아이콘에만 유형색), 구성은 캡슐 틴트, 출처는 사각 태그, 수치는 배지 없이 글자만.

## elevation_levels

### 0 · 평면

- **use**: 기본 카드·표 셸·아코디언·모달 아닌 모든 표면. **그림자는 기본 없음**이 이 시스템의 출발점이다
- **treatment**: 그림자 없음. 흰 표면 + slate-200 보더 1px + 라운드 16

### 1 · card-soft

- **use**: 링크형 카드·강조 카드(StatCard 등). 누를 수 있음을 그림자로 말하되 호버로 뜨지는 않는다
- **treatment**: 0 1px 2px rgba(15,23,42,0.05), 0 10px 30px -12px rgba(30,43,116,0.16) — 두 번째 겹이 코발트 틴트(primary-80 16%)라 회색 그림자보다 브랜드 온도에 맞는다. 호버 반응 없는 **정적** elevation

### 2 · 드롭다운·패널

- **use**: GNB 드롭다운 패널·사이트맵 오버레이·셀렉트 팝업. 문서 흐름 위에 잠깐 얹히는 층
- **treatment**: 0 20px 25px -5px rgba(15,23,42,0.10)

### 3 · 모달

- **use**: 결정을 요구하며 뒤를 막는 층. **opacity 색 배경 금지 규칙의 예외 4종 중 하나**가 이 스크림이다
- **treatment**: 킷 기본 그림자 + 스크림 slate-900/45. 최대폭 512(폼) / 400(작은 창)

### — · 헤더 blur

- **use**: 스크롤 시 헤더 뒤가 비치는 층. #krds-header 에 직접 걸면 containing block 이 되어 헤더 안으로 포털되는 킷 모바일 드로어(fixed)가 헤더 박스에 갇힌다
- **treatment**: ::before 레이어에 backdrop-filter (헤더 자신에게 직접 걸지 않는다)

## font_substitutes

라틴·숫자와 한글을 스택 한 줄로 제어한다. 라틴 서체에 한글 글리프가 없어 한글만 자동으로 다음 서체로 넘어가는 구조라, 두 서체가 각자 맡은 문자를 그린다. 서체 파일을 이 시스템이 함께 주지는 않으므로 산출 시점에 붙여야 하며, 붙이지 않으면 조용히 시스템 서체로 떨어지고 화면은 그럴듯하게 렌더돼 알아채기 어렵다. 대체가 불가피하면 한글 쪽을 같은 계열 원본 서체로 바꾸되 자간 보정은 그대로 유지한다.

## key_characteristics

- 정부 디자인시스템 위에 얹은 2차 층 — 원본 토큰은 불변으로 두고 색 프리미티브·치수 오버라이드·도메인 토큰만 덮는다. 2차 층을 걷어내면 순정으로 돌아간다
- 주색은 코발트다 — 원본의 파랑을 브랜드 코발트로 덮어쓴다. 이름은 그대로이고 값만 바뀌므로, 토큰 이름으로 같은 색이라고 판단하면 틀린다
- 본문 기준 크기가 관제 채널보다 한 단 작다 — 이쪽은 15, 관제는 17 이상이다
- 굵기 상한이 600 이다 — 700 이상을 쓰지 않고 위계는 크기와 색으로 만든다
- 모서리 값이 관제 채널보다 넓게 퍼져 있다 — 명령은 둥글게, 내용 상자는 각지게라는 원칙이 값 구성을 가른다
- 그림자는 기본 없음에서 출발한다 — 표면은 평면이 기본이고 띄우는 것은 예외다

## whitespace_principle

**간격은 관계가 멀수록 커진다.** 한 요소 안의 조각 6~8 → 형제 칩·버튼 6~8 → 카드 사이 16 → 컬럼 사이 24 → 블록 사이 20 → 구획 사이 32~48 → 페이지 섹션 80. 라벨과 그 대상은 밀착(4~6px)시키고 구획 경계는 그 5~10배로 벌린다. 즉 '붙어 있는 것'은 4~6px, '다음 항목'은 20px 부터이며, 그 사이 값(10~16)은 한 컴포넌트 안에서만 쓴다(KPI 아이콘칩 → 숫자 16 등).

★ **아이콘·배지 안 글리프와 옆 글자 사이는 4 다** (2026-08-26 확정 · 글자 14 인 자리 14곳). 사다리 첫 칸(한 요소 안의 조각 6~8)의 예외다 — 글자가 14 로 내려간 자리에서 6 은 글리프를 글자에서 떼어 놓아 둘이 한 낱말로 안 읽힌다. ★ **정본(design.md §1)은 아직 「조각 6~8 … 밀착(4~6px)」 그대로다** — 이 DS 가 먼저 반영했고 가이드 갱신이 남아 있다(2026-08-26 사용자 확정). 두 문서가 갈리면 이 자리에 한해 DS 가 뒤늦은 쪽이 아니라 앞선 쪽이다.

가로 여백은 **'클릭할 수 있을수록, 클수록' 넉넉하게** 준다. 같은 캡슐이라도 정보 배지 < 필터 칩 < 버튼 순으로 좌우 패딩이 커진다(자기 글자크기 대비 0.77em → 1em → 1.33em). 버튼은 사이즈가 한 단계 오를 때 패딩 **비율 자체가** 커진다(높이 대비 0.39 → 0.45 → 0.54). 반면 입력은 사이즈와 무관하게 좌우 14 고정이다 — 내용이 왼쪽 정렬로 흐르는 요소라 시작점이 흔들리면 폼 전체가 어긋난다. 버튼과 입력의 결정적 차이다.

**표면 패딩은 라운드보다 크게.** 패딩 ≥ 라운드 × 1.25 (라운드 16 → 패딩 20~24, 라운드 24 → 패딩 32급). 지키지 않으면 모서리 곡선에 내용물이 끼어 보인다.

킷이 자기 여백을 갖는 컴포넌트는 그 값을 존중한다 — 페이지네이션 위 여백 40(--krds-pc-gap-layout-h2-h3)은 KRDS 가 정한 레이아웃 리듬이라 3단 사다리로 덮지 않는다.

## responsive_breakpoints

### 모바일

- **width**: ≤ 767px
- **key_changes**: 헤더는 킷 모바일 드로어. 좌측 LNB 숨김. 사이트맵 오버레이 비활성(데스크톱 1024+ 전용). 화면 3단계에 따라: 완전 반응형 화면은 전 기능 사용, 조회 전용 화면은 생성·편집 액션을 숨기고 'PC 환경에서 이용' 안내로 대체, 데스크톱 전용 화면은 차단이 아니라 안내 화면 + 돌아가기 동선. **모션(히어로·이벤트 복셀 캐릭터)은 모바일에서도 재생한다** — 옵트아웃은 OS 의 prefers-reduced-motion 에 위임하며 모바일이라는 이유로 일괄 비활성화하지 않는다

### 태블릿

- **width**: 768 ~ 1023px
- **key_changes**: 별도 설계 없이 데스크톱 레이아웃의 유연한 축소로 처리한다. 헤더는 1024 미만이므로 여전히 킷 모바일 드로어이고 LNB 도 숨김

### 데스크톱

- **width**: ≥ 1024px
- **key_changes**: 결합형 헤더(로고·GNB·계정·전체메뉴 한 줄) + 전체메뉴 오버레이. 좌측 LNB 240(접으면 64 레일). 콘텐츠 최대폭 1200 + 좌우 24. AI 마크 hero 크기 도출(자리의 선에 맞춰 사각 높이로 역산)은 이 폭에서만 성립한다 — 폭이 줄면 줄이지 말고 내린다

