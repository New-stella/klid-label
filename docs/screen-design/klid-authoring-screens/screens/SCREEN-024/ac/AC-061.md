---
logicraft_item: AC-061
type: acceptance
version: 3
status: CHANGED
prev_version: 1
raw: AC-061.json
---

> ⚠️ **버전 변경 감지 — logicraft v1 → v3**
> change_summary: 본문 변경 없음 — 확인 내역을 기록한다. 이 기준의 축은 목록을 어떤 순서로 어떻게 나눠 돌려주는가이고, 이번에 넓어진 관리자 유효창은 역할을 바꾸는 쓰기에만 요구되므로 목록 조회의 정렬·페이징 계약은 달라지지 않는다. 본문에 폐기된 화면·인터페이스 참조가 없는 것도 함께 확인했다. 직전 리비전은 본문 변경을 담지 않는다.
> ↳ 요약/구현 노트 재검토 후 작성된 코드에 반영. 직전 요약은 git diff 확인.

# 사용자 목록은 서버가 고정한 순서로 쪽 단위로 돌아온다

## notes

정렬 기준을 요청이 고르지 못하게 서버가 고정하는 것이 이 기준의 축이다. 지금 볼 것을 고르는 일은 정렬이 아니라 검색어와 역할 조건이 맡는다.

## priority

medium

## scenario

### then

- 목록은 등록 순서로 한 쪽 분량만 돌아온다
- 확정하기 전에는 서버에 다시 묻지 않는다
- 확정하면 첫 쪽부터 다시 보여준다

### when

- 검수자가 목록을 연 뒤 검색어를 입력하고 확정한다

### given

- 사용자가 한 쪽에 담기는 수보다 많다

## statement

사용자 목록은 서버가 정한 등록 순서로만 정렬되어 쪽 단위로 돌아오며, 검색어와 역할 조건은 확정했을 때에만 서버에 다시 물어 첫 쪽부터 보여준다.

## is_negative

false

## implementation

### status

planned

### modules

_(empty)_

### records

_(empty)_

### progress

0

### subtasks

_(empty)_

### module_paths

_(empty)_

## derived_domain_ids

- DOMAIN-001

## verification_method

automated_test

## verification_status

unverified

## derived_from_use_cases

- UC-030
