---
logicraft_item: AC-1048
type: acceptance
version: 6
status: UNCHANGED
prev_version: null
raw: AC-1048.json
---

# UC-003 해상도 변경 수행 — 프리셋 예약→비디오 복사+프레임 균일배율 리스케일→좌표 재계산→finalize (happy)

## evidence

### [1]

- **type**: document
- **result**: pass
- **reference**: 화면 시나리오 — [해상도 변경 /video/{id}] 1) 대상 영상·프리셋(1080P/720P/480P) 지정 실행 · 기대: 프리셋마다 새 파생영상, 비디오 복사+프레임 균일배율 리스케일, 좌표 재계산 적재, finalize 시 ACCEPTED
- **captured_at**: 2026-08-31T03:00:00Z

### [2]

- **type**: log
- **result**: pass

**reference**:

select aug_type_cd, count(*) from klid_at.ls_data_aug group by aug_type_cd order by 1;

결과 (검증환경 dev · 상용은 동일 쿼리로 확인):
+-------------+-------+
| aug_type_cd | count |
+-------------+-------+
| RESL_1080P  |     2 |
| RESL_480P   |     3 |
| RESL_720P   |     3 |
| WINTER      |     1 |
+-------------+-------+

- **captured_at**: 2026-08-31T03:00:00Z

## last_run

### passed

true

### run_at

2026-08-31T03:00:00Z

### duration_ms

0

## priority

high

## scenario

### then

- 프리셋마다 산출 크기 계산·원본 동일 프리셋 스킵·나머지 새 RAW_SN을 PENDING 예약
- 비디오는 원본(비식별) 복사, 프레임은 종횡비 보존 균일 배율 리스케일(패딩 없음, 축소·확대 허용)
- 좌표를 균일 배율 곱셈만으로 재계산 적재(오프셋 가산 없음)
- finalize 성공 시 ACCEPTED 확정(실패 시 예약행 삭제)

### when

- 목표 프리셋을 지정해 해상도 변경을 실행한다

### given

- 해상도 변경 대상 영상이 있다(파생본 아님)

## statement

검수자가 대상 영상과 목표 프리셋(미지정 시 3종 전체)을 지정하면(POST /v1/videos/{rawSn}/resolution) 시스템이 프리셋마다 산출 크기를 계산해 원본과 같아지는 프리셋은 스킵하고 나머지 프리셋마다 새 파생영상(RAW_SN)을 PENDING으로 예약한다. 비디오 파일은 원본(비식별) 복사, 프레임은 종횡비 보존 균일 배율로 리스케일하며(짧은 변을 프리셋 값에 맞추되 긴 변이 상한을 넘으면 배율을 낮추고 패딩 없음, 축소·확대 허용), 라벨/이미지 좌표를 그 균일 배율의 곱셈만으로 재계산해 적재한다(오프셋 가산 없음). finalize 성공 시 파생이 ACCEPTED로 확정된다(실패 시 예약행 삭제).

## brownfield

### status

new

## is_negative

false

## attached_files

_(empty)_

## implementation

### status

implemented

### modules

_(empty)_

### records

_(empty)_

### progress

100

### subtasks

_(empty)_

### last_updated

2026-08-31T14:39:46.161Z

### module_paths

_(empty)_

## derived_domain_ids

- DOMAIN-007

## verification_method

automated_test

## verification_status

passing

## derived_from_use_cases

- UC-003
