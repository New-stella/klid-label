---
logicraft_item: AC-1032
type: acceptance
version: 8
status: UNCHANGED
prev_version: null
raw: AC-1032.json
---

# UC-037 마킹 끝난 영상 일괄 올리기 — 폴더 재귀 짝짓기·판정·비동기 적재·비식별 후 마킹 활성 (happy)

## evidence

### [1]

- **type**: document
- **result**: pass
- **reference**: 화면 시나리오 — [산출물 가져오기 화면] 1) 산출물 종류에서 이벤트 마킹 선택·위치·필수값 입력 2) 짝짓기 판정 목록 확인 후 일괄 적재 · 기대: 재귀 짝짓기·판정, 비동기 적재, 비식별 후 예약 마킹 활성
- **captured_at**: 2026-08-31T03:00:00Z

## last_run

### passed

true

### run_at

2026-08-31T03:00:00Z

### duration_ms

0

## priority

high

## scenario

### then

- 폴더 재귀로 마킹 문서·영상이 이름으로 짝지어진다(구조 비의존, 바로가기 미추종, 상한 도달 시 일부만 훑음 안내)
- 짝마다 구간·시점·FPS·전체 프레임 대조로 적재 가능 여부가 판정된다(판정 단계 미저장)
- 일괄 적재는 비동기·항목별(동시 처리 상한)로 영상 복사·생성·예약 마킹 생성한다
- 마킹 문서의 프레임 이미지는 적재하지 않고 시점 위치로만 쓴다
- 비식별 완료 후 예약 마킹이 활성으로 전이되어 이후 처리가 시작된다

### when

- 폴더 일괄 짝짓기·판정 후 일괄 적재를 실행한다

### given

- 외부에서 이벤트 마킹까지 끝낸 영상 묶음이 폴더에 있다
- 관리자가 폴더 위치와 필수 지정값을 입력한다

## statement

관리자가 산출물 가져오기 화면에서 산출물 종류로 이벤트 마킹을 고르고 위치와 사람이 정해야 하는 값(이벤트 유형·지자체 코드·카메라 식별자·개인정보 유형)을 지정하면, 시스템이 폴더를 재귀로 훑어 마킹 문서를 모으고 각 문서가 적은 영상 파일 이름으로 짝을 찾는다(폴더 구조 비의존, 바로가기 미추종, 상한 도달 시 일부만 훑음을 함께 알림). 짝마다 구간 수·시점 수·역산 FPS와 실측값·전체 프레임 수를 대조해 적재 가능 여부를 판정해 목록으로 돌려주고(이 단계 미저장), 관리자가 확인 후 일괄 적재를 실행하면(일부 선택 가능) 작업을 등록하고 곧바로 반환한다. 실제 적재는 항목별 비동기(동시 처리 상한)로 영상 파일을 복사·생성하고 외부 시점 배열의 마킹을 예약 상태로 만든 뒤 비식별을 태우며, 비식별이 끝나 마킹 가능 상태가 되면 예약 마킹을 활성으로 전이시켜 이후 처리를 시작한다.

## brownfield

### status

new

## is_negative

false

## attached_files

_(empty)_

## implementation

### status

in_progress

### modules

_(empty)_

### records

_(empty)_

### progress

85

### subtasks

_(empty)_

### last_updated

2026-09-03T11:02:08.784Z

### module_paths

_(empty)_

## derived_domain_ids

_(empty)_

## verification_method

manual_test

## verification_status

passing

## derived_from_use_cases

- UC-037
