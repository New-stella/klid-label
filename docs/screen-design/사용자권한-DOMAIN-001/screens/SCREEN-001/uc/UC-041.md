---
logicraft_item: UC-041
type: use_case
version: 11
status: NEW
prev_version: null
raw: UC-041.json
---

# 인증 인계와 채널·역할 인가

## goal

관제서버나 포털이 발급한 토큰을 인계받아 저작도구에 진입하고, 채널과 저작도구가 보관한 역할로 접근할 수 있는 기능이 갈린다.

## actor

내부 사용자

## title

인증 인계와 채널·역할 인가

## status

draft

## priority

must

## frequency

상시

## main_flow

### [1]

- **step**: 1
- **actor**: 관제서버
- **action**: 상위 시스템이 발급한 토큰을 저작도구 프론트가 인계받는다 — 채널마다 인계 수단이 다르다

### [2]

- **step**: 2
- **actor**: 시스템
- **action**: 토큰의 발급처(iss)를 검증한다 — iss 클레임이 없는 경우를 통과시킨다. iss 가 있는데 허용목록 밖이면 여전히 거부한다. 시크릿 서명검증과 exp(만료) 필수는 그대로 유지한다

### [3]

- **step**: 3
- **actor**: 시스템
- **action**: 토큰의 채널 표식으로 내부와 포털을 가른다 — 표식이 없으면 내부로 본다

### [4]

- **step**: 4
- **actor**: 시스템
- **action**: 내부 채널이면 저작도구가 보관한 역할로 인가를 해석한다 — userId 클레임으로 저작도구 마스터 LS_ACNT_USER.USER_ID 로 조회한다. 다중 매칭이거나 0건 매칭이면 fail-closed 로 무권한 처리한다(CWE-639). 기존 숫자 sub(USER_NO) 식별 경로는 그대로 두고 바꾸지 않는다. 이어서 해석된 신원(userNo)을 principal 의 주체 식별자로 정규화해, 이후 per-user 스코핑·소유권·감사·작업 잠금이 raw 로그인 ID 가 아니라 실제 userNo 를 본다 — 해석 실패면 raw 를 유지하며 fail-closed 가 그대로 성립하고, 숫자 sub(내부·포털) 경로는 정규화 전후 값이 같아 무변경이다

### [5]

- **step**: 5
- **actor**: 시스템
- **action**: 역할이 없는 내부 진입자는 그 시점에 작업자로 자동 등록한다 — 이미 부여된 역할은 덮지 않으며, 개발용 로그인이 발급하는 표준 계정은 이 자동 등록 대상이 아니다

### [6]

- **step**: 6
- **actor**: 시스템
- **action**: 채널과 역할로 각 창구의 접근을 판정한다 — 관리자는 검수자 자리를 계층으로 물려받되 작업자 전용 자리는 열리지 않는다

### [7]

- **step**: 7
- **actor**: 내부 사용자
- **action**: 세션이 만료되면 각 상위 시스템의 로그인 화면으로 되돌아간다

## brownfield

### status

modified

### decided_by

ADR-012

### change_kind

- auth-change

### diff_summary

1차 자체 로그인에서 상위 시스템 토큰 인계로 바뀌고 채널 축이 더해짐

### legacy_source

#### repo

KLID-AI-PF-001

#### type

module

#### identifier

KLID-AI-PF-001

#### legacy_artifact_id

LEGACY-001

## description

저작도구는 독립 로그인 화면을 두지 않고 상위 시스템이 발급한 토큰을 인계받아 진입한다. 진입 이후 무엇에 닿을 수 있는지는 두 축이 함께 정한다 — 어느 채널로 들어왔는지와 어떤 역할을 가졌는지다.

[인가에 쓰는 역할은 저작도구가 보관한 값이다] 토큰에 다른 체계의 역할만 실려 있어도 저작도구가 보관한 값으로 인가하며, 그 값이 해석되지 않으면 보호된 창구에서 거부한다. 포털 채널은 보관값 없이 포털 역할로 인가한다.

[채널 격리는 역할로 뚫리지 않는다] 포털 채널 토큰은 내부 창구와 관리 창구에 닿지 못하고 내부 채널 토큰은 포털 창구에 닿지 못한다. 관리자에게 검수자 권한을 물려주는 계층도 이 격리를 열지 않는다.

[역할이 없어도 들어올 길은 남긴다] 역할이 배정되지 않은 내부 사용자는 보호된 창구에서 거부되지만 자기 정보 조회와 관리자 등록 창구에는 도달한다 — 그 둘까지 막으면 아무도 처음 들어올 수 없다.

[해석된 신원은 principal 로 정규화한다] 관제 인계 토큰(비숫자 sub)을 userId 로 유일 해석하면 그 신원(userNo)을 principal 의 주체 식별자로 정규화해 내부·포털 토큰과 동형으로 만든다 — 이후 per-user 스코핑·소유권·감사·작업 잠금이 raw 로그인 ID 가 아니라 실제 userNo 를 일관되게 본다. 해석 실패면 raw 를 유지하며 fail-closed 가 그대로 성립한다([[ADR-063]]).

## stakeholders

### [1]

- **actor**: 내부 사용자
- **interest**: 저작도구에 따로 로그인하지 않고 관제서버에서 이어 들어오길 원한다

### [2]

- **actor**: 포털 회원(PORTAL_USER)
- **interest**: 포털에서 받은 자격으로 본인 작업 범위에만 접근하길 원한다

### [3]

- **actor**: 발주기관
- **interest**: 채널과 역할에 따라 접근 범위가 갈려 권한 밖 기능이 열리지 않길 원한다

## preconditions

- 상위 시스템이 저작도구와 같은 발급처로 토큰을 발급한다
- 저작도구는 세션 토큰을 스스로 발급하지 않는다

## attached_files

_(empty)_

## implementation

### status

implemented

### modules

_(empty)_

### records

_(empty)_

### progress

100

### subtasks

_(empty)_

### last_updated

2026-09-04T12:23:57.791Z

### module_paths

_(empty)_

## postconditions

- 채널과 역할이 해석되어 이후 요청의 인가 판정에 쓰인다
- 역할이 없던 내부 진입자는 작업자로 등록되어 배정 대상 목록에 나타난다 — 단 개발용 로그인이 발급하는 표준 계정은 역할 없는 상태로 남아 최초 관리자 등록 창구로 유도된다

## alternate_flows

### 역할이 해석되지 않음

**steps**:

- 보호된 창구에서 거부된다
- 자기 정보 조회와 관리자 등록 창구에는 도달한다
- 라벨 마스터 읽기 같은 내부 조회에서는 거부된다

- **trigger**: 내부 채널인데 보관된 역할이 없거나 허용 목록 밖 값이다

### 채널이 어긋남

**steps**:

- 양쪽 모두 권한 없음으로 거부된다
- 역할 계층으로도 열리지 않는다

- **trigger**: 포털 토큰이 내부 창구를, 내부 토큰이 포털 창구를 부른다

### 토큰이 없거나 만료됨

**steps**:

- 인증 실패로 거부된다
- 자원의 존재 여부가 드러나지 않는다

- **trigger**: 토큰이 없거나 유효기간이 지났다

## realizes_dfeats

- DFEAT-001
- DFEAT-002

## related_screens

- SCREEN-001
- SCREEN-003

## extension_points

_(empty)_

## secondary_actors

- 포털 회원(PORTAL_USER)
- 관제서버
- 포털 서버

## extends_use_cases

_(empty)_

## realizes_features

_(empty)_

## includes_use_cases

_(empty)_

## tech_considerations

- 역할 해석이 되지 않을 때 열어 주지 않고 닫는다 — 의심스러울 때 통과시키면 인가가 무력해진다
- 채널 판정과 역할 판정은 서로 다른 축이라 한쪽을 통과했다고 다른 쪽이 열리지 않는다
- 자동 등록은 조건부 갱신으로 한 번만 일어나며 매 요청마다 쓰지 않는다

## covered_by_acceptances

- AC-1016
- AC-1017
