# D9 데이터베이스 설계서 (CBD 표준 산출물 위키)

> 출처: NIA CBD 가이드(2011.12) docs/CBD.pdf 인쇄면 p.89~95
> 대응: 범정부 EA "DV6 물리데이터모델"

## 1. 작성 목적 / ## 2. 작성 방법 (PDF 원문)

### 1. 작성 목적 (원문)
최종적으로 설계된 테이블과 인덱스를 데이터베이스 공간에 맵핑시키고 저장공간 등의 **물리 모델**을 기술한다.

### 2. 작성 방법 (원문)
부서에서 운영하는 데이터베이스 목록을 작성하고, 데이터베이스의 물리적 상세내용을 작성한다.

## 3. 산출물 양식 (표 구조 전수)

### (1) 제·개정 이력

| 날짜 | 버전 | 작성자 | 승인자 | 내용 |
|---|---|---|---|---|
| | | | | |

### (2) 산출물 식별 헤더박스

| D9 | **데이터베이스 설계서** (산출물 제목) | | |
|---|---|---|---|
| 시스템명 | (값) | 서브시스템명 | (값) |
| 단계명 | 설계 | 작성일자 | (값) / 버전 (값) |

### (3) 1. 데이터베이스 목록

| 데이터베이스 ID | 명칭 | 주관부서 | 비고 |
|---|---|---|---|
| | | | |

> 상위 컬럼 그룹 헤더 "데이터베이스" 아래 ID / 명칭 2개 하위 컬럼 + 주관부서 + 비고.

### (4) 2. 데이터베이스 정의 (DB별)

정의 헤더박스:

| 데이터베이스 ID | (값) | 데이터베이스명 | (값) | Storage Group | (값) |
|---|---|---|---|---|---|
| Bufferpool | (값) | | | 인덱스 BP | (값) |

본문 표 (7컬럼):

| TS ID | TS 용량 | 테이블 ID | 테이블 명 | 인덱스 ID | 인덱스 용량 | 비고 |
|---|---|---|---|---|---|---|
| | | | | | | |

### (5) 3. 테이블 명세 (테이블별)

명세 헤더박스:

| 테이블ID | (값) | 테이블명 | (값) |
|---|---|---|---|
| 데이터베이스명 | (값) | TS명 | (값) |
| 트리거 구성 | (값) | | |
| 테이블 설명 | (값) | | |

운영 메타 표 (6컬럼):

| 초기건수 | 증가량(일) | 보관주기 | 최대건수 | 용량 | 비고 |
|---|---|---|---|---|---|
| | | | | | |

컬럼 명세 표 (9컬럼):

| 컬럼명 | 컬럼ID | 타입 및 길이 | Not Null | PK | FK | IDX | 기본값 | 제약조건 |
|---|---|---|---|---|---|---|---|---|
| | | | | | | | | |

## 4. 항목 설명 (원문 — 전 항목)

### 데이터베이스 목록
- **데이터베이스 ID**: 실제 물리적으로 구현하는 데이터베이스 ID를 부여하여 기입한다.
- **데이터베이스명칭**: 데이터베이스가 주로 관리하는 데이터를 대표하는 명칭을 기입한다.
- **주관부서**: 데이터베이스의 관리 책임을 가진 부서를 기입한다.
- **비고**: 기타 고려사항 등을 기입한다.

### 데이터베이스 정의
- **데이터베이스 ID**: 데이터베이스 ID를 기입한다.
- **데이터베이스명**: 데이터베이스 명칭을 기입한다.
- **Storage Group**: 데이터베이스내의 객체에서 사용할 Default Storage Group명을 기술한다.
- **Bufferpool**: 데이터베이스내의 테이블 스페이스에서 사용할 Default Bufferpool명을 기술한다.
- **인덱스 BP**: 데이터베이스내의 Index에서 사용할 Default Bufferpool명을 기술한다.
- **TS ID**: 테이블 스페이스의 식별자를 기술한다.
- **TS 용량**: 테이블 스페이스의 할당용량을 표시한다.
- **테이블 ID**: 테이블의 식별자를 기술한다.
- **테이블명**: 테이블 명칭을 기술한다.
- **인덱스 ID**: 인덱스 식별자를 기술한다.
- **인덱스 용량**: 인덱스의 저장공간의 크기를 산정하여 기술한다.
- **비고**: 예외사항 및 추가사항을 기술한다.

### 테이블 명세
- **테이블 ID**: 테이블의 식별자를 기술한다.
- **테이블명**: 테이블 명칭을 기술한다.
- **데이터베이스명**: 데이터베이스 명칭을 기입한다.
- **TS명**: 테이블 스페이스의 명칭을 기술한다.
- **트리거 구성**: 테이블에 트리거가 구성되어 있을 경우 트리거 로직을 기술한다.
- **테이블 설명**: 테이블의 목적 및 역할을 간략하게 기술한다.
- **초기건수**: 테이블이 최초 생성될 때 보유한 데이터의 건수를 기재한다.
- **증가량(일)**: 일정주기별 데이터 발생건수를 기술한다.
- **보관주기(일별)**: 해당 테이블내 데이터의 보관주기를 기술한다.
- **최대건수**: 테이블이 관리되는 기간(보관주기)내에 발생이 예상되는 최대 데이터 건수를 기술한다.
- **용량**: 데이터 보관 최대 건수와 데이터의 길이를 고려하여 산정한 데이터 용량을 기술한다.
- **비고**: 기타 고려사항 등을 기술한다.
- **컬럼명**: 테이블 컬럼의 내용과 특성을 인식할 (수) 있는 명칭을 기술한다.
- **컬럼 ID**: 테이블 컬럼 ID를 기술한다.
- **타입 및 길이**: 컬럼의 타입과 최대 허용 길이를 기술한다.
- **NOT NULL**: 필수항목 여부를 기술한다.
- **PK (Primary Key)**: 주키를 의미한다.
- **FK (Foreign Key)**: 외래키를 의미한다.
- **INX (Index)**: 인덱스를 의미한다. (※ 양식 표 컬럼명은 "IDX")
- **기본값**: 속성의 기본값이 있는 경우에 그 값을 기재한다.
- **제약조건**: 속성의 특이한 제약조건이 있는 경우 기재한다.

## 5. 작성 사례 요점 (실제 값/패턴 + 산출물코드)

- **데이터베이스 목록**:
  - `JJ_DB_040 | 중계거래DB | 운영부`
  - `JJ_DB_050 | 상품DB | 판매부`
- **데이터베이스 정의** (`JJ_DB_050 상품DB`):
  - Storage Group `JJ_PROD_010`, Bufferpool `BP2`, 인덱스 BP `BP0`.
  - 본문: `JJ_TS_140 | 400MB | JJ_TB_040 | 고객 | JJ_IDX_140 | 100MB`, `JJ_TS_150 | 60MB | JJ_TB_050 | 거래처 | JJ_IDX_150 | 20MB`.
- **테이블 명세** (`JJ_TB_040 고객`, 인쇄면 p95):
  - 테이블ID `JJ_TB_040`, 테이블명 `고객`, 데이터베이스명 `상품DB`, TS명 `JJ_TS_110`.
  - 트리거 구성: `없음`.
  - 테이블 설명: 상품 판매관리를 위하여 등록된 고객정보를 관리한다.
  - 운영 메타: 초기건수 `5,000` | 증가량(일) `50` | 보관주기 `영구` | 최대건수 `100,000` | 용량 `300MB`.
  - 컬럼:
    - `ID` | 컬럼ID `Cust_ID` | `CHAR(7)` | NotNull Y | PK Y | IDX `XAW040`
    - `비밀번호` | 컬럼ID `PSWD` | `VARCHAR(16)` | NotNull N
- 코드 체계: DB `JJ_DB_NNN`, 테이블스페이스 `JJ_TS_NNN`, 테이블 `JJ_TB_NNN`, 인덱스 `JJ_IDX_NNN`, Storage Group `JJ_PROD_NNN`.

## 6. 함정 / 주의

- **물리 모델 산출물** — D8(논리)의 엔티티/속성을 물리 테이블/컬럼으로 맵핑. 논리 타입(문자/숫자/날짜)이 아닌 **물리 타입+길이**(CHAR(7), VARCHAR(16) 등)로 기술.
- **운영 메타(초기건수·증가량·보관주기·최대건수·용량) 전 테이블 필수** — 용량은 최대 건수×데이터 길이 기반 산정 근거 포함.
- **DB2 물리 용어 사용**: Storage Group / Bufferpool / 인덱스 BP / Tablespace(TS)는 가이드가 DB2 기준으로 작성됨. 그대로 복사하지 말고 실제 운영 DBMS 등가물로 적응(§7).
- 인덱스 ID와 인덱스 용량은 정의 표에서 테이블별로 산정, 컬럼 명세의 IDX 컬럼(Y/N 또는 인덱스명)과 일관성 유지.
- 양식 컬럼명은 "IDX"(테이블 명세), 항목 설명 본문은 "INX(Index)"로 표기 — 동일 의미.

## 7. KLID 프로젝트 적용 메모

- **DB2 물리 용어 → PostgreSQL 등가물로 적응 표기**:
  - Storage Group / Bufferpool / 인덱스 BP → PostgreSQL은 별도 객체 없음(기본 shared_buffers 운영). 비고/별표로 "PostgreSQL은 Bufferpool/Storage Group 개념 미적용" 명시.
  - Tablespace(TS) → PostgreSQL TABLESPACE 또는 스키마(`klid_at`)로 대응. TS 용량은 운영 산정치로 기재.
  - 타입: CHAR/VARCHAR/INTEGER/TIMESTAMP/BYTEA(영상·Quartz blob) 등 PostgreSQL 타입.
- **코드 체계**: DB `KLID-AT-DB-NNN`, 테이블 `KLID-AT-TB-NNN`, 인덱스 `KLID-AT-IDX-NNN`(사례 `JJ_*` 대응).
- **공유 스키마(MNG_* / QRTZ_*)는 물리 설계 비대상** — 데이터베이스 목록에 "공유(READ)" 비고만 표기. 저작도구 전용 LS_* 테이블만 D9 정식 테이블 명세 작성. MNG_* 진실원은 LogiCraft ERD-024.
- **D8 엔티티와 1:1 추적**: 테이블 명세에 관련 엔티티 ID 행(D8 EN과 1:1)을 둬 추적 사슬 유지. 행정안전부 공통표준용어 검증(컬럼ID/명) 권장 — KLID 추가 규칙.
- **JPA ddl-auto=validate 정합**: 컬럼 명세는 Flyway 마이그레이션(LS_* 전용)과 엔티티 정의에 일치시킨다. MNG_* 공유 테이블 변경 시 관제서버팀 선승인 필수.
