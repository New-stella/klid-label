# 2차 검증 총괄 — 2026-08-02

> 1차(2026-08-01)에서 발견된 CRITICAL 1건 + HIGH 9건(중복 제거) 수정 완료 후 재검증.
> 범위: 수정된 이슈 타겟 재검증(A/B/C/D/E) + 신규 전수검증(F/G/H)

## 타겟 재검증 (버그수정 확인)

| 대상 | 결과 |
|---|---|
| A(JWT exp·정렬allowlist·로그마스킹) | 3/3 PASS |
| B(마킹영구고착·재처리레이스·스트리밍PII) | 3/3 PASS |
| C(SAM2 mock게이트) | 1/1 PASS |
| D+E(export/통지CRITICAL·PARTIAL뷰배제·통지인증헤더) | 3/3 PASS |
| **합계** | **10/10 PASS** (실동작은 배포이미지 미반영으로 일부 BLOCKED→정적+테스트로 대체 판정) |

## 신규 전수검증 (F/G/H)

| 클러스터 | 케이스 | PASS | FAIL | PARTIAL | BLOCKED/N-A | 이슈 |
|---|---:|---:|---:|---:|---:|---:|
| F 포털 | 169 | 141 | 4* | 9 | 0 | 27 |
| G ai-server+목업계약 | 163 | 149 | 2 | 4 | 3 | 27 |
| H FE/E2E | 293 | 262 | 5 | 6 | 1 | 29 |
| **합계** | **625** | **552** | **11** | **19** | **4** | **83** |

*F의 FAIL 4건은 전부 기존에 확정된 "포털 SAM2 노출" 정책위반의 재확인(신규 아님, 심각도 CRITICAL→HIGH 완화 확인).

## 이번 회차 이슈 심각도 분포

| 심각도 | 건수 |
|---|---:|
| CRITICAL | 0 |
| HIGH | 13 |
| MEDIUM | 31 |
| LOW | 39 |
| **합계** | **83** |

## 신규 HIGH 발견 (조치 필요)

1. **[F] `/v1/portal/datamart/labels` 게이트 전무** — APPROVED·신고 게이트 없이 rawSn만으로 라벨 덤프 가능 (CWE-862/639/359)
2. **[F] 라벨 PUT 크기상한 URL인코딩 우회** — A클러스터 HMAC 우회와 동일 계열(CWE-436), 필터가 원시 URI 사용
3. **[F] 포털 SAM2/AI도구 노출 지속** — 정책위반 재확인, 심각도 완화(비식별본 전송+신고게이트 확인), kill-switch 부재
4. **[G] YOLO track이 mock폴백 시 입력검증 스킵** — 기존 테스트가 conftest 강제설정으로 거짓통과
5. **[G] SAM2 mock폴백이 잘못된 좌표에 재충돌(500)** — 우아한 저하 실패
6. **[G] SAM2 track bbox계산이 try블록 밖** — 폴백 없이 500
7. **[H] 세션만료 시 상위 로그인 복귀 URL 미주입** — 전 배포 설정에 누락, 복귀 수단 없음
8. **[H] `?token=` URL 인계 채널이 운영 기본값에서 활성** — CLAUDE.md "미사용" 정책 위반(CWE-598)
9. **[H] 라벨 저장 시 낙관적 동시성 버전 토큰 미전송** — FE가 BE 응답의 labelVersion을 매핑 안 함, lost-update 무경고 발생 실측
10. **[H] 전체워크플로 E2E 4건이 존재하지 않는 픽스처 참조** — 검수종결 E2E 커버리지 사실상 0
11. **[H] 포털 라벨링에 AI 도구 노출** (F와 동일 축, FE단 재확인)

## 반복 패턴 관찰 (구조적 함정)

- **URL 인코딩 우회 계열**(CWE-436) — A(HMAC 필터), F(라벨 크기상한 필터) 2곳에서 동일 패턴. 원시 URI 기반 필터 전수 점검 권장.
- **mock 폴백의 2차 실패**(G) — YOLO/SAM2 모두 "실패 시 mock으로 전환"하는 폴백 코드가 원인이 된 예외를 그대로 재트리거해 500. 폴백 경로 자체의 방어적 코딩 재점검 필요.
- **E2E 테스트 자산 자체 결함** — H-part6/H-part8에서 반복: 존재하지 않는 픽스처 참조, 공허 단언(`if(count>0)` 가드), 잘못된 POM 페이지 타겟. E2E 스위트 전체 감사 권장.
- **자체 목업 없음 원칙 재확인** — self-fill 결함 A~H 전체에서 0건.

## 다음 단계 제안

1. 신규 HIGH 11건 중 조치 필요한 것(카탈로그 재확인 성격 제외) 우선순위 검토
2. 버그수정 사이클(Phase 1~5) 커밋 — 현재 79개 파일 미커밋 상태
3. MEDIUM/LOW 83건은 별도 사이클로 이월
