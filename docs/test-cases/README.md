# 학습데이터 저작도구 — 테스트 케이스 위키

> 전수 테스트 케이스 카탈로그
> 최초 도출 2026-07-25 · 최신화 2026-07-30(1회차) · 2026-08-03(3회차 — 사용자 확정 5건) · 2026-08-03(4회차 — 근거 `file:line` 전수 재확인) · 2026-08-05(6회차 — 비식별 신고 단계 구분 + 재개 지점 분기) · 2026-08-05(7회차 — 머지 합류 후 전량 실측 재측정) · 2026-08-05(10회차 — 작성자 표시명 사번→이름 계약 변경) · 2026-08-05(11회차 — `UserNameResolver` 수렴에 따른 근거 라인드리프트 정정) · **2026-08-05(12회차 — ★근거에서 라인번호 폐지, 파일 단위로 전환)**
> 범위: BE 단위+통합 · FE 컴포넌트/화면 · ai-server · 외부 벤더 목업 계약 · E2E + 보안 전 계층
> 기준: 실제 구현 코드 + R1 요구/수용기준 양면 전수
> **검증 시 해당 클러스터 파일만 로드하세요** (토큰 절약)

## 최신화 이력

| 회차 | 일자 | 대상 변경 | 정정 | 신규 | 폐기 | 총 케이스 |
|:--:|------|------|--:|--:|--:|--:|
| — | 2026-07-25 | (최초 도출) | — | — | — | **1,376**(실측) |
| **1** | **2026-07-30** | 07-25 이후 38커밋(BE 332파일 · FE 73파일, Phase 1~10) | **942** | **531** | **15** | **1,907** |
| 2 | 2026-07-31 ~ 08-02 | 클러스터별 부분 최신화(H 회차2 busy 16건 · B 회차2 B-1 재작성 25건 · F 포털 SAM2 제거) | — | — | — | **1,931**(실측) |
| **3** | **2026-08-03** | **2026-08-03 사용자 확정 5건**(`80171828`·`b27b3108`·`d8a7a2cc`·`e58aa086` + 영상 목록 필터 미커밋분) | **7** | **46** | **0** | **1,977**(당시 기재, 실측 아님 — 회차 4 참조) |
| **4** | **2026-08-03** | **근거 `file:line` 전수 재확인 회차** — 8클러스터 1,954행 전량을 실제 코드와 대조(클러스터당 담당 1명 + H 보완 1명) | **≈699** | **4** | **0** | **1,958**(실측) |
| **4-1** | **2026-08-03** | 회차 4 부산물 코드 수정(SAM2 track 응답 최소 정점 수 검증 신설 + `Sam2SegmentService` dead code 제거)에 따른 **C 클러스터 동기화** | **2** | **2** | **0** | **1,960**(당시 기재 — **실측 아님**. 현행 기준 동시점 실측은 **1,983**, 아래 "총계 산식" 참조) |
| **5** | **2026-08-04** | 사용자 표시명 노출(공지 작성자 · 비식별 신고자) — **B 클러스터 동기화** | **0** | **2** | **0** | **1,962**(실측) |
| **5-1** | **2026-08-04** | 라벨링 우측 속성 패널 겹침 수정(AI 분할 정밀도 카드 잘림) — **H 클러스터 동기화**. 신규 3건 중 1건(TC-FE-306)은 **미해결 등재**(좌측 도구바 `저장` 버튼 잘림 — 별건) | **2** | **3** | **0** | **1,965**(실측) |
| **5-2** | **2026-08-04** | 자동마킹 `EVNT_TYPE_CD` 적재 수정 — **B 클러스터 동기화**. `main`(PR #77, `bc21bf63`)에서 **병렬로 진행돼 머지로 합류**했다 — 회차 5·5-1 과 내용 의존관계는 없고 B 파일 안에서는 자체 번호 5(상대)·6(표시명)으로 재정렬했다 | **2** | **4** | **0** | **1,969**(실측) |
| **5-3** | **2026-08-04** | 회차 4 부산물 코드 수정(**D-ISSUE-21 write skew** — 롤백/검수승인의 활성 버전 조회를 프레임 행 락 이후로 이동)에 따른 **D 클러스터 동기화**. `qa-0803`(PR #80)에서 **병렬로 진행돼 머지로 합류**했다 — 회차 5~5-2 와 내용 의존관계는 없다(D 파일 로컬 회차 번호는 4) | **43** | **2** | **0** | **1,971**(실측) |
| **5-4** | **2026-08-04** | 회차 5-3 의 리뷰 지적(MEDIUM) 보강 — 선취 잠금 **유지 확정**(근거 주석 고정) + 승인 구간 elapsed 관측 + IT `pg_locks` 관측 스코프 축소(flaky 차단) + 이력 건수 단언 + **역순 경합 케이스 신설**에 따른 **D 클러스터 동기화**(D 파일 로컬 회차 번호는 5) | **5** | **1** | **0** | **1,972**(실측) |
| **5-5** | **2026-08-04** | 회차 5-4 부산물 — 잠금 순서 근거 주석의 부정확 서술 정정(**실행 코드·동작·정책 무변경**, 3-트랜잭션 이론적 교착 인지·별도 이슈 이월 기록)에 따른 **D 클러스터 동기화**(D 파일 로컬 회차 번호는 6) | **3** | **0** | **0** | **1,972**(실측) |
| **6** | **2026-08-05** | **비식별 누락 신고의 단계 구분 + 해소 후 재개 지점 분기**(사용자 확정, 구속 · V171 `DCLR_STP_CD`) + 선결 결함 수정(재비식별 재추출이 `FRM_NO`→`VDO_FRM_NO`) + 마킹 화면 신고 버튼 신설 + **QA DEV_FIX 2건**(마킹 재개 fail-closed 자기검증 · 재추출 전부-skip 감사행) | **4** | **24** | **0** | **2,024**(실측) ⚠ 머지 합류(2026-08-05)로 **회차 5 → 6 재부여**(main 회차 5~5-5 와 번호 충돌). 총계는 회차 7 에서 실측 재측정. |
| **6-1** | **2026-08-05** | 회차 5 후속 — **비식별 신고 목록에 신고 단계 노출**(BE 응답 optional 추가 + 신고 관리 화면 「신고 단계」 열, 레거시 `null` 은 '미상') | **0** | **6** | **0** | **2,030**(실측) ⚠ 머지 합류(2026-08-05)로 **회차 5-1 → 6-1 재부여**(main 회차 5~5-5 와 번호 충돌). 총계는 회차 7 에서 실측 재측정. |
| **6-2** | **2026-08-05** | **카탈로그 정합 3건(문서 전용 · 코드 무변경)** — ①F `TC-PORTAL-060~062` **ID 중복** 재번호(F-3 쪽 → `095~097`) ②C `TC-LABEL-125` **stale 폐기 표기**(회차 4 가 놓친 개인정보 리셋 폐기의 잔여분) ③**총계 산식 커밋별 재계산 + 기준선 재설정**(구 "기타 6" 잔차 폐기) | **3** | **0** | **1** | **2,030**(실측, 불변 — 재번호·폐기 표기 모두 행을 남김) ⚠ 머지 합류(2026-08-05)로 **회차 5-2 → 6-2 재부여**(main 회차 5~5-5 와 번호 충돌). 총계는 회차 7 에서 실측 재측정. |
| **7** | **2026-08-05** | **머지로 합류 — 실측 재측정.** `db-fix-0804`(V165~V171 · MNG_* 9종 제거 · 신고 단계 구분) ↔ `main`(3차 전수검증 8건 · 표시명 노출 · 라벨 저장 게이트 412) 병합 후 8클러스터 전량 재측정. **케이스 신규·폐기 0** — 양쪽 행의 **합집합**만 남기고 ID 충돌분만 재번호했다(아래 각주) | **0** | **0** | **0** | **2,099**(실측) |
| **8** | **2026-08-05** | 머지 후 드리프트 정정 3건(문서·주석) + **신고 후처리 캐시 정책 반전**(`invalidateQueries` → `removeQueries`, 사용자 확정 · 프로덕션 변경) — **B**(사용자 마스터 표기 `MNG_ACCT_USER`→`LS_ACNT_USER`) · **E**(`VideoMetaMapper`/`NiaVideo` 근거 라인 5건) · **H**(TC-FE-066 실계약 정합 + **TC-FE-315 신규** 재진입 노출 창 가드, CWE-359) | **8** | **1** | **0** | **2,100**(실측) |
| **9** | **2026-08-05** | **영상 목록·상세 개인정보 유무 화면 노출 폐지(R1/R2, 응답 필드는 존치)** + **이벤트유형 필터 표시명 그룹핑이 영상목록·작업목록·배정목록·통계에 적용(R3~R5)** — **B**(TC-VIDEO-006 폐기→006a·019~021 신설, 회차 3 부터의 "카테고리 키" 축 드리프트 동시 정정) · **D**(D-3a `TC-ASSIGN-027~035`·D-3b `TC-STAT-001~005` 신설 — 통계 BE 케이스 최초 등재) · **H**(TC-FE-301 축 정정 + `TC-FE-316~319` 신규 — `PrivacyBadge` 컴포넌트 삭제 회귀 가드). v2-wiki 5종 동기화(05/12/04/17/18) 동반 | **4**(TC-VIDEO-007/008/009 · TC-FE-301) | **22**(B 4 · D 14 · H 4) | **1**(TC-VIDEO-006 폐기 표기) | **2,122**(실측) |
| **10** | **2026-08-05** | **작성자 표시명(사번→이름) 계약 변경** — 표시용 응답 필드에 사번(`REG_ID`)이 그대로 내려와 화면에 "2001"·"1001" 이 찍히던 결함 수정. **C**(라벨 이력 `GET /v1/frames/{srcSn}/label-history` — `actorName` 신규 추가, `actor` 사번 하위호환 유지) · **D**(버전목록 `GET /v1/frames/{srcSn}/versions` — `authorName` **의미가 사번→이름으로 변경(필드명 불변·Breaking)** + 사번은 신규 `authorNo` 로 분리, 롤백 `POST /v1/versions/{version}/rollback` — `registeredUserName` 신규 추가·`registeredUserNo` 사번 유지). 해석은 신설 공용 헬퍼 `user/service/UserNameResolver.java`(페이지 사번을 `findByUserNoIn` 1회, N+1 금지 · 비숫자/미존재/null 은 예외 아닌 null). v2-wiki [13 §13.7](../v2-wiki/13-version-control.md#137-작성자-표시명-사번-이름-정정-2026-08-05-외부-fe-팀-고지-대상) 동기화 동반 | **2**(TC-VERSION-013·015 라인드리프트) | **12**(C 5 · D 7) | **0** | **2,134**(실측) |
| **11** | **2026-08-05** | **사번→표시명 판정의 `UserNameResolver` 수렴(순수 리팩토링)에 따른 근거 `file:line` 드리프트 정정** — 동작·기대결과 변경 없음(케이스 신설·폐기 없음). `IssueThreadService.resolveNames`·`ReviewService.lookupUserNames`(회차 10 각주가 "미통합"이라 적었던 그 후속 수렴 과제)가 이번에 `UserNameResolver`(신규 `resolveAllByNo`/`resolveOneByNo` — `USER_NO` 축)로 수렴됐고, `AssignmentService`·`VideoQueryService`·`TaskBoardService`·`StatsService`·`NoticeService`·`TaskQueryService`·`DeidentReportService` 도 같은 헬퍼로 갈아탔다. **B**(`VideoQueryService.java` 12건) · **C**(`UserNameResolver.java` 3건) · **D**(`ReviewService.java`·`AssignmentService.java`·`StatsService.java`·`TaskQueryService.java`·`UserNameResolver.java` 61건) 총 76건 정정 | **76**(B 12 · C 3 · D 61) | **0** | **0** | **2,134**(실측, 불변 — 행 추가·삭제 없음) |
| **12** | **2026-08-05** | **★근거에서 라인번호 폐지(사용자 확정, 구속)** — 근거 칸을 **파일 단위**로 축약하고 `:105-143` 형태의 라인 참조를 전 클러스터에서 제거. 라인 기반 정정 이력(`구 126-134 드리프트`)·파일 총 줄 수 표기도 함께 제거하고, 표 헤더를 `근거(file:line)` → **`근거(파일)`** 로 통일. 판정 주체를 알려주는 **심볼명·짧은 설명은 보존**(`PresetService.java(resolveLabels)` · `VlmUrlPolicyTest.java:cgnatRangeRejectedInBothPolicies`). 근거 폐지 사유·새 규칙(`파일명(심볼명)` 형식 · 심볼 가능하면 반드시 · 기존 행 백필 없음)은 아래 「근거는 `파일명(심볼명)` 으로 적는다」 절 | **2,011**(라인 제거) + **51**(잔여 이력·괄호 정리) | **0** | **0** | **2,134**(실측, 불변 — 문서 전용, 코드 무변경) |
| **13** | **2026-08-05** | **★버전 ↔ 현재 작업본 diff 신설 + diff 비교축 확장(구 동작 2건 폐기)** — ①신규 EP `GET /v1/versions/{version}/diff-with-working`(from=버전 스냅샷, to=현재 작업본 `LS_DATA_LBL`). 승인 버전이 **1건뿐인 프레임**은 `/diff?compareWith=` 로 비교 대상이 없어 변경 내역을 볼 수 없던 것을 해소 ②**비교축 확장(R7)** — `LabelSnapshot.equalsContent` 에 `labelId`·`trackId` 편입. 구 동작(3필드만 비교)에서는 **트랙 병합**처럼 시스템 자신이 `TaskModifiedEvent(LABEL_UPDATED)` 를 발행해 export 를 `v{n+1}` 로 재생성·관제 재통지까지 한 변경이 diff 에선 "변경 없음"이었다. 비교기를 `/diff` 와 공유하므로 **기존 EP 의 판정 결과도 함께 바뀐다**(계약·스키마는 무변경) ③**FE 단일 선택 시맨틱 전환** — 커밋 1건 클릭이 구 "직전 버전"(`list[idx+1]`) 대신 **현재 작업본**과 비교. 변경 0건은 "변경 없음" 안내(에러와 구분). ④무력했던 가드 3종 실효화(정렬 결정성·AI 메타 동봉·게이트 순서) + 프레임 전환 stale diff 요청 차단(두 축 대칭). **D**(TC-DIFF-032~047 신설 + TC-DIFF-001·007·031 정정) · **H**(TC-FE-320~327 신설). v2-wiki [13 §13.3.1~13.3.3](../v2-wiki/13-version-control.md) 동기화 동반 | **3** | **24**(D 16 · H 8) | **0** | **2,158**(실측) ⚠ 머지 합류(2026-08-05) — 총계는 회차 14 에서 8클러스터 실측 재측정. |
| **14** | **2026-08-05** | **어노테이션(NIA export JSON) 정정 3건(@req R9) + `AI_CRT_YN` 동결 도출식 결함 정정(@req R10)** — `dataset.name`(영상 파일명 → `{이벤트명} 데이터셋 구축`) · `info.year`(`now()` → 검수완료 연도 고정, 재export 불변) · `video.event_id`(`EVNT_TYPE_CD` → 인입 `EVNT_ID`, 미송신이면 null) · `AI_CRT_YN`(`ORGNL_RAW_SN != null` → `SRC_TYPE IN ('GENERATED','AUGMENTED')`, 판정은 `LsDataRaw.genAiYnOf` 단일 원천). **기존 동결 행 백필 없음**(소급 UPDATE 금지 — 재승인 시 갱신). 규칙이 SQL·Java 두 곳에 존재하는 데이터셋명은 실 DB 대조 가드 신설 | **3**(D 1 · E 2) | **5**(D `TC-MARTVIEW-038/039` · E `TC-EXPORT-054~056`) | **0** | **2,189**(머지 합류 후 8클러스터 실측 재측정 — dev-pf-0805 단독 시점은 **2,165** 였고 여기에 main 신규 24건(D `TC-DIFF-032~047` 16 · H `TC-FE-320~327` 8)이 합류했다) ⚠ 직전 표기 `2,141` 은 클러스터 표의 **D 행이 221 로 stale**(실측 240)이라 어긋나 있었다 — 이번에 8클러스터 전량 재측정해 기준선을 다시 맞췄다 ⚠ 머지 합류(2026-08-05)로 **회차 13 → 14 재부여**(main 의 버전 diff 회차가 커밋 시각상 앞서 13 에 배치됨) |
| **15** | **2026-08-06** | **내부 업로드 기술메타 ffprobe 자동 채움** — 관리 화면 TUS 업로드 **완료 시점**에 측정값으로 `LS_DATA_INGEST` 의 **비어 있는** 기술메타 8컬럼(`VDO_LEN_SEC`·`FPS`·`VDO_CDC`·`WDTH`·`VRTC`·`RESL`·`FRME_CNT`·`ASPRT_RT`)만 채운다. **B**(B-19 신설 `TC-ULD-001~024`, 신규 프리픽스). 핵심: `COALESCE` + `NULLIF(BTRIM(…),'')` 로 **사용자 입력·공백문자열 보존**(판정 주체는 DB) · `PXL`·`BIT` 는 표기 규약 부재로 **채우지 않고 DTO 에 필드 자체를 두지 않음** · back-fill 은 **파일 이동 이전**(기능 + 교착 회피 CWE-833) · `REQUIRES_NEW` 격리 · **0행은 정상** · 신뢰 경계는 SQL 이 아니라 `isUploadAreaPath` 하나라 **호출부 단일성이 보안 요건**(CWE-915) · ffprobe **완료당 1회** + probe 대상은 임시파일(CWE-59/367) + stdout 임시파일·stderr `DISCARD` 로 교착 제거 + **출력 절단 시 전량 미상**(fail-closed). v2-wiki [05 §5.2](../v2-wiki/05-video-management.md) 동기화 동반(구 서술 "비운 키만 적재 후 ffprobe 가 채운다"·"추측해 채우지 않는다" **폐기** — `VideoMetaService` 는 `LS_DATA_META` `video.*` 6키만 채워 인입 컬럼은 영구 NULL 이었다) | **0** | **24**(B 24) | **0** | **2,213**(실측) |
| **16** | **2026-08-06** | **메타 조회 응답 정합 — `vlm.accuracy` 분리 + 편집 차단(@req R12)** — `GET /v1/frames/{srcSn}/meta` 응답에 **`readOnlyMeta` 필드를 신설**해 화면 전용 일치도를 편집 목록(`items`)에서 분리하고, `PUT` 이 그 키를 `video.*` 와 **동일한 400** 으로 거부한다. **B**(B-21 신설 `TC-VLM-066~078`). 핵심: **하위호환은 추가만**(기존 `items`·`technicalMeta` 및 항목 5필드 이름·타입·시맨틱 불변 — 레코드 컴포넌트로 기계 고정) · 판정은 **`vlm.*` 네임스페이스 안의 화이트리스트**(fail-closed — 향후 읽기전용 키가 늘어도 새지 않되 레거시 구간 키·`manual-timeseries` 는 계속 편집 가능) · **술어 소유자는 `MetaService` 하나**(DTO·FE 복제 금지) · 저장 경로를 조회에 의존하지 않고 서버가 직접 차단(CWE-20/915) · 거부 메시지에 요청 키 미echo(CWE-117/209) · 검토행 **배치 조회 1회 유지**(N+1) · `autoApproveOnVideoApproval` 무영향(accuracy 는 검수큐 미진입). v2-wiki [09 §9.4-1](../v2-wiki/09-vlm-timeseries.md) 동기화(조회 응답 계약 신설 + 구 갭 서술 "accuracy 도 편집·저장 가능" **폐기**). ⚠ FE 렌더 배선은 **Phase 6** 이라 현재 화면에서는 일치도가 보이지 않는다(편집·저장은 불가능해졌다) | **0** | **13**(B 13) | **0** | **2,271**(실측) |
| **17** | **2026-08-06** | **★라벨링 시계열 메타 패널 — 전문 1개 편집 + 일치도 읽기전용 + 레거시 병기(@req R8/R9)** — 회차 16 이 BE 응답을 3목록으로 갈랐지만 화면 어댑터가 `readOnlyMeta` 를 읽지 않아 **일치도가 화면에서 사라져 있었다**. 이번 회차에 FE 를 정합했다 — ①어댑터 수용(`toFrameMeta`, 누락 응답은 빈 배열) ②편집 슬롯은 `vlm.description`·`manual-timeseries` **알려진 키 화이트리스트**(접두 파싱 금지 · BE fail-closed 와 대칭 · **편집 단위 = 저장 단위** 원칙 유지) ③레거시 구간행 **읽기 전용 병기**(삭제·숨김 금지 = 보존 확정) + `start_sec` **숫자** 정렬(문자열이면 구간 10개 초과 시 `10-18` 이 `8-16` 앞으로 온다 — 기존 결합 로직의 결함도 함께 수정) ④일치도 읽기 전용 렌더(백분율 환산, 미지의 읽기 전용 키도 일반 렌더) ⑤검수 화면 `ReviewMetaPanel` 에도 '참고 정보'로 표시(판정기 공유, 복제 금지). **H**(H-22 신설 `TC-FE-340~347`). v2-wiki [09 §9.3·§9.4-1](../v2-wiki/09-vlm-timeseries.md) 동기화 동반(구 서술 "남은 갭 — 일치도가 화면에서 사라진 상태" **해소**). FE 전용 · BE 무변경 | **0** | **8**(H 8) | **0** | **2,279**(실측) |
| **18** | **2026-08-06** | **★학습데이터 export JSON `video.vd_description` 배선(@req R10) — 구 동작 "항상 null 하드코딩" 폐기** — 외부 VLM `verify` 콜백이 서술 전문을 `LS_DATA_META['vlm.description']` 에 적재하면서 원천이 생겼다. 조달 규칙(사용자 확정): **① 전문 → ② 없으면 보존된 레거시 구간 행(`0-8`·`8-16` …)을 `start_sec` 오름차순 이어붙임 → ③ 없으면 `null`**(빈 문자열 아님 · 지어내지 않음). **E**(E-7 `TC-EXPORT-057~066` 신설 + TC-EXPORT-027 정정). 핵심: 판정 단일 원천 신설 `VlmDescriptionPolicy`(매퍼·서비스 재유도 금지) · **숫자 정렬**(문자열이면 구간 10개 초과 시 `10-18` 이 `8-16` 앞 — FE 패널이 겪은 것과 같은 함정) · **구간형 아닌 키 무시 + 예외 미전파**(CWE-20) · **자동 절단 금지**(사일런트 손실 방지) · **파생 부모 폴백 없음** · **`vlm.accuracy` 는 export 미포함**(R12 유지). ★**"저장은 됐는데 산출물이 안 바뀐다" 축을 세트로 차단** — ①콘텐츠 해시 조건부 블록 `VDSC` 편입(하위호환: 값 없으면 미append → 기존 승인분 전량 재산출 방지) ②승인 후 서술 변경 시 `TaskModifiedEvent(exportRegenerated=**true**)`(구 "R13 은 regen=false" **폐기**). v2-wiki [24 §24.4.3](../v2-wiki/24-dataset-export.md) 신설 + [09 §9.4-1](../v2-wiki/09-vlm-timeseries.md) 동기화 동반. BE 전용 | **1** | **10**(E 10) | **0** | **2,289**(실측) |

| **19** | **2026-08-06** | **★회차 18 DEV_FIX — `manual-timeseries` 조달 편입 + 값 무변경 재생성 억제(@req R10)** — ①**조달 우선순위 3단화**: 전문(`vlm.description`) → **`manual-timeseries`**(사람이 직접 쓴 전문) → 레거시 구간 → `null`. FE 시계열 패널은 **편집 가능한 항목이 하나도 없을 때**(메타 0건 **또는 레거시 구간뿐**) 이 키로 신규 등록 슬롯을 띄우는데, 회차 18 조달은 그 값을 무시하고 **편집조차 불가능한 옛 구간 이어붙임**을 대신 내보냈다(조용한 손실). 구 기대 *"`manual-timeseries` 는 조달 무관 키"* 는 **폐기** ②**무변경 저장이 export 버전을 적층하던 결함(CWE-770)**: 판정이 **키만** 보고 값 비교를 하지 않았고 재생성이 `ControlNotifyDebouncer` 를 거쳐 **`force=true`** 로 위임돼 해시 멱등 skip 도 타지 못했다 → 같은 값 N회 저장에 `v2·v3·v4…` 가 이미지 2벌 전량 복사와 함께 적층 + 매번 관제 통지(전 버전 보존 정책이라 삭제도 안 됨). 최종 조건 = **(조달 참여 키) AND (값 실제 변경)**, 무변경 저장은 **200 성공 + 통지 발행**이고 재생성 플래그만 생략 ③**문서 정정 2건(코드 무변경)**: `LabelContentHasher` 의 `VDSC` 블록은 **현재 배포 형상에서 아무것도 게이트하지 않는다**(`force=false` 진입 경로 0건 — 휴면 리스너 `DatasetExportBridge.onReExport` 뿐)는 사실 명시 / `DatasetExportTxService` 의 *"파생은 자기 메타가 없으면 null"* 주석이 사실과 달랐다(`DerivedMetaCopier` 가 부모 메타를 키 필터 없이 물리 복사). **E**(E-7 `TC-EXPORT-067~075` 신설 + `TC-EXPORT-059·061·064·065·066` 정정). mutation 실증 5종. v2-wiki 24 §24.4.3 · 09 §9.4-1 동기화 동반. BE 전용 | **5** | **9**(E 9) | **0** | **2,298**(실측) |

| **20** | **2026-08-06** | **`features/auto/metaKeys` 판정 단일 원천의 전용 단위 테스트 신설(문서·테스트 전용 · 프로덕션 코드 무변경)** — 회차 17 코드 리뷰가 LOW 로 남긴 항목. 이 모듈은 **라벨링 편집 패널·검수 읽기 패널·어댑터 세 곳이 공유**하는데 소비자 컴포넌트 테스트로 **간접 커버**될 뿐이라 판정 경계(비구간 키 혼재·동률·범위 밖 값·미지 키)가 고정돼 있지 않았다. 신설 `features/auto/__tests__/metaKeys.test.ts`(**25 tests**)가 `startSecOf`·`compareByStartSec`·`isEditableMetaKey`·`editableMetaLabel`·`readOnlyMetaLabel`·`formatReadOnlyMetaValue` 6종을 직접 고정한다. **기존 간접 테스트는 중복이어도 유지**(화면 계약과 판정 계약은 다른 축이라 서로를 대체하지 않는다). **H**(H-23 `TC-FE-348~355` 신설). 동반 문서 정합 — CLAUDE.md 에 verify 전환 확정 정책 반영(구 `describe` 서술 폐기 표기) · v2-wiki [09](../v2-wiki/09-vlm-timeseries.md) §9.2-0 `frame_policy` 도출 규칙 신설 + 검수큐 화이트리스트 서술의 적용 경로 명시 | **0** | **8**(H 8) | **0** | **2,306**(실측) |

| **21** | **2026-08-06** | **★라벨 색상 결함 핫픽스 — 판정원이 하드코딩 색상표에서 라벨 마스터로 이동(구 정책 폐기)** — 사용자 신고 *"저장하면 객체 색이 바뀐다"*. 인과 사슬: 캔버스 신규 라벨 4경로(BBOX·POLYGON·AI 분할·KEYPOINT)가 **`labelId` 미설정** → `serializeLabel` 이 `null` 직렬화 → BE 가 `LS_LABEL` 조인 skip → 재조회 `color` null → `getLabelDisplayColor` 가 **`trackId` 해시색으로 낙하**. 생성 payload 를 단일 진입점(`newLabelFrom`)으로 모으고, 분류 드롭다운은 `classId`·`labelId`·`color` 를 함께 갱신하며, 라벨명만 오는 SAM2 Track 은 **동명 1건일 때만** 역해석(0건·2건 이상 `null` fail-closed — 추측하면 다른 분류로 저장). **구 정책 폐기**: 우측 객체 패널 **그룹 헤더** 색 판정원 `labelColors.LABEL_CLASS_DEFS`(하드코딩 9종) 폐지 + **파일 삭제** — 마스터와 어긋나는 두 번째 진실원이었고, 같은 파일의 **표시명** 치환이 2026-08-03 에 같은 이유로 폐지된 전례의 연장선. **개별 항목 막대의 `trackId` 해시색은 의도된 동작이라 보존**(그룹=분류축 / 항목=트랙축 경계를 가드로 고정). 그룹 대표는 `className` 그룹핑이 `labelId` 유일성을 보장하지 않으므로 **최소 `labelId` → `id` 사전순**으로 순서 독립 선택(`items[0]` 은 정렬·필터·재조회로 색이 흔들린다). **백필 없음**(사용자 확정 — 기존 `labelId=null` 행은 재저장 시 자연 복구) · **BE·마이그레이션 변경 0**. **H**(H-20 신설 `TC-FE-328~334`) · TC-FE-293 정정 1건(표시명 축 무변경 — 인용 심볼 보유 파일 삭제 사실 명기) | **1**(H 1) | **7**(H 7) | **0** | **2,313**(실측) ⚠ 머지 합류(2026-08-06)로 **회차 16 → 21 재부여** — 브랜치 회차 16~20 과 번호가 겹쳐 뒤에 이어 붙였습니다. H 파일 안에서는 이 회차가 **H 회차 14**(`TC-FE-328~334`)로 먼저 배치되고 브랜치분이 H 회차 15~17(`TC-FE-335~355`)로 재번호됐습니다 |
| **22** | **2026-08-06** | **★VLM 위탁 게이트 반전 + 목서버 `framerate` 상한 폐기(사용자 확정, 구속)** — 로컬 목서버 실동작 드라이브에서 드러난 2건을 고쳤다. ①**`event_type` 은 더 이상 위탁을 막지 않는다** — 구 동작(미수신·허용목록 밖이면 외부 호출 0건 + SKIPPED 보류)을 폐기하고 **조달값을 그대로 실어 항상 위탁**한 뒤 수용 여부를 **벤더 응답**이 정하게 했다. 허용목록이 벤더 enum 의 **우리 쪽 사본**이라 벤더가 값을 넓히면 정상 값을 우리가 먼저 막기 때문이다. 값을 지어내지는 않는다(미조달이면 `null` 전송 → 벤더 4xx → 확정 실패 기록). 구 보류사유 상수 2종은 **과거 `LS_BATCH_PROC_LOG` 행의 재개 배선 키**라 존치. ②**목서버 `framerate` 상한(`le=240`) 제거** — `framerate` 는 FPS 가 아니라 **추출 간격**(규격서 §2.1 본문)인데 목서버만 표의 "기준 FPS" 해석에 남아 있어, 자동 마킹 `intervalFrames=300` 이 **422 로 위탁을 죽였다**. **B**(B-22 신설 `TC-VLM-079~084`) · **G**(`TC-AIMOCK-54~55`). ⚠ 미확인 — **실벤더의 `framerate` 상한 여부**(관제·벤더 확답 대기, BE 에도 `intervalFrames` 상한 검증 없음 = B-ISSUE-23) | **0** | **8**(B 6 · G 2) | **0** | **2,321**(실측) |
| **23** | **2026-08-06** | **목서버 verify `event_type` 완화 — 어떤 값이든·없어도 수락**(사용자 확정 · 목서버 전용) — 회차 22 가 BE 사전 차단을 폐기했는데 목서버가 구 계약(**required + enum 6종**)을 들고 있어 ①관제 값이 아직 없는 영상은 전부 `400 Field required` ②우리가 모르는 값은 전부 422 라 **로컬·dev 에서 파이프라인을 한 건도 완주시킬 수 없었다**. 스키마를 `Optional[str]`(상한 100자)로 열고 표준 6종은 종전 전용 서술·그 밖은 **폴백 서술**로 응답한다(값을 지어내지 않음). **G**(`TC-AIMOCK-56~58` 신설, 구 케이스 `유효하지않은_event_type은_422` 폐기·대체). ⚠ **목서버 한정 완화이며 실벤더가 관대하다는 근거가 아니다** — 규격상 required + enum 6종이라 벤더 확답이 오면 되돌리거나 토글로 감싼다 | **0** | **3**(G 3) | **0** | **2,324**(실측) |
| **24** | **2026-08-06** | **★검증이벤트유형 입력을 「6종 프리셋 + 직접 입력」으로 개방 + 판정축 목록 → 형식**(사용자 확정 "6+수동입력") — 회차 22 가 위탁 게이트를 열었는데 **우리 쓰기 통로(dev 업로드 화면·DTO·서비스)만 6종에 갇혀** 있어, 벤더가 enum 을 넓히거나 미지 값을 시험할 때 화면으로 재현할 수 없었다(관제 인입분은 우리 코드를 거치지 않아 원래 어떤 값이든 들어온다 — 비대칭). ①**화면** — select 에 `직접 입력` 추가(센티넬 `__manual__` 은 화면 모드 표식이라 **전송되지 않는다**), 고르면 자유입력칸(label 연결·`maxLength=20`)이 열리고 **프리셋에서 전환하면 이전 값을 비운다**(안 비우면 화면은 "직접 입력"인데 `fire` 가 나가는 조용한 오전송) ②**서버** — 판정 단일 원천 신설 `LsDataIngest.isVrfcEvntTypeFormatValid`(소문자·숫자·밑줄 + 컬럼 폭 20자)를 DTO `@AssertTrue` 와 `TusUploadService` 가 함께 호출. **길이는 `VARCHAR(20)` 에서 오고**(안 막으면 INSERT 시점 DB 오류 500), **문자 집합은 이 값이 외부 벤더 바디·로그에 그대로 실리기 때문**(CWE-117) ③`VRFC_EVNT_TYPES` 6종은 **허용목록이 아니라 프리셋**으로 격하. **B**(TC-ULD-038·044 정정 + `TC-ULD-049~050` 신설) · **H**(TC-FE-335 정정 + `TC-FE-356~358` 신설) | **3**(B 2 · H 1) | **5**(B 2 · H 3) | **0** | **2,329**(실측) |

| **25** | **2026-08-06** | **★라벨링 저장 진입점 일원화 — 헤더 `[저장]` 제거 + 좌측 도구바 잘림 결함(TC-FE-306) 해소**(사용자 확정) — 헤더 우측 `[저장]` 과 좌측 도구바 저장 아이콘이 **같은 `handleSave` 를 부르는 중복 진입점**이었다. ⚠**순서를 뒤집어** 진행 — 도구바에는 짧은 뷰포트(≈690px 이하)에서 맨 끝 `저장` 이 잘려 **영구 클릭 불가**인 미해결 결함이 회차 5 부터 등재돼 있었으므로, 먼저 고치고 헤더를 지웠다(반대로 하면 알려진 결함 위에 유일한 대안을 없앤다). ①**TC-FE-306 해소** — 버튼 목록에 스크롤 계약(`flex-1`+`min-h-0`+`overflow-y-auto`+`overflow-x-hidden`) + **툴팁 `createPortal` → body**(`position: fixed`). `overflow-y` 를 non-visible 로 두면 `overflow-x` 도 auto 로 강제돼 툴팁이 함께 잘리므로 **portal 이 선행 조건**이었다 ②헤더에는 **버튼이 아니라 상태**만(`저장 중...`(구 버튼 라벨 이관) / `● 편집 중` / `✓ 저장됨`) ③**잠금(`LOCKED_FOR_REDEIDENT`)은 편집 차단(busy)과 다른 축**이라 `LabelingPage` 가 `saveDisabled` 로 도구바에 전달(누락 시 조용히 샌다). `Ctrl+S`·`handleSave`·WORKER `[검수제출]` **무변경**. ⚠ jsdom 은 레이아웃 미계산이라 잘림을 폭·높이로 단언하면 **거짓 통과** — 구조 계약만 고정하고 mutation 2종으로 실증. **H**(TC-FE-306 미해결 → 해소 · **H-24 `TC-FE-359~363` 신설**) | **1**(H 1) | **5**(H 5) | **0**(테스트 코드 `LabelHeader_액션_primary_토큰` 1건 폐기 — 대응 TC 행 없음) | **2,334**(실측) |
| **26** | **2026-08-07** | **★라운드 정합 반영 — 신고 게이트 확대 · 속성값 인가 · 업로드 세션 존재 오라클 차단 · 개인정보 메타 출처 · 캔버스 상단 옵션바.** ①**비식별 신고 게이트(412) 6경로 확대** — 속성값 조회·저장(**C**, C-7 `TC-LABEL-163~172`) + event_annotation 저장·승인·반려 · 검수 승인(**D**, D-1a `TC-REVIEW-053~058`). 라벨 본문 저장·개인정보 메타 PUT 이 이미 412 인데 **같은 축의 여섯 경로가 열려 있어** 차단이 우회됐다. 전부 역할 무관·인가 이후·해소 시 자동 개방이며, **`GET /v1/videos/{rawSn}/event-annotation`(조회)은 막지 않는 것이 확정 동작**이다(TC-REVIEW-057). ②**속성값 경로에 영상 단위 인가 신설(CWE-639)** — 검사 순서를 **존재(404) → 인가(403) → 게이트(412) → 업무 규칙(400)** 으로 결박했고, 그 순서 때문에 **구 동작 2건이 뒤집혔다**(미연결 객체 PUT 400→403 · 고아 객체 200→404). ③**업로드 세션 소유자 불일치 403 → 404** — 내부 6경로(**B**, B-23 `TC-ULD-051~056`) + 포털 3경로(**F**, `TC-TUS-012` 정정 + `TC-TUS-033~035`). 403 은 **세션 존재 오라클**(CWE-209)이라 미존재와 상태코드·메시지가 동일해야 한다. **인증 401 · 역할 403 · 만료 410 · offset 409 는 불변**. ④**프리셋 `eventTypeCd` 상한 32 → 20**(**C** `TC-PRESET-19~20`) — 21~32자가 DTO 를 통과한 뒤 `VARCHAR(20)` INSERT 에서 DB 오류(500)로 터지던 것을 입구 400 으로 옮겼다. ⑤**프레임 개인정보 메타 조회에 출처 3필드 신설**(**E** `TC-META-048~050`) — 카탈로그가 회차 4 부터 `*Source` 를 기대결과로 적고 있었으나 **응답 계약에는 없어 화면이 받을 수 없던** 갭을 닫았다(하위호환은 추가만). ⑥**캔버스 상단 옵션바 신설**(**H**, H-25 `TC-FE-364~377`·`TC-A11Y-016`) — 삭제·실행취소·다시실행·저장 + 프레임 이동이 좌측 도구바/헤더에서 옵션바로 모였다(양쪽 중복 0). 헤더의 프레임 위치 표시 폐지 · `DarkToolbar` → `ToolBar` 개명. ⚠ **이 회차가 반영하지 못한 것** — `GET /v1/dev/batch/pending` 페이징은 **현재 작업본에 구현이 없어**(응답이 여전히 `List<Long>`) 케이스를 만들지 않았다 · 공지 발행 일시 정책 정정은 **카탈로그에 공지 BE 클러스터 자체가 없어** 대응 행이 없다(B 회차 11 이 이미 등재한 기존 갭) · 검수 화면 하단 액션 바 폐지(`ReviewActionBar` 제거)는 이번 반영 목록 밖이라 손대지 않았다 | **10**(E 1 · F 3 · H 6) | **45**(B 6 · C 12 · D 6 · E 3 · F 3 · H 15) | **0** | **2,379**(실측) |
| **27** | **2026-08-08** | **★D6 통지 트리거 반전 + 검수 헤더/프레임이동바 FE 배선 + 공통 컴포넌트 6종 조합형 전환 + KRDS 팔레트 완결.** ①**D6 — 산출·통지 트리거를 「검수 승인」한 곳으로 일원화**(사용자 확정, 구속 · **D**, D-1b `TC-REVIEW-059~068` + D-6a `TC-NOTIFY-059~062`). **구 정책(폐기)**: 승인 후 수정 시 저장 즉시 export 재생성 + 관제 통지. **새 정책**: 수정은 `LS_RAW_DATA_STATUS.REVLT_YN`(V177 신설)로 표시만 세우고 **재승인** 시점에 나간다 — 표시 있는 APPROVED 는 상태 전이 없이(멱등) 재승인(TC-REVIEW-060) / 표시 없는 재승인 시도는 여전히 **409**(TC-REVIEW-059) / 재승인은 `TASK_COMPLETED` 를 다시 보내지 않고 **다음 flush tick 에서 축적분이 `TASK_MODIFIED` 로 나간다**(TC-REVIEW-061) / 축적 윈도우 짝이 어긋나면 폴백+WARN(TC-REVIEW-062) / **반려는 재검토 축과 무관하게 완료 상태에서 그대로 비활성**(TC-REVIEW-065, 데이터마트 뷰가 라이브 APPROVED 만 게이트하는 것과 동일 이유로 확장하지 않는다) / `needsRecheck` 는 목록·단건 응답에 노출(TC-REVIEW-068, API-008/API-014). **보류 중 flush 안 됨** — 후보 선정(`findFlushableAnchors`)·클레임(`claimForFlush`) 두 지점 모두 `NOT EXISTS(REVLT_YN='Y')` 필터로 경합 창까지 차단(TC-NOTIFY-059~061). ②**검수 헤더/프레임이동바/재승인 FE 배선**(**H**, H-8a `TC-FE-378~389`) — 승인·반려는 `ReviewHeader` 단독 담당(구 `ReviewActionBar` 삭제), 헤더 바로 아래 별도 상단바에 라벨링 캔버스와 동일한 `FrameNavigator` 재사용([H-25](H-frontend-e2e.md#h-25-캔버스-상단-옵션바-신설--편집-액션프레임-이동-이관-screen-005--2026-08-07-신설) 말미의 "미배선" 서술을 **정정**), `needsRecheck` 시 승인 버튼만 재활성 + 목록 배지 병기. ③**공통 컴포넌트 6종 조합형 전환**(**H**, H-26 `TC-FE-390~423` + TC-FE-184 정정) — `Field`·`Card`·`DataTable`(`@tanstack/react-table`)·`Select`(Radix, 빈 문자열 센티넬)·`Checkbox`(Radix 3상태)·`Textarea`(자동확장). `UI-003` 사양 자기모순(`SelectTrigger` 기본 32px vs DS-001 44px 하한) 해소. ④**KRDS 팔레트 완결**(**H**, H-27 `TC-FE-424~427`) — 구 `#0F4C97` 계열 0건, info 배지 대비 교정. ⚠ **개발용 배치 대기열 페이징**(Q7, **B**, B-24 `TC-BATCH-215~221`) — 회차 26 이 "구현 없음"으로 보류했던 그 갭이 이번에 반영됐다(`page`/`size`, 상한 100, `List<Long>`→`Page` Breaking) | **1**(H TC-FE-184) | **71**(B 7 · D 14 · H 50) | **0** | **2,450**(실측) |

| **28** | **2026-08-08** | **★AI 정밀도 기본값 전용 조회 경로 신설 + 검수 상세 우측 패널 3탭 구조.** ①**`GET /v1/ai-defaults`**(**A**, A-8 `TC-AIDEF-001~012` · **H**, H-29 `TC-FE-442~445`) — 라벨링 화면이 AI 도구 슬라이더 초기값을 받는 경로를 **관리 영역 밖**으로 분리했다. **구 동작 2단계 모두 폐기**: 검수자 전용 `GET /v1/manage/configs` 무조건 호출(WORKER 진입마다 403 누적) → 역할 게이팅(403 은 사라졌지만 **작업자가 기본값을 아예 못 받음** — 문제를 옮긴 것). 그 조회의 읽기 권한만 넓히는 안은 **채택하지 않았다** — 응답에 설정 전량 + **마지막 수정자 계정 식별자**가 실린다(TC-AIDEF-008). 핵심 가드는 **응답 키 집합 정확 단언**(TC-AIDEF-007, 설정 키가 늘어도 새지 않는다)이고 **쓰기 대응물 부재**를 405 로 결박(TC-AIDEF-012). 조회 실패 3종(NOT_FOUND·비숫자·타입 불일치)은 전부 **생략**으로 낮춰 화면이 자체 상수로 폴백한다. FE 가드는 **관리 영역 0건 + 전용 경로 1건 이상 두 축을 함께** 센다(한쪽만 세면 구 처방으로 되돌아간다). ②**검수 상세 우측 패널 3탭**(**H**, H-28 `TC-FE-428~441`) — 구 구현이 5개 패널을 세로로 나열하던 것을 사양(`SCREEN-019`)의 **객체(기본)/메타/이슈** 구성에 맞췄다. 이 종류 변경의 실제 위험은 배치가 아니라 **조용한 소실**이라 구 5패널 기능 전수 잔존을 결박(TC-FE-439)했고, **탭은 표시만 전환**하므로 객체 선택이 스토어에 남아 탭 왕복 후에도 복원된다(TC-FE-438). WAI-ARIA Tabs 전량(상호 연결·roving tabIndex·←/→ 순환·Home/End) + 객체 트리 listbox/option 계약 유지 회귀. 승인·반려 헤더 단독은 aside 축에서 한 번 더 결박(TC-FE-440). ⚠ **D 소급 반영 +11** — 통계 지표 축 통일 회차(D 회차 17 `TC-STAT-006~010` · 18 `TC-STAT-011~016`)가 D 파일에는 등재됐으나 **이 장부에 반영되지 않아** 278 로 정체돼 있었습니다. 케이스를 소급 신설한 것이 아니라 실측으로 재정렬한 것입니다(278 → **289**) | **0** | **30**(A 12 · H 18) | **0** | **2,501**(실측 — 신규 30 + D 소급 반영 11) |
| **30** | **2026-08-08** | **★조작 수단 보강 + 사양 정합 9건(전부 FE · H)** — H-31~H-38 신설. ①**마킹 칩 개별 삭제**(H-31 `TC-FE-456~459`) — 구 동작은 삭제 수단이 `Del`/`Backspace` **단축키뿐**이라 마우스만 쓰는 사용자에게 개별 삭제 경로가 없었다. 단축키는 유지(대체 아님) → TC-FE-114·125 정정 ②**영상 목록 기간 필터**(H-32 `460~467`) — date 입력 두 벌 직접 재구현 → 사양 컴포넌트 `DateRangePicker`(UI-029, 소비처 0 이라 계약이 실행으로 확인된 적 없던 컴포넌트). **값 형식·URL 직렬화·상호 min/max 제약 무변경** → TC-FE-301 근거 정정 ③**검수 프레임 썸네일 스트립**(H-33 `468~476`) — 최하단 Footer → **캔버스 위**(SCREEN-019) + 접기/펼치기(기본 펼침, 접힘은 **DOM 제거**). ★접기 토글은 `role="toolbar"` **바깥**(툴바 안이면 토글 포커스의 ←/→ 가 프레임을 바꾼다) ④**라벨 속성 정의 사이드 시트**(H-34 `477~485`) — 인라인 `<section>` → 우측 시트(SCREEN-035). 바깥 클릭으로 닫지 않고(작성 중 유실 방지) 중첩 폼 중 ESC 는 폼만 닫으며 **행 선택 강조 폐기**. portal 전환으로 XSS 가드 단언 축을 `container`→`document` 로 정정(그대로 뒀으면 **항상 통과하는 가짜 가드**) ⑤**캔버스 회전·격자**(H-35 `486~501`) — 회전은 표시 전용이라 **저장 좌표 불변**(`geometry.angle`=0, 레이어 변환 축)이고 회전 중 편집 봉인(좌표계 어긋남 → **찍은 위치와 다른 좌표 저장** 방지). 격자는 `listening=false` 순수 오버레이 ⑥**영역 확대**(H-36 `502~516`) — **라벨을 만들지 않는다**(같은 좌클릭 드래그가 확대·도형 생성 양쪽으로 발화하면 데이터 오염). 12px 미만 무시 · 기존 줌 상·하한 공유 · 회전 중에도 동작 · 캔버스 밖 종료는 취소 ⑦**페이지네이션 계약 전환**(H-37 `517~533`) — `size`+`totalElements` 되계산(두 번째 진실원) → 서버 `totalPages` 그대로. ⚠ **사용자 가시 변화**: 총 건수 요약이 컨트롤에서 제거돼 **검수 목록·공지 목록·사용자 관리 3화면**에서 총 건수가 사라진다 ⑧**목록 3화면 공용 수렴**(H-37) — 영상 처리 현황·작업 목록의 **고정 1~7 번호**, 증강 요청의 **번호 없는 이전/다음**은 8페이지 이상에서 **마지막 페이지 도달 경로가 없었다**. 번호 접근명 `N페이지`·터치 타깃 44px 도 함께 확보 → TC-FE-183 정정 ⑨**사용자 관리 저장 잠금**(H-38 `534~537`) — 사양의 "원래 값과 같으면 비활성" 미구현이라 **PATCH 없는 빈 왕복**이 가능했다. ⚠ 회차 29(H-30) 행이 이 표가 아니라 파일 말미에 붙어 있습니다(위치만 어긋난 것이며 수치는 반영돼 있습니다) | **4**(H TC-FE-114 · 125 · 183 · 301) | **82**(H) | **0** | **2,583**(실측) |
| **31** | **2026-08-09** | **빈 상태 안내 + 시각 일관성(전부 FE · BE 무변경)** — ①**마킹 마크 0건 빈 상태**(H-39 `TC-FE-538~542`): 칩 목록을 `localMarks.length > 0` 일 때만 렌더해 **0건이면 패널이 통째로 사라졌고**, 사용자가 "기능이 없다"와 "아직 마크가 없다"를 구분할 수 없었다. 확정 사양대로 빈 상태 안내를 노출한다. ★안내는 **모드별로 다르다** — 자동 모드에서 `Space` 는 발화하지 않으므로(`mode !== 'MANUAL'` 조기 리턴) 수동 안내를 그대로 쓰면 **거짓 안내**가 된다. 칩 개별 삭제(H-31)·`Del`/`Backspace` 는 **유지**되고 마지막 마크 삭제 시 빈 상태 복귀까지 고정 ②**글리프 재유입 가드 등재**(H-40 `543~545`): 이모지·기호 전량 아이콘 교체(`9ef40bdc`)는 반영됐으나 **가드가 카탈로그에 없어** 장부상 검증 대상 밖이었다 — 기준선이 빈 상태로 유지되는 것과 교체가 **의미를 잃지 않았음**(저장 상태는 문구로 낭독 · 색만으로 전달 금지)을 등재. 신규 결함이 아니라 **누락 등재** ③**폼 컨트롤 모서리 반경 통일**(H-41 `546~547`): `Input` 만 `rounded-lg`(8px)이고 나머지 넷은 `rounded-md`(6px)라 같은 폼에서 텍스트 입력만 혼자 둥글었다 → 다수에 맞춰 내린다. 검사 명제는 "6px 이어야 한다"가 아니라 **"다섯이 같다"**. **RED 실증 4종**(반경 되돌림 · 구 조건부 렌더 되돌림 · 모드 분기 제거 · 아이콘→기호 되돌림) 모두 해당 가드만 정확히 FAIL | **0** | **10**(H) | **0** | **2,593**(실측 — **H 클러스터 +10 만 반영한 값**) ⚠ **동시 작업 주의**: 이 회차를 쓰는 시점에 **E 클러스터(증강)에 다른 작업의 미커밋 신설분 +8**(294 → 302)이 워크트리에 함께 있었습니다. 이 행의 2,593 은 **그 +8 을 제외한** `직전 2,583 + 이번 H +10` 이며, E 쪽 회차 행이 등재될 때 총계를 **2,601 로 재측정**해야 합니다 — 그쪽이 2,583 을 기준으로 이어 세면 이번 +10 이 지워집니다. **→ 회차 32 에서 두 증감을 합산해 2,601 로 재측정했습니다.** |
| **32** | **2026-08-09** | **증강 결과 응답에 요청일시 추가**(E-2B `TC-AUG-122~129`) — 결과 화면 작업 요약이 요구하던 시각 값을 **공급하는 API 가 하나도 없었다**(결과 응답에는 채택·반려 결정 시각만 있고 축이 다르다). 목록 API 가 이미 쓰던 산출식(그룹 최소 등록일시)을 **한 벌로 추출해 두 경로가 공유**하게 하고 결과 응답에 필드를 추가했다. ★핵심 가드는 **조기 반환 지점**이다 — 항목 0건·페이지 범위 밖 두 곳이 있어 한 곳만 채우면 **빈 잡에서만 값이 비는** 결함이 된다(RED 실증). 값이 없을 때 화면이 `-` 를 보이는 축과 **필드가 없는 구 응답 하위호환**도 함께 고정. ⚠ FE 3행을 H 가 아니라 **E 에 넣었다** — 같은 시점 H 파일이 다른 작업의 미커밋 편집 중이라 `TC-FE` ID 충돌을 피한 것이며, 이관이 필요하다 | **0** | **8**(E) | **0** | **2,601**(실측 — 회차 31 H +10 과 이번 E +8 을 합산) |
| **33** | **2026-08-09** | **★문의 스레드 응답에 작성자 역할 추가 — 검수자 문의 ↔ 작업자 문의 구분(추가만 · 하위호환)** — 문의는 원래 작업자→검수자 단방향이었으나 **검수자도 등록**하게 되면서 "누가 낸 문의인가"가 실질적 의미를 갖는데, 작성자 역할 축이 **댓글**(`IssueCommentResponse.authorRoleCd`)에만 있고 **스레드** 응답에는 없어 화면에서 두 문의가 구분되지 않았다(이름·사번만 표시). **D**(D-1c `TC-REVIEW-069~077`) — `IssueThreadResponse.reportedUserRoleCd` 신설. 댓글 축과 **같은 값 공간·같은 타입**이되 이름은 짝 필드(`reportedUserNo`/`reportedUserName`) 접두를 따른다(같은 응답 안의 `comments[].authorRoleCd` 와 **가리키는 사람이 달라** 같은 이름을 쓰면 갈린다). **N+1 회피** = 이름 축과 같은 형태로 페이지 사번을 모아 `LsUserRoleRepository.findByUserNoIn` **1회**(표시명 `LS_ACNT_USER` ↔ 역할 `LS_USER_ROLE` 로 테이블이 달라 쿼리를 합칠 수 없고 각각 1회다) · 사번 파싱은 `UserNameResolver.toUserNo` **단일 규칙 재사용** · 해석 실패는 `null`(지어내지 않음) · 등록 응답은 토큰 역할을 그대로 써 **되읽지 않는다**. **해석 시점 비대칭은 의도** — 댓글은 작성 시점 컬럼, 스레드는 조회 시점 현재 매핑(스레드에 역할 컬럼을 신설해도 과거 행은 백필 근거가 없어 전부 `null`). **H**(H-42 `TC-FE-548~552`) — 표기는 **기존 헬퍼 `issueAuthorLabel` 재사용**(복제 금지)에 **빈 역할 분기**만 추가해 역할 `null` 이면 빈 괄호 없이 이름만 남긴다(댓글 축도 함께 보호). mutation 실증 5종(BE 3 · FE 2). ⚠ **클러스터 표 E 행이 294 로 stale** 이었다(실측 302 — 회차 32 의 E +8 이 합계 행에만 반영됨) — 이번에 8클러스터 전량 실측해 재정렬했다 | **1**(README E 행 stale 정정) | **14**(D 9 · H 5) | **0** | **2,615**(8클러스터 전량 실측) |
| **34** | **2026-08-09** | **사양 정합 3건 + 교차 관심사 가드 1건(전부 FE · H · BE 무변경)** — ①**증강 결과 작업 요약 5칸 확정**(H-43 `TC-FE-553~555`): 요청일시를 더하면서 **6칸**이 됐고 확정 사양에 없는 `증강 유형` 칸이 남아 있었다. 그 값은 사라지지 않는다 — 유형은 **결과 항목 단위** 정보라 항목 탭 라벨과 비교 이미지 라벨이 항목마다 **항상** 보여 주며, 오히려 요약 쪽이 여러 항목의 유형을 **합집합으로 뭉쳐** 어느 항목의 유형인지 말하지 못했다. 그리드 열 수도 5로 되돌린다(칸만 지우면 마지막 열이 빈 채로 남는다). ⚠ 살아 있는 브레이크포인트는 **`md`·`xl` 둘뿐**이라 `sm:`/`lg:` 로 되돌리면 클래스가 생성되지 않아 반응형이 통째로 사라진다. **칸 개수만 세면 어느 칸이 바뀌었는지 모른 채 통과**하므로 라벨 목록을 순서까지 고정 → **TC-FE-213 정정**(구 6번째 칸 `증강 유형 (이 페이지)` 폐기 명시) ②**마킹 완료 버튼 항상 활성 + 마크 0건 안내**(H-44 `TC-FE-556~561`·`TC-A11Y-017`): **구 정책 폐기** — 마크 0건이면 툴바가 버튼을 비활성화하고 단축키 경로는 조용한 early return 이라 사용자는 "눌리지 않는다"·"눌렀는데 아무 일도 없다" 만 겪고 **왜인지를 화면이 말하지 않았으며** 보조기술에는 아무 신호도 남지 않았다. 확정 사양은 **항상 클릭 가능 + 누르면 토스트로 사유를 말하고 제출만 차단**이고, 그 토스트가 `role="alert"` 라 낭독된다(TC-A11Y-017 — **버튼을 항상 활성으로 두는 근거**라 이 단언이 빠지면 시각 사용자에게만 사유가 전달되는 상태로 되돌아간다). ★**저장 중 비활성은 별개 축이라 유지**한다 — 함께 걷어내면 in-flight 재클릭으로 마킹 POST 가 중복 발화한다. 버튼 클릭과 `Enter` **두 진입점이 같은 안내**를 내는지와, 1건 이상이면 **안내 없이 정상 제출**되는 역방향까지 고정 → **TC-FE-118 정정**(막는 **수단**이 바뀌었을 뿐 `mutate` 미발생은 그대로 유효) ③**팬 도구 컴포넌트 폐지**(H-45 `TC-FE-562`): 참조 0건 파일 제거. **도구 열거값과 표시명은 유지**한다 — 포털 업로드 라벨링 화면이 그 값으로 `이동` 버튼을 **실제로 렌더**하므로 함께 지우면 그 화면의 도구가 사라진다. 화면 이동 조작 자체는 캔버스 셸(스페이스 홀드+드래그·중클릭 드래그)이 담당하므로 컴포넌트 폐지가 조작을 없앤 것이 아니다 ④**목 자식 전달 계약 가드**(H-46 `TC-FE-563~568` · 교차 관심사): 캔버스 계열 **67개 파일이 konva 목을 각자 복제**하는데 **그 계약을 검증하는 테스트가 한 건도 없었다**. 67벌을 동시에 끊고 전체를 돌리면 **4개 파일 18건만 실패하고 63개 파일은 전량 통과**한다(recharts 공용 목도 끊어도 38건 전부 통과) — 즉 지금은 무해하지만 **신호가 0**이다. konva 는 **소스 스캔**(목이 각 파일 안에 복제돼 밖에서 실행할 수 없다) · recharts 는 **동작 검증**(공용이라 실행 가능)이며 "일관성"을 이유로 **두 방식을 통일하지 말 것**. 가드 자신이 눈머는 것을 막는 장치 3종(스캔 대상 0건이면 실패 · 팩토리 추출 실패면 실패 · 판정기 양성 대조군) 포함. ⚠ **"아무것도 안 잡는 것처럼 보이는 것이 정상"** 이며 평시에 늘 통과한다 — 통과한다는 이유로 지우면 63개 파일이 계약 파손을 다시 조용히 통과시킨다. **후속** — 근본 해소는 목 67벌을 **공유 모듈 한 곳으로 모으는 것**이고 그때 H-46 은 폐기 대상이 된다(계약을 공유 모듈이 직접 단언하게 되므로). 67개 파일을 건드리는 별건이라 이번 범위 밖이다. ⚠ **①의 정정 대상이 예고와 달랐다** — 착수 지시는 "기존 케이스의 기대결과에서 `증강 유형 (이 페이지)` 폐기" 였으나 **그 문자열을 담은 카탈로그 행이 실재하지 않았다**(전 클러스터 grep 0건). 요약 카드의 범위 표기 테스트 자체가 미등재 상태라, 같은 요약 카드를 가리키는 **TC-FE-213** 에 정정을 실었다 | **2**(H TC-FE-118 · 213) | **17**(H) | **0** | **2,632**(8클러스터 전량 실측) |
| **35** | **2026-08-10** | **★연동 서버 주소 설정 + 관리자 단기 유효창(R11)** — 외부 연동 4종(비식별 서버·AI 추론 서버·외부 시계열 분석 벤더·관제 통지 수신처) 주소를 운영 화면에서 **재기동 없이** 바꾼다. **A**(A-9 `TC-ENDP-001~017` · `TC-ADMS-001~027`) · **H**(H-56 `TC-FE-622~633`). ①**설정 키 = 애플리케이션 속성명**(별도 키명을 만들면 매핑표가 두 번째 진실원이 된다) · **시드하지 않는다**(행이 없으면 배포 기본값 — 그래서 최초 저장 시 행을 만들고 목록에는 저장 전까지 안 나온다). ②**★즉시 반영** — `WebClient` 의 `baseUrl` 은 빈 생성 시점에 고정돼, 배선을 빠뜨리면 **저장은 되는데 호출 주소는 그대로**인 "코드는 맞는데 실동작 0건" 결함이 된다. 실제 소켓 두 개로 판정하고 필터 배선 제거 mutation 으로 가드 유효성을 확인했다. ③**SSRF** — 스킴 allowlist + **해석 후** 내부망 대역 차단 + **전송 직전 재검증**(DNS rebinding, fail-closed). 판정기는 `ExternalUrlPolicy` 재사용이라 회차 3·4 의 IPv6 ULA·CGNAT·NAT64 우회 판정을 승계한다. 거부 문구에 **해석된 IP 를 싣지 않는다**(CWE-209). ④**관리자 단기 유효창** — 패스워드 해시·속도 제한은 권한 자가부여와 공유하되 진입점만 분리(그 서비스는 역할 없는 사람 전용이라 REVIEWER 에게는 409). 토큰은 **무상태 서명**이라 2노드 교차 검증되고 **새 테이블이 없다**. 서명 키를 JWT 키에서 파생해 인증 토큰과 상호 통용되지 않으며 **역할 클레임이 없어 권한을 승격시키지 않는다**. 유효기간 상한 30분은 설정으로도 넘을 수 없다. ⑤게이트는 컨트롤러가 아니라 **서비스**에 둔다("게이트를 호출처마다 배선하면 샌다" 차단). 주소 외 키는 무영향(기존 계약 회귀 가드 동반). ⚠ **`authoring.integration.deidentify.base-url` 은 현 배포 형상에서 실효가 없다** — 그 키가 구동하는 빈은 주입 대상 0건이고 실제 비식별 위탁은 `kpst.deid.base-url` 로 나간다(별도 보고). | **1**(`SystemConfigControllerTest` 목록 길이 단언 — 허용 키 수 기준 → **시드 키 수** 기준. 시드하지 않는 설계 때문에 두 수가 갈렸고, 같아야 한다고 단언하면 그 설계를 되돌리라는 압력이 된다) | **56**(A 44 · H 12) | **0** | **2,770**(8클러스터 전량 실측) ⚠ **동시 작업 주의**: 이 회차 시점 워크트리에 **다른 회차의 미커밋 신설분**이 B(494→542) · C(311→317) 에 함께 있습니다. 이 행의 2,770 은 그 분량까지 포함한 **전량 실측**이며, 클러스터 표의 B·C 행은 **그쪽 회차가 등재할 때** 갱신됩니다(이 회차가 남의 장부를 대신 고치지 않습니다). |
| **36** | **2026-08-10** | **★R11 정책 반전 2건(사용자 확정) — 구 동작 폐기.** ①**비식별 키 교체** `authoring.integration.deidentify.base-url` → **`kpst.deid.base-url`**: 구 키가 구동하는 빈은 **주입 대상 0건**이라 바꿔도 위탁 주소가 달라지지 않았다. 실제 위탁이 나가는 빈에 배선을 옮기고 구 키는 화이트리스트에서 제거(실효 0 인 칸을 화면에 남기지 않는다). **진행조회만 저수준 클라이언트라 필터 훅이 없어** 절대 URI 로 따로 처리했다 — 빠뜨리면 프로젝트는 새 서버, 진행조회는 옛 서버로 갈려 작업이 영원히 완료되지 않는다. ⚠ **TLS 구성은 주소를 따라가지 않는다**(자체 CA 신뢰가 기동 시점 스킴으로 고정 — 인증서 검증을 낮추지 않고 WARN 으로 드러냄). ②**IP 대역 차단 폐지**: 사설·링크로컬·루프백 차단 + **전송 직전 재검증(DNS rebinding)** 전부 제거. 근거 — 이 연동들은 **실제로 내부망의 별도 서버에 있을 가능성이 높아** 대역으로 막으면 정당한 대상을 막는다. 망 통제는 **인프라 계층** 책임. 남는 검증은 **스킴 + 형식**뿐. 대역 케이스는 **지우지 않고 기대값을 뒤집었다**("막지 않는다"가 계약). 회차 35 가 추가했던 `ExternalUrlPolicy.publicNetwork()`·`Violation`·`ViolationException` 은 도달 불가가 되어 제거했고 그 파일은 **완전 원복**(기존 소비자 계약 무변경). **A**(TC-ENDP-018~020 신설 · 003·004·012·`TC-ADMS-025` 반전 · 005 전제 정정 · 006 폐기) · **H**(TC-FE-634 신설 · 628 교체) | **7** | **4**(A 3 · H 1) | **1**(A) | **2,774**(8클러스터 전량 실측 — 회차 35 의 동시 작업 주의 사항 동일: B·C 의 타 회차 미커밋 신설분 포함) |
| **37** | **2026-08-10** | **★R11 연동 주소 설정 보안 리뷰 지적 반영 — 코드 동작 변경 8건, 전부 A(A-9 `TC-ENDP-026~037` · A-6 `TC-LOG-006`).** ①**비식별 신뢰 가드가 화면 저장으로 우회됐다** — 판정이 기동 시 배포값 1회뿐이라 주소가 호출 시점에 정해지는 R11 이후 실제 값과 갈렸다. 저장 시점에도 **같은 판정 함수**를 태워 운영 400 / 그 외 WARN(기동 시와 동일 강도). ⚠ 축은 **호스트명**이지 IP 대역이 아니며 `TC-ENDP-028` 이 **폐지된 대역 판정의 부활을 차단**한다. ②**주소를 바꾸면 정적 자격증명이 새 호스트로 따라갔다**(CWE-522) — 호스트가 다르면 인증 헤더를 떼고 WARN(값 미출력). 판정 축은 호스트라 포트·경로 변경에는 유지. ③**주소 userinfo 400 + 로그 마스킹 이중 방어** — 마스킹이 키워드 기반이라 `user:pass@` 를 못 잡는 것을 테스트가 실측으로 고정. ④**override 부재가 캐시되지 않았다** — 행 없음이 정상인데 예외가 나고 **예외는 캐시되지 않아** 외부 호출마다 DB 왕복(대화형 핫패스 포함). `findString`(`Optional`)로 부재를 값으로 캐시하되 기존 계약·즉시 반영은 불변. ⑤경계·기능 3건(base 경로 세그먼트 경계 · 언더스코어 호스트 통과(기대값 반전) · CORS `X-Admin-Session`). **HIGH 2건 mutation 실증.** ⚠ 전체 회귀 미실행 — 영향 범위 481 케이스만 실행 |
| **38** | **2026-08-10** | **★표 표면 정합(R13) 머지 + H 클러스터 번호 재부여.** 표 표면·헤더 타이포 정합이 별도 브랜치에서 병행돼 머지로 합류했다. 두 브랜치가 같은 부모에서 갈라져 **회차 번호와 케이스 ID 를 동시에 점유**해 실질 충돌이 났고(소스 충돌은 0), 26건·4섹션으로 더 크고 내부 상호참조가 조밀한 표 정합 쪽을 고정한 뒤 **R11·R2 분을 H-51~52 → H-55~56 / `TC-FE-591~608` → `617~634`(+26) 로 이동**했다. 내용은 한 글자도 바꾸지 않고 번호만 옮겼으며 상호참조(README · C-marking-labeling · A-auth-common · v2-wiki 화면 IA)도 함께 맞췄다. ⚠ **ID 재부여는 이 카탈로그에서 이례적**이라 다음 사람이 옛 번호로 찾을 수 있게 이 행을 남긴다 — `TC-FE-591~616` 은 이제 **표 표면 정합** 소관이고 연동 서버 주소·신고 버튼 비활성은 `617~634` 다. 합계는 8클러스터 전량 재측정으로 갱신했고, 그 과정에서 **클러스터 표 B·C 행의 stale**(다른 회차 신설분이 커밋됐는데 표에 미반영)도 함께 정정했다 | **0**(번호 이동은 정정이 아니다 — 기대결과·전제·근거 무변경) | **0** | **0** | **2,819**(8클러스터 전량 실측 — B 542 · C 317 · H 673 이 직전 기재값과 갈렸다) |
| **40** | **2026-08-12** | **★배치 실패 복구(R1) 등재 — 「전용 배치 실패 관리 화면은 신설하지 않는다」가 출발점.** 영상 목록에 이미 배치 단계 `실패` 필터가 있어 같은 일을 하는 **두 번째 목록**을 만들지 않고, 조치를 실패가 이미 보이는 자리(영상 상세 SC-009 · 목록 SC-007 일괄 작업 바)에 붙였다. **B**(B-29 `TC-BATCH-222~288`) — ①실패 사유를 영상 상세 `batchFailureReason` 으로 내리되 **사용자 문구로 변환**한다(판정 단일 원천 `BatchFailureReasonPolicy` 는 내부 원문을 **인자로 받지 않아** 구조적으로 흘릴 수 없다 · CWE-209). ★**단계 미상 실패도 사유만은 전달**되며 그래서 단계 배열이 아니라 **영상 단위 필드**다 ②단건 재실행은 **신규 API 가 아니다** ③**VLM·YOLO·SAM2 3종 수동 스킵/해제** — 저장은 **새 테이블·새 컬럼 없이 `LS_BATCH_PROC_LOG` 재사용**(스킵·해제 둘 다 `SKIPPED` + `ERR_CD` 구분, 판정은 **마지막 표식 행**, 정렬 키는 시각이 아니라 **PK**). ★기존 `SKIP_REASON_*` 재사용 금지 — 그 축은 신고 해소 시 재개 대상이라 사람이 누른 스킵에 쓰면 **사람의 결정을 뒤집고 외부 벤더로 재위탁**한다 ④일괄 재시작은 **부분 성공**이고 한 건도 성공 못 해도 **200** — **일괄 배정과 의도적으로 다른 정책**. **H**(H-59 `TC-FE-698~725`) — 사유는 서버 문구 그대로, 단계 미상 실패는 "확인 불가" 병기, 조작 패널이 **마킹 화면으로 새지 않게** `BatchStageIndicator` 바깥에 둠, 부분 성공 시 **실패분만 선택 유지**. ⚠ **파생영상 비대칭 미해결** — 신규 3종은 거부(400)하나 **기존 단건 재기동은 거부하지 않는다**(계약 변경이라 범위 밖, 두 파일 절 말미에 명시). v2-wiki [07 §7.5-1](../v2-wiki/07-batch-pipeline.md)·[05 §5.5.4](../v2-wiki/05-video-management.md)·[04 화면·IA](../v2-wiki/04-screens-ia.md)·[09](../v2-wiki/09-vlm-timeseries.md)·[18](../v2-wiki/18-database.md) 동기화 동반 | **0** | **95**(B 67 · H 28) | **0** | **3,133**(8클러스터 전량 실측 — 클러스터 표의 **stale 4건**(B·C·D·H)도 함께 정정. 상세는 위 클러스터 표 합계 칸) |
| **41** | **2026-08-12** | **★재기동을 비동기 접수로 반전(사용자 확정) + 건너뛴 단계가 화면에서 사라지던 결함 수정.** 회차 40 이 등재한 조치 수단이 **정상 동선에서 거의 항상 실패로 보이던 것**을 닫는다. ①**재기동은 접수까지만 동기** — 실패 상태 선점(원자 클레임)만 요청 안에서 하고 파이프라인 실행은 전용 풀로 넘긴다. 구 동작은 프레임 추출·외부 추론을 요청 스레드에서 끝까지 기다려 **클라이언트 타임아웃(30초)에 먼저 걸렸고**, 성공한 재기동이 실패로 보여 사용자가 재클릭했으며 일괄 100건이면 워커 스레드 하나가 수십 분 묶였다. ★**전용 풀이 필요한 이유** — 기존 배치 풀은 포화 시 **호출 스레드에서 실행**하는 정책이라 고치려는 결함이 그대로 되살아난다. 거부는 조용히 삼키지 않고 전파해 접수 실패로 알린다(접수 응답이 거짓말이 되지 않게). ★**`PROCESSING` 고착 3중 차단** — 비동기는 예외를 아무도 받지 못해, 마감을 놓치면 그 영상은 이후 모든 재기동이 409 로 막혀 복구 불가가 된다. `@Async` 프록시가 실제로 도는지와 포화 거부 전파는 **mutation 으로 실증**. Quartz 자동 재시도 경로는 이미 스케줄러 스레드라 **코드 변경 0**(이중 비동기 금지). ②**패널 노출 축 분리** — 구 동작은 실패가 있을 때만 노출해, 건너뛴 뒤 재기동이 성공하면 영역이 사라지고 그 단계가 **이후 모든 재기동에서 조용히 건너뛰어진 채 화면에는 완료로 보였다**(되돌릴 창구 없음). 노출을 「실패 ∪ 스킵」으로 넓히되 **건너뛰기 버튼 조건은 넓히지 않고**(그 비확대를 가드로 고정) 스킵 전용 모드에서는 사유 블록을 억지 문구 없이 생략하고 재실행 버튼도 두지 않는다(실패가 없으면 선점할 것이 없어 누를 때마다 막히는 버튼이 된다). ③**접수 후 폴링** — `FAIL` 을 종료로 보는 판정 때문에 접수 직후 폴링이 꺼지는 것을 **호출 수 계수로 실증**하고 유예 창으로 해결(창 안에 시작되면 기존 규칙이 이어받고, 끝내 안 되면 스스로 멈춘다 — 무한 폴링 없음). ④일괄 요청 배열 원소 최솟값 검증 추가(단건은 400 인데 일괄만 건별 실패로 갈리던 축 불일치). **B**(B-29-i `TC-BATCH-289` 외 13건) · **H**(H-59f `TC-FE-726~730` · H-59g `731~734`, 기존 3건 정정) | **5**(H 3 · B 2) | **23**(B 14 · H 9) | **0** | **3,156**(8클러스터 전량 실측) |
| **42~43** | **2026-08-12** | **★「문제가 생긴 곳부터 재시도」로 재기동 경로를 둘로 분리(사용자 확정).** 앞 회차가 "되돌린 스킵이 있으면 완주 영상도 전체 재기동을 받는다"로 풀었는데 그게 **데이터 파괴 경로**였다 — 전체 재기동은 파이프라인을 순회하고 **트랙 보간은 건너뛰는 조건이 없어 항상 돌아** 작업자가 손댄 보간 라벨을 사람이 고쳤는지 보지 않고 전량 삭제·재생성한다(삭제 이력도 승인 스냅샷도 없어 **복구 지점 0**). ①**전체 재기동은 실패 영상 전용으로 환원**(완주 허용 철회 + 직전에 넣었던 승인 이력 게이트도 제거해 기존 계약 복원 — 그 조합은 도달 불가로 확인됨) ②**되돌린 단계를 지목하는 재수행 신설** — 범위를 요청이 고른다(그 단계만 / 그 단계부터 끝까지). 하나로 고정할 수 없는 이유는 **뒤 단계가 그 결과를 쓰는지가 단계마다 다르기 때문**이다(시계열은 뒤가 안 쓰고, AI 탐지는 뒤의 분할·보간이 받아 쓴다). ★★**대상 단계를 요청이 자유롭게 지정하지 못한다** — 서버가 실제로 되돌린 단계만 수락한다(임의 지정이 열리면 앞 단계를 건너뛰도록 강제할 수 있다). ★**선결 결함**: 범위를 표현하려 기존 토글 축을 보니 `BatchStep.isEnabled` 기본이 무조건 참이라 **VLM·보간이 토글을 무시**하고 있었다 — 그대로면 「그 단계만」이 보간을 돌려 막으려던 파괴가 그대로 난다. 기본 구현을 고치고 3곳 복제 규칙을 1곳으로 합쳤다(토글 없는 컨텍스트는 전 단계 실행이라 기존 경로 동작 무변경). ★**실패 영상은 이 재수행을 받지 않는다** — 오케스트레이터가 루프 후 **조건 없이** 완료 마감을 호출하므로, 프레임 추출 실패 영상에 한 단계만 돌리면 **프레임 0장 영상이 완료로 마감**된다. 실패 영상엔 사람이 만든 라벨이 없어 전체 재기동이 안전하다는 것이 이 분리의 대칭점. **B**(B-29-k `TC-BATCH-315~337`) · **H**(H-59i `TC-FE-745~756`) — 화면은 재수행 버튼을 둘 두고 「여기부터」에만 보간 파괴를 **누르기 전에** 알린다(고지 + 취소 두 겹). ⚠ **남긴 한계**: 되돌린 단계 목록이 응답 계약에 없어 화면이 되돌리기 직후의 로컬 상태로 버튼을 띄운다 — **새로고침·재진입 시 버튼이 사라진다**(우회는 다시 건너뛰었다 되돌리기). 영구화하려면 응답 계약 변경 필요 | **6**(B 3 · H 3) | **45**(B 23 · H 22) | **4**(B) | **3,213**(8클러스터 전량 실측) |
| **44** | **2026-08-12** | **★★오토라벨을 「작업 묶음」으로 반전 — 네 라운드 반복 사고의 뿌리를 뽑는다(사용자 확정).** 조작 단위를 개별 단계에서 **시계열 / 오토라벨(AI 탐지 · AI 분할 · 트랙 보간)** 두 묶음으로 바꿨다. 구 구조는 **보간만 단위 밖**이라 어떤 재수행에서도 무조건 돌아, 사람이 손댄 보간 라벨을 **삭제 이력도 승인 스냅샷도 없이** 전량 지웠다(복구 지점 0). 보간이 묶음 **안**으로 들어와 함께 켜지고 꺼지면서 그 구멍이 **구조적으로 소멸**한다. 함께 사라진 것 — **범위 선택**(묶음이 곧 범위라 `scope` 요청 본문·버튼 두 갈래 폐지) · 「AI탐지만 돌리면 뒤가 어긋난다」 · 「AI분할만 돌리면 힌트가 없어 라벨 0건」. 보간 재계산의 **의미도 달라진다** — 이제 "오토라벨을 통째로 다시 만든다"를 사용자가 고른 **예상되는 결과**다. ★**저장 축은 묶음 코드**를 `PROC_STEP_CD` 에 넣는다(신규 컬럼·테이블 0건). 구성 단계 3건에 나눠 적는 안은 **부분 스킵 상태가 스키마상 표현 가능**해 훗날 한 경로만 어긋나도 판정이 흔들리는데, 묶음 코드는 그것을 **표현 자체로 불가능**하게 만든다. 안전 근거는 직접 확인했다 — 그 컬럼을 단계로 해석하는 프로덕션 3지점이 **전부 `SKIPPED` 제외 조회를 경유**하고, 무필터 조회는 프로덕션 소비자 0건이며 CHECK 제약·참조 뷰도 없다(회귀 가드로 고정 — 깨지면 영상 상세가 500 이라 조용히 넘어가지 않는다). ★**재수행 실패가 영상 상태를 훼손하지 않는다** — 선점 직전 상태로 되돌리고 자동 재시도 큐에 넣지 않는다. 안 그러면 완주 영상이 실패로 강등되고 **범위를 모르는 자동 재시도가 전 단계를 돌려** 같은 파괴가 난다. ★★그 복구는 **작업 상태까지** 되돌려야 한다 — 기존 보상이 작업 상태를 안 건드린 전제("완주 축은 작업 상태를 클레임한 적 없다")는 **파이프라인 시작 전**에만 참이고, 실행 중 실패는 진입 가드가 이미 전이시킨 뒤라 배치 단계만 되돌리면 작업자가 **검수 제출을 못 한다**. 두 상황을 한 메서드로 합치지 않았다. 전체 재기동(실패 영상 전용)의 강등·자동 재시도는 **그대로**이며 회귀 가드로 고정. **B**(`TC-BATCH-338~353`) · **H**(`TC-FE-757~762`) | **40**(B 24 · H 16) | **22**(B 16 · H 6) | **6**(B 5 · H 1) | **3,235**(8클러스터 전량 실측) |
| **45** | **2026-08-12** | **적대검증이 남긴 두 갈래 정리(패키지 반전의 마무리).** ①**`Error` 갈래가 복구 계약을 따르지 않았다** — `RuntimeException` 쪽에만 묶음 재수행 원상 복구를 넣어, `Error`(OOM 등)로 빠지면 완주 영상이 배치·작업 상태 모두 실패로 강등됐다. 그러면 **전체 재기동 경로가 열려 보간이 무경고로 전량 재계산** — 다섯 라운드에 걸쳐 막은 파괴가 되살아난다. 두 갈래를 같은 기준으로 통일하되 **목적지는 별도 헬퍼**로 두었다(인자에 숨으면 조용히 뒤바뀐다). ★`Error` 는 **삼키지 않고 되던진다** — 복구는 삼키는 헬퍼 안에 있어 복구가 실패해도 원인이 덮이지 않으며, 전용 `Error` 타입으로 실제 OOM 없이 실증했다. 비동기 러너도 같은 사각(보상 없이 이탈)을 닫았다. ②**재수행 실패 후 화면이 모순된 상태를 내밀었다** — 서버가 영상을 완주로 복구하되 로그에는 실패를 남기므로, 상세가 「단계 전부 완료」와 「배치 처리 실패」를 동시에 보이고 그 재실행 버튼은 **항상 409** 였다. ★**행동은 상태 축이, 정보는 로그 축이** 정하도록 갈랐다 — 버튼은 서버가 실제로 받는 조건(영상 상태)으로, 사유는 그대로 남기되 이름만 「직전 실패」로. 기존 모드 분기(처리 중·스킵만)는 글자 그대로 유지. **B**(`TC-BATCH-354~358`) · **H**(H-59j `TC-FE-763~768`) | **3**(H) | **11**(B 5 · H 6) | **0** | **3,246**(8클러스터 전량 실측) |
| **42** | **2026-08-12** | **★되돌리기가 실효를 갖게 한다 + 재기동 접수 폭 제한.** 회차 40 이 등재한 「VLM·오토라벨 수동 스킵 + 되돌리기」가 **되돌려도 아무 일도 일어나지 않는 상태**였다 — 스킵으로 완주한 영상은 배치 단계가 `COMPLETED` 인데 재기동 클레임이 `FAILED` 만 잡고 다른 재실행 트리거도 없어 **영구 409** 였다. **B**(B-29-j `TC-BATCH-303~314`) — ①**되돌린 단계가 실제로 있는** 완주 영상도 `COMPLETED→PROCESSING` **단일 조건부 UPDATE** 로 받는다(전제 판정은 기존 스킵 판정과 **같은 축**인 「마지막 표식 행」이라 규칙을 복제하지 않는다). 스킵 이력이 없는 완주 영상은 전 단계가 멱등 skip 이라 **아무 일도 하지 않는 재기동**이므로 받지 않는다 ②**한번이라도 검수가 완료된 영상은 400** — 확정된 학습데이터는 되돌리지 않으며, 판정은 신고 차단·프레임 폐기 차단과 **같은 단일 원천**(지금 상태가 아니라 **이력**)이고 412 가 아닌 것은 **영구 조건**이라서다 ③보상 롤백은 **선점 직전 상태로** 되돌린다 — 완주 영상을 `FAILED` 로 떨어뜨리면 아무것도 실패하지 않았는데 화면이 「실패」가 된다 ④접수가 **상태 선점**을 동반하는데 전용 풀 큐가 100 이라 일괄 100건이 **2건만 돌고 98건이 선점된 채 대기**했고 그 구간 재배포면 전부 `PROCESSING` 고착 + 애플리케이션 내 복구 수단 0 이었다 — 큐를 **실행 능력 1배(4)** 로 좁혀 대기 폭을 구조적으로 제한하고 넘치는 분량은 건별 사유로 거부한다(**일괄 상한 100 은 불변**). ⚠ **회수 스위퍼는 범위 밖** — 큐 4 + 실행 4 = 최대 8건 고착 위험은 **인지·수용**이며 그 상태를 고정하는 케이스는 두지 않았다(스위퍼를 만들 때 폐기 대상이 되므로). ⚠ 재실행 멱등은 **결과 축**이다 — 오토라벨 추론과 보간은 다시 돈다(B 파일 미등재 안내에 실측 보강). v2-wiki [07 §7.5-1](../v2-wiki/07-batch-pipeline.md) 동기화 동반 | **2**(B — 큐 용량·멱등 안내) | **12**(B 12) | **0** | **3,168**(8클러스터 실측 — B 656 → 668) |
| **46** | **2026-08-13** | **★「처리 중」 고착 회수 — 회차 42 가 인지·수용으로 남긴 최대 8건 고착의 복구 수단을 만든다.** 선점만 남고 실행이 사라지면(대기 중 재배포) 그 영상은 이후 모든 재기동이 409 로 막혀 **애플리케이션 안에 복구 수단이 0** 이었다. **B**(B-29-l `TC-BATCH-359~412`) — ①**되돌릴 목표는 선점 출발 상태가 정한다** — 전체 재기동 출발이면 실패로, 묶음 재수행 출발이면 **완주로**. 완주 영상을 실패로 되돌리면 전체 재기동이 열려 보간이 전량 재계산되므로 이 구분이 기능의 전부다. 그 출발 상태는 **DB 어디에도 없어**(비동기 러너의 인자로만 존재) 선점 시 `LS_BATCH_PROC_LOG` 에 표식 행을 열고 실행이 끝나면 닫는다 — **새 컬럼·테이블·마이그레이션 0건** ②★**알 수 없으면 회수하지 않는다** — 표식이 없거나(표식을 쓰지 않는 다른 진입 경로) 해석되지 않으면 손대지 않고 남긴다. 추측 복구보다 고착 존치가 안전하다 ③★**판정 축은 경과 시간이 아니라 「진행이 멈췄는가」** — 시간만 보면 **살아 있는 파이프라인을 뺏는다**. 「선점 시각」과 「진행 로그 마지막 갱신」 중 **나중** 것으로 판정한다(큐에서 대기 중인 정상 접수는 진행 로그가 직전 실행 때의 옛 시각이라 진행 로그만 보면 즉시 멈춤으로 오판된다) ④★**표식은 그 선점 에피소드와 결속돼야 한다** — 판정이 「마지막 표식이 열려 있나」뿐이면 닫힘 한 줄이 유실됐을 때 그 영상의 **이후 아무 고착에나 옛 출발 축이 적용**된다. 표식이 열린 뒤 배치가 **종결까지 간 흔적**이 있으면 지난 에피소드의 잔재로 보고 회수하지 않는다 ⑤되돌리기와 표식 닫기를 **한 트랜잭션**으로 묶어 「상태만 되돌아간 반쪽」을 없앴다(조건부 UPDATE 가 0행이면 기록도 없어 원자성 유지) ⑥**판정 불가 후보가 회수 큐 앞머리를 영구 점유**하면 뒤의 진짜 대상이 영영 차례를 못 받아 스위퍼가 조용히 무력화된다 — 창을 회전시킨다 ⑦임계는 설정값 **기본 24시간 · 하한 60분**이고 그 미만·0·음수·파싱 실패면 **기동이 실패**한다(파괴적 기능은 fail-open 되면 안 된다). 값을 크게 잡은 근거는 **살아 있는 선점을 뺏지 않는 것이 회수 지연보다 중요**하다는 것이며, 대가로 실제 고착도 최대 하루 방치된다. ⚠ **남는 위험(인지·수용)** — 진입 가드가 「지금 처리 중인가」만 보고 「내 것인가」를 몰라, 회수 직후 재선점 시 옛 작업이 병행 실행될 수 있다. 이 코드가 만든 결함이 아니라 기존 한계이며 닫으려면 선점 소유권 토큰이라는 별도 설계가 필요하다(임계 24시간이라 도달 가능성은 매우 낮다). v2-wiki [07 §7.5-2](../v2-wiki/07-batch-pipeline.md) 동기화 동반 | **15**(B — 회차 40 임계 기본값 1건 + 회차 41 근거·기대결과 14건) | **54**(B 54) | **0** | **3,300**(8클러스터 전량 실측 — B 712 → 766) |
| **47** | **2026-08-13** | **영상 표시명 폴백을 화면 전체에서 하나로 통일** — V185(관제 회신 2026-08-12, `VMS_CCTV_ID` NULL 허용)로 도입한 판정 단일 원천 `CctvDisplayNamePolicy`(CCTV명 → `VMS_CCTV_ID` → `영상 #{rawSn}`)가 영상 목록·상세에만 배선돼 있어 나머지 화면은 자체 폴백을 들고 있었다. **작업목록(REVIEWER)·배정목록(WORKER)** 은 둘 다 없는 영상에서 `null` 을 그대로 내려 **행 전체가 빈칸**이었고, **검수목록**은 `video #N`, **증강 검수·증강 결과**는 `(이름 없음)`(여러 영상이 모두 같은 문구가 되어 식별 불가)으로 표기가 셋으로 갈려 있었다. 각 서비스의 자체 삼항식·상수를 제거하고 정책기로 수렴한다. **D**(D-3e `TC-ASSIGN-036~038` · `TC-REVIEW-078` 신설) · **E**(`TC-AUG-038` 정정 — 구 `(이름 없음)` 폐기). ⚠ **응답 필드는 하나도 줄이지 않았다** — `AssignmentResponse.Item.videoTitle` 은 우리 FE 가 읽지 않지만 실제 화면을 외부 팀이 개발하므로 필드를 남기고 값만 통일했다 | **1**(E 1) | **4**(D 4) | **0** | **3,304**(D 400 → 404) |
| **48** | **2026-08-13** | **관제 회신(`docs/저작도구-관제회신-답변-20260812.md` §4 Q2·§7) 문서 정합 — BE 코드 무변경.** 관제 회신에 맞춰 이미 반영된 구현 3건이 카탈로그에 없던 것을 신설했다. ①**통지 페이로드 `output_ver_no` 신설**(@design INT-007) — 완료·수정 통지에 선택 필드로 산출 버전번호를 실어 관제가 재픽업 여부를 판단하게 한다. required 8필드(null 이어도 키 유지)와 정반대로 값이 없으면 키 자체가 빠진다. **D**(D-6b `TC-NOTIFY-063~066` 신설). ②**이벤트 어노테이션 단독 수정도 재생성 대상**(구 `exportRegenerated=false` 결함 해소) — 「검수 완료 후 수정은 예외 없이 재승인 시 새 버전」 원칙(D-1b)에서 유일하게 빠져 있던 경로를 닫아 재export 경로가 7→8 로 늘었다. **D**(D-1d `TC-REVIEW-079` 신설 + TC-NOTIFY-014 정정). ③**`FRME_CNT` 산정 = 벌 합계가 아니라 벌별 쓰기 건수의 최댓값**(V185, DDL 없이 V173 이 남긴 COMMENT 만 재지정) — 관제가 이 값을 "추출·라벨링 프레임 수"로 그대로 적재하므로 원본+비식별 2벌 합산은 실제의 2배로 부푼다. **E**(TC-EXPORT-076~078 신설). 정본 규격서 §4-2 의 예외 서술("이벤트 어노테이션처럼 폴더가 안 생기는 예외 경로")도 함께 정정 — ②로 그 예외 자체가 소멸했으므로 구체 사례 대신 처리 규약만 남겼다(문서 전용, 카운트 영향 없음) | **1**(D TC-NOTIFY-014) | **8**(D 5 · E 3) | **0** | **3,312**(D 404 → 409 · E 302 → 305) |
| **49** | **2026-08-18** | **★장부 동기화 — 이 표(README)만 갱신, 케이스 신규/정정/폐기 0건.** 각 클러스터 파일 자체 머리말(그 파일의 `## 변경 이력` 이 이미 반영해 온 값)과 이 파일 §검증 순서 표의 「케이스 수」 칼럼을 `grep -acE '^\| ~*TC-'` 실측으로 전수 대조했더니 5개 파일이 어긋나 있었다 — B 766→**799**(회차 진행분 미반영) · C 339→**342**(동일) · D 409→**423**(동일) · F 180→**210**(F 회차 6·7 둘 다 미반영 — F 파일 자체 머리말도 「201」로 회차 7 표기가 누락돼 있어 이번에 함께 「210」으로 정정했다) · H 806→**909**(H 회차 49~57 누적분 — H 파일 회차 56·57 각주가 이미 "README 장부 미갱신"을 명시하고 있었다). A·E·G 는 실측=장부 일치라 변경 없음. **이 회차가 새로 만든 케이스는 0건** — 각 클러스터 파일에 이미 기록돼 있던 값을 이 표로 옮겨 적은 것뿐이다(상세 내용은 각 파일 자체 `## 변경 이력` 참조) | **0** | **0** | **0** | **3,495**(회차 49 — 위 5개 파일 장부 동기화) |

> 회차별 상세(무엇을 왜 고쳤는지)는 **각 클러스터 파일 상단의 `## 변경 이력` 섹션**에 있습니다.
> - 07-25 최초 카탈로그의 `~1,210` 은 추정치였고 **표 행 실측은 1,376** 이었습니다(1,376 + 신규 531 = 1,907).
> - 정정 942건의 대부분은 **근거 `file:line` 드리프트 정정**이며, 기대결과·전제가 실제로 뒤바뀐 건수는 각 담당이 분류한 범위에서 B 32 · C 33 · H 21 건입니다(나머지 클러스터는 별도 분류하지 않음).
> - **회차 2 는 별도 총계 갱신 없이 클러스터별로만 반영**됐습니다(F 169→177, H 293→309). 회차 3 착수 시점 실측 총계가 1,931 이었고, 이 행은 그 차이를 사후 기록한 것입니다.
> - **회차 3 신규 46건**: B-18 `TC-VIDEO-001~018`(18) + H-3 하위 3개 절 `TC-FE-276~296`(21) + H-18 `TC-FE-297~303`(7). 정정 7건은 TC-FE-079·087·156·268 · TC-REVIEW-016 + C-6·D-4 절 머리말입니다. **폐기 0건** — 결정 2 가 페이지를 없앴지만 버전·diff·롤백 **기능** 은 인라인 패널로 그대로 남아 폐기 대상이 아닙니다.
> - **회차 4(근거 전수 재확인)**: 착수 시점 총계 표기 `1,977` 은 실측이 아니었습니다 — 표 행 실측은 **1,954**(A 232·C 273·E 232·F 173 이 헤더 표기와 어긋나 있었음)였고, 신규 4건(TC-AUG-043 · TC-EXC-018~020)을 더해 **1,958** 로 확정했습니다. 신규분은 모두 **코드에 구현돼 있는데 검증 케이스가 아예 없던 갭**입니다 — 증강 '결정 전 결과물 실재 필수' 게이트(`AugmentReviewService.requireGeneratedResult`)와 405 핸들러(`GlobalExceptionHandler:261-272`, 신설 이전에는 `handleUnknown` 으로 흘러 500 이었음). 정정 ≈699건의 **대부분은 라인 드리프트**이고, **기대결과·전제가 실제로 뒤바뀐 건은 24건**입니다(A 3 · B 11 · C 1 · D 3 · E 5 · F 3 · G 2 · H 8 — 클러스터별 상세는 각 파일 `## 변경 이력`). **폐기 0건**.
> - ⚠ **다음 회차 규칙 — "소스 파일이 안 바뀌었으니 근거도 맞다"를 검증 근거로 쓰지 말 것.** 회차 4 에서 이 추론이 **두 번 틀렸습니다**: ①F-7 절 머리말의 "07-25 이후 무변경" 주장이 사실이 아니어서(`476bc91a`·`dcdbb827`) 그 구간이 직전 회차에 통째로 건너뛰어져 있었고 ②H 담당이 같은 근거로 6개 절을 생략했으나 그 절들이 인용하는 소스 59종 중 **14종이 실제로 변경**돼 있어(보완 재확인에서 27건 추가 정정, 그중 H-16 은 17건이 **엉뚱한 함수**를 가리키고 있었음) 재작업이 필요했습니다. **인용 라인은 매 회차 직접 열어 확인합니다.**
> - **회차 4-1 카운트 정정**: C 클러스터 머리말이 실제 표 행 수보다 **+2 크게** 적혀 있었습니다(행 273 ↔ 표기 275). 회차 4 이전부터 있던 오차이며, 신규 2건이 들어오면서 드러났습니다. 회차 4가 "총계는 실측"을 원칙으로 세웠으므로 **머리말도 표 행 수 실측(275)으로 맞췄습니다.**
> - ⚠ **"최신 회차에 쓴 케이스라 정확할 것"도 성립하지 않습니다.** 회차 3 이 `TC-FE-276~278` 을 작성한 **같은 날** 후속 커밋 `5c10c0cd` 이 `TimeseriesSidePanel.tsx` 를 재작성해 그 3건이 당일 드리프트했습니다.

## 검증 순서 (권장 — 위에서부터 차례로)

> **A→H 순서가 곧 검증 순서**입니다. 의존성·데이터 흐름(토대 → 데이터 입구 → 작업 → 종결 → 파생 → 외부채널 → 추론 → 통합) 기준으로 정렬돼 있어, 앞 단계에서 토대 결함을 먼저 걸러냅니다.

| 순서 | 파일 | 도메인 | 케이스 수 | 왜 이 순서 | ID 프리픽스 |
|:--:|------|--------|:---:|------|------|
| **1** | [A-auth-common.md](A-auth-common.md) | 인증/권한/공통 인프라 **+ AI 정밀도 기본값 읽기 전용 조회 + 연동 서버 주소·관리자 단기 유효창** | **323** | 모든 도메인이 의존하는 **토대**(인증·응답·예외·듀얼DS·기동 가드) | TC-AUTH / TC-AUTHZ / TC-CORS / TC-STREAM / TC-HMAC / TC-CLAIM / TC-TRACE / TC-RESP / TC-EXC / TC-DS / TC-CACHE / TC-SYSCFG / TC-PROF / TC-ACT / TC-RES / TC-ROLE / TC-COMMON / **TC-LOG · TC-SORT · TC-BLANK · TC-CFG · TC-HEALTH · TC-AIDEF · TC-ENDP · TC-ADMS** |
| **2** | [B-batch-deidentify.md](B-batch-deidentify.md) | 배치 파이프라인/비식별화 **+ 영상 목록 조회 + 내부 업로드 기술메타 back-fill + 메타 조회 응답 정합 + VLM 위탁 게이트 + 업로드 세션 존재 오라클 차단 + 개발용 배치 대기열 페이징** | **799** | **데이터 입구**(적재→비식별→파이프라인). 동시성·PII 고위험 | TC-BATCH / TC-DEID / TC-VLM / TC-STREAM / TC-VIDEO / **TC-ULD** |
| **3** | [C-marking-labeling.md](C-marking-labeling.md) | 마킹/라벨링 | **342** | 적재된 데이터에 대한 **핵심 작업** | TC-MARK / TC-LABEL / TC-SAM2 / TC-KEYPOINT / TC-TRACK / TC-PRESET |
| **4** | [D-review-version-notify.md](D-review-version-notify.md) | 검수/버전관리/관제통지 **+ D6 통지 트리거 반전(재승인/보류) + 통계 지표 축 통일 + 문의 스레드 작성자 역할 + 영상 표시명 폴백 공용 + 통지 `output_ver_no` + 이벤트 어노테이션 재생성** | **423** | 작업을 닫는 **워크플로우 종결**(승인→스냅샷→export→통지) | TC-REVIEW / TC-ASSIGN / **TC-STAT** / TC-VERSION / TC-DIFF / TC-NOTIFY / TC-MARTVIEW |
| **5** | [E-augment-resolution-export-meta.md](E-augment-resolution-export-meta.md) | 증강/해상도/Export/메타 **+ FRME_CNT 산정 정정(V185)** | **305** | 검수 완료 후 나오는 **파생 산출물** + 외부 위탁 | TC-AUG / TC-RESL / TC-EXPORT / TC-META |
| **6** | [F-portal.md](F-portal.md) | 포털(외부 채널) **+ F-12d 데이터마트 다운로드·만료일 화면 배선** | **210** | 내부 파이프라인과 **분리된 외부 채널** | TC-PORTAL / TC-PORTALUP / TC-TUS |
| **7** | [G-ai-server.md](G-ai-server.md) | ai-server + **외부 벤더 목업 계약** | **184** | BE와 계약으로만 연결된 **독립 추론 서버**(언제든 병행 가능) + 로컬·dev 검증이 전부 경유하는 목업 계약 | TC-AIYOLO / TC-AISAM2 / TC-AIVLM / TC-AICONTRACT / TC-AIINFRA / **TC-AIMOCK** |
| **8** | [H-frontend-e2e.md](H-frontend-e2e.md) | FE 화면/컴포넌트/E2E **+ 공통 컴포넌트 조합형 전환 + 검수 헤더/프레임이동바 + KRDS 팔레트 완결 + 검수 우측 패널 3탭 + 마킹 칩 삭제·기간 필터·검수 썸네일 스트립·속성 사이드 시트·캔버스 회전/격자/영역확대·페이지네이션 수렴 + 마킹 빈 상태·글리프 가드·폼 반경 통일 + 이슈 스레드 작성자 역할 + 증강 요약 5칸·마킹 완료 버튼 항상 활성·팬 도구 폐지·목 자식 전달 가드 + 연동 서버 주소 카드(잠금·관리자 유효창) + 온디맨드 「AI 자동 추적」 배선** | **909** | 전 계층을 통합하는 **최상위**. BE 안정 후 E2E가 의미 있음 → 마지막 | TC-FE / TC-E2E / TC-A11Y |
| — | [UNCERTAINTIES.md](UNCERTAINTIES.md) | 확정 정책 + 확인 필요 항목 | — | 검증 내내 PASS/FAIL 판정 기준 | — |
| | **합계** | | **3,495**(회차 49 — 각 클러스터 파일 자체 머리말과 대조한 **장부 동기화**, 신규 케이스 신설 아님. B 766→799(+33, B 파일 자체 회차 진행분이 이 장부에 미반영 상태였다) · C 339→342(+3, 동일) · D 409→423(+14, 동일) · F 180→210(+30 — **이 표는 F 파일의 「회차 6」(180→201, F-12) 조차 반영되지 않은 채 더 낡아 있었다**. F 파일 자체 머리말은 이번 F-12d 작업 중 함께 「210」으로 정정했다 — 그 파일 상단의 리드 숫자가 「201」로 남아 있던 것도 회차 7 표기 누락이었다) · H 806→909(+103, H 회차 49~57 누적분이 이 장부에 미반영 상태였다 — H 파일 자체 회차 56·57 각주가 "README 장부 미갱신"을 이미 명시하고 있었다). A·E·G 는 변경 없음(실측=장부 일치)) 구 서술 — **3,312**(회차 48 — D 404 → 409 · E 302 → 305). 구 서술 — **3,304**(회차 47 — D 400 → 404, 다른 클러스터 무변경). 구 서술 — **3,300**(8클러스터 전량 실측, 회차 46 — B 766 · H 806). 구 서술 — **3,246**(8클러스터 전량 실측, 회차 45 — B 712 · H 806). 구 서술 — **3,235**(8클러스터 전량 실측, 회차 44 — B 707 · H 800). 구 서술 — **3,213**(8클러스터 전량 실측, 회차 42~43 — B 668 → 691 · H 772 → 794). 구 서술 — **3,168**(8클러스터 전량 실측, 회차 42 — B 656 → 668. 이번 회차 신설분은 **B +12** 이며 stale 보정분은 없다. 구 서술 — **3,156**(8클러스터 전량 실측, 회차 41 — B 642 → 656 · H 763 → 772. 이번 회차 신설분은 **B +14 · H +9** 이며 stale 보정분은 없다. 구 서술 — **3,133**(8클러스터 전량 실측, 회차 40 — B 542 → 642 · C 317 → 339 · D 298 → 400 · H 673 → 763 으로 클러스터 행 **4건의 stale 을 함께 정정**했다. 이 중 이번 회차 신설분은 **B +67 · H +28** 이고 나머지(B +33 · C +22 · D +102 · H +62)는 앞선 회차들이 자기 파일만 갱신하고 이 표를 갱신하지 않아 누적된 **미반영분 보정**이다 — 케이스를 소급 신설한 것이 아니다. 구 서술 — **2,819**(8클러스터 전량 실측 — 회차 38 에서 **머지 트리 전량 재측정**: B 494 → 542 · C 311 → 317 · H 646 → 673 으로 클러스터 행 3건의 stale 을 함께 정정했다. B·C 는 다른 회차의 신설분이 커밋되며 반영된 것이고 H 는 이번 머지분이다. 구 서술 — 회차 34 에서 **H +17**(회차 34 에서 **H +17**(H-43 `TC-FE-553~555` · H-44 `TC-FE-556~561`+`TC-A11Y-017` · H-45 `TC-FE-562` · H-46 `TC-FE-563~568`). 구 서술 — 회차 33 에서 **D +9**(문의 스레드 작성자 역할 D-1c `TC-REVIEW-069~077`) · **H +5**(H-42 `TC-FE-548~552`). ⚠ 같은 회차에 **클러스터 표 E 행의 stale(294 → 실측 302)** 을 함께 정정했다 — 회차 32 의 `E +8` 이 합계 행에만 반영되고 클러스터 행에는 안 들어가 있었다. 구 서술 — 회차 32 에서 **E +8**: 증강 결과 요청일시 계약 E-2B `TC-AUG-122~129`. 회차 31 행의 2,593 은 **H +10 만 반영한 값**이었고, 같은 시점 워크트리에 E 클러스터 +8 이 함께 있어 두 회차를 합산해 재측정했습니다. 구 서술 — 회차 31 에서 **H +10**: 마킹 마크 0건 빈 상태 H-39 `TC-FE-538~542` · 화면 글리프 재유입 가드 누락 등재 H-40 `543~545` · 폼 컨트롤 모서리 반경 통일 H-41 `546~547`. 직전 실측 **2,583** 은 회차 30 까지 반영된 값으로 실측과 일치했습니다. 구 서술 — 회차 30 에서 **H +82**: 마킹 칩 개별 삭제 H-31 `TC-FE-456~459` · 영상 목록 기간 필터 사양 컴포넌트 배선 H-32 `460~467` · 검수 프레임 썸네일 스트립 캔버스 위 이동+접기/펼치기 H-33 `468~476` · 라벨 속성 정의 사이드 시트 H-34 `477~485` · 캔버스 회전/격자 H-35 `486~501` · 영역 확대 H-36 `502~516` · 페이지네이션 계약 전환+목록 3화면 수렴 H-37 `517~533` · 사용자 관리 저장 잠금 H-38 `534~537`. 직전 실측 **2,501** 은 회차 29(H-30 `TC-FE-446~455` +10)까지 반영된 값으로 **실측과 일치했습니다** — 다만 그 +10 이 아래 회차 표에 반영되지 않아 H 파일 상단의 누적 표기만 481 로 정체해 있었고, 회차 30 에서 함께 정정했습니다. 구 서술 — 회차 28 에서 **A +12 · H +18 = +30**: AI 정밀도 기본값 전용 조회 A-8 `TC-AIDEF-001~012` · H-29 `TC-FE-442~445` / 검수 상세 우측 패널 3탭 H-28 `TC-FE-428~441`. 여기에 **D 소급 반영 +11**(D 회차 17·18 통계 지표 축 통일 `TC-STAT-006~016` — D 파일엔 등재됐으나 이 장부가 278 로 정체돼 있었습니다. 소급 신설이 아니라 실측 재정렬)이 더해집니다). 직전 실측 2,450 의 내역은 다음과 같습니다 — 회차 27 에서 **B +7 · D +14 · H +50 = +71**: D6 통지 트리거 반전(재승인·보류 flush·needsRecheck 노출) · 개발용 배치 대기열 페이징 · 검수 헤더 통합/상단 프레임 이동 바 · 공통 컴포넌트 6종 조합형 전환 · KRDS 팔레트 완결). 직전 실측 2,379 의 내역은 다음과 같습니다 — 회차 26 에서 **B +6 · C +12 · D +6 · E +3 · F +3 · H +15 = +45**: 신고 게이트 확대 · 속성값 인가 · 업로드 세션 존재 오라클 차단 · 개인정보 메타 출처 · 캔버스 상단 옵션바). 직전 실측 2,334 의 내역은 다음과 같습니다 — 회차 25 에서 **H +5**: 저장 진입점 일원화 H-24 `TC-FE-359~363`, 393→398). 2026-08-06 VLM verify 전환 작업 — **E +19**(회차 18: `video.vd_description` 배선 — 조달 규칙 + 해시 편입 + 재생성 트리거, 272→282 · 회차 19: 수동 전문 조달 편입 + 무변경 재생성 억제, 282→291) · **H +16**(회차 17: 라벨링 시계열 메타 패널 전문 1개 편집 + 일치도 읽기전용 + 레거시 병기, 374→382 · 회차 20: `metaKeys` 판정 단일 원천 전용 단위 테스트, 382→390). 여기에 **머지 합류분 H +7**(회차 21: 라벨 색상 마스터 연결 — H-20 `TC-FE-328~334`, 362→369)이 더해졌습니다. 직전 실측 2,271 의 내역은 다음과 같습니다 — **B +53**(회차 21·22·23: 인입 검증이벤트유형 + 콜백 규격 전환 + 동시성 수정 + **메타 조회 응답 정합**, 420→473) · **H +5**(회차 14: dev 업로드 검증이벤트유형 select, 369→374). ⚠ 직전 장부 2,213 은 **B 행이 420 으로 정체돼** 있던 값이라 이번에 실측으로 재정렬했습니다(케이스를 소급 신설한 것이 아닙니다). | |

> **카운트 기준(회차 5 통일)** = 각 파일의 **표 데이터 행 수**이며 **폐기 행을 포함**합니다(행을 지우지 않으므로). 변경 이력 표·현황 요약 표는 제외합니다.
> 폐기 표기가 ID 취소선·케이스명 취소선·기대결과 `[폐기]` 로 제각각이라 "폐기 제외" 는 기계적으로 재현되지 않기 때문입니다(커밋 `b158f7ec` 에서 A·E·F 에 먼저 적용한 기준을 회차 5 에서 B·C·H 로 확장).
> 재현 방법(**회차 7 에서 명령으로 고정**): 각 파일에서 `grep -cE '^\| ~*TC-'` 를 실행합니다 — 표의 ID 칸이 `TC-` 로 시작하는 행을 세되 **ID 자체가 취소선(`~~TC-…~~`)인 폐기 행도 포함**합니다. 변경 이력 표(ID 칸이 회차 번호)·현황 요약 표는 이 패턴에 걸리지 않아 자동 제외됩니다.
> ⚠ **`grep -c '^| TC-'`(물결표 없는 형태)를 쓰지 마세요** — ID 취소선 폐기 행 **29건**(A 3 · B 2 · C 4 · E 16 · F 4)을 통째로 놓쳐 합계가 **2,070** 으로 나옵니다. 위 "폐기 행 포함" 규칙과 어긋나며, 지금까지 일부 회차가 이 명령으로 세어 머리말이 파일마다 다르게 어긋나 있었습니다(회차 7 에서 전부 정정).
>
> **총계 산식(2026-08-05 재계산 — 커밋별 실측)**
>
> | 시점(커밋) | 실측 총계 | 직전 대비 |
> |---|--:|---|
> | 회차 4-1 (`21a3b25b`) | **1,983** | — (표에 기재된 `1,960` 은 실측이 아님 ↓) |
> | 회차 5 착수 (`5cf4f778`) | **2,000** | **+17** = A +7(`582dab96` 사용자 자동등록·MNG_* 제거) · B +5(`bc21bf63` 4 + `5cf4f778` 1) · E +5(`5cf4f778`) |
> | 헤더 정규화 (`b158f7ec`) | **2,000** | **±0** — 표 행을 1건도 바꾸지 않았고 **머리말 숫자만** 실측 정정(A 242→243 · E 232→253 · F 169→177) |
> | 회차 6-2 (`c6d776b3`) | **2,030** | **+30** = 회차 6 신규 24 + 회차 6-1 신규 6 (B +19 · C +4 · H +7) — ⚠ 당시 명령이 `^| TC-` 라 ID 취소선 폐기 행을 놓친 값입니다 |
> | **현재 — 머지 후(회차 7)** | **2,099** | **+69** = ①`main` 쪽 합류분(3차 전수검증 신규 · 표시명 노출 · 라벨 저장 게이트 회귀 가드 등) ②카운트 명령 정정으로 드러난 ID 취소선 폐기 행 **29건**. **케이스를 새로 만들거나 지운 것은 0건**이며 ID 충돌분만 재번호했습니다 |
> | **현재 — 회차 8** | **2,100** | **+1** = H `TC-FE-315`(신고 후 재진입 라벨 캐시 잔존 차단 가드) 1건. 나머지 회차 8 변경은 전부 **기존 행의 근거·기대결과 정정**이라 행 수에 영향이 없습니다 |
> | **현재 — 회차 9(이벤트유형 표시명 그룹핑 + 개인정보 유무 화면 노출 폐지)** | **2,122** | **+22** = B +4(`TC-VIDEO-006` 폐기 표기는 행 유지 + `006a`·`019~021` 신설 4행) · D +14(D-3a `TC-ASSIGN-027~035` 9건 + D-3b `TC-STAT-001~005` 5건 신설) · H +4(`TC-FE-316~319` 개인정보 컬럼/항목 미노출 회귀 가드) |
> | **현재 — 회차 10(작성자 표시명 사번→이름 계약 변경)** | **2,134** | **+12** = C +5(`TC-LABEL-158~162`, 라벨 이력 `actorName` 신설) · D +7(`TC-VERSION-018~022` 5건 + `TC-DIFF-030~031` 2건, 버전목록 `authorName` 의미 변경·롤백 `registeredUserName` 신설) |
> | 회차 14 (머지 합류 후 8클러스터 재측정) | **2,189** | 위 변경 이력 표 회차 13·14 참조 |
> | **현재 — 회차 15(내부 업로드 기술메타 ffprobe 자동 채움)** | **2,213** | **+24** = B +24(B-19 신설 `TC-ULD-001~024`, 신규 프리픽스). 정정·폐기 0건 |
> | 회차 16-1 (VLM verify 전환 · 검증이벤트유형) | **2,258** | 위 변경 이력 표 회차 15 및 B 회차 20~22 참조 — 직전 장부가 B 행 정체로 과소 기재돼 있어 실측 재정렬 |
> | **현재 — 회차 16(메타 조회 응답 정합 · `vlm.accuracy` 분리)** | **2,271** | **+13** = B +13(B-21 신설 `TC-VLM-066~078`). 정정·폐기 0건 |
> | **현재 — 회차 18(`vd_description` 배선)** | **2,289** | **+18** = 회차 17 H +8(FE 시계열 메타 패널) + 회차 18 E +10(`TC-EXPORT-057~066`). 폐기 0건 |
> | 회차 19(수동 전문 조달 편입 · 무변경 재생성 억제) | **2,298** | **+9** = E +9(`TC-EXPORT-067~075`) |
> | 회차 20(`metaKeys` 전용 단위 테스트) | **2,306** | **+8** = H +8(H-23 `TC-FE-348~355`). 정정·폐기 0건 |
> | 회차 21(라벨 색상 마스터 연결 · 머지 합류) | **2,313** | **+7** = H +7(H-20 `TC-FE-328~334`). `main` 합류분이며 케이스 신설·폐기는 그 회차의 7건 그대로입니다 |
> | 회차 22(VLM 위탁 게이트 반전 · 목서버 framerate 상한 폐기) | **2,321** | **+8** = B +6(B-22 `TC-VLM-079~084`) · G +2(`TC-AIMOCK-54~55`). 정정·폐기 0건 |
> | 회차 23(목서버 verify event_type 완화) | **2,324** | **+3** = G +3(`TC-AIMOCK-56~58`). 목서버 전용 완화이며 BE 변경 0 |
> | 회차 24(검증이벤트유형 6+직접입력 · 판정축 형식 전환) | **2,329** | **+5** = B +2(`TC-ULD-049~050`) · H +3(`TC-FE-356~358`). 정정 3건(B 2 · H 1) |
> | **현재 — 회차 25(라벨링 저장 진입점 일원화 · 도구바 잘림 TC-FE-306 해소)** | **2,334** | **+5** = H +5(H-24 `TC-FE-359~363`). 정정 1건(TC-FE-306 미해결 → 해소), 폐기 0건(테스트 코드 1건 폐기는 대응 TC 행 없음) |
> | **현재 — 회차 26(신고 게이트 확대 · 속성값 인가 · 업로드 세션 존재 오라클 차단 · 개인정보 메타 출처 · 캔버스 상단 옵션바)** | **2,379** | **+45** = B +6(B-23 `TC-ULD-051~056`) · C +12(C-7 `TC-LABEL-163~172` + `TC-PRESET-19~20`) · D +6(D-1a `TC-REVIEW-053~058`) · E +3(`TC-META-048~050`) · F +3(`TC-TUS-033~035`) · H +15(H-25 `TC-FE-364~377`·`TC-A11Y-016`). 정정 **10건**(E 1 · F 3 · H 6 — B·C·D 는 신설만), 폐기 표기 행 신설 0건. ⚠ 정정 10건 중 **기대값이 실제로 뒤집힌 것은 3건**(F `TC-TUS-012` 403→404 · H `TC-FE-363` "마지막 버튼 = 저장" 폐기 · H `TC-FE-361` "도구 버튼은 활성" 단언 이관)이고 나머지는 근거 파일명·검증 지점 이동입니다 |
> | **현재 — 회차 27(D6 통지 트리거 반전 · 검수 헤더/프레임이동바 FE 배선 · 공통 컴포넌트 6종 조합형 전환 · KRDS 팔레트 완결)** | **2,450** | **+71** = B +7(B-24 `TC-BATCH-215~221`, `/v1/dev/batch/pending` 페이징) · D +14(D-1b `TC-REVIEW-059~068` + D-6a `TC-NOTIFY-059~062`) · H +50(H-8a `TC-FE-378~389` 12 · H-26 `TC-FE-390~423` 34 · H-27 `TC-FE-424~427` 4). 정정 **1건**(H `TC-FE-184` — DataTable 조합형 전환 반영), 폐기 표기 행 신설 0건. C·E·F·G·A 는 이번 회차 변경 대상 밖입니다 |
> | **현재 — 회차 49(장부 동기화, 이 표 이후 회차 28~48 은 개별 각주로 갱신되지 않고 각 클러스터 파일 자체 머리말에만 누적돼 있었다)** | **3,495** | **+183**(직전 장부 표기 `3,312` 대비, 누적 미반영분) = B +33 · C +3 · D +14 · F +30(180→210, F 회차 6·7 둘 다 미반영) · H +103 · A/E/G 는 0. **이 회차가 새로 만든 케이스는 0건** — 실측이 각 파일 자체 머리말(그 파일 `## 변경 이력` 이 이미 누적해 온 값)과 100% 일치함을 확인한 뒤 이 표를 그 값에 맞춘 것뿐이다. 위 회차 28~48 개별 행들은 **그 시점 실측이 아니라 그 시점까지의 서술**로 남겨 두며(각주 규약 — 소급 수정 안 함), 이번 정정으로 무효화되지 않는다 |
>
> ⚠ **구 각주의 산식 "회차 4-1 기재 1,960 + `b158f7ec` 헤더 정규화 30 + B 회차5 신규 4 + 기타 6 = 2,000" 은 폐기합니다** — 커밋별로 다시 세어 보니 **항 자체가 성립하지 않습니다.** `b158f7ec` 의 기여는 0(헤더 전용 커밋)이고, 실제 증가분은 위 표의 +17 뿐입니다. 미추적 잔차를 "기타 N" 으로 뭉뚱그리지 않습니다.
> ⚠ **`1,960` ↔ 동시점 실측 `1,983` 의 차이 23 은 소급 분해되지 않습니다.** 1,960 은 카운트 기준이 통일되기 전(일부 클러스터가 **폐기 행을 제외**하고 세던 시기)의 **머리말 장부값 사슬**에서 온 값이며, 그 시점 머리말 합계(1,956)와도 4 만큼 어긋나 있었습니다. 따라서 **이 시점(`c6d776b3`) 실측 2,030 을 기준선으로 재설정**하고, 이후 회차는 "**직전 커밋 실측 + 이번 회차 증감**"으로만 산출합니다(장부값을 이어 붙이지 않습니다).
>
> ⚠ 회차 6 에서 C 카탈로그 신규 4건이 기존 `TC-LABEL-137~140`(C-ISSUE-41 좌표 clamp)과 **ID 충돌**해 `TC-LABEL-150~153` 으로 재번호했습니다(행 수 불변 → 카운트 영향 없음). 같은 성격으로 2026-08-05 에 F 카탈로그의 **`TC-PORTAL-060·061·062` 중복 3건**(F-3 저장 검증 ↔ F-4 포털 SAM2)을 F-3 쪽만 **`TC-PORTAL-095~097`** 로 재번호했습니다 — 1·2차 검증 결과 문서가 인용 중인 F-4 쪽 번호를 보존했고, 행 수는 불변입니다.
>
> ⚠ **회차 7(머지 합류) ID 충돌 재번호 — 행 수 불변, 카운트 영향 0**
>
> | 파일 | 구 ID | 새 ID | 충돌 상대(먼저 점유한 쪽) |
> |---|---|---|---|
> | B | `TC-DEID-094`·`095`(신고자 표시명, `main`) | **`TC-DEID-113`·`114`** | 브랜치의 신고 단계 케이스 `TC-DEID-094~112` |
> | C | `TC-LABEL-150~153`(신고 단계, 브랜치) | **`TC-LABEL-154~157`** | `main` 의 `TC-LABEL-150`·`150a`·`150b`(C-ISSUE-61/62) |
> | H | `TC-FE-304~307`(마킹 신고 버튼, 브랜치) | **`TC-FE-311~314`** | `main` 의 `TC-FE-304~306`(우측 속성 패널 레이아웃) |
>
> 회차 번호도 겹쳐 재부여했습니다 — A 3→5 · B 6·7·7-1→12·13·13-1 · C 4·5·6→8·9·10 · E 4→7 · F 3→4 · H(날짜 라벨)→7·8, README 5·5-1·5-2→6·6-1·6-2. **행은 하나도 지우지 않았습니다.**

> 순서는 권장일 뿐 강제는 아닙니다. 특정 도메인만 급하면 그 클러스터부터 지정해도 됩니다. G(ai-server)는 독립적이라 어느 시점에나 끼워 넣을 수 있습니다.

## 표 컬럼 규약

| 컬럼 | 의미 |
|------|------|
| ID | 케이스 고유 식별자 |
| 케이스명 | 한글 서술형. 끝의 `(신규)` = 그 파일 `## 변경 이력` 의 **직전 회차 추가분** 표기(2026-07-30 1회차 / 2026-08-03 3·4회차) |
| 전제 | 테스트 사전 조건 |
| 입력/조건 | 투입 입력·시나리오 |
| 기대결과 | 검증할 단언 |
| 계층 | unit / integration / component / e2e / a11y / security |
| 우선순위 | H(High)·M(Medium)·L(Low) 또는 P0(Critical)~P2 |
| 근거(파일) | **`파일명(심볼명)`** — 라인번호를 적지 않고, 심볼은 **가능하면 반드시** 적습니다(아래 규칙) |

### ★ 근거는 `파일명(심볼명)` 으로 적는다 — 라인번호 금지 (2026-08-05 사용자 확정, 구속)

근거 칸에는 **판정 주체**를 `PresetService.java(resolveLabels)` 형태로 적습니다. `:105-143` 같은 **라인번호는 적지 않고**, **심볼은 특정 가능하면 반드시 적습니다.**

- **라인번호를 폐지한 이유 — 유지비가 정보 가치를 압도했습니다.** 회차 4 는 정정 ≈699건 중 **기대결과·전제가 실제로 바뀐 건이 24건뿐**이었고 나머지는 전부 라인 드리프트였습니다. 회차 11 은 **76건 전량이 드리프트**(순수 리팩토링, 동작 무변경)였습니다. 즉 회차 노동의 대부분이 "코드를 여는 시간 몇 초"를 위해 소모됐습니다.
- **stale 라인은 없느니만 못합니다.** 회차 4 에서 H-16 의 **17건이 엉뚱한 함수를 가리키고** 있었습니다. 검증자가 그 라인을 열면 잘못된 판정을 합니다 — 틀린 좌표는 좌표가 없는 것보다 위험합니다.
- **★ 심볼은 생략하지 않습니다.** 심볼은 메서드·클래스·테스트명 등 **그 판정을 실제로 수행하는 지점**을 가리키며, 라인과 달리 **리네임 전까지 안정적이면서 검색으로 즉시 도달**합니다. 판정 지점이 여러 곳이면 ` · ` 로 나열합니다(`LabelService.java(bulkUpsert · isNewLabel)`). **심볼 특정이 불가능한 경우**(설정 `application.yml`·마이그레이션 SQL·정책 상수 파일 등)에만 파일명 단독을 허용합니다.
- **적어도 되는 것**: 심볼명(`VlmUrlPolicyTest.java:cgnatRangeRejectedInBothPolicies`) · 짧은 성격 설명(`(신고 게이트 · 락 409)`). **적지 않는 것**: 라인번호, 라인 기반 정정 이력(`구 126-134 드리프트`), 파일 총 줄 수.
- **정정이 필요한 경우**: 파일 **리네임·삭제·이동**, **판정 주체의 클래스 이동**(회차 4 의 `FrameImageController` → `FrameImageService`), **심볼 리네임**. 라인 이동은 더 이상 정정 사유가 아닙니다.
- ⚠ **기존 행은 일괄 백필하지 않습니다 (2026-08-05 사용자 확정).** 회차 12 시점 심볼 보유율은 **13%(287/2,134)** 이고, 나머지 1,847행은 파일명 단독입니다. 코드를 열어 판정 메서드를 찾는 작업이라 기계 치환이 불가능해 전수 보강 비용이 회차 4 급이기 때문입니다. **앞으로 정정·신설로 손대는 행부터 심볼을 채워** 보유율을 점진적으로 올립니다 — 즉 파일명 단독 행이 남아 있는 것은 **결함이 아니라 미보강 상태**이며, 그 자체를 드리프트로 집계하지 않습니다.

> ⚠ 아래 "다음 회차 규칙"(소스 무변경을 근거로 재확인을 생략하지 말 것)은 **그대로 유효합니다.** 파일이 통째로 사라지거나 판정이 다른 클래스로 이동한 것은 라인 폐지와 무관하게 놓칠 수 있습니다.

### 폐기 케이스 표기

정책 변경으로 무효가 된 케이스는 **행을 삭제하지 않고** `~~취소선~~` + 기대결과 칸에 `**[폐기 {일자}]** 사유 + 대체 케이스`로 남깁니다.
1차 검증 결과 문서(`docs/검증결과/`)가 TC ID를 참조하므로 추적성을 위해 보존합니다. **폐기 케이스는 검증 대상이 아닙니다.**

## 검증 실행 방법

| 방법 | 진입점 | 용도 |
|------|--------|------|
| 슬래시 커맨드 | `/verify-tc {클러스터} [회차]` (예: `/verify-tc B`, `/verify-tc C 2차`) | 이 저장소 안에서 실행 (`.claude/commands/verify-tc.md`) |
| **이식용 프롬프트** | **[VERIFY-PROMPT.md](VERIFY-PROMPT.md)** 내용을 복사해 새 세션에 붙여넣기 | **다른 컴퓨터·다른 환경**에서 동일하게 실행 (자립형 — 커맨드·memory 의존 없음) |

검증 기준: **목업서버 기동 + 배치 실구동한 정상 시나리오 위에서 실동작 판정**. 외부 연동은 전부 `mock-server`(:9400) 경유하며, 본 프로그램이 외부 응답 없이 값을 자체 생성하면(self-fill) 결함입니다. 결과는 `docs/검증결과/{날짜}/{회차}/` 에 회차별로 쌓입니다.

## 검증 시 참고

- **판정 기준은 [UNCERTAINTIES.md](UNCERTAINTIES.md) 의 "확정 정책(★)" 절을 먼저 읽으세요.** 그중 5건은 **결함으로 재보고하면 안 되는 확정 정책**입니다 — ①신고 게이트 판정 범위 = 자기 rawSn 행 하나(조상/자손 전파는 도입 후 철회) ②목록 정렬 strict(400) vs lenient(200 폴백) 비대칭 ③좌표 검증 2축(사용자 저장=400 거부 / AI 검출=clamp) ④**라벨명은 마스터 등록명 그대로**(코드 한글 사전 치환 폐지 — 되살리지 말 것) ⑤**영상 목록 필터 오류 처리 비대칭**(날짜·길이 오류=400 / 미등록 이벤트 코드=0건).
- 다수 케이스는 기존 테스트로 **부분 커버**되어 있습니다 → 신규 작성 전 중복 확인 권장.
  테스트 자산(2026-07-30 실측): BE `*Test.java`/`*IT.java` **516 파일** · FE `*.test.*` **290 파일** · E2E `e2e/specs/*.spec.ts` **11 파일** · ai-server `tests/` **13 파일** · mock-server **9 파일**.
- 최신 기준선(2026-08-04 FULL 회귀 실측): **BE `cleanTest` 600 클래스 / 5,207 tests / 실패 0** · **FE 340 files / 2,067 tests PASS**. 구 기준선(BE 4,196 / FE 338 files·2,038 tests)은 각각 2026-07-30 · 2026-08-03 시점 값이라 대체한다 — **회귀가 UP-TO-DATE 로 스킵되지 않았는지 판정할 때 이 실행 수를 하한으로 쓴다**(과거 거짓 PASS 실사고 재발 방지).
- 우선순위 P0/H(보안·상태전이·동시성)부터 검증 착수 권장.
| **29** | **2026-08-08** | **H-30 신설 — WorkerStatsTable 통계 테이블 검증 케이스** — 6컬럼 렌더링(작업자/라벨/진행/검수/오토라벨/반려율) + 필드 축 정렬 + 값 결손 자리표시 + 빈 상태 안내. **H**(H-30 `TC-FE-446~455`) — **PixelTest 원칙**: 각 행의 수치가 전부 서로 달라야 렌더 필드를 단언으로 구분 가능. 구 결함(진행/라벨 컬럼이 둘 다 reviewed 값을 그렸던 축 불일치)이 같은 값으로는 감지되지 않음을 고정 | **0** | **10**(H 10) | **0** | **2,501**(실측) |
