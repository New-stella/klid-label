# E. 증강 + 해상도 파생 + Export + 메타 입력 — 테스트 케이스

> 333 케이스(표 행 실측 — **폐기 행 포함**, 행을 지우지 않으므로) · 계층: unit / integration / security / component · [← README](README.md) ※ 카운트 = `grep -cE '^\| ~*TC-'`(ID 취소선 폐기 행 포함, 2026-08-06 회차 14 정정 — 수동 전문 조달 + 무변경 재생성 억제 TC-EXPORT-067~075 신설로 282→291 · 회차 15 에서 291→294, 프레임 개인정보 메타 출처 3필드 TC-META-048~050 신설 · 회차 16 에서 294→302, E-2B `TC-AUG-122~129` 신설 · 회차 18 에서 302→305, FRME_CNT 산정 정정(V185) TC-EXPORT-076~078 신설 · 회차 20 에서 305→310, 외부 증강 미연동 시 요청 접수 503 거부 TC-AUG-130~134 신설 · 회차 21 에서 310→319, 해상도 파생 가변 캔버스 전환 TC-RESL-099~107 신설 · 회차 22 에서 319→323, CO-018 DEV_FIX 가드 TC-RESL-108·109·109b·110 신설(⚠ 당시 헤더 카운트가 갱신되지 않아 표기 319 ↔ 실측 323 으로 어긋나 있었고 회차 23 에서 정정했다) · 회차 23 에서 323→324, 종단 가드 TC-RESL-111 신설 · **회차 24 에서 324→333, 증강 위탁 v1.3 정합 TC-AUG-135~143 신설**)

## 변경 이력

| 회차 | 일자 | 정정 | 신규 | 폐기 | 요약 |
|:--:|------|:--:|:--:|:--:|------|
| 1 | 2026-07-30 | 39건(+근거 전 행 재확인) | 92건 | 8건 | Phase 7·8 전체 반영 — 증강 외부 위탁 실배선(`HttpExternalAugmentClient` · job_id 외부 발급 · 100장 청크 · `POST /v1/genai/callback` 신설, 구 `/v1/aug/callback`·`AugmentResultRequest` 폐기) · 만료 스윕/dead-letter 집계축 · 중복 증강 409(V143) · **파생 생성 ↔ 비식별 신고 분리**('F' 통과, 'N'만 차단) · `ParentDeidArtifactGuard` · stale 창 판정축 교체(복사 원자성) · 레터박스 종횡비 보존 좌표 변환 · 파생 비디오 실제 복사 · export co-locate/재생성/통지 순서 · 촬영환경 self-fill 제거 · export JSON 키 `event_annotation`→`event` · **E-5B 신설**(해상도 파생 산출물 비식별 저장소 이관 백필 — 순서 계약·실패 지점별 원본 유실 0·경로 가드·캐시 무효화·유예 삭제 스윕) |
| 2 | 2026-08-02 | 2건(TC-AUG-015 재정의) | 0건 | 3건 | **E-ISSUE-01** — E-1 절의 TC-AUG-012/013/014(중복 증강 요청 차단 409 + `UK_LS_DATA_AUG_ACTVTN` 최종방어)가 검증하던 정책이 2026-07-31 사용자 확정으로 **전량 폐기**됨(`CLAUDE.md` "★중복 증강 요청 차단" 섹션 참고 — 사전 조회 가드·409 분기·안내 문구·인덱스 4종 모두 제거, `V153__add_ls_data_aug_prompt_and_drop_active_unique.sql` 가 `UK_LS_DATA_AUG_ACTVTN` DROP). 3건 모두 `~~취소선~~` + `[폐기 2026-08-02]` 처리, TC-AUG-015 를 "모든 상태에서 재요청 허용"으로 갱신해 대체 케이스로 삼음. TC-AUG-018(V143 마이그레이션 가드)은 각주만 추가(과거 이력 검증용으로 존치, 현재 정책 아님) |
| 3 | 2026-08-03 | 약 145건(근거 `file:line` 재확인) | 1건(TC-AUG-043) | 0건 | **근거 `file:line` 전수 재확인 회차** — 231행 전체를 실제 소스와 대조. E-1~E-9 전 절에서 라인 드리프트 다수 확인·정정(주로 `AugmentRequestService`/`AugmentReviewService`/`GenAiCallbackService`/`AugmentJobSubmitService`/`VideoResolutionService`/`DatasetExportService`/`FramePrivacyMetaService` 등 리팩터가 잦았던 파일). **동작 자체가 바뀐 건**: ①TC-AUG-020/021 — 2026-07-31 축 분리로 재승인 차단 메커니즘이 `LsDataAug.applyReviewStatus`(폐기)에서 `LsDataAugRvw.ensurePending()`으로 이동, 메시지도 "이미 처리된 증강 검수입니다"로 변경 → **TC-AUG-043 신설**("결정 전 결과물 실재 필수 게이트", `LsDataAug.isGenerationSucceeded()`/`requireGeneratedResult`, 기존 카탈로그에 미기재였던 2026-07-31 DEV_FIX HIGH) ②TC-AUG-069/071/072 — 웹훅 산출물 검증·건수대조 로직이 `GenAiCallbackService`에서 `AugmentJobSuccessApplier`(웹훅/회수 공용 단일원천)로 이관 ③TC-AUG-061 — `job_id` 정규식에 점(`.`)전용 세그먼트 차단(`(?!\.+$)`)이 추가됨 ④E-3B(TC-AUG-100~113) — 위탁이 동기 `.block()`에서 Phase C-3 논블로킹(Flux/concatMap)으로 전면 재작성, TC-AUG-110 은 "재부착 금지" 단정을 "재측정 후 조건부 허용"으로 정정 ⑤E-4/E-5 — `VideoResolutionService`(Phase 3 오케스트레이터)와 `ResolutionDerivativeService`(Phase 2 예약, 신설 중간계층)로 분리됨, `ResolutionPersistService`/`ResolutionSnapshotService`/`ResolutionFileMaterializer`/`AsyncResolutionRunner`/`DerivedMetaCopier`는 근거가 전부 정확했음(리팩터 후에도 최신 유지). E-5B 는 재확인 결과 여전히 파일 0건(삭제 확정) — 변경 없음 |
| 4 | 2026-08-04 | 35건(기존 케이스 근거·기대결과 재확인/정정) | 8건(TC-AUG-044~048, TC-META-045~047) | 0건 | **3차 전수 검증(테스트케이스 실동작 검증, `docs/검증결과/2026-08-03/3차/E-result.md`) 반영 회차** — 7개 파트 병렬 실동작 검증 결과를 카탈로그에 동기화. ①**E-1 갭 보강**: E-ISSUE-04(1차)가 지적한 신규 동작 8종 중 요청측 5종을 TC-AUG-044~048(파생 영상 증강 차단·가드 순서·prompt 4종 검증·PROMPT_CN 원문 보관·types-prompt 비파생)로 신설, TC-AUG-001 prompt 필수 5필드 입력열 정정(2회차째 미반영이던 드리프트) ②**E-3/E-3B**: TC-AUG-055(FK CASCADE로 ② 분기 도달불가)·TC-AUG-057(부모 역산 쿼리 특성상 도달불가, mock 단위테스트로만 검증)·TC-AUG-108(실패 기록 주체 라인 보강)·TC-AUG-120(★부모↔파생 `video.*` 기술메타 불일치 실측 — 구 "부모와 동일이 정상" 단정 정정, E-ISSUE-21) 4건 정정 ③**E-4/E-5**: TC-RESL-013(정수 ordinal 바인딩 미해소 갭 각주), TC-RESL-037/048(procLog 상태값 오기 `SUCCESS`→`SUCCEEDED` 정정), TC-RESL-041(★목적 파일↔부모 프레임 1:1 불변식 명문화 + 실측 FAIL 반영, E-ISSUE-61), TC-RESL-066(DELETE SQL 실제 위치·3조건뿐 명시, E-ISSUE-63) 5건 정정 ④**E-6**: TC-EXPORT-003(휴면 트리거 사실 명시)·TC-EXPORT-042(근거가 엉뚱한 토글을 가리키던 드리프트 정정) 2건 ⑤**E-7/E-8/E-9 — ★V163 개인정보 3필드 정책 반전(커밋 `0d290c4e`, 2026-08-03) 카탈로그 동기화**(E-part6/E-part7 독립 발견·상호 정합 확인): `ExportPrivacyPolicy` 도입으로 `ORIGINAL`=3필드 모두 판정 안 함(null), `DEIDENTIFIED`=수동값 우선(미입력 시 기본상수 Y/N/N)로 정반대 확정 — TC-EXPORT-022/023/027/028/029/030(6건)·TC-META-005/006(라인 드리프트 2건)·TC-META-030~044(15건, 프리필 원천·export 반영 방향·근거라인 전면 정정) 반영, 방치 시 정상 동작 3~6건이 다음 회차 거짓 FAIL로 재발견될 뻔함(E-ISSUE-101/121) ⑥**E-9 신설 3건**: TC-META-045(신고구간 단건 412)·046(신고구간 벌크 412+GET 200)·047(영상↔프레임 입도 차이 허용, 단 역방향 조합은 E-ISSUE-122로 별도 기록된 실제 제품결함) — (구 "[3차·E-5 part4 추가 정정 2026-08-03]" 블록인용은 위 ③에 통합, 중복 삭제) 정정 총 35건 + 신규 8건 = 43건. 상세 판정 집계·30건 이슈 목록은 `docs/검증결과/2026-08-03/3차/ISSUES.md` "E클러스터" 절 참조 |

| 5 | 2026-08-04 | 8건(`ResolutionSnapshotService` 근거 라인 드리프트 + TC-RESL-041 실측 FAIL 해소 반영) | 2건(TC-RESL-096/097) | 0건 | **E-ISSUE-61 수정 반영 회차(3차 검증 유일의 실데이터 유실 축)** — 해상도 파생 목적 프레임 파일명이 부모 deid 경로 basename 을 재사용해, 서로 다른 소스 디렉터리의 동명 프레임(`frame_001.jpg`)이 단일 목적 디렉터리(`frames/deid/{newRawSn}/`)에서 **무경고로 덮어써지며 프레임이 유실**되던 결함을 닫았다. ①파일명을 **파생 자신의 FRM_NO 기반**(`frame-{frameNo}.{ext}`, 증강 경로 `AugmentExtractSnapshot` 의 `frame-{i}.{ext}` 와 동일 규약)으로 변경 — `UK_LS_DATA_SRC_RAW_FRAME(RAW_SN, FRM_NO)` 유일성이 곧 파일명 유일성이 된다. 확장자는 소스에서 승계하되 이미지 allowlist 로 제한(비허용·미상은 `jpg`) ②`buildFrameSpecs` 에 **목적 경로 중복 fail-fast**(`seenDstPaths`, 기존 `seenFrameKeys` 가드와 동일 스타일·`ErrorCode.INTERNAL_ERROR` 재사용) 추가 — 제약 드리프트 시에도 덮어쓰기 대신 파생 전체를 실패 종결. **TC-RESL-041 의 3차 실측 FAIL 을 해소 표기**로 갱신하고 회귀 케이스 TC-RESL-096/097 신설. 저장모델(`LS_DATA_AUG`/`LS_DATA_AUG_LBL_MAP`)·DB 스키마 무변경. ⚠ **이미 유실된 프로덕션 파생물(rawSn 98·163·165 등)의 데이터 정합 복구는 본 수정 범위 밖**(코드 결함만 닫음 — 재생성 필요) |
| 6 | 2026-08-04 | 0건 | 1건(TC-RESL-098) | 0건 | **E-ISSUE-61 커버리지 보강 회차(코드 무변경 — 테스트만 추가)** — 5회차에서 신설한 `extensionOf` 의 확장자 allowlist 방어가 **주석에 CWE-22/CWE-20 근거로 명시돼 있는데 정작 그 방어를 검증하는 테스트가 없다**는 coverage 지적(HIGH 1 + MED 1)을 닫았다. `ResolutionSnapshotServiceTest` 에 3건 추가 — ①`허용목록에_없는_확장자는_jpg로_강제된다`(`.webp`·`.gif`) ②`확장자가_없는_소스는_jpg로_강제된다`(확장자 없음·점으로 끝나 빈 확장자) ③`대문자_확장자도_allowlist에_매칭되어_원확장자로_승계된다`(`.PNG`→`png`·`.JPEG`→`jpeg`). **RED 실증 완료** — 폴백/allowlist 제거 시 3건 전부 FAIL, `toLowerCase(Locale.ROOT)` 만 제거 시 ③만 FAIL(다른 12건은 영향 없음)로 각 가드를 정확히 겨냥함을 확인. ⚠ 대소문자 정규화는 **구현이 이미 하고 있던 동작**이라 코드 수정 없음(놀람 방지용 characterization test) — `extensionOf` 는 basename 전체를 소문자화한 뒤 allowlist 매칭하므로 대문자 확장자가 대문자라는 이유만으로 jpg 폴백되지 않는다. 프로덕션 코드·저장모델·DB 스키마 전부 무변경 |
| 7 | 2026-08-04 | 7건(TC-AUG-117 각주 · TC-RESL-052 · TC-EXPORT-022/023/030 · TC-META-030/034/044) | 5건(TC-EXPORT-044/045/046 · TC-META-045/046) | 0건 | **★export 개인정보 3필드 = 산출종류 × 블록 4칸으로 반전**(사용자 확정, 구속) — 원천(`orgnl`)은 `video`=**관제 인입값**(`LS_DATA_INGEST`, V166 신설 · V170 fail-closed DB DEFAULT `N`/`N`/`Y`) / `image`=**정책 상수 `N`/`N`/`Y`**(프레임 단위 원천 판정 데이터 부재), 비식별(`deid`)은 종전대로 수동값 우선(미입력 시 `Y`/`N`/`N`). **구 정책(2026-08-03) "원천 3필드 전부 `null`" → 폐기**(근거: 관제가 원천 판정을 무조건 송신 + export JSON 이 재적재되는 왕복 자산이라 `null` 이 "유실"과 구분 불가). ⚠ 구 정책의 **"값을 지어내지 않는다" 조항은 존치** — `video` 원천에서 관제 미송신 = `null`. 파생영상은 원천 축이 `video`·`image` 모두 `null`. 부수 확정: **비식별 축 3필드는 INSERT 시점 실적재**(엔티티 팩토리 — `@DynamicInsert` 부재로 DB DEFAULT 불가), **부모 미입력 시 파생은 적재 기본값 계승**(구 "파생도 null" 폐기), **원천 축이 콘텐츠 해시 `SPRV` 조건부 블록에 편입**, **판정·상수 단일 원천 정적 가드 신설**. 신고 시 3필드 리셋 폐기의 검증은 B(TC-DEID-058)·C(TC-LABEL-90/97) 소관이라 E 는 중복 신설 없이 파급만 반영 ⚠ **머지 합류(2026-08-05)로 회차 번호 4 → 7 재부여** — main 의 회차 4~6 과 겹쳤다. |
| 8 | 2026-08-05 | 2건(TC-EXPORT-027 · TC-EXPORT-029 근거 라인) | 0건 | 0건 | **머지 후 근거 `file:line` 드리프트 정정(케이스 내용·기대결과 무변경)** — 두 건 모두 머지가 만든 드리프트가 아니라 **사전 드리프트**(양쪽 브랜치 공통)였다. ①TC-EXPORT-027 `VideoMetaMapper.java:101` → **:130** (101 은 `type (미보유)`, `vd_description` 매핑은 `NiaVideo` 생성자 마지막 인자인 130행. `NiaVideo.java:46` 은 실측 정확해 유지) ②TC-EXPORT-029 `VideoMetaMapper.java:144-152` → **:68-70(적용), :173-181(정의)** (144-152 는 `composeCoordinates`/`asString`. `firstNonBlank` 정의는 173-181, 촬영환경 3필드 적용은 68-70). 표 행 추가·삭제 0 → 머리말·README 합계 불변 |
| 9 | 2026-08-05 | 3건(TC-EXPORT-024 · TC-EXPORT-028 · TC-META-047 근거 라인) | 0건 | 0건 | **`VideoMetaMapper`/`NiaVideo` 인용 행 근거 라인 잔여 드리프트 일괄 정정(케이스 내용·기대결과 무변경)** — 회차 8 에서 "다음 회차"로 미뤘던 건을 `grep -n` 재실측으로 확정했다. ①TC-EXPORT-024 `:61-64` → **`:72-75`**(61-64 는 촬영환경 meta 폴백 근거 주석. 실제 fail-secure 는 `kindVideoPath` 삼항 + `basename` 산출) ②TC-EXPORT-028 `:46-59` → **`:57-70`**(46-59 는 5인자 오버로드 javadoc·시그니처. 실제 산출은 우선순위 근거 주석 57행부터 `weather`/`timeOfDay`/`season` 68-70) ③TC-META-047 `:63-79` → **`:76-95`** + `NiaJsonBuilder.java:151-160` → **`:178-181`**(63-79 는 촬영환경 구간과 kind 경로 주석에 걸쳐 있었다. 실제 `video` 3필드는 입도 근거 주석 76-88 + `resolveVideo*` 3회 호출 89-95, `image` 3필드는 `resolveImage*` 3회 호출 178-181). **재확인 결과 정확해 무변경**: TC-EXPORT-027 `NiaVideo.java:46` · TC-EXPORT-029 `:68-70,:173-181`(회차 8 정정분) · TC-EXPORT-030 `:92-95`(`resolveVideo*` 3회 호출과 정확히 일치) · TC-EXPORT-026 `NiaVideo.java:12`(`@JsonInclude(ALWAYS)`). 표 행 추가·삭제 0 → 머리말·README 합계 불변 |
| 10 | 2026-08-05 | 2건(TC-EXPORT-004 · TC-EXPORT-015 원장 컬럼명) | 7건(TC-EXPORT-047~053) | 0건 | **V173 — 산출 원장 표준용어 정합(@req R3) + 데이터구축용량 신설(@req R4) 반영 회차** — ①`EXPORT_*` 는 표준 미등록 약어(정본 CSV 대조: `EXPORT` 는 *수출*=`EXP` 의 영문일 뿐)라 산출물=`OUTPUT`·프레임=`FRME` 로 5건 개명(`EXPORT_SN`→`OUTPUT_SN` 등). **테이블명·JPA 자바 필드명은 불변**(`@Column(name)` 값만 — 파생 쿼리 메서드 3종 연쇄 방지) ②`DATA_ETBL_CPCT BIGINT`(표준도메인 수B20) 신설 — 버전 폴더 총 바이트를 관제 `dataset_versions.data_etbl_cpct` 에 공급. ★**계획 전제 정정**: 착수 시점 전제는 "뷰가 rename 을 조용히 추종해 `V_COMPLETED_VIDEO` 출력명이 바뀐다"였으나 **실측 결과 거짓** — PostgreSQL 은 뷰 *본문*만 추종하고 출력명은 자동 별칭으로 보존한다(`SELECT new_name AS old_name`). 즉 이번 변경은 **관제 연동 계약면을 건드리지 않았다**(TC-EXPORT-048 로 고정). 기존 `DatamartViewSlimIT` 의 뷰 SELECT 는 구 이름 그대로 유지하고 원장 INSERT 만 새 이름으로 바꿨다 |

| 11 | 2026-08-05 | 1건(TC-EXPORT-048) | 0건 | 0건 | **V174 — `V_COMPLETED_VIDEO` 30컬럼 재작성의 파급(이 카탈로그는 출력명 축만 영향)** — 10차가 고정한 "뷰 출력명은 바뀌지 않는다"는 **V173 단독 상태의 사실**이었고, V174 가 뷰를 `DROP`+`CREATE` 로 명시 재작성하면서 출력명까지 표준 물리명(`OUTPUT_PATH_NM`·`OUTPUT_STTS_CD`·`FRME_CNT`)으로 맞췄다 → TC-EXPORT-048 을 "본문은 추종 · 출력명은 V174 가 명시 지정"으로 정정(구 기대결과는 폐기 표기). `DATA_ETBL_CPCT` 는 10차 시점에 "어느 뷰에도 노출되지 않음"이었으나 **V174 로 뷰 23번 컬럼으로 노출**된다(관제 `dataset_versions.data_etbl_cpct` 직결) — 산출·용량 케이스(TC-EXPORT-050~053)의 기대결과는 불변. 뷰 자체의 30컬럼 계약 케이스는 [D 카탈로그](D-review-version-notify.md) TC-MARTVIEW-024~035 소관 |
| 12 | 2026-08-05 | 2건(TC-EXPORT-024 · TC-EXPORT-036) | 3건(TC-EXPORT-054~056) | 0건 | **어노테이션(NIA export JSON) 정정 3건 반영 회차(@req R9)** — 사업 어노테이션 표준 대조에서 드러난 결함이다. ①**`dataset.name`**(TC-EXPORT-054 신설) 경로 basename → **`{이벤트명} 데이터셋 구축`**(뷰 `DATST_NM` 과 같은 규칙, kind 무관). 규칙이 SQL·Java 두 곳에 존재하므로 실 DB 대조 가드를 [D](D-review-version-notify.md) TC-MARTVIEW-039 로 고정 ②**`info.year`**(TC-EXPORT-055 신설) `LocalDate.now()` → **검수완료 연도(`RVW_CMPL_DT`)로 고정** — 재export 시 값이 바뀌던 멱등성 결함(수용기준 A8). `date_created` 는 다른 개념이라 now() 유지 ③**`video.event_id`**(TC-EXPORT-056 신설) `EVNT_TYPE_CD` → **인입 `EVNT_ID`** — 축이 다른 값을 싣고 있었다. 미송신이면 `null`(유형코드 폴백 금지). 파급 정정 2건: TC-EXPORT-024 의 *"deid 경로 미상 시 `dataset.name` 도 null"* 기대 **폐기**(name 이 경로 파생이 아니게 되어 경로 누수 축이 아니다), TC-EXPORT-036 의 kind 분기 범위를 `src_path` 로 한정. `AI_CRT_YN` 동결 도출식 정정(@req R10)은 [D](D-review-version-notify.md) TC-MARTVIEW-038 소관 |
| 13 | 2026-08-06 | 1건(TC-EXPORT-027) | 10건(TC-EXPORT-057~066) | 0건 | **★`video.vd_description` 배선 — 구 동작 "항상 null 하드코딩" 폐기(@req R10)** — 외부 VLM `verify` 콜백이 서술 전문을 `LS_DATA_META['vlm.description']` 에 적재하면서 원천이 생겼다. 조달 규칙(사용자 확정): **① 전문 → ② 없으면 보존된 레거시 구간 행을 `start_sec` 오름차순 이어붙임 → ③ 없으면 `null`**(빈 문자열 아님 · 지어내지 않음). 판정 단일 원천은 신설 `dataset/export/json/VlmDescriptionPolicy` 하나이며 매퍼·서비스가 재유도하지 않는다. 핵심 방어: **숫자 정렬**(문자열이면 구간 10개 초과 시 `10-18` 이 `8-16` 앞 — TC-EXPORT-058) · **구간형 아닌 키 무시 + 예외 미전파**(CWE-20 — TC-EXPORT-061) · **자동 절단 금지**(사일런트 손실 방지, 과대 길이는 관측 로그만) · **파생 부모 폴백 없음**(TC-EXPORT-066) · **`vlm.accuracy` 는 export 미포함**(R12 유지 — TC-EXPORT-063). ★**"저장은 됐는데 산출물이 안 바뀐다" 축을 세트로 차단** — ①콘텐츠 해시 조건부 블록 `VDSC` 편입(TC-EXPORT-064) ②승인 후 서술 변경 시 `TaskModifiedEvent(exportRegenerated=**true**)`(TC-EXPORT-065). **구 기대 "R13 은 regen=false" 는 폐기** — 서술이 export 입력이 되면서 디스크가 실제로 달라진다. `MetaService.update` 는 조달 참여 키일 때만 true(무관 키는 false — 관제 헛 재픽업·저장소 증폭 방지). mutation 실증 7종 완료(우선순위 역전·문자열 정렬·null→빈문자열 2곳·해시 미편입·regen 트리거 2곳). v2-wiki [24 §24.4.3](../v2-wiki/24-dataset-export.md) 신설 + [09 §9.4-1](../v2-wiki/09-vlm-timeseries.md) 동기화 동반. BE 전용 |

| 14 | 2026-08-06 | 5건(TC-EXPORT-059 · 061 · 064 · 065 · 066) | 9건(TC-EXPORT-067~075) | 0건 | **★`manual-timeseries` 조달 편입(구 "조달 무관 키" 폐기) + 값 무변경 재생성 억제(@req R10)** — 회차 13 DEV_FIX. ①**조달 우선순위 3단화** — 전문(`vlm.description`) → **`manual-timeseries`**(사람이 직접 쓴 전문) → 레거시 구간 → `null`. FE 시계열 패널은 **편집 가능한 항목이 하나도 없을 때**(메타 0건 **또는 레거시 구간뿐**) 이 키로 신규 등록 슬롯을 띄우는데, 구 조달은 그 값을 무시하고 **편집조차 불가능한 옛 구간 이어붙임**을 대신 내보냈다(조용한 손실). 키 문자열은 `VlmDescriptionPolicy.MANUAL_TIMESERIES_META_KEY` **단일 원천**이며 정적 가드로 고정(TC-EXPORT-075) ②**무변경 저장이 버전을 적층하던 결함(CWE-770)** — 구 판정은 **키만** 보고 값 비교를 하지 않았고, 재생성이 `ControlNotifyDebouncer` 를 거쳐 **`force=true`** 로 위임돼 해시 멱등 skip 도 타지 못했다. 같은 값 N회 저장 → `v2·v3·v4…` 가 이미지 2벌 전량 복사와 함께 적층 + 매번 관제 통지. 최종 조건 = **(조달 참여 키) AND (값 실제 변경)** — 웹훅 경로 `changed` 가드와 동형. 무변경 저장 자체는 **200 성공**이고 통지도 발행하며 **재생성 플래그만** 생략(TC-EXPORT-071~074) ③**문서 정정 2건(코드 무변경)** — `LabelContentHasher` 의 `VDSC` 블록은 **현재 배포 형상에서 아무것도 게이트하지 않는다**(`force=false` 진입 경로 0건, 휴면 리스너 `DatasetExportBridge.onReExport` 뿐)는 사실을 명시(TC-EXPORT-064) / `DatasetExportTxService` 의 *"파생은 자기 메타가 없으면 null"* 주석이 사실과 달랐다 — `DerivedMetaCopier.copyMetaAndReviews` 가 부모 메타를 **키 필터 없이 물리 복사**하므로 파생은 부모 서술을 **실제로 갖는다**(산출 결과는 정합, TC-EXPORT-066). mutation 실증 5종 완료(조달·참여 제거 / 우선순위 역전 2종 / 값 비교 무력화 / 키 리터럴 복제). v2-wiki [24 §24.4.3](../v2-wiki/24-dataset-export.md) · [09 §9.4-1](../v2-wiki/09-vlm-timeseries.md) 동기화 동반. BE 전용 |
| 15 | 2026-08-07 | 1건(TC-META-030) | 3건(TC-META-048~050) | 0건 | **★프레임 개인정보 메타 조회 응답에 출처(`*Source`) 3필드 신설 — 영상 축과 동일 계약.** 이 카탈로그는 회차 4(2026-08-04)부터 TC-META-030 에 `*Source=DERIVED` 를 기대결과로 적고 있었으나 **응답 계약에는 그 필드가 없어 화면이 받을 수 없었다**(커밋 `0d290c4e` 가 "프레임 패널의 항목별 출처 뱃지 부재 — 응답 계약 변경 필요"로 미해소 등재한 그 갭). 이제 `anonymitySource`·`pseudonymitySource`·`privacyIncludedSource` 가 실제로 내려간다. ①**어휘는 영상 축 상수를 참조**한다(리터럴 재선언 금지 — 두 축이 다른 문자열을 내리면 FE 가 한 벌로 처리하지 못한다) ②**출처는 필드별 판정**이며 술어가 프리필 폴백 조건과 **같아야** 값과 출처가 어긋나지 않는다 ③**하위호환은 추가만** — 기존 4필드의 이름·타입·시맨틱 불변. ⚠ **FE 규약이 함께 지켜져야 한다** — 사용자가 직접 고르지 않은 `DERIVED` 필드를 그대로 PUT 으로 되돌려 보내면 **기본상수가 사람의 판정으로 승격**되고 서버는 출처를 알 수 없어 막지 못한다(조회 OpenAPI `description` 에 명시) |

| 16 | 2026-08-09 | 0건 | 8건(TC-AUG-122~129) | 0건 | **★증강 결과 조회 응답에 요청일시(`requestedAt`) 신설 — 작업 요약의 "생성일" 을 공급하는 API 가 하나도 없던 갭 해소.** 화면 요약은 요청 시각 칸을 요구하는데 잡·항목 응답 어디에도 그 값이 없어 화면이 만들 수 없었다(공급원 부재). ①**필드명은 목록 API 가 이미 쓰는 `requestedAt` 을 그대로 채택**한다(계약 일관성 — 같은 뜻에 다른 이름을 만들지 않는다) ②**산출 축은 목록과 동일한 `MIN(REG_DT)`** 이고 판정 단일 원천은 `AugmentReviewService.resolveRequestedAt` 다. 결과 조회가 산출식을 복제하면 두 번째 진실원이 생겨 같은 잡이 화면마다 다른 시각을 표시한다 ③**항목별 `decidedAt`(채택·반려 결정 시각)과 축이 다르다** — 서로 대체할 수 없다 ④**반환 지점이 셋**(항목 0건 조기 반환 · 항목 축 페이지 범위 밖 조기 반환 · 정상 경로)이라 한 곳만 채우면 "빈 페이지에서만 값이 사라지는" 결함이 된다 — 조립을 `Paging.response` 한 곳으로 모아 컴파일러가 누락을 막고, 세 지점을 각각 케이스로 고정한다(TC-AUG-122·124·125) ⑤**증강 행이 0건이거나 등록일시가 전부 비면 `null`** 이며 잡 진입 시각·현재 시각으로 **지어내지 않는다** ⑥**하위호환은 추가만** — 기존 10필드의 이름·타입·의미·기본값 불변(외부 FE 팀 공유 계약면, TC-AUG-123) ⑦화면은 값이 없으면 `-` 를 보이고 결정 시각으로 대체하지 않는다(TC-AUG-127~129). mutation 실증 2종(조기 반환 한 곳의 값 설정 제거 → TC-AUG-125 실패 / 화면 바인딩을 상수 `-` 로 치환 → TC-AUG-127 실패). ⚠ **FE 행(TC-AUG-127~129)이 이 카탈로그에 있는 것은 예외**다 — 원칙상 화면 케이스는 `H-frontend-e2e.md` 소관이나 그 파일이 **다른 작업의 미커밋 편집 중**이라 동시 수정을 피했다. H 로 이관될 때 이 3행은 폐기 표기 후 옮긴다 |
| 17 | 2026-08-13 | 1건(TC-AUG-038) | 0건 | 0건 | **영상 표시명 폴백을 화면 전체에서 하나로 통일 — 증강 2화면의 자체 상수 `"(이름 없음)"` 폐기.** V185(관제 회신 2026-08-12, `VMS_CCTV_ID` NULL 허용)로 도입된 판정 단일 원천 `CctvDisplayNamePolicy`(CCTV명 → `VMS_CCTV_ID` → `영상 #{rawSn}`)를 영상 목록·상세 외의 화면들도 쓰게 했다. 증강 검수 잡카드(`AugmentReviewService.toJob`)와 증강 결과 조회(`AugmentResultViewService.resolveCctvName`)가 각자 들고 있던 상수를 제거하고 정책기에 위임한다. **구 표기가 왜 나쁜가** — `"(이름 없음)"` 은 이름 없는 영상이 여럿이면 **모든 행이 같은 문구**가 되어 어느 영상인지 식별할 수 없고, 같은 영상이 작업목록에서는 `영상 #N` 으로 보여 표기가 갈렸다. 같은 라운드에서 작업목록(REVIEWER·WORKER)·검수목록도 같은 정책기로 수렴했다(→ [D-3e](D-review-version-notify.md)). 표 행 추가·삭제 0 → 머리말·README 합계 불변 |
| 18 | 2026-08-13 | 0건 | 3건(TC-EXPORT-076~078) | 0건 | **관제 회신(`docs/저작도구-관제회신-답변-20260812.md` §7 · V185) 반영 — `FRME_CNT` 산정 방식이 벌 합계에서 실제 프레임 수(N)로 바뀐 것을 카탈로그에 신설.** 관제가 이 값을 `datasets.img_nocs`·`dataset_versions.data_etbl_nocs` 에 "추출·라벨링 프레임 수"로 그대로 적재하는데, 원본(orgnl)+비식별(deid) 2벌 쓰기 건수를 합산하면 정확히 2배로 부풀고 1벌만 산출하는 파생영상과 값의 축이 갈린다. **벌별 쓰기 건수의 최댓값**을 적재해 영상 유형(일반 2벌/파생 1벌)과 무관하게 일관된 N 을 낸다. TC-EXPORT-076(부분실패 시 최댓값)·077(파생 1벌도 같은 축)·078(e2e — 구 `FRAME_COUNT*2` 기대값 폐기) 신설. **V185 는 DB DDL 변경 없이 V173 이 남긴 "원본벌+비식별벌 합계" COMMENT 만 재지정**한다 — 산정 로직 자체는 이전부터 있었고 문서·주석만 실제와 어긋나 있었다(코드 변경 없음, 문서 정합 회차). 이 회차와 세트인 통지 `output_ver_no`·이벤트 어노테이션 재생성 결함 해소는 [D 카탈로그](D-review-version-notify.md) 회차 23(D-6b `TC-NOTIFY-063~066` · D-1d `TC-REVIEW-079`) 소관 |
| 19 | 2026-08-15 | 0건 | 0건 | 1건(TC-EXPORT-041) | **`DeidentReportResolvedEvent` 즉시 force 재생성 기대값 폐기 — 코드 변경 0(문서 정합).** [D 회차 16 ⑤](D-review-version-notify.md)가 해소 시점의 즉시 재산출·재통지를 폐기했는데 export 축의 중복 행이 갱신되지 않아 남아 있었다. 코드 실측: 그 이벤트는 `src/main` 발행 **0건**인 휴면 확장점이고, 해소는 `TaskModifiedEvent(exportRegenerated=true, needsRecheck=true)` 로 **재검토 표시만** 세운다 — force 전량 재생성과 관제 통지는 **재승인 시점**에 디바운스 flush 로 나간다. ⚠ **"범위는 그 영상 하나(파생 팬아웃 없음)"는 불변**이다 — 폐기된 것은 *시점*이지 *범위*가 아니므로 그 조항을 함께 버리지 말 것. ⚠ **강제 재생성(멱등 우회) 근거도 그대로 유효**하다 — 콘텐츠 해시가 이미지 픽셀을 반영하지 않아 해시가 같아도 디스크의 비식별 이미지는 바뀌어 있다(그래서 재승인 시점의 재생성이 force 여야 한다). 대체 케이스 = [D TC-NOTIFY-059~061](D-review-version-notify.md). 같은 오기가 6개 층에 있었고 한 라운드로 함께 정정했다 — 전체 범위는 [B 회차 47](B-batch-deidentify.md) 참조. 표 행 추가·삭제 0 → 머리말·README 합계 불변 |
| 20 | 2026-08-19 | 1건(TC-AUG-045) | 5건(TC-AUG-130~134) | 0건 | **★외부 증강 미연동(`authoring.augment.external.mode=noop`)이면 요청 접수 자체를 503 으로 거부(R8).** 구 동작은 접수가 성공해 `LS_DATA_AUG` PENDING 행이 생기는데 위탁도 콜백도 없어, 화면에는 **만료 스윕이 돌 때까지 「진행 중」**으로 보였다(데이터 오염은 없다 — 가짜 산출물을 만들지 않고 PENDING→만료로 회수된다. 남는 문제가 "되지도 않을 요청이 접수된다" 하나라 그것만 고친다). ①**판정 단일 원천 `AugmentExternalModePolicy`** 신설 — 빈 배선을 결정하는 **같은 프로퍼티**를 **같은 기본값(`http`)**으로 읽는다(`GenAiIntegrationWiringGuard` 전례). 빈 타입 검사(`instanceof`)는 AOP 프록시에 취약해 쓰지 않는다 ②**게이트는 요청 접수 한 곳에만** 둔다 — 콜백 수신·조회·검수·취소로 확산시키면 이미 접수된 건이 회수되지 못하고 고착된다. `NoopExternalAugmentClient` 자체와 `EXTERNAL_DISABLED` 조회 응답·만료 스윕은 **그대로 유지**한다 ③**평가 순서가 계약**이다 — 인가·형식 검증 뒤(미인증 호출자에게 연동 상태 노출 금지, CWE-209), 파생 차단 뒤(영구 조건 400 이 일시 조건 503 보다 먼저), DB 조회 앞(거부할 요청에 왕복 3회 금지, CWE-770). TC-AUG-045 의 순서 계약을 그에 맞춰 정정 ④**기본값을 뒤집지 말 것** — `NoopExternalAugmentClient` 가 `havingValue="noop"` 명시 전용이라 미설정=http 배선이고, 기본을 noop 으로 잡으면 "빈은 http 인데 접수는 503" 모순이 된다. mutation 실증 3종(게이트 호출 제거 → TC-AUG-130·133 실패 / 게이트를 인가 앞으로 이동 → TC-AUG-132 및 파생 순서 케이스 실패 / 판정 기본값을 noop 으로 반전 → 미설정·빈 값 케이스 실패) |
| 21 | 2026-08-26 | 5건(TC-RESL-004·005·015·041·045) | 9건(TC-RESL-099~107) | 0건 | **★해상도 파생 리스케일이 16:9 고정 캔버스+검정 레터박스에서 「짧은변 기준 + 긴변 상한」 가변 캔버스로 전환(CO-018 · `ADR-018` v5 · `AC-003` v8 · `API-092` v8 · `API-179` v6, 2026-08-26 사용자 확정·구속).** 프리셋의 두 수치가 **고정 캔버스가 아니라 크기 상한**이 되어 산출 프레임 크기가 원본 종횡비에 따라 달라지고 **패딩이 0** 이다. ①**스킵 판정축 전환** — 「프리셋 값이 원본과 같은가」 → **「산출 크기가 원본과 같은가」**(TC-RESL-004), 400 문구도 함께 교체(TC-RESL-005) ②**좌표 변환에서 오프셋 소멸** — `x' = x*scale + offsetX` → **`x' = x*scale`**, 그 결과 `SCALE_X/SCALE_Y` 만으로 원본 좌표가 완전 역산되어 구 「추적성 한계·오프셋 컬럼 신설은 후속 과제」 서술이 소멸(TC-RESL-045) ③**응답 `targetW/targetH` 가 실제 산출 크기**(생성·조회 공통, TC-RESL-015) ④신규 9건 — 산출 크기 표 5종(TC-RESL-099) · 박스 피팅 비회귀(100) · 산출크기 동일 스킵 회귀 가드(101) · 응답 산출크기(102) · 조회 API 산출크기(103)와 실측 불가 폴백(104) · **패딩 부재 픽셀 실증(105)** · 리사이즈 멱등(106) · 긴변 클램프 mutation 가드(107). ⚠ **축별 독립 배율(왜곡, E-ISSUE-26)로 되돌아가는 것이 아니다** — 종횡비 보존은 유지되고 바뀐 것은 「남는 영역을 패딩으로 채우느냐」 하나다(경위 3단계: 축별 독립 배율 → 종횡비 보존+검정 패딩 → 종횡비 보존+가변 캔버스). ⚠ **기존 파생본은 재생성하지 않는다**(사용자 확정) — 검정 패딩이 든 채 존치하고 신규 생성부터 적용 |
| 22 | 2026-08-26 | 2건(TC-RESL-041 · TC-RESL-106) | 4건(TC-RESL-108 · 109 · 109b · 110) | 0건 | **★CO-018 DEV_FIX — 리사이저에 변환이 두 번 걸리던 결함 수정 + 그것을 통과시킨 가드 보강.** 회차 21 구현은 Phase B 가 `imageResizer.resize(.., snapshot.targetW(), snapshot.targetH())` 로 **이미 도출이 끝난 산출 크기**를 넘겼는데, 리사이저는 그 인자를 **크기 상한**으로 받아 `LetterboxTransform` 을 스스로 적용한다 — 같은 원본에 변환이 두 번 걸렸다. 상용 28해상도 × 프리셋 3종 = 84조합 중 **2건**(`2560×1080`+480p → 파일 853 vs 스냅샷 854 · `1366×768`+1080p → 1919 vs 1920)에서 산출 파일이 1px 작아져 ①라벨 최우측 좌표가 이미지 경계를 **최대 2px 이탈** ②응답·DB 가 보고하는 크기와 실제 파일 불일치 ③`LetterboxTransform` 이 스스로 내건 "픽셀과 라벨이 같은 계산기를 쓴다"는 불변식 위반. **수정 = 리사이저에 `snapshot.preset().width()/height()` 를 넘긴다** — 계산기 입력이 Phase A 와 완전히 같아져 불변식이 복원된다(DB·계약 영향 0, 포트 계약 무변경). ⚠ **이 결함은 회차 21 이 새로 만든 것**이다 — 그 전에는 리사이저가 프리셋 값을 받아 스냅샷과 입력이 같았으므로 구조적으로 발생할 수 없었다. ⚠⚠ **TC-RESL-106 의 판단은 옳았고 실패한 것은 케이스 선정뿐이다** — `AC-003` 이 열거한 5종이 **전부 멱등 구간**이라 속성이 거짓인데도 통과했다(정상 경로 케이스만으로는 이 결함 클래스를 한 건도 잡지 못한다). 이제 비멱등 2종을 명시 고정하고, 배선은 **실제 이미지 파일 크기**(109) 와 **mock 인자**(108) 두 층에서 단언한다. mutation 실증 2026-08-26: `snapshot.targetW()/targetH()` 로 되돌리면 TC-RESL-108 **FAILED**. ⚠ **TC-RESL-041 의 720p 조합은 프리셋(1280×720)과 산출 크기(1280×720)가 우연히 같아 이 배선을 구분하지 못한다** — 두 값이 갈리는 조합이 반드시 필요하다. 추가로 TC-RESL-110 은 **4:3 원본에서 파생이 3 → 2건으로 줄어드는 운영 변화**를 전용 케이스로 고정한다(회차 21 은 주석 언급뿐이었다) | BE 전용
| 23 | 2026-08-26 | 0건 | 1건(TC-RESL-111) | 0건 | **★CO-018 마무리 — 종단 가드 1건 신설 + 헤더 카운트 정정.** ①회차 22 가 4행(TC-RESL-108·109·109b·110)을 신설하고 **헤더 카운트만 갱신하지 않아** 헤더가 스스로 명시한 산식(`grep -cE '^\| ~*TC-'`)의 실측(323)과 표기(319)가 어긋나 있었다 — 이 저장소가 감사에서 반복 지적한 **개수 표기 드리프트**라 같은 라운드에서 맞춘다(회차 22 의 *"4건 신설"* 서술 자체는 정확했고 어긋난 것은 헤더 숫자 하나다). ②TC-RESL-111 은 「`materialize` → **실제** 리사이저 → 산출 파일 치수」를 잇는 **종단** 가드다 — 회차 22 가 세운 가드는 인자 층(108, Mockito 라 파일 미생성)과 속성 층(109, 리사이저 직접 호출로 materializer 미경유)뿐이고 흐름 통합시험은 `ImageResizer` 를 `@MockBean` 으로 격리해, **배선과 산출물을 한 줄로 잇는 지점이 0** 이었다. 이번 결함 자체는 108 이 확실히 잡지만 **리사이저 구현 교체·인자 의미 변경**처럼 그 시야 밖에서 같은 증상이 나는 변화는 세 층 모두 통과한다. ⚠ 비멱등 조합(`2560×1080`+480p)이어야 한다 — 멱등 구간은 두 배선이 같은 결과를 낸다. mutation 실증 2026-08-26: `snapshot.targetW()/targetH()` 로 되돌리면 TC-RESL-111 **FAILED**(`expected: 854 but was: 853`) · 같은 실행에서 TC-RESL-108 도 FAILED(2/7) | BE 전용
| 24 | 2026-08-27 | 4건(TC-AUG-001 · 046 · 047 · 048) | 9건(TC-AUG-135~143) | 0건 | **★외부 증강 위탁 요청을 연동명세서 v1.3 으로 정합(CO-20260827-증강위탁-v13-정합 · `@design INT-008`·`API-060`)** — 요청 본문 구조와 값 규칙이 함께 바뀐 회차다. ①**생성 조건이 `prompt`(객체) → 최상위 `mtdt`(객체)로 이동**하고 `prompt` 는 **자유 지시문 문자열**(≤1000, 선택)로 재정의됐다. 구 객체 형식(`prompt.condition`/`prompt.text`)과 최상위 `condition` 은 계약에서 제외됐고 명세가 *"prompt 값으로 객체를 전달하지 않습니다"* 를 명시한다 — 객체를 실으면 벤더가 `400 INVALID_PARAMETER` 다 ②**★구 정책 「생성 조건은 자유 문자열이며 서버가 값 자체를 좁히지 않는다」(2026-07-31) 폐기** — v1.3 은 다섯 축 전부의 허용 코드를 닫았다(`time` DAWN\|DAY\|DUSK\|NIGHT · `season` SPRING\|SUMMER\|AUTUMN\|WINTER · `weather` CLEAR\|CLOUDY\|RAIN\|SNOW\|FOG\|WINDY · `terrain` ROAD\|UNDERPASS\|RIVER\|URBAN\|RESIDENTIAL\|RURAL\|MOUNTAIN\|FOREST · `severity` LOW\|MEDIUM\|HIGH). 그래서 구 케이스가 정상값으로 쓰던 `TUNNEL`·`BRIDGE` 는 이제 **400** 이다 ③**이벤트 유형 조달 축 반전** — 구 구현은 위탁 시점에 영상의 관제 이벤트 코드(`LS_DATA_RAW.EVNT_TYPE_CD`, 미상이면 `ETC`)를 실었는데 계약 허용값은 `FLOOD`·`WILDFIRE` 둘뿐이라 그 값들은 벤더에서 전부 거부된다. 이제 **요청 본문 필수 `evntType`** 이며 서버가 관제 코드에서 변환하지 않는다(두 분류 축이 다른 체계라 변환은 추정이 된다). `evntSubtype`(침수 전용, 선택)도 함께 받는다 ④**보관도 분리 형태**(`{"mtdt":{...},"prompt":"..."}`)로 남겨 나간 값과 보관값이 갈라지지 않게 한다. ⚠ **기존 적재분(구 평면 형태 `{"time":...}`)은 마이그레이션하지 않는다** — 조회 경로가 이 값을 파싱하지 않고 문자열 그대로 내려주므로 두 형태가 공존해도 깨지지 않는다(dev DB 실측 4행). **불변**: 다섯 항목 전부 필수는 **우리 규칙**이라 유지(계약은 최소 1개 — 더 엄격한 쪽이 의도된 선택) · 증강 종류(`types[]`)를 생성 조건에서도 이벤트 유형에서도 파생하지 않음 · 콜백 축(`POST /v1/genai/callback`)·청크 분할·4xx 비재시도 무변경. ⚠ **목 서버 왕복 검증은 이 회차에서 수행하지 못했다** — 시험 전제가 요구하는 `ai-server/.venv` 가 워크트리에 없어 스킵된다(잔여) |
| 24-1 | 2026-08-27 | 3건(TC-AUG-135 · 142 · 048 — 전제·판정축 명시) | 0건 | 0건 | **회차 24 DEV_FIX(시험 가드 전용 · 운영 코드 무변경)** — 독립 QA 가 회차 24 의 부정 시험 3건이 **공허했음**을 실증했다. `mtdtOutsideAllowedCodesReturns400`·`unknownEventTypeReturns400`·`arbitraryTypeStillRejected` 가 쓰던 영상(8503·8512·8506)이 **시드되지 않아**, 허용 코드 방어가 무너져 바인딩이 통과해도 서비스가 `NOT_REVIEWED` 를 냈고 **그것 역시 400** 이라 `status().isBadRequest()` 만 보던 시험이 그대로 GREEN 이었다(QA 실증 — `Terrain` 에 `TUNNEL`, `EventType` 에 `ETC` 를 더해도 24/24 통과). 즉 **AC2(허용 코드 밖 거부)에 실효 검증이 0** 이었고, 같은 취약성이 **CWE-22·`RESL_` 침범을 막는 `types[]` enum 방어 시험**에도 있었다. **조치** — 세 시험 모두 ①영상을 `APPROVED`+대표프레임으로 **시드**하고 ②`errorCode=INVALID_INPUT` 단언을 덧붙였다. ★**시드가 주 판정축이다** — 방어가 무너지면 응답이 **200** 이 되어 오류 코드 체계와 무관하게 RED 다. errorCode 단언은 「400 이 나더라도 그것이 입력 검증 400 인가」를 못박는 두 번째 그물이다. **조합 변이 실증 완료**(QA 는 두 축을 각각만 측정했다) — 세 코드 공간을 **동시에** 넓힌 상태에서 세 시험 전부 RED, 실패 사유는 셋 다 `Status expected:<400> but was:<200>`(= 시드 축이 잡았다). 운영 코드는 변이 후 백업본으로 원복하고 **3파일 sha256 완전일치**를 대조했다. ⚠ **런타임 동작은 처음부터 정상이었다**(DTO 가 enum 을 필드 타입으로 직접 써 Jackson 이 끊는다) — 고친 것은 시험뿐이라 케이스 신설·폐기 0건이고 표 행 수는 **333 불변**이다 |

> **표기 규칙** — 케이스명 끝 ` (신규)` = 이번 회차 추가, 케이스명 `~~취소선~~` + 기대결과 `**[폐기 2026-07-30]**` = 정책 변경으로 무효화(추적성 위해 행 보존).

> **2026-08-05 헤더 카운트 정정(케이스 내용 변경 없음)**: 머리말 총계 232 → **253** 로 실측 정정. ★**2026-08-05 머지 합류 시 253 → 262 로 재정정** — `main` 쪽 신규 행 합류 + 카운트 명령 정정(구 `grep -c '^| TC-'` 는 ID 취소선 폐기 행을 놓쳤다 → `grep -cE '^\| ~*TC-'` 로 고정, [README](README.md) 회차 7). 회차 1~4 가 케이스를 추가하면서 머리말을 갱신하지 않아 누적 21건이 어긋나 있었다. **카운트 기준 = 표 행 실측(폐기 행 포함)** — 이 파일의 폐기 표기는 ID 취소선이 아니라 **케이스명 취소선**(예: `TC-AUG-003`)이라, "폐기 제외" 규칙은 기계적으로 재현되지 않는다.

---

## E-1. 증강 요청 (AugmentRequestService / AugmentController / AugmentRequestBridge)

| ID | 케이스명 | 전제 | 입력/조건 | 기대결과 | 계층 | 우선 | 근거(파일) |
|----|---------|------|----------|---------|------|:--:|----------------|
| TC-AUG-001 | 검수완료 영상 증강 요청 성공 (단건 계약) | APPROVED 영상 1건, 대표프레임 존재 | REVIEWER, `{videoIds:[N], types:["WINTER"], evntType:"FLOOD", mtdt:{time,season,weather,terrain,severity}, prompt?:"..."}` — ⚠ **[2026-08-27 회차 24 정정]** 구 형태 `prompt:{5필드}` 는 폐기(v1.3). `mtdt` 다섯 항목은 **허용 코드**이며 전부 필수, `evntType` 도 필수(`FLOOD`\|`WILDFIRE`), `prompt` 는 자유 지시문 **문자열**(선택). 누락 시 각각 `400 INVALID_INPUT` | 200, `{jobId, requestedAt, videoCount:1, typeCount:1, createdCount:1}`. `LS_DATA_AUG` PENDING 1행 INSERT(`IDMP_KEY=AUG-<uuid>`, `OTSD_JOB_ID`는 **null** — 외부 202 에서 발급, `PROMPT_CN`에 전송 원문을 **분리 형태**(`{"mtdt":{...},"prompt":"..."}`)로 적재 — 회차 24) | integration | High | AugmentRequestService.java; AugmentRequestRequest.java |
| TC-AUG-002 | 미검수 영상 거부 | 대상이 APPROVED 아님 | REVIEWER | 400 `NOT_REVIEWED` + `data.blockedVideoIds`, aug 행 0건 | integration | High | AugmentRequestService.java |
| TC-AUG-003 | ~~videoIds/types 중복 정규화~~ | — | `videoIds:[13,13]` | **[폐기 2026-07-30]** 단건 계약 확정(E-ISSUE-08)으로 distinct 정규화 로직이 **삭제**됐다. DTO `@Size(max=1)` + 서비스 `requireSingleSelection` 이 2건 이상을 400 으로 거부하므로 distinct 가 성립할 입력 자체가 없다 → 대체 케이스는 TC-AUG-017 | unit | — | AugmentRequestService.java; AugmentRequestRequest.java |
| TC-AUG-004 | **프레임 미추출 영상 = 412 거부** | 대표프레임(MIN SRC_SN) 없음 | REVIEWER | **412 PRECONDITION_FAILED** + `data.skippedVideoIds=[rawSn]`, aug 행 0건. 구 "건별 스킵 + 200" 폐기(silent no-op 제거) | integration | High | AugmentRequestService.java |
| TC-AUG-005 | WORKER 증강 요청 차단 | WORKER 토큰 | POST /v1/augments/request | 403 (Controller `@PreAuthorize` 1차 + 서비스 `requireReviewer` 2차) | security | High | AugmentRequestService.java; AugmentController.java |
| TC-AUG-006 | 인증 토큰 없음 | actor=null | 요청 | 401 UNAUTHORIZED | security | High | AugmentRequestService.java |
| TC-AUG-007 | **idempotencyKey만 발급 · externalJobId 미발급** | 정상 요청 | 내부 발급 | `IDMP_KEY=AUG-<uuid>`(≤64, `^[A-Za-z0-9_-]+$`), 형식 위반 시 INTERNAL_ERROR. `OTSD_JOB_ID`는 요청 시점 **null**(외부 202 응답 job_id 로 반전 — 구 `JOB-<uuid>` 자체 발급 폐기) | unit | High | AugmentRequestService.java |
| TC-AUG-008 | 적재 실패 시 실패 회신 | `augRepository.save` 예외 | REVIEWER | 500 INTERNAL_ERROR("증강 요청을 생성하지 못했습니다.") + `skippedVideoIds`. 예외를 삼키고 200 을 주지 않는다 | unit | Med | AugmentRequestService.java |
| TC-AUG-009 | AFTER_COMMIT — 롤백 시 행·위탁 미발생 | 요청 tx 롤백 | — | aug 행 0건 + 외부 위탁 0회. `LS_WEBHOOK_IDEMPOTENCY` 선기록은 **제거**됨(발급 원장은 `LS_DATA_AUG_JOB.IDMP_KEY`) | integration | High | AugmentRequestBridge.java |
| TC-AUG-010 | 콜백 URL 조립 | base trailing slash/blank | 요청 | `AugmentCallbackUrlResolver.resolve()` = base 정규화 + **`/v1/genai/callback`**(구 `/v1/aug/callback` 폐기) | unit | Med | AugmentCallbackUrlResolver.java; WebhookProtectedPaths.java |
| TC-AUG-011 | jobId 동시성 유일성 | 다중 동시 요청 | REVIEWER | AtomicLong 충돌 없음(응답 placeholder — 미영속) | unit | Low | AugmentRequestService.java |
| TC-AUG-012 | ~~중복 활성 요청 차단 — PENDING 안내~~ | 같은 (대표프레임 × 종류) PENDING 존재 | 동일 요청 재전송 | **[폐기 2026-08-02]** 중복 증강 요청 차단 정책 전량 폐기(2026-07-31 사용자 확정, `CLAUDE.md:243` / `V153__add_ls_data_aug_prompt_and_drop_active_unique.sql` 가 `UK_LS_DATA_AUG_ACTVTN` DROP) — 사전 조회 가드·409 분기·`data.duplicatedRequests` 응답 필드가 모두 제거되어 이 요청은 이제 200 이다. 대체 케이스 = TC-AUG-015 | integration | — | AugmentRequestService.java |
| TC-AUG-013 | ~~중복 활성 요청 차단 — ACCEPTED 안내~~ | 같은 (대표프레임 × 종류) ACCEPTED 존재 | 동일 요청 | **[폐기 2026-08-02]** 위와 동일 사유로 폐기 — `applyReviewStatus` 전이 제약과 무관하게 ACCEPTED 존재 여부를 사전 조회하는 코드 자체가 삭제됐다. 대체 케이스 = TC-AUG-015 | unit | — | AugmentRequestService.java; LsDataAug.java |
| TC-AUG-014 | ~~동시 요청 최종 방어 = DB 부분 유니크~~ | 두 tx 가 서로의 미커밋 행 미관측 | 동시 동일 요청 | **[폐기 2026-08-02]** `UK_LS_DATA_AUG_ACTVTN` 인덱스가 V153 에서 DROP 되어 이 최종 방어 자체가 존재하지 않는다(`LsDataAug.java:65` 폐기 이력 주석). ⚠ 남은 409 분기(`AugmentRequestService.java:384-399`)는 **다른 제약**(멱등키 `IDMP_KEY` UNIQUE·FK 정합 충돌)이며 중복 증강 차단이 아니다 — 되살리는 근거로 오인하지 말 것. 대체 케이스 = TC-AUG-015 | integration | — | AugmentRequestService.java |
| TC-AUG-015 | **모든 상태에서 재요청 허용 (PENDING/ACCEPTED/REJECTED 무관, 2026-08-02 갱신)** | 같은 (대표프레임 × 종류)에 기존 PENDING·ACCEPTED·REJECTED 요청이 이미 존재 | 동일 요청 재전송 | 200 정상 접수, 신규 `LS_DATA_AUG` PENDING 1행 추가 INSERT — 기존 요청 상태와 무관하게 항상 허용된다(2026-07-31 정책 전환: 증강 결과는 요청마다 다르게 생성되므로 원하는 결과가 나올 때까지 재요청하는 것이 정상 동선). 사전 조회 가드·인덱스가 모두 제거되어 오조작(연타) 방어는 **FE 단독 책임** | integration | High | AugmentRequestService.java; LsDataAug.java |
| TC-AUG-016 | **비식별 누락 신고 구간 요청 차단 (신규)** | 대상 영상 `DE_IDNTF_YN='F'` | REVIEWER 요청 | 412 PRECONDITION_FAILED("비식별 재처리 대기 중인 영상은 증강을 요청할 수 없습니다.") + `blockedVideoIds`. 판정은 `DeidentReportGate` 단일 원천 | security | High | AugmentRequestService.java |
| TC-AUG-017 | **단건 계약 위반 거부 (신규)** | — | `videoIds:[1,2]` 또는 `types` 2건 | 400 INVALID_INPUT — DTO `@Size(max=1)` 1차 + 서비스 `requireSingleSelection` fail-closed 2차(초과분 조용한 절단 금지) | unit | High | AugmentRequestRequest.java; AugmentRequestService.java |
| TC-AUG-018 | **V143 선행 정리 — ACCEPTED 2건이면 마이그레이션 중단 (신규)** | 같은 (SRC_SN, AUG_TYPE_CD) ACCEPTED 2건 | Flyway V143 실행 | `RAISE EXCEPTION` 으로 중단(기동 실패), 강등하지 않음. PENDING 중복만 REJECTED 로 종결하고 행·검수이력은 보존. ⚠ **[각주 2026-08-02]** 이 케이스가 방어하던 인덱스(`UK_LS_DATA_AUG_ACTVTN`)는 V153 에서 DROP 됐다 — 현재 정책 검증이 아니라 **과거 마이그레이션(V143) 자체의 가드 로직 회귀 검증용**으로만 존치한다 | integration | High | V143__add_ls_data_aug_active_unique.sql (DO $$ 가드 + WITH ranked UPDATE) |
| TC-AUG-044 | **파생 영상 증강 요청 차단 (2026-08-04 3차 신설 — E-ISSUE-04 ①번 갭 반영)** | 대상 영상 `ORGNL_RAW_SN IS NOT NULL`(증강·해상도 파생본) | REVIEWER 요청 | 400 INVALID_INPUT("파생 영상은 증강 요청 대상이 아닙니다.") + `skippedVideoIds`. 원본 rawSn 은 내려주지 않음(CWE-209/639). **실동작 확인**: rawSn=78(`ORGNL_RAW_SN=26`)로 요청 → 실제로 이 메시지·코드 그대로 응답 | security | High | AugmentRequestService.java |
| TC-AUG-045 | **가드 순서 계약 — 파생 판정이 검수 판정보다 앞선다 (2026-08-04 3차 신설 · 2026-08-19 회차 20 정정)** | 파생 영상이면서 동시에 미검수(APPROVED 아님) | REVIEWER 요청 | 순서는 인가(403/401) → 단건·프롬프트 형식(400) → 파생(400) → **외부 미연동(503)** → 미검수(400 NOT_REVIEWED) → 신고(412) → 프레임 미추출(412). 파생본은 통상 미검수 상태이므로 순서가 바뀌면 "파생이라 거부"가 아니라 "미검수라 거부"라는 엉뚱한 사유가 먼저 뜬다. ⚠ **외부 미연동 게이트는 파생 차단 <뒤>**다 — 파생은 영구 조건(400)이고 미연동은 일시 조건(503)이라, 순서가 뒤바뀌면 파생 영상 요청자가 "일시 장애"로 오인해 재시도를 반복한다 | integration | High | AugmentRequestService.java(request · requireNotDerivative · requireExternalLinked); AugmentRequestContractTest.java(미연동이어도_파생영상은_400이_먼저_난다) |
| TC-AUG-046 | ~~prompt 필드별 검증 4종(자유 문자열 · 공백/50자/보이지 않는 문자)~~ | — | ①필드 공백만(`"  "`) ②50자 초과 ③보이지 않는 문자(ZWSP U+200B)만 | **[폐기 2026-08-27 회차 24]** 이 케이스가 검증하던 계약(생성 조건 5필드 = **자유 문자열**, 길이·공백만 형식 검증)이 v1.3 정합으로 폐기됐다 — 다섯 축은 이제 **허용 코드 enum** 이라 공백·초과길이·보이지 않는 문자가 Jackson 바인딩 단계를 통과할 수 없다(그 경로 자체가 소멸). 대체 케이스 = **TC-AUG-135**(허용 코드 밖 400) · **TC-AUG-138**(자유 지시문 길이 상한) · **TC-AUG-139**(보이지 않는 문자만인 지시문은 「없음」으로 접수 — 선택 필드라 400 이 아니다) | unit | — | AugmentRequestRequest.java(Mtdt) |
| TC-AUG-047 | **PROMPT_CN 원문 보관 = 전송본과 동일 (2026-08-04 3차 신설 · 2026-08-27 회차 24 형태 정정)** | 정상 요청 | REVIEWER, `mtdt` 5항목 + 선택 `prompt` 지정 | `LS_DATA_AUG.PROMPT_CN` 에 저장된 JSON 이 외부로 나간 바디와 **같은 분리 형태**다 — `{"mtdt":{...},"prompt":"..."}`, 지시문이 없으면 그 키 자체가 없다(`@JsonInclude(NON_NULL)` 과 같은 결과). 직렬화 원본을 공유해 전송본↔저장본 분기가 없다. ⚠ 구 기대(5필드가 최상위에 평평한 `{"time":...}`)는 **v1.3 이전 적재분의 모양**이며 마이그레이션하지 않는다 — 두 형태 공존이 정상이다 | unit | Med | AugmentRequestService.java(serializePrompt); AugmentRequestContractTest.java(생성조건이_DB에_분리형태로_보관되어_결과에서_역추적된다 · 자유지시문이_없으면_보관JSON에도_그_키가_없다) |
| TC-AUG-048 | **증강 유형(types)은 생성 조건·이벤트 유형에서 파생되지 않는다 (2026-08-04 3차 신설 · 2026-08-27 회차 24 축 확장)** | — | `types:["NIGHT"]` + `mtdt.season=WINTER` + `evntType=FLOOD` + 지시문에 `../../etc RESL_1080P` | `AUG_TYPE_CD` 는 오직 `types` 필드(닫힌 enum 3종)에서만 결정된다. ⚠ **허용 코드로 닫힌 뒤에도 이 가드는 유효하다** — 코드 공간이 겹치기 때문이다(`Season.WINTER` ↔ `AUG_WINTER`). 유형으로 흘러가면 산출물 경로 순회(CWE-22)·`RESL_` 네임스페이스 침범이 열리므로 구조적으로 분리 | unit | Med | AugmentRequestRequest.java(types); AugmentRequestContractTest.java(증강종류는_생성조건이_아니라_types_enum에서만_결정된다); AugmentRequestControllerTest.java(증강종류는_여전히_enum_3종만_허용되고_임의_문자열은_400 — 시드된 영상에서 판정하므로 enum 이 열리면 200 으로 갈려 RED. 변이 실증 완료) |
| TC-AUG-130 | **외부 미연동이면 요청 접수를 503 으로 거부 (2026-08-19 회차 20 신설 — R8)** | `authoring.augment.external.mode=noop`(dev/stg/prd 기본) | REVIEWER, 정상 요청(APPROVED + 대표프레임 존재) | **503 SERVICE_UNAVAILABLE**("외부 증강 시스템과 연동되지 않아 요청을 접수할 수 없습니다.") + `skippedVideoIds`. `LS_DATA_AUG` PENDING 행 0건 · 외부 위탁 0회. 구 동작(접수 성공 → 위탁·콜백 없음 → 만료 스윕까지 화면에 「진행 중」) 폐기 | integration | High | AugmentRequestService.java(requireExternalLinked); AugmentRequestServiceTest.java(미연동이면_503으로_거부된다); AugmentRequestContractTest.java(외부_연동이_미연동이면_요청_접수를_503으로_거부한다) |
| TC-AUG-131 | **미연동 거부 응답은 배선 상세를 노출하지 않는다 (2026-08-19 회차 20 신설)** | `mode=noop` | REVIEWER 요청 | 오류 메시지에 모드 값(`noop`)·프로퍼티 키(`authoring.augment.external.mode`)·구현 클래스명(`Noop…`)이 **포함되지 않는다**(CWE-209 — 배선 상세는 운영 정보다). 막힌 영상 식별자(`skippedVideoIds`)는 다른 게이트와 동일하게 알린다 | security | High | AugmentRequestService.java(requireExternalLinked); AugmentRequestContractTest.java(미연동_거부_응답은_배선_상세를_노출하지_않는다) |
| TC-AUG-132 | **미연동이어도 인가 판정이 먼저다 (2026-08-19 회차 20 신설)** | `mode=noop` | ①WORKER 토큰 ②`actor=null` | ①403 FORBIDDEN ②401 UNAUTHORIZED — 503 이 아니다. 미인증·비권한 호출자에게 연동 상태를 알려주면 그 자체가 정보 노출이므로 **연동 판정 자체를 수행하지 않는다**(판정 컴포넌트 호출 0회) | security | High | AugmentRequestService.java(request · requireReviewer); AugmentRequestContractTest.java(미연동이어도_비권한_호출자는_인가_실패가_먼저_난다) |
| TC-AUG-133 | **미연동이면 DB 조회로 넘어가지 않는다 (2026-08-19 회차 20 신설)** | `mode=noop` | REVIEWER 요청 | 검수상태 조회·비식별 신고 판정·대표프레임 조회 **각 0회**. 어차피 거부할 요청에 DB 왕복을 태우지 않는다(CWE-770 — 프롬프트 검증을 DB 조회 앞으로 당긴 것과 같은 근거) | unit | Med | AugmentRequestService.java(request); AugmentRequestContractTest.java(미연동이면_DB_조회로_넘어가지_않는다) |
| TC-AUG-134 | **연동(http)이면 기존 접수 경로가 그대로 동작한다 (2026-08-19 회차 20 신설)** | `mode=http`(코드 기본값 · local 프로파일) 또는 미설정 | REVIEWER, 정상 요청 | 200 + `createdCount:1`, `LS_DATA_AUG` PENDING 1행. 신규 게이트가 기존 판정을 바꾸지 않는다. **판정 기본값이 http 여야 하는 이유** — `NoopExternalAugmentClient` 가 `@ConditionalOnProperty(havingValue="noop")` 명시 전용(`matchIfMissing` 없음)이라 미설정=http 클라이언트 배선이며, 기본값을 뒤집으면 "빈은 http 인데 접수는 503" 모순이 된다 | integration | High | AugmentExternalModePolicy.java(isNotLinked); AugmentExternalModePolicyTest.java(미설정이면_연동됨이다_빈_배선_기본값과_같다); ExternalAugmentClientBeanConditionTest.java(httpIsDefault) |
| TC-AUG-135 | **★생성 조건이 허용 코드 밖이면 400 (2026-08-27 회차 24 신설 — v1.3)** | **영상이 APPROVED + 대표프레임 보유**(시드 필수 — 아래 판정축 참조) | `mtdt.terrain="TUNNEL"`·`"BRIDGE"`(구 케이스가 정상값으로 쓰던 값) · `time="MIDNIGHT"` · `weather="폭우"` · `severity="CRITICAL"` · `season="RESL_1080P"` · `terrain="../../etc/passwd"` | 전부 **400 INVALID_INPUT** — Jackson enum 바인딩 단계에서 거부된다. ★**판정축은 「미시드 영상의 400」이 아니라 「시드된 영상에서도 400」이다** — 영상을 시드하지 않으면 허용 코드가 넓어져 바인딩이 통과해도 서비스가 `NOT_REVIEWED`(**역시 400**)를 내 시험이 그대로 GREEN 이 된다. 시드해 두면 방어가 무너지는 순간 **200** 이 되어 RED 다(변이 실증: `Terrain` 에 `TUNNEL` 추가 → `expected:<400> but was:<200>`). `errorCode` 단언은 그 위의 두 번째 그물(다른 사유의 400 과 구분). ⚠ **구 정책 「계약이 허용값을 규정하지 않으므로 우리가 좁히지 않는다」(2026-07-31)는 폐기** — 되살리면 코드 밖 값이 벤더에서 `400 INVALID_PARAMETER` 로 되돌아와 위탁 자체가 실패한다 | integration | High | AugmentPrompts.java(Time·Season·Weather·Terrain·Severity); AugmentRequestControllerTest.java(생성조건이_허용코드_밖이면_400) |
| TC-AUG-136 | **★구 계약 객체형 prompt 는 400 (2026-08-27 회차 24 신설 — v1.3)** | — | `prompt:{"condition":"눈","text":"겨울로"}`(구 v1.1 형태) | **400**. 명세가 *"prompt 값으로 객체를 전달하지 않습니다"* 를 명시하며 v1.3 은 `prompt` 를 문자열로 재정의했다. 외부 요청 바디에서도 `prompt` 가 **문자열이거나 아예 없어야** 한다 | integration | High | GenAiJobSubmitRequest.java(prompt); AugmentRequestControllerTest.java(구계약_객체형_prompt는_400); HttpExternalAugmentClientTest.java(요청_바디가_명세서_v1_3_스키마와_일치함) |
| TC-AUG-137 | **★위탁 바디 = mtdt 최상위 + prompt 문자열 (2026-08-27 회차 24 신설 — v1.3)** | 정상 요청 | 실제 전송 바디 검사 | `mtdt` 가 **최상위 객체**(키 순서 time→season→weather→terrain→severity)이고 `prompt` 는 **문자열**이다. 지시문·침수 세부 유형이 없으면 그 키 **자체가 나가지 않는다**(`@JsonInclude(NON_NULL)`). 구 고정문구 키(`style`/`instruction`)와 구 최상위 `condition` 은 **부재**. 외부 발급값(`job_id`)은 우리가 보내지 않는다 | integration | High | GenAiJobSubmitRequest.java; HttpExternalAugmentClient.java(toRequestBody); HttpExternalAugmentClientTest.java(요청_바디가_명세서_v1_3_스키마와_일치함 · 자유지시문과_침수세부유형은_각자_최상위_필드로_나간다) |
| TC-AUG-138 | **자유 지시문 길이 상한 1000자 (2026-08-27 회차 24 신설)** | — | ①1001자 ②정확히 1000자 | ①**400 INVALID_INPUT** — 메시지에 **입력 원문을 되돌려주지 않는다**(CWE-359 반사 노출 차단). 상한이 없으면 저장 컬럼(`PROMPT_CN` VARCHAR(4000))을 넘겨 적재 500 이 되고 무제한 입력이 외부로 중계된다(CWE-770) ②**200**(off-by-one 으로 정상 입력을 막지 않는다) | unit | High | AugmentPrompts.java(MAX_PROMPT_LENGTH); AugmentRequestService.java(normalizePromptText); AugmentRequestContractTest.java(자유지시문_길이_상한_초과시_400 · 자유지시문_길이_상한_경계값은_허용된다) |
| TC-AUG-139 | **자유 지시문은 선택이라 비어도 400 이 아니다 (2026-08-27 회차 24 신설)** | APPROVED + 대표프레임 | ①`prompt` 미지정 ②`prompt:"   "` ③보이지 않는 문자만(NBSP·ZWSP·BOM·WJ) | 셋 다 **200** 이고 지시문은 `null` 로 남아 외부 바디·보관 JSON 에서 키가 생략된다. ⚠ 구 계약에서는 이 입력이 **400** 이었다 — 그때는 다섯 축이 자유 문자열이라 빈 조건이 벤더로 나가면 결과가 비결정적이 됐기 때문이다. **태도가 갈리는 근거는 필수/선택 차이**이지 검증을 느슨하게 한 것이 아니다 | integration | Med | AugmentRequestService.java(normalizePromptText); AugmentRequestControllerTest.java(보이지_않는_문자만_입력한_자유지시문은_지시문_없음으로_접수된다 · 보이지_않는_문자가_섞여도_내용이_있으면_200) |
| TC-AUG-140 | **자유 지시문 제어문자 정규화 (2026-08-27 회차 24 — 대상이 5필드에서 지시문으로 이동)** | 정상 요청 | 지시문에 개행·CR·탭·NUL 혼입 | `VisibleTextNormalizer` 가 걷어내 저장·전송본에 원문자·JSON 이스케이프 **양쪽 다** 남지 않는다. 이스케이프 축까지 보지 않으면 Jackson 이 escape 로 바꿔 담은 것을 「제거됐다」고 오판한다. 로그 축은 「요청 원문을 아예 로그에 싣지 않는다」로 별도 차단(두 겹) | integration | High | AugmentRequestService.java(normalizePromptText); AugmentRequestServiceTest.java(개행이_포함된_자유지시문은_저장·전송본에_원본_개행이_남지_않는다) |
| TC-AUG-141 | **★이벤트 유형은 요청 본문에서 오고 관제 코드로 변환하지 않는다 (2026-08-27 회차 24 신설 — v1.3)** | 관제 이벤트 코드가 `EVT` 인 영상 | `evntType:"WILDFIRE"` 로 요청 | 위탁 커맨드·외부 바디의 `evnt_type` 이 **`WILDFIRE`** 다. ⚠ **구 동작 폐기** — 위탁 시점에 `LS_DATA_RAW.EVNT_TYPE_CD` 를 조회해 싣고 미상이면 `ETC` 로 대체했는데, 계약 허용값은 `FLOOD`·`WILDFIRE` 둘뿐이라 그 값들은 벤더가 전부 거부한다. 위탁 서비스는 이 값을 얻으려 **영상을 다시 읽지 않는다**(다시 읽으면 두 벌이 되어 갈라진다) | integration | High | AugmentJobSubmitService.java(submitChunkAsync); AugmentRequestedItemEvent.java(evntType); AugmentJobSubmitServiceTest.java(이벤트유형은_요청에서_받은_값을_그대로_중계한다); AugmentRequestContractTest.java(이벤트유형은_요청본문에서_오고_관제코드로_대체되지_않는다) |
| TC-AUG-142 | **이벤트 유형 필수 + 허용 코드 (2026-08-27 회차 24 신설)** | ②는 **영상이 APPROVED + 대표프레임 보유**(시드 필수 — TC-AUG-135 와 같은 이유) | ①`evntType` 미지정 ②`"ETC"`·`"EV01000101"`·`"FIRE"`·`"flood"` | ①**400 INVALID_INPUT** ②전부 **400 INVALID_INPUT**(enum 바인딩 거부). ★②의 판정축은 TC-AUG-135 와 동일하다 — 시드된 영상이라야 `ETC` 가 허용값이 되는 순간 **200** 으로 갈려 RED 다(변이 실증: `EventType` 에 `ETC` 추가 → `expected:<400> but was:<200>`). 관제 이벤트 코드는 계약값이 아니다 — 두 분류 축이 다른 체계라 서버가 변환하면 그건 추정이고, 추정한 값이 그대로 위탁에 실린다 | integration | High | GenAiContract.java(EventType); AugmentRequestControllerTest.java(이벤트유형이_없으면_400 · 이벤트유형이_허용코드_밖이면_400) |
| TC-AUG-143 | **침수 세부 유형은 침수일 때만 (2026-08-27 회차 24 신설)** | APPROVED + 대표프레임 | ①`evntType=WILDFIRE` + `evntSubtype=ROAD_FLOOD` ②`evntType=FLOOD` + `evntSubtype=UNDERPASS_FLOOD` | ①**400 INVALID_INPUT** — 계약에 산불 세부 코드가 정의돼 있지 않아 함께 보내면 벤더가 400 이다. **조용히 떨어뜨려 보내지 않는 대신 거부한다**(무시하면 요청자는 자기가 고른 값이 사라진 줄 모른다). DTO `@AssertTrue` + 서비스 fail-closed 2겹 ②**200**, `evnt_subtype` 이 그대로 중계된다(과잉 차단 회귀 가드) | integration | High | AugmentRequestRequest.java(isEvntSubtypeAllowed); AugmentRequestService.java(requireSubtypeAllowed); AugmentRequestControllerTest.java(침수_세부유형을_산불과_함께_보내면_400 · 침수_세부유형은_침수와_함께면_200) |

## E-2. 증강 검수 (AugmentReviewService)

| ID | 케이스명 | 전제 | 입력/조건 | 기대결과 | 계층 | 우선 | 근거(파일) |
|----|---------|------|----------|---------|------|:--:|----------------|
| TC-AUG-020 | 증강 결과 승인 PENDING→ACCEPTED | 검수행(RVW) PENDING **이고** 생성 결과(`AUG_PROC_STTS_CD`) 이미 ACCEPTED(파생 실재, `isGenerationSucceeded()`) | REVIEWER accept | 200, ACCEPTED, `LS_DATA_AUG_RVW` accept, syncDecision best-effort | integration | High | AugmentReviewService.java |
| TC-AUG-021 | 이미 처리된 결과 재승인 차단 | 검수행(RVW) ACCEPTED/REJECTED | accept | 409("이미 처리된 증강 검수입니다. status=…") — ★기전 정정(2026-08-03): 축 분리(2026-07-31)로 `LsDataAug.applyReviewStatus` 는 더 이상 accept/reject 에서 호출되지 않는다. 재결정 차단은 `LsDataAugRvw.ensurePending()`(검수 행 자신의 가드)이 담당 | unit | High | LsDataAugRvw.java |
| TC-AUG-022 | 반려 사유 누락 거부 | 검수행 PENDING | reject reason=null/blank | 400("반려 사유는 필수입니다.") — 서비스 1차(요청 진입 즉시) + `LsDataAugRvw.reject` 2차 | unit | High | AugmentReviewService.java |
| TC-AUG-023 | 반려 정상 PENDING→REJECTED | 검수행 PENDING, 생성 결과 ACCEPTED | REVIEWER reject+사유 | 200, REJECTED, RVW reject | integration | High | AugmentReviewService.java |
| TC-AUG-024 | **해상도 파생 accept 차단** | `AUG_TYPE_CD=RESL_720P` | accept | 400("해상도 파생 결과는 검수 대상이 아닙니다.") | unit | High | AugmentReviewService.java |
| TC-AUG-025 | 해상도 파생 reject 차단 | RESL_ 행 | reject | 400(`loadOrThrow` 게이트 동일) | unit | High | AugmentReviewService.java |
| TC-AUG-026 | 존재하지 않는 증강행 검수 | 미존재 dataAugSn | accept | 404 | unit | Med | AugmentReviewService.java |
| TC-AUG-027 | 외부 sync 실패 best-effort | `syncDecision` throw | accept | 본 tx 영향 없음(warn), 200. ⚠ 현 활성 구현 `HttpExternalAugmentClient.syncDecision` 은 외부 계약 미정의라 **no-op(로그 후 true)** — throw 분기는 프로덕션 도달 불가(단위 스텁으로만 검증 가능) | unit | Med | AugmentReviewService.java; HttpExternalAugmentClient.java |
| TC-AUG-028 | 검수 WORKER/미인증 차단 | WORKER·null | accept/reject | 403/401 | security | High | AugmentReviewService.java |
| TC-AUG-029 | 잡카드 전체 페이징 — SRC_SN 그룹 최신순 | 다수 행 | `listAll(page,size)` | Page, 영상단위 그룹핑, 일괄 조회 4종(row/rawSn/cctv/review)로 N+1 없음 | integration | Med | AugmentReviewService.java |
| TC-AUG-030 | **types/resolutionTypes 분리 노출** | WINTER+RESL_720P 혼재 | 잡카드 | `types=[WINTER]`, `resolutionTypes=[RESL_720P]`(FE accept/reject 숨김) | unit | High | AugmentReviewService.java |
| TC-AUG-031 | AUG_ORDER 정렬 | 여러 종류 | 잡카드 | WINTER→NIGHT→RAIN→RESOLUTION→RESL_1080P/720P/480P, 미정의 99 | unit | Low | AugmentReviewService.java |
| TC-AUG-032 | **aggregateStatus — dead-letter→FAILED (실경로 도달)** | `DEAD_LETTER_AT` 존재 | 집계 | FAILED. E-ISSUE-06 해소 — `markDeadLetter()` 프로덕션 호출자 2곳 확보(`AugmentResultService.markProcessingFailure` 실패 인계 단일 깔때기, `AugmentExtractPersist.markAugProcessingFailed` async 추출 실패) | integration | High | AugmentReviewService.java; AugmentResultService.java; AugmentExtractPersist.java |
| TC-AUG-033 | aggregateStatus — 전부 terminal→COMPLETED | 전 행 ACCEPTED/REJECTED, dead-letter 없음 | 집계 | COMPLETED, `completedAt=max(RVW_DT)` | unit | Med | AugmentReviewService.java |
| TC-AUG-034 | aggregateStatus — 일부 종료→IN_PROGRESS / 전부 PENDING→REQUESTED | 혼합 / 전 PENDING | 집계 | IN_PROGRESS / REQUESTED | unit | Med | AugmentReviewService.java |
| TC-AUG-035 | **해상도 in-flight 집계 정합** | RESL 예약 PENDING만 | 집계 | COMPLETED 아님(`markResolutionGenerated` 로 ACCEPTED 전이된 뒤에만 terminal) | integration | High | AugmentReviewService.java; LsDataAug.java |
| TC-AUG-036 | **aggregateResultStatus 매핑 — FAILED 분기 도달 가능** | dead-letter 행 존재 | `/{jobId}/result` | COMPLETED / **FAILED** / PROCESSING. 구 "FAILED 도달 불가"(E-ISSUE-06)는 TC-AUG-032 배선으로 해소 | unit | Med | AugmentReviewService.java |
| TC-AUG-037 | aggregateResultStatus SRC_SN 폴백/무데이터 | RAW_SN 매핑 없음 / 집계 0 | result | `findBySrcSnIn` 폴백, 전무 시 PROCESSING | unit | Low | AugmentReviewService.java |
| TC-AUG-038 | cctvName 비-옵셔널 폴백 (정정 2026-08-13 — 구 `"(이름 없음)"` 폐기) | 매핑/시드 부재 또는 blank | 잡카드 · `/{jobId}/result` | **`영상 #{videoId}`**(항상 non-null). 판정 단일 원천은 `CctvDisplayNamePolicy` 이며 잡카드·결과조회 두 화면이 같은 표기를 쓴다. ⚠ 구 상수 `"(이름 없음)"` 은 **여러 영상이 전부 같은 문구**가 되어 식별이 불가능했고, 같은 영상이 작업목록에서는 `영상 #N` 으로 보여 표기가 갈렸다 | unit | Low | video/dto/CctvDisplayNamePolicy.java(`resolve`) · AugmentReviewService.java(`toJob`) · AugmentResultViewService.java(`resolveCctvName`) · AugmentCctvDisplayNameTest.java |
| TC-AUG-039 | findBySource(srcSn) 필터 | 매핑 row 없음 | `?srcSn=` | 빈 Page | unit | Low | AugmentReviewService.java |
| TC-AUG-040 | list size 한도 초과 | `size>100`, srcSn 미지정 | GET /v1/augments | 400("size 한도 초과 (max=100)"). ⚠ `srcSn` 지정 시 분기가 앞서 size 검증 미적용 | unit | Med | AugmentController.java |
| TC-AUG-041 | **검수 이력 rawSn 역해석 실패 = 409 (신규)** | `LS_DATA_AUG.SRC_SN` 이 삭제된 프레임을 가리킴 | accept/reject | 409 CONFLICT("증강 결과의 원본 영상 정보를 확인할 수 없어…"). 구 하드코딩 `0L`(존재하지 않는 영상 참조) 제거 — V146 FK 이후 500 이 되던 경로 | integration | High | AugmentReviewService.java; V146__add_ls_data_raw_child_fk.sql |
| TC-AUG-042 | **검수행 선재 재사용 (신규)** | 이미 `LS_DATA_AUG_RVW` 존재 | accept | 기존 행 재사용(역해석 미수행) — `findLatestByDataAugSn` 우선 | unit | Med | AugmentReviewService.java |
| TC-AUG-043 | **★결정 전 결과물 실재 필수 게이트 (신규, 2026-08-03 발견)** | 증강 요청 직후 — 생성 결과(`AUG_PROC_STTS_CD`) 아직 PENDING(파생 미확정) | accept/reject | 409 CONFLICT — 생성 진행 중이면 "증강 결과가 아직 생성되지 않았습니다…", 실패/취소로 종결됐으면 "생성에 실패했거나 취소된 증강은 사용 여부 결정 대상이 아닙니다…"로 사유 구분. 판정 단일원천은 `LsDataAug.isGenerationSucceeded()`(2026-07-31 DEV_FIX HIGH) — 축 분리로 사라진 "생성 전 결정 불가" 불변식을 명시 가드로 복원, 없으면 사람이 보지 않은 결과물이 등재 게이트를 통과한다 | integration | High | AugmentReviewService.java; LsDataAug.java |

## E-2B. 증강 결과 조회 응답 — 요청일시 `requestedAt` (AugmentResultViewService / AugmentReviewService.resolveRequestedAt) — 2026-08-09 신설

> **구 동작 → 폐기**: 결과 응답에 잡 단위 시각이 **하나도 없었다**. 화면 작업 요약은 요청 시각 칸을 요구하는데 공급원이 없어 그 칸을 만들 수 없었다(잡·항목 응답 모두 부재).
> **필드명은 목록 API 가 이미 쓰는 `requestedAt` 을 그대로 쓴다** — 같은 뜻에 다른 이름을 만들면 두 화면의 계약이 갈린다.
> **산출 축은 목록과 같은 `MIN(REG_DT)`** 이고 판정은 `AugmentReviewService.resolveRequestedAt` **한 곳**에서만 한다. 산출식을 결과 조회 쪽에 복제하면 두 번째 진실원이 생겨 같은 잡이 화면마다 다른 시각을 표시한다.
> **`decidedAt`(채택·반려 결정 시각)과 축이 다르다** — "언제 만들어 달라고 했는가" 와 "언제 결정했는가" 라 서로 대체할 수 없다.
> **반환 지점이 셋이라는 것이 이 절의 존재 이유다** — 잡 단위 값을 페이징 경로마다 손으로 채우면 한 곳이 빠지고, 그 결함은 **빈 페이지에서만** 드러나 늦게 발견된다.

| ID | 케이스명 | 전제 | 입력/조건 | 기대결과 | 계층 | 우선 | 근거(파일) |
|----|---------|------|----------|---------|------|:--:|----------------|
| TC-AUG-122 | **정상 경로에 그룹 최소 등록일시가 요청일시로 실린다** (신규 · 핵심) | 같은 영상에 증강 행 2건(반복 요청 허용 정책) — `REG_DT` 가 서로 다름 | `GET /v1/augments/{jobId}/result` | `requestedAt` = **더 이른** `REG_DT`(그룹 `MIN`). 최신값·현재 시각이 아니다 | unit | High | AugmentResultViewService.java(`result`) · AugmentReviewService.java(`resolveRequestedAt`) · AugmentResultRequestedAtTest.java(`정상_경로에_그룹_최소_등록일시가_요청일시로_실린다`) |
| TC-AUG-123 | 요청일시 추가로 **기존 응답 필드는 바뀌지 않는다** (신규 · 하위호환) | 외부 FE 팀도 소비하는 계약면 | 정상 조회 | `jobId`·`status`·`message`·`page`·`size`·`itemPage`·`itemSize`·`totalElements`·`totalPages` 의 이름·타입·의미·기본값 불변(추가만) | unit | High | AugmentResultResponse.java(`AugmentResultResponse`) · AugmentResultRequestedAtTest.java(`요청일시_추가로_기존_응답_필드는_바뀌지_않는다`) |
| TC-AUG-124 | **증강 행이 0건이면 `null`** (신규 · 반환 지점 ① 항목 0건 조기 반환) | `findByOriginalRawSn` 결과 0건 | 정상 조회 | `requestedAt=null` · `results=[]` · `totalElements=0`. 잡 진입 시각·현재 시각으로 **지어내지 않는다** | unit | High | AugmentResultViewService.java(`result` 항목 0건 조기 반환) · AugmentResultRequestedAtTest.java(`증강_행이_0건이면_요청일시는_null_이다`) |
| TC-AUG-125 | **항목 페이지 범위 밖 조기 반환에도 요청일시가 실린다** (신규 · 반환 지점 ② · 결함 차단) | 항목 1건 | `itemPage=5` | `results=[]` 이지만 `requestedAt` 은 정상 경로와 **같은 값**. 잡 단위 사실이라 페이징과 무관하다 — 이 지점만 빠뜨리면 "빈 페이지에서만 값이 사라진다" | unit | High | AugmentResultViewService.java(`result` 페이지 범위 밖 조기 반환 · `Paging.response`) · AugmentResultRequestedAtTest.java(`항목_페이지_범위_밖_조기반환에도_요청일시가_실린다`) |
| TC-AUG-126 | 등록일시가 전부 비면 `null` (신규 · 경계) | 증강 행은 있으나 `REG_DT` 가 전부 null(레거시 형상) | 정상 조회 | `requestedAt=null` · 항목은 정상 반환. 최소값을 만들 근거가 없으면 값을 만들지 않는다 | unit | Med | AugmentReviewService.java(`resolveRequestedAt`) · AugmentResultRequestedAtTest.java(`등록일시가_모두_비면_요청일시는_null_이다` · `요청일시_산출은_목록과_같은_최소_등록일시_규칙이다`) |
| TC-AUG-127 | 화면 작업 요약이 응답의 요청일시를 표시한다 (신규 · **FE**) | 응답 `requestedAt` 존재 | 결과 화면 진입 | 요약에 `요청일시` 라벨 + 포맷된 값 표시(`formatDateTime`). 결정 시각(`decidedAt`)을 대신 집어오지 않는다 | component | High | pages/AugmentResultPage.tsx(`AugmentResultPage` 작업 요약 dl) · features/augment/__tests__/AugmentResultPage.requestedAt.test.tsx(`응답의_요청일시를_요약에_표시한다`) |
| TC-AUG-128 | 요청일시가 `null` 이면 `-` 를 보인다 (신규 · **FE** · 경계) | 응답 `requestedAt=null` | 결과 화면 | `-` 표시. 값이 없다는 사실을 그대로 말한다(추정 표기 금지) | component | High | pages/AugmentResultPage.tsx(`AugmentResultPage`) · features/augment/__tests__/AugmentResultPage.requestedAt.test.tsx(`요청일시가_null_이면_대시를_보인다`) |
| TC-AUG-129 | 필드가 없는 구 응답에서도 `-` 를 보인다 (신규 · **FE** · 하위호환) | 이 필드를 모르는 구 서버 응답(`undefined`) | 결과 화면 | `-` 표시 · 다른 시각 값(결정 시각 등)이 새어 들어오지 않는다 | component | Med | features/augment/types.ts(`AugmentResultPage` 인터페이스 — optional/nullable) · features/augment/__tests__/AugmentResultPage.requestedAt.test.tsx(`요청일시_필드가_없는_구_응답에서도_대시를_보인다`) |

## E-3. 증강 결과 웹훅 — `POST /v1/genai/callback` (GenAiCallbackController / GenAiCallbackService / AugmentJobRollup / AugmentResultService)

> **계약 전면 교체**: 구 `POST /v1/aug/callback` + `AugmentResultRequest`(증강 1건 = 콜백 1건, HMAC 서명)는 **제거**됐다. 신 계약은 「생성형 AI API 연동명세서 v1.1」 정합 — **job 단위** 웹훅(무서명 3계층 방어), 증강 1건은 100장 상한 때문에 여러 job 으로 분할 위탁되므로 **전 job 종결 후에만** 롤업으로 증강 1건을 확정한다.

| ID | 케이스명 | 전제 | 입력/조건 | 기대결과 | 계층 | 우선 | 근거(파일) |
|----|---------|------|----------|---------|------|:--:|----------------|
| TC-AUG-050 | **전 job SUCCEEDED 롤업 → 신규 증강영상 생성** | job 전량 종결·전부 SUCCEEDED, 부모 `hasDeidentArtifact()` true, 프레임 존재 | 마지막 job `status=SUCCEEDED` | 200 `{applied:true, requestId}`, aug ACCEPTED, 새 `LS_DATA_RAW`(`ORGNL_RAW_SN`=부모, `DATA_STTS_CD=PENDING`, `DE_IDNTF_YN='N'`), `RAW_FILE_PATH_NM`=**자기 비식별 사본 경로**, AFTER_COMMIT `AsyncAugmentFrameRunner` 기동 | integration | High | GenAiCallbackService.java; AugmentJobRollup.java; AugmentResultService.java |
| TC-AUG-051 | 실패 롤업 REJECTED·영상 미생성 + dead-letter | job 중 1건 이상 FAILED | 마지막 job 콜백 | aug REJECTED + `RTRY_NMTM`+1 + `DEAD_LETTER_AT` 기록, `createAugmentedVideo` 미호출. 부분 실패 = 전체 실패(fail-closed) | integration | High | AugmentJobRollup.java; AugmentResultService.java |
| TC-AUG-052 | **재전송 멱등(1차 앵커) non-PENDING skip** | aug 이미 ACCEPTED | 동일 롤업 재진입 | `DUPLICATE` → 200 `{applied:false}`, 중복 영상 미생성 | integration | High | AugmentResultService.java; AugmentApplyResult.java |
| TC-AUG-053 | **otsd_job_id 선점 — 재수신 200 / 타 증강 409** | ①자기 자신 보유 ②다른 증강 보유 | 롤업 | ① 1차 앵커/선점검사로 흡수 → 200 `applied:false` ② **쓰기 이전** 선점검사에서 409(트랜잭션 오염 없음). E-ISSUE-05(PG 25P02 500) 해소 — 위반 후 소유자 재조회는 `AugmentJobIdOwnerLookup`(REQUIRES_NEW)에서만 | integration | High | AugmentResultService.java |
| TC-AUG-054 | ~~augType 불일치 차단~~ | row=WINTER, req=RAIN | 콜백 | **[폐기 2026-07-30]** augType 대조 제거 — 새 계약은 `request_id → LS_DATA_AUG_JOB → dataAugSn` 으로 대상을 역산하므로 외부가 종류를 잘못 실어 다른 행에 인계할 경로 자체가 없다 → 대체 오배송 방어는 TC-AUG-067(job_id mismatch) | unit | — | AugmentResultService.java (주석) |
| TC-AUG-055 | **미발급 request_id = 401 / job 대비 aug 행 부재 = 404** | ①`LS_DATA_AUG_JOB.IDMP_KEY` 미존재 ②job 은 있으나 aug 행 없음 | 콜백 | ① 401 UNAUTHORIZED("발급되지 않은 request_id 입니다.") ② 404. 401 은 필터 rate-limit 에 집계돼 탐색 공격 차단. ⚠ **②는 라이브 도달 불가 — 방어 코드로만 존치**(2026-08-03 3차 확인, 1차 E-ISSUE-21 반영 누락분): `fk_ldaj_data_aug ... ON DELETE CASCADE` 라 aug 행이 지워지면 job 행도 함께 사라져 "job 은 있는데 aug 행 없음" 상태가 스키마상 성립하지 않는다. 검증 대상은 ①만 | security | High | GenAiCallbackService.java; V(FK) fk_ldaj_data_aug ON DELETE CASCADE |
| TC-AUG-056 | ~~부모 비식별 미완('F') PII 게이트~~ | 부모 `DE_IDNTF_YN='F'` | SUCCESS 롤업 | **[폐기 2026-07-30]** ★정책 반전 — "파생영상은 비식별 신고 체계 바깥"(2026-07-29 확정). 신고(`'F'`) 구간에도 **파생을 생성**한다. 신고가 막는 것은 외부 위탁뿐(TC-AUG-016 / TC-AUG-072) → 대체 케이스는 TC-AUG-064 | security | — | AugmentResultService.java; LsDataRaw.hasDeidentArtifact() |
| TC-AUG-057 | **부모 프레임 0건 = 실패 확정** | 부모 프레임 없음 | SUCCESS 롤업 | `ParentGate.FAIL` → aug **REJECTED + dead-letter**(구 "생성 보류" 폐기 — 재개 트리거가 없어 PENDING 영구 고착이 되므로). 영상 0건. ⚠ **라이브 도달 불가 — mock 단위테스트로만 검증 가능**(2026-08-03 3차 확인): 게이트가 부모를 `srcRepository.findById(aug.getSrcSn()).getRawSn()` 으로 역산하므로 origin 프레임이 존재하는 한 `findByRawSnOrderByFrameNoAsc(parentRawSn)` 은 최소 그 행 1건을 반환한다(필터 없는 파생쿼리). 커버는 `AugmentResultServiceTest.부모_프레임이_없으면_실패로_확정되고_영상과_프레임러너_미트리거` | unit | High | AugmentResultService.java; LsDataSrcRepository.java |
| TC-AUG-058 | ~~rawFilePathNm SSRF 차단~~ | 내부망/metadata URL | 콜백 | **[폐기 2026-07-30]** 신 콜백 페이로드(`GenAiCallbackRequest`)에 `raw_file_path_nm` 필드가 없고 `AugmentOutcome.succeeded/failed` 가 항상 null 을 넣으므로 웹훅 경로로는 도달 불가. `validateFilePath`(SSRF+allowlist)는 다층 방어로 코드에 존치 → 대체는 TC-AUG-069(output_file_path 허용루트) | security | — | AugmentOutcome.java; AugmentResultService.java |
| TC-AUG-059 | ~~augTypeCd 화이트리스트~~ | WINTER/NIGHT/RAIN 외 | 콜백 | **[폐기 2026-07-30]** `AugmentResultRequest` 자체가 제거됨(`aug_type_cd` 필드 부재) | unit | — | GenAiCallbackRequest.java |
| TC-AUG-060 | **status 화이트리스트** | `status` 값 | 콜백 | `^(RUNNING\|SUCCEEDED\|FAILED)$` 외 400. `CANCELED` 는 웹훅으로 발신되지 않는다(취소는 만료 스윕이 회수 — TC-AUG-073) | unit | Med | GenAiCallbackRequest.java |
| TC-AUG-061 | **request_id / job_id 패턴·길이** | 특수문자·초과 | 콜백 | `request_id` `^[A-Za-z0-9_-]+$` ≤128, `job_id` `^(?!\.+$)[A-Za-z0-9_.:-]+$`(점(`.`)만으로 이뤄진 세그먼트 차단 추가, CWE-22) ≤200 위반 시 400 | security | Med | GenAiCallbackRequest.java |
| TC-AUG-062 | ~~dataAugSn null·rawFilePathNm>1000~~ | 필수 누락/초과 | 콜백 | **[폐기 2026-07-30]** 두 필드 모두 신 페이로드에 없음 → 대체 필수값 검증은 TC-AUG-076 | unit | — | GenAiCallbackRequest.java |
| TC-AUG-063 | 동시 콜백 직렬화(FOR UPDATE) | 마지막 job 콜백 동시 2건 | 콜백 | ①`GenAiCallbackService` 가 **job 갱신 이전에** `findByDataAugSnForUpdate` 로 aug 행 잠금(순서 뒤집으면 롤업 통째 유실) ②`AugmentResultService.handle` 이 같은 행 잠금 + PENDING 앵커로 재방어 → 증강영상 정확히 1건 | integration | High | GenAiCallbackService.java; AugmentResultService.java |
| TC-AUG-064 | **부모 비식별 미완('N') = 실패 확정 (신규)** | 부모 `DE_IDNTF_YN='N'`/null | SUCCESS 롤업 | `ParentGate.FAIL` → aug REJECTED + dead-letter, 영상 0건. 판정은 `LsDataRaw.hasDeidentArtifact()`(`'Y'`\|`'F'` 통과) 단일 헬퍼 — 해상도 파생 경로와 동일 축 | security | High | AugmentResultService.java |
| TC-AUG-065 | **IP allowlist 미설정 = 전면 차단 (신규)** | `webhook.genai.allowed-ip-cidrs` 미설정/`none` | 콜백 | 403 (VLM allowlist 와 반대로 **fail-closed**) | security | High | GenAiWebhookIpAllowlist.java |
| TC-AUG-066 | **위탁↔수신 배선 짝 기동 가드 (신규)** | `authoring.augment.external.mode=http` + allowlist 미설정 | 부팅 | `IllegalStateException` 으로 **기동 실패**. 위탁만 열리고 콜백이 전건 403 이면 aug 가 PENDING 영구 고착되므로 경고가 아니라 차단 | integration | High | GenAiIntegrationWiringGuard.java |
| TC-AUG-067 | **job_id 오배송 차단 (신규)** | job 이 202 로 받아 둔 `externalJobId` 와 콜백 `job_id` 상이 | 콜백 | 409 CONFLICT("job_id 가 일치하지 않습니다."), 상태 미변경 | security | High | GenAiCallbackService.java |
| TC-AUG-068 | **RUNNING 진행 갱신 = 롤업 없음 (신규)** | 비종결 job | `status=RUNNING` | job `JOB_STTS_CD=RUNNING` 갱신, 200 `applied:true`, 결과 처리·롤업 미수행 | unit | Med | GenAiCallbackService.java |
| TC-AUG-069 | **output_file_path 허용 읽기루트 밖 = 400·상태 미변경 (신규)** | `results[].output_file_path` 가 `raw-mount-roots ∪ external-read-roots` 밖 | SUCCEEDED 콜백 | 400("output_file_path 가 허용된 저장 경로가 아닙니다."), **job 상태 미변경**(재전송 여지 보존), 메트릭 `augment.callback.rejected{reason=output_path}` + WARN. 경로 원문 미노출. ★기전 정정(2026-08-03): 검증·거부 로직은 `GenAiCallbackService` 가 아니라 웹훅/회수 공용 `AugmentJobSuccessApplier.verifyOutputPaths`(S15 — 결과조회 회수 경로와 단일 원천 통합)로 이동했다 | security | High | AugmentJobSuccessApplier.java; VideoArtifactRootResolver.java |
| TC-AUG-070 | **읽기 루트 기본 빈값 = 쓰기축과 동일 fail-closed (신규)** | `authoring.storage.external-read-roots` 미설정 | 콜백 산출 경로 검증 | 읽기 허용 루트 = 쓰기 allowlist 만 → 벤더 전용 경로는 전부 400. 쓰기 base 를 넓히지 않고 축을 분리한다 | security | High | VideoArtifactRootResolver.java |
| TC-AUG-071 | **위탁 건수 ↔ 수신 건수 불일치 = job FAILED (신규)** | `LS_DATA_AUG_JOB_FILE` N건 vs `results` M건(N≠M) | SUCCEEDED 콜백 | 짝짓기 불성립 → job `FAILED(ERR_CD=RESULT_COUNT_MISMATCH)`, 롤업 부분실패 규칙으로 증강 1건도 실패. ★기전 정정(2026-08-03): `GenAiCallbackService` 가 아니라 `AugmentJobSuccessApplier.applySucceeded` 가 건수 대조를 수행한다 | integration | High | AugmentJobSuccessApplier.java; LsDataAugJob.java |
| TC-AUG-072 | **SUCCEEDED 인데 results 없음 = 400 (신규)** | `results` null/빈 배열 | SUCCEEDED 콜백 | 400("SUCCEEDED 결과에는 results 가 필요합니다."), 메트릭 `reason=missing_results`. 빈 증강본 확정 차단. ★기전 정정(2026-08-03): 검증은 `AugmentJobSuccessApplier.verifyOutputPaths` 진입부(웹훅/회수 공용) | unit | High | AugmentJobSuccessApplier.java |
| TC-AUG-073 | **롤업 보류 = applied:false (신규)** | 2청크 중 1청크만 SUCCEEDED | 콜백 | `DEFERRED` → 200 `{applied:false}`. 구 "무조건 `applied:true`" 폐기 — 외부가 인계 완료로 오해하던 것 차단(job 상태 갱신은 그대로 커밋) | integration | High | GenAiCallbackService.java; AugmentApplyResult.java |
| TC-AUG-074 | **비종결 job 만료 스윕 회수 (신규)** | job 이 무갱신 경과 임계(기본 360분, 하한 5분) 초과 | 스윕 tick | 조건부 UPDATE `claimExpired` 로 1행 클레임 → `FAILED(ERR_CD=EXPIRED)` → 전 job 종결 시 롤업(실패 확정). 클레임 0행이면 no-op(2노드 중복 회수 방지). 메트릭 `augment.job.expired` 는 **커밋 이후**에만 증가 | integration | High | AugmentJobExpirySweeper.java; AugmentJobExpiryTxService.java |
| TC-AUG-075 | **job 0건 고아 PENDING 증강 회수 (신규)** | 위탁 전 롤업이 예외로 끝나 job·콜백 0건, PENDING 장기 잔존 | 스윕 tick | `expireOrphanPending` → REJECTED + dead-letter. ★신고 구간(`'F'`)도 **회수 대상**(구 제외 술어 제거 — 깨울 주체가 없어 PENDING 영구 고착) | integration | High | AugmentJobExpirySweeper.java; AugmentJobExpiryTxService.java |
| TC-AUG-076 | **필수 필드 누락 400 (신규)** | `request_id`/`job_id`/`status` blank | 콜백 | 400 `@NotBlank`. `results` 100건 초과도 400(계약 상한) | unit | Med | GenAiCallbackRequest.java |
| TC-AUG-077 | **미지 필드 무시 (신규)** | 외부가 신규 필드 추가 | 콜백 | `@JsonIgnoreProperties(ignoreUnknown=true)` 로 수신 성공(계약 확장 내성) — 최상위 레코드와 내부 `ResultItem` 레코드 양쪽에 적용 | unit | Low | GenAiCallbackRequest.java |
| TC-AUG-078 | **강등된 PENDING 의 성공 콜백 폐기 관측** | aug 가 REJECTED(V143 정리·위탁 0건 롤업·반려)인데 성공 결과 도착 | 롤업 | 200 `applied:false` + **WARN** `success result discarded — aug already terminal(REJECTED)`. "요청했는데 파생영상이 없다"의 유일 단서 | unit | Med | AugmentResultService.java |

## E-3B. 증강 외부 위탁 (AugmentJobSubmitService / HttpExternalAugmentClient / AugmentJobRecorder)

> Phase 7-A1 신설 영역. `LS_DATA_AUG_JOB`(V140) · `LS_DATA_AUG_JOB_FILE`(V141).

| ID | 케이스명 | 전제 | 입력/조건 | 기대결과 | 계층 | 우선 | 근거(파일) |
|----|---------|------|----------|---------|------|:--:|----------------|
| TC-AUG-100 | **100장 청크 분할 위탁 (신규)** | 비식별 프레임 250장 | AFTER_COMMIT 위탁 | 3 청크로 분할, 청크별 `request_id = {augIdmpKey}-{jobSeq}`, `LS_DATA_AUG_JOB` 3행 + `LS_DATA_AUG_JOB_FILE` 250행. 상한은 계약 100 을 넘길 수 없다(설정으로 낮추기만 가능) | integration | High | AugmentJobSubmitService.java |
| TC-AUG-101 | **job_id 는 외부가 발급 (신규)** | 정상 위탁 | `POST /api/genai/jobs` 202 | 응답 검증 3축 통과 시에만 `markAccepted(외부 job_id)` — `request_id` echo 일치 + `status=RECEIVED` + `job_id` non-blank. 하나라도 어긋나면 `EXTERNAL_API_ERROR` | integration | High | HttpExternalAugmentClient.java |
| TC-AUG-102 | **Idempotency-Key 헤더 (신규)** | 재시도 | 위탁 | `Idempotency-Key: {request_id}` 전송 — 동일 키 재요청 시 외부가 기존 job 반환(중복 job 미생성) | unit | Med | HttpExternalAugmentClient.java |
| TC-AUG-103 | **4xx = 비재시도 (신규)** | 외부 400/404 | 위탁 | `NonRetryableExternalException` 으로 Retry/CircuitBreaker 에서 제외, 본문 소비·해제(누수 방지), 응답 본문 원문 미노출(CWE-209). 연결 실패·타임아웃·5xx 만 재시도 | integration | High | HttpExternalAugmentClient.java |
| TC-AUG-104 | **전량 선기록 후 위탁 (신규)** | 2번째 청크 선기록 실패 | 위탁 | **한 건도 위탁하지 않고** 이미 선기록된 앞 청크까지 `FAILED(ERR_CD=ISSUE_RECORD_FAILED)` 로 종결 → 롤업이 부분 프레임셋을 전량으로 오인해 성공 확정하는 경로 제거 | integration | High | AugmentJobSubmitService.java |
| TC-AUG-105 | **비식별 경로 부재 = 위탁 거부 (신규)** | 프레임 중 `DE_IDNTF_SRC_FILE_PATH_NM` blank 존재 | 위탁 | 한 건도 위탁하지 않고 `LS_DATA_AUG_JOB`(FAILED/`DEID_PATH_MISSING`) 기록. **원본 경로 폴백 금지** | security | High | AugmentJobSubmitService.java |
| TC-AUG-106 | **위탁 전 신고 = 거부(보류 아님) (신규)** | 대상 영상 `'F'` | 위탁 진입 | 프레임 경로 조회조차 하기 전 차단 → `LS_DATA_AUG_JOB`(FAILED/`DEID_REPORT_OPEN`) + `SubmitOutcome.of(0)` → 브릿지가 즉시 실패 롤업(REJECTED). 구 `WITHHELD_*` 정책 보류·재개 리스너 **폐기** | security | High | AugmentJobSubmitService.java; AugmentRequestBridge.java |
| TC-AUG-107 | **위탁 도중 신고 관측 = 남은 청크 중단 (신규)** | 2/3 청크 전송 후 신고 커밋 | 청크 루프 | 청크마다 무잠금 재판정 → 남은 job 을 `FAILED(ERR_CD=DEIDENT_REPORT)` 로 종결(비종결 방치 시 롤업 영구 보류). 이미 나간 청크는 되돌릴 수 없으므로 증강 1건은 실패로 마감 | security | High | AugmentJobSubmitService.java |
| TC-AUG-108 | **청크 위탁 실패 건별 격리 (신규)** | 2번째 청크 HTTP 실패 | 위탁 | 해당 job `FAILED(SUBMIT_FAILED)` + 사유 기록(사유 문구는 상태코드만 — 벤더 본문 미노출), 3번째 청크는 계속 위탁. 집계 판정은 수신부 롤업 책임. ★기전 정정(2026-08-03): 실패 기록은 `AugmentJobSubmitService` 가 아니라 `onErrorResume` → `AugmentSubmitOutcomeRecorder.onSubmitFailed` → `AugmentJobRecorder.markSubmitFailed`(조건부 원자 UPDATE — 지각 신호가 콜백 상태를 강등하지 못한다)가 수행한다 | integration | Med | AugmentJobSubmitService.java; AugmentSubmitOutcomeRecorder.java; AugmentJobRecorder.java |
| TC-AUG-109 | **위탁 0건 = 즉시 실패 롤업 (신규)** | 전 청크 위탁 실패 | 브릿지 | `requiresFailureRollup()` → `handleInNewTransaction(failed)` 로 REJECTED + dead-letter. 단 **미종결 job 이 하나라도 있으면 롤업하지 않는다**(동시 재요청 보호) | integration | High | AugmentRequestBridge.java; AugmentJobSubmitService.java |
| TC-AUG-110 | **★커넥션 풀 데드락 회귀 가드 (신규, 2026-08-03 기전 정정)** | 위탁 동시 2건 | `submit()` 호출 스택 | **현재(무애너테이션) 상태**에서 외부 HTTP 왕복 시점에 `TransactionSynchronizationManager.isSynchronizationActive()`=false. `@Transactional(readOnly)` 재부착은 커넥션 풀 데드락 실측(30s 후 `CannotCreateTransactionException`)으로 금지. `NOT_SUPPORTED` 도 별도 테스트에서 커넥션 고갈 재현됐으나 **인과는 미규명** — 클래스 javadoc 은 "무조건 금지"가 아니라 "되돌리려면 두 관측(동기화 활성·커넥션 고갈)을 재측정하고 고갈 미재현 근거를 남긴 뒤 바꾼다"는 조건부 서술이다(구 서술 "재부착 금지"를 단정으로 읽지 말 것). ⚠ `isActualTransactionActive()` 는 관측축으로 부적합 | integration | High | AugmentJobSubmitService.java |
| TC-AUG-111 | **evnt_type 폴백 (신규)** | 영상 `EVNT_TYPE_CD` null/blank | 위탁 바디 | `"ETC"` 로 채움(계약 필수 필드) | unit | Low | AugmentJobSubmitService.java |
| TC-AUG-112 | **위탁 로그에 절대경로 미출력 (신규)** | 위탁/거부 | 로그 | 개수·식별자 수준만 기록, 파일 절대경로 미노출 + CR/LF sanitize(CWE-117/209) | security | Med | HttpExternalAugmentClient.java; AugmentJobSubmitService.java |
| TC-AUG-113 | **`mode=noop` 시 위탁 미수행 (신규)** | `authoring.augment.external.mode=noop` | 요청 | `NoopExternalAugmentClient` 활성(HTTP 0건). 만료 스윕은 자기 토글만 보므로 후보 0건으로 무해하게 동작 | unit | Med | HttpExternalAugmentClient.java; NoopExternalAugmentClient.java; AugmentJobExpirySweeper.java |
| TC-AUG-114 | **외부 산출 프레임 실반입 3중 검증 (신규)** | Phase B | 반입 | ①읽기 허용 루트(lexical + 실경로, 심링크 CWE-59) ②정규 파일 + size>0 ③**부모 비식별 프레임과 해상도 동일**. 하나라도 어긋나면 반입 거부 → all-or-nothing 실패 | security | High | AugmentFrameProducer.java |
| TC-AUG-115 | **부분 쓰기 방지 (.part → atomic move) (신규)** | 복사 중 중단 | 반입 | 목적 경로에 반쯤 쓰인 파일 미잔존. 소스는 `NOFOLLOW_LINKS` 로 열어 검증~복사 사이 심링크 교체 시 실패(fail-closed, CWE-367) | security | High | AugmentFrameProducer.java |
| TC-AUG-116 | **파생 비디오 = 부모 비식별본 실제 복사 (신규)** | Phase B | 반입 | `{deidBase}/videos/augment/{parentRawSn}/{newRawSn}/{augType}.mp4` 로 복사. 소스 경로는 `LS_DEIDENT_PROC_LOG.DE_IDNTF_FILE_PATH_NM` **값을 읽어** 정한다(mock=`deidentified.mp4` / KPST=`{stem}-mask{ext}` — 조합·추측 금지). 소스 부재 시 NOT_FOUND(원본 폴백 없음) | integration | High | AugmentFrameProducer.java; AugmentExtractSnapshot.java |
| TC-AUG-117 | **파생 프레임은 비식별 컬럼에만 적재 (신규)** | Phase C | 영속 | `SRC_FILE_PATH_NM`=**null**, `DE_IDNTF_SRC_FILE_PATH_NM`=산출 경로. 두 컬럼 동일값 금지(마트 뷰 "두 경로 상이" 불변식). ⚠ 부기 사유였던 *"export ORIGINAL 벌 anonymity 오표기 차단"* 은 **폐기 2026-08-04** — 파생영상은 원천 축이 전부 `null` 이라(TC-EXPORT-044) ORIGINAL 개인정보 표기가 파생 경로에서 성립하지 않는다. **마트 뷰 불변식은 그대로 유효**하므로 케이스는 존치 | integration | High | AugmentExtractPersist.java |
| TC-AUG-118 | **증강 라벨 복사는 좌표 그대로 (신규)** | 해상도 동일 전제 | Phase C | `LsDataLbl.copyForNewSrc` + `LS_DATA_AUG_LBL_MAP(COORD_RECALC_YN='N')`. 해상도 불일치는 TC-AUG-114 가 사전 차단 | integration | High | AugmentExtractPersist.java |
| TC-AUG-119 | **async 추출 실패 = RAW FAILED + aug dead-letter (신규)** | Phase A~C 예외 | 러너 | ①Phase B 산출 cleanup(+잔존 시 ERROR·`augment.cleanup.failed`) ②`markRawDataFailed` ③`markAugProcessingFailed`(REQUIRES_NEW, `AUG_PROC_STTS_CD` **미변경** — 멱등 앵커 무충돌). ③ 없으면 파생 RAW 는 FAILED 인데 집계는 성공으로 보인다 | integration | High | AsyncAugmentFrameRunner.java; AugmentExtractPersist.java |
| TC-AUG-120 | **ffprobe 는 확정 성공 이후에만 (신규)** | Phase C PERSISTED | 러너 | `AsyncVideoMetaRunner` 를 확정 블록 **밖**에서 별도 catch 로 기동 — 사본 확정 전 probe 레이스 제거, 메타 실패가 확정 결과를 되돌리지 않음. ⚠ **정정(2026-08-03 실측) — 구 서술 "`video.*` 가 부모와 동일한 것이 정상" 은 무조건 성립하지 않는다**: 파생의 `video.*` 는 `DerivedMetaCopier` 가 부모 값을 복사한 뒤 `AsyncVideoMetaRunner` 가 **파생 비식별 사본을 ffprobe 로 실측한 값으로 덮어쓴다**. 부모의 `video.*` 는 `LS_DATA_INGEST` **인입 선언값 우선**(`bit_rate` 만 ffprobe 전용)이라, 인입 선언이 실제 파일과 다르면 파생은 부모와 **다른 값**을 갖는다. 실측: rawSn 101(인입 선언 640x480·30fps·10000ms) vs 파생 158(ffprobe 320x240·10fps·5000ms, 비디오는 101 비식별본의 **바이트 동일 사본**). 기대결과는 "부모와 동일" 이 아니라 "**파생 사본을 실측한 값**" 이다 | integration | High | AsyncAugmentFrameRunner.java; DerivedMetaCopier.java; VideoMetaService.java |
| TC-AUG-121 | **Phase C SKIPPED = cleanup 금지 (신규)** | 중복 트리거 패자 | 러너 | 파일이 승자와 동일 경로라 cleanup·FAILED 전이 모두 skip | integration | High | AsyncAugmentFrameRunner.java |

## E-4. 해상도 파생 오케스트레이션 (VideoResolutionService / VideoController)

| ID | 케이스명 | 전제 | 입력/조건 | 기대결과 | 계층 | 우선 | 근거(파일) |
|----|---------|------|----------|---------|------|:--:|----------------|
| TC-RESL-001 | 프리셋 미지정 3종 전체 생성 | APPROVED 원본, 원본≠모든 프리셋 | REVIEWER, body 생략 | 201, `derivatives` 3건 CREATED, 각 새 RAW_SN | integration | High | VideoResolutionService.java |
| TC-RESL-002 | 프리셋 부분 지정 | `presets:[RESL_720P,RESL_480P]` | REVIEWER | 201, 2건만 | integration | High | ResolutionChangeRequest.java |
| TC-RESL-003 | presets 중복 제거 | `[720P,720P]` | 요청 | distinct 1건 | unit | Low | ResolutionChangeRequest.java |
| TC-RESL-004 | **산출 크기가 원본과 같은 프리셋 스킵** | 원본=1920×1080 | 3종 요청 | RESL_1080P skip(결과 목록 제외), 720/480만. ★**판정축 전환(2026-08-26 · CO-018)** — 구 기준 「프리셋 값이 원본 해상도와 같은가」(`preset.width()==srcW && preset.height()==srcH`)는 **폐기**. 프리셋이 고정 캔버스가 아니라 크기 상한이 되면서 의미를 잃었고, 그대로 두면 원본 `960×720` 에 720p(1280×720)가 스킵되지 않아 **원본과 동일한 파생본**이 생성된다. 반올림 때문에 배율이 정확히 1.0 이 아니어도 산출 크기가 같아질 수 있으므로 **배율이 아니라 산출 크기로** 판정한다 | integration | High | VideoResolutionService.java(changeResolution) |
| TC-RESL-005 | **전부 스킵 시 400** | 대상 프리셋의 **산출 크기가 전부 원본과 동일** | 요청 | 400("산출 크기가 원본과 달라지는 적용 가능한 해상도 프리셋이 없습니다."). 구 문구 "원본과 동일하지 않은 적용 가능한 해상도 프리셋이 없습니다." 는 **폐기**(판정축이 프리셋 값 → 산출 크기로 바뀜, CO-018) | integration | High | VideoResolutionService.java(changeResolution) |
| TC-RESL-006 | **전부 실패 시 500** | 모든 `createOne` 예외 | 요청 | 500("요청한 모든 해상도 프리셋의 파생영상 생성에 실패했습니다."), 내부 사유 미노출 | integration | High | VideoResolutionService.java |
| TC-RESL-007 | **부분 실패 격리 201** | 1프리셋 실패 | 요청 | 201, FAILED(rawSn null)+CREATED 혼재 | integration | High | VideoResolutionService.java |
| TC-RESL-008 | **업스케일 허용** | 원본 480p, 목표 1080p | 요청 | 거부 없이 생성(구 `targetH>=srcH` 가드 제거) | integration | High | VideoResolutionService.java |
| TC-RESL-009 | 증강본/파생본 거부 | `ORGNL_RAW_SN≠null` | 요청 | 400("원본 영상에만 해상도 변경 가능") | unit | High | VideoResolutionService.java |
| TC-RESL-010 | 미검수 영상 거부 | APPROVED 아님 | 요청 | 409("검수 완료(APPROVED)된 영상만 해상도 변경할 수 있습니다.") | unit | High | VideoResolutionService.java |
| TC-RESL-011 | 영상 미존재 | 미존재 rawSn | 요청 | 404 | unit | Med | VideoResolutionService.java |
| TC-RESL-012 | 프레임 해상도 확인 불가 | 첫 프레임 dim≤0 | 요청 | 400("원본 프레임 해상도를 확인할 수 없습니다.") | unit | Med | VideoResolutionService.java |
| TC-RESL-013 | 프리셋 enum 화이트리스트 | `["RESL_240P"]` | 요청 바디 | 400 Jackson 거부. ⚠ **문자열 축만 강제됨(미해소, E-ISSUE-42 계열, 2026-08-03 3차 재확인)** — 정수 ordinal(`[0]`/`[1]`/`[2]`)은 Jackson 기본 동작상 `RESL_1080P`/`RESL_720P`/`RESL_480P` 로 그대로 바인딩되어 서비스 로직까지 진입한다(`fail-on-numbers-for-enums` 미설정, `@JsonCreator` 없음). 값 공간이 넓어지진 않으나(0~2=동일 3종) 계약 밖 표현 허용 + enum 상수 추가 시 조용한 재매핑 위험(CWE-20) | security | High | ResolutionPreset.java (enum 상수 3종) |
| TC-RESL-014 | WORKER/미인증 차단 | WORKER·null | 요청 | 403/401 | security | High | VideoController.java |
| TC-RESL-015 | 응답 형태 계약 | 성공 | 201 | `{derivatives:[{rawSn,goalResCd,targetW,targetH,status}]}`, 내부경로 미노출. ★**`targetW`/`targetH` 는 요청 프리셋 수치가 아니라 실제 산출 크기다**(2026-08-26 · CO-018 · `API-092` v8) — 원본 `1440×1080` 을 `RESL_720P` 로 요청하면 `960×720`. 실패(FAILED) 항목도 같은 축으로 표기한다 | unit | Med | ResolutionChangeResponse.java; VideoResolutionService.java(createOne) |
| TC-RESL-016 | measureFirstFrame 경로 CWE-22 | base 밖 경로 | 요청 | 400("원본 프레임 경로가 허용된 저장 경로를 벗어납니다.") | security | High | VideoResolutionService.java |
| TC-RESL-017 | deid 프레임 경로 base 허용 | 비식별 절대경로 | 요청 | raw base ∪ deid base 두 축 허용 통과 | unit | Med | VideoResolutionService.java |
| TC-RESL-018 | **★부모 비식별 산출물 실재 동기 확인 (신규)** | `DE_IDNTF_YN='F'` 인데 산출물 부재(비식별 API 실패형) | 요청 | **409 CONFLICT**("원본의 비식별 산출물을 찾을 수 없어 해상도 파생을 만들 수 없습니다.") — 예약 이전 동기 거부. 통과시키면 201 후 async 확정이 반드시 실패하고 cleanup 이 흔적을 지워 "성공으로 보이는 소멸"이 된다 | security | High | ParentDeidArtifactGuard.java; VideoResolutionService.java |
| TC-RESL-019 | **★신고('F') + 산출물 실재 = 생성 허용 (신규)** | `DE_IDNTF_YN='F'`(신고형, 비식별본 존재) | 요청 | 201 정상 생성. 해상도 파생은 외부 위탁이 전혀 없는 내부 ffmpeg 리스케일이라 신고 구간 생성이 외부 유출을 만들지 않는다(2026-07-29 구속 정책) | security | High | ParentDeidArtifactGuard.java; LsDataRaw.hasDeidentArtifact() |
| TC-RESL-020 | **부모 procLog 경로가 허용 저장경로 밖 (신규)** | `DE_IDNTF_FILE_PATH_NM` 이 2-way 검증 실패 | 요청 | 400 INVALID_INPUT("경로가 허용된 비식별 저장 경로를 벗어납니다."), 경로 원문 미노출 | security | High | ParentDeidArtifactGuard.java |
| TC-RESL-021 | **파생 확정 상태 조회 API (신규)** | 파생 3건(확정/진행/실패 혼재) | `GET /v1/videos/{rawSn}/resolution` | 200 `{derivatives:[...]}` — `'Y'`+COMPLETED→COMPLETED / `DATA_STTS_CD=FAILED`→FAILED / 그 외→IN_PROGRESS. 비-해상도 파생(외부 증강)은 제외. E-ISSUE-24(비동기 확정 실패 미가시) 해소. ★기전 추가(2026-08-03): `VideoResolutionService.changeResolution`/`loadAndValidate` 등 오케스트레이션 로직이 `ResolutionDerivativeService`(Phase 2, 예약 1건 담당)로 일부 위임됐다 — 컨트롤러가 직접 부르는 것은 여전히 `VideoResolutionService` | integration | High | VideoResolutionService.java; VideoController.java |

| TC-RESL-099 | **★프리셋은 크기 상한 — 산출 크기가 원본 종횡비로 정해진다 (신규 · CO-018)** | 720p 프리셋, 원본 5종 | `changeResolution` | `1920×1080→1280×720` / `1440×1080→960×720`(짧은 변 기준) / `640×480→960×720`(업스케일) / `1080×1920→720×1280`(세로) / `2560×1080→1280×540`(긴 변 상한). **세로·파노라마는 세 후보안(박스 피팅 / 짧은변만 / 짧은변+상한)이 갈리는 유일한 지점**이라 반드시 포함한다 | unit | High | LetterboxTransform.java(of); ResolutionLetterboxTest.java |
| TC-RESL-100 | **★박스 피팅으로 되돌리지 않는다 (신규 · CO-018)** | 원본 `1080×1920`(세로) | 배율 산정 | 박스 피팅 `min(targetW/srcW, targetH/srcH)` 은 `405×720` 을 내지만 확정 규칙은 `720×1280` 을 낸다. 새 규칙은 짧은변/긴변으로 **정렬한 뒤** 비교한다 — 두 식을 혼동하면 세로 영상이 과소 산출된다(사용자가 명시적으로 기각한 안) | unit | High | LetterboxTransform.java(of); ResolutionLetterboxTest.java |
| TC-RESL-101 | **★산출 크기 동일 스킵 회귀 가드 (신규 · CO-018)** | 원본 `960×720`, `presets:[RESL_720P]` | 요청 | 400 — 프리셋 수치(1280×720)와 원본은 다르지만 **산출 크기**는 `960×720` = 원본이라 스킵된다. 구 판정(`preset.width()==srcW`)이면 스킵되지 않아 **원본과 동일한 파생본**이 만들어졌다 | unit | High | VideoResolutionService.java(changeResolution) |
| TC-RESL-102 | **★응답이 실제 산출 크기를 싣는다 (신규 · CO-018)** | 원본 `1440×1080`, `presets:[RESL_720P]` | 요청 | 201, `targetW=960` `targetH=720`(프리셋 수치 1280 아님). **부분 실패(FAILED) 항목도 같은 축**으로 표기해 성공/실패가 다른 축을 가리키지 않는다 | unit | High | VideoResolutionService.java(createOne); ResolutionChangeResponse.java |
| TC-RESL-103 | **★조회 API 도 실제 산출 크기 (신규 · CO-018 · `API-179` v6)** | 원본 `1440×1080` 실측 가능, 720p 파생 1건 | `GET /v1/videos/{rawSn}/resolution` | 200, `targetW=960` `targetH=720`. 생성 API 와 같은 축이어야 화면이 두 값을 다르게 보여주지 않는다 | unit | High | VideoResolutionService.java(listDerivatives) |
| TC-RESL-104 | **★조회 API 는 원본 실측 불가 시 프리셋 상한으로 폴백 (신규 · CO-018)** | 원본 프레임 부재/경로 부재 | `GET /v1/videos/{rawSn}/resolution` | 200 + 프리셋 상한 값. 이 엔드포인트는 **비동기 확정 실패를 보여주는 유일한 통로**(E-ISSUE-24)라 여기서 4xx/5xx 를 내면 볼 수단이 사라진다 | unit | High | VideoResolutionService.java(measureFirstFrameQuietly) |

## E-5. 해상도 파생 예약/확정 (Reservation·Snapshot·Materialize·Persist·Runner)

| ID | 케이스명 | 전제 | 입력/조건 | 기대결과 | 계층 | 우선 | 근거(파일) |
|----|---------|------|----------|---------|------|:--:|----------------|
| TC-RESL-030 | 예약행 PENDING 커밋 + 새 RAW | 정상 | `reserveAndCreate` | `LS_DATA_AUG(RESL_*, PENDING)` flush + `createFromResolution`, AFTER_COMMIT 러너 트리거 | integration | High | ResolutionReservationPersister.java |
| TC-RESL-031 | **부모 잠금하 산출물 게이트 — 'N'만 차단** | 부모 `DE_IDNTF_YN='N'`/null | 예약 | 409("비식별 산출물이 있는 원본 영상만 해상도 파생영상을 만들 수 있습니다."), 파생 미생성. ★`'F'`(신고)는 **통과**(2026-07-29 확정) | security | High | ResolutionReservationPersister.java |
| TC-RESL-032 | 대표프레임 SRC_SN null fail-fast | `firstFrameSrcSn=null` | 예약 | 400("대표 프레임을 확인할 수 없습니다.") — INSERT 이전 거부 | unit | Med | ResolutionReservationPersister.java |
| TC-RESL-033 | **부분유니크 중복 예약 차단** | 동일 (SRC_SN, RESL_*) 재/동시요청 | 예약 flush | 409("동일 영상에 해당 해상도 파생 결과가 이미 존재합니다.") — `UK_LS_DATA_AUG_RESL`(V125, **상태 무관**) | integration | High | ResolutionReservationPersister.java; V125 |
| TC-RESL-034 | 출력 경로 CWE-22 + 비식별 서브트리 강제 | traversal 상대경로 | 예약 | 400("출력 경로가 허용된 비식별 저장 경로를 벗어납니다.") — base 하위 + `StorageSubtreePolicy.isDeidentifiedArtifact` 이중 강제(두 base 동일 설정에서도 원본 서브트리 유출 차단) | security | Med | ResolutionReservationPersister.java |
| TC-RESL-035 | Phase A 멱등 skip | 파생 RAW 이미 `'Y'` | snapshot | `Optional.empty` → 러너 skip | unit | High | ResolutionSnapshotService.java |
| TC-RESL-036 | **Phase A 부모 산출물 재검증 — 'N'만 abort** | 예약~확정 창 부모 `'N'` | snapshot | CONFLICT → 러너 FAILED. ★`'F'` 는 **통과**(예약 게이트와 동일 정책, 구 "부모 'F' → CONFLICT" 폐기) | security | High | ResolutionSnapshotService.java |
| TC-RESL-037 | Phase A 비식별 비디오 경로 부재 | 최신 성공 procLog 없음 — ⚠ **실제 적재 상태값은 `SUCCEEDED`**(`SUCCESS` 아님). `proc_stts_cd='SUCCESS'` 로 조회하면 0건이라 오판한다 | snapshot | 404("원본 비식별 영상 경로를 찾을 수 없습니다") | unit | Med | ResolutionSnapshotService.java; LsDeidentProcLogRepository.findLatestSuccessByDataRawSn(REQ_DT DESC) |
| TC-RESL-038 | Phase A 프레임 0건 fail-fast | 부모 프레임 없음 | snapshot | INTERNAL_ERROR("파생할 프레임이 없습니다") | unit | Med | ResolutionSnapshotService.java |
| TC-RESL-039 | Phase A 중복 videoFrameNo fail-fast | 부모 중복 프레임키 | snapshot | INTERNAL_ERROR(라벨 이중매핑 차단) | unit | Med | ResolutionSnapshotService.java |
| TC-RESL-040 | **Phase A deid 프레임 경로 strict** | `deidFilePath` blank/null | snapshot | CONFLICT("비식별 프레임 경로가 없어 파생영상을 생성할 수 없습니다") — 원본 픽셀 복제 + `'Y'` 위장 차단, **원본 폴백 없음** | security | High | ResolutionSnapshotService.java |
| TC-RESL-041 | Phase B 비디오 복사 + 프레임 리스케일 | 스냅샷 확정 | materialize | 비식별 비디오 복사 → `videoDst`, 전 프레임 `targetW×targetH` 리스케일(★2026-08-26 · CO-018 — 스냅샷의 `targetW/targetH` 는 프리셋 수치가 아니라 **실제 산출 크기**다. ⚠ **[정정 회차 22]** 구 기대의 *"리사이저는 그 값을 다시 상한으로 받아도 같은 크기를 낸다=멱등"* 은 **거짓**이며, 리사이저에 넘기는 값은 **프리셋 상한**이다 → TC-RESL-106·108). DB 접근 0 · 부모 잠금 0. ★**목적 파일은 부모 프레임과 1:1**이어야 한다 — 부모 프레임 N건이면 `frames/deid/{newRawSn}/` 산출 파일도 N건. ✅ **E-ISSUE-61 해소(2026-08-04)** — 구 구현은 목적 파일명이 부모 deid 경로 basename 파생이라 서로 다른 디렉터리의 동명 프레임이 1개 파일로 덮어써졌다(3차 실측 FAIL). 목적 파일명을 파생 자신의 FRM_NO 기반(`frame-{frameNo}.{ext}`)으로 바꿔 1:1 이 성립한다 → 상세는 TC-RESL-096/097 | integration | High | ResolutionFileMaterializer.java; ResolutionSnapshotService.java |
| TC-RESL-042 | Phase B 원본 비식별 파일 부재 | `deidVideoSrc` 미존재 | materialize | 404("원본 비식별 영상 파일을 찾을 수 없습니다.") — 원본 폴백 없음 | unit | Med | ResolutionFileMaterializer.java |
| TC-RESL-043 | Phase B 리사이즈 게이트(DoS) | 동시 다수 | materialize | `ResizeConcurrencyGate` 세마포어가 **복사+리사이즈 전체**를 한 슬롯으로 통제 | integration | Med | ResolutionFileMaterializer.java |
| TC-RESL-044 | Phase C 확정 영속 | Phase B 산출 | persist | PERSISTED — 프레임 INSERT + `copyScaledLabels` + `markResolutionGenerated`(PENDING→ACCEPTED) + `markDeidentified('Y')` + `markCompleted` + SUCCESS procLog + `DerivedMetaCopier`(순서 고정: 확정 블록 **이후**) | integration | High | ResolutionPersistService.java |
| TC-RESL-045 | **★종횡비 보존 균일 배율 좌표 변환(오프셋 없음)** | 부모 라벨(BBOX/POLYGON/세그/키포인트), 비-16:9 원본 | `copyScaledLabels` | `LetterboxTransform` 균일 배율(`scaleX==scaleY==box.scale`) → **`x' = x*scale`(오프셋 가산 없음)**. ★**정정(2026-08-26 · CO-018)** — 구 기대값 `x' = x*scale + offsetX`(중앙 오프셋)는 검정 레터박스 시절 것이라 **폐기**. 가변 캔버스 전환으로 패딩이 사라져 `offsetX==offsetY==0` 이다. 구 축별 독립 배율(강제 왜곡, E-ISSUE-26) 폐기는 그대로 유효. 매핑행은 기존 컬럼(`COORD_RECALC_YN='Y'`, `SCALE_X/SCALE_Y`)만 기록하며, **오프셋이 없으므로 그 두 값만으로 원본 좌표가 완전히 역산**된다(구 「추적성 한계·오프셋 컬럼 신설은 후속 과제」 서술 소멸) | integration | High | ResolutionSnapshotService.java; ResolutionPersistService.java(copyScaledLabels) |
| TC-RESL-046 | 부모 라벨 0건 | 없음 | `copyScaledLabels` | 0 반환 | unit | Low | ResolutionPersistService.java |
| TC-RESL-047 | ~~Phase C stale PII 게이트 — 재신고~~ | capturedAt 이후 부모 신고 | persist | **[폐기 2026-07-30]** ★판정축 교체 — "capturedAt 이후 신고 이력 존재" 조건이 **제거**됐다(순수 신고 결합, 파생 생성은 원본 신고와 무관). 남은 두 조건은 신고가 아니라 **복사 원자성**을 방어한다 → TC-RESL-048 / TC-RESL-049 | security | — | ResolutionPersistService.java (주석) |
| TC-RESL-048 | **Phase C stale — 비식별 경로 변경 abort** | 최신 성공(`SUCCEEDED`) procLog 경로 ≠ 스냅샷 경로 | persist | CONFLICT("스냅샷 이후 원본 비식별본이 변경되어…") → 러너가 cleanup + FAILED. 경로 해석 불가도 불일치로 취급(abort) | security | High | ResolutionPersistService.java |
| TC-RESL-049 | **Phase C stale — 파일 mtime 교체 abort** | 같은 경로, `mtime > capturedAt` | persist | CONFLICT(제자리 교체 방어). **파일 존재 시에만** 판정하고 `stat` 실패는 보수적 통과(로그만) | security | Med | ResolutionPersistService.java |
| TC-RESL-050 | **Phase C 부모 재잠금 최종 게이트 — 'N'만 abort** | 부모 `'N'`/null | persist | CONFLICT abort. ★`'F'` 통과. 잠금 순서는 항상 parent → newRaw(교착 방지) | security | High | ResolutionPersistService.java |
| TC-RESL-051 | **중복 finalize CAS skip** | 파생 newRaw 이미 `'Y'` | persist | `Result.SKIPPED`(프레임 재삽입 없음) — newRaw 재잠금으로 두 finalize 직렬화 | integration | High | ResolutionPersistService.java |
| TC-RESL-052 | 프레임 개인정보 3필드 복사 (정정 2026-08-04) | 부모 anony/psdo/prvc | `insertFrames` | 파생에 복사, **부모 미입력(레거시)이면 `null` 이 아니라 적재 기본값 `Y`/`N`/`N`**(구 기대 "부모 null 이면 null" 폐기 — `LsDataSrc.create` 팩토리가 `orInsertDefault` 적용). **`SRC_FILE_PATH_NM`=null**(파생엔 원본 픽셀이 실재하지 않음, 정책 A). ⚠ `ResolutionPersistService.insertFrames` 의 인라인 주석("부모 미입력이면 파생도 null")은 팩토리 변경을 아직 반영하지 못한 **주석 드리프트** — 실제 동작은 팩토리 기준 | unit | Med | LsDataSrc.java; ResolutionPersistService.java |
| TC-RESL-053 | 예약 aug 슬롯 해제 | finalize 실패 | `releaseReservedAug` | `RESL_` 접두 + 라벨맵 미참조일 때만 삭제(예약행을 남기면 `UK_LS_DATA_AUG_RESL` 이 상태 무관이라 재요청 영구 락아웃) | integration | High | ResolutionPersistService.java |
| TC-RESL-054 | 슬롯 해제 방어 — 라벨맵 참조 시 미삭제 | `LS_DATA_AUG_LBL_MAP` 참조 | `releaseReservedAug` | 삭제 skip(승자 보호) + WARN | unit | Med | ResolutionPersistService.java |
| TC-RESL-055 | 슬롯 해제 방어 — 비-RESL 미삭제 | 오배송 non-RESL | `releaseReservedAug` | 삭제 skip + WARN | unit | Med | ResolutionPersistService.java |
| TC-RESL-056 | isAlreadyFinalized FOR UPDATE 판정 | newRaw `'Y'` 또는 COMPLETED | 조회 | true(cleanup·FAILED 스킵 근거). `findByRawSnForUpdate` 로 승자 Phase C 커밋과 부분 직렬화 — 잔여 창(승자-뒤짐·락 타임아웃)은 미폐쇄(정직한 한계 명시) | unit | Med | ResolutionPersistService.java |
| TC-RESL-057 | 러너 A→B→C 정상 완주 | 전 단계 성공 | `finalizeDerivative` | 로그 완료, 예외 없음 | integration | High | AsyncResolutionRunner.java |
| TC-RESL-058 | 러너 snapshot empty skip | Phase A empty | 러너 | skip, 후속 미실행 | unit | Med | AsyncResolutionRunner.java |
| TC-RESL-059 | 러너 persist SKIPPED — 파일 미정리 | Phase C SKIPPED | 러너 | cleanup 미실행(승자 산출물 보호) | integration | High | AsyncResolutionRunner.java |
| TC-RESL-060 | **러너 실패 정리 — 승자 보호 선점검** | 예외 + 승자 확정 | `handleFailure` | `isAlreadyFinalized` 를 **cleanup 보다 먼저** 확인 → cleanup·FAILED 전이 모두 skip | integration | High | AsyncResolutionRunner.java |
| TC-RESL-061 | 러너 실패 정리 — cleanup+슬롯해제+FAILED | 예외 + 미확정 | `handleFailure` | WARN + `resolution.finalize.failed` 메트릭 → 아티팩트 cleanup → `releaseReservedAug` → `markRawDataFailed` | integration | High | AsyncResolutionRunner.java |
| TC-RESL-062 | cleanup 원본 미삭제 보장 | `videoDst`=파생경로만 | cleanup | `frames/deid/{newRawSn}/` + 파생 비디오 파일만 삭제. 원본 프레임(`frames/raw/**`)·원본 영상 미삭제. 경로 키에 파생 RAW_SN 포함이라 타 파생과 미공유 | security | High | ResolutionFileMaterializer.java |
| TC-RESL-063 | @Async 예외 삼킴 | 러너 예외 | `runAsync` | 예외 전파 없음 | unit | Med | AsyncResolutionRunner.java |
| TC-RESL-064 | createResolutionPending RESL_ 접두 강제 | `augTypeCd` 비-RESL | 엔티티 | 400 | unit | Med | LsDataAug.java |
| TC-RESL-065 | markResolutionGenerated 이중전이 차단 | 이미 ACCEPTED | 전이 | 409 | unit | Med | LsDataAug.java |
| TC-RESL-066 | **확정 실패 파생 RAW 고아 정리 (신규)** | FAILED 파생 RAW, 프레임 0건, cleanup 성공 | `deleteFailedDerivativeRaw` | 잠금 + 4조건 재확인(파생임 · `'Y'` 아님 · FAILED · 프레임 0건) 통과 시에만 DELETE, DELETE 문에도 동일 조건 동봉(검사~삭제 창 0건 삭제). E-ISSUE-23(고아 무한 누적) 해소. ⚠ **DELETE SQL 실측 3조건뿐**(`ORGNL_RAW_SN IS NOT NULL` · `DATA_STTS_CD='FAILED'` · 프레임 NOT EXISTS) — `DE_IDENT_YN<>'Y'` 는 서비스 선검사에만 있고 SQL 에 없다(미해소, E-ISSUE-64→3차 E-ISSUE-66) | integration | High | ResolutionPersistService.java; VideoRepository.java |
| TC-RESL-067 | **파일 잔존 시 RAW 행 보존 (신규)** | cleanup 후에도 파일 잔존 | `handleFailure` | `deleteFailedDerivativeRaw` **미호출** — 유일한 DB 포인터를 지우면 추적 불가 고아 파일이 되므로 FAILED 상태로 보존 + WARN | integration | High | AsyncResolutionRunner.java |
| TC-RESL-068 | **레거시 raw base 파생 디렉터리 정리 (신규)** | 구 스킴 `{rawBase}/resolution/{newRawSn}/` 잔존 | cleanup | 함께 재귀 삭제. `legacyRoot` 하위 + 루트 자기자신 아님 두 조건 통과 시에만. raw base 미설정은 "정리 대상 없음"이라 잔존 판정에 미반영 | unit | Med | ResolutionFileMaterializer.java |
| TC-RESL-069 | **파생 비디오 경로 키에 파생 RAW_SN 포함 (신규)** | 같은 (부모, 프리셋) 재시도 | 경로 산정 | `StorageSubtreePolicy.resolutionVideoFile(parent, newRawSn, preset)` — 구 `(부모,프리셋)` 키에서 cleanup 이 타 파생 참조 파일을 지우던 실패 클래스 구조적 제거. INSERT 시 잠정 `.pending/` 경로 → 확정 RAW_SN 으로 즉시 교체(잠정값 커밋 경로 없음) | integration | High | ResolutionReservationPersister.java |
| TC-RESL-070 | **파생 메타 복사 + 미검수 검수행 (신규)** | 부모 메타 N건(일부 검수행 보유) | `DerivedMetaCopier` | 메타 값 **전체 복사**(`video.*` 포함, 배치 upsert 1회) + 부모에 검수행이 있던 메타키만 파생에 **PENDING** 검수행 생성(APPROVED 승계 안 함). `(metaSn, metaTypeCd)` 선재 skip 으로 UNIQUE 위반 원천 차단 | integration | High | DerivedMetaCopier.java |
| TC-RESL-071 | **메타 복사 호출 순서 계약 (신규)** | Phase C | persist | `copyMetaAndReviews` 는 확정 블록(`markResolutionGenerated`/`markDeidentified`/`markCompleted`/procLog) **이후**에 호출. 앞에 두면 배치 upsert 의 `clear()` 로 dirty 변경이 유실돼 영구 미확정 고착 | integration | High | ResolutionPersistService.java; DerivedMetaCopier.java |
| TC-RESL-096 | **★파생 프레임 목적 파일명 = 파생 자신의 FRM_NO (신규)** | 부모 프레임 basename 이 임의(`frame_001.png` 등) | `buildFrameSpecs` | 목적 경로는 `frames/deid/{newRawSn}/frame-{frameNo}.{ext}` — **부모 basename 재사용 금지**(E-ISSUE-61 데이터 유실 회귀 가드). 확장자만 소스에서 승계하되 이미지 allowlist(jpg/jpeg/png/bmp) 밖이면 `jpg` 로 확정. 서로 다른 소스 디렉터리의 **동명 프레임 2건**을 입력해도 목적 파일이 각각 생성되어 디스크 산출 파일 수 = 입력 프레임 수 | integration | High | ResolutionSnapshotService.java |
| TC-RESL-097 | **목적 경로 중복 fail-fast (신규)** | 같은 FRM_NO 프레임 2건(videoFrameNo 는 상이 → 기존 프레임키 가드 통과) | `buildFrameSpecs` | INTERNAL_ERROR("파생 프레임 목적 경로가 중복됩니다") — 덮어쓰기 대신 파생 전체를 실패 종결(러너 cleanup + FAILED). `UK_LS_DATA_SRC_RAW_FRAME(RAW_SN, FRM_NO)` 가 정상이면 도달하지 않는 **제약 드리프트·데이터 오염 대비 최종 방어선** | security | High | ResolutionSnapshotService.java; V4__phase5_data_src_lbl_meta.sql |
| TC-RESL-098 | **목적 확장자 allowlist 강제 + 대소문자 정규화 (신규)** | 부모 비식별 프레임 basename 의 확장자가 임의 | `extensionOf`(← `fileNameOf`) | ①**allowlist(jpg/jpeg/png/bmp) 밖**(`.webp`·`.gif`)이면 승계하지 않고 `jpg` 로 확정 ②**확장자 미상**(확장자 없음·점으로 끝나 빈 확장자)도 `jpg` 로 확정 — 소스 basename 조각이 목적 파일명에 새지 않는다(CWE-22/CWE-20). 목적 확장자가 곧 `ImageResizer` 출력 포맷이라 이름↔내용도 어긋나지 않는다 ③**대문자 확장자**(`.PNG`·`.JPEG`)는 `toLowerCase(Locale.ROOT)` 정규화 후 allowlist 에 매칭되어 **원 확장자 계열을 승계**하고 목적 파일명은 소문자로 확정된다(대문자라는 이유만으로 jpg 폴백되지 않음) | unit | Med | ResolutionSnapshotService.java |

| TC-RESL-105 | **★산출 프레임에 패딩이 0 이다 (신규 · CO-018)** | 네 사분면을 서로 다른 색(빨강/초록/파랑/**검정**)으로 칠한 `1080×1920` 원본 | `Java2DImageResizer.resize` | 캔버스가 `720×1280`(=산출 크기)이고 네 모서리 픽셀이 각각 원본 사분면 색과 일치한다. ⚠ **"모서리가 검정이 아니다" 단정으로 대체 금지** — 원본이 검은 영상이면 위양성이다(그래서 한 사분면을 일부러 검정으로 둔다). 구 레터박스라면 캔버스가 `1280×720` 이고 좌우가 검정 패딩이었다 | integration | High | Java2DImageResizer.java(resize); ResolutionLetterboxTest.java |
| TC-RESL-106 | **[정정 2026-08-26 회차 22] ~~리사이즈 멱등~~ → 재적용은 멱등이 아니다** | 멱등 5종 + **비멱등 2종**(`2560×1080`+480p · `1366×768`+1080p) | `LetterboxTransform.of(src, first.drawW(), first.drawH())` | 구 기대(**"산출 크기를 상한으로 다시 넣어도 같다"**)는 **거짓**이다 — 긴 변 상한이 정확히 걸리고 반올림이 올림으로 떨어지는 구간에서 재적용이 **1px 작은 크기**를 낸다. 구 케이스가 열거한 5종이 **전부 멱등 구간**이라 속성이 거짓인데도 통과했다(케이스 선정 결함). 이제 비멱등 2종을 명시적으로 고정하고, 그것이 **Phase B 가 산출 크기가 아니라 프리셋 상한을 넘겨야 하는 근거**임을 문서화한다 → TC-RESL-108 | unit | High | LetterboxTransform.java(of); ResolutionLetterboxTest.java(recomputingFromComputedSizeIsNotIdempotent) |
| TC-RESL-107 | **★긴 변 상한 클램프 mutation 가드 (신규 · CO-018)** | 원본 `2560×1080` | 배율 산정 | 짧은 변만 맞추면 긴 변이 `2560→1280` 상한을 넘는다. 클램프를 제거하면 TC-RESL-099(파노라마)·이 케이스가 **FAIL** 해야 한다 — 가드가 실제로 가드하는지의 실증(2026-08-26 mutation 2종 실측: 클램프 제거 2건 FAIL · 박스 피팅 치환 3건 FAIL · 고정 캔버스 복원 5건 FAIL) | unit | High | LetterboxTransform.java(of); ResolutionLetterboxTest.java |
| TC-RESL-108 | **★리사이저에는 산출 크기가 아니라 프리셋 상한을 넘긴다 (신규 · CO-018 DEV_FIX)** | 스냅샷 `preset=RESL_480P` · 원본 `2560×1080` · 산출 `854×360`(두 값이 갈리는 조합) | `ResolutionFileMaterializer.materialize` | `imageResizer.resize(.., 854, 480)`(프리셋 상한)로 호출되고 `(854, 360)`(산출 크기)로는 **호출되지 않는다**. 리사이저는 인자를 상한으로 받아 `LetterboxTransform` 을 스스로 적용하므로, 이미 도출이 끝난 산출 크기를 넘기면 **같은 원본에 변환이 두 번** 걸려 파일이 1px 작아진다. ⚠ **TC-RESL-041 의 720p 조합(프리셋 1280×720 = 산출 1280×720)으로는 이 배선이 구분되지 않는다** — 두 값이 우연히 같아 어느 쪽을 넘겨도 통과한다(그래서 이 결함이 통과됐다). mutation 실증 2026-08-26: `snapshot.targetW()/targetH()` 로 되돌리면 이 케이스 **FAILED** | unit | High | ResolutionFileMaterializer.java(materialize); ResolutionFileMaterializerTest.java(materializePassesPresetLimitNotComputedSize) |
| TC-RESL-109 | **★산출 파일 크기 = 스냅샷 산출 크기 (신규 · CO-018 DEV_FIX)** | 720p 5종 + **비멱등 2종**(`2560×1080`+480p→`854×360` · `1366×768`+1080p→`1920×1079`) | 실제 이미지 산출(`@TempDir` + ImageIO) | 산출 **파일**의 폭·높이가 `LetterboxTransform.of(src, preset)` 결과와 정확히 같고, 라벨 최대 좌표 `round(srcW*scale)` 가 그 **파일** 경계 안에 든다. ⚠ **계산기 수준 단언만으로는 부족하다** — 계산기끼리 맞아도 파일이 1px 작으면 ①최우측 라벨이 이미지 밖으로 나가고 ②응답·DB 가 보고하는 크기와 실제 파일이 갈린다. 잘못된 배선의 증상은 TC-RESL-109b 가 픽셀로 직접 고정한다 | integration | High | Java2DImageResizer.java(resize); LetterboxTransform.java(of); ResolutionLetterboxTest.java(resizedFileMatchesSnapshotSize) |
| TC-RESL-109b | **★잘못된 배선의 픽셀 증상 — 파일 1px 축소 + 라벨 경계 이탈 (신규 · CO-018 DEV_FIX)** | 원본 `2560×1080` + 480p | 프리셋 상한 산출 vs 산출 크기 산출 | 프리셋 상한을 넘긴 파일은 `854` 폭, 산출 크기를 넘긴 파일은 `853` 폭. 스냅샷 배율로 변환된 라벨 최대 x(`854`)가 후자의 최대 인덱스(`852`)를 **넘는다**. 이 케이스는 mutation 없이도 결함의 기전을 픽셀로 남긴다 | integration | High | Java2DImageResizer.java(resize); ResolutionLetterboxTest.java(feedingComputedSizeShrinksFileAndPushesLabelOutOfBounds) |
| TC-RESL-110 | **★4:3 원본은 1080p 가 스킵되어 파생이 3건이 아니라 2건 (신규 · CO-018)** | 원본 `1440×1080` · presets 미지정(표준 3종) | `changeResolution` | `RESL_1080P` 는 짧은 변이 이미 1080 이라 배율 1.0 → 산출 크기가 원본과 같아 **스킵**. `RESL_720P`(960×720) · `RESL_480P`(640×480) 2건만 생성된다. ⚠ **사용자 가시 동작 변화** — 구 고정 캔버스 규칙에서는 `1920×1080` 필러박스 파생이 생성되어 3건이었다(운영자가 파생 개수 감소를 결함으로 오인할 수 있다) | unit | Med | VideoResolutionService.java(changeResolution); VideoResolutionServiceTest.java(fourByThreeSourceSkips1080pPreset) |
| TC-RESL-111 | **★종단 가드 — materialize → 실제 리사이저 → 산출 파일 치수 (신규 · CO-018 DEV_FIX)** | `ResolutionFileMaterializer` 에 **실제 `Java2DImageResizer`** 주입 · 비멱등 원본 `2560×1080` 프레임을 `@TempDir` 에 실제 이미지 파일로 배치 · 스냅샷 `preset=RESL_480P` · 산출 `854×360` | `ResolutionFileMaterializer.materialize` | 산출 **파일**이 `854×360`(=스냅샷 `targetW/targetH`)이고 라벨 최대 좌표 `round(srcW*scaleX)` 가 그 파일 경계 안에 든다. ⚠ **기존 세 층 어디에도 이 연결이 없었다** — TC-RESL-108 은 Mockito 인자 검증이라 파일을 만들지 않고, TC-RESL-109 는 리사이저를 직접 호출해 materializer 를 경유하지 않으며, 흐름 통합시험은 `ImageResizer` 를 `@MockBean` 으로 격리한다. 이 케이스가 **리사이저 구현 교체·인자 의미 변경**처럼 인자 층의 시야 밖에서 같은 증상이 나는 변화를 잡는다. ⚠ **멱등 조합(예: `1920×1080`+720p)으로 대체 금지** — 두 배선이 같은 결과를 내 이 결함 클래스를 한 건도 잡지 못한다. mutation 실증 2026-08-26: `snapshot.targetW()/targetH()` 로 되돌리면 이 케이스 **FAILED**(`expected: 854 but was: 853`) | integration | High | ResolutionFileMaterializer.java(materialize); Java2DImageResizer.java(resize); ResolutionFileMaterializerTest.java(materializeWithRealResizerWritesSnapshotSizedFile) |

## ~~E-5B. 해상도 파생 산출물 비식별 저장소 이관 백필~~ **[폐기 2026-07-30]**

> **[폐기 2026-07-30 — 기능 자체가 제거됨]** 이 절의 대상(`ResolutionBackfillService`/`TxService`/`SweepJob`/전용 컨트롤러/DTO)은 코드에서 **삭제**됐다. 구 스킴(`{raw}/resolution/…`)으로 만들어진 파생 산출물을 비식별 서브트리로 옮기는 **1회성 데이터 정정 배치**였고, 대상 데이터가 생길 수 있었던 창은 해상도 파생 도입(V125, 2026-07-22)부터 PII 격리 수정(V133, 2026-07-26)까지 **나흘**뿐이다. 그 사이 파생을 생성한 환경은 dev(cudo_246)뿐이었고 거기서 이미 이관 완료(구 스킴 프레임 0건·삭제 대기 마커 0건 실측), stg/상용에는 대상 데이터가 없음을 확인했다. 신규 파생본은 처음부터 올바른 위치에 생성되므로 재발 대상도 아니다.
>
> **대체 케이스 없음** — 검증 대상이 아니다. 파생 산출물의 비식별 서브트리 격리 자체는 생성 경로(E-5 해상도 파생)와 `StorageSubtreePolicy` 판정에서 계속 검증된다. 필요 시 git 이력(`dedb67b1`)에서 복구 가능.
>
> *(폐기 전 원문 — 이력 보존용)* E-ISSUE-21/22/41 후속 **데이터 정정 운영 배치**. Flyway 는 파일을 옮길 수 없어 스키마·뷰(V133) 변경과 분리했다. 이미 raw 저장소에 만들어진 파생 비디오·프레임을 비식별 서브트리로 옮기고 DB 경로를 정정한다. **PII 저장소 이관 + 원본 삭제**를 수반하므로 순서·실패 안전성·경로 가드가 핵심 축이다.
>
> **3차 재확인(2026-08-03)**: `grep -rl 'ResolutionBackfill' backend/src` → Java 소스 0건(잔존은 `V133__…sql` 주석과 `CacheConfig.java` 주석뿐). 16행 전건 `~~취소선~~` 표기 정상. 폐기 표기 **정확**.

| ID | 케이스명 | 전제 | 입력/조건 | 기대결과 | 계층 | 우선 | 근거(파일) |
|----|---------|------|----------|---------|------|:--:|----------------|
| ~~TC-RESL-080~~ | **★순서 계약 — 원천검증 → Copy → Verify → DB 커밋 → 유예 등록 (삭제 아님) (신규)** | 이관 대상 파생 1건 | `migrateOne` | 순서가 정확히 ①`validateMigrationSource` ②`Files.copy`(move 아님 — 원본 보존) ③크기 일치 verify ④`txService.commitRelocation`(REQUIRES_NEW) ⑤`markStale`(대기열 등록) ⑥레거시 등록 ⑦`streamMetaCacheEvictor.evict`. **`migrateOne` 은 어떤 파일도 삭제하지 않는다** — 삭제는 유예 경과 후 스윕만 수행. 역전 시 깨지는 것: 삭제 선행이면 중간 실패에서 복구 불가, 커밋 선행-복사 후행이면 DB 가 없는 파일을 가리켜 스트리밍/export 전면 404 | integration | High | ResolutionBackfillService.java; ResolutionBackfillTxService.java |
| ~~TC-RESL-081~~ | **Verify 실패 = DB 미커밋 + 원본 유실 0 (신규)** | 복사본 크기 불일치 또는 목적지 부재 | `copyAndVerify` | `INTERNAL_ERROR("이관 파일 검증에 실패했습니다.")` → `migrateOne` 이탈 → **커밋·유예 등록 미실행**, 구 파일 그대로. DB 경로는 raw 그대로 유지(NULL 로 만들지 않는다 — 정상 서빙 중인 파생이 즉시 404 되는 것 방지). 해당 rawSn 만 `failures[{rawSn, reasonCode}]` 로 격리 반환되고 나머지 대상은 계속 처리 | integration | High | ResolutionBackfillService.java |
| ~~TC-RESL-082~~ | **DB 커밋 실패 = 삭제 0 + 재실행 멱등 (신규)** | `commitRelocation` 예외 | `migrateOne` | 예외가 `run()` 의 건별 catch 로 격리 → `markStale` 미도달(**유예 대기열 등록 0 = 구 파일 삭제 후보에 오르지 않음**). 재실행 시 `copyAndVerify` 가 "원본 존재" 경로로 재복사(REPLACE_EXISTING)해 정상 진행 | integration | High | ResolutionBackfillService.java |
| ~~TC-RESL-083~~ | **삭제 실패 = 마커 보존 + 다음 스윕 재시도 (신규)** | `Files.delete(target)` IOException | `sweepOne` | `false` 반환(삭제 실패) + **마커를 지우지 않는다** → 다음 스윕이 재시도. WARN 로그(경로 원문 미노출) | integration | High | ResolutionBackfillService.java |
| ~~TC-RESL-084~~ | **멱등 재실행 — 이미 이관 완료면 skip, 단 레거시 등록은 매 실행 재시도 (신규)** | 이미 목표 위치를 가리킴 | `migrateOne` | 프레임은 `src.equals(dst)` 로 **개별 판정** skip. `frameUpdates` 비고 `newVideoPath` null 이면 `skippedRawSns` 에 담기되 **`markLegacyArtifactsStale` 는 그때도 수행**(M-3 — 등록 실패를 삼킨 뒤 다음 실행이 skip 으로 빠지면 구 파일이 영구 잔존) | integration | High | ResolutionBackfillService.java |
| ~~TC-RESL-085~~ | **★원천(복사소스=삭제대상) 경로 가드 4종 (신규)** | 오염/레거시 DB 경로 | `validateMigrationSource` | ①두 base(raw/deid) 밖 → 400("원천 경로가 허용된 저장소를 벗어납니다.") ②`frames/raw/**`(원본 프레임 서브트리) → **409 거부**(부모 원본을 비식별 벌로 복제한 뒤 원본을 삭제하는 최악 시나리오 원천 차단) ③존재하는데 정규 파일 아님(심링크·디렉토리, `NOFOLLOW_LINKS`) → 409 ④해석 불가 → 400. **원본 폴백 없음** | security | High | ResolutionBackfillService.java |
| ~~TC-RESL-086~~ | **목적 경로 가드 — 비식별 서브트리 강제 (신규)** | 상대경로 traversal | `safeDeidPath` | base 하위 + `StorageSubtreePolicy.isDeidentifiedArtifact` 동시 충족 아니면 400("이관 목적 경로가 비식별 저장 경로를 벗어납니다.") — 두 base 가 동일 경로인 운영 형상에서도 원본 서브트리로 새지 않는다(CWE-22/359) | security | High | ResolutionBackfillService.java |
| ~~TC-RESL-087~~ | **★캐시 무효화 이중 (신규)** | 백필 전 이미 스트리밍된 rawSn | 이관 | ①`commitRelocation` 이 `evictAfterCommit(rawSn)` — **커밋 후** 발화(커밋 전 evict 는 동시 요청이 옛 경로를 재캐싱), 롤백 시 미실행 ②`migrateOne` 말미에 `evict(rawSn)` 한 번 더(`@Cacheable` get→load→put 비원자로 커밋 전 읽은 요청이 옛 값을 재설치한 창 축소). 무효화 없으면 stream-meta TTL(5분) 동안 구 경로 해석 → `FileNotFoundException` 500 | integration | High | ResolutionBackfillTxService.java; ResolutionBackfillService.java; CacheConfig.java |
| ~~TC-RESL-088~~ | **★유예 < stream-meta TTL 이면 기동 거부 (신규)** | `authoring.resolution-backfill.stale-grace-minutes` < 5 | 부팅 | `@PostConstruct` 에서 `IllegalStateException` — **fail-closed**. 유예의 존재 이유가 "다른 노드의 stale 캐시가 TTL 동안 옛 경로를 해석해도 파일이 살아 있게" 하는 것이라, 짧은 유예는 보증을 조용히 무력화(500 재발). WARN 이 아니라 차단 | integration | High | ResolutionBackfillService.java |
| ~~TC-RESL-089~~ | **유예 마커 계약 — SHA-256 파일명·mtime 리셋 금지 (신규)** | 같은 파일 반복 등록 | `markStale` | 마커명 = 대상 절대경로의 SHA-256 hex + `.pending`(경로 1:1, 파일명 제약 회피). **이미 있으면 덮어쓰지 않는다**(유예 시작시각이 뒤로 밀려 영영 삭제되지 않는 것 방지). `CREATE_NEW` + `FileAlreadyExistsException` 무시로 동시 실행 안전. 등록 실패는 **구 파일 보존**으로 귀결(안전측) + 다음 실행 재시도 | unit | High | ResolutionBackfillService.java |
| ~~TC-RESL-090~~ | **★스윕 삭제 4조건 + 실패 시 보존 (신규)** | 마커 1건 | `sweepOne` | 모두 충족해야 삭제 — ①`marker mtime + grace ≤ now` ②`countReferencesToFilePath == 0`(RAW·procLog·프레임 경로 4컬럼 합산) ③삭제 가능 위치(두 base 하위 + `frames/raw/**` 아님, **마커 내용을 신뢰하지 않고 삭제 직전 재판정**) ④정규 파일(`NOFOLLOW_LINKS`). **mtime 조회 실패 → 미경과(보존)**, **참조 조회 예외 → 보존**. 저장소 밖·비정규 파일은 삭제하지 않고 마커만 버려 무한 재시도 방지 | security | High | ResolutionBackfillService.java; VideoRepository.java:countReferencesToFilePath |
| ~~TC-RESL-091~~ | **스윕 2노드 안전성 + 조회 상한 (신규)** | 두 노드 동시 발화 | 스윕 | 안전성 근거가 **DB 조건부 UPDATE 클레임이 아니라 파일시스템 멱등**이다 — 마커 단위 조건부 처리 + `Files.deleteIfExists`(이미 없으면 조용히 통과) + `discardMarker` 멱등. 1회 처리 상한 `SWEEP_MAX_MARKERS=5000`(정렬 후 limit), 잔여는 다음 스윕이 이어받는다. ⚠ 최악의 경우 같은 파일에 대한 삭제 시도가 두 노드에서 겹칠 수 있으나 결과는 동일(파일 1회 삭제) | integration | High | ResolutionBackfillService.java |
| ~~TC-RESL-092~~ | **dry-run 계약 (신규)** | `dryRun=true` | `POST /v1/videos/resolution-backfill?dryRun=true` | 파일 복사·DB 변경·삭제 **전부 0**. 대상 집계·감사·뷰 제외 목록만 산출하고 `staleDeletedCount=0` + `stalePendingCount`=현재 마커 수(`countPendingOnly`). `migrated`/`skipped`/`failures` 는 비어 있다(루프가 `continue`) | integration | High | ResolutionBackfillService.java |
| ~~TC-RESL-093~~ | **API 인가 + 응답 정보노출 통제 (신규)** | WORKER·미인증 / 정상 | `POST /v1/videos/resolution-backfill` | WORKER 403 / 미인증 401 / REVIEWER 200. 응답 `ResolutionBackfillResponse` 에 **파일 경로 원문 없음** — 식별자(rawSn)·건수·추상 사유코드(예외 클래스명·`Verdict` 이름)만(CWE-209). **★노출 범위(2026-07-30 변경)**: 경로는 `/v1/videos/resolution-backfill` 그대로지만 전용 `ResolutionBackfillController` 로 분리돼 **local/dev 에서만 빈 등록**(allowlist `@Profile`) — stg/prd 는 **404**(엔드포인트 부재). 배포 표식 `ENV=stg\|prd` 면 기동 자체를 거부한다 | security | High | ResolutionBackfillController.java; ResolutionBackfillResponse.java |
| ~~TC-RESL-094~~ | **감사 부분 스캔 플래그 (신규)** | 비식별 경로 보유 프레임 20만 행 초과 | `auditDeidentifiedFramePaths` | `AUDIT_MAX_ROWS=200,000` 도달 시 1행 프로브로 잔여 확인 → `auditTruncated=true` + WARN. **`auditViolations=[]` 를 "영향 없음"의 근거로 쓸 수 없다**. 판정기는 서빙과 동일한 `StorageSubtreePolicy.verifyDeidentifiedFile`(SQL `POSITION` 근사 폐기), 커서 페이징 500행 | integration | High | ResolutionBackfillService.java |
| ~~TC-RESL-095~~ | **프리셋 미확정 파생 — 프레임만 이관 + 노출 (신규)** | 확정 실패로 `LS_DATA_AUG`(RESL_*, ACCEPTED) 예약행이 삭제된 파생 | `migrateOne` | 발견은 **파생 축**(`ORGNL_RAW_SN` + `VMS_CLIP_ID LIKE '%_RESL_%'`)으로 하므로 대상에 **포함**된다. 영상 파일 경로 규약을 만들 수 없어 **영상은 손대지 않고 프레임만 이관**하고, 해당 rawSn 을 `unresolvedPresetRawSns` + WARN 으로 드러낸다(침묵 skip 아님) | integration | High | ResolutionBackfillService.java; VideoRepository.java:findResolutionDerivativeTargets |

## E-6. 데이터셋 Export (DatasetExportService / Bridge / Runner / TxService / Recoverer)

| ID | 케이스명 | 전제 | 입력/조건 | 기대결과 | 계층 | 우선 | 근거(파일) |
|----|---------|------|----------|---------|------|:--:|----------------|
| TC-EXPORT-001 | 검수승인 AFTER_COMMIT 강제 재생성 | `ReviewApprovedEvent` | 브릿지 | `runner.runApprovalAsync(rawSn)` — force=true | integration | High | DatasetExportBridge.java |
| TC-EXPORT-002 | 승인 경로 멱등 skip 미적용 | force=true, 무수정 재승인 | export | 매 승인 새 버전 폴더 + 산출물 전량 재생성 | integration | High | DatasetExportService.java |
| TC-EXPORT-003 | 재동결 경로 멱등 skip | force=false, 동일 해시 | export | `IDEMPOTENT_SKIP`(행 미INSERT, metric+log만). PARTIAL 도 멱등 baseline 에 포함. ⚠ **유일한 발행 트리거(`DatasetReExportEvent`)가 휴면**(발행처 0건 — `TaskModifiedEvent(regen=true)` 단일 축으로 통일)이라 실동작으로는 도달하지 않는 확장점이다 | integration | High | DatasetExportService.java; DatasetExportBridge.java(휴면 사유); DatasetExportTxService.java(baseline IN SUCCEEDED,PARTIAL) |
| TC-EXPORT-004 | **정상 산출 SUCCEEDED + OUTPUT_PATH_NM = 영상 루트** | 프레임·메타 존재 | export | ORIGINAL+DEIDENTIFIED write, `skipped=0` → SUCCEEDED, `OUTPUT_PATH_NM`(V173 개명, 구 `EXPORT_PATH_NM`) = **`{dirname(RAW_FILE_PATH_NM)}/{rawSn}`**(버전 루트 아님 — 관제가 `v1`·`v2`·`deid/` 를 한 경로 아래에서 본다) | integration | High | DatasetExportService.java(export); DatasetExportPathResolver.java:resolveVideoRoot |
| TC-EXPORT-005 | **일부 프레임 부재 PARTIAL** | 원천 이미지 일부 부재 | export | `totalSkipped>0` → PARTIAL, `dataset.export.skipped_frames` 증가 | integration | High | DatasetExportService.java |
| TC-EXPORT-006 | 산출 0건 FAILED | `totalWritten==0` | export | `markFailed`, 승인 롤백 없음 | integration | Med | DatasetExportService.java |
| TC-EXPORT-007 | **파생영상 = ORIGINAL 벌 미생성(PARTIAL 아님)** | 해상도/증강 파생 RAW export | export | 전 프레임 `SRC_FILE_PATH_NM` 이 비어 있으면 ORIGINAL 벌을 **아예 만들지 않고** PARTIAL 판정에서도 제외 → deid 1벌만 SUCCEEDED. 구 동작(ORIGINAL 전 프레임 skip → 항상 PARTIAL 강등) 폐기 | integration | High | DatasetExportService.java |
| TC-EXPORT-008 | 버전 채번 UK 충돌 재시도 | 동시 승인 동일 버전 | `insertWithRetry` | 재채번 최대 3회(UK 백스톱) | integration | High | DatasetExportService.java |
| TC-EXPORT-009 | 버전 채번 재시도 소진 | 3회 UK 위반 | export | null → `VERSION_EXHAUSTED` outcome, abort | unit | Med | DatasetExportService.java |
| TC-EXPORT-010 | 파일쓰기 실패 시 승인 불변 | `writer.write` throw | export | `markFailed` 만, 승인 롤백 없음(@Async 삼킴), 예외 클래스명만 로깅 | integration | High | DatasetExportService.java |
| TC-EXPORT-011 | 입력 부재 NO_INPUT | `loadPreparation` empty | export | 조기 return, `no_input` metric(행 미INSERT) | unit | Med | DatasetExportService.java |
| TC-EXPORT-012 | 메트릭 outcome 정확히 1회 | 모든 종결 분기 | export | `dataset.export.result` counter 1회 + `dataset.export.duration` timer 1회 stop(finally 단일 지점, 조기 return·예외 이탈 포함) | unit | Med | DatasetExportService.java |
| TC-EXPORT-013 | 브릿지 조건부 비활성화 | `authoring.dataset-export.enabled=false` | 부트 | 브릿지 빈 미생성 | unit | Low | DatasetExportBridge.java |
| TC-EXPORT-014 | **폴더 구조 계약 (co-locate)** | 승인 export | 폴더 | `{dirname(원본영상)}/{rawSn}/v{n}/{orgnl\|deid}/` + 프레임별 JSON. 고정 `labeling_root` 폐기 | integration | Med | DatasetExportPathResolver.java:resolve |
| TC-EXPORT-015 | **산출 base 3중 가드 + fail-secure (신규)** | `RAW_FILE_PATH_NM` blank 또는 허용 마운트 루트 밖 | export | 기본 루트로 조용히 새지 않고 export 레코드 INSERT 후 즉시 FAILED(`OUTPUT_PATH_NM`=null) + ERROR 로그(경로 원문 미노출). allowlist → normalize → `toRealPath` 3단 | security | High | DatasetExportService.java(resolveVideoRoot 가드) |
| TC-EXPORT-016 | **비식별 신고 게이트 = 산출 자체 skip (신규)** | `DE_IDNTF_YN='F'` | 승인/수정/재동결/회수 어느 트리거든 | `export()` 진입부 단일 게이트에서 예외 이탈 → `LS_DATASET_EXPORT` **행 미생성**(FAILED 아님), metric `outcome=deident_blocked` + WARN, **통지도 함께 보류** | security | High | DatasetExportService.java; AsyncDatasetExportRunner.java |
| TC-EXPORT-017 | **쓰기 중 신고 접수 = 마감 차단 + v{n} 폴더 삭제 (신규)** | 산출 도중 신고 커밋 | 마감 직전 | `finalizeUnlessUnderDeidentReport` 가 RAW 잠금 하 재판정 → false → export 행 **삭제** + 이번 실행이 만든 `v{n}` 만 재귀 삭제(전 버전·`deid/`·원본 미영향, 3중 경로가드 통과 시에만) + 예외 이탈로 통지 보류 | security | High | DatasetExportService.java; DatasetExportTxService.java |
| TC-EXPORT-018 | **export 성공 후에만 통지 (신규)** | 승인 경로 | `runApprovalAsync` | `doExport` true 일 때만 `DatasetExportCompletedEvent` 발행(동기 리스너 → export→통지 순서 보장). 실패면 **통지 보류** — 관제가 구 버전 폴더를 픽업하지 않는다 | integration | High | AsyncDatasetExportRunner.java |
| TC-EXPORT-019 | **승인 후 수정 = 재export 후 통지 (신규)** | 디바운스 flush | `runReExportThenNotify` | export 성공 시에만 `afterExport`(통지) 실행. 콜백은 `Runnable` 로 받아 통지 패키지 컴파일 의존 없음 | integration | High | AsyncDatasetExportRunner.java |
| TC-EXPORT-040 | **실패 export 회수 후 통지 재개 (신규)** | FAILED 행 존재, `RTY_NMTM` < 상한 | 회수기 | `claimForRetry` 조건부 UPDATE(2노드 중복 방지) 통과분만 `runApprovalAsync` 재산출 → 성공 시 완료 이벤트 재발행(통지 유실이 아니라 성공 시점으로 지연). 신고 구간 건은 시도 상한 소진 방지를 위해 제외 | integration | High | DatasetExportFailureRecoverer.java; DatasetExportTxService.java |
| ~~TC-EXPORT-041~~ | ~~**신고 해소 시 보류분 복구**~~ **[폐기 2026-08-15]** | ~~`'F'→'Y'` resolve + APPROVED~~ | ~~`DeidentReportResolvedEvent`~~ | **폐기 사유**: [D 회차 16 ⑤](D-review-version-notify.md) 통지 트리거 반전으로 해소 시점의 즉시 force 재생성·통지가 폐기됐다. 해소는 `TaskModifiedEvent(exportRegenerated=true, needsRecheck=true)` 로 **재검토 표시만** 세우고 재생성·통지는 **재승인 시점**에 나간다. `DeidentReportResolvedEvent` 는 발행처 0건 휴면 확장점(코드 실측). 범위가 그 영상 하나라는 것은 불변. 대체 케이스 = [D TC-NOTIFY-059~061](D-review-version-notify.md) | — | — | ~~DatasetExportBridge.java~~ → DeidentReportService.java(`publishResolvedForExportRecovery`) |
| TC-EXPORT-042 | **재export 트리거는 control-notify 토글과 무관 (신규)** | `authoring.control-notify.enabled=false` | 승인 후 수정 | 재export 는 정상 수행됨(토글은 통지 발송만 게이팅). 보장 지점 3곳 — ①축적 리스너가 **무조건 활성**(`@Component` 만, `@ConditionalOnProperty` 없음) ②디바운서 빈도 무조건 활성이며 `send()` 가 `notifyService==null` 여부와 무관하게 `runReExportThenNotify` 호출(통지 콜백만 null) ③반대로 통지 리스너만 토글 종속 | integration | High | TaskModifiedAccumulateListener.java; ControlNotifyDebouncer.java; ControlNotifyEventListener.java (토글 종속은 여기만) |
| TC-EXPORT-043 | **retention 정리 로직 없음 (신규)** | 여러 번 재승인 | 저장소 | `v1..vN` 전 버전 **보존**(삭제 잡 미구현이 확정 정책 — 롤백/복구 요구 충족). 저장소 증폭은 감수 | unit | Med | DatasetExportService.java (TODO 주석) |
| TC-EXPORT-047 | **V173 원장 컬럼 표준용어 정합 (신규)** | 마이그레이션 적용 | `information_schema` | `OUTPUT_SN`·`OUTPUT_PATH_NM`·`OUTPUT_STTS_CD`·`OUTPUT_VER_NO`·`FRME_CNT` 존재 + **구 이름 5종 부재**(rename 이지 컬럼 추가가 아니다). 테이블명 `LS_DATASET_EXPORT` 는 불변, PK/UK 는 제약명에 컬럼이 없어 재생성 없이 참조만 자동 갱신 | integration | High | V173__standardize_ls_dataset_export_columns.sql; V173DatasetExportStandardTermMigrationIT |
| TC-EXPORT-048 | **뷰 본문은 rename 을 추종하고 출력명은 V174 가 명시 지정한다** | V173 + V174 적용 후 | `pg_get_viewdef` + `V_COMPLETED_VIDEO` 컬럼 목록 | ①**본문**은 새 원장 물리명 참조(`output_path_nm`·`frme_cnt`·`output_stts_cd`) = rename 이 뷰를 깨지 않았다는 증거 ②**출력명**은 V174 가 지정한 표준 물리명이고 구 이름(`export_path_nm`·`frame_cnt`·`export_stts_cd`)은 **사라졌다**. ⚠ **구 기대결과 "출력 컬럼명은 구 이름 유지 · 관제 계약면 무변경" 은 V174 로 폐기** — V173 단독 상태에서만 참이었다(PostgreSQL 이 출력명을 자동 별칭으로 보존). 계약면을 실제로 바꾼 것은 V174 다 | integration | **Critical** | V174__rebuild_completed_video_view.sql; V173DatasetExportStandardTermMigrationIT.뷰_본문은_rename_을_추종하고_출력명은_V174_가_명시_지정한다 |
| TC-EXPORT-049 | **동명이표 오염 가드 (신규)** | 전역 치환 시도 | 스키마 + 과거 마이그레이션 원문 | `LS_MON_NOTI_ACML.EXPORT_RPRCS_YN` 불변(현재 실재하는 유일한 동명이표) · DROP 된 `LS_DATA_SET`(V8)·`LS_RESOLUTION_EXPORT`(V55) 의 **과거 SQL 원문**이 오염되지 않음(신규 설치 재생 결과 보존) · `LS_DATA_INGEST.FRME_CNT`(V172) 무영향 | security | High | V173DatasetExportStandardTermMigrationIT (동명이표 4케이스) |
| TC-EXPORT-050 | **DATA_ETBL_CPCT 적재 = 버전 폴더 총 바이트 (신규)** | export 성공/부분 | 마감 | `{영상루트}/v{n}` 하위 정규 파일 바이트 합이 **디스크 실측치와 일치**. 마감(SUCCEEDED/PARTIAL) **같은 트랜잭션**에서 기록 — 별도 UPDATE 로 분리하면 마감됐는데 용량만 빠지거나 차단 분기(행 삭제)와 순서가 꼬인다 | integration | High | DatasetExportService.java(finalize 호출부); DatasetExportTxService.finalizeUnlessUnderDeidentReport; DatasetExportE2EIT.exportRecordCarriesOutputCapacity |
| TC-EXPORT-051 | **용량은 버전 단위이지 영상 누적이 아니다 (신규)** | v1·v2 + `deid/` 영상 공존 | 용량 산출 | 대상 `v{n}` 하위만 합산 — 형제 버전·비식별 영상은 제외. 재승인 v2 의 용량이 v1 을 포함하지 않는다(관제 `dataset_versions` 는 버전 단위 용량) | unit | High | DatasetExportFolderSizeCalculatorTest.excludesSiblingVersionsAndDeidVideo; DatasetExportE2EIT.capacityIsPerVersionNotCumulative |
| TC-EXPORT-052 | **용량 산출 실패해도 export 는 성공 종결 (신규)** | 경로 가드 거부 · 폴더 부재 · I/O 실패 | 용량 산출 | 예외 전파 없이 `null`(=미산출) 반환 + WARN(경로 원문 미출력, CWE-359). outcome 은 그대로 COMPLETED/PARTIAL — **용량 때문에 학습데이터 산출이 실패하면 안 된다**. `RuntimeException` 까지 잡아 호출부 바깥 catch 의 FAILED 오분류를 차단 | security | High | DatasetExportFolderSizeCalculator.java; DatasetExportServiceTest.exportSucceedsEvenWhenCapacityUnavailable |
| TC-EXPORT-053 | **용량 산출 경로 안전 + 심링크 미추종 (신규)** | `v{n}` 자리/하위 심링크 교체 | 용량 산출 | `resolveUnder`(세그먼트 정확 일치) → `resolveRealPathUnder`(실경로 재확인) 통과 후 **그 실경로로** 순회(CWE-22/59/367). 심링크는 따라가지도 합산하지도 않아 폴더 밖 용량이 섞이지 않음. `Files.walk` 는 try-with-resources(CWE-400) | security | High | DatasetExportFolderSizeCalculatorTest.doesNotFollowOrCountSymlinks |
| TC-EXPORT-076 | **★FRME_CNT 산정 = 벌별 쓰기 건수의 최댓값 — 합계가 아니다 (신설 · V185)** | ①원본·비식별 모두 4건 전량 산출 ②원본 5건 전량, 비식별은 원천 부재로 3건만 산출(2건 skip, 부분실패) | `service.export(rawSn)` | ①`FRME_CNT=4`(구 합계 8 은 폐기) ②`FRME_CNT=5`(더 많이 산출된 벌 — 합계 8 도 최소값 3 도 아니다. 한 벌에만 있는 프레임도 산출물이므로 누락하지 않는다). **PARTIAL 판정(스킵>0)은 이 변경과 무관하게 그대로**(벌 합계 `totalSkipped` 기준 유지) — 관제가 `datasets.img_nocs`·`dataset_versions.data_etbl_nocs` 에 이 값을 "추출·라벨링 프레임 수"로 그대로 적재하므로 2벌 합산은 실제 프레임 수의 2배로 부푼다 | unit | **Critical** | DatasetExportService.java(FRME_CNT 는 벌 합계가 아니다 — 클래스 javadoc) · DatasetExportServiceTest(한쪽_벌만_부분실패하면_FRME_CNT_는_더_많이_산출된_벌의_프레임수다) |
| TC-EXPORT-077 | **파생영상 1벌 산출도 FRME_CNT 가 실제 프레임수 N — 일반영상과 같은 축이다 (신설 · V185)** | 파생(증강·해상도) 영상 4프레임, 비식별 1벌만 산출(원본 픽셀 부재로 ORIGINAL 벌 자체가 없음) | `service.export(rawSn)` | `FRME_CNT=4` — 벌 수(1 vs 2)에 따라 값이 갈리지 않는다. **부재한 ORIGINAL 벌이 0 으로 집계돼 결과를 끌어내리면 안 된다**(최솟값을 쓰면 파생영상 전량이 0 으로 적재되는 회귀) | unit | **Critical** | DatasetExportServiceTest(파생영상_1벌_산출도_FRME_CNT_가_실제_프레임수_N_이라_일반영상과_같은_축이다) |
| TC-EXPORT-078 | **승인 산출 e2e — FRME_CNT 가 실제 산출건수와 일치(2벌 합계 아님) (신설 · V185)** | 승인+export 전체 파이프라인, 원본·비식별 두 벌 모두 `FRAME_COUNT` 건 산출 | 승인 → export 완료 | `LS_DATASET_EXPORT.FRME_CNT == FRAME_COUNT`(구 기대값 `FRAME_COUNT * 2` — 원본+비식별 합산 — 는 폐기). 원장 컬럼 COMMENT 도 이 산정 방식으로 재지정됨(V185, DDL 변경 없음 — V173 이 남긴 "원본벌+비식별벌 합계" 서술만 정정) | integration | High | V185__align_control_ingest_contract_and_export_frme_cnt_comment.sql · DatasetExportE2EIT(승인_산출시_LS_DATASET_EXPORT_v1이_SUCCEEDED이고_FRAME_CNT가_산출건수와_일치한다) |

## E-7. Export JSON 포맷 (NiaJsonBuilder / NiaAnnotationDoc / NiaVideo / NiaImage / VideoMetaMapper)

| ID | 케이스명 | 전제 | 입력/조건 | 기대결과 | 계층 | 우선 | 근거(파일) |
|----|---------|------|----------|---------|------|:--:|----------------|
| TC-EXPORT-020 | **★최상위 키는 `event` (rename)** | 동결 `eventAnnotation` JsonNode | build | 프레임 문서 최상위 키가 **`event`**(구 `event_annotation` 폐기, E-ISSUE-44 확정). 내부 필드/클래스명은 `eventAnnotation` 유지. 위치는 `video` 다음 고정. `JsonNode` pass-through 로 키 순서·형태 보존 | unit | High | NiaAnnotationDoc.java |
| TC-EXPORT-021 | event 값 null 처리 | 동결 event 없음 | build | `"event": null` (키는 항상 present — 클래스 `@JsonInclude(ALWAYS)`) | unit | Med | NiaAnnotationDoc.java |
| TC-EXPORT-022 | **image 원천은 정책 상수 — 프레임 수동값이 원천 축을 덮지 않는다 (정정 2026-08-04)** | 프레임 수동 `ANONY_INCL_YN='Y'` | ORIGINAL / DEIDENTIFIED build | ORIGINAL `image` = 정책 상수 `N`/`N`/`Y`(`ORGNL_DEFAULT_*`) — 프레임 수동값은 **비식별 축** 판정이라 원천에 실리지 않는다. DEIDENTIFIED 는 수동값 우선(`Y`). ⚠ 구 기대 *"`ExportKind` 파생 + 수동 override 전면 금지"* 는 **폐기**(2026-08-03 에 deid 수동 우선으로, 2026-08-04 에 원천이 상수 축으로 전환) | unit | High | ExportPrivacyPolicy.java; NiaJsonBuilder.java |
| TC-EXPORT-023 | pseudonymity/privacyIncluded 수동 우선 (정정) | 프레임 psdo/prvc 저장 | DEIDENTIFIED build | 수동값 우선, blank/null 이면 **비식별 기본상수**(`DEID_DEFAULT_*` = `N`/`N`) 폴백(CHAR(1) 공백 패딩도 blank 취급). 구 표현 "파생 폴백" 은 폐기 — 폴백 대상은 파생 로직이 아니라 정책 상수다 | unit | High | ExportPrivacyPolicy.java; NiaJsonBuilder.java |
| TC-EXPORT-024 | **deid 산출 경로 fail-secure** | `deidVideoPath=null` | DEIDENTIFIED build | `dataset.src_path`·`video.filename` **null**(원본 절대경로·파일명 미노출). ⚠ **[정정 2026-08-05 회차 12]** `dataset.name` 은 이제 **경로 파생이 아니라 데이터셋명**(R9-a)이라 경로 미상이어도 정상 값이 나간다 — 구 기대 *"`dataset.name` 도 null"* 은 **폐기**(경로 누수가 아니다. 누수 가드는 `src_path`·`filename` 두 필드) | security | High | NiaJsonBuilder.java(buildDataset); VideoMetaMapper.java(kind 별 경로 선택 + basename) |
| TC-EXPORT-025 | malformed 라벨 skip | 라벨 매핑 예외 | `buildAnnotations` | 해당 1건 skip, `lblSn` 만 로깅, 문서 전체는 정상 생성 | unit | Med | NiaJsonBuilder.java |
| TC-EXPORT-026 | 잉여키 제거/키 유지 정합 | 미보유 필드 | 직렬화 | `@JsonInclude(ALWAYS)` 로 키 유지, 값 null | unit | Med | NiaVideo.java; NiaImage.java |
| TC-EXPORT-027 | **vd_description 필드 존재** | video 블록 | 직렬화 | `vd_description` 키 포함. ⚠ **[정정 2026-08-06 회차 13]** 값은 이제 **VLM 서술 조달값**이다(TC-EXPORT-057~065) — 구 기대 *"값은 null — 데이터 출처 없음"* 은 **폐기**(외부 VLM verify 콜백으로 원천이 생겼다). 원천이 없을 때만 null 이고 키는 항상 유지된다 | unit | Med | NiaVideo.java; VideoMetaMapper.java(toVideo) |
| TC-EXPORT-028 | **weather/time_of_day/season — 수동값만** | raw 수동값 / meta 동결값 | `toVideo` | raw(최신 수동값) → meta(동결값) 우선순위. ★self-fill 제거로 **동결값도 수동값만**(미입력은 null) — `SHT_DT` 파생은 export·동결 경로에서 완전 폐기. blank→null 정규화. `ExportKind` 로 분기하지 않아 2벌 동일 | unit | High | VideoMetaMapper.java(우선순위 근거 주석 + weather/timeOfDay/season 산출); DatasetVideoMetaSnapshotService.java |
| TC-EXPORT-029 | 촬영환경 blank→null 정규화 | 빈 문자열 | `toVideo` | `firstNonBlank` null 정규화(스냅샷 표현과 일치) | unit | Med | VideoMetaMapper.java(적용), (정의) |
| TC-EXPORT-030 | **video 개인정보 3필드 = 원천/비식별 축 분리 (정정 2026-08-04)** | 관제 인입값 존재 + 영상 단위 수동값 존재 | `toVideo` | ORIGINAL = **관제 인입값 `LS_DATA_INGEST.*_INCL_YN`(V166/V170) 그대로**(미송신 필드는 `null` — 지어내지 않는다), DEIDENTIFIED = 영상 단위 수동값 `LS_DATA_RAW.*_INCL_YN`(V163) 우선·미입력 시 `Y`/`N`/`N`. 촬영환경은 2벌 동일(불변). ⚠ 구 기대 *"N/Y 상수 오버라이드"* 폐기 | unit | High | VideoMetaMapper.java; ExportPrivacyPolicy.java |
| TC-EXPORT-044 | **파생영상은 원천 축이 video·image 모두 null (신규)** | `ORGNL_RAW_SN` non-null(증강·해상도 파생) | ORIGINAL build | 인입값도 정책 상수도 싣지 않고 6필드 전부 `null`(키는 유지). 파생 비디오는 부모 **비식별본** 복사라 "비식별 처리 전 원천" 대상이 없다 — 결손이 아니라 정상. 판정 분기는 `SourcePrivacyMeta.sourceExists()`, 조달은 `DatasetExportTxService` 가 영상 행으로 명시(리포지토리 술어와 이중화) | unit | High | SourcePrivacyMeta.java; DatasetExportTxService.java |
| TC-EXPORT-045 | **원천 축 개인정보는 콘텐츠 해시 입력 — 조건부 블록 (신규)** | 관제가 인입 판정을 정정 재송신 | 재동결(force=false) | `SPRV` 블록으로 해시에 편입되어 재산출된다(빠지면 저장은 바뀌었는데 파일이 옛 값으로 고착). **값이 하나도 없으면 append 하지 않아 기존 해시 유지**(도입 이전 승인분 하위호환). `image` 원천값은 정책 상수라 해시 입력이 아니다 | unit | High | LabelContentHasher.java |
| TC-EXPORT-046 | **판정·상수 단일 원천 정적 가드 (신규)** | 프로덕션 소스 스캔 | 아키텍처 테스트 | ①`DEID_DEFAULT_*`/`ORGNL_DEFAULT_*` **정의**는 `ExportPrivacyPolicy` 에만(다른 파일은 참조만) ②`VideoMetaMapper`·`NiaJsonBuilder` 는 3필드를 각각 `resolve*` **3회 호출**로만 산출(리터럴·삼항 인라인 시 실패) ③화면 프리필 2종이 상수를 하드코딩하지 않는다. 주석 스트리핑 후 스캔(오탐 방지) | unit | High | ExportPrivacyPolicySingleSourceGuardTest.java |
| TC-EXPORT-031 | meta null 방어 | meta=null | `prepareContext` | 400("영상 메타가 null 입니다.") | unit | Low | NiaJsonBuilder.java |
| TC-EXPORT-032 | FORMAT_VERSION/info 계약 | 정상 | `prepareContext` | `info.version="1.3"`, `type="instances"` | unit | Low | NiaJsonBuilder.java |
| TC-EXPORT-033 | **image.file_name 은 ExportFileNaming 단일 지점 (신규)** | 프레임 문서 | `buildImage` | `ExportFileNaming.imageFileName(frameNo)` — 구 `frame-{n}.jpg` 직접 조립 금지(같은 폴더에 실재하지 않는 파일을 가리키던 드리프트) | unit | High | NiaJsonBuilder.java |
| TC-EXPORT-034 | **frame_num = VDO_FRM_NO, 미측정이면 null (신규)** | `VDO_FRM_NO` null | `buildImage` | `frame_num=null` — `FRM_NO`(추출 순번) 폴백 금지(의미가 다른 값을 영상 내 위치로 위장) | unit | High | NiaJsonBuilder.java |
| TC-EXPORT-035 | **파생영상 video.filename 은 비식별 사본 (신규)** | 파생 RAW export | DEIDENTIFIED build | 파생은 deid 1벌만 산출되며 `filename`/`dataset.src_path` 는 파생 자신의 비식별 사본 경로 basename. 동결 스냅샷의 `RAW_FILE_PATH_NM` 은 파생에서 null 이므로 ORIGINAL 벌 경로가 실릴 여지가 없다 | integration | High | DatasetVideoMetaSnapshotService.java; DatasetExportService.java |
| TC-EXPORT-036 | **dataset 블록 kind 분기 (신규)** | ORIGINAL/DEIDENTIFIED | `buildDataset` | **`src_path` 만** kind 로 갈린다 — ORIGINAL=`meta.RAW_FILE_PATH_NM`, DEIDENTIFIED=`deidVideoPath`. 비식별 산출물에 원본 경로가 새지 않는다. ⚠ **[정정 2026-08-05 회차 12]** `name` 은 R9-a 이후 **kind 무관**(이벤트명 파생 — TC-EXPORT-054), `identifier` 도 종전대로 kind 무관 | unit | High | NiaJsonBuilder.java(buildDataset) |
| TC-EXPORT-037 | **frm_expln pass-through (신규)** | 프레임 설명 입력 | `buildImage` | `NiaImage` 마지막 필드로 `src.getFrmExpln()` 전달(프레임 설명 수정도 재export 7경로 중 하나) | unit | Med | NiaJsonBuilder.java |
| TC-EXPORT-054 | **dataset.name = 데이터셋명(파일명 아님) (신규 · R9-a)** | 동결 스냅샷 `EVNT_NM`='보행자 통행' / `EVNT_NM` null | `buildDataset`(ORIGINAL·DEIDENTIFIED) | `dataset.name` = **`{이벤트명} 데이터셋 구축`**, **kind 무관 동일 값**. 구 구현은 경로 basename(확장자 제거)을 넣어 `"original"`/`"deidentified"` 라는 **영상 파일명**이 나갔다(사업 어노테이션 표준의 이 필드는 데이터셋 이름). 이벤트명 null 이면 **null**(' 데이터셋 구축' 만 남은 문자열을 지어내지 않는다 — 뷰 SQL 의 NULL 전파와 동일). 뷰 `DATST_NM` 과의 규칙 일치는 [D 카탈로그](D-review-version-notify.md) TC-MARTVIEW-039 가 실 DB 에서 대조 | unit | High | NiaJsonBuilder.java(datasetName · buildDataset) · NiaJsonBuilderTest(dataset_name_이_영상파일명이_아니라_데이터셋명 · 이벤트명이_없으면_dataset_name_은_null_이다) |
| TC-EXPORT-055 | **info.year = 검수완료 연도로 고정(재export 불변) (신규 · R9-b)** | 활성 스냅샷 `RVW_CMPL_DT`=2024-12-31 / `RVW_CMPL_DT` null | `prepareContext` 2회(재export 상당) | `info.year`=**2024**(현재 연도와 다름) 이고 두 번 산출해도 값이 같다. 구 구현 `LocalDate.now().getYear()` 는 연말·연초 재산출에서 값이 갈렸다. 뷰 `DATA_ETBL_YR`(`to_char(RVW_CMPL_DT,'YYYY')`)과 **같은 개념·같은 조달처**이며 미상이면 **null**(지어내지 않음 — `to_char(NULL)` 과 동일). ⚠ `info.date_created` 는 **그대로 now()** — '이 문서를 만든 날짜'라는 다른 개념이라 고정 대상이 아니다 | unit | High | NiaJsonBuilder.java(prepareContext — year 산출) · NiaJsonBuilderTest(재export_해도_info_year_가_바뀌지_않는다 · 검수완료일시가_없으면_info_year_는_null_이다) |
| TC-EXPORT-056 | **video.event_id = 인입 EVNT_ID(유형코드 아님) (신규 · R9-c)** | 인입 `EVNT_ID`='ABA_0001' / 관제 미송신(null) — 두 경우 모두 `EVNT_TYPE_CD`='EV02000102' 존재 | `toVideo` | `event_id`=`ABA_0001`. 구 구현은 `EVNT_TYPE_CD` 를 실었는데 **축이 다른 값**이다(유형=분류 체계 / `EVNT_ID`=개별 이벤트 인스턴스 식별자, 사업 표준 예제 `ABF_0001` 과 성격 일치). **미송신이면 `null` 이고 유형코드로 폴백하지 않는다** — 폴백하면 애초의 결함으로 되돌아간다. 조달은 인입 LATERAL(`IngestSourceLink` 단일 진실원)이며 **파생영상도 부모 인입값을 상속**한다(개인정보 3필드만 원본 한정) | unit | High | VideoMetaMapper.java(toVideo — event_id) · IngestSourceRepository.java(findSourceMeta) · IngestSourceLink.java(SQL_LATERAL_JOIN) · VideoMetaMapperTest(video_event_id_가_인입_EVNT_ID_값 · EVNT_ID_미제공시_event_id_가_null) |
| TC-EXPORT-057 | **vd_description = verify 서술 전문 (신규 · R10)** | `LS_DATA_META['vlm.description']`='보행자가 횡단보도를 건넌다.' | resolve → toVideo → 직렬화 | `video.vd_description` = 그 값 그대로. 구 구현은 **항상 null 하드코딩**(`// vd_description (미보유 — 데이터 출처 없음)`)이었다 — 외부 VLM `verify` 콜백 적재로 원천이 생겨 해소. 조달 규칙의 **단일 소유자는 `VlmDescriptionPolicy`** 이며 매퍼·서비스가 재유도하지 않는다 | unit | High | VlmDescriptionPolicy.java(resolve) · VideoMetaMapper.java(toVideo) · VlmDescriptionPolicyTest(verify_서술이_있으면_그_값이_vd_description에_들어간다) · NiaJsonBuilderTest(verify_서술이_있으면_그_값이_vd_description에_들어간다) |
| TC-EXPORT-058 | **레거시 구간은 `start_sec` 숫자 오름차순 이어붙임 (신규 · R10)** | 전문 없음 + 보존된 레거시 구간 14건(`0-8`·`8-16`…`104-112`, 입력 순서 무작위) | resolve | `start_sec` **숫자** 오름차순으로 개행 이어붙임. ⚠ **문자열 정렬이면 구간 10개 초과 시 `10-18` 이 `8-16` 앞에 온다**(FE 시계열 패널이 겪은 것과 같은 함정) — 파싱해 `long` 으로 정렬한다. 동률 `start_sec` 은 `end_sec` → metaKey 순 3차 정렬로 **결정적**(입력 순서 무관) | unit | High | VlmDescriptionPolicy.java(joinLegacySegments) · VlmDescriptionPolicyTest(레거시_구간만_있으면_start_sec_오름차순으로_이어붙인다 · 같은_start_sec_구간이_여럿이어도_결정적으로_이어붙는다) |
| TC-EXPORT-059 | **전문이 레거시보다 우선 (신규 · R10)** | `vlm.description` + 레거시 구간 **동시 존재** | resolve | 전문이 이긴다(레거시는 무시). **레거시 행은 삭제·마이그레이션하지 않고 보존**하므로 두 원천이 공존하는 것이 정상 상태다. 전문이 blank 면 **다음 순위**로 내려간다(blank=미입력, 프로젝트 공통 규약). ⚠ **[정정 2026-08-06 회차 14]** 조달은 이제 **3단**이다 — 전문 → `manual-timeseries` → 레거시(TC-EXPORT-067~069). 구 기대 *"전문이 blank 면 곧바로 레거시"* 는 **폐기**(사이에 수동 전문이 들어온다) | unit | High | VlmDescriptionPolicy.java(resolve · firstValueOf) · VlmDescriptionPolicyTest(verify_서술과_레거시가_함께_있으면_verify_서술이_우선한다 · 전문이_공백뿐이면_레거시_구간으로_내려간다) |
| TC-EXPORT-060 | **원천 0건이면 빈 문자열이 아니라 null (신규 · R10)** | 메타 0건 / 값이 전부 blank·null | resolve → 직렬화 | `vd_description` **키는 유지**(`@JsonInclude(ALWAYS)`)되고 값은 **`null`**. `""` 로 채우면 *"판정했는데 내용이 없다"* 는 **거짓 사실**이 된다(export JSON 은 재적재되는 왕복 자산). 값을 지어내지 않는다 | unit | High | VlmDescriptionPolicy.java(resolve · normalize) · VlmDescriptionPolicyTest(메타가_없으면_vd_description은_null이다 · 빈_문자열이_아니라_null이다) · NiaJsonBuilderTest(메타가_없으면_vd_description은_null이고_키는_유지된다 · 빈_문자열이_아니라_null이다) |
| TC-EXPORT-061 | **구간형이 아닌 키가 섞여도 깨지지 않는다 (신규 · R10)** | `video.fps`·`vlm.accuracy`·`이상한키`·`-8`·`0-`·`a-b`·11자리 과대 구간 혼재. ⚠ **[정정 2026-08-06 회차 14]** 전제에서 `manual-timeseries` **제거** — 그 키는 무시 대상이 아니라 **조달 우선순위 2**다(TC-EXPORT-067) | resolve | 정규식(`^\d{1,10}-\d{1,10}$`)에 걸리는 구간만 대상. 나머지는 **조용히 무시**하고 **예외를 던지지 않는다** — export 가 이 값 하나 때문에 깨지면 안 된다(CWE-20 fail-safe). 자릿수 10 제한으로 `Long.parseLong` 오버플로를 구조적으로 배제. `null` 원소도 NPE 없이 통과 | security | High | VlmDescriptionPolicy.java(LEGACY_SEGMENT_KEY · matchSegment) · VlmDescriptionPolicyTest(구간형이_아닌_키가_섞여도_깨지지_않는다 · 과대_구간초는_구간키로_보지_않는다 · null_원소가_섞여도_NPE_없이_처리된다) |
| TC-EXPORT-062 | **vd_description 은 orgnl/deid 두 벌이 동일 (신규 · R10)** | 서술 있는 영상 | ORIGINAL / DEIDENTIFIED build | 두 산출물의 `video.vd_description` 이 **같다** — 영상 단위 값이라 `ExportKind` 로 갈리지 않는다(개인정보 3필드와 **다른 축**). 산출 종류로 분기시키지 말 것 | unit | Med | NiaJsonBuilder.java(build — vdDescription pass-through) · NiaJsonBuilderTest(vd_description은_ORIGINAL과_DEIDENTIFIED_두벌이_동일하다) |
| TC-EXPORT-063 | **accuracy 는 export 에 없다 (R10/R12)** | `vlm.accuracy` 적재된 **과거** 영상 | 프레임 문서 직렬화 | 문서 어디에도 `accuracy`/`vlm_accuracy` 키가 없다. 일치도는 **화면 전용**이며 검수큐 미진입이라 `V_COMPLETED_META` 에도 구조적으로 도달하지 않는다 — **의도된 설계이므로 "마트/파일에 안 나온다"를 결함으로 보고하지 말 것**. ⚠ **전제 정정(2026-08-24)** — 판정 창구를 연동하지 않게 되어 이 키는 **새로 생기지 않는다**. 케이스는 과거 적재분을 대상으로 계속 유효하다(기존 행을 지우지 않으므로 제외 규칙이 여전히 필요하다) | unit | High | VlmResultService.java(META_KEY_ACCURACY — 과거 적재분 전용 상수) · NiaVideo.java(필드 부재) · NiaJsonBuilderTest(accuracy는_export에_포함되지_않는다) |
| TC-EXPORT-064 | **서술이 바뀌면 해시가 달라진다 — 멱등 skip 차단 (신규 · R10)** | 같은 라벨·프레임·메타, 서술만 상이 | `LabelContentHasher.hash` | 해시가 **달라진다**(조건부 블록 마커 `VDSC`). **하위호환** — 서술이 없거나 blank 면 아무것도 append 하지 않아 **기존 승인분 해시가 유지**된다(전량 재산출 방지). ⚠ **[정정 2026-08-06 회차 14]** 구 기대의 *"빠지면 재동결(force=false) 경로가 멱등 skip 된다"* 는 **현재 배포 형상에서 성립하지 않는다** — 해시가 게이트하는 유일한 지점 `DatasetExportService.export` 의 `!forceRegenerate && isUnchangedFromLastExport()` 에 `force=false` 로 진입하는 프로덕션 경로가 **0건**이기 때문이다(유일 후보 `DatasetExportBridge.onReExport` 가 소비하는 `DatasetReExportEvent` 는 발행처 0 인 휴면 리스너, 나머지는 전부 `force=true`). 블록은 **미래 정합용으로 유지**하되 무변경 재생성 억제는 **호출부**가 책임진다(TC-EXPORT-071) | unit | Med | LabelContentHasher.java(appendVdDescription · VD_DESCRIPTION_MARKER) · DatasetExportService.java(export — forceRegenerate 게이트) · DatasetExportBridge.java(onReExport — 휴면) · LabelContentHasherTest(VLM_서술이_바뀌면_해시가_달라진다 · VLM_서술이_없으면_기존_해시가_유지된다) |
| TC-EXPORT-065 | **승인 후 서술 변경은 export 재생성을 동반 (신규 · R10)** | APPROVED 영상 / ①재위탁 콜백으로 서술 변경 ②REVIEWER 가 `vlm.description`·레거시 구간 수정 ③조달과 무관한 키(`event` 등) 수정 | `TaskModifiedEvent` 발행 | ①②는 `exportRegenerated=**true**` → 디바운스 flush 가 `runReExportThenNotify(force=true)` 로 **export 전량 재생성 후 통지**(순서 직렬화). ③은 **false** — 디스크가 그대로인데 재생성을 걸면 관제가 안 바뀐 파일 수천 개를 헛 재픽업하고 저장소도 증폭된다. 참여 판정은 `VlmDescriptionPolicy.participates` **단일 소유자**(키 규칙 복제 금지). ⚠ 구 기대 *"R13 은 `exportRegenerated=false`"* 는 **폐기**(서술이 export 입력이 되면서 디스크가 실제로 달라진다). ⚠ **[정정 2026-08-06 회차 14]** ③의 예시였던 `manual-timeseries` 는 **조달 참여 키로 승격**돼 `true` 다(TC-EXPORT-070) — 무관 키 예시를 `event` 로 교체했다. 또 참여 키라도 **값이 실제로 바뀌어야** `true` 다(TC-EXPORT-071) | unit | High | VlmResultService.java(recheckIfApproved) · MetaService.java(update — regeneratesExport 판정) · VlmDescriptionPolicy.java(participates) · VlmResultServiceTest(서술이_바뀌면_재승인시_산출물이_갱신된다) · MetaServiceTaskModifiedGuardTest(서술_전문_수정은_export_재생성을_동반한다 · 레거시_구간_수정도_export_재생성을_동반한다 · 조달과_무관한_키는_값이_바뀌어도_재생성하지_않는다) |
| TC-EXPORT-066 | **파생영상은 자기 rawSn 메타만 본다 — 부모 폴백 없음 (신규 · R10)** | 자기 `LS_DATA_META` 0건인 영상 | `loadPreparation` | `metaRepository.findByRawSn(자기 rawSn)` **1회**만 조회하고 부모를 뒤지지 않는다 → `vd_description`=`null`. ⚠ **[근거 주석 정정 2026-08-06 회차 14]** *"파생은 자기 메타가 없으면 null"* 이라는 구 주석은 **오해를 부른다** — `DerivedMetaCopier.copyMetaAndReviews` 가 생성 시점에 부모 메타를 **키 필터 없이 물리 복사**하므로 파생은 자기 rawSn 행으로 부모 서술을 **실제로 갖는다**(파생 비디오가 부모 비식별본의 복사본이라 서술도 유효 — 산출 결과는 정합). 여기서 폴백을 두지 않는다는 것은 **조회 시점 부모 재해석을 하지 않는다**(스냅샷 시맨틱 — 이후 부모 정정은 재전파되지 않음)는 뜻이며, 실제로 복사가 없던 파생만 null 이다. 조달값은 **해시와 컨텍스트 양쪽**에 같이 실린다 — 한쪽만 실으면 stale 고착 또는 멱등 판정 불일치 | unit | High | DatasetExportTxService.java(loadPreparation — vdDescription 조달) · DerivedMetaCopier.java(copyMetaAndReviews) · DatasetExportTxServiceTest(자기_rawSn_메타의_서술이_해시와_컨텍스트에_함께_실린다 · 메타가_없으면_서술은_null로_전달된다) |
| TC-EXPORT-067 | **`manual-timeseries` 전문이 vd_description 에 실린다 — 조달 우선순위 2 (신규 · R10)** | 전문(`vlm.description`) 없음 + `manual-timeseries`='작업자가 직접 쓴 상황묘사' | resolve → toVideo | `video.vd_description` = 그 값. FE 시계열 패널은 **편집 가능한 항목이 하나도 없을 때**(메타 0건 **또는 레거시 구간뿐**) 이 키로 신규 등록 슬롯을 띄우므로, 사람이 그 칸에 쓴 전문이 곧 상황묘사다. 구 구현은 이 키를 조달에서 빠뜨려 **조용한 손실**이었다 | unit | High | VlmDescriptionPolicy.java(resolve · MANUAL_TIMESERIES_META_KEY) · VlmDescriptionPolicyTest(manual_timeseries로_작성한_전문이_vd_description에_들어간다) |
| TC-EXPORT-068 | **`manual-timeseries` 가 레거시보다 우선 (신규 · R10)** | `manual-timeseries` + 레거시 구간 **동시 존재** | resolve | 수동 전문이 이긴다. 근거: 수동 전문은 **사람이 직접 쓴 것**이고 레거시 구간은 **폐기된 describe 자동 산출물**이다. 레거시가 이기면 사람이 방금 쓴 전문이 무시되고 **편집조차 불가능한 옛 구간 이어붙임**이 나간다(FE 는 레거시뿐인 영상에도 수동 슬롯을 띄운다) | unit | High | VlmDescriptionPolicy.java(resolve) · VlmDescriptionPolicyTest(manual_timeseries와_레거시가_함께_있으면_manual이_우선한다) |
| TC-EXPORT-069 | **`vlm.description` 이 `manual-timeseries` 보다 우선 · manual blank 면 레거시로 (신규 · R10)** | ①두 키 공존 ②`manual-timeseries`='   '(blank) + 레거시 | resolve | ①`vlm.description` 이 이긴다 — FE 는 전문 키가 있으면 수동 슬롯을 **아예 띄우지 않아** 정상 상태에서 둘은 공존하지 않는다. 공존하면 과거 데이터이며 현행 편집 대상인 전문이 최신이다 ②blank 는 **미입력**으로 다뤄 다음 순위(레거시)로 내려간다(전문 키와 동일 규약) | unit | High | VlmDescriptionPolicy.java(resolve · normalize) · VlmDescriptionPolicyTest(vlm_description과_manual이_함께_있으면_vlm_description이_우선한다 · manual_timeseries가_공백이면_레거시로_내려간다) |
| TC-EXPORT-070 | **`manual-timeseries` 키 수정도 export 를 재생성한다 (신규 · R10)** | APPROVED 영상 / REVIEWER 가 `manual-timeseries` 값 변경 | `TaskModifiedEvent` 발행 | `exportRegenerated=**true**`. ⚠ 구 기대 *"`manual-timeseries` 는 조달 무관 키라 false"*(TC-EXPORT-065 ③)는 **폐기** — 조달 우선순위 2 로 승격돼 그 값이 산출물에 실린다. false 로 두면 **저장은 됐는데 산출물이 옛 전문으로 고착**된다 | unit | High | MetaService.java(update — regeneratesExport 판정) · VlmDescriptionPolicy.java(participates) · VlmDescriptionPolicyTest(조달에_참여하는_키만_participates_true_다) · MetaServiceTaskModifiedGuardTest(manual_timeseries_키_수정은_export를_재생성한다) |
| TC-EXPORT-071 | **★값이 바뀌지 않으면 재생성하지 않는다 (신규 · R10 · CWE-770)** | APPROVED 영상 / `vlm.description` 을 **기존과 동일한 값**으로 N회 저장 | `TaskModifiedEvent` 발행 | `exportRegenerated=**false**`. 키만 보고 판정하면 재생성이 `ControlNotifyDebouncer` 를 거쳐 **`force=true`** 로 위임되므로 해시 멱등 skip(`isUnchangedFromLastExport`)도 **타지 못해**, `v2·v3·v4…` 가 **이미지 2벌 전량 복사와 함께** 쌓이고(전 버전 보존 정책이라 삭제도 안 된다) 관제 통지도 매번 나간다. 최종 조건 = **(조달 참여 키) AND (값이 실제로 변경됨)** — 웹훅 경로의 `changed` 가드와 동형. FE dirty 체크에 의존하지 않는다(클라이언트가 임의 payload 를 보낼 수 있다) | unit | High | MetaService.java(update · upsertItem — 옛 값 비교) · VlmResultService.java(applyResults — changed 가드) · MetaServiceTaskModifiedGuardTest(값이_바뀌지_않으면_export를_재생성하지_않는다) |
| TC-EXPORT-072 | **값이 바뀐 항목이 하나라도 있으면 재생성한다 (신규 · R10)** | APPROVED 영상 / 한 요청에 **무변경 항목 + 실제 변경 항목** 혼재 | `TaskModifiedEvent` 발행 | `exportRegenerated=**true**`. 항목별 OR 판정이라 **하나라도 바뀌면** 산출물이 달라진다 — 전량 무변경일 때만 억제한다(과소 억제로 stale 고착이 나면 안 된다) | unit | High | MetaService.java(update — 항목별 OR) · MetaServiceTaskModifiedGuardTest(값이_바뀐_항목이_하나라도_있으면_재생성한다) |
| TC-EXPORT-073 | **조달 키의 신규 등록도 재생성한다 (신규 · R10)** | APPROVED 영상 / `manual-timeseries` 가 **없던 상태**에서 최초 등록 | `TaskModifiedEvent` 발행 | `exportRegenerated=**true**` — 값이 없다가 생긴 것도 산출 내용 변경이다. 신규 등록 경로(검토행 PENDING 생성)에서도 억제되지 않아야 한다(옛 값 비교의 경계 케이스) | unit | High | MetaService.java(upsertItem — before.isEmpty()) · MetaServiceTaskModifiedGuardTest(조달_키의_신규_등록은_재생성한다) |
| TC-EXPORT-074 | **값 무변경 저장도 200 으로 성공하고 통지 자체는 발행한다 (신규 · R10)** | APPROVED 영상 / 시드와 동일한 값으로 `PUT /v1/frames/{srcSn}/meta` | HTTP 응답 + 이벤트 | **200 OK** + 응답 body 에 저장값. 억제 대상은 **재생성 플래그뿐**이며 저장·통지(`ChangeType.META_UPDATED`) 계약은 그대로다 — 무변경 저장을 오류(4xx)로 바꾸면 화면 저장 동선이 깨진다 | integration | High | MetaController.java(update) · MetaService.java(update) · MetaControllerTest(값_무변경_저장도_200으로_성공한다) · MetaServiceTaskModifiedGuardTest(값_무변경_저장도_통지_자체는_발행한다) |
| TC-EXPORT-075 | **`manual-timeseries` 키 문자열 단일 원천 (신규 · R10)** | 프로덕션 소스 전체(주석 스트리핑) | 정적 스캔 | 리터럴 `"manual-timeseries"` 는 **`VlmDescriptionPolicy` 한 파일에만** 존재한다(다른 파일은 상수 참조). 복제되면 조달 우선순위 2 가 그 지점에서 갈린다 — 이 저장소의 반복 결함. **공허한 성공 방지**로 "정책 파일이 실제로 그 키를 보유한다"를 함께 고정한다. FE 미러(`features/auto/metaKeys.ts`)는 언어 경계라 스캔 대상이 아니다 | security | High | VlmDescriptionPolicy.java(MANUAL_TIMESERIES_META_KEY) · VlmDescriptionPolicySingleSourceGuardTest(수동_전문_슬롯_키_리터럴은_정책_파일에만_있다 · 정책_파일이_실제로_수동_전문_슬롯_키를_보유한다) |

## E-8. 촬영환경 메타 (EnvironmentMetaService — 영상 단위)

| ID | 케이스명 | 전제 | 입력/조건 | 기대결과 | 계층 | 우선 | 근거(파일) |
|----|---------|------|----------|---------|------|:--:|----------------|
| TC-META-001 | 조회 — 수동값 우선 프리필 | 수동 저장값 존재 | GET environment-meta | 저장값 + `source=MANUAL` | integration | High | EnvironmentMetaService.java |
| TC-META-002 | 조회 — 파생 프리필(화면 전용) | 수동값 없음, `SHT_DT` 존재 | GET | `timeOfDay`/`season` 파생 + `source=DERIVED`, `weather=null`. ★조회 프리필은 **유지**(응답에 출처를 함께 내려 투명) — 폐기된 것은 **동결·산출 경로의 파생**이다 | unit | High | EnvironmentMetaService.java; DatasetVideoMetaSnapshotService.java |
| TC-META-003 | PUT 전체 교체 저장 | 3필드 전송 | update | `changeShootingEnvironment` dirty checking — 3필드만 UPDATE(배치 컬럼 미간섭) | integration | High | EnvironmentMetaService.java |
| TC-META-004 | **null 필드 = 수동값 삭제** | weather만 값 | PUT | 나머지 수동값 삭제 → 조회 시 파생 폴백 | unit | High | EnvironmentMetaService.java |
| TC-META-005 | 허용값 화이트리스트 weather | "폭우" 등 | PUT | 400 — 맑음/흐림/비/눈/안개만 | security | High | ShootingEnvironmentVocabulary.java |
| TC-META-006 | 허용값 timeOfDay/season | DAY/NGT · 4계절 외 | PUT | 400 | unit | High | ShootingEnvironmentVocabulary.java |
| TC-META-007 | 길이 상한 | 허용값 초과 길이 | PUT | 400(허용값이 모두 컬럼 길이 이내라 화이트리스트 검사에 함께 걸림) | unit | Med | EnvironmentMetaService.java |
| TC-META-008 | **APPROVED 후 수정 재동결 + 통지** | 검수완료 영상 수정 | PUT | 동결 스냅샷 재동결(`RVW_CMPL_DT` **승계** — 편집 시각으로 덮지 않음) + `TaskModifiedEvent(META_UPDATED, exportRegenerated=true)` | integration | High | EnvironmentMetaService.java |
| TC-META-009 | ~~재동결 시 export 미재생성~~ | APPROVED 수정 | PUT | **[폐기 2026-07-30]** ★정책 반전(C-1b, 2026-07-27 확정) — 재동결만 하고 파일이 옛 촬영환경으로 남으면 "데이터마트 학습데이터셋 동기화" 요구가 미충족. 이제 **새 버전 폴더로 전량 재생성**한다 → 대체는 TC-META-017 | integration | — | EnvironmentMetaService.java |
| TC-META-010 | 미검수 영상 수정 — 재동결·통지 없음 | PENDING | PUT | 재동결·통지 모두 미수행(이후 최초 승인의 materialize 가 수동값을 캡처) | unit | Med | EnvironmentMetaService.java |
| TC-META-011 | 재동결 시 활성 스냅샷 부재 fail-safe | APPROVED인데 스냅샷 없음 | PUT | skip(warn), 예외 없음 | unit | Low | EnvironmentMetaService.java |
| TC-META-012 | **동시성 — env저장 vs 승인 materialize** | PUT 과 approve 동시 | PUT | `videoRepository.flush()`(raw 행 락) → `acquireRawLock(rawSn)`(advisory) **순서 고정** → 상태 판정. 잠금 순서 단방향(교착 없음), 양방향 창 폐쇄 | integration | High | EnvironmentMetaService.java |
| TC-META-013 | WORKER 본인배정 아닌 영상 | 타 영상 | GET/PUT | 403(`LabelAccessGuard.verifyRawAccess` — IDOR) | security | High | EnvironmentMetaService.java |
| TC-META-014 | 미인증/포털 채널 | null / PORTAL | 요청 | 401 / 채널 격리 403 | security | High | EnvironmentMetaController.java |
| TC-META-015 | 영상 미존재 | 미존재 rawSn | GET/PUT | 404 | unit | Med | EnvironmentMetaService.java |
| TC-META-016 | 로그 PII 미출력 | 허용값 외 입력 | PUT | 필드명만 로깅, 입력 원문·PII 미노출(CWE-117/209) | security | Med | EnvironmentMetaService.java |
| TC-META-017 | **APPROVED 후 수정 = export 새 버전 전량 재생성 (신규)** | 검수완료 영상 촬영환경 수정 | PUT | `exportRegenerated=true` → 디바운스 flush 가 **export(force=true) 먼저** 수행 후 통지 → `v{n+1}` JSON `video.weather/time_of_day/season` 에 새 값 반영 | integration | High | EnvironmentMetaService.java; AsyncDatasetExportRunner.java |
| TC-META-018 | **★동결값은 수동값만 (self-fill 폐기) (신규)** | 수동값 미입력 + `SHT_DT` 존재 | 승인 materialize | `DAY_NGT_CD`/`SESN_CD`/`WTHR_NM` **모두 null 동결** — `SHT_DT` 규칙 파생 폐기(여름 18:00 이 NGT 로 오분류되던 실증). blank 는 null 로 정규화해 해시 표현 일치 | integration | High | DatasetVideoMetaSnapshotService.java |
| TC-META-019 | **레거시 파생 동결값 정정 백필 — dry-run (신규)** | NGT/SUMMER 파생 동결 행 잔존 | `GET /v1/dev/dataset-video-meta/shooting-env-correction-targets` | 200, 대상 건수만 반환. **재동결·export 재생성·통지 미발생**. REVIEWER 전용 + `@Profile("!prd")` | integration | High | DatasetVideoMetaBackfillDevController.java (GET) |
| TC-META-020 | **레거시 정정 백필 — 실행 + 상한 + 멱등 (신규)** | 대상 다수 | `POST /v1/dev/dataset-video-meta/shooting-env-corrections` | 1회 실행당 `env-correction.max-per-run`(기본 200) 상한, 응답에 `corrected`+`remaining`. 정정된 영상은 판별식에서 빠져 재호출 시 0건(자연 멱등). 정정 1건마다 재동결 → `TaskModifiedEvent` → export 재생성 | integration | High | DatasetVideoMetaBackfillDevController.java (POST); DatasetVideoMetaBackfillService.java |

## E-9. 프레임 개인정보 메타 (FramePrivacyMetaService — 프레임 단위)

> **★ 2026-08-03 정책 반전 반영 (3차 검증 회차 정정, 커밋 `0d290c4e` / V161·V163)** — 이 절의 기대결과는 아래 확정 정책을 기준으로 한다(`CLAUDE.md` "개인정보 보호" 절 · `ExportPrivacyPolicy` 클래스 주석):
> ① **프리필 원천 = `ExportPrivacyPolicy` 비식별 기본상수(Y/N/N)**. 구 `PRVC_TYPE_CD=='ANONY'→Y` 파생은 **폐기**(업로드가 `PRVC` 고정이 되면서 신규 영상이 전부 `anonymity=N` 을 보여주던 결함).
> ② **export 반영 = 정확한 반전**. `ORIGINAL` 산출물은 3필드 **모두 null**(판정하지 않음), `DEIDENTIFIED` 산출물만 **수동값 우선**(미입력 시 Y/N/N). 구 정책("`ORIGINAL` 한정 override / `DEIDENTIFIED` 는 수동값 무시")은 폐기 — **되살리지 말 것**.
> ③ **입도 2축**: `video` 블록 ← 영상 단위 수동값(`LS_DATA_RAW.*_INCL_YN`, V163, `VideoPrivacyMetaService`) / `image` 블록 ← 프레임 단위 수동값(`LS_DATA_SRC.*_INCL_YN`, V130, 이 절). 두 값이 달라도 모순이 아니다.
> ④ **저장(PUT)은 비식별 신고 구간(`DE_IDNTF_YN='F'`)에서 412** — 단건·벌크 모두. 조회(GET)는 차단하지 않는다.
> ⚠ 영상 축 API(`GET/PUT /v1/videos/{rawSn}/privacy-meta`)는 **이 절의 범위 밖**이며 아직 전용 케이스가 없다(E-ISSUE-126).

| ID | 케이스명 | 전제 | 입력/조건 | 기대결과 | 계층 | 우선 | 근거(파일) |
|----|---------|------|----------|---------|------|:--:|----------------|
| TC-META-030 | **조회 — 저장값 우선 / 미저장 시 정책 기본상수 프리필 (정정)** | 저장값 유무 | GET privacy-meta | 저장값 우선(`MANUAL`), 미저장 시 `ExportPrivacyPolicy.DEID_DEFAULT_*`(`Y`/`N`/`N`)를 **참조**해 프리필 + `*Source=DERIVED`. ⚠ 구 기대 *"`PRVC_TYPE_CD=ANONY→Y` 파생"* 은 **폐기**(2026-08-03 — 그 파생이 화면 `N` ↔ export `Y` 불일치의 원인이었다). ⚠ 2026-08-04 적재 기본값 도입으로 **신규 프레임은 항상 저장값을 가져 `MANUAL`** 이며, 프리필이 타는 경로는 **레거시 프레임뿐**(안전망으로 존치). ⚠ **2026-08-07 정정** — `*Source` 는 이때까지 **응답 계약에 없어 화면이 받을 수 없었고**(0d290c4e 커밋 메시지가 "프레임 패널의 항목별 출처 뱃지 부재 — 응답 계약 변경 필요"로 미해소 등재), 이제 응답 3필드로 실제 내려간다 → TC-META-048~050 | integration | High | FramePrivacyMetaService.java(`toEffective` · `source`) · FramePrivacyMetaServiceTest.java(`저장값없으면_비식별_기본상수_프리필` · `수동저장후_재조회유지`) |
| TC-META-031 | 단건 PUT 전체 교체 | 3필드 | update | `updatePrivacyMeta`, null=수동값 삭제 | integration | High | FramePrivacyMetaService.java |
| TC-META-032 | 값 화이트리스트 `^[YN]$` | "1"/"true" | PUT | 400(`@Pattern`) | security | High | FramePrivacyMetaUpdateRequest.java |
| TC-META-033 | path/body srcSn 불일치 | 상이 | PUT | 400(CWE-345) | security | High | FramePrivacyMetaController.java |
| TC-META-034 | **프레임 수동값은 비식별 축만 덮는다 (정정 2026-08-04)** | 프레임 `ANONY_INCL_YN='Y'` | export 대조 | `deid` 벌 `image.anonymity="Y"`(수동값 반영) / `orgnl` 벌은 정책 상수 `N` 유지 — 축이 섞이지 않는다(TC-EXPORT-022 와 쌍). ⚠ 구 기대 *"수동값은 화면·기록용만이고 export 는 `ExportKind` 파생 유지(전면 미덮음)"* 는 **폐기** | unit | High | ExportPrivacyPolicy.java; NiaJsonBuilder.java |
| TC-META-035 | 벌크 저장 N+1 제거 | 다수 프레임 | PUT /privacy-meta | `findAllById` 1회 + rawSn당 인가 1회 + `saveAll` 1회 | integration | Med | FramePrivacyMetaService.java |
| TC-META-036 | 벌크 미존재 프레임 404 | 미존재 srcSn 포함 | 벌크 PUT | 404(전체 롤백) | unit | High | FramePrivacyMetaService.java |
| TC-META-037 | 벌크 타 영상 403 | 타 배정 rawSn 포함 | 벌크 PUT | 403(404先→403後 순서 보존) | security | High | FramePrivacyMetaService.java |
| TC-META-038 | 벌크 원자성 | 부분 실패 | 벌크 PUT | `@Transactional` 전체 롤백 | integration | Med | FramePrivacyMetaService.java |
| TC-META-039 | **APPROVED 후 수정 = 재export + 통지 디바운스** | 검수완료 다프레임 | 벌크 PUT | 프레임별 `TaskModifiedEvent(META_UPDATED, exportRegenerated=true)` → rawSn 1회 코얼레스 → export(force) 먼저, 그 뒤 통지 | integration | High | FramePrivacyMetaService.java |
| TC-META-040 | 벌크 항목 수 초과/빈 목록 | 초과·empty | 벌크 PUT | 400 | unit | Med | FramePrivacyMetaController.java; FramePrivacyBulkRequest.java |
| TC-META-041 | WORKER 본인배정/미인증 | 타 프레임·null | GET/PUT | 403/401 | security | High | FramePrivacyMetaService.java |
| TC-META-042 | 로그 판단값 미출력 | 저장 | PUT | `srcSn`·`rawSn` 만 로깅(CWE-359) | security | Med | FramePrivacyMetaService.java |
| TC-META-043 | **단건 수정도 재export 트리거 (신규)** | APPROVED 영상 프레임 1건 | 단건 PUT | `exportRegenerated=true` 로 `TaskModifiedEvent` 발행 → 새 버전 폴더 전량 재생성 후 통지(재export 7경로 중 "개인정보 메타 수정") | integration | High | FramePrivacyMetaService.java |
| TC-META-044 | **파생영상 프레임의 개인정보 3필드 상속 (정정 2026-08-04)** | 부모 프레임 anony/psdo/prvc 입력 | 파생 확정 | 증강·해상도 파생 프레임이 부모 값을 복사. ⚠ **부모가 미입력(레거시)이면 파생은 `null` 이 아니라 적재 기본값 `Y`/`N`/`N`** — 구 기대 *"부모 null 이면 파생도 null(파생 폴백)"* 폐기. export 산출값은 프리필 상수와 같아 파일 내용은 불변. 두 경로 동일 규칙 | integration | Med | LsDataSrc.java; ResolutionPersistService.java; AugmentExtractPersist.java |
| TC-META-045 | **비식별 3필드는 INSERT 시점에 실제 값으로 적재 (신규)** | 신규 영상·프레임 생성 | 팩토리/`@Builder` | `LsDataRaw`·`LsDataSrc` 모두 `Y`/`N`/`N` 실적재(상수는 `ExportPrivacyPolicy.DEID_DEFAULT_*` **참조**, 복제 금지). ⚠ **DB 컬럼 DEFAULT 로는 불가** — 두 엔티티에 `@DynamicInsert` 가 없어 Hibernate 가 모든 컬럼을 명시 INSERT 하므로 DEFAULT 가 주 적재 경로에 적용되지 않는다(인입 `LS_DATA_INGEST` 는 관제가 직접 INSERT 라 DB DEFAULT 가 유일 수단 — V170). 기존 행 백필 없음 | unit | High | LsDataRaw.java; LsDataSrc.java |
| TC-META-046 | **신고 구간 개인정보 PUT 412 — 영상·프레임 양축 (신규)** | `DE_IDNTF_YN='F'` | `PUT /v1/videos/{rawSn}/privacy-meta` · `PUT /v1/frames/{srcSn}/privacy-meta` · 벌크 | 412 PRECONDITION_FAILED(양쪽 모두, 역할 무관). GET 은 차단하지 않는다. 촬영환경 PUT 은 대상 아님. ⚠ 게이트 **근거만 교체**됨(2026-08-04) — 구 근거 "신고가 리셋한 판정을 되돌릴 수 있어서" 는 리셋 폐기로 무효, 현행 근거는 "잘못된 비식별본 위의 판정을 새로 쓰면 resolve 후 관제로 나간다". **게이트 자체는 제거 금지** | security | High | LabelAccessGuard.java; VideoPrivacyMetaService.java; FramePrivacyMetaService.java |
| TC-META-047 | **영상 축 ↔ 프레임 축 입도 차이 허용 (신규 2026-08-03)** | `LS_DATA_RAW` 수동값 ≠ 해당 프레임 `LS_DATA_SRC` 수동값 | export `deid/*.json` | `video` 블록은 영상 단위 값, `image` 블록은 프레임 단위 값을 각각 싣는다 — 두 값이 달라도 **모순이 아니라 입도가 다른 두 사실**("영상 어딘가엔 있지만 이 프레임엔 없다"). 구 "DEIDENTIFIED 는 수동 override 를 보지 않는다" 억제는 폐기. ⚠ 단 역방향(`image=Y` / `video=N`) 은 정책이 성립 불가로 규정한 조합인데 영상 축 미입력 시 기본상수 N 으로 실제 발생한다 → E-ISSUE-122 | integration | High | export/json/VideoMetaMapper.java(입도 근거 주석 + video 3필드 산출); export/json/NiaJsonBuilder.java(image 3필드 산출) |
| TC-META-048 | **★조회 응답에 `*Source` 3필드 신설 — 영상 축과 동일 계약 (신규 2026-08-07)** | 프레임 1건 | `GET /v1/frames/{srcSn}/privacy-meta` | 응답에 `anonymitySource`·`pseudonymitySource`·`privacyIncludedSource` 가 포함되고 값은 `MANUAL`/`DERIVED`. **어휘는 영상 축 상수를 참조**한다(`VideoPrivacyMetaResponse.SOURCE_MANUAL`/`SOURCE_DERIVED`) — 리터럴을 재선언하면 두 축이 다른 문자열을 내려 FE 가 한 벌로 처리하지 못한다. 구 응답은 4필드(`srcSn`+값 3개)뿐이라 **화면이 프리필과 사람의 판정을 구분할 수 없었다** | integration | High | FramePrivacyMetaResponse.java · FramePrivacyMetaService.java(`source`) · FramePrivacyMetaServiceTest.java(`출처_어휘는_영상축과_동일`) |
| TC-META-049 | **출처는 필드별로 판정된다 (신규 2026-08-07)** | 3필드 중 **일부만** 수동 저장(예 익명만 저장) | GET | 저장한 필드만 `MANUAL`, 나머지는 `DERIVED` — 레코드 단위가 아니라 **필드 단위**다. 판정 술어는 프리필 폴백 조건과 **같은 술어**(`null` 또는 공백이면 미저장)여야 값과 출처가 어긋나지 않는다 | unit | High | FramePrivacyMetaService.java(`toEffective` · `isPresent`) · FramePrivacyMetaServiceTest.java(`출처는_필드별_판정` · `프리필은_DERIVED_출처` · `수동저장값은_MANUAL_출처`) |
| TC-META-050 | **하위호환 — 추가만 (신규 2026-08-07 · 회귀 가드)** | 기존 소비자 | GET | 기존 4필드(`srcSn`·`anonymity`·`pseudonymity`·`privacyIncluded`)의 **이름·타입·시맨틱 불변**이고 3필드가 **추가**됐을 뿐이다(`rules/api-design.md` — 응답 필드 추가만 하위호환, 삭제·개명·타입 변경은 breaking). ⚠ **FE 규약은 함께 지켜져야 한다** — 사용자가 직접 고르지 않은 `DERIVED` 필드를 그대로 PUT 으로 되돌려 보내면 **기본상수가 사람의 판정으로 승격**되고 서버는 출처를 알 수 없어 막지 못한다(OpenAPI `description` 에 명시) | integration | High | FramePrivacyMetaResponse.java · FramePrivacyMetaController.java(조회 `@Operation` description) |

> **신고 시 3필드 리셋 폐기(2026-08-04)의 E 카탈로그 파급**: 리셋 자체의 검증은 **B 카탈로그 TC-DEID-058**(보존 확인) + 폐기 표기 TC-DEID-035/052 · C 카탈로그 TC-LABEL-90/~~97~~ 이 담당하므로 여기서는 중복 케이스를 만들지 않는다. E 에서 달라지는 것은 위 TC-META-030(프리필이 타는 경로 축소) · TC-META-044(부모 미입력 시 적재 기본값) · TC-META-046(412 게이트 근거 교체, 게이트 유지)뿐이다.

---

> **불확실 항목 해소 (이번 회차)**
> - **#16 `ExternalAugmentClient` 실연동** → **해소**. `HttpExternalAugmentClient`(`POST /api/genai/jobs`, 202 → 외부 job_id) 가 `mode=http`(기본) 에서 활성, `NoopExternalAugmentClient` 는 `mode=noop` 전용. `syncDecision` 은 양 구현 모두 no-op(외부 계약 미정의).
> - **#17 `AsyncAugmentFrameRunner`** → **해소**. A(`AugmentExtractSnapshot`, REQUIRES_NEW) / B(`AugmentFrameProducer`, 트랜잭션 밖 파일 I/O — 외부 산출물 **반입**, 구 ffmpeg 재추출 폐기) / C(`AugmentExtractPersist`, REQUIRES_NEW) 3빈 분리. 실패 시 cleanup + RAW FAILED + aug dead-letter.
> - **#18 `DerivedMetaCopier`** → **해소**. `video.*` 포함 **전체 복사**가 정상(파생 비디오는 부모 비식별본 복사라 재인코딩 없음) · 부모에 검수행이 있던 메타키만 **PENDING** 검수행 신설 · 배치 upsert 라 **확정 블록 이후 호출** 계약.
> - **#19 `DatasetExportWriter`/`DatasetExportTxService`** → **해소**. Tx 경계는 전부 REQUIRES_NEW, 마감은 `finalizeUnlessUnderDeidentReport`(RAW 잠금 재판정 → 차단 시 export 행 삭제), 회수기는 `claimForRetry` 조건부 UPDATE.
> - **#20 event_annotation 동결 소스(cot 배열→객체)** → **부분 해소**. export 최상위 키가 **`event`** 로 확정(`NiaAnnotationDoc`), 동결 소스는 `LS_DATASET_VIDEO_META.EVNT_ANNO_CN`(`resolveApprovedEventAnnotation`, 승인 시점 payload 원문, 멱등 해시 포함). `JsonNode` pass-through 라 배열/객체 양형이 그대로 보존된다. ⚠ `EvntAnnoService`(event_annotation 수정)는 **재동결 미배선**이라 승인 시점 동결본이 그대로 나가는 것이 현재 사실(CLAUDE.md 예외 조항).
