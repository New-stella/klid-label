-- =============================================================================
-- V147: 증강 요청 프롬프트 보관 컬럼 신설 + 활성 중복 유니크(V143) 해제.
--
-- 【1】 PROMPT_CN 신설 — 외부로 전송한 prompt(요청 조건) JSON 원문 보관.
--   「생성형 AI API 연동명세서 v1.1」 §4.1 의 prompt 는 자유 구조 dict 다. 구 구현은 증강 유형별
--   <고정 문구>를 서버가 만들어 보냈으나, 이제 REVIEWER 가 5필드(time/season/weather/terrain/severity)를
--   직접 입력해 보낸다. 어떤 조건으로 만든 결과물인지 사후에 역추적할 수 없으면 같은 (영상×종류)를
--   여러 번 요청해 나온 파생본들을 구분할 수 없으므로(아래 【2】로 반복 요청이 허용된다)
--   <보낸 원문 그대로> 남긴다. 선례: LS_BATCH_PROC_LOG.REQ_PAYLOAD_CN(요청 페이로드 원문 보관).
--
-- 【2】 UK_LS_DATA_AUG_ACTVTN(V143) DROP — "요청 1회 = 파생영상 1건" 구속 정책 폐기(2026-07-31).
--   근거(사용자 확정): 증강 결과 이미지는 요청마다 다르게 생성되므로, 원하는 결과가 나오지 않으면
--   같은 영상·같은 종류로 <다시 요청하는 것이 정상 동선>이다. V143 의 전제("같은 종류를 또 만들
--   이유가 없다")가 성립하지 않는다. 연타(오조작) 방어는 FE 단독 책임으로 이관한다.
--   ※ 되돌리려면(DROP 은 비가역) 인덱스 재생성 전에 활성 중복(PENDING·ACCEPTED)을 먼저 정리해야
--     한다 — V143 의 0)/1) 선행 정리 블록을 그대로 다시 수행할 것.
--
--   ★ UK_LS_DATA_AUG_RESL(V125)은 <절대 드롭하지 않는다> — 해상도 파생(RESL_ 접두) 전용 중복방지로
--     성격이 다르다(외부 위탁이 아닌 저작도구 내부 생성물이라 같은 프리셋을 두 번 만들 이유가 없다).
--     그래서 아래 DROP 은 인덱스명을 명시 지정한다(테이블 단위 일괄 삭제 금지).
--
-- 표준용어 준거 (정본 CSV 직접 조회: docs/표준용어/산업용어/{사업표준단어,사업표준용어,사업표준도메인}.csv):
--   용어) <등록된 복합용어를 단어 재조합보다 우선 채택> —
--         프롬프트내용 = PROMPT_CN, 사업도메인 내용V4000
--         (출처: KLID-BM 표준용어정의서 2026-05-28 배포). 즉 물리명·타입·크기 모두 등록값 그대로다.
--   단어) 프롬프트=PROMPT(사업표준단어 등록, 금칙어 아님) / 내용=CN(형식단어, 도메인분류 '내용').
--   도메인) 내용V4000 = VARCHAR(4000).
--         ※ 실사용량은 이보다 훨씬 작다 — 5필드 × 50자(DTO @Size 상한) = 250자 + JSON 키/구분자
--           오버헤드 63자 = 313자이고, 값이 전부 따옴표라 이스케이프로 2배가 되는 최악에도 563자다.
--           그럼에도 <임의로 축소하지 않는다>: 크기는 등록 용어에 연결된 표준도메인이 정하며
--           (임의 크기 = 감리 지적 대상), PostgreSQL 의 varchar 는 가변장(varlena)이라 상한을 크게
--           잡아도 실제 저장량·성능 비용이 없다.
--         ※ 참고로 <요청>의 표준약어는 REQ 가 아니라 DMND 다(사업/공통표준단어 공통). 선존
--           REQ_DT·REQ_KND_CD·REQ_PAYLOAD_CN 은 기존 드리프트이며, 신규 컬럼은 그 드리프트를
--           답습하지 않기 위해 REQ_* 조합을 쓰지 않고 등록 용어 PROMPT_CN 을 채택했다.
--
--   ★ PROMPT_CN vs PROMPT — <재검토 완료(2026-07-31), PROMPT_CN 유지>. 다시 열지 말 것.
--     같은 CSV 에 의미가 더 정확해 보이는 용어가 하나 더 있다(두 행 원문):
--       676행  프롬프트,PROMPT,생성형AI 입력 프롬프트 텍스트(DFEAT-053). 프롬프트(prog:355).,,,V,4000,,,,,KLID-PT D003 ERD-006
--       1424행 프롬프트내용,PROMPT_CN,"안내 문구, 메시지, 입력 힌트 등의 내용 [영문] Prompt Content",,내용V4000,,,,,,,KLID-BM 표준용어정의서 (2026-05-28 배포)
--     설명문만 보면 676(PROMPT)이 이 컬럼의 의미와 정확히 맞다. 그럼에도 PROMPT_CN 을 유지하는 근거:
--       ① <표준도메인 연결> — PROMPT_CN 은 사업도메인 '내용V4000'(VARCHAR(4000))에 연결돼 있어
--          "타입·크기는 표준용어에 연결된 표준도메인을 그대로" 라는 규칙을 물리명·타입·크기 세 축 모두
--          충족한다. 676(PROMPT)은 사업도메인 칸이 비어 있고 V/4000 을 직접 선언한 형태다.
--       ② <같은 시스템 내 선례> — 저작도구 v1 DB 설계서가 이미 <AI 생성 프롬프트>를 PROMPT_CN 으로
--          적재한다(LS_DATA_SRC.PROMPT_CN, KLID-AI 저작도구 통합설계서/DB설계서 V1.4). 여기서 PROMPT 로
--          갈아타면 <같은 의미에 물리명이 두 벌>이 되어 그 자체가 새 드리프트다.
--       ③ <형식단어 관행> — LS_* 컬럼은 모두 형식단어로 끝난다(_CD/_NM/_YN/_SN/_CN). PROMPT 단독은
--          이 관행에서 벗어난다.
--     두 용어 모두 VARCHAR(4000) 이라 어느 쪽을 택해도 물리 타입·크기는 동일하다(선택은 명명 축의 문제).
--
-- 우리 소유 LS_* 테이블(관제 MNG_* 아님) — 협의 불요. 멱등(IF NOT EXISTS / IF EXISTS).
-- LS_DATA_AUG 를 참조하는 뷰는 컬럼 추가에 영향받지 않는다(ADD COLUMN 은 뷰 재생성 불요).
-- =============================================================================

-- 1) 프롬프트 원문 보관 컬럼 — 기존 행은 NULL(구 고정 프롬프트 시절 요청이라 입력값이 없다).
ALTER TABLE LS_DATA_AUG ADD COLUMN IF NOT EXISTS PROMPT_CN VARCHAR(4000);

COMMENT ON COLUMN LS_DATA_AUG.PROMPT_CN IS
    '외부 증강 위탁 시 전송한 prompt(요청 조건 5필드: time/season/weather/terrain/severity) JSON 원문. '
    'V147 이전 요청 및 해상도 파생(RESL_*)은 NULL.';

-- 2) 활성 중복 유니크 해제 — 인덱스명 명시 지정(UK_LS_DATA_AUG_RESL 보존).
--    unquoted 식별자는 PostgreSQL 이 소문자로 접어 저장하므로 이 문장이 그대로 매칭된다.
DROP INDEX IF EXISTS UK_LS_DATA_AUG_ACTVTN;
