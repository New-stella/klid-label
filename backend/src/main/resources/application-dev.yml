spring:
  datasource:
    control:
      jdbc-url: jdbc:mariadb://${CONTROL_DB_HOST:192.168.102.101}:${CONTROL_DB_PORT:13307}/${CONTROL_DB_NAME:klid_system}?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Seoul
      username: ${CONTROL_DB_USERNAME}
      password: ${CONTROL_DB_PASSWORD}
      driver-class-name: org.mariadb.jdbc.Driver
    portal:
      jdbc-url: jdbc:mariadb://${PORTAL_DB_HOST}:${PORTAL_DB_PORT}/${PORTAL_DB_NAME}?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Seoul
      username: ${PORTAL_DB_USERNAME}
      password: ${PORTAL_DB_PASSWORD}
      driver-class-name: org.mariadb.jdbc.Driver
  quartz:
    properties:
      org.quartz.jobStore.isClustered: 'true'
      org.quartz.jobStore.clusterCheckinInterval: '20000'
      org.quartz.scheduler.instanceId: AUTO

logging:
  level:
    root: INFO
    kr.co.cudo.authoring: DEBUG
