# 인계 — 포털향 남은 작업 전부 (2026-09-08)

| 항목 | 값 |
|---|---|
| 넘기는 사람 | 세션 `portal-0908` |
| 브랜치 | **`portal-0908`** (원격에 push 됨 · 기준 커밋은 §1) |
| 넘기는 범위 | **포털향 남은 작업 전부** — ①라벨 즉석 생성 ②업로드 TUS ③화면 DS-002 |
| 장부 | `.claude/change-orders/MASTER.md` + 각 CO 파일 §7 |

---

## §1 ★먼저 — 기준 브랜치는 `main` 이 아닙니다

**작업물이 `main` 에 없습니다.** `portal-0908` 브랜치에만 있고 PR 전입니다
(`main` 최신 = `e40ae0a8f`). `main` 에서 분기하면 아래가 **하나도 없어** 처음부터 다시 만들게 됩니다.

```
frontend/design-tokens/ds002.js        main:없음  portal-0908:있음
frontend/design-tokens/channel.js      main:없음  portal-0908:있음
frontend/src/test/designChannel.ts     main:없음  portal-0908:있음
frontend/src/features/portal/uploads/hooks/useUploadLabelSource.ts   main:없음
```

```bash
git fetch
git worktree add ../<새-워크트리> -b <새-브랜치> origin/portal-0908
```

- **머지는 PR 고정**입니다(워크트리는 `main` 직접 머지 불가 — 메인 워크트리가 `main` 을 점유).
- `main` 과의 차이: **28파일 / +3,751 / −1,128**.
- 워크트리라면 **로컬 키트에 쓰지 마세요**(`docs/design/**`·`docs/screen-design/**` 갱신·다운로더
  실행 금지), **IMPREC 미기록**. 기록할 내용은 PR 본문 `## 키트 반영 대기` 로 올립니다.
- **FE 전체 회귀는 배타 레인**입니다 — 부하성 flaky 가 실측됐습니다. 지금 `issue-0908-4f` 세션이
  같은 레인을 씁니다. **진입 전 알리고 답을 받으세요.** 스코프 시험은 자유입니다.

---

## §2 이미 끝난 것 — 다시 만들지 마세요

| 커밋 | 내용 | 검증 |
|---|---|---|
| `9fe5a63f6` | **포털 라벨링 화면 흡수** — 업로드 갈래가 쓰던 별도 미니멀 화면(459줄)을 폐기하고 관제 라벨링 도구(`portalMode`)로 통합. 창구 차이는 데이터 계층이 흡수 | FE FULL 559→**4,843** / 실패 0 |
| `89414cd5e` | **DS-002 토큰 레이어** — 채널 선택 + 포털 토큰 + 가드 채널 축 + `PortalContentTabs`·`PortalLayout` 적용 | FE FULL **560 files / 4,907 / 실패 0** · `tsc` 0 · lint 59 무증가 |
| `c9b83e3c1`·`4dad3d971`·`106f2393e` | 장부 | — |

산출물 실측: 포털 CSS 에 `2e45dc`(포털 주색) 25건·`64748b`(slate) 7건 / **관제 CSS 는 바이트 동일**.

⚠ **lint 기준선은 59 입니다** — 직전 보고의 61 은 틀렸고 QA 가 바로잡았습니다.
⚠ **FE 기준선은 전건 green 입니다** — 한 담당이 *"기준선에 6파일 43건 실패"* 로 보고했으나, QA 가
`git archive HEAD frontend backend deploy ai-server mock-server docker` 로 **완전한** 기준선을 세워
**557/4,832 전건 통과**를 실측했습니다. 그 43건은 `frontend/` 만 뽑아 배포 형상 가드가 깨진 것입니다.
**「기준선이 원래 빨갛다」를 전제로 삼지 마세요** — 진짜 회귀가 그 뒤에 숨습니다.

---

## §3 남은 작업 ① — 포털 라벨 즉석 생성 ★진행 중이던 것

**CO: `CO-20260908-포털-라벨-즉석생성.md`** (읽어 주세요 — 아래는 요약)

### 확정된 사양 (사용자 확정 2026-09-08 · 구속)

> *"포털에 구지 라벨 마스터가 필요할까? 그냥 그때그때 쓰면 되지 않을까 싶어서. 다만 라벨 화면에서
> 필요한 라벨은 그때그때 만들 수 있어야 할거 같아."* · *"대신 불러올때는 기존 라벨정보들을 다
> 보여주고"* — *"작업의 라벨정보들"*

1. **포털에 라벨 마스터를 두지 않는다** — 전용 표·관리 화면 **0**.
2. **라벨링 화면에서 그때그때 만든다** — 이름을 입력해 바로 쓴다.
3. **작업을 열면 그 작업의 기존 라벨을 전부 목록으로 보여준다.** ★**마스터 없이 재사용이 성립하는
   지점이 이것이다** — 두 번째 객체를 그릴 때 첫 번째에서 만든 이름을 고르면 된다.
4. 저장 형태 무변경 — 이름만 남고 마스터 연결(`labelId`)은 계속 `null`.
5. **데이터마트 갈래는 종전대로 마스터** — 그 자산은 관제에서 와 연결이 이미 있고 화면은 그것을
   보존해 되돌려 보낸다. **이 축은 안 건드린다.**

### 어디까지 됐나

**Phase 3.6(설계 선반영) 라운드 1 완료 — `UC-027` v32 → v33.** 개요·주 흐름 5단계에 「자산 출처로
갈리는 라벨 목록 + 그 작업의 라벨 재사용 + 즉석 생성」을 넣고, 대안 흐름 2·사후조건 1·구현 시
고려사항 4를 신설했습니다. 데이터마트 갈래(마스터 참조·트랙 식별자 왕복)는 무변경입니다.
검증: 기대본과 완전 일치(비-equal opcode 0) · 기준선 대비 `insert` 2건뿐(`replace`·`delete` 0 =
1자 치환·절단 없음) · 신규 2,482자 희귀 음절 전수 점검 손상 0.

**남은 cascade 큐** (담당이 이어서 `mc-logi-update` 로 돌리면 됩니다):

| ITEM | 등급 | 무엇을 |
|---|:--:|---|
| `SCREEN-029` | **high** | 라벨 선택 영역을 자산 출처로 가른다 — 업로드는 「이 작업의 라벨 + 만들기」(고르기/만들기 구분 · 80자 입구 차단 · 빈 이름 목록 제외 · 이름 기반 결정론 색 · 0건 정상 안내) |
| `AC-1070` | **high** | 정상 경로 3건 신설 — ①작업 전체 라벨이 중복 없이 뜬다(다른 프레임 것 포함) ②만든 라벨이 저장 후 재조회에도 유지 ③같은 이름은 프레임을 옮겨도 같은 색 |
| `AC-1071` | medium | 부정 경로 2건 신설 — 80자 초과 입구 차단 · 빈 이름은 목록 제외(저장은 허용) |
| `SEQ-019` | medium | 작업 단위 라벨 목록 조달 + 그 자리 생성. `SCREEN-029` 뒤 |
| `SD-025` | medium | `SCREEN-029` 시안 재게시. **로컬 원본을 고쳐 게시하고 미러가 따라오게** — 미러를 직접 고치지 마세요 |
| `SCREEN-033`·`SCREEN-044`·`NAV-002`·`SD-026`·`MOD-017`·`MOD-021` | low | **본문 대조 결과 변경 불요** — 다시 열지 마세요 |

⚠ **`stale` 표식이 소실됐습니다** — 쓰기 전 `UC-027` 은 `stale=true`,
사유 `"DFEAT-053의 data.implemented_by_endpoints 변경 — realizes"` 였는데 쓰기 후 `false` 가 됐습니다.
**이번 편집은 그 축을 전혀 다루지 않았으므로 해소가 아니라 소실**입니다. **같은 사유가
`AC-1070`·`AC-1071`·`SEQ-019` 에 아직 서 있으니** 그것들을 열 때 별도로 판정해 주세요.

⚠ **`use_case` 의 `tech_considerations` 원소 상한은 500자**입니다(이번에 실측 — `E_VALIDATION
too_big` 으로 **원자 거부**라 서버에 아무것도 안 쓰였습니다). `screen_spec` 섹션 설명 상한 1000자와
별개 값입니다.

⚠ **판단이 필요한 것 하나** — 이번 회차 기록을 `brownfield.notes`(회차 원장)가 아니라
`change_summary` 에 실었습니다. 직전 여섯 회차는 notes 에 남겼으므로 **관례와 어긋납니다.**
notes 는 전면 재작성만 가능한 장문 필드라 절단·1자 치환 위험을 지는 것이 이유입니다. 되돌릴지 판단해 주세요.

- **코드는 한 줄도 안 건드렸습니다.**

### ★실측해 둔 것 — 다시 조사하지 마세요

**작업 단위 라벨명 목록을 주는 창구가 없습니다.** `/v1/portal/uploads/**` 전수 확인:

| 창구 | 주는 것 | 쓸 수 있나 |
|---|---|---|
| `GET /{uldSn}/frames` | 프레임 페이징 목록 | ❌ 라벨이 안 실린다 |
| `GET /frames/{uldFrmeSn}/labels` | **프레임 1건**의 라벨 | ❌ 작업 전체를 모으려면 프레임 수만큼 호출 |
| `GET /{uldSn}/export` | 자산 메타+프레임+라벨 **전량** | ❌ 데이터는 있으나 **첨부 다운로드 계약**이고 폴리곤 좌표까지 전부 |

리포지토리에도 업로드 축 라벨명 distinct 쿼리가 **없습니다**(`DISTINCT` 4건은 전부 포털 *작업* 축).
⇒ **읽기 전용 조달 창구 신설이 필요할 공산이 큽니다.** 단 **API ITEM ID 를 담당이 지어내게 하지
마세요**(LogiCraft 규약: AI 임의 ID 생성 금지) — 사양에는 요구만 적고 신설은 사용자 판단으로.

### 서버는 이미 이 설계입니다

`PortalUploadLabelService` 주석이 명시합니다 — *"라벨 마스터 연결(`labelId`)은 **비운다** — 포털
라벨링은 마스터를 참조하지 않는 **자유 입력**이고, 이름으로 마스터를 역추정하면 동명·비활성
마스터에 잘못 붙는다."* **못 하는 것은 화면입니다** — `LabelPickerModal` 의 입력칸은 마스터를
거르는 **검색 필터**이지 라벨을 만드는 자리가 아닙니다.

### ★되살리지 말 것

**`CO-20260908-업로드라벨-마스터연결-단절` 은 폐기됐습니다.** *"창구에 `labelId`·`trackId` 가 없는
것은 결함"* 이라고 단정했는데 틀렸습니다 — 그 근거로 든 `SCREEN-029` 문장은 **데이터마트에서 불러온
라벨**을 가리키고, 업로드 자산은 사용자가 처음부터 그리는 것이라 **보존할 연결이 애초에 없습니다.**
그 안대로 하면 포털 사용자가 전역 관제 마스터에 묶여 **문제를 안 풉니다.**

---

## §4 남은 작업 ② — 포털 업로드(TUS) 진행률·청크 재개

**CO: `CO-20260908-포털업로드-진행률-청크재개.md`** · 상태 **⏸ 대기(착수 전)**

다른 세션(`issue-0908-4f`)이 조사·지시서를 마치고 넘긴 건입니다.

1. `patchChunk` 에 axios `onUploadProgress` 배선 → `offset + loaded` 로 **바이트 단위** 진행률.
   **청크 완료 시점의 기존 보고는 그대로** — 최종 오프셋이 서버 응답값으로 확정되게.
2. 청크 타임아웃·네트워크 오류 시 `fetchOffset`(HEAD)으로 **서버 오프셋 재동기화 후 자동 재시도**
   (상한 1~2회 — 값은 제안). 넘으면 종전대로 실패.
3. `PortalUploadPage` 에 **「일시정지」·「이어서 올리기」 노출**.
4. `PortalTusUploadController` 에 **`@design` 태그를 심는다**(현재 0건).

⚠ 설계 선반영이 **⏳ 대기**입니다 — CO §6 이 *"쓰기 전에 본문을 먼저 열어 대조할 것"* 을 명시합니다.
⚠ 이 작업과 아래 §5 의 `SCREEN-033` 이 **같은 파일(`PortalUploadPage.tsx`)** 입니다. 한 사람이
다 하시니 충돌은 없지만, **스타일을 먼저 입히고 버튼을 얹는 순서**가 다시 안 고치는 길입니다.

---

## §5 남은 작업 ③ — 포털 화면에 DS-002 입히기 (D 라운드)

토큰은 들어갔고 **화면 본문이 아직 관제 모양**입니다. 화면 격차 **149건**.

| # | 화면 | 파일 | 자료 |
|:--:|---|---|---|
| 1 | SCREEN-028 포털 홈 | `pages/portal/PortalHomePage.tsx` | `reports/sd-portal-20260908/SCREEN-028.md` — 69/6/3 |
| 2 | SCREEN-033 포털 업로드 | `pages/portal/PortalUploadPage.tsx` | `reports/sd-portal-20260908/SCREEN-033.md` — 80/6/5 |
| 3 | SCREEN-029 포털 라벨링 | `pages/portal/PortalLabelingPage.tsx` 외 | **대조 미실시** · ⚠ §3 이 이 화면 사양을 바꿉니다 → **§3 뒤에** |
| 4 | SCREEN-044 포털 증강 | `pages/portal/PortalAugmentPage.tsx` | **시안 자체가 없음** → §7-② |
| 5 | SCREEN-045 업로드 영상 마킹 | `pages/portal/PortalUploadMarkingPage.tsx` | **시안 자체가 없음** → §7-② |
| 6 | 채널 가드 잔여 | `src/test/designTokensPortalChannel.test.ts` | §8 |

---

## §6 ★함정 — 전부 이번에 실제로 밟았습니다

1. ★★**팔레트가 1:1 이 아닙니다.** 관제(DS-001)와 포털(DS-002)은 **토큰 이름이 같고 값만 다릅니다.**
   시안 CSS 머리말이 경고합니다 — *"이름으로 같은 색이라고 판단하면 조용히 다른 색이 뜬다."*
   이 세션이 대조 담당 둘에게 *"팔레트가 시안 변수와 1:1"* 이라는 **틀린 전제**를 줬고 둘 다
   독립적으로 깨고 보고했습니다. **이름 대조로 판정하지 마세요.**

2. ★**ITEM 의 `nav-link` 는 본문 상단 탭의 규격이 아닙니다.** 그 항목의 `use_for` 는
   **GNB 항목 · LNB 1단**입니다. 이 세션이 *"`nav-link` 가 규격을 준다"* 고 지시했다가 담당이
   바로잡았습니다 — 그대로 갔으면 탭 높이가 48→40 으로 줄어 **접근성이 후퇴**했습니다.

3. ★**`stale: false` 를 정합의 증거로 읽지 마세요.** 상류가 v39→v41 로 바뀌었는데 표식이 끝내
   안 켜진 사례가 같은 날 실측됐습니다. **`DS-002` 는 상대 정의의 사본이라 전파 그래프 밖**이라
   표식이 애초에 안 움직입니다. 판정은 **본문 직접 대조**뿐입니다.

4. **게시본으로 접근성을 판정하지 마세요** — 게시 sanitizer 가 `scope`·`autocomplete`·`inputmode`·
   `min` 을 지웁니다. 판정은 **로컬 원본**으로.

5. ★**시안은 아티팩트로 올리지 않습니다** — `reports/screen-design/` 에 HTML + PNG 로 냅니다
   (프로젝트 구속 규칙 · **제안조차 금지**). 렌더 스크립트는 `frontend/` 안에 둬야 의존이 잡힙니다.

6. **`labelId` 배선을 잃지 마세요** — `classId` 만 채우면 저장 후 라벨 마스터 조인이 끊겨 색·
   라벨명·속성 정의가 함께 죽습니다. **저장 전에는 정상으로 보여 발견이 늦는** 실사고 이력이
   있습니다. 생성 payload 는 `OverlayLayer.newLabelFrom` 단일 진입점을 거칩니다.
   ⚠ 단 **업로드 갈래는 예외**입니다(§3 — 거기선 `labelId` 가 `null` 인 것이 사양).

---

## §7 ★사용자 결정이 필요한 것 둘 — 임의로 정하지 마세요

① **`Pagination` 높이 44 → 40.** DS-002 는 40 을 요구하는데 현재 44 에는 **KRDS 터치 타깃 근거가
   문서로 남아 있습니다.** 줄이면 **접근성 후퇴**입니다.
   ⚠ 이동 탭의 44→48 과 혼동하지 마세요 — 그건 **키우는** 방향이라 성격이 다르고 이미 반영됐습니다.

② **`SCREEN-044`·`SCREEN-045` 는 시안 자체가 없습니다.** 대조 대상이 없으므로 **시안 생성이 선행**
   돼야 하고, 그 생성을 이 라운드에 포함할지가 사용자 결정입니다.

---

## §8 검증 규율 · 잔여

- **변이 검증 없이 「가드를 넣었다」고 하지 마세요.** 이번 라운드의 **M9(축 목록에서 관제 제거)** 가
  「케이스가 틀리는 게 아니라 **실행되지 않는**」 함정을 실증했습니다. 축을 도는 시험에는 **그 목록
  자체를 지키는 단언**과 **음성 대조**를 짝으로 두세요.
- ⚠ **잔여 1건** — 채널 가드에 **절대값 고정 단언이 없습니다.** 목록 A↔B↔C 일치만 보므로
  **셋에서 동시에 빼면 통과**합니다. `toEqual(['control','portal'])` 류를 하나 넣어 주세요.
- **잔여 — 초록으로 덮지 마세요**: 런타임 E2E 미수행 · BE 미검증(메타 창구 200 여부는 목만) ·
  busy 프레임 스코핑 시험 부재 · DS-002 화면은 아직 관제 모양.
- **PR 본문 「키트 반영 대기」로 넘길 것**: `SCREEN-029.implementation` 이 `implemented/100`
  (`IMPREC-129`·`143`·`398`)으로 **거짓 주장**이었습니다 — 이번 흡수로 충족됐으나 그 사실을
  기록으로 남겨야 합니다.

---

## §9 ★상대(포털 Host)에 제기할 것 — 우리가 고칠 수 없습니다

`DS-002` 는 상대 정의 v9 의 **참조 사본**이고 ITEM 자신이 *"값을 고치지 않는다. 고칠 것이 있으면
상대에 제기하고, 반영되면 다시 옮겨 온다"* 고 규정합니다.

1. ★**내부 모순 — 표 헤더 색.** `typography_use.label` 은 *"색 slate-400"* 인데, 같은 ITEM 의
   `color_usage.neutral.avoid_for` 는 *"흰 배경 위 의미 텍스트에 gray-400 이하(4.5:1 미달)"* 를
   금지합니다. slate-400 은 흰 면 위 **2.85:1** 이라 그 금지에 걸립니다.
   → 이번 라운드는 **500 을 채택**했습니다(ITEM 자신의 규칙과 정합하고 시안도 독립적으로 `--n-5`).
2. `success` scale 의 **600·700 이 같은 값**입니다.

---

## §10 관련 파일

| 축 | 경로 |
|---|---|
| 장부 | `.claude/change-orders/MASTER.md` |
| 라벨 즉석 생성 | `.claude/change-orders/CO-20260908-포털-라벨-즉석생성.md` |
| 업로드 TUS | `.claude/change-orders/CO-20260908-포털업로드-진행률-청크재개.md` |
| 화면 DS-002 | `.claude/change-orders/CO-20260908-포털-DS002-토큰레이어-도입.md` (§7 에 실측·변이 10건) |
| 라벨링 흡수(완료) | `.claude/change-orders/CO-20260908-포털-업로드라벨링-관제도구흡수.md` |
| 폐기(참고) | `.claude/change-orders/CO-20260908-업로드라벨-마스터연결-단절.md` |
| 대조 리포트 | `reports/sd-portal-20260908/{SCREEN-028,SCREEN-033}.md` |
| 토큰 | `frontend/design-tokens/{ds002,channel}.js` |
| 가드 | `frontend/src/test/{designChannel.ts,designTokensPortalChannel.test.ts,tableSurfaceConvention.test.ts}` |
