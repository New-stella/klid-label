| 항목 | 값 |
|---|---|
| CO 번호 | CO-NNN |
| 제목 | (한 줄 요약) |
| 대상 도메인 | DOMAIN-0NN(이름) · (프론트면 SCREEN-NNN) |
| 구현 상태 | 📝 작성 / 🔨 구현중 / ✅ 완료 / ⚠️ 부분 |
| LogiCraft 설계반영 | ⏳ 대기 / 🎨 완료 / — (해당없음) |
| 생성일 | YYYY-MM-DD |

> ★ 이 프로젝트는 **설계를 먼저 확정하고 코드가 뒤따른다**(`klid-dispatch` Phase 3.6). 따라서 정상 흐름에서는 §6 이 **구현 전에** 🎨 가 된다. ⏳ 로 남는 것은 긴급 예외이며, 그건 빚이므로 `/klid-design-backfill` 로 갚는다.

---

## §1 배경

(왜 이 변경이 필요한가. 증상·요청 맥락·근본 원인. 도구를 모르는 사람이 이것만 읽고 이해할 수 있게.)

## §2 변경 요지

(무엇을 무엇으로 바꾸는가. 3~5줄.)

## §3 도메인별 변경 상세

> 이 절이 각 구현 에이전트에 `change_detail` 로 그대로 전달된다. 에이전트가 받아 구현할 만큼 구체적으로.

### DOMAIN-0NN (이름) — `klid-d0NN-implementer`

- **대상 파일·심볼**: (경로 · 클래스/메서드)
- **변경**: (무엇을 어떻게)
- **불변(건드리면 안 되는 것)**: (기존 계약·상태 전이·인접 도메인 경계)
- **주의**: (이 도메인 특유의 함정 — 에이전트 지침과 중복돼도 여기 다시 적는다)
- **수용기준**: (검증 가능한 단언. 가능하면 AC-NNN 인용)

### 프론트 (SCREEN-NNN) — `klid-web-implementer`

- **대상 화면·컴포넌트**:
- **소비 API 계약**: (API-NNN — 필드·타입·nullable·enum)
- **변경**:
- **불변**:
- **수용기준**:

### 공유기반 선처리 (해당 시 — 메인 직접, Phase 3.5)

- **대상**: `common/` · `batch/` · `db/migration/` · 앱 진입점 중 무엇
- **변경**:
- ⚠ 새 컬럼·테이블이면 **표준용어·표준도메인**(물리명+타입+크기) 판정을 CSV 정본 grep 으로 먼저 끝낸다.

## §4 영향·리스크

- **하위호환**: (기존 API 호출자·저장 데이터·관제 계약면에 미치는 영향)
- **되돌리기**: (문제 시 롤백 방법)
- **리스크**: (놓치면 무엇이 깨지나)

## §5 검증

- (어떻게 확인할 것인가 — 자동 테스트 · 수동 시나리오 · 목서버 기동 필요 여부)
- (`docs/test-cases/` 카탈로그에 해당 케이스가 있으면 ID 인용)

## §6 관련 설계 ITEM  ★구현 전에 먼저 확정한다

> `klid-dispatch` Phase 3.6 이 이 목록을 `mc-logi-update` 로 넘겨 **구현 전에** 반영한다.
> 「나중에 반영할 예상 목록」이 아니라 **「지금 고칠 대상」** 이다.

| ITEM | 타입 | 무엇을 어떻게 | 근거 |
|---|---|---|---|
| API-NNN | api_endpoint | | |
| ERD-NNN | erd | | |
| AC-NNN | acceptance | | |

**cascade 예상 하위**: (AC·SCREEN·SEQ·CDIAG·CMP — 있으면 ID 를 적어 위임 시 명시적 대상으로 넘긴다)

**확정: ** (Phase 3.6 회수 후 `<ITEM-ID> v<N>` 목록을 여기 적는다)

## §7 구현 로그

| 일시 | 도메인 | 에이전트 | 결과 | QA | 커밋 |
|---|---|---|---|---|---|
| | | | | | |

**미반영·보류 항목**: (Phase 5.9 에서 사용자 동의를 못 받은 노하우 후보, 정보부족 항목 등 — 버리지 말고 여기 남긴다)
