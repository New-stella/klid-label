# 진행 상태 — 단위시험 증적 + 비식별 해소 완화 (2026-09-09)

> 세션 교체에도 살아남는 상태 기록. 값은 전부 실측이다. **추측으로 채운 값은 없다.**

---

## 1. 증적 촬영 — 48/48 완료 ✅

```
원장   ~/Desktop/CBD-캡처루트-20260909/capture-manifest.json
       케이스 48 · 컷 90 · 파일 88
대조   D11 48케이스와 양방향 차이 0 · 빈 칸 0
컷 분포 1장×17 · 2장×21 · 3장×9 · 4장×1
```
⚠ **컷 수(90) ≠ 파일 수(88)** — 같은 순간·같은 화면을 두 케이스가 나눠 쓰는 자리가 둘이다
(`003-01`↔`003-02-cut2`, `007-01`↔`023-01-cut2`). **행 수·건수는 컷 기준으로 센다.**

### ★★ 증적 해상도 = 1920×1080 화면 한 장 (2026-09-09 사용자 확정, 구속)

```
한 컷 = 모니터 화면 한 장(정확히 1920×1080)
스크롤 분할 · fullPage 둘 다 쓰지 않는다   (CAPTURE_MAX_PAGES=1)
검증 대상이 아래에 있으면  shotAt(대상을 화면 안으로 끌어온 뒤 한 장) 또는 shotEl(그 조각만)
두 장이 필요하면 컷을 둘로 나눈다 — 한 컷을 잘라 두 장으로 만들지 않는다
```
- ⚠ **구 동작 폐기**: `1600×1000 + fullPage:true` 는 스크롤 전체를 이어붙여 **모니터에 존재하지
  않는 그림**을 만들었다(실측 최대 `1600×2673`). *"합성한 것 아니냐"* 로 읽힌다.
- ⚠ **구 동작 폐기**: 스크롤 분할 최대 3장 — 나뉜 장마다 내용이 중간에서 잘려 어색하다.
- ★**판정 기준은 「화면 전부를 담았는가」가 아니라 「그 케이스가 검증하는 것이 보이는가」다.**
  ✅ 구판 1080 초과 27장 **전수 확인 완료**(2026-09-09). 잘려서 대상을 잃은 것은 **2건**이었고
  둘 다 `shotAt` 으로 고쳤다:
  - `010-01-cut1` — **채택 버튼이 화면 아래**라 「증강 결과 채택」 증적에 결정 수단이 없었다
  - `001-01-cut1` — 생성 조건을 채우며 스크롤이 내려가 **조건 5항목이 화면 위로** 잘려 나갔다
    (`계절=겨울 · 날씨=눈` 이 이 케이스의 증명 대상인데 그것이 없었다)
  ⇒ **대상이 첫 화면 밖이면 `shotAt`(대상을 화면 안으로 끌어와 한 장)**, 조각만 필요하면 `shotEl`.
- 구판 증적은 `~/Desktop/CBD-캡처루트-20260909-백업-1600x1000/` 에 보존.

### ⚠ `capture.env` 는 git 에 없다 — 지우면 값을 잃는다
`.gitignore` 대상이라(서버별 로컬 설정) **레포에서 복구되지 않는다.** 값은 `capture.env.example`
이 같은 내용을 들고 있으므로 거기서 되살리되 `CAPTURE_OUT` 만 그 회차 폴더로 바꾼다.
(2026-09-09 에 실제로 한 번 잃었다 — gitignore 발화 시험에서 흉내 파일로 덮어썼다.)

### 실행 (AI 없이 돌아간다)
```bash
cd frontend
./run-capture.sh                 # 전 케이스 + 원장 생성
./run-capture.sh 032-01 040-01   # 지정한 것만
./run-capture.sh --list          # 케이스 목록
./run-capture.sh --manifest-only # 이미 찍힌 파일로 원장만 다시
```
- 설정 `frontend/capture.env` (예시·주의는 `capture.env.example`) · 안내서 `frontend/CAPTURE-README.md`
- ★**시나리오 `frontend/CAPTURE-SCENARIO.md`** — 무엇을 어떤 순서로 준비해 찍는가(인입·비식별
  해시확인·마킹 배정 경로·값의 유래·촬영 순서·소진 케이스·검증 세 축). **처음부터 새로 할 때 정본.**
- 돌리기 전 스스로 점검한다 — node·playwright·chromium·**서버 200**·dev 로그인 창구.
  하나라도 어긋나면 **촬영하지 않고 멈춘다**(빈 화면을 조용히 찍는 것이 가장 나쁜 결과라서).
- 쿼리 증적: `node capture-query-evidence.mjs` — 246 실 DB 에서 돌린 쿼리+결과를 PNG 로 굽는다.
  **0행이거나 행 수 표시가 없으면 실패로 낸다.** 대상 4건: `002-01`·`007-03`·`009-01`·`038-01`

### ★ 포털 4케이스는 포털 채널로 찍는다 (024-01·02 · 027-01·02)
포털 화면은 **포털 채널 산출물에만** 있다(`VITE_BUILD_CHANNEL=portal`). 관제 채널 배포본에는
라우트가 **번들에 아예 없어** 404 다 — 런타임 잠금이 아니다.
```bash
cd frontend && set -a; . ./capture.env; set +a
# ① Origin 을 떼는 로컬 중계 (브라우저 Origin 이 서버 CORS 허용목록에 없어 403 이 난다)
node capture-origin-proxy.mjs &                                 # :15174 → CAPTURE_APP
# ② 포털 채널 FE
BACKEND_ORIGIN=http://localhost:15174 VITE_BUILD_CHANNEL=portal npx vite --port 15173 &
# ③ capture.env 에  CAPTURE_PORTAL_APP=http://localhost:15173
```
⚠ **이 중계는 「해소」가 아니라 「우회」다.** 실배포는 FE·BE 가 **동일 출처**라 CORS 가 적용되지
않으므로 증적은 유효하나, **임베딩 형상(교차 출처)에서의 동작은 재지 않았다.** 아래 §5 참조.

### 하네스가 배운 함정 (같은 실수 반복 방지)
- 확인창은 브라우저 `confirm` 이 아니라 **앱 모달**(`[data-testid=modal-backdrop]`) — backdrop 이 다음 클릭을 막는다
- 라벨명 입력에 `type` 속성이 없어 `input[type=text]` 가 **색상 칸**을 집는다 → placeholder 로 집을 것
- 설정 화면의 `저장` 은 카드마다 있다 — **순번(`nth`) 금지**. 그 카드 고유 입력에서 **저장 버튼을 품은 가장 가까운 조상**으로 올라갈 것
- 작업자 콤보 첫 항목은 **플레이스홀더**("작업자 선택") — 고르면 저장이 잠긴 채
- **같은 값을 다시 넣으면 폼이 dirty 가 안 돼 저장이 잠긴다** → 현재값과 다른 값을 고를 것
- 1이벤트 1프리셋이라 프리셋 있는 유형에 만들면 **409**
- 추적 모드 기본값은 **「검토 후 수락」** — 그대로 돌리면 라벨이 안 붙는다. 붙어도 **저장해야 확정**
- 추적은 **이미 라벨이 있는 자리에 겹쳐 반영하지 않는다**("겹쳐 반영하지 않았습니다"). 반대로 대상이
  드문 프레임에선 검출이 0("반영할 검출이 없습니다"). **둘 다 정상** — 증적은 결과 패널의 건수로 잡는다
- 롤백은 「롤백」 버튼이 아니라 **「이 버전으로 시작」 → 창 닫기 → 저장**. 창이 남는 것이 의도된 동작
  (*"아직 저장되지 않았습니다"* 안내를 위해)
- 승인 버전이 둘 이상이면 라벨 화면 진입 시 **「시작 버전 선택」이 자동으로** 뜬다
- 프레임은 **그 작업자 배정분**이어야 한다 — 아니면 403(정상 인가 동작)
- 공지 첨부 허용 확장자: `pdf doc docx xls xlsx ppt pptx png jpg jpeg zip hwp hwpx` — 그 밖은 400(정답)

---

## 2. D11 단위시험 절차서

```
정본   docs/cbd-inputs/D11-단위시험절차서-초안.md        ← 생성기가 --draft 로 무는 자리
사본   /Users/ck/Documents/workspace/klid/docs/design/D11-R1-…-정합수정.md   ★git 밖
현재   블록 35 · 실케이스 48 · 범위 제외 라벨 행 5
```
★ **두 벌이 갈리는 것이 이 파일의 유일한 위험이고, 2026-09-09 에 실제로 갈려 있었다** —
레포 사본이 개정 5.6·58케이스에 멈춘 채 레포 밖만 6.0·48케이스로 앞서 있었다(그대로 뽑았으면
**옛 판이 산출물로 나갔다**). `python3 docs/cbd-inputs/verify.py` 가 그것을 잡는다.
**고친 뒤에는 반드시 그 검사를 돌린다.**
- 개정 5.7 외부 산출물·파일 업로드 제외 / 5.8 예상결과 전면 재작성 / 5.9 **관리자 기능 제외** /
  6.0 **포털은 범위에 유지**(포털 채널로 수행 가능함이 실증됨 — 제외 철회)
- 제외 5블록은 **케이스 행이 아니라 라벨 행**(`| 시험 범위 제외 | 관리자 기능 | | |`)
  → 케이스 행으로 두면 생성기가 시험결과에 「성공」을 찍어 **범위 밖인 것을 성공했다고 주장**한다
- ⚠ `UC-016` description 이 **4000자 상한에 도달** — 다음 편집자는 한 글자도 못 넣는다

---

## 3. 비식별 해소 완화 (CO-20260909-해소파일)

**요청**: 해소 후보에서 기존 산출물도 고를 수 있게. **남는 판정은 무결성 하나.**
**대가(인지·수용)**: 재비식별 없이도 해소가 가능해진다 — 마스킹 누락이 남은 산출물이 정상으로 복귀할 수 있다.

### 설계 확정 — 21건 변경
```
ADR-027 v4  ADR-022 v6  ADR-006 v5   API-202 v3  API-094 v10  API-032 v10  API-091 v13
SEQ-014 v16 SEQ-001 v26  DFEAT-048 v20  DFEAT-041 v17  DOMAIN-012 v12
SCREEN-032 v29  UC-016 v33  AC-1066 v10  AC-1067 v6  CMP-003 v14  CDIAG-003 v14  EVT-008 v9
```
**무변경 판정 3건**(축이 달라 고치면 안 되는 것): `AC-1064`·`UC-011`·`SEQ-012` — 전부 **KPST 실패
회수 축**이다(신고 해소 축이 아니다). `EXTSYS-003`·`INT-005` 도 같은 이유로 미수정 확정.

★ **CO 작성 때 예상 7건 → 실제 21건.** 못 본 부류가 둘이다:
- **링크가 없어 영향 분석 밖** — `DFEAT-041`·`EVT-008` (도메인 전수 스윕에서 발견)
- **낱말이 달라 검색 밖** — `API-091` 의 「끝난 뒤」·「재비식별 결과 위에서」

★ **`EVT-008` 은 결론은 맞는데 근거가 거짓이 된 형태**였다 — 강제 재생성 근거가 「제자리 교체한
뒤에만 통과하므로 반드시 바뀌어 있다」였다. 결론은 두고 근거만 갈아 끼웠다.

### 구현 (Phase 4)

**백엔드 ✅ 완료** (`klid-d012-implementer`, 2026-09-09)
```
파일   label/service/DeidentArtifactCandidateFinder · DeidentReportService · dto/DeidentCandidateResponse
       + 같은 축 테스트 3파일
회귀   9,218 tests · failures 0 · errors 0 · skipped 6   (10분 7초 · 결과 XML 982)
변이   M1 시간 조건 부활      → finder·service·controller 3층 10건 발화
       M2 무결성 판정 제거     → 양성 대조 4건 발화 (안 푼 축이 남아 있음을 증명)
       M3 경로 순회 방어 약화  → 순회 가드 2건 발화
IMPREC IMPREC-445~448
```
★ **판정 지점을 한 곳으로 모았다** — 신설한 `isEligibleForResolve(Path)` 한 메서드가 판정의 전부이고,
그 자리 javadoc 에 사유·대가·되돌리는 법이 있다. 되돌릴 때 볼 자리가 한 군데다.
★ **응답 계약 무변경** — `API-202 v3` 본문을 읽고 판단했다. 필드는 그대로
`fileName·sizeBytes·modifiedAt·eligible·current` 이며 「신고 이후 생성」 전용 필드는 **없다**(지어내지 않음).
화면은 `candidate.modifiedAt` 과 신고행 `reportDt` 를 대조하면 된다(둘 다 FE 가 이미 갖고 있다).

**프론트 🔄 진행 중** (`klid-web-implementer`)
`frontend/src/features/deident/components/DeidentResolveDialog.tsx` ·
`frontend/src/features/deident/reportTypes.ts`
⚠ **BE 변경으로 화면 문구가 거짓이 됐다** — `eligible=false` 가 이제 무결성 불통과만 뜻하는데
`INELIGIBLE_HINT` 는 여전히 *"신고 이후에 만들어진 정상 영상 파일이 아니라…"* 로 시간 얘기를 한다.

### ★ 증적 재촬영 결과 (2026-09-09 · 1920×1080 전환)

```
케이스 48 · 컷 90 · 파일 88 · 빈 케이스 0 · 규격 밖 0장
  화면 1920×1080  74장     요소 부분캡처 10장     쿼리 렌더(2560×N) 4장
```
전건 재촬영에서 **실패 13건**이 났고 **제품 결함은 0건**이었다. 원인 세 갈래:
- **복구한 `capture.env` 값이 실제 동작하던 값이 아니었다** — `FRAME_AI`(배정 없는 PENDING 영상) ·
  `FRAME_TRACK`(이미지 경로가 빈 프레임) · `RAW_SN`(이미 COMPLETED) · `INGEST_CLIP`(이미 적재) ·
  해상도 대상(세 프리셋이 이미 다 있어 「전부 스킵」 400)
- **하네스 판정이 사양과 어긋났다** — `039-01` 이 *"객체 수가 늘어야 한다"* 로 단정했으나 D11 은
  *"검출이 없으면 0건으로 정상 처리된다"* 라고 명시하고, 이 시스템은 배치가 이미 오토라벨을 붙여
  재실행 시 중복 억제가 정상이다 · `010-01` 이 「결정 대기」가 **사라지길** 기다렸다(여럿이면 정상) ·
  증강 카드를 「결정 대기」 글자만 보고 골라 **채택·반려가 차단된 해상도 파생**을 집었다 ·
  `016-01` 이 `039-01` 과 프레임을 공유했는데 전제가 정반대다(→ `CAPTURE_FRAME_REPORT` 로 분리)
- **내 데이터 입력이 불완전했다** — 인입 행에 `vms_cctv_id`(CCTV명 컬럼)를 안 넣어 목록에 `영상 #37`
  로 떴다

### ★★ 비식별 영상으로 전면 재촬영 (2026-09-09 밤 · 사용자 지시)

구 증적은 **마스킹이 안 된 원본 복사본**을 보고 있었다(비식별 엔진의 모델 파일 부재 → 조용한 폴백).
엔진 복구 후 새 영상으로 파이프라인을 다시 태워 갈아탔다.

```
CCTV-023 (raw 43)   인입 → 비식별 → 마킹 → 프레임 6 → 오토라벨 78
검증(★파일로)       원본 11,243,026B bfbf44c4… / 비식별 22,771,420B 65f3150d…  ← 해시 다름
                    목서버: deid rendered — 실제 마스킹(엔진)
                    육안: 도로 표지·현수막 글자와 번호판 자리가 검게 마스킹됨
capture.env         RAW_SN=43 · MAIN=102 · AI=107 · META=103 · TRACK=104→105 · REPORT=40(raw 21)
백업                ~/Desktop/CBD-캡처루트-20260909-백업-비식별전-2221 (88장 · 30M)
```

⚠⚠ **원장은 마스킹을 증명하지 못한다** — `face_dtct_cnt`·`noplt_dtct_cnt` 는 시뮬레이션 값이라
파일이 원본 그대로여도 채워진다(실측: 18/12 인데 sha256 이 원본과 동일). **반드시 해시를 뜬다.**

### ★ 이 라운드에서 고친 하네스 결함 5건 (전부 조용히 틀리는 부류)

| 결함 | 증상 |
|---|---|
| `pickTool` 이 창을 세기 전에 셈 | 라벨 선택을 건너뛰고 뜬 창이 드래그를 막음 → 「편집 중」 안 뜸 |
| `openLabel` 이 시간으로 대기 | 캔버스 준비 전 도구 클릭 → 드래그 무시 (상태 대기로 교체) |
| 검수제출이 `disabled` 로 잔존 | 이미 제출된 상태에서 30초 클릭 타임아웃 (`submitForReview` 로 관용 처리) |
| 마킹 단계 영상 배정 창구 부재 | `019-01` 403 → `prep-marking-assign` 신설(마킹 설정 → 수동) |
| 중복 7건 | 케이스가 자기 대상을 겨냥하지 않고 화면 전체를 찍음 |

### ★ 새로 만든 것

- **`frontend/CAPTURE-SCENARIO.md`** — 시나리오 정본(9절). 인입 · **비식별 해시 확인** · 마킹 배정 경로 ·
  값 7종의 유래 · 촬영 순서 · 소진 케이스 · 검증 세 축 · **벌별 원장 쪼개기**.
- **`auditManifest`**(하네스 내장) — 원장 생성 때마다 빈 칸·규격·**중복**을 자동으로 훑는다.
  발화 시험 완료(심어 놓고 잡히는 것 확인). ⚠ 중복은 눈으로 못 잡는다 — 7건 중 하나가
  「수동 라벨링」 증적의 **빈 캔버스**였다.
- **`frontend/bin/split-manifest.py`** — 벌별 원장 쪼개기(각 벌 D11 에서 케이스ID 를 읽는다).
- **`docs/cbd-inputs/I1-소스파일목록.txt`** 갱신 + **뽑는 규칙**을 그 README 에 기록
  (`git ls-files ai-server backend frontend`).

### ★ CBD 3벌 재생성 (2026-09-09 · `~/Desktop/CBD-생성본-20260909g`)

```
포털포함 14종 · 증적 90컷/48케이스     포털제외 14종 · 83컷/44     포털향 14종 · 7컷/4
폐기 0 · 소스목록 어긋남 0
```
⚠ **원장은 벌마다 달라야 한다** — 전건을 주면 `포털제외`·`포털향` 이 「원장의 케이스ID 가 D11 에
없다」로 **멈춘다**(멈추는 것이 옳다). 상세는 시나리오 §8.

## ★★★ 사용자 지시 5건 — ①④ 완료 · ②③ 촬영 대기 · ⑤ 사용자 담당 (2026-09-10)

> 사용자: *"중복 컷 합치기는 어려우면 그건 내가 할게. 일단 4번까지 해줘."* →
> *"단위시험 절차서랑 결과서만 해주면 되"* → *"재채번으로 가고. 포털포함 문서의 경우에는 포털이
> 마지막으로 가고. 그리고 관리자 기능들도 빼달라고 했는데 아직 있는것 같아서 그것도 다시 확인해주고."*

### ★★ 순서의 진실원은 D11 초안이 아니었다 (착수 전 반드시 알아야 할 사실)

`D11 초안의 블록 순서를 바꿔도 산출물은 그대로 나온다.` 레시피가 **D3 §2 컴포넌트 목록**
(= CO 번호 = UC 번호 오름차순)을 순회하며 카드를 내고, 초안은 `blocks.get(coId)` 로 **키 조회**만
됐기 때문이다. 그래서 생성기를 함께 고쳤다 — 아래 「생성기 변경」.

### ✅ ④ 시험 순서 재배치 — 완료

```
진입·기반 → 영상 인입 → 비식별 → 마킹·시계열 → 작업 배정 → 라벨링 → 검수·버전 → 증강·해상도 → 현황·공지 → 포털
1.1 UT-001 인증 인계   …   1.23 UT-023 증강 요청   …   1.29~1.30 포털(마지막)
```
- 증강이 **1번 → 23번**으로 갔고 포털이 **마지막 두 자리**다(사용자 요청 확인).
- D11 초안 개정 **6.1**. 블록 본문은 한 글자도 안 바꿨다(기계 대조 0건 차이).

### ✅ ID 재채번 — 완료 (사용자 확정: 「순서대로 재채번」)

**UT-001(인증 인계) ~ UT-030(포털 자산 업로드) 연속**, 케이스 ID 48건 자동 추종.
- ⚠ **UT 번호 ≠ CO 번호가 됐다.** 두 축을 잇는 것은 각 카드의 **「관련 컴포넌트 ID」 한 칸뿐**이다
  — 그 칸을 비우면 추적이 통째로 끊긴다.
- CO 결번(012·014·015·017·020…)이 UT 에 옮아오지 않아 001~030 연속이 됐다.
- I2 결과서·R3 추적표는 **D11 md 를 읽으므로 자동 추종**한다(따로 고칠 것 없음).
- 증적 88장 파일명·캡처 원장·하네스 케이스 키·시나리오 문서까지 **전부 재매핑 완료**.
  매핑표: `<스크래치패드>/d11-final/case-id-map.json` (48건).

### ✅ 관리자 기능 제외 — 완료 (사용자 지적: 「아직 있는 것 같다」)

**있었다.** 「시험 범위 제외」 라벨이 붙은 5블록이 **카드로는 그대로 나오고** 있었다 —
관리자 기능(사용자 계정·역할 관리)이 설명·컴포넌트 ID·프로그램 ID 를 달고 두 번째 자리에.
읽는 사람에게는 「시험 대상인데 아직 안 적었다」로 보인다.
- 이제 **카드를 내지 않는다.** 카드 35 → **30**.
- 조용히 사라지지 않게 `SCOPE_EXCLUDED_NOT_CARDED` 경고가 **5건을 사유와 함께 전건 열거**하고,
  카드 수 불변식도 `카드 30 + 제외 5 = 35(D3 §2 컴포넌트 수)` 로 말한다.
- 산출물에서 「관리자」·「ADMIN」·「사용자 관리」·「역할 변경」 **각 0건**(양성 대조로 검사기 확인).
  남은 「계정」 17건은 전부 **작업자·검수자 권한 대조 절차**라 정상이다.
- 제외 5블록은 초안에만 남기고 **900번대 번호**를 줬다(본 절차 번호와 섞이지 않게 · 재채번과 충돌 방지).

### ✅ ① 비고에 컷 설명 — 완료

`frontend/capture-captions.mjs` **신설**(48케이스 · 90캡션). 원장이 매 실행 재생성되므로 캡션은
**코드에 있어야 살아남는다.**
```
KLID-AT-UT-017-01 | 키프레임 사이 자동 보간 작업 전 — 현재 프레임 객체 수
KLID-AT-UT-017-01 | 보간 실행 후 — 사이 프레임에 좌표가 채워진 결과
KLID-AT-UT-017-01 | 보간 결과 저장 후 저장됨 표시
```
사용자가 예로 든 그 형태 그대로다. **I2 증적 표 90행 · 비고 빈 칸 0.**
- 누락 방지: 캡션 없는 컷을 **이름으로 열거**한다(`!! 캡션 없는 컷 1건 — … 컷 3/3`). 발화 시험 통과.
- ⚠ **내가 만든 그 고지가 한 번 거짓말했다** — 공유 컷 처리(`SHARED`)가 캡션을 덮어쓰는데 계수는
  그 **전에** 해서 `90/90` 이라 말했고 실제로는 88이었다. 산출물에서 확인해 잡았다.
  **「검사가 통과했다」를 산출물 대조 없이 믿지 말 것.**

### ✅ ② 오토라벨 전후 · ③ SAM2 외곽 분할 — 완료 (2026-09-10)

**②** cut1 「0개 객체」(라벨을 전부 지우고 저장) → cut2 「10개 객체 · AI 탐지 10건 적용됨」.
찍은 **뒤에** 저장한다 — 저장하면 토스트가 사라져 컷이 밋밋해지고, 저장하지 않으면 뒤따르는
③이 라벨 없는 프레임을 연다.

**③** 도로면을 클릭해 **차선을 따라 도로 경계**를 딴 폴리곤. 원인이 세 겹이었다:
1. 클릭 좌표가 코드에 박혀 있어 **빈 노면**을 찍었다
2. 라벨 자리를 찾게 고쳤더니 **이미지 레이어를 라벨 레이어로 오인**했다(채움 비율로 가른다)
3. ★★**그러고도 전건 실패였는데 원인은 판정이었다** — 클릭으로 만든 분할은 **미리보기**일 뿐
   객체가 되지 않는다(`asPreview: true`). **확정은 Enter** 다. 같은 좌표를 모델에 직접 물으니
   **0.62·0.72·0.96**(임계 0.3)이 나왔다. **분할은 내내 성공하고 있었고 성공을 못 알아본 것**이다.

- ⚠ **여러 점은 역효과다** — 한 점 0.53 → 세 점 0.32. SAM2 는 점이 늘면 그 점들을 **모두 품는**
  영역을 찾으므로 조금만 어긋나도 대상이 흐려진다.
- ⚠ **라벨이 붙은 가장 넓은 덩어리는 비식별 가림막이었다** — 면적으로 고르면 개인정보를 가린
  검은 띠의 외곽선을 딴 그림이 증적으로 남는다(사용자: *"마스킹 블록은 좀 곤란하지"*).
  그래서 대상을 **화면에서 크고 또렷한 것**(도로면·건물)으로 옮겼다. 좌표는 캔버스 **비율**로 준다.
- ⚠ 화면 **가장자리에 걸친** 대상은 외곽선이 프레임에 잘려 좁고 길게 나온다 — 안쪽부터 시도한다.

### ★★★ 재채번이 낳은 결함 하나 — 잡았다 (2026-09-10)

**UT 번호를 「그 벌의 카드 차례」로 매기면 같은 ID 가 벌마다 다른 시험을 가리킨다.**
실측: 전 범위 벌의 `UT-001` 은 인증 인계인데 **포털향 벌의 `UT-001` 은 포털 라벨 작업**이었다.
케이스 ID 는 증적 파일명·캡처 원장·앞뒤 산출물이 물고 있는 **식별자**라, 그것이 벌마다 흔들리면
**엉뚱한 증적이 오류 없이 붙는다** — `split-manifest.py` 의 「증적 없는 케이스 1건」 경고가
그 직전에 잡아 줬다(그 경고가 없었으면 그대로 나갔다).

**고친 방향**: 번호의 근거를 「이 벌의 카드 차례」에서 **「모든 벌이 공유하는 외부 초안에서의
차례」**로 옮겼다. 한정 벌은 001 부터 시작하지 않지만(포털향 = `UT-029`·`UT-030`) 그 안에서
여전히 오름차순이라 차례는 읽히고, **어느 벌에서든 같은 시험이 같은 번호**다.

⚠ 이 성질은 앞으로도 지켜야 한다 — 벌을 새로 만들거나 초안 순서를 바꿀 때 **벌 사이에 같은
번호가 같은 시험인지** 반드시 대조할 것(대조 명령은 각 벌 D11 의 `### 1.N 단위시험 …` 줄).

### 🔲 (참고) 원래 계획 — 촬영 준비 완료 상태였을 때의 기록

**246 에 네트워크가 닿지 않아 촬영을 못 했다**(ping 100% loss · SSH handshake timeout, 2회 시도).
서버가 살아나면 `./run-capture.sh 015-01 016-01` 한 번으로 끝난다.

- **② `015-01`(옛 039-01)** — 이제 **프레임의 라벨을 전부 지우고 저장한 뒤** 「전」을 찍고,
  AI 탐지를 돌려 「후」를 찍는다. 구 동작은 배치가 이미 붙여 둔 라벨 위에 다시 돌려 11→14 밖에
  안 늘어 전후가 눈에 보이지 않았다.
  ⚠ **실제 작업 데이터를 지운다** — 대상은 증적 전용 영상(raw 43)의 프레임이어야 한다.
  ⚠ **지우고 저장까지** 해야 한다(저장 안 하면 서버엔 그대로라 중복 억제로 「후」가 예전 그림).
- **③ `016-01`(옛 005-01)** — 클릭 좌표 하드코딩(`555,520`)이 원인이었다. 이제 **라벨이 그려진
  픽셀**을 후보로 뽑아(오토라벨 상자 자리 = 대상이 실재하는 자리) 성공할 때까지 최대 12곳 시도하고,
  **적용이 안 되면 촬영하지 않고 실패**한다(「낮은 신뢰도」 안내만 남은 화면은 증적이 아니다).
  ⚠ 246 의 SAM2 는 **목이 아니다** — 그 23%는 실제 모델의 정직한 출력이었다.
- ⚠ **②가 ③보다 먼저 돌아야 한다**(③의 클릭 후보가 ②가 붙인 라벨에서 나온다). `--all` 은 알파벳순이라 자동.

### ✅ ⑤ 중복 컷 합치기 — 완료 (컷 90 → 88)

**판정 기준은 「그 케이스의 예상결과가 그 컷을 요구하는가」다.** 그림이 비슷하다는 것만으로 빼면
증명이 사라진다 — 실제로 차이율 0.25% 미만인 케이스가 6건이었는데 그중 넷은 각 컷이 **서로 다른
예상결과를 증명**하고 있었다.

| 케이스 | 컷3 | 판정 | 근거 |
|---|---|---|---|
| `002-01` | 재진입 유지 | **제거** | 예상결과 4항 어디에도 「다시 열어도 유지」가 없다(그건 003·004 가 요구한다) |
| `004-01` | 재진입 유지 | **제거** | cut2 와 **차이율 0.00%**(픽셀 동일) — 같은 그림을 한 장 더 실어도 증거가 되지 못한다 |
| `003-01` | 재진입 2.5px | 유지 | 예상결과 2 가 「화면을 다시 열어도 2.5 가 유지된다」를 명시 |
| `019-01`·`019-02`·`028-01` | — | 유지 | 0.2%대지만 각 컷이 별개 예상결과(검수중→완료·재승인 안내 / 반려 전후 / 미발행→발행) |

- ★**검증은 남기고 사진만 뺐다** — `expectVisible` 로 값이 유지되는지는 계속 확인한다.
  기능 확인을 없앤 것이 아니라 같은 그림이 한 장 더 실리는 것을 없앴다.
- **`004-01` cut2 캡션 보정** — 카드 영역만 찍어 저장 알림이 프레임 밖이라 「저장 직후의 결과
  안내」가 사실이 아니었다. 「저장한 뒤 — 재진입해도 같은 값이 유지된다」로 고쳤다.
- 원장 **48케이스 · 88컷 · 캡션 88/88** · I2 증적 표 **88행 · 비고 빈 칸 0**.
- ⚠ 삭제한 두 장은 `~/Desktop/작업백업-20260910-1124/캡처루트` 에 있다.

### ⚠ 벌별 원장 2개를 격리했다 — 다시 만들어야 한다

`manifest-포털제외.json`·`manifest-포털향.json` 이 **옛 채번 · 90컷 시절 · 캡션 없음** 이었다.
그대로 두면 다음 생성에서 옛 판이 섞인다 →
`~/Desktop/CBD-캡처루트-20260909/_낡은-벌별원장-재생성필요/` 로 옮기고 사유를 적어 뒀다.
CBD 3벌을 생성한 뒤 `python3 frontend/bin/split-manifest.py` 로 다시 만든다.

### 생성기 변경 (`md_uml/convert-service` — 피어 레포, 미커밋)

| 파일 | 무엇 |
|---|---|
| `recipes/unittest.js` | `orderComponentsForCards` 신설(초안 등장 순) · `utIdOf` 를 **카드 차례** 기반으로 · 범위 제외 블록은 **카드를 내지 않음** + `SCOPE_EXCLUDED_NOT_CARDED` |
| `recipes/unittestresult.js` | 원장이 `{src, caption}` 수용 · 「비고」를 캡션으로 채움 · 채운 칸 수를 경고에 고지 |
| `captureLedger.js` | `ledgerCuts`·`cutsToManifest` 가 캡션을 관통(잃으면 비고가 통째로 빈다) |

**사업 무관하게 설계했다** — 순서·캡션 어느 것도 코드에 박히지 않고 전부 입력(초안·원장)이 정한다.

### ✅ CBD 3벌 재생성 · 제출 묶음까지 완료 (2026-09-10)

```
~/Desktop/KLID-AT-설계산출물-20260910.tar.gz   396MB · 142파일
sha256 4a8fdff4160250a6933d33f66d06ed6c14c5a29dec1ed8be5f6df5bf506721c1
생성본 ~/Desktop/CBD-생성본-20260910/{포털포함,포털제외,포털향}  각 14종
검증   풀어서 바이트 대조 어긋남 0 · NFD 0 · AppleDouble 0 · 소유자 (0,'')
```

**소스 zip 도 다시 뽑았다** — 이전 것은 6커밋 전 판본이었다. 3,617파일 · 풀어서 바이트 대조 0건 차이.

### ⚠ 이 라운드가 남긴 세 가지 (다음 사람이 반드시 알 것)

**① 공용 변환 서버가 낡았다 — 피어 협의 사항**
xlsx 규격 결함(`<sheetPr>` 자식 순서 · 인쇄영역 절대참조)이 이전 회차와 **수치까지 똑같이** 나왔다.
핸드오프 §6 은 그것을 「해결됨(피어 `21883a1`)」으로 적었고 그 커밋은 **HEAD 에 실제로 있다.**
그런데 **로컬 변환 서버로 다시 뽑으니 바로 사라졌다** — 즉 고친 코드가 `100.125.213.13:8090` 에
반영되지 않았다. **당분간 xlsx(R1·R3)는 로컬로 재변환해야 한다.**
```bash
node bin/reconvert.mjs --dir <벌> --docs R1,R3 --convert-url http://127.0.0.1:8799
```

**② macOS tar 가 `._` 파일을 함께 넣는다 — 윈도우에서 쓰레기로 보인다**
소유자를 맞추려 `--uid 0 …` 를 붙였더니 파일이 **142 → 289** 로 늘었다(AppleDouble 147개).
`COPYFILE_DISABLE=1` + `--no-xattrs` 를 함께 줘야 한다. **파일 수를 세지 않으면 안 보인다.**
```bash
COPYFILE_DISABLE=1 tar --uid 0 --gid 0 --uname '' --gname '' --no-xattrs -czf <출력> <폴더>
```

**③ 남은 검증 결함은 「그림」 한 축 — 사용자 확정으로 그대로 둔다(A안)**
증적 스크린샷 안 작은 글씨가 인쇄 시 2.7pt 미만이 된다(88장 중 21장). 1920px 화면을 지면
150mm 폭에 넣으면 **1px 이 0.258pt** 라 화면에서 10.5px 이상인 글자만 살아남는다. 미달분은
부가 설명·눈금 글씨(7~9px)이고, **화면 판정에 쓰이는 요소(제목·상태 배지·알림)는 12px 이상**이라
인쇄본에서도 읽힌다. 이전 회차도 같은 상태로 제출됐다.

### ✅ D2 「기술적 고려사항」 칸 · SCREEN-001 그림 (2026-09-10 사용자 지적 2건)

**① 그 칸에 엉뚱한 데이터가 들어가 있었다** — 소비 API 36 · 화면 주소 37 · 대상 화면 38 ·
접근 역할 34건. 넷 다 화면 메타이고 **1차 제출본 표에는 그 칸 자체가 없다**(제출본 실측:
KLID-AI-SC-002 = *"1영상 처리 시 시스템 자원을 많이 사용하므로 1건씩 배치 처리로…"* ·
KLID-AI-SC-001 = **빈 칸**). 원인은 `screen_spec` 스키마에 **그 칸을 담을 필드가 없어**
생성기가 있는 것으로 때운 것.
- 입력을 신설했다 — `docs/cbd-inputs/D2-화면별-기술적고려사항.md`(20화면 43항목).
  **전부 화면 사양 본문에 이미 서술돼 있던 내용**이고 새로 판단한 제약은 0건이다.
- 제약이 없는 화면은 **비운다**(마커 아님 — 제출본과 같다). 몇 칸을 채웠는지는
  `SCREEN_TECH_NOTE` 경고가 분모와 함께 말한다.
- ⚠ **옮기며 어휘가 바뀐 자리가 있어 기계 대조로는 진짜와 가짜가 안 갈린다** — 어간 대조기가
  일부러 심은 가짜는 잡았는데(90%·73%) 진짜 항목도 같은 점수를 냈다. 낱말을 원천 그대로
  되돌리고 그 사실을 입력 파일 머리에 적었다. **대조는 사람이 화면 사양을 열어서 한다.**
- ⚠ 배선하다 한 번 깨뜨렸다 — 입력을 `source` 로 넘겼더니 **화면이 0건**이 돼 생성이 멈췄다.
  그 통은 스냅샷을 **대체**하는 자리이고 D2 는 스냅샷을 읽는다. D11·I2 가 같은 형태로 넘겨도
  무해했던 것은 **그 둘이 스냅샷을 읽지 않기 때문**이다. `localInputs` 로 넘긴다.

**② SCREEN-001 그림이 왼쪽에 눌려 있었다** — 좌측 메뉴가 없는 화면인데 `wf-body`(2열 그리드
220px + 1fr)를 쓰고 자식이 `wf-content` 하나뿐이라 **220px 열에 짜부라져** 제목이 세로로 한 글자씩
쪼개졌다. 다른 **37화면은 전부 `wf-app > main` 직결**이라 이 한 건만 어긋나 있었다.
- 원장 렌더를 받아 **두 자리만** 고쳤다(문자 opcode `delete` 만 — 다른 바이트 불변, 27자 감소).
  `upload_static_render` 로 게시(v23, sanitize 제거 0건) 후 **서버에서 다시 받아** 확인.
- ⚠ **스냅샷을 새로 떠야 산출물에 반영된다** — 렌더 HTML 은 `screens[].renderHtml` 에 붙고
  그건 `pin-snapshot` 이 `attachWireframes` 로 채운다. 기존 스냅샷을 그대로 쓰면 옛 그림이 나온다.
- ⚠ **REST 업로드는 401** 이다(세션 인증 전용). 읽기는 되고 쓰기는 MCP 도구로만.
  ★ 스냅샷 토큰은 **MCP 설정의 `Authorization: Bearer` 값에서 접두를 뗀 것**을
  `LOGICRAFT_TOKEN` 으로 주면 통한다(헤더 이름은 서로 다르나 값은 같다).

**재생성 범위** — 사용자 지시로 **포털포함 벌만** 다시 뽑았다(스냅샷 `_원천스냅샷-20260910-포함.json`).
포털제외·포털향은 ①만 반영된 판이고 ②는 안 들어갔다.

### ⚠ macOS 가 제출 묶음에 쓰레기를 넣는다 — 두 번 당했다

`._*`(AppleDouble) 147건 · `.DS_Store` 1건. **둘 다 파일 수를 세어서 잡았다**(142 → 289, 142 → 143).
tar 옵션에 셋을 함께 준다:
```bash
COPYFILE_DISABLE=1 tar --uid 0 --gid 0 --uname '' --gname '' --no-xattrs --exclude '.DS_Store' -czf …
```

### 다음에 할 일
1. **공용 변환 서버 갱신**(피어) — 그전까지 xlsx 는 로컬 재변환
2. 포털 4케이스(`029-*`·`030-*`)는 포털 채널로 찍는다 — 이번 라운드에서 손대지 않았다
3. `md_uml` push 는 피어와 협의 후

---

### ★ 남은 일 (순서)
1. 프론트 구현 회수 → **`klid-qa-verifier`** 독립 QA(백엔드·프론트)
2. 246 재배포 → **`016-02` 성공 경로 촬영**(지금은 창구와 전제조건 가드까지만 찍혀 있다)
3. 로컬 키트 SYNC · `SD-021` 시안 재게시 · `SCREEN-032`·`SEQ-001` 정적 렌더 미러 재생성
4. 위키 `docs/v2-wiki/` · 코드 층 대조
5. 피어 신호 오면 CBD 3벌 재생성 → 검증 → 배포판 압축

### ★★ 이번 구현이 남긴 함정 (다음에 같은 실수를 막는다)
- **완화 변경의 가드를 「완화된 쪽이 통과한다」로만 짜면, 판정이 통째로 사라져도 계속 초록이다.**
  실측: 변이 M2(판정을 `return true`)에서 완화 가드는 **전부 통과**했고 실패한 것은 무결성 양성
  대조 4건뿐이었다. **푼 축과 안 푼 축을 각각 변이로 때려야** 무엇이 남았는지 증명된다.
- **판정식을 제거하면 그 경계값 시험들이 통째로 같은 뜻으로 수렴한다** — 기대값만 뒤집어 다 남기면
  「이름만 다른 같은 시험」이 쌓인다. 대표만 남기고 왜 걷어냈는지를 그 자리에 적을 것.
  (실측: 시간 조건 경계 8건이 전부 같은 뜻이 됐다 → 325줄 감소, 다른 축 손실 0)
- **`./gradlew … | grep -c FAILED` 로 검증하지 말 것** — 실패가 **0건일 때 grep 이 종료코드 1** 을 내
  복합 명령이 실패로 보고된다. 백그라운드면 「failed」 알림까지 뜬다. 실측으로 그렇게 오판할 뻔했다.
  판정은 **결과 XML 집계**로 한다.

### 판단 대기
- `AC-1067` 에 400 거부(빈 파일명·목록 밖)를 넣을지 — `UC-016 v33`·`API-094 v10` 은 규정하는데 AC 에 없다
- `AC-1066`·`AC-1067` 의 `verification_status=passing`(2026-08-31)은 **폐기된 전제를 검증한 것**이라
  넓어진 단언을 증명하지 못한다 — `unverified` 로 되돌릴지
- ㉯ 판정 3건에 남는 읽힘 위험 — 한정어를 넣으려면 `EXTSYS-003`·`INT-005` 확정을 뒤집어야 한다

---

## 4. CBD 산출물 재생성 (피어 md-uml-ec 소관)

- **배치**: `§2 시험결과 증적`을 문서 끝에서 각 단위시험 블록 아래로 옮김 — 완료
- **행당 그림 하나**: 한 칸에 여러 장을 쌓으면 **셀 높이가 지면을 넘어 아래가 잘린다.**
  컷 수만큼 행을 늘린다. **행 수 = 컷 수 = 90** (수치를 코드에 박지 말 것)
- ✅ **피어 작업 도착** — `md_uml@8541134 fix(I2): 증적 표를 한 행에 그림 하나로`. 워킹트리 깨끗.

### 재생성 입력 — 전부 경로가 확정됐다 (스크래치패드 의존 0)

| 인자 | 경로 |
|---|---|
| `--snapshot` | `~/Desktop/CBD-생성본-20260909f/_원천스냅샷-20260909f-{포함\|제외\|포털향}.json` (09-09 16:10 · 설계 확정 10:26 이 이미 들어 있다) |
| `--migrations` | `backend/src/main/resources/db/migration` |
| `--source-list` | **`docs/cbd-inputs/I1-소스파일목록.txt`** ← 스크래치패드에서 레포로 들여옴(3,602줄 · 실재 확인 3,602/3,602) |
| `--draft` | **`docs/cbd-inputs/D11-단위시험절차서-초안.md`** ← 레포 사본이 정본. 09-09 에 갈려 있던 것을 맞췄다 |
| `--captures` | `~/Desktop/CBD-캡처루트-20260909/capture-manifest.json` |
| `--captures-root` | `~/Desktop/CBD-캡처루트-20260909` |
| 코드 | `--cbd-project KLID --cbd-subsystem AT` · `--docs all` · `--variant database=v2` |

⚠ **직전 회차(`20260909f`)는 증적이 27컷/17케이스뿐**이다. 지금 원장은 **90컷/48케이스**라
그 회차 산출물과 직접 비교하면 증적 축이 크게 늘어난 것으로 나오는데 **그것이 정상**이다.

세 벌이 다 찬다: 포털제외 33블록 · 포털포함 35블록 · **포털향 2블록**(그 둘이 전부)
⚠ 포털을 범위에서 빼면 **포털향 벌이 통째로 빈다** — 그래서 철회했다. 다시 빼지 말 것.

---

## 5. ★ 남겨 두는 위험 — CORS 는 지금 결함이 아니지만 유효기간이 있다

246 실측: 동일 출처 201 · 다른 출처 전부 403 · `CORS_ALLOWED_ORIGINS` 미설정(빈 값).
**빈 값이 의도된 값이다** — FE·BE 를 같은 출처로 배포하기 때문이고, 템플릿·설정 문서·회귀 가드
(`DevProfileWiringGuardTest`)가 전부 그렇게 말한다.

⚠ **`INT-013` 의 Module Federation 임베딩이 들어오면 Host 오리진에서 우리 BE 를 부르게 되어
CORS 가 적용된다.** 그때 `CORS_ALLOWED_ORIGINS` 에 포털 Host 오리진이 없으면 **전량 403** 이다.
배포 문서에 변수는 있으나 **그 값을 채워야 한다는 사실이 어디에도 없다.** 임베딩 착수 시 확인할 것.

---

## 6. xlsx (해결됨)

`<sheetPr>` 자식 순서가 `pageSetUpPr → outlinePr` 로 뒤집혀 Excel 이 시트를 통째로 버렸다
(`CT_SheetPr` 는 `tabColor → outlinePr → pageSetUpPr`). 설계산출물·인계 폴더 **13개 전량 교정**.
파이프라인은 피어가 `21883a1` 로 고쳤고 `submit_check` 에 xlsx 규격 축이 늘었다.
⚠ Print_Area 상대참조(`$A1:$L40`)도 함께 고쳤으나 **그것은 차단 원인이 아니었다** — 인과를 물려주지 말 것.
