# 구현 계획: 배정 목록(작업목록·WORKER) 검색 필터 서버사이드 전환

> 생성: 2026-07-30 | 유형: DEV (규모 L, BE 중심 + FE) | 상태: 착수 대기
> 🎯 목표: 라벨러 작업목록의 검색·상태·이벤트유형 필터가 **현재 페이지 20건이 아니라 전체 데이터셋 기준**으로 동작하고, 전체건수·페이지네이션·드롭다운 옵션이 그와 일치하며, 정렬 키가 allowlist 로 통제되는 상태.
>
> ⚠ 이 파일은 이전 계획("1차 검증 이슈 74건 수정", 9/10 Phase 완료)을 사용자 승인 하에 교체한 것이다. **남아 있던 Phase 10(잔여 9건 — self-fill 원천 매핑·오토라벨 좌표정책·무결성·성능)은 git 이력에서 복원**할 것: `git show HEAD:.claude-plan.md`

## Phase 진행 현황

- [x] Phase 1: BE 검색 조건 계층 + 정렬 allowlist + 작업중 판정 일원화 ✅ PASS
  > 구현: `assignment/dto/AssignmentSearchCondition.java`(신규) · `assignment/repository/AssignmentQueryRepository.java`(신규) · `assignment/domain/AssignmentWorkStatus.java`(신규) · `common/util/LikeEscape.java`(신규) + `AssignmentController`·`AssignmentService`·`AssignmentResponse`·`SortAllowlist`·`TaskBoardQueryRepository`·`LsTaskAssignmentRepository` 수정 + 테스트 5종
  > QA: 코드리뷰 PASS · API 하위호환 PASS · DB OK · 요구추적 CONFORM · 빌드 SCOPED PASS · 보안 CONDITIONAL PASS(MEDIUM 2 → DEV_FIX 해소) · 적대검증 BROKEN → DEV_FIX 후 CLOSED · 커버리지 HIGH 해소
  > ★계획 대비 변경: **'작업중' 판정 신호를 `LS_DATA_LBL` 존재 → `LS_DATA_LBL_HSTRY` 저장이벤트 존재로 교체**. 배치 오토라벨이 `AUTO_LBL_YN='Y'` 로 라벨을 미리 적재해 배정 직후 전 영상이 '작업중'이 되는 결함을 적대검증이 잡음. 판별식은 V139 뷰 `V_COMPLETED_LABEL_CHANGE` 와 동일(`ADD/MDFCN/DEL_CNT` 합>0)
  > ⚠ 수용된 한계(사용자 확정): 트랙 분할·병합·객체 속성 저장은 이력을 남기지 않아 그 작업만 한 경우 '대기'로 표시됨. 이력 도입 이전 레거시 작업분도 동일(백필 안 함)
- [x] Phase 2: BE 이벤트유형 옵션 API (전체 기준) ✅ PASS
  > 구현: `GET /v1/assignments/event-types` — `AssignmentController`·`AssignmentService`(`listEventTypeOptions` + 인가 조립 `scopeForActor` 단일화)·`AssignmentQueryRepository`(`findDistinctEventTypes`, `buildWhere(..., includeEventType)`) + 신규 테스트 `AssignmentEventTypeOptionsTest`(18)·`SaveHistoryChangeViewParityIT`(2)
  > QA: 코드리뷰 PASS(9.5/10) · 빌드 SCOPED PASS(494) · 보안 PASS(조건 없음, CRITICAL/HIGH 0)
  > 캐리오버 처리: `hasSaveHistoryExists` javadoc 정정(실제 기록/미기록 경로 + 알려진 한계) · V139 뷰 판별식 파리티 가드 IT
  > → Phase 3 로 이월된 LOW 2건: 옵션 절단(truncated=true) 경로 미검증 · SQL `trim` ≠ Java `normalize` 로 옵션↔필터 정합이 제어문자 데이터에서 깨짐
- [x] Phase 3: 하위호환 회귀 가드 (ListApiBackwardCompatibilityIT 확장) ✅ PASS
  > 구현: `ListApiBackwardCompatibilityIT` +5(기본정렬 불변·필터 optional·미등록키 400·**세 엔드포인트 정책 차이 교차 검증**·event-types 인가) · `AssignmentEventTypeOptionsTest` +2(절단 501종 실증·제어문자 왕복)
  > 캐리오버 처리: 신규 `common/util/ControlCharNormalizer.java` 로 Java 입력·SQL 옵션·SQL 필터 비교의 정규화 축을 **단일 원천**으로 통일 (RED 실측: 옵션에 `"\t"` 자체가 별도 항목으로 노출되고 있었음)
  > QA: 빌드 SCOPED PASS(145) · DB 쿼리비용 수용가능(개선 권고 1) · 요구추적 CONFORM
  > → Phase 4 로 이월: `normalizedAsJava` 의 `replace` 64중첩 → `translate(col, literal, '')` 단일 호출로 경량화(비차단 권고)
- [x] Phase 4: FE 서버사이드 전환 (클라이언트 필터 제거 + 전체건수·옵션 정합) ✅ PASS
  > 구현: `features/task/{types,api,boardParams}.ts` · 신규 `hooks/useAssignmentEventTypes.ts` · `pages/TaskListPage.tsx`(클라이언트 재필터 제거·건수 서버값·옵션 API 연동) · `TaskFilters.tsx` · `lib/queryKeys.ts` + 신규 테스트 14 + 기존 3 갱신
  > 캐리오버: `ControlCharNormalizer` 를 `translate` 단일 호출로 경량화(파라미터 바인딩은 PG `SELECT DISTINCT` 제약으로 500 → `chr()` 인라인 유지)
  > 문서: `docs/v2-wiki/04-screens-ia.md`·`12-review-assignment.md` (서버필터 전환·신규 API·IN_PROGRESS 판정 근거·알려진 한계)
  > QA: 코드리뷰 PASS(이슈 0) · 요구추적 **CONFORM**(R1~R8 + 수용기준 6개 전부, Phase 경계 누락 없음) · **FULL 회귀 PASS**(BE 4,290 + FE 1,691 = 5,981, 실패 0)
  > 판단: debounce 는 타이머 대신 제출(조회 버튼) 기반 — `onChange` 즉시 조회 경로가 없음을 코드로 확인, 요구 취지(연타 스팸 방지) 충족

## 요구사항 요약

- 목적: 라벨러가 작업목록에서 검색·필터를 걸면 **전체 배정 중에서** 찾아야 하는데, 지금은 현재 페이지에 담긴 20건 안에서만 걸러진다. 뒷페이지 항목은 검색해도 나오지 않고 "전체 N건" 표시도 실제와 어긋난다.
- 범위: 포함 = `GET /v1/assignments`(WORKER 시각) 필터·정렬·집계 + 이벤트유형 옵션 API + FE 작업목록 화면. 제외(out of scope) = REVIEWER 경로(`/v1/tasks/board`) 계약 변경 · 라벨링/검수 화면 · 배정 생성·재배정 로직.
- 주요 액터: WORKER(라벨링 작업자) — 본인 배정 목록. REVIEWER 는 같은 화면에서 별도 엔드포인트를 타므로 이번 변경 대상 아님.
- 요구 목록:
  - **R1**: `q`(영상명/작업자명)·`workStatus`·`eventTypeCd` 필터를 **서버사이드 전체 기준**으로 전환. FE 클라이언트 필터(`TaskListPage.tsx:316-335`) 제거 | 출처: 사용자 명시
  - **R2**: 전체건수·페이지네이션을 서버 `totalElements` 기준으로 통일 (현재 `visibleRows.length` = 현재 페이지 내 집계, `TaskListPage.tsx:346-352`) | 출처: 조사 중 발견 → 사용자 확인
  - **R3**: 정렬 = **allowlist + 미등록 키 400(strict)**. FE 가 실제 보내는 키와 기존 기본값(`regDt,desc`)은 allowlist 에 포함해 **기존 호출 동작 불변** | 출처: 구체화 Q&A
  - **R4**: 이벤트유형 드롭다운 옵션을 **전체 기준 목록**으로 제공 (현재 `TaskListPage.tsx:224-231` 이 현재 페이지 20건에서만 수집) | 출처: 구체화 Q&A
  - **R5**: `IN_PROGRESS(작업중)` 옵션 유지 + BE 구분 근거 신규 구현. **판정 기준 = 그 영상에 저장된 라벨 존재 여부**(사용자 확정 2026-07-30, 스키마 변경 없음) | 출처: 구체화 Q&A
  - **R6**: WORKER IDOR 방어(본인 배정만) 유지 — **인가 조건과 필터 조건을 분리**. REVIEWER 전용 `workerId` 파라미터가 WORKER self-scope 강제와 섞이지 않게 할 것 | 출처: 사용자 명시
  - **R7**: `ListApiBackwardCompatibilityIT` 에 `/v1/assignments` 커버리지 신설 | 출처: 조사 중 발견 → 사용자 확인
  - **R8** (파생 — 계획 수립 중 도출): 필터 판정과 **응답 상태 표시가 같은 근거를 써야 한다**. 필터만 바꾸고 `AssignmentResponse.mapToFeStatus` 를 그대로 두면 "작업중으로 필터했는데 목록엔 '대기'로 표시"되는 모순이 생긴다.
- 수용 기준:
  - [ ] 필터를 걸어도 전체건수·페이지 수가 **전체 데이터셋 기준**으로 정확하다(현재 페이지 집계 아님)
  - [ ] 파라미터 없는 기존 호출의 응답(정렬·페이징·필드)이 변경 전과 **동일**하다
  - [ ] 미등록 정렬 키는 400, FE 전송 키와 기본값 `regDt,desc` 는 200
  - [ ] 이벤트유형 옵션이 **페이지와 무관하게** 전체 배정 기준으로 나온다
  - [ ] WORKER 가 **어떤 필터·정렬·`workerId` 조합으로도** 타인 배정을 볼 수 없다
  - [ ] `workStatus=IN_PROGRESS` 필터 결과의 각 행이 목록에서도 '작업중'으로 표시된다 (R8)
- 비기능 요구사항: 목록 1회 조회의 추가 쿼리는 상수 개(N+1 금지). LIKE 검색은 이스케이프 후 바인딩(CWE-89). 정렬 개수 상한(CWE-770).

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | **없음** (R5 를 라벨 존재 EXISTS 로 파생 — 컬럼·마이그레이션·백필 없음) |
| 기존 API | `GET /v1/assignments` 에 optional 파라미터 3종 추가(기본값 불변) + 정렬 allowlist 도입(미등록 키 500→400) |
| 신규 API | `GET /v1/assignments/event-types` |
| 도메인 모듈 | `kr.co.cudo.authoring.assignment.{controller,service,repository,dto}`, `common.util.SortAllowlist` |
| 프론트엔드 | `pages/TaskListPage.tsx`, `features/task/{api.ts,types.ts,components/TaskFilters.tsx}` |
| 설정/환경 | 없음 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | 정렬 allowlist 도입으로 **지금 우연히 동작하던 정렬 키가 400** 이 됨(임의 엔티티 필드가 그대로 통과 중) | HIGH | FE 가 실제 보내는 키 전수 확인 후 allowlist 구성. 기본값 `regDt` 필수 포함. Phase 3 IT 로 고정 |
| 2 | 필터 판정(WHERE)과 표시 매핑(`mapToFeStatus`)이 갈라져 **필터 결과와 화면 표시가 모순**(R8) | HIGH | 라벨 존재 여부를 쿼리 **프로젝션으로 함께 반환**해 WHERE 와 표시가 같은 값을 쓰게 한다. 전용 테스트로 고정 |
| 3 | 라벨 존재 EXISTS 가 행마다 평가돼 **목록 조회 비용 증가** | MED | 상관 서브쿼리 1회(EXISTS)로 제한, 조인 금지(행 증식). `LS_DATA_LBL` 프레임 FK 인덱스 확인 후 필요 시 기록만(마이그레이션은 별건) |
| 4 | 인가 조건(WORKER self)과 필터 조건(`workerId`)이 한 객체에 섞여 **IDOR** | HIGH | 조건 객체에서 두 필드를 분리하고, WORKER 면 서비스가 `workerId` 를 **무시하고 self 로 강제**. 필터 조합 IDOR 테스트 필수 |
| 5 | `EVNT_TYPE_CD` 인덱스 부재로 이벤트 필터가 풀스캔 | LOW | board 가 이미 같은 상태라 신규 갭 아님. 이번 범위에서 인덱스 추가하지 않고 기록만 |
| 6 | 이벤트유형 옵션 API 가 목록과 **다른 조건**을 써서 "옵션엔 있는데 고르면 0건" | MED | 목록과 **같은 `buildWhere` 공유**, 단 자기 축(eventTypeCd)만 제외 |

---

## 상세 계획

### Phase 1: BE 검색 조건 계층 + 정렬 allowlist + 작업중 판정 일원화

**목표**: `GET /v1/assignments` 가 `q`/`workStatus`/`eventTypeCd` 를 전체 기준으로 필터링하고, 정렬 키를 allowlist 로만 해석하며, '작업중' 판정이 필터와 표시에서 동일 근거를 쓴다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `assignment/dto/AssignmentSearchCondition.java` | 생성 | 검색 조건 record — **인가 축과 필터 축 분리** |
| `assignment/repository/AssignmentQueryRepository.java` | 생성 | QueryDSL 검색 — 단일 `buildWhere` 를 목록·count 가 공유 |
| `common/util/SortAllowlist.java` | 수정 | `ASSIGNMENT` 맵 추가 (strict `resolve` 사용) |
| `assignment/controller/AssignmentController.java` | 수정 | optional 파라미터 3종 + `SortAllowlist.apply` |
| `assignment/service/AssignmentService.java` | 수정 | 조건 조립(인가 강제) → 쿼리 리포지토리 위임, 기존 enrich 유지 |
| `assignment/dto/AssignmentResponse.java` | 수정 | `mapToFeStatus` 가 라벨 존재 신호를 받아 `IN_PROGRESS` 산출 |

**핵심 로직 / 시그니처**:
```java
// AssignmentSearchCondition — 인가 축(selfUserNo)과 필터 축(workerIdFilter)을 분리 (R6)
public record AssignmentSearchCondition(
        Long selfUserNo,      // WORKER 면 non-null → 무조건 이 값으로 강제 (필터가 아니라 인가)
        Long workerIdFilter,  // REVIEWER 전용 필터. WORKER 요청이면 서비스가 null 로 무시
        String q,             // 영상명/작업자명 부분일치 (LIKE, 이스케이프 필수)
        String workStatus,    // PENDING | IN_PROGRESS | REVIEW_PENDING | COMPLETED | REJECTED
        String eventTypeCd) {}

// AssignmentQueryRepository
Page<AssignmentRow> search(AssignmentSearchCondition cond, Pageable pageable);
//   AssignmentRow = 배정 + rawSn + **hasLabel(boolean)** 프로젝션 (R8 — 표시와 필터가 같은 근거)

// 작업중 판정 (R5) — 라벨 존재 EXISTS. 조인 금지(행 증식 차단)
//   hasLabel = EXISTS (SELECT 1 FROM LS_DATA_LBL l
//                        JOIN LS_DATA_SRC s ON l.<프레임FK> = s.SRC_SN
//                       WHERE s.RAW_SN = a.RAW_DATA_ID)
//   ⚠ 실제 컬럼명은 구현 시 엔티티에서 확인할 것(LS_DATA_LBL 의 프레임 FK 명)
// workStatus 필터 매핑:
//   IN_PROGRESS → (BE 상태가 FE PENDING 으로 매핑되는 값) AND hasLabel
//   PENDING     → (동일 BE 상태) AND NOT hasLabel
//   그 외       → 기존 mapToFeStatus 의 역매핑 그대로
```

**주의 (Critical)**:
- **정렬**: 시간축 단일 기준만 allowlist 에 넣는다. 상태 우선순위 `ORDER BY CASE` 금지(CLAUDE.md 구속). 페이지 경계 안정성을 위해 tie-break 키를 마지막에 고정.
- **allowlist 구성 전에** FE 가 이 엔드포인트로 실제 전송하는 정렬 키를 전수 확인하고 기본값 `regDt,desc` 를 반드시 포함시킬 것(위험 #1).
- **LIKE 이스케이프**는 `TaskBoardQueryRepository.escapeLike` 와 동일 규약을 재사용한다. 새로 구현하지 말 것.
- **enrich 로직 보존**: 기존 batch-lookup(cctvName/dataStts/eventInfo)은 그대로 두고 WHERE 절만 쿼리 계층으로 올린다. N+1 을 새로 만들지 말 것.

**테스트 케이스** (RED 에 그대로 사용):
- `검색어로_필터하면_현재페이지가_아닌_전체에서_찾는다`
- `검색어_필터시_전체건수가_필터_적용_기준으로_반환된다`
- `상태_IN_PROGRESS_는_라벨이_저장된_배정만_반환한다`
- `상태_PENDING_은_라벨이_없는_배정만_반환한다`
- `IN_PROGRESS_로_조회한_행은_응답_status_도_IN_PROGRESS_다`
- `이벤트유형_필터가_적용된다`
- `WORKER_는_workerId_를_보내도_본인_배정만_조회된다`
- `WORKER_는_어떤_필터조합으로도_타인_배정을_볼_수_없다`
- `미등록_정렬키는_400`
- `기존_기본정렬_regDt_desc_는_200_이고_결과_순서가_변경_전과_같다`
- `LIKE_와일드카드_문자를_검색어로_넣어도_이스케이프된다`
- `정렬키_개수가_상한을_넘으면_400`

**수용 기준**:
- [ ] 파라미터 없는 호출의 응답이 변경 전과 동일(순서·페이징·필드)
- [ ] 필터 적용 시 `totalElements` 가 전체 기준으로 정확
- [ ] `workStatus=IN_PROGRESS` 결과의 모든 행이 응답에서도 `IN_PROGRESS`
- [ ] WORKER 의 `workerId` 파라미터가 무시됨(403 아님 — 기존 계약 유지)

**의존성**: 없음

---

### Phase 2: BE 이벤트유형 옵션 API (전체 기준)

**목표**: 작업목록의 이벤트유형 드롭다운이 현재 페이지가 아니라 **본인 배정 전체**에 존재하는 이벤트유형을 보여준다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `assignment/controller/AssignmentController.java` | 수정 | `GET /v1/assignments/event-types` 추가 |
| `assignment/service/AssignmentService.java` | 수정 | 옵션 조회 위임 |
| `assignment/repository/AssignmentQueryRepository.java` | 수정 | `findEventTypes(cond)` — Phase 1 의 `buildWhere` 공유 |

**핵심 로직 / 시그니처**:
```java
@GetMapping("/event-types")
@PreAuthorize("isAuthenticated()")
public ApiResponse<List<EventTypeOption>> eventTypes(
        @RequestParam(required = false) Long workerId,
        @RequestParam(required = false) @Size(max = 100) String q,
        @RequestParam(required = false) String workStatus,
        @AuthenticationPrincipal TokenClaims actor);
// 목록과 같은 buildWhere 를 쓰되 eventTypeCd 조건만 제외 (자기 축 제외 — 위험 #6)
// 인가는 목록과 동일 규칙(WORKER = self 강제)
```

**주의**: REVIEWER 쪽 `/v1/tasks/board/event-types` 의 응답 형태(코드/명칭 구조)를 먼저 확인해 **FE 가 두 경로를 같은 컴포넌트로 다룰 수 있게** 맞춘다. 다르게 만들면 FE 에 분기가 하나 더 생긴다.

**테스트 케이스**:
- `이벤트유형_옵션은_현재페이지가_아닌_전체_배정에서_수집된다`
- `이벤트유형_옵션은_검색어_상태_필터를_반영한다`
- `이벤트유형_옵션은_자기축_eventTypeCd_필터에는_영향받지_않는다`
- `WORKER_는_본인_배정의_이벤트유형만_본다`

**수용 기준**:
- [ ] 뒷페이지에만 존재하는 이벤트유형도 옵션에 나온다
- [ ] 옵션에서 고른 값으로 필터하면 0건이 아니다
- [ ] WORKER 가 타인 배정의 이벤트유형을 볼 수 없다

**의존성**: Phase 1 (buildWhere 공유)

---

### Phase 3: 하위호환 회귀 가드

**목표**: 이 엔드포인트의 필터·정렬 정책이 미래에 바뀌면 **테스트가 깨지도록** 고정한다. 현재는 `/v1/assignments` 가 회귀 가드에 전혀 포함돼 있지 않아 아무것도 깨지지 않는다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `test/.../ListApiBackwardCompatibilityIT.java` | 수정 | `assignments(...)` 헬퍼 + 케이스 추가 |

**테스트 케이스**:
- `assignments_파라미터_없는_호출은_변경_전과_동일한_기본정렬_페이징`
- `assignments_신규_필터_파라미터는_전부_optional`
- `assignments_미등록_정렬키는_400_이고_board_와_동일_정책`
- `assignments_는_reviews_의_lenient_폴백을_따르지_않는다` (정책 혼선 방지 — CLAUDE.md 구속)

**수용 기준**:
- [ ] 기존 호출 형태가 전부 200 이고 응답 구조 불변
- [ ] board(strict) / reviews(lenient) 정책 차이가 테스트로 문서화됨

**의존성**: Phase 1, 2

---

### Phase 4: FE 서버사이드 전환

**목표**: 화면이 필터를 서버로 보내고, 클라이언트 재필터를 하지 않으며, 전체건수·드롭다운 옵션이 서버 기준으로 표시된다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `features/task/types.ts` | 수정 | `TaskListParams` 에 `q`/`workStatus`/`eventTypeCd` 추가 |
| `features/task/api.ts` | 수정 | 이벤트유형 옵션 API 함수 추가 |
| `pages/TaskListPage.tsx` | 수정 | 파라미터 조립 · **클라이언트 필터 제거** · totalElements 서버값 · 옵션 API 연동 |
| `features/task/components/TaskFilters.tsx` | 수정 | WORKER 이벤트 옵션을 서버 옵션으로 교체(상태 옵션 목록은 유지) |

**핵심 변경 지점**:
```ts
// TaskListPage.tsx:121-141  taskParams useMemo → q/workStatus/eventTypeCd 포함해 서버 전송
// TaskListPage.tsx:316-335  visibleRows 의 WORKER 분기 클라이언트 필터 → 제거 (allRows 그대로)
// TaskListPage.tsx:346-352  totalElements 의 WORKER 분기(visibleRows.length) → 서버 값으로 통일
// TaskListPage.tsx:224-231  이벤트유형 옵션 수집(현재 페이지 tasks) → 옵션 API 결과로 교체
```

**주의**:
- 화면 진입 기본값은 **FE 가 명시 전송**한다(BE 기본값 불변 원칙). 필터가 비어 있으면 그 파라미터 자체를 보내지 않는다.
- 검색 입력은 debounce(300ms) 적용 — 매 타이핑마다 서버 조회 금지.
- REVIEWER 분기는 건드리지 않는다(out of scope).

**테스트 케이스**:
- `작업목록_검색어_입력시_서버_파라미터로_전송된다`
- `작업목록_클라이언트_재필터가_적용되지_않는다`
- `전체건수는_서버_totalElements_를_표시한다`
- `이벤트유형_옵션은_옵션_API_결과를_사용한다`
- `필터가_비어있으면_해당_파라미터를_전송하지_않는다`

**수용 기준**:
- [ ] 검색·필터 결과가 전체 데이터셋 기준
- [ ] "전체 N건" 과 페이지 수가 필터 결과와 일치
- [ ] REVIEWER 화면 동작 무변경

**의존성**: Phase 1, 2

---

## 실행 가이드

1. `/cc-tdd Phase 1: BE 검색 조건 계층 + 정렬 allowlist + 작업중 판정 일원화`
2. `/cc-tdd Phase 2: BE 이벤트유형 옵션 API`
3. `/cc-tdd Phase 3: 하위호환 회귀 가드`
4. `/cc-tdd Phase 4: FE 서버사이드 전환`

---

## 환경 메모 (이전 계획에서 승계 — 유효)

- 로컬 스택: `docker compose` — postgres(5432) / backend(8080) / ai-server(9300) / **mock-server(9400)** / frontend(5173). 로컬은 **두 compose 파일 동시 지정 필수**
- DB 실제 스키마 **`public`**(klid_at 아님), 계정 `klid_user` / `klid_system`
- 참조 데이터 `rawSn=26` — 프레임 16 / 라벨 131 / APPROVED / export SUCCEEDED
- 다음 Flyway 버전 **V145** 부터 (이번 계획은 스키마 변경 없음)
- ⚠ gradle 은 `cleanTest test` 필수(UP-TO-DATE 스킵이 거짓 PASS 를 만든다) · **테스트 동시 실행 금지**(test-results XML 충돌로 위양성) · macOS 에 `timeout` 없음
- ⚠ 전체 회귀는 15~20분급 — `CLAUDE.md` "## 빌드 예산"(1800s/slowBuild) 참조, 단일 셸 호출 금지(백그라운드+폴링 또는 세그먼트 분할)
