# 구현 계획: 라벨링 캔버스 UX 개선 (v1 대비 조작성/안내 마찰 해소)

> 생성: 2026-07-16
> 상태: ✅ 완료 (전체 Phase 완료 2026-07-16)

## Phase 진행 현황

- [x] Phase 1: 뷰 조작성 (A 단축키 툴팁 정정 · B 팬 드래그 · C 프레임 전환 뷰 유지) ✅ 완료 (2026-07-16, 1138/1138 · 적대적 WEAK→보강 · 코드리뷰 PASS. blur가드·팬rAF·ref 보강)
- [x] Phase 2: 안내·안전 (D 단축키 치트시트 · E 프레임 이동 미저장 가드) ✅ 완료 (2026-07-16, 1153/1153 · 적대적 WEAK→보강 · 코드리뷰 PASS. 모달중 단축키억제·치트시트 메모·WASD표기 보강)
- [x] Phase 3: 객체·피드백 (F 객체 표시/숨김·잠금 · G 로딩 인디케이터) ✅ 완료 (2026-07-16, 1177/1177 · 적대적 ROBUST · 코드리뷰 CONDITIONAL→보강. 잠금 선택가드·스피너 role=status)

## 요구사항 요약

- 목적: 1차(v1) 사용자 화면 안내서(docs/v1-wiki/21) 대조 검토 결과, 우리 라벨링 캔버스의 "불편함"은 기능 부재보다 **조작성(팬·단축키·저장)·안내(도움말·로딩)** 마찰에 집중됨. 해당 마찰을 해소한다.
- 범위:
  - 포함(FE-only): 캔버스 팬·뷰 유지·단축키 정합/치트시트·프레임 이동 저장 가드·객체 표시숨김/잠금·로딩 피드백
  - 제외(out of scope): H(재생 배속·그리드), I(orphan 컴포넌트 정리·마스크 브러시 배선) — Tier 3 후속 별건. 신규 BE/DB 변경 없음.
- 주요 액터: WORKER(라벨러)·REVIEWER(검수) — 라벨링 캔버스 사용자
- 요구 목록:
  - R1(A): 좌측 툴바(`DarkToolbar`) 툴팁/단축키 표기를 실제 키맵(`labelingKeymap.SHORTCUT_KEYMAP`)과 일치시킨다 — 오표기(선택=S→실제 Esc, SAM추적=T→실제 Shift+T, T=표시/숨김) 제거, 키맵 단일출처 파생 | 출처: 검토 확정(버그)
  - R2(B): 확대 상태에서 화면 팬(뷰 이동) 조작 추가 — 스페이스+드래그(주) + 중클릭 드래그(부)로 `setPan` 구동, 팬 경계 클램프 | 출처: 검토 확정
  - R3(C): 프레임 전환 시 줌/팬 유지(연속 확대검수). 동일 영상 내 프레임 이동엔 뷰 보존, 영상 변경/해상도 상이 시에만 fit 리셋 + 수동 Fit 컨트롤 | 출처: 검토 확정
  - R4(D): 단축키 치트시트/도움말 모달 — `?`(Shift+/) 또는 툴바 도움말 버튼, `SHORTCUT_KEYMAP` 단일출처 렌더 | 출처: 검토 확정
  - R5(E): 프레임 이동 시 미저장(dirty) 유실 방지 — 이동 전 확인 모달(저장 후 이동/저장 안 함/취소), 기존 닫기 가드와 정합 | 출처: 검토 확정
  - R6(F): 객체 목록(`ObjectClassTree`) 개별 표시/숨김 버튼 + 잠금(lock). 잠금 라벨은 캔버스 선택/편집/이동 차단 | 출처: 검토 확정
  - R7(G): 로딩/진행 피드백 — 캔버스 이미지 로드 스피너 + SAM2 분할 요청 진행 인디케이터 | 출처: 검토 확정
- 수용 기준:
  - AC1: 툴바 각 도구 툴팁의 단축키가 `SHORTCUT_KEYMAP`과 100% 일치(하드코딩 제거). 잘못된 키 표기 0건
  - AC2: zoom>1 상태에서 스페이스+드래그로 뷰가 이동하고, `panX/panY`가 클램프 범위 내로 갱신됨. zoom==1(fit)에선 팬 no-op
  - AC3: 동일 영상 내 다음/이전 프레임 이동 후에도 zoom/pan 유지. 영상 변경 또는 이미지 해상도 상이 시 fit 리셋. Fit 버튼/단축키로 수동 초기화 가능
  - AC4: `?`(또는 도움말 버튼)로 치트시트 모달이 열리고, 표시된 키/설명이 `SHORTCUT_KEYMAP`에서 파생됨. ESC/닫기로 닫힘, 포커스 트랩
  - AC5: dirtyLabels 비어있지 않을 때 프레임 이동 시 확인 모달 노출 → "저장 후 이동"은 저장 후 이동, "저장 안 함"은 dirty 버리고 이동, "취소"는 현재 프레임 유지. dirty 없으면 즉시 이동
  - AC6: 객체 목록 각 항목에 표시/숨김·잠금 토글. 숨김=캔버스 미표시, 잠금=선택/드래그/리사이즈/삭제 차단(툴팁 안내). 잠금/숨김 상태 아이콘 반영
  - AC7: 이미지 로드 중 캔버스에 스피너, SAM2 분할 요청 in-flight 동안 진행 인디케이터 표시(완료/실패 시 해제)
- 비기능 요구사항: 무회귀(bbox/폴리곤/SAM분할·추적/키포인트/track_id 머지/줌 fit/저장/복사붙여넣기). strict TS·any 금지·불변성(Set 새로 생성). 접근성(모달 포커스 트랩·aria, 버튼 aria-label/aria-pressed). 팬·뷰유지가 좌표계(원본 해상도 기준) 정합을 깨지 않을 것.

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | 없음 |
| 기존 API | 없음 (FE-only) |
| 도메인 모듈 | FE `features/label` (캔버스·툴바·객체패널·스토어·단축키·SAM2 훅) |
| 설정/환경 | 없음 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | 팬 도입이 기존 휠 줌-투-포인트(`CanvasShell.handleWheel`)와 좌표 변환 충돌 | MED | 팬은 `panX/panY`만 갱신, 좌표→이미지 역변환 로직 재사용. 팬 후에도 pointer→image 매핑 단위테스트로 검증 |
| 2 | 뷰 유지(R3)가 해상도 상이 프레임에서 좌표 어긋남/여백 유발 | MED | 다음 프레임 이미지 dims가 현재와 다르면 fit 리셋(안전). 동일 dims만 뷰 보존 |
| 3 | 프레임 이동 저장 가드(R5)가 재생(auto-play)·단축키 연속 이동과 충돌 | MED | dirty일 때만 개입. 재생 중엔 저장 가드 대신 재생 정지 or dirty 없을 때만 진행(설계에서 확정) |
| 4 | 잠금(R6) 상태가 캔버스 여러 상호작용 경로(선택/Transformer/드래그/삭제/단축키)에 일관 적용 안 됨 | MED | `lockedLabelIds` 가드를 선택·이동·리사이즈·삭제·단축키 삭제 진입점에 공통 헬퍼로 적용 + 테스트 |
| 5 | 뷰 유지·팬이 프레임 스트립/슬라이더와 상태 경합 | LOW | 뷰 상태는 store(zoom/pan) 단일출처. UI는 구독만 |

---

## 상세 계획

### Phase 1: 뷰 조작성 (A 단축키 툴팁 정정 · B 팬 드래그 · C 프레임 전환 뷰 유지)

**목표**: 확대 상태에서 뷰를 자유 이동(팬)하고, 프레임을 넘겨도 확대 뷰가 유지되며, 툴바 단축키 표기가 실제 동작과 일치한다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/features/label/components/DarkToolbar.tsx` | 수정 | (R1) 하드코딩 단축키 툴팁을 `SHORTCUT_KEYMAP`에서 도구별 조합 문자열 파생. 헤더 주석 정정 |
| `frontend/src/features/label/hooks/labelingKeymap.ts` | 수정(경미) | (R1) 도구 id→표시용 키 조합 문자열 헬퍼 `formatBindingKeys(id)` export(치트시트/툴팁 공용) |
| `frontend/src/features/label/canvas/CanvasShell.tsx` | 수정 | (R2) 스페이스+드래그 / 중클릭 드래그 팬 핸들러 추가 → `setPan` + 클램프. (R3) 프레임 전환 시 뷰 유지 로직 연동 |
| `frontend/src/stores/useLabelStore.ts` | 수정 | (R2) `clampPan(x,y,zoom,viewport,image)` 유틸 or `setPan` 클램프 옵션. (R3) `resetView` 유지 + `fitView()` 명시 액션(선택) |
| `frontend/src/pages/label/LabelingPage.tsx` | 수정 | (R3) 프레임 전환 시 무조건 `resetView()` 호출(203-206) 제거 → 동일 영상·동일 해상도면 뷰 보존, 아니면 fit. 수동 Fit 버튼/단축키 배선 |
| `frontend/src/features/label/components/__tests__/DarkToolbar.test.tsx` | 생성/수정 | (R1) 툴팁이 키맵과 일치 검증 |
| `frontend/src/features/label/canvas/__tests__/pan.test.ts` (또는 store 테스트) | 생성 | (R2) 팬 클램프·좌표 매핑 단위테스트 |

**핵심 로직 / 시그니처**:
```ts
// labelingKeymap.ts — 툴팁/치트시트 공용 표시 문자열 (예: 'Shift+T', 'Esc', 'B')
export function formatBindingKeys(id: string): string;

// useLabelStore.ts — 팬 클램프 (뷰포트 밖으로 이미지가 완전히 벗어나지 않게)
export function clampPan(panX: number, panY: number, zoom: number,
  view: { w: number; h: number }, image: { w: number; h: number }): { x: number; y: number };

// CanvasShell — 스페이스 누른 상태 드래그(또는 중클릭 드래그)로 팬
// space keydown → isPanning=true(커서 grab), pointermove Δ → setPan(clampPan(...))
```
- R3: `LabelingPage`에서 프레임 전환 effect가 현재/다음 이미지 dims를 비교 — 같으면 뷰 보존(resetView 미호출), 다르거나 영상 변경이면 `resetView()`. 초기 진입은 fit.

**테스트 케이스** (cc-tdd RED):
- `툴바_도구_툴팁의_단축키가_키맵과_일치한다` (선택=Esc, SAM추적=Shift+T, BBOX=B ...)
- `formatBindingKeys가_수식키조합을_사람표기로_반환한다`
- `zoom1_fit에서는_팬이_no_op이다`
- `zoom_확대상태에서_드래그시_panXY가_클램프범위내로_갱신된다`
- `clampPan은_이미지가_뷰포트밖으로_완전이탈하지_않게_제한한다`
- `동일영상_동일해상도_프레임이동시_zoom_pan이_유지된다`
- `해상도상이_또는_영상변경시_뷰가_fit으로_리셋된다`

**수용 기준**:
- [ ] AC1 툴팁 키맵 일치
- [ ] AC2 팬 드래그 + 클램프
- [ ] AC3 프레임 전환 뷰 유지 + 조건부 리셋

**의존성**: 없음

### Phase 2: 안내·안전 (D 단축키 치트시트 · E 프레임 이동 미저장 가드)

**목표**: `?`로 전체 단축키를 한눈에 보고, 미저장 상태로 프레임을 넘겨 작업을 잃지 않는다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/features/label/components/ShortcutCheatSheet.tsx` | 생성 | (R4) `SHORTCUT_KEYMAP`을 kind(도구/이동/액션)별 그룹핑해 렌더하는 모달. 공통 `Modal` 재사용(포커스트랩·ESC 내장) |
| `frontend/src/features/label/hooks/useLabelingShortcuts.ts` | 수정 | (R4) `?`(shift+/) → 치트시트 토글 바인딩 추가(입력 포커스 시 무시 규칙 유지) |
| `frontend/src/features/label/components/DarkToolbar.tsx` 또는 `LabelHeader.tsx` | 수정 | (R4) 도움말(?) 버튼 추가 → 치트시트 오픈 |
| `frontend/src/pages/label/LabelingPage.tsx` | 수정 | (R5) `jumpTo(idx)` 래핑 — dirty면 확인 모달, 아니면 즉시 이동. 치트시트 open state |
| `frontend/src/features/label/components/FrameNavGuardModal.tsx` | 생성 | (R5) 저장 후 이동 / 저장 안 함 / 취소 3옵션 모달(닫기 가드와 동일 톤) |
| `frontend/src/features/label/components/__tests__/ShortcutCheatSheet.test.tsx` | 생성 | (R4) 키맵 파생 렌더·ESC 닫힘 |
| `frontend/src/pages/label/__tests__/LabelingPage.frameGuard.test.tsx` | 생성 | (R5) dirty 시 가드·3분기 동작 |

**핵심 로직 / 시그니처**:
```tsx
// ShortcutCheatSheet — kind별 섹션(도구/프레임/액션), 각 행: formatBindingKeys(id) + label
interface ShortcutCheatSheetProps { open: boolean; onClose: () => void }

// LabelingPage — 프레임 이동 가드
// requestJump(idx): dirtyLabels.size>0 ? openGuard({idx}) : doJump(idx)
// onGuardSave → save() 성공 후 doJump / onGuardDiscard → clearDirty()+doJump / onGuardCancel → close
```
- 재생(auto-play) 중엔 가드 대신: dirty면 재생 정지 + 가드, 아니면 진행(위험 #3 대응 — 설계에서 확정).

**테스트 케이스** (cc-tdd RED):
- `물음표_단축키로_치트시트가_열린다`
- `치트시트는_SHORTCUT_KEYMAP의_모든_바인딩을_그룹별로_표시한다`
- `치트시트_ESC로_닫힌다`
- `dirty상태_프레임이동시_확인모달이_노출된다`
- `저장후이동_선택시_save후_해당프레임으로_이동한다`
- `저장안함_선택시_dirty를_버리고_이동한다`
- `취소_선택시_현재프레임을_유지한다`
- `dirty없으면_즉시_이동한다`

**수용 기준**:
- [ ] AC4 치트시트 모달(키맵 파생·ESC·포커스트랩)
- [ ] AC5 프레임 이동 미저장 가드 3분기

**의존성**: 없음 (Phase 1과 독립 — 순차 진행)

### Phase 3: 객체·피드백 (F 객체 표시/숨김·잠금 · G 로딩 인디케이터)

**목표**: 객체 목록에서 개별 표시/숨김·잠금을 제어하고, 이미지·AI 요청 진행이 시각적으로 드러난다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/stores/useLabelStore.ts` | 수정 | (R6) `lockedLabelIds: Set<string>` + `toggleLabelLock(id)` 추가(불변 Set). setLabels/reset 시 초기화. 편집 액션(updateLabel/removeLabel)에서 잠금 라벨 no-op 가드 |
| `frontend/src/features/label/components/ObjectClassTree.tsx` | 수정 | (R6) 각 객체 행에 표시/숨김(eye) + 잠금(lock) 토글 버튼(aria-pressed). 숨김/잠금 아이콘 상태 반영 |
| `frontend/src/features/label/canvas/layers/LabelsLayer.tsx` | 수정 | (R6) 잠금 라벨: 선택 불가·Transformer 비활성·드래그 불가(listening/draggable=false) |
| `frontend/src/features/label/canvas/layers/OverlayLayer.tsx` | 수정 | (R6) 잠금 라벨 대상 편집 진입 차단 |
| `frontend/src/features/label/hooks/useLabelingShortcuts.ts` | 수정 | (R6) 삭제 단축키가 잠금 라벨엔 no-op |
| `frontend/src/features/label/canvas/CanvasShell.tsx` | 수정 | (R7) 이미지 로드 중 스피너 오버레이(konva image status) |
| `frontend/src/features/label/canvas/layers/OverlayLayer.tsx` | 수정 | (R7) SAM2 분할 요청 in-flight 스피너/진행 인디케이터(`useSam2Segment` isPending) |
| `frontend/src/features/label/components/__tests__/ObjectClassTree.test.tsx` | 수정/생성 | (R6) 표시숨김·잠금 토글·아이콘 상태 |
| `frontend/src/stores/__tests__/useLabelStore.lock.test.ts` | 생성 | (R6) 잠금 시 편집/삭제 no-op·불변성 |

**핵심 로직 / 시그니처**:
```ts
// useLabelStore
lockedLabelIds: Set<string>;
toggleLabelLock: (id: string) => void; // 새 Set 생성(불변)
// updateLabel/removeLabel 진입부: if (get().lockedLabelIds.has(id)) return; // 잠금 no-op

// ObjectClassTree 행 액션: [eye toggle → toggleLabelVisibility(id)] [lock toggle → toggleLabelLock(id)] [연필][삭제]
```
- R7: `CanvasShell`에서 이미지 로드 상태(`status !== 'loaded'`)면 중앙 스피너. `OverlayLayer`/부모에서 `useSam2Segment().isPending`이면 캔버스 좌하단/중앙 진행 인디케이터.

**테스트 케이스** (cc-tdd RED):
- `객체행_표시숨김_토글시_hiddenLabelIds가_갱신된다`
- `객체행_잠금_토글시_lockedLabelIds가_갱신된다`
- `잠금_라벨은_updateLabel_removeLabel이_no_op이다`
- `잠금_토글은_기존Set을_변형하지_않고_새Set을_만든다`
- `삭제_단축키는_잠금_라벨에_적용되지_않는다`
- `이미지_로드중이면_캔버스에_스피너가_표시된다`
- `SAM2분할_요청중이면_진행_인디케이터가_표시된다`

**수용 기준**:
- [ ] AC6 객체 표시숨김·잠금(편집/삭제 차단)
- [ ] AC7 이미지·SAM2 로딩 인디케이터

**의존성**: 없음 (Phase 1·2와 독립 — 순차 진행)

## 실행 가이드

1. `/cc-tdd Phase 1: 뷰 조작성 (단축키 툴팁 키맵 정합 + 스페이스드래그 팬 + 프레임 전환 뷰 유지)` → `/cc-tdd Phase 2: 안내·안전 (단축키 치트시트 모달 + 프레임 이동 미저장 가드)` → `/cc-tdd Phase 3: 객체 표시숨김·잠금 + 이미지/SAM2 로딩 인디케이터`
