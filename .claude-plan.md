# 구현 계획: 1차 위키 대조 반영 (사용자 체크 6항목)

> 생성: 2026-07-16
> 상태: ✅ 완료 (전체 4 Phase / R1~R6 완료 2026-07-16)

## Phase 진행 현황

- [x] Phase 1: FE 저비용 (R1 검수 프레임 상태색 · R6 SAM 다중클릭 정제) ✅ 완료 (2026-07-16, 1188/1188 · 적대적 BROKEN→DEV_FIX 2회→해소. R1: 반려는 영상단위라 프레임색 제외, 미해소이슈 빨강+저장 연두+현재. 라벨링 반려 유령도 앱전역 제거)
- [x] Phase 2: 작업목록 상태 우선순위 정렬 (R2, BE) ✅ 완료 (2026-07-16, R2 6/6 · 회귀 268/268 · 적대적 ROBUST · DB/코드리뷰 CONDITIONAL(HIGH 0). ★블로커: 사용자 varchar V107 뷰버그(ATTR→ATRB 미정합) 명시허가 하에 수정—R2검증+varchar 기동불가 동시 해소. 후속: TASK_ASSIGNMENT 복합인덱스·CASE↔mapBoardStatus 통합)
- [x] Phase 3: 트랙 관리 확장 (R4 트랙 삭제 · R5 트랙 split, BE+FE) ✅ 완료 (2026-07-16, BE Track/Merge/Interp/Deident GREEN·FE 1195/1195. 적대적 BROKEN→DEV_FIX→ROBUST. FK정리(ATTR_VAL→AI_INFO→AUG_MAP→LBL bulk)·재보간 touched 통지 union·로그살균·확인다이얼로그·실FK 통합테스트. 후속: merge 통지 갭·DatasetExportServiceIT 사전실패)
- [x] Phase 4: YOLO 오토라벨 클래스 필터 (R3, ai-server+BE+FE) ✅ 완료 (2026-07-16, ai-server 91/91·BE green·FE 1204/1204. 적대적 ROBUST·계약 PASS·코드리뷰 PASS. COCO 영문 고정클래스·빈=None=전체 3레이어 일관·하위호환. 후속(LOW): ai-server 원소 검증 defense-in-depth)

## 요구사항 요약

- 목적: 1차(v1) 사용자 화면 안내서 대조 검토에서 사용자가 체크한 반영 항목 구현. 라벨링/검수/작업목록 편의·정확도 향상.
- 범위:
  - 포함: R1 검수 프레임 상태색 · R2 작업목록 정렬 · R3 YOLO 클래스 필터 · R4 트랙 삭제(프레임 이후) · R5 트랙 split · R6 SAM 다중클릭 정제
  - 제외(out of scope): **R7 폴리곤/폴리라인 트랙 보간 — 탐색 결과 폴리곤 보간은 이미 구현됨(`batch/interpolation/TrackInterpolator.interpolatePolyshape`), 사용자 결정으로 제외**. 90°회전·영역확대·그리드·메타 다음프레임(FE 편의 미선택), 반려사유 라디오·라벨러 자가배정(정책). SKELETON/SEGMENT 보간·POLYLINE 코드값.
  - 주요 액터: REVIEWER(검수·작업목록·정렬), WORKER(라벨링 캔버스 SAM/YOLO/트랙)
- 요구 목록:
  - R1: 검수 화면 프레임 타임라인(`features/review/components/FrameTimeline.tsx`)에 프레임별 상태색(반려=주황/미해소이슈=빨강/현재=강조, 선택적 저장=연두). **BE 변경 불필요** — `useIssueThreads(videoId)`의 srcSn+issueTypeCd로 FE 산출, `features/label/components/frameStatus.ts`(resolveFrameStatus/FRAME_STATUS_BORDER) 재사용 | 출처: 사용자 체크
  - R2: 작업목록(`pages/TaskListPage.tsx`, REVIEWER board + WORKER 경로) 상태 우선순위 정렬. **가용 상태 매핑**(반려 REJECTED > 검수대기 IN_REVIEW/REVIEW_PENDING > 배정/진행 ASSIGNED > 미배정 PENDING > 완료 COMPLETED/APPROVED) → 날짜(regDt DESC) → ID(rawSn). BE `VideoRepository` 신규 @Query(LS_RAW_DATA_STATUS JOIN + ORDER BY CASE), 응답 스키마 무변경 | 출처: 사용자 체크
  - R3: 라벨링 툴바 YOLO 오토라벨에 **객체종류(클래스) 선택** 추가. 버튼은 이미 있음(`DarkToolbar.tsx:118`). 4레이어 확장: ai-server `YoloRequest.classes` 필터 + BE autolabel 프록시 class 전달 + FE `requestAutolabel(srcSn, classIds?)` + 클래스 선택 팝업(라벨 마스터 재사용) | 출처: 사용자 체크
  - R4: **트랙 삭제(해당 프레임부터 뒤 전체)**. 신규 — BE `DELETE` 엔드포인트(LS_DATA_SRC JOIN frameNo≥X + TRCK_ID, 자식 LS_DATA_LBL_AI_INFO→부모 LS_DATA_LBL 순서 삭제, IDOR guard, WorkLock 배타락) + FE `deleteTrack` API + `ObjectClassTree` 트랙 액션 + `LabelingPage` 배선 | 출처: 사용자 체크
  - R5: **트랙 split(분할)**. 신규 — BE `splitTrack(rawSn, trackId, atFrameNo)`: atFrameNo 이후 라벨을 새 미사용 trackId로 재지정 + 엔드포인트(mergeTracks 역연산 패턴, WorkLock/IDOR) + FE `splitTrack` API + UI 진입점 | 출처: 사용자 체크
  - R6: **SAM 다중 positive-click 정제**. FE-only(타입 `Sam2SegmentRequest.points:number[][]` + ai-server `sam2.py` N-point 이미 수용). `OverlayLayer` 포인트 누적 state + 확정 트리거(Enter/더블클릭/버튼) 2단계 + 도구 이탈 시 초기화. AI-server 무변경 | 출처: 사용자 체크
- 수용 기준:
  - AC1: 검수 프레임 썸네일이 반려/미해소이슈/현재를 색으로 구분, 이슈 프레임 식별·점프 가능. 라벨링 화면 무회귀
  - AC2: 작업목록이 반려>검수대기>배정>미배정>완료 순 + 날짜·ID tiebreak로 정렬(BE ORDER BY). 페이지 경계에서도 일관
  - AC3: YOLO 실행 시 선택 클래스만 검출·저장. 클래스 미선택 시 전체(기존 동작). ai-server classes 필터 정확
  - AC4: 트랙 삭제 시 지정 프레임 이후 해당 trackId 라벨만 삭제, 이전 프레임·타 트랙 불변. FK 고아 없음. 권한/락 적용
  - AC5: 트랙 split 시 atFrameNo 기준 이후가 새 trackId로 분리, 이전은 원 trackId 유지. 좌표 불변
  - AC6: SAM 분할에서 다중 클릭 누적 후 확정 시 누적 포인트로 1회 분할. 도구 이탈/프레임전환 시 누적 초기화. 기존 단일클릭·박스·mock/저신뢰 차단 무회귀
- 비기능 요구사항: strict TS·any금지·불변성·접근성(FE). BE: `ApiResponse<T>` 표준·@Valid·`@PreAuthorize` 권한(REVIEWER/WORKER)·포털 차단(ADR-013)·N+1 방지·PreparedStatement 바인딩·마이그레이션 없음(코드값 미도입). ai-server Resilience4j 경유. 무회귀(트랙머지·SAM분할/추적·보간·검수·작업배정).

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | 없음 (R4/R5는 기존 LS_DATA_LBL.TRCK_ID UPDATE/DELETE, 신규 컬럼 없음) |
| 기존 API | R3 autolabel 요청에 선택적 classes 추가(하위호환), R2 board 정렬 순서 변경(스키마 불변). 신규: R4 트랙삭제·R5 split 엔드포인트 |
| 도메인 모듈 | BE label(track)·assignment/video(board)·ai-server yolo. FE features/{review,task,label} |
| 설정/환경 | 없음 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | R4/R5 트랙 삭제·분할이 보간 산출물(INTERPOLATED)·머지와 상호작용해 정합 깨짐 | HIGH | 삭제/분할 후 필요 시 `TrackInterpolationStep.interpolateSingleTrack` 재보간(머지 패턴). WorkLock 배타락 + 트랜잭션. 겹침/경계 테스트 |
| 2 | R4 frameNo가 LS_DATA_LBL에 없음(LS_DATA_SRC에만) | MED | LS_DATA_SRC JOIN으로 frameNo≥X 필터(TrackInterpolationStep srcSn↔frame 매핑 참조). 파라미터 바인딩 |
| 3 | R2 상태 우선순위 CASE가 board(LsDataRaw)와 status(LS_RAW_DATA_STATUS) 두 테이블 상태 혼선 | MED | 행 표시 상태의 원천인 LS_RAW_DATA_STATUS.DATA_STTS_CD 기준 JOIN + ORDER BY CASE. mapBoardStatus와 정합 |
| 4 | R3 클래스 필터가 ai-server 클래스명↔라벨 마스터 매핑 불일치 | MED | ai-server YOLO 클래스명과 FE 라벨 마스터 매핑 확인. 미선택=전체(기존) fallback |
| 5 | R6 다중클릭 확정 UX가 기존 단일클릭 즉시적용 흐름과 충돌 | MED | SAM_SEGMENT 도구에서만 누적 모드, 확정 트리거 명확화. 기존 박스/mock/저신뢰 경로 무회귀 테스트 |
| 6 | R5 split 후 새 trackId 생성 규칙(미사용 번호) 충돌 | MED | 영상 내 max trackId+1 또는 UUID 규칙. 유니크 보장 |

---

## 상세 계획

### Phase 1: FE 저비용 (R1 검수 프레임 상태색 · R6 SAM 다중클릭)

**목표**: 검수자가 이슈/반려 프레임을 썸네일 색으로 식별하고, SAM 분할을 다중 클릭으로 정제할 수 있다. 둘 다 FE-only, BE/ai-server 무변경.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/features/review/components/FrameTimeline.tsx` | 수정 | (R1) props에 `rejectionSrcSns?/inquirySrcSns?/savedSrcSns?: Set<number>` 추가, 삼항 2색 → `resolveFrameStatus`+`FRAME_STATUS_BORDER` |
| `frontend/src/pages/ReviewPage.tsx` | 수정 | (R1) `useIssueThreads(review.videoId)` → srcSn 집합 3종 useMemo(LabelingPage:333-361 패턴) → FrameTimeline 주입 |
| `frontend/src/features/label/canvas/layers/OverlayLayer.tsx` | 수정 | (R6) SAM_SEGMENT 포인트 누적 state(`segPoints`), 클릭 시 누적, 확정 트리거(Enter/더블클릭/확정버튼)로 `segment({points:누적})` 1회, 도구이탈/프레임전환 초기화. 박스·mock·저신뢰 경로 유지 |
| `frontend/src/features/review/components/__tests__/FrameTimeline.test.tsx` | 생성/수정 | (R1) 상태색 반영 |
| `frontend/src/features/label/canvas/layers/__tests__/OverlayLayerSamMultiClick.test.tsx` | 생성 | (R6) 누적·확정·초기화 |

**핵심 로직 / 시그니처**:
```tsx
// FrameTimeline props 확장
interface FrameTimelineProps { frames; currentFrameIdx; onSelect;
  rejectionSrcSns?: Set<number>; inquirySrcSns?: Set<number>; savedSrcSns?: Set<number>; }
// 각 프레임: resolveFrameStatus({isCurrent, hasRejection, hasInquiry, hasLabel}) → FRAME_STATUS_BORDER[status]
// ReviewPage: rejection=REJECTION&srcSn!=null, inquiry=INQUIRY&stts!=RESOLVED&srcSn!=null, saved=f.labels.length>0
// OverlayLayer(R6): segPoints:number[][] state; handleSegmentClick→append; confirm→segment({points}); tool변경 effect→setSegPoints([])
```

**테스트 케이스** (RED):
- `검수_프레임에_반려_srcSn이면_주황_테두리`
- `검수_프레임에_미해소_이슈면_빨강_테두리`
- `현재_프레임은_강조_테두리`
- `이슈없는_프레임은_기본_테두리` (무회귀)
- `SAM_다중클릭시_포인트가_누적된다`
- `SAM_확정시_누적포인트_전체로_segment_1회_호출`
- `SAM_도구이탈시_누적포인트_초기화`
- `SAM_박스드래그·단일클릭_기존동작_무회귀`

**수용 기준**:
- [ ] AC1 검수 상태색
- [ ] AC6 SAM 다중클릭 누적·확정·초기화

**의존성**: 없음

### Phase 2: 작업목록 상태 우선순위 정렬 (R2, BE)

**목표**: 작업목록이 반려>검수대기>배정>미배정>완료 순 + 날짜·ID로 정렬돼 재작업 건이 상단 노출된다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/src/main/java/kr/co/cudo/authoring/video/repository/VideoRepository.java` | 수정 | 신규 @Query — LS_RAW_DATA_STATUS JOIN + `ORDER BY CASE s.dataSttsCd ... END, r.regDt DESC, r.rawSn`(Pageable) |
| `backend/src/main/java/kr/co/cudo/authoring/assignment/service/TaskBoardService.java` | 수정 | `listBoard`에서 기존 `findAllByDataSttsCdOrderByRegDtDesc` → 신규 정렬 쿼리 교체 |
| `backend/src/main/java/kr/co/cudo/authoring/assignment/service/AssignmentService.java` | 수정(선택) | WORKER 경로도 동일 우선순위 정렬 정합(가능 범위) |
| `backend/src/test/.../TaskBoardServiceTest 또는 Repository 테스트` | 생성/수정 | 정렬 순서 검증 |

**핵심 로직 / 시그니처**:
```java
// VideoRepository — 상태 우선순위 정렬
@Query("SELECT r FROM LsDataRaw r JOIN LsRawDataStatus s ON s.rawSn = r.rawSn " +
       "WHERE s.dataSttsCd IN :statuses " +
       "ORDER BY CASE s.dataSttsCd WHEN 'REJECTED' THEN 0 WHEN 'IN_REVIEW' THEN 1 " +
       "WHEN 'ASSIGNED' THEN 2 WHEN 'PENDING' THEN 3 WHEN 'COMPLETED' THEN 4 WHEN 'APPROVED' THEN 5 ELSE 6 END, " +
       "r.regDt DESC, r.rawSn")
Page<LsDataRaw> findBoardOrderedByStatus(@Param("statuses") List<String> statuses, Pageable pageable);
// 실제 상태코드/조인 컬럼은 LsRawDataStatus(STTS_*)·mapBoardStatus 확인 후 확정
```

**테스트 케이스** (RED):
- `작업목록_반려가_최상단_정렬`
- `동일상태내_최신_regDt_우선`
- `동일상태_동일날짜_rawSn_tiebreak`
- `기존_board_필터·페이징_무회귀`

**수용 기준**:
- [ ] AC2 상태 우선순위 + 날짜·ID tiebreak, 페이지 경계 일관

**의존성**: 없음

### Phase 3: 트랙 관리 확장 (R4 트랙 삭제 · R5 split, BE+FE)

**목표**: 트랙을 특정 프레임 이후로 일괄 삭제하고, 한 트랙을 특정 프레임 기준 두 트랙으로 분할할 수 있다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/.../label/controller/TrackMergeController.java` (또는 신규 TrackController) | 수정/생성 | (R4) `DELETE /v1/videos/{rawSn}/tracks/{trackId}?fromFrameNo=N` (R5) `POST /v1/videos/{rawSn}/tracks/{trackId}/split` body `{atFrameNo}` |
| `backend/.../label/service/TrackMergeService.java` 또는 신규 TrackEditService | 수정/생성 | (R4) `deleteTrackFrom(rawSn, trackId, fromFrameNo, actor)` (R5) `splitTrack(rawSn, trackId, atFrameNo, actor)`. WorkLock 배타락 + IDOR(`LabelAccessGuard`) + 자식→부모 삭제/재지정 + 필요시 재보간 |
| `backend/.../batch/repository/LsDataLblRepository.java` | 수정 | 트랙+frameNo 범위 조회/삭제 쿼리(LS_DATA_SRC JOIN) |
| `backend/.../label/dto/*` | 생성 | TrackSplitRequest/Response, TrackDeleteResponse |
| `frontend/src/features/label/api.ts` | 수정 | `deleteTrack(rawSn, trackId, fromFrameNo)`, `splitTrack(rawSn, trackId, atFrameNo)` |
| `frontend/src/features/label/components/ObjectClassTree.tsx` | 수정 | 트랙 단위 액션(삭제/분할) 진입점(연필 옆) |
| `frontend/src/pages/label/LabelingPage.tsx` | 수정 | 핸들러 배선 + 성공 시 재조회 무효화 |
| BE/FE 테스트 | 생성 | 삭제 범위·split 경계·권한·무회귀 |

**핵심 로직 / 시그니처**:
```java
// R4: fromFrameNo 이후(포함) trackId 라벨 삭제 — LS_DATA_SRC JOIN frameNo>=N, 자식(AI_INFO)→부모(LBL) 순서, 겹침 없음
int deleteTrackFrom(Long rawSn, String trackId, int fromFrameNo, TokenClaims actor);
// R5: atFrameNo 이후(포함) 라벨을 새 미사용 trackId로 reassign — 이전은 원 trackId 유지
TrackSplitResponse splitTrack(Long rawSn, String trackId, int atFrameNo, TokenClaims actor);
// 새 trackId = 영상 내 max(trackId)+1 (유니크 보장)
```

**테스트 케이스** (RED):
- `트랙삭제_지정프레임이후_해당트랙만_삭제`
- `트랙삭제_이전프레임·타트랙_불변`
- `트랙삭제_자식AI_INFO_고아_없음`
- `트랙삭제_권한없으면_403·타영상_404`
- `트랙split_atFrame이후_새trackId·이전_원trackId`
- `트랙split_좌표_불변`
- `트랙split_새trackId_유니크`
- `기존_트랙머지_무회귀`

**수용 기준**:
- [ ] AC4 트랙 삭제(범위·FK·권한/락)
- [ ] AC5 트랙 split(경계·좌표불변·유니크)

**의존성**: 없음 (트랙머지 인프라 재사용)

### Phase 4: YOLO 오토라벨 클래스 필터 (R3, ai-server+BE+FE)

**목표**: YOLO 오토라벨 실행 시 원하는 객체종류만 선택해 검출·저장한다. 미선택 시 전체(기존).

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `ai-server/app/schemas.py` | 수정 | `YoloRequest`에 `classes: list[str] | None = None` 추가 |
| `ai-server/app/routers/yolo.py` | 수정 | `_predict_yolox`에서 classes 지정 시 해당 라벨만 필터 |
| `backend/.../batch/...AiServerClient 또는 autolabel proxy` | 수정 | autolabel 요청에 classes 전달(선택) |
| `backend/.../label/controller/LabelController.java` (autolabel 엔드포인트) | 수정 | 요청 DTO에 선택적 classes(@Valid), 하위호환 |
| `frontend/src/features/label/api.ts` | 수정 | `requestAutolabel(srcSn, classIds?)` 시그니처 확장 + body |
| `frontend/src/features/label/hooks/useAutolabel.ts` | 수정 | `autolabel(classIds?)` |
| `frontend/src/features/label/components/DarkToolbar.tsx` 또는 신규 팝업 | 수정/생성 | YOLO 클릭 시 클래스 선택 팝업(라벨 마스터 `useLabelMasters` 재사용) |
| `frontend/src/pages/label/LabelingPage.tsx` | 수정 | 팝업 상태·핸들러 배선 |
| BE/FE/ai-server 테스트 | 생성 | 클래스 필터·미선택 전체·하위호환 |

**핵심 로직 / 시그니처**:
```python
# schemas.py
class YoloRequest(BaseModel):
    image_b64: str; conf_threshold: float = 0.4; imgsz: int = 1280; iou: float = 0.5
    classes: list[str] | None = None  # None=전체
# yolo.py _predict_yolox: if req.classes: detections = [d for d in detections if d.label in set(req.classes)]
```
```ts
// api.ts
export function requestAutolabel(srcSn: number, classIds?: string[]): Promise<AutolabelResponse>;
```

**테스트 케이스** (RED):
- `YOLO_클래스선택시_해당클래스만_저장` (ai-server + BE)
- `YOLO_클래스_미선택시_전체_검출` (기존 동작 무회귀)
- `autolabel_요청_classes_하위호환` (classes 없어도 200)
- `FE_클래스팝업_선택_전달`

**수용 기준**:
- [ ] AC3 클래스 필터 검출·저장, 미선택 전체, ai-server 정확

**의존성**: 없음

## 실행 가이드

1. `/cc-tdd Phase 1: 검수 프레임 상태색 + SAM 다중클릭 정제 (FE-only)` → `/cc-tdd Phase 2: 작업목록 상태 우선순위 정렬 (BE)` → `/cc-tdd Phase 3: 트랙 삭제 + split (BE+FE)` → `/cc-tdd Phase 4: YOLO 오토라벨 클래스 필터 (ai-server+BE+FE)`
