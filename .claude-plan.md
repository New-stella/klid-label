# 구현 계획: 용어 표준 3계층 정합 (도메인·단어·용어)

> 생성: 2026-07-10 | 상태: 진행 중 | 브랜치: feat/krds-design-system
> 🎯 목표: `docs/표준용어/` 전체 사전(공공+산업) 기준 전수 감사 결과, 비정합 컬럼·도메인을 표준에 정합. 완료 시 LS_* 스키마가 사업/공공 표준용어사전과 이름·약어·타입·길이까지 일치(E 제외분·false-friend 제외).
> 진실원: `reports/glossary-db-audit-detail-2026-07-10.md §5`

## Phase 진행 현황

- [x] Phase 1: 용어사전 POLL 등록 (LogiCraft, 비파괴) — 완료: 단어 폴링/POLL(id624) + 용어 POLL_STTS_CD(1848)·POLL_LAST_DT(1849)·POLL_ATMPT_CNT(1850)
- [x] Phase 2: 컬럼 rename — 이름 정합 (Flyway V83 + 엔티티) — 완료: V83 ALTER RENAME ×5 + LsDeidentProcLog/LsDataLblHstry @Column. QA 3게이트 PASS(build+test GREEN, ddl-validate OK, MNG_ 무변경, View/native 참조 0)
- [x] Phase 3: 안전 타입 정합 — FRM_NO BIGINT + truncation 길이확대 (Flyway V84 + 엔티티) — 완료: V84 + 엔티티11 + FRM_NO Integer→Long(9소비처 toIntExact) + REQ/RES_PAYLOAD→TEXT + 뷰3종 재생성. QA 3게이트 PASS(1733 GREEN, ddl-validate OK, 뷰계약 동일). 후속 LOW: LsDataSrcRepository.findByRawSnAndFrameNo Integer→Long 정정(비블로킹)
- [x] Phase 4: 여부 도메인 CHAR(1) 전환 — YN 13건(LS_* 실측: VARCHAR1 11 + BOOLEAN 2; JOB_DMND_YN·CLCT_YN은 MNG_ 제외) (Flyway V85 + YesNoConverter) — 완료: @JdbcTypeCode(CHAR)+length=1, DE_IDENT_YN 3값 보존, CHECK 문자식 재작성, V_COMPLETED_VIDEO 재생성. QA 3게이트 PASS(1743 GREEN, ddl-validate OK) + DEV_FIX 1회(length=1)
- [ ] Phase 5: 테이블 삭제 — LS_DATA_SET drop + LS_TUS_UPLOAD 기능제거 (개별 승인)
- [ ] Phase 6: 문서 정합 — D8/D9 설계서 반영 (폐쇄망 Word/HWP 수동 가이드)

## 요구사항 요약

- 목적: 전수 감사(전체 사전 대조)에서 확정된 비정합분을 표준에 정합. 표준 등록분=완전정합, 미등록=등록 원칙(사용자 확정).
- 범위(포함): 저작도구 소유 LS_* 테이블의 컬럼명·타입·길이 + POLL 용어 등록.
- 범위(제외): **E**(용어사전 도메인 수정 — LBL_ID/EVNT_ID→BIGINT·ROLE_CD→VARCHAR·TEXT 5건·여부 term 재지정), **F**(길이 초과 축소 9건), **G**(false-friend 무변경: VER @Version·PBLCN_STTS_CD enum·MngClip 관제 MNG_ 공유). 관제 공유 MNG_* 미변경.
- 주요 액터: 시스템(스키마) — 런타임 동작 변화 없음, 명명·타입 표준화만.
- 요구 목록:
  - R1[B]: POLL(폴링)을 LogiCraft 사업 단어+용어로 등록 | 출처: 감사(미등록 확인)
  - R2[C]: 컬럼 5건 rename — REGISTERED_AT→REG_AT, REQ_KIND_CD→REQ_KND_CD, POLL_ATTEMPT_CNT→POLL_ATMPT_CNT, KPST_PRJ_ID→DE_IDNTF_PJT_ID, KPST_DATASET_ID→DE_IDNTF_DATST_ID | 출처: 감사+사용자(KPST→DE_IDNTF)
  - R3[D2]: LS_DATA_SRC.FRM_NO INTEGER→BIGINT (일련번호B20) | 출처: 도메인 감사
  - R4[D3]: truncation 위험 11건 길이 확대(실제<표준) | 출처: 도메인 감사
  - R5[D1]: 여부 공공 C1 채택 → YN 14건 CHAR(1) 전환(VARCHAR1 12 + BOOLEAN 2) | 출처: 사용자 결정(공공 여부C1). **검증: gov `~여부` 1424건 전부 여부C1(CHAR1), 예외 0**
  - R6[A]: LS_DATA_SET(orphan) drop + LS_TUS_UPLOAD(폐지) 기능 제거 | 출처: 감사(삭제 후보)
- 수용 기준:
  - [ ] 전체 빌드·테스트 GREEN (backend gradle)
  - [ ] `ddl-auto=validate` 로 애플리케이션 부팅 성공 (엔티티↔DB 스키마 일치)
  - [ ] 각 rename/타입변경 컬럼이 데이터 무손실로 전이 (RENAME/ALTER TYPE, DROP 아님)
  - [ ] LS_DEIDENT_PROC_LOG·LS_DATA_LBL_HSTRY 참조 쿼리·View 정합
  - [ ] YN CHAR(1) 전환 후에도 'Y'/'N' 값 read/write 및 직렬화(JSON) 정상
  - [ ] 데이터마트 View(V_COMPLETED_*) 재생성 후 정합 (해당 컬럼 노출 시)
- 비기능: PostgreSQL, Flyway V83부터 신규 마이그레이션만 추가(기존 파일 수정 금지), 단일 인스턴스, 롤백 가능하도록 각 마이그레이션 원자적.

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | LS_DEIDENT_PROC_LOG(rename 4·POLL), LS_DATA_LBL_HSTRY(REGISTERED_AT), LS_DATA_SRC(FRM_NO), YN 14 컬럼(다수 테이블), truncation 11 컬럼, LS_DATA_SET/LS_TUS_UPLOAD DROP |
| 기존 API | 컬럼명은 내부 — API DTO 필드/JSON 키는 유지(엔티티 @Column name만 변경, 필드명 불변 원칙). TUS 삭제 시 관련 엔드포인트 제거 |
| 도메인 모듈 | batch(LsDeidentProcLog·LsDataLbl), video(LsDataSrc), label(LsLabelPresetCode), deadline, assignment, augment, notice, upload(TUS 제거), 기타 YN 보유 엔티티 |
| 설정/환경 | 없음 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | BOOLEAN→CHAR(1) 전환 시 엔티티 필드타입·직렬화·쿼리 파급 (BBOX/POLYGON_ENABLED) | HIGH | `AttributeConverter`(Boolean↔'Y'/'N', @Convert)로 필드 Boolean 유지 + DB CHAR(1). 필드타입 미변경으로 파급 최소화. 실패 케이스 테스트 |
| 2 | VARCHAR(1)→CHAR(1) 전환 시 trailing space/비교 이슈 | MED | PostgreSQL CHAR(1)은 1자라 패딩 무의미. 값 'Y'/'N' 유지, 비교/트림 확인 |
| 3 | rename된 컬럼이 native 쿼리/QueryDSL/@Query/View에 하드코딩 | HIGH | rename 전 grep으로 전체 참조 탐색 후 일괄 수정. ddl-validate + 테스트로 검증 |
| 4 | LS_TUS_UPLOAD 삭제가 upload/ 패키지 전체(controller/service/repo/scheduler) 제거 → 기능 회귀 | HIGH | 별도 승인 후 진행. 참조(FK·다른 서비스) 사전 확인. Phase 5 격리 |
| 5 | LS_DATA_SET drop 시 운영 klid_system에 실데이터 존재 가능 | MED | drop 전 존재/사용 검증. VideoRepository 비활성 쿼리 잔재 정리 |
| 6 | ddl-auto=validate 부팅이 타입 카테고리 불일치에 민감 | MED | 각 Phase 후 build-validator로 부팅+테스트 확인, DEV_FIX 루프 |

---

## 상세 계획

### Phase 1: 용어사전 POLL 등록 (LogiCraft, 비파괴)

**목표**: 미등록 어휘 POLL(폴링)을 사업 표준 단어·용어로 등록해 이후 POLL_* 컬럼이 표준 정합이 되게 한다. 코드 변경 없음(MCP 레지스트리).

**작업**:
- `program_word_create`: 단어 "폴링" / 약어 POLL / 영문 Polling / 설명 "외부 시스템 비동기 처리 결과를 주기적으로 조회하는 폴링". brownfield 메타 포함.
- `program_glossary_create` (필요 시): POLL_STTS_CD(폴링상태코드)·POLL_LAST_DT(폴링최종일시)·POLL_ATMPT_CNT(폴링시도횟수) 합성 용어. LogiCraft 정책상 한글명·설명 확정 데이터만 등록.

**수용 기준**:
- [ ] POLL 단어가 `program_word_search`로 조회됨
- [ ] warnings 없음(있으면 사용자 보고)

**의존성**: 없음 (Phase 2의 POLL_ATMPT_CNT rename과 정합)

**실행**: PM 직접(LogiCraft MCP — 코드 아님)

### Phase 2: 컬럼 rename — 이름 정합 (Flyway V83 + 엔티티)

**목표**: 비표준 토큰을 표준 약어로 rename. 데이터 무손실(ALTER RENAME).

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/src/main/resources/db/migration/V83__rename_glossary_std_columns_2.sql` | 생성 | ALTER TABLE ... RENAME COLUMN ×5 (+인덱스 rename 동반 시) |
| `batch/entity/LsDeidentProcLog.java` | 수정 | @Column name: REQ_KIND_CD→REQ_KND_CD, POLL_ATTEMPT_CNT→POLL_ATMPT_CNT, KPST_PRJ_ID→DE_IDNTF_PJT_ID, KPST_DATASET_ID→DE_IDNTF_DATST_ID (필드명·게터 유지, @Column name만 변경) |
| `batch/entity/LsDataLbl.java`/`LsDataLblHstry` (해당 엔티티) | 수정 | @Column name: REGISTERED_AT→REG_AT |
| 참조 쿼리/Repository/View | 수정 | 위 컬럼 참조하는 native @Query·QueryDSL·V_COMPLETED_* View 정합 |

**핵심 작업**:
```sql
-- V83
ALTER TABLE LS_DEIDENT_PROC_LOG RENAME COLUMN REQ_KIND_CD TO REQ_KND_CD;
ALTER TABLE LS_DEIDENT_PROC_LOG RENAME COLUMN POLL_ATTEMPT_CNT TO POLL_ATMPT_CNT;
ALTER TABLE LS_DEIDENT_PROC_LOG RENAME COLUMN KPST_PRJ_ID TO DE_IDNTF_PJT_ID;
ALTER TABLE LS_DEIDENT_PROC_LOG RENAME COLUMN KPST_DATASET_ID TO DE_IDNTF_DATST_ID;
ALTER TABLE LS_DATA_LBL_HSTRY RENAME COLUMN REGISTERED_AT TO REG_AT;
```
- rename 전 `grep -rn "KPST_PRJ_ID\|KPST_DATASET_ID\|REQ_KIND_CD\|POLL_ATTEMPT_CNT\|REGISTERED_AT"` 로 전체 참조 확인.

**테스트 케이스** (RED):
- `LS_DEIDENT_PROC_LOG_rename된_컬럼으로_저장_조회_정상`
- `ddl_validate_부팅_성공_rename_반영`
- `KPST_폴링_관련_배치_기존_동작_회귀없음`

**수용 기준**:
- [ ] 5개 컬럼 rename 후 build+test GREEN
- [ ] validate 부팅 성공
- [ ] KPST 폴링(KpstDeidentPollJob 등) 동작 회귀 없음

**의존성**: Phase 1(POLL 등록) 권장 선행

### Phase 3: 안전 타입 정합 — FRM_NO BIGINT + truncation 확대 (Flyway V84 + 엔티티)

**목표**: 비파괴 타입 정합. INTEGER→BIGINT(확대), VARCHAR 길이 확대(truncation 위험 제거).

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `db/migration/V84__align_column_types.sql` | 생성 | ALTER TYPE BIGINT ×1 + ALTER TYPE VARCHAR(n) ×11 |
| `video/entity/LsDataSrc.java` | 수정 | FRM_NO 필드 Integer→Long (VDO_FRM_NO 동일 개념 여부 확인·동반) |
| 해당 엔티티들 | 수정 | @Column length 갱신: CMNT_CN·ERR_MSG_CN·STNG_VALUE(4000), FILE_PATH(1000), IDMP_KEY·VMS_CLIP_ID(128), VMS_CCTV_ID(64), EVNT_NM(200), OTSD_JOB_ID(200). **REQ_PAYLOAD_CN·RES_PAYLOAD_CN → TEXT**(D9 문서가 이미 TEXT, VARCHAR4000 아님) |

**핵심 작업**:
```sql
-- V84
ALTER TABLE LS_DATA_SRC ALTER COLUMN FRM_NO TYPE BIGINT;
ALTER TABLE LS_ISSUE_COMMENT ALTER COLUMN CMNT_CN TYPE VARCHAR(4000);
-- ... (11 길이 확대, 각 테이블)
```
- 길이 확대·Int→BigInt 모두 데이터 무손실 확대.

**테스트 케이스** (RED):
- `FRM_NO_BIGINT_범위값_저장_조회`
- `truncation_대상_컬럼_확대길이_초과데이터_저장_성공`
- `ddl_validate_타입_일치_부팅`

**수용 기준**:
- [ ] 12개 컬럼 타입/길이 변경 후 build+test GREEN
- [ ] validate 부팅 성공 (엔티티 length/타입 ↔ DB 일치)

**의존성**: 없음 (Phase 2와 독립, 순차 권장 — 마이그레이션 버전 순서)

### Phase 4: 여부 도메인 CHAR(1) 전환 — YN 14건 (Flyway V85 + 엔티티)

**목표**: 공공 여부C1(CHAR1) 채택. VARCHAR(1) YN 12건 + BOOLEAN 2건 → CHAR(1) 'Y'/'N'. 최고 위험 격리 Phase. (검증: gov `~여부` 1424건 전부 여부C1)

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `db/migration/V85__yn_char1_domain.sql` | 생성 | VARCHAR(1)→CHAR(1) ×12, BOOLEAN→CHAR(1) ×2 (USING 절로 boolean→'Y'/'N') |
| `common/converter/YesNoConverter.java` | 생성(없으면) | `AttributeConverter<Boolean,String>` — true↔'Y', false↔'N'. BBOX/POLYGON 필드 Boolean 유지용 |
| `label/entity/LsLabelPresetCode.java` | 수정 | BBOX_ENABLED·POLYGON_ENABLED에 `@Convert(YesNoConverter.class)` + @Column CHAR(1) |
| YN 보유 엔티티들 | 수정 | VARCHAR(1) 12건 @Column을 CHAR(1)로(columnDefinition="char(1)"). 필드 String 유지 |

**핵심 작업**:
```sql
-- V85 (BOOLEAN→CHAR 예)
ALTER TABLE LS_LABEL_PRESET_CODE ALTER COLUMN BBOX_ENABLED TYPE CHAR(1)
  USING (CASE WHEN BBOX_ENABLED THEN 'Y' ELSE 'N' END);
-- VARCHAR(1)→CHAR(1) 예
ALTER TABLE LS_DATA_RAW ALTER COLUMN DE_IDENT_YN TYPE CHAR(1);
```
- 대상 12(VARCHAR1): ACTVTN_YN·ANONY_INCL_YN·AUTO_LBL_YN·COORD_RECALC_YN·DE_IDENT_YN·JOB_DMND_YN·MUTABLE_YN·PRVC_INCL_YN·PRVC_YN·PSDO_INCL_YN·UPEND_FIX_YN·USE_YN
- 대상 2(BOOLEAN): BBOX_ENABLED·POLYGON_ENABLED
- **선행 확인**: CLCT_YN 실제 타입/매핑 확인 후 대상 편입 여부 결정.
- Hibernate CHAR 매핑: String 필드 + `columnDefinition="char(1)"`(bpchar). validate 통과 형태로.

**테스트 케이스** (RED):
- `BOOLEAN_ENABLED_컬럼_CHAR1_전환후_true_Y_false_N_왕복`
- `VARCHAR1_YN_컬럼_CHAR1_전환후_Y_N_read_write`
- `여부_컬럼_JSON_직렬화_Y_N_유지`
- `ddl_validate_CHAR1_일치_부팅`

**수용 기준**:
- [ ] 14개 YN 컬럼 CHAR(1) 전환 후 build+test GREEN
- [ ] BBOX/POLYGON Boolean 필드 값 왕복 정상(converter)
- [ ] validate 부팅 성공
- [ ] YN 관련 기존 로직(비식별·마킹·프리셋) 회귀 없음

**의존성**: Phase 3 이후 (마이그레이션 버전 순서)

### Phase 5: 테이블 삭제 — LS_DATA_SET + LS_TUS_UPLOAD (개별 승인)

**목표**: orphan/폐지 테이블 제거. **파괴적 — 각 삭제 전 사용자 개별 승인.**

**작업**:
- **5-A LS_DATA_SET drop**: 운영 klid_system 존재/사용 검증 → 미사용 확인 시 `db/migration/V86__drop_ls_data_set.sql`. VideoRepository 비활성 쿼리 잔재 정리.
- **5-B LS_TUS_UPLOAD 제거 (개별 승인 필수)**: upload/ 패키지(TusUploadController/Service/Repository/entity/TusUploadCleanupJob/DurationProbeFfprobe/VideoMagicByteValidator) + 라우팅/보안설정 + `V87__drop_ls_tus_upload.sql`. 다른 서비스 참조 사전 확인. FE 연동(있으면) 확인.

**테스트 케이스** (RED):
- `LS_DATA_SET_drop_후_부팅_및_영상조회_정상`
- `TUS_제거_후_전체_빌드_테스트_GREEN_참조없음`

**수용 기준**:
- [ ] LS_DATA_SET drop 후 validate 부팅 + 영상 도메인 회귀 없음
- [ ] LS_TUS_UPLOAD 제거 후 컴파일·테스트 GREEN, 잔존 참조 0
- [ ] 각 삭제 전 사용자 승인 획득

**의존성**: 마지막 (위험도 최고 → 최후)

### Phase 6: 문서 정합 — D8/D9 설계서 반영 (폐쇄망 수동)

**목표**: 코드 스키마 변경을 D8 엔티티관계모형설계서·D9 데이터베이스설계서(폐쇄망 Word/HWP)에 반영. 직접 편집 불가 → **찾아바꾸기 가이드** 제공.

**산출물**: `reports/D8D9-표준정합-수정가이드-2026-07-10.md` (완료됨)
- § A rename(D8·D9 물리명+논리명), § B YN VARCHAR(1)→CHAR(1)(D9), § B-2 BOOLEAN→CHAR(D8·D9), § C 길이확대(D8·D9), § D 삭제 테이블 블록 제거, § E 문서 무수정(이미 정합: FRM_NO·VMS_*·payload TEXT)
- 리포 미러(`docs/design/D8·D9`)도 같은 규칙으로 갱신 검토(선택) — cc-doc-gen/LogiCraft 재생성 경로 확인

**수용 기준**:
- [ ] 가이드가 코드 최종 변경분과 1:1 정합 (rename/타입/길이/삭제 누락 0)
- [ ] 폐쇄망본 반영 후 D8/D9 ↔ 실제 스키마 일치 (사용자 수동 확인)

**의존성**: Phase 2~5 확정 후 (최종 스키마 기준으로 가이드 검증)

---

## 실행 가이드

1. `Phase 1 진행해줘` — LogiCraft POLL 등록 (PM 직접, 코드 없음)
2. `/cc-tdd Phase 2: 컬럼 rename 5건 (Flyway V83 + 엔티티 + 참조)`
3. `/cc-tdd Phase 3: FRM_NO BIGINT + truncation 길이확대 + payload TEXT (Flyway V84)`
4. `/cc-tdd Phase 4: 여부 CHAR(1) 전환 14건 + YesNoConverter (Flyway V85)`
5. `/cc-tdd Phase 5: LS_DATA_SET drop + LS_TUS_UPLOAD 제거 (개별 승인)`
6. `Phase 6 진행해줘` — D8/D9 수정 가이드 최종 검증 (문서, 코드 없음)
