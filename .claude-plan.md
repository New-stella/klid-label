# 구현 계획: PostgreSQL 전환 + Gitea 제거(라벨 버전/이력 DB화)

> 생성: 2026-05-29 15:30 / 갱신: 2026-05-29 (PG 마이그레이션 상세 플랜 병합)
> 상태: ✅ 전체 완료 (Phase 0~7) — 라이브 통합검증(cc-verify/ui-tester, 풀스택 기동 필요)만 후속
> 비고: 이전 계획(시계열 메타 UI 개편)은 완료되어 본 계획으로 대체함. 별도 `.claude-plan-pg-migration.md`의 PG 마이그레이션 상세를 본 계획에 흡수·통합함.

## Phase 진행 현황

- [x] Phase 0: 설계문서 수정 (CLAUDE.md + docs/design) — PostgreSQL/DB-히스토리 반영, Gitea 제거
  > 구현: CLAUDE.md(전체 현행화 — PG 전환·DB 스냅샷 버전관리·GiteaClient/GITEA_* 제거·버전태그 정리), docs/design/{기능정의서(FEAT-002 비즈니스규칙), R1(v1.2 changelog+헤더), brownfield(현행화 메모+diff_summary 2건 정정)}
- [x] Phase 1: DB 드라이버·빌드·환경설정 전환 (MariaDB→PostgreSQL)
  > 구현: build.gradle(postgresql/flyway-database-postgresql/testcontainers-postgresql), application-*.yml ×5 PG datasource, application.yml Quartz PostgreSQLDelegate, gradle.lockfile 재생성. compileJava PASS
- [x] Phase 2: Flyway 마이그레이션 SQL PostgreSQL 전환 (MNG_*/QRTZ_* 격리 포함)
  > 구현: 26개 MariaDB 문법 마이그레이션 PG 전환(IDENTITY/TEXT/TIMESTAMP/ON CONFLICT/별도 INDEX), 백틱·ENGINE·AUTO_INCREMENT 잔존 0. 캐리포워드: 실제 PG clean migrate 검증은 Phase 3에서
- [x] Phase 3: JPA Entity 컬럼 정합 + 테스트 인프라(Testcontainers PG) — ddl-auto=validate 통과
  > 구현: PostgresTestContainer(postgres:16-alpine) + build.gradle `systemProperty 'api.version','1.44'`(docker-java 연결), HikariCP 풀 축소+max_connections=300, LsDataLbl.pointsJson @Lob→TEXT(PG large-object 버그), columnDefinition LONGTEXT→TEXT. Flyway clean migrate V1~V52 실 PG 검증, validate 부팅 OK
- [x] Phase 4: QueryDSL·네이티브 쿼리 PostgreSQL 호환성
  > 구현: 네이티브 쿼리 IFNULL→COALESCE 등 PG 치환(잔존 0). **검증: ./gradlew clean build GREEN, 1018/1018 테스트 PASS**
- [x] Phase 5: Gitea 제거 → DB 스냅샷 버전관리 (BE — version 도메인 재설계)
  > 구현: GiteaClient/PathPolicy/async·fallback(9파일)/HealthIndicator 삭제, LsLabelVersion→versionHash+labelPayload, VersionService DB 스냅샷(commit/list/diff/rollback), V53(복합 UNIQUE+LS_GITEA_FALLBACK_QUEUE DROP), yml/.env gitea 제거, 테스트 6파일 정리. **build GREEN 966/966**, git grep gitea(src/main·yml·test)=0. CORS 바인딩 버그 수정. API 식별자=versionHash
- [x] Phase 6: 프론트 version 기능 신규 DB API 연동
  > 구현: types.ts(RollbackResponse 신규 계약·LabelDiff before/after Shape|null), api.ts(diff path=toHash/query=fromHash), label/api.ts(commit 주석). giteaCmtHash·/diff/files 이미 부재. **npm run build(tsc+vite) 타입에러 0, version 단위 23/23 PASS**. 후속: ui-tester 라이브(풀스택 필요), npm audit 7건(6 moderate/1 high)
- [x] Phase 7: 통합 검증 + 잔여 Gitea 참조 0 확인
  > 검증: gitea 참조 — backend/src/main·java 0, application*.yml·.env 0, backend/src/test 0, frontend/src 0 (과거 마이그레이션 V6/V24/V41/V44만 Flyway append-only 정상 잔존, V53가 무력화). BE build GREEN 966, FE build GREEN 228. **/cc-verify ALL PASS**: 네이티브 PostgreSQL 부팅+Flyway V0~V53+validate, 라이브 API(목록/diff/rollback HTTP 200), 실브라우저 UI(로그인→목록→diff→롤백 920cc33 ROLLBACK, 콘솔에러 0). 리포트: reports/verify/2026-05-29-pg-gitea-version.md. 후속: npm audit 7건

## 요구사항 요약

- **목적**: 운영에 Gitea를 올릴 수 없어 라벨 버전관리를 DB 기반으로 전환. 동시에 사용 DB를 MariaDB→PostgreSQL로 변경(관제서버도 PG 전환 예정).
- **범위(포함)**:
  - 저작도구 소유 테이블(LS_*)의 PostgreSQL용 구성 + 듀얼 데이터소스(control+portal) 드라이버 전환
  - Flyway SQL·Entity·QueryDSL/네이티브 쿼리·Quartz·테스트의 PG 적응
  - Gitea 완전 제거 → 라벨 전체 스냅샷 DB 저장, diff·rollback 앱 계산
  - 설계문서(CLAUDE.md, docs/design) 선반영 후 코드 수정
- **범위(제외)**:
  - 관제 공유 MNG_* / QRTZ_* 스키마 소유 변경 (PG에서 validate/연동만, 인프라가 제공). MNG_* DDL은 우리 운영 마이그레이션에서 분리
  - `ai-server`(DB 미사용) / `frontend`(DB 직접 접근 없음)
  - logicraft 동기화 (추후 1차 정합 후 별도 반영)
  - 기존 운영 데이터 이관(데이터 펌프) + 기존 Gitea 커밋 데이터의 DB 이관 (운영 미적재 가정, 스키마/코드 전환만)
- **주요 액터**: WORKER/REVIEWER(라벨 저장·버전 조회·diff·rollback), 시스템(배치 저장·Quartz)
- **비기능**: ddl-auto=validate 유지(엔티티↔PG 타입 정합 필수), 라벨 스냅샷 1MB 제한 유지, 기존 테스트 회귀 0

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| 빌드 | `mariadb-java-client`→`org.postgresql:postgresql`, `flyway-mysql`→`flyway-database-postgresql`, `testcontainers:mariadb`→`testcontainers:postgresql` |
| 설정 | `application-{local,dev,stg,prd}.yml` ×5: dual datasource(control+portal) JDBC URL/드라이버, MySQL 전용 쿼리파라미터 제거, Quartz `driverDelegateClass`; `application.yml` gitea 블록 제거 |
| Flyway SQL | `db/migration/` (V0~V52) 중 MariaDB 전용 문법: AUTO_INCREMENT(~26), ENGINE=(4), utf8mb4/COLLATE(4), JSON(3), LONGTEXT(3), ON UPDATE CURRENT_TIMESTAMP(5), Quartz DDL(V2), Views(V52) |
| Entity | `columnDefinition="LONGTEXT"`→`TEXT`, JSON 매핑 4건 점검, `ON UPDATE` 의존 UPD_DT→`@UpdateTimestamp`, `@GeneratedValue(IDENTITY)` 33건은 호환(확인만) |
| 쿼리 | 네이티브 쿼리 3건(VideoRepository ×2, MngAcctUserAuthrtRepository ×1) MySQL 함수(IFNULL 등) 감사 + QueryDSL DB 의존 함수 감사 |
| DB 스키마(신규) | LS_LABEL_VERSION에 LABEL_PAYLOAD(TEXT)+VERSION_HASH 추가, GITEA_CMT_HASH 제거; LS_GITEA_FALLBACK_QUEUE drop (신규 마이그레이션 **V53**) |
| 도메인 모듈 | `version`(전면 재설계), `label`/`portal`(commit 호출부), `common.client`(GiteaClient 제거), `observability`(헬스/메트릭), `common.config`(Resilience4j/WebClient) |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | MariaDB 고유 문법(~30개 마이그레이션) PG 비호환 | HIGH | Phase 2 PG 표준 SQL 일괄 전환 + Testcontainers PG로 clean migrate 검증 |
| 2 | MNG_* 공유 테이블을 우리 Flyway가 생성 중이면 관제 PG 스키마와 충돌 | HIGH | MNG_* DDL을 운영 마이그레이션에서 분리 → 테스트 전용(`test/resources`) 또는 `IF NOT EXISTS` 격리 |
| 3 | Quartz `StdJDBCDelegate` + MariaDB Quartz DDL → PG 부팅 실패 | HIGH | `PostgreSQLDelegate` + Quartz 공식 PG 스키마(`tables_postgres.sql`, BLOB→bytea) |
| 4 | 테스트가 H2(MariaDB 호환) 기반 — PG 전환 시 다수 깨짐 | HIGH | Phase 3에서 Testcontainers PostgreSQL 베이스로 일괄 전환 |
| 5 | `ON UPDATE CURRENT_TIMESTAMP`(MariaDB 전용) PG 미지원 | MED | UPD_DT는 JPA `@UpdateTimestamp`/`@PreUpdate`로 앱 갱신 전환(트리거 회피) |
| 6 | `JSON` 컬럼 — PG `jsonb` vs `TEXT`가 엔티티 매핑과 불일치 | MED | 엔티티가 `TEXT` 매핑이면 DDL도 `TEXT` 통일(jsonb 미사용), validate 통과 우선 |
| 7 | 식별자 대소문자 — PG는 unquoted를 소문자 폴딩, MariaDB는 대문자 테이블명 | MED | 엔티티 `@Table/@Column` 대문자 ↔ PG unquoted lower 생성/조회 일관 확인, 필요시 물리네이밍 전략 점검 |
| 8 | label↔version 순환 의존(@Lazy) — Gitea 제거 리팩토링 시 깨질 수 있음 | MED | commit() 시그니처 유지(내부만 DB화)하여 LabelService 영향 최소화 |
| 9 | ddl-auto=validate — 엔티티 타입과 PG 컬럼 타입 불일치 시 부트 실패 | MED | 엔티티 매핑↔PG DDL 1:1 검증, build-validator 부팅 테스트 |

---

## 상세 계획

### Phase 0: 설계문서 수정 (CLAUDE.md + docs/design) ✅ 완료

**완료 내역**:
- `CLAUDE.md` — 기술스택(MariaDB→PostgreSQL, postgresql 드라이버, flyway-database-postgresql, Testcontainers PG), DB 정책(LS_* 소유·MNG_* validate), 버전관리 규칙(Gitea 자동 커밋→DB 스냅샷, diff/rollback 앱 계산), 공통 인프라 GiteaClient 제거, 환경변수 GITEA_* 제거, 버전태그(V1.x~V2.0/V52) 전부 정리.
- `docs/design/기능정의서.md` — FEAT-002에 DB 스냅샷 비즈니스 규칙 추가.
- `docs/design/R1-사용자요구사항정의서.md` — v1.2 changelog + 헤더 버전 갱신.
- `docs/design/brownfield-변경명세-2026-05-29.md` — 현행화 메모 + FEAT-002/REQ-009 diff_summary "Gitea→DB 스냅샷" 정정.

**수용 기준**: ✅ CLAUDE.md 모순 Gitea 잔여 0, ✅ 버전관리 규칙 DB 스냅샷 기술, ✅ 환경변수 GITEA_* 제거.

### Phase 1: DB 드라이버·빌드·환경설정 전환

**목표**: PG 드라이버·Flyway·Testcontainers로 의존성 교체 + 5개 프로파일 yml datasource/Quartz를 PG로 전환하여 애플리케이션이 PG를 바라보고 부팅 가능.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/build.gradle` | 수정 | `org.mariadb.jdbc:mariadb-java-client`→`org.postgresql:postgresql`, `flyway-mysql`→`flyway-database-postgresql`, `testcontainers:mariadb`→`testcontainers:postgresql`. H2 유지 여부 결정(Phase 3에서 제거 가능) |
| `application-local.yml` | 수정 | control+portal `jdbc:mariadb`→`jdbc:postgresql`, driver `org.postgresql.Driver`, MySQL 전용 파라미터(`characterEncoding`/`allowPublicKeyRetrieval`/`useSSL`) 제거·PG 등가 |
| `application-dev.yml` | 수정 | 동일 (control+portal dual) |
| `application-stg.yml` | 수정 | 동일 |
| `application-prd.yml` | 수정 | 동일 |
| `application.yml` | 수정 | Quartz `org.quartz.jobStore.driverDelegateClass`=`org.quartz.impl.jdbcjobstore.PostgreSQLDelegate`. dialect는 Hibernate 자동감지 유지(또는 `PostgreSQLDialect` 명시). gitea integration 블록 제거(Phase 5와 협의) |
| `backend/.env.example` | 수정 | DB 환경변수 PG화, GITEA_* 제거 |
| `backend/gradle.lockfile` | 수정 | 의존성 변경 반영 (재생성) |

**핵심 변경**:
```
- runtimeOnly 'org.mariadb.jdbc:mariadb-java-client'
+ runtimeOnly 'org.postgresql:postgresql'
- implementation 'org.flywaydb:flyway-mysql'
+ implementation 'org.flywaydb:flyway-database-postgresql'
- testImplementation 'org.testcontainers:mariadb:1.19.8'
+ testImplementation 'org.testcontainers:postgresql:1.19.8'
# yml: jdbc-url: jdbc:postgresql://${HOST}:${PORT}/${NAME} / driver-class-name: org.postgresql.Driver
```

**수용 기준**:
- [ ] `./gradlew compileJava` 성공, 의존성 트리에 mariadb 잔존 없음
- [ ] gradle.lockfile 재생성되어 빌드 통과
- [ ] Quartz delegate가 PostgreSQLDelegate로 설정됨

**처리 방식**: 설정/빌드 Phase → "Phase 1 진행해줘" + build-validator
**의존성**: Phase 0

### Phase 2: Flyway 마이그레이션 SQL PostgreSQL 전환

**목표**: `db/migration/` SQL을 PG 문법으로 전환하여 빈 PG에서 V0~V52 전체가 오류 없이 적용됨. MNG_*/QRTZ_* 공유 테이블 DDL은 운영 마이그레이션에서 분리.

**전환 규칙** (전 파일 일괄):
| MariaDB | PostgreSQL |
|---------|-----------|
| `BIGINT NOT NULL AUTO_INCREMENT` + `PRIMARY KEY` | `BIGINT GENERATED BY DEFAULT AS IDENTITY` (IDENTITY 전략 정합) |
| `ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=...` | 제거 |
| `LONGTEXT` / `MEDIUMTEXT` | `TEXT` |
| `DATETIME` | `TIMESTAMP` |
| `JSON` | `TEXT` (엔티티 매핑과 통일, 위험 #6) |
| `... ON UPDATE CURRENT_TIMESTAMP` | 절 제거 (UPD_DT는 앱 갱신, 위험 #5) |
| 인라인 `INDEX`/`UNIQUE KEY` | 별도 `CREATE INDEX`/`CONSTRAINT` |
| 백틱 식별자 | 제거 또는 큰따옴표 |
| `IF NOT EXISTS` | PG도 지원 — 유지 가능 |

**특수 처리**:
- `V2__*quartz*.sql`: Quartz DDL을 공식 PG 스키마(`tables_postgres.sql`)로 치환(BLOB→bytea). LS_* 부분은 일반 규칙 적용
- `V52__*datamart_views*.sql`: `V_COMPLETED_*` 4종 — `CREATE OR REPLACE VIEW` PG 호환 확인 + MySQL 함수(`IFNULL`→`COALESCE`, 백틱 등) 치환
- MNG_* 포함 마이그레이션: MNG_* DDL을 테스트 전용 위치로 이동 또는 `IF NOT EXISTS`로 격리 (위험 #2)

**수용 기준**:
- [ ] 빈 PostgreSQL에 V0~V52 전체 적용 성공 (`flyway migrate` 무오류)
- [ ] AUTO_INCREMENT/ENGINE/LONGTEXT/ON UPDATE/백틱 잔존 0건
- [ ] Quartz 테이블이 PG 스키마로 생성됨

**처리 방식**: SQL 위주 → "Phase 2 진행해줘" + build-validator(Testcontainers PG migrate 검증)
**의존성**: Phase 1

### Phase 3: JPA Entity 컬럼 정합 + 테스트 인프라(Testcontainers PG)

**목표**: `ddl-auto=validate`가 PG 생성 스키마와 충돌 없이 통과하고, 테스트가 PG 컨테이너에서 그린.

**생성·수정 파일** (점검 대상 47엔티티, 실제 수정은 소수):
| 파일 | 동작 | 설명 |
|------|------|------|
| `columnDefinition="LONGTEXT"` 보유 엔티티 | 수정 | `LONGTEXT`→`TEXT` |
| JSON 매핑 엔티티 4건(POINTS/PAYLOAD/MARKS/CONTENT_BASE64) | 점검 | `TEXT` 통일 확인 |
| `ON UPDATE CURRENT_TIMESTAMP` 의존 UPD_DT | 수정 | `@UpdateTimestamp`/`@PreUpdate` 적용 |
| `@GeneratedValue(strategy=IDENTITY)` 33건 | 확인 | PG IDENTITY 호환 — 변경 불필요 |
| `backend/src/test/.../support/*PostgresTestBase*.java` | 생성 | Testcontainers `PostgreSQLContainer`(예: `postgres:16`) 공통 베이스(@Container, @DynamicPropertySource) |
| `backend/src/test/resources/application-local.yml` | 수정 | H2 in-memory → Testcontainers PG 연동 |

**테스트 케이스**:
- `엔티티_검증_PG스키마와_일치 (ddl-auto=validate 부팅 성공)`
- `UPD_DT가_수정시_자동갱신됨 (앱 레벨 타임스탬프)`
- 기존 Repository/Service 테스트가 PG 컨테이너에서 그린

**수용 기준**:
- [ ] PG 기동 시 Hibernate `validate` 통과 (스키마 불일치 예외 0)
- [ ] LONGTEXT columnDefinition 잔존 0건
- [ ] 기존 테스트 스위트 PG 기반 그린(회귀 0)

**처리 방식**: `/cc-tdd Phase 3` (Entity 정합 + 테스트 베이스, developer-tdd) + build-validator
**의존성**: Phase 2

### Phase 4: QueryDSL·네이티브 쿼리 PostgreSQL 호환성

**목표**: MySQL 전용 함수/문법을 PG 등가로 치환하여 모든 쿼리가 PG에서 동작.

**점검·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `video/repository/VideoRepository.java` | 점검·수정 | 네이티브 쿼리 2건 — `IFNULL`→`COALESCE`, `LIMIT/OFFSET`, 날짜함수, `GROUP BY` 엄격성 PG 호환 |
| `user/repository/MngAcctUserAuthrtRepository.java` | 점검·수정 | 네이티브 쿼리 1건 동일 점검 |
| QueryDSL 사용 Repository 전반 | 점검 | `Expressions.stringTemplate` 등 DB 의존 함수 호출 감사 |

**테스트 케이스**:
- `VideoRepository_네이티브쿼리_PG에서_정상조회`
- `사용자권한_조회_PG에서_정상`

**수용 기준**:
- [ ] 네이티브/QueryDSL 쿼리 관련 테스트 전부 GREEN
- [ ] MySQL 전용 함수(IFNULL, GROUP_CONCAT 등) 잔존 0건

**처리 방식**: `/cc-tdd Phase 4` (developer-tdd) + build-validator
**의존성**: Phase 2(스키마), Phase 3

### Phase 5: Gitea 제거 → DB 스냅샷 버전관리 (BE)

**목표**: 라벨 저장 시 전체 스냅샷을 DB에 저장하고, listVersions/diff/rollback이 DB만으로 동작한다. Gitea 코드 전면 제거.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `db/migration/V53__alter_ls_label_version_payload.sql` | 생성 | LS_LABEL_VERSION에 `LABEL_PAYLOAD TEXT`, `VERSION_HASH VARCHAR(64)` 추가, `GITEA_CMT_HASH` 제거, UNIQUE(VERSION_HASH); `LS_GITEA_FALLBACK_QUEUE` DROP. (※ 기존 마이그레이션이 V52까지 존재 → 다음 번호 V53. 운영 미적재면 V24/V41 in-place 수정도 대안) |
| `version/entity/LsLabelVersion.java` | 수정 | giteaCmtHash→versionHash + labelPayload 필드, create() 시그니처 변경 |
| `version/service/VersionService.java` | 수정 | commit(): 페이로드 직렬화→스냅샷 저장(VERSION_HASH=payload SHA-256); listVersions(): DB만; diff(): 두 스냅샷 DB 조회 후 LabelDiffDto 계산; rollback(): 대상 스냅샷 복원→새 active 버전. Gitea/fallback 의존성 전부 제거 |
| `version/service/GiteaPathPolicy.java` | 삭제 | |
| `version/async/Gitea*` (3) | 삭제 | in-memory fallback 큐/잡/스케줄링 |
| `version/fallback/*` (6) | 삭제 | 영속 fallback 큐/repo/서비스/잡/스케줄링/엔티티 |
| `common/client/GiteaClient.java` + `dto/CommitResponse,DiffFile,DiffResponse` | 삭제 | |
| `observability/health/GiteaHealthIndicator.java` | 삭제 | |
| `observability/metrics/ExternalApiMetrics.java` | 수정 | gitea 메트릭 제거 |
| `common/config/Resilience4jConfig.java`, `WebClientConfig.java` | 수정 | gitea 인스턴스/빈 제거 |
| `sysconfig/.../ManageHealthController.java` | 수정 | gitea health 항목 제거 |
| `label/service/LabelService.java`, `portal/service/PortalLabelService.java` | 수정 | commit() 호출 유지(내부 DB화), Gitea 직접 참조 제거 |
| `version/dto/*` (VersionItem/DiffResponseDto 등) | 수정 | fromGitea 제거, fromLabelVersion 중심 + payload 기반 |

**핵심 시그니처**:
```java
// VersionService (controlTransactionManager)
public String commit(Long srcSn, String labelsJson, TokenClaims actor); // 반환=versionHash
public List<VersionItem> listVersions(Long srcSn, TokenClaims actor);    // DB만
public DiffResponseDto diff(String fromHash, String toHash, TokenClaims actor); // DB 스냅샷 비교
public VersionResponse rollback(Long srcSn, String toHash, TokenClaims actor);  // 스냅샷 복원→새 버전
```

**테스트 케이스**:
- `라벨_저장시_전체_스냅샷이_DB에_저장된다`
- `동일_페이로드_재저장시_VERSION_HASH가_같다`
- `diff가_Gitea_없이_두_DB스냅샷으로_계산된다`
- `rollback시_대상_스냅샷이_새_active_버전으로_복원된다`
- `PORTAL_채널은_버전관리_미제공`
- `소스코드에_GiteaClient_참조가_없다`(아키텍처 가드)

**수용 기준**:
- [ ] `git grep -i gitea -- backend/src/main` 결과 0
- [ ] listVersions/diff/rollback이 Gitea 없이 동작
- [ ] 기존 VersionControllerTest 등 신규 동작에 맞게 그린

**처리 방식**: `/cc-tdd Phase 5` (developer-tdd, opus). diff/rollback 동시성·접근권한 시나리오 포함.
**의존성**: Phase 3 (Entity/validate), Phase 4

### Phase 6: 프론트 version 기능 신규 DB API 연동

**목표**: 버전 이력/Diff/롤백 UI가 신규 DB 기반 API(versionHash 식별자)로 동작.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/features/version/*` (API 클라이언트·타입) | 수정 | SHA→versionHash/versionNo 식별자, 응답 타입 갱신 |
| `frontend/src/features/version/DiffViewer.tsx` | 수정 | DB diff 응답 구조 반영 |
| `frontend/src/features/version/HistoryPage.tsx`, `HistoryPanel.tsx` | 수정 | 버전 목록/롤백 호출 갱신 |
| `frontend/src/features/version/__tests__/*` | 수정 | mock 응답을 신규 스키마로 |

**테스트 케이스**:
- `버전_목록이_DB_API_응답으로_렌더된다`
- `Diff_뷰어가_신규_diff_스키마를_표시한다`
- `롤백_버튼_클릭시_versionHash로_API_호출`

**수용 기준**:
- [ ] version 기능 단위테스트 그린
- [ ] ui-tester 브라우저 검증 PASS(이력 조회→diff→롤백 플로우)

**처리 방식**: `/cc-tdd Phase 6` (FE) + ui-tester
**의존성**: Phase 5 (API 계약 확정 후)

### Phase 7: 통합 검증 + 잔여 Gitea 참조 0 확인

**목표**: 전체 빌드/테스트 그린, Gitea 흔적 제거 완료, 통합 시나리오 검증.

**작업**:
- `git grep -i gitea` 전역 0 확인(문서 히스토리 메모 제외)
- 전체 `./gradlew clean build` (PG Testcontainers) + `npm test`
- `/cc-verify` 통합 시나리오(라벨 저장→버전 생성→diff→rollback, PG 부팅)

**수용 기준**:
- [ ] BE/FE 빌드·테스트 전부 그린 (회귀 0)
- [ ] backend/src·frontend/src에 gitea 코드 참조 0
- [ ] cc-verify 통합 시나리오 PASS

**처리 방식**: build-validator + `/cc-verify`
**의존성**: Phase 6

---

## 실행 가이드

1. Phase 1: "Phase 1 진행해줘" — build.gradle + application-*.yml ×5 + Quartz delegate + .env PG 전환
2. Phase 2: "Phase 2 진행해줘" — Flyway db/migration SQL 일괄 PG 문법 전환 (MNG_*/QRTZ_* 격리)
3. Phase 3: `/cc-tdd Phase 3: JPA Entity 컬럼/UPD_DT 정합 + Testcontainers PG 베이스 (ddl-auto=validate)`
4. Phase 4: `/cc-tdd Phase 4: QueryDSL·네이티브 쿼리 PG 호환성 치환`
5. Phase 5: `/cc-tdd Phase 5: Gitea 제거 → DB 스냅샷 버전관리(VersionService 재설계)`
6. Phase 6: `/cc-tdd Phase 6: 프론트 version 기능 신규 DB API 연동`
7. Phase 7: `/cc-verify` — 통합 시나리오 + 잔여 Gitea 참조 0 확인
