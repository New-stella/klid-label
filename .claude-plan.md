# 구현 계획: ccarch-authoring 정합성 정렬

> 생성: 2026-05-19 · 상태: Phase 1·2 완료, Phase 3 대기
> ccarch 워크스페이스 업로드(MCP)는 제외. VlmMetaStep cleanup은 Phase 4에 합류.

## Phase 진행 현황

- [x] **Phase 1**: VLM 외부 위탁 (커밋 `c6c7fd1`) — VlmClient 신설(SSRF/HTTPS/응답검증), VlmTimeseriesStep, ai-server vlm.py 영상메타 회귀가드, VlmMetaStep NO-OP. 754/754 PASS, DEV_FIX 1회
- [x] **Phase 2**: 비동기 결과 수신 webhook 3종 (커밋 `11a76d3`) — HmacWebhookFilter + WebhookIdempotencyLedger(영속 V39/InMemory), V40 LS_DEIDENT_PROC_LOG UNIQUE, ExternalUrlValidator(CGNAT/IPv4-mapped/IDN), DTO @Size, prd 환경변수 강제. 794/794 PASS, DEV_FIX 2회
- [ ] **Phase 3**: Deidentify 비동기 정합 + Gitea fallback 큐 (V41)
- [ ] **Phase 4**: 비동기 표준 컬럼 (V42/V43) + Augment race 흡수 + VlmMetaStep 완전 폐기

---

## Phase 3: Deidentify 비동기 + Gitea fallback

### 변경 파일
- `DeidentifyClient`: idempotencyKey 자동 발급 + `X-Idempotency-Key` 헤더 + `webhookIdempotencyLedger.recordIssued(key, "DEIDENTIFY")` 호출
- `DeidentifyStep`: 위탁 후 non-blocking — 결과는 Phase 2 webhook 으로 수신. `LS_BATCH_PROC_LOG` PENDING 마킹
- `GiteaClient`: `@CircuitBreaker(fallbackMethod="giteaFallback")` — fallback method 에서 `LS_GITEA_FALLBACK_QUEUE` 적재. `gitea.fallback.enabled` 기본 false (dev:false, stg/prd:true)
- 신규: `LsGiteaFallbackQueue` 엔티티 + Repository + Service + `GiteaFallbackRetryJob` (Quartz, 1분 주기, 지수 백오프, MAX_RETRY=5 → DEAD_LETTER)
- V41 마이그레이션: `LS_GITEA_FALLBACK_QUEUE` (idempotencyKey UNIQUE, operation, path, content_base64, retryCount, status, nextRetryAt, deadLetterAt)

### 시나리오 (HIGH)
- 큐 무한 적재 → MAX_RETRY=5 + DEAD_LETTER 마킹
- RetryJob 동시 실행 race → RETRYING 상태 마킹 후 처리 (status 전이)
- 동일 idempotencyKey 충돌 → UNIQUE + DataIntegrityViolationException 흡수

### 수용 기준
- DeidentifyClient 호출 시 idempotencyKey가 `LS_WEBHOOK_IDEMPOTENCY` 에 ISSUED 상태로 적재
- Gitea 정상 흐름 영향 0건. CircuitBreaker open 시 환경별 동작 (dev: 예외, stg/prd: 큐 적재)
- RetryJob: 지수 백오프 + dead-letter 종료

### 실행 가이드
`/cc-tdd Phase 3` → `/cc-code-review` → `/cc-commit`

---

## Phase 4: 비동기 표준 컬럼 + Augment race + VlmMetaStep cleanup

### 변경 파일
- V42 마이그레이션: `LS_DATA_META`, `LS_DATA_AUG` 에 `IDEMPOTENCY_KEY VARCHAR(64) UNIQUE`, `EXTERNAL_JOB_ID VARCHAR(128) UNIQUE`, `RETRY_COUNT INT DEFAULT 0`, `DEAD_LETTER_AT DATETIME NULL` 추가
- V43 마이그레이션: 기존 행 idempotencyKey backfill (PK 기반 결정적 UUID, 충돌 0 보장)
- 엔티티 매핑 추가: `LsDataMeta`, `LsDataAug`
- `AugmentResultService`: Deident/Vlm 와 동일하게 `DataIntegrityViolationException` race 흡수 catch 추가
- `VlmMetaStep.java` + `VlmMetaStepTest.java` 삭제 (Phase 1 NO-OP 처리분 완전 폐기)

### 시나리오 (MEDIUM)
- backfill 대량 행 성능 → 인덱스 생성 후 backfill 순서, 또는 batch update
- UNIQUE 충돌 → 결정적 UUID 사용으로 충돌 0

### 수용 기준
- 2개 테이블에 표준 컬럼 추가 + backfill 완료
- AugmentResultService race 흡수 (Deident/Vlm 와 일관)
- VlmMetaStep 클래스 + 테스트 git에서 삭제

### 실행 가이드
`/cc-tdd Phase 4` → `/cc-code-review` → `/cc-commit` → `/cc-verify`

---

## 결정 사항

- VLM 책임: 외부 위탁 (CLAUDE.md V1.8)
- 비동기 결과 수신: 콜백 웹훅 + HMAC + idempotency allowlist
- ccarch MCP 업로드: 제외 (코드 정합성만)
- Migration 번호: V39/V40 (Phase 2 완료) → V41 (Phase 3) → V42/V43 (Phase 4)
