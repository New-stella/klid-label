# 구현 계획: 작업목록·검수목록 정렬/필터 관행안 개편 (BE 서버화 + FE 연동)

> 생성: 2026-07-29 | 유형: DEV (규모 XL, BE + FE) | 상태: 착수 대기
> 🎯 목표: 두 목록에서 **정렬은 시간축 하나로 단순화**하고 **우선순위는 필터로 표현**하는 업계 관행 구조로 바꾼다. 필터·집계를 전부 BE로 옮겨 페이지를 넘겨도 결과·KPI 숫자가 전체 기준으로 정확해지고, 기존 API 호출은 파라미터 변경 없이 그대로 동작한다.

## Phase 진행 현황

- [x] Phase 1: 작업목록 BE — 정렬 정책 전환 + 필터 파라미터 신설 + 정렬 allowlist ✅ PASS
  > 구현: `assignment/domain/BoardWorkStatus.java`·`assignment/dto/TaskBoardSearchCondition.java`·`assignment/repository/TaskBoardQueryRepository.java`·`common/util/SortAllowlist.java` 신규 + `TaskBoardController`·`TaskBoardService`·`VideoRepository` 수정 + 테스트 5종
  > **설계 이탈(DESIGN-STALE, 승인)**: 계획의 JPQL `@Query`+`countQuery` → **QueryDSL `TaskBoardQueryRepository`**. 사유 = 목록/count 두 문자열에 4종 조건을 이중 관리하면 조건 불일치(위험 7·HIGH-2)가 구조적으로 재발. QueryDSL 로 하나의 `BooleanBuilder` 를 목록·count 가 공유. 부수효과로 조인 0개(전부 상관 EXISTS) → 행 증식(HIGH-1) 원천 차단
  > 사이클: 시나리오(HIGH 6) → 구현(3974 GREEN) → QA 7종 → DEV_FIX 1차(MED 4 + LOW 2) → 빌드 단독 재검증
  > QA: code-reviewer PASS · api-designer PASS(하위호환 7항목 충족) · database-reviewer PASS · requirements-verifier **CONFORM** · security-reviewer CONDITIONAL→해소 · adversarial WEAK→해소 · build-validator **PASS**
  > 테스트: 3974 → **3982** (실패 0 / 스킵 5 / 회귀 0). HIGH 6건 전부 방어코드+실패테스트 보유
  > DEV_FIX 해소: 정렬항목 개수 상한·중복제거(CWE-770) · `workStatus=` blank 400 비일관 · `workStatusPredicate` 죽은분기 제거+fail-fast·미지 상태코드 SQL 경로 테스트 · 공허한 단언/누락 단언 정정 · `toLowerCase(Locale.ROOT)` · tie-break javadoc 정정
- [x] Phase 2: 작업목록 BE — KPI 집계 엔드포인트 + 이벤트유형 옵션 조회 ✅ PASS
  > 구현: `GET /v1/tasks/board/summary`(KPI 6필드) + `GET /v1/tasks/board/event-types` 신규. `assignment/dto/TaskBoardSummaryResponse.java`·`EventTypeOptionsResponse.java` 신규, `TaskBoardQueryRepository`(`countByWorkStatus`·`findDistinctEventTypes`·`buildWhere` 3-arg 오버로드)·`TaskBoardSearchCondition`(`statusOnly()`)·`TaskBoardService`·`TaskBoardController` 수정
  > **설계 이탈(승인)**: 계획의 "GROUP BY 1회" → **조건부 집계 단일 쿼리**(`COUNT(*)` + `SUM(CASE ...) × 5`). 사유 = 버킷 판정식이 상관 서브쿼리라 PostgreSQL 이 `GROUP BY` 표현식 내 외부 컬럼 참조를 거부(42803). 라운드트립 1회·`BoardWorkStatus` 파생이라는 본질 요구는 유지되고, 부수효과로 `COUNT(*)` vs 버킷합 대조가 가능해져 **버킷 겹침까지 fail-closed 검출**
  > **PM 결정 2건**: ①응답에 `completed` 포함 → `total = unassigned+inProgress+reviewPending+completed+rejected` 불변식 성립(카드는 5종만 표시) ②`event-types` 는 `status` 축만 반영(필터 걸었다고 옵션이 사라지면 되돌아갈 수 없음)
  > ★ **`inProgress` 필드 = `BoardWorkStatus.PENDING`**(배정됨·검수 미제출). 시스템에 `IN_PROGRESS` 워크플로 값은 **존재하지 않는다**(계획 위험 4 해소·문서화 완료)
  > QA: code-reviewer PASS · security-reviewer PASS · api-designer PASS · requirements-verifier **CONFORM** · database-reviewer CONDITIONAL(성능 후속) · adversarial WEAK → DEV_FIX 로 해소 · build-validator **PASS**
  > 테스트: 3982 → **4011** (실패 0 / 스킵 5 / 회귀 0)
  > DEV_FIX 해소: `statusOnly()` 죽은코드+공허테스트(단위테스트 신설로 실효화) · **UNASSIGNED 두 축 계약 테스트 고정** · 이벤트유형 절단 은폐 → `{items, truncated}` 응답 계약 변경 · 신규 엔드포인트 `status=` blank 400 → 기본값 폴백 · 불변식 테스트 0==0 정정 · 옵션값↔필터 trim 통일 · javadoc 드리프트 정정
- [x] Phase 3: 작업목록 FE — 서버 필터·정렬·KPI 연동, 클라이언트 필터 제거 ✅ PASS
  > 구현: 신규 `features/task/{boardParams,boardSort}.ts`·`components/{TaskBoardTable,TaskBoardKpiCards,TaskWorkerKpiCards}.tsx`·`hooks/{useTaskBoardSummary,useTaskBoardEventTypes}.ts`·`lib/compactParams.ts` + `TaskListPage`·`TaskFilters`·`KpiCard`·`types`·`api`·`queryKeys` 수정
  > 사이클: 시나리오(HIGH 11) → 구현 → QA 4종 → **DEV_FIX 1차(적대 BROKEN·요구 GAP)** → 재QA → **DEV_FIX 2차(신규 MED/LOW 4)** → 최종 빌드
  > QA 최종: code-reviewer PASS · fullstack-coordinator CONDITIONAL→해소 · requirements-verifier **CONFORM**(AC-6 GAP 닫힘) · adversarial **WEAK**(HIGH 3 전부 닫힘) · build-validator **PASS**
  > 테스트: FE 1559 → **1632** (tsc 0 error / 린트 신규 위반 0 / 회귀 0). `TaskListPage.tsx` 920 → 676줄(`TaskBoardTable` 추출)
  > **DEV_FIX 1차 — 적대검증이 잡은 HIGH 3건**: ①구 URL `?status=` 축 반전(구 URL 은 워크플로축이었는데 배치축으로 해석 → 겹치는 4값이 400 없이 의미만 뒤집혀 **파이프라인 미완료 영상까지 노출 + 배정 버튼**) → URL `status`=워크플로축 환원 + 배치축은 상수 고정(URL 로 조작 불가) ②R6/AC-6 헤더 정렬 미구현(`boardSort.ts` 가 죽은 코드, `?sort=` 는 URL 에서 조용히 삭제) → **촬영일시·영상ID 컬럼 신설 + 정렬 state + URL sort 왕복 + `aria-sort`** ③WORKER KPI 카드 무단 삭제(테스트가 퇴행을 봉인) → `TaskWorkerKpiCards` 로 구 동작 복원
  > **DEV_FIX 1차 MEDIUM 7건**: 필터변경 시 직전 page 로 요청 1회 더 · 미제출 검색어가 카드클릭으로 소실 · WORKER `IN_PROGRESS` URL 왕복 소실 · 목록 에러 + stale 행 위 배정 가능 · 2번째 초기화 no-op(동일 참조) · `safePage` vs 요청 `page` 불일치 · refetch 후 화면에 없는 선택 잔존
  > **DEV_FIX 2차**: WORKER "전체 N건"이 클라이언트 필터 결과와 어긋나는 회귀 · 초기화가 정렬을 안 되돌림 · 실패 배너 문구가 WORKER 에 부적절 · `?sort=regDt,asc` 복구 경로 주석
  > 테스트 허점 정정: 목이 BE 계약 위반(`summary.total`≠`totalElements`) · 기존 테스트가 신규 EP 미스텁(네트워크 passthrough) · `compactParams` 전용 테스트 신설 · 요청 **횟수** 단언 추가
- [x] Phase 4: 검수목록 BE — sort 수용 결함 수정 + 필터 서버화 + KPI 집계 ✅ PASS
  > 구현: `review/repository/ReviewQueryRepository.java`(QueryDSL)·`review/dto/{ReviewSearchCondition,ReviewSummaryResponse}.java` 신규 + `ReviewController`·`ReviewService`·`ReviewRepository`(구 `searchByStatus` 제거)·`SortAllowlist`(`REVIEW` 매핑) 수정
  > **R7 결함 본체 제거**: 컨트롤러가 `sort` 를 버리던 것 + JPQL 정적 `ORDER BY s.updDt DESC` 가 Pageable Sort 를 무력화하던 것 → 제출일 정렬이 실제로 동작
  > **★AC-7 개정(PM)**: 검수목록은 **lenient**(미등록 정렬 키 → 400 아닌 기본 정렬 폴백 + WARN). 근거 = 변경 전 이 엔드포인트는 `sort` 를 받지도 않아 **항상 200** 이었고, 400 을 내면 FE 가 보존·재전송하는 `sort` 로 북마크·뒤로가기가 죽는다(R8 Critical). 작업목록은 strict(400) 유지 — 거긴 변경 전 500 이었으므로 400 이 개선
  > QA: code-reviewer PASS · security-reviewer PASS · requirements-verifier **CONFORM** · database-reviewer CONDITIONAL(인덱스 커버리지 주장 정정) · adversarial **WEAK** → DEV_FIX 로 해소 · build-validator **PASS**
  > 테스트: 4011 → **4053** (실패 0 / 스킵 5). ⚠ 전체 실행 시 `AugmentRequestServiceTest` 1건이 HikariPool 커넥션 고갈로 간헐 실패 — **단독 재실행 18/18 GREEN 으로 flaky 확정**(assertion 실패 아님, 이번 변경 무관)
  > DEV_FIX 해소: **AC-8 하위호환 파손(미등록 sort → 400)** → lenient 폴백 · 표시↔검색 tie-break 회귀 가드 신설(변이 테스트로 강도 실증) · 화이트리스트 확장 시 KPI 조용한 과소집계 → fail-fast · 표시명 폴백 판정(`isBlank` vs `trim`) 통일 · 정렬 상한 이중 정의 파생화 · `SortAllowlist.REVIEW` 형태 가드 테스트
  > ★ **개발자가 지시를 거부한 판단이 옳았음**: V124 마이그레이션 **주석 정정**을 지시했으나, Flyway 체크섬은 주석까지 포함해 계산돼 이미 배포된 파일을 고치면 다음 기동이 `checksum mismatch` 로 실패한다(해소하려면 전 환경 `flyway repair`). 동일 내용을 `ReviewQueryRepository` javadoc 으로 갈음 — **PM 승인**
- [x] Phase 5: 검수목록 FE — 진입 기본값(FIFO) + 헤더 정렬 + 서버 필터 연동 ✅ PASS
  > 구현: `features/review/reviewListParams.ts`(필터↔URL↔서버 단일 매핑)·`components/{ReviewKpiCards,ReviewListFilters}.tsx`·`hooks/useReviewSummary.ts` 신규 + `ReviewListPage`(297줄 재작성)·`api`·`types`·`useReviewList`·`queryKeys` 수정
  > QA: code-reviewer PASS · fullstack-coordinator PASS · requirements-verifier **CONFORM**(AC-5·AC-6 닫힘) · adversarial **WEAK** → DEV_FIX 로 해소 · build-validator **PASS**
  > 테스트: FE 1632 → **1674** (tsc 0 error / 린트 신규 위반 0 / 회귀 0)
  > **구조적 위험 대응** — 이 화면은 BE 가 관대해 FE 실수를 알려주지 않는다(잘못된 `status` → 400 아닌 빈 결과 200, 잘못된 정렬 키 → 조용한 폴백). 그래서 검증을 **200 응답의 내용물·실제 전송 파라미터**로 했다: `Record<ReviewStatus, ReviewStatusParam>` 역매핑(키 누락 = 컴파일 에러) + axios params 실측 단언
  > DEV_FIX 해소: 초기화가 300ms 뒤 검색어를 되살림 · **상태 헤더 정렬이 기본 FIFO 를 조용히 파괴**(전 행이 같은 상태라 tie-break `RAW_DATA_ID DESC` 가 실질 정렬이 됨 → sortable 제거) · page 범위 초과 시 "총 9건"+"항목 없음" 자기모순 · 목록 에러와 0건 미구분 · 필터 전환 중 이전 행에 새 필터 배지 · 기존 테스트가 summary 미스텁이라 KPI 영구 에러 상태에서만 통과 · URL 정규화 스킵 · 카드 라벨 하드코딩 · `aria-label` 이 모든 행에서 "검수 시작"
- [x] Phase 6: 하위호환 회귀 검증 + 문서 동기화 ✅ PASS
  > 구현: `ListApiBackwardCompatibilityIT`(11건) 신설 — 두 목록 API 의 하위호환을 한 파일에서 회귀 가드. **프로덕션 코드 무변경**(테스트 1 + 문서 2만)
  > ★ 11번째 테스트가 **완전히 같은 정렬 키 문자열**(`nonExistentField,asc`)을 두 엔드포인트에 보내 **작업목록 400 / 검수목록 200** 을 한 테스트에서 대비 고정 — 나중에 "일관성"을 이유로 한쪽을 바꾸면 여기서 먼저 깨진다
  > 문서: `docs/v2-wiki/04-screens-ia.md`(두 화면 정렬·필터·KPI 동작, 기존 blockquote 톤 유지·전면 재작성 없음) + 프로젝트 `CLAUDE.md` 에 **"목록 화면 정렬·필터 정책(2026-07-29 확정, 구속)"** 신설
  > 테스트: 4053 → **4064** (실패 0 / 스킵 5 / 회귀 0). 실행 증거 — `cleanTest build` 18분10초, 505 클래스. flaky 였던 `AugmentRequestServiceTest` 는 이번 전체 실행에서 18/18 GREEN
  > 발견 결함 **없음**(10항목 전부 첫 실행에서 기대대로 동작, 자율 수정 0회)
  > 기록: `board?size=101` 은 400 이 아니라 `max-page-size=100` 으로 조용히 캡되고 `reviews?size=101` 은 400 — **둘 다 변경 전 동작 그대로**라 하위호환 이슈 아님(10번 테스트가 이 비대칭도 고정)

## 요구사항 요약

- **목적**: 작업목록의 상태 우선순위 정렬(`CASE`)이 검수 축을 배정 화면에 끌어와 미배정 건이 진행중 건보다 아래로 밀리는 문제를 해소한다. 동시에 두 화면의 필터·KPI가 현재 페이지 20건 기준 클라이언트 계산이라 숫자·결과가 틀리는 문제를 BE 이관으로 바로잡는다.
- **범위**
  - 포함: `GET /v1/tasks/board`(+신규 summary), `GET /v1/reviews`(+신규 summary), `TaskListPage.tsx`, `ReviewListPage.tsx`, `TaskFilters.tsx`, 관련 FE api/types/hooks
  - 제외(out of scope): WORKER 작업목록(`GET /v1/assignments`) 정렬 정책 변경, 영상 처리 현황 등 다른 목록 화면, 이전 계획의 Phase 10 잔여 9건
- **주요 액터**: REVIEWER(두 목록 모두), WORKER(작업목록의 본인 배정분 — 이번 변경 대상 아님)

### 요구 목록

| ID | 요구 | 출처 |
|----|------|------|
| **R1** | 작업목록 정렬에서 상태 우선순위(`CASE`) 제거 — 상태는 필터로만 표현. 시간축 단일 정렬로 단순화 | 사용자 확정(관행안) |
| **R2** | 작업목록 진입 기본값 = 전체 · 등록일 최신순 (**FE가 명시 파라미터로 전송**, BE 기본값은 현행 유지) | 사용자 선택 |
| **R3** | 작업목록 KPI 5카드(전체/미배정/작업중/검수요청/반려) + 카드 클릭 시 해당 필터 적용. 현재 4카드이며 '미배정' 신규 추가 | 사용자 선택 |
| **R4** | 검수목록 진입 기본값 = 검수요청(PENDING) · 제출일 오래된순(FIFO) + KPI 4카드 클릭 필터 (**FE가 명시 전송**, BE 기본값은 현행 '전체' 유지) | 사용자 선택 |
| **R5** | 두 목록의 필터(상태·검색어·이벤트유형) + KPI 집계를 **전부 BE로 이관**, FE 클라이언트 필터 제거 | 사용자 확정 |
| **R6** | 두 목록 모두 컬럼 헤더 정렬 제공 — BE는 정렬 키 **allowlist 검증** 후 수용(임의 필드 주입 차단) | 사용자 선택 |
| **R7** | 검수목록 `sort` 파라미터 무시 결함 수정 — `ReviewController.java:68` 이 sort를 버리고 `ReviewRepository` JPQL 정적 `ORDER BY` 가 Pageable Sort 를 무력화 | 조사 중 발견(R6 종속) |
| **R8** | **기존 방식 호출 하위호환 (Critical)** — 신규 파라미터 전부 optional / BE 기본값 변경 금지 / 작업목록 워크플로 상태는 기존 `status`(배치 상태 축) 재사용 금지, 별도 파라미터 신설 / 정렬 키 alias 매핑(`submittedAt`→`updDt`), 미지정 시 기존 기본 정렬 폴백 | 사용자 명시 |

### 수용 기준 (requirements-verifier 역추적 기준)

- [ ] **AC-1 (R1,R2)**: 작업목록 진입 시 등록일 최신순 단일 정렬로 표시되고, 상태 그룹핑이 나타나지 않는다
- [ ] **AC-2 (R3,R5)**: 작업목록·검수목록 KPI 숫자가 **전체 기준**이다(현재 페이지 집계 아님). 총건수 > size 인 데이터에서 KPI 합이 페이지 행 수와 무관하게 전체와 일치
- [ ] **AC-3 (R5)**: 필터 적용 후 2페이지로 넘겨도 필터가 유지된 결과가 반환된다(서버 필터)
- [ ] **AC-4 (R3)**: KPI 카드 클릭 시 해당 상태 필터가 적용되고 페이지가 0으로 리셋된다
- [ ] **AC-5 (R4)**: 검수목록 진입 시 검수요청(PENDING)만, 제출일 오래된 순으로 표시된다
- [ ] **AC-6 (R6,R7)**: 두 목록의 컬럼 헤더 정렬이 실제로 동작한다(서버 정렬, 오름/내림 토글 반영)
- [ ] **AC-7 (R6)**: allowlist 밖 정렬 키(`?sort=password,asc` 등)가 **쿼리에 반영되지 않는다**. ⚠ **응답 코드는 엔드포인트별로 다르다 (2026-07-29 PM 개정 — R8 우선 판단)**:
  - **작업목록(`/v1/tasks/board*`) = strict → 400.** 변경 전에도 `@PageableDefault` 로 `Pageable` 을 받아 잘못된 키면 `PropertyReferenceException`(500)이었으므로 400 은 하위호환 파손이 아니라 개선이다
  - **검수목록(`/v1/reviews*`) = lenient → 200 + 기본 정렬(`updDt DESC`) 폴백 + WARN 로그.** 변경 전 이 엔드포인트는 `sort` 를 **파라미터로 받지도 않고 버렸으므로 어떤 값이 와도 200** 이었다. 400 을 내면 FE 가 URL 에 보존·재전송하는 `sort` 때문에 **북마크·뒤로가기에서 목록 전체가 죽는다**(R8 Critical 위반). 폴백해도 미등록 키가 쿼리에 닿지 않으므로 **AC-7 의 보안 목적은 동일하게 달성**된다
  - 두 정책의 차이는 `SortAllowlist.resolve`(strict) / `resolveLenient`(lenient) 로 분리돼 있고, 서로의 정책을 교차 적용하지 않음을 회귀 테스트로 고정한다
- [ ] **AC-8 (R8)**: 기존 파라미터 조합(`/v1/tasks/board?status=COMPLETED&page=0&size=20`, `/v1/reviews?page=0&size=20`)으로 호출 시 **변경 전과 동일한 결과 집합**을 반환한다
- [ ] **AC-9 (R8)**: 작업목록 `status=UNASSIGNED` 특수값 동작이 변경 전과 동일하다

### 비기능 요구사항

- 정렬·필터 파라미터는 **파라미터 바인딩만** 사용. 정렬 키는 화이트리스트 매핑으로만 해석하며 사용자 입력 문자열을 JPQL/네이티브 쿼리에 직접 연결하지 않는다 (CWE-89)
- REVIEWER 전용 접근 제어 유지(`@PreAuthorize` + 서비스 `requireReviewer` 이중 가드). 필터 서버화로 노출 범위가 넓어지지 않을 것 (CWE-862/863)
- 검색어 입력 길이 제한(`@Size(max=100)`) + LIKE 와일드카드(`%`, `_`) 이스케이프
- KPI count 쿼리는 목록 쿼리와 동일 조건에서 인덱스를 타야 함. 영상 5,000건 + 증강/해상도 파생으로 대상 행이 수만 규모까지 증가 가능
- 기존 N+1 회피 batch lookup 구조(작업목록 7종·검수목록 5종)를 깨지 않을 것

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | **없음** (마이그레이션 없음). 단, 검색·정렬 컬럼 인덱스 필요 여부는 Phase 1·4 에서 `database-reviewer` 판정에 따름 |
| 기존 API | `GET /v1/tasks/board`·`GET /v1/reviews` — **optional 파라미터 추가만**(하위호환 유지). 신규: `GET /v1/tasks/board/summary`, `GET /v1/tasks/board/event-types`, `GET /v1/reviews/summary` |
| 도메인 모듈 | BE: `assignment`(controller/service/dto), `review`(controller/service/repository/dto), `video.repository`, `common`(정렬 allowlist 유틸 신규) / FE: `features/task`, `features/review`, `pages/TaskListPage`, `pages/ReviewListPage`, `components/common/KpiCard` |
| 설정/환경 | **없음** |
| 문서 | `docs/v2-wiki/04-screens-ia.md`(화면 동작), 프로젝트 `CLAUDE.md`(목록 정렬·필터 정책 구속 규칙 신설) |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | **R8 위반** — 구현 중 "기본값을 BE에서 바꾸는 게 깔끔하다"는 유혹으로 기본 정렬·기본 필터를 BE에서 변경 → 기존 호출의 결과 집합이 조용히 달라짐 | **HIGH** | Phase 1·4 에 **하위호환 회귀 테스트를 RED 로 먼저** 작성. Phase 6 에서 전 엔드포인트 재검증. 진입 기본값은 FE에서만 전송 |
| 2 | **정렬 키 주입** — `sort` 문자열을 그대로 `Sort.by()`/쿼리에 전달하면 임의 엔티티 필드 노출·오류 유발 | **HIGH** | 화이트리스트 Map(FE key → 엔티티 필드) 로만 해석. 매핑에 없으면 `ErrorCode.INVALID_INPUT` 400. 다중 정렬 키 전부 검증 |
| 3 | **작업목록 `status` 축 혼선** — 기존 `status`는 배치 상태(`LS_DATA_RAW.DATA_STTS_CD`) + 특수값 `UNASSIGNED` 인데, FE 필터 상태는 워크플로 축(`rowStatus`). 한 파라미터에 겹치면 기존 호출 의미가 깨짐 | **HIGH** | 워크플로 상태는 **신규 파라미터 `workStatus`** 로 분리. 기존 `status` 의 regex·의미·`UNASSIGNED` 분기 그대로 유지 |
| 4 | **KPI '작업중' 이 항상 0일 가능성** — `TaskBoardService.mapBoardStatus` 는 `IN_PROGRESS` 를 반환하지 않는다(ASSIGNED→`PENDING`). 현재 FE KPI 는 `rowStatus === 'IN_PROGRESS'` 를 세므로 REVIEWER 시각에서 0으로 고정됐을 수 있음 | **MED** | Phase 2 에서 **실데이터로 먼저 확인**하고 카드 정의를 매핑 실제값에 맞춰 확정. 임의로 매핑을 바꾸면 기존 뱃지 표시가 함께 변하므로 표시 로직과 분리해 결정 |
| 5 | **검색 대상이 조인 컬럼** — 영상명은 `MNG_RESOURCE_CCTV.CCTV_NM`(없으면 `VMS_CCTV_ID` 폴백), 작업자명은 `LS_TASK_ASSIGNMENT`(최신 LABELER)→`MNG_ACCT_USER.USER_NM`. 서버 검색 시 조인 + LIKE full scan 위험 | **MED** | 목록·count 쿼리를 동일 조인으로 구성하고 `database-reviewer` 로 실행계획 판정. **MNG_* 는 공유 스키마라 인덱스 추가도 관제팀 협의 대상** — 임의 추가 금지 |
| 6 | **검수목록 status 코드 축 불일치** — FE `STATUS_OPTIONS` 는 `REVIEW_PENDING/REVIEWING/COMPLETED/REJECTED`, BE 는 `PENDING/IN_REVIEW/APPROVED/REJECTED`. `ReviewResponse` 에 BE→FE 매핑은 있으나 역방향 없음 | **MED** | FE→BE 역매핑을 **FE api 계층**에서 수행해 BE 는 기존 BE 코드만 수용(R8 보존). BE 가 FE 코드도 받는 이중 수용은 하지 않는다 |
| 7 | **count 쿼리 누락으로 페이징 깨짐** — 목록 쿼리에 조인을 추가하면 기존 `countQuery` 와 조건이 어긋나 `totalPages` 가 틀어짐 | **MED** | 모든 `@Query` 에 조건이 일치하는 `countQuery` 명시. 페이징 정합 테스트(총건수·마지막 페이지) 포함 |
| 8 | 파생영상(증강·해상도)이 작업목록 대상에 포함되어 행 수가 배로 늘어남 | **LOW** | 기존 동작 유지(파생도 배정·검수 대상). 성능은 위험 5·7 대응으로 흡수 |
| 9 | `.claude-plan.md` 는 `.gitignore` 에 있어도 **git 추적 중** — 이번 교체로 워킹트리가 더러워짐 | **LOW** | 정보 공유용. 커밋 시 의도된 변경으로 포함 |
| 10 | **★ Phase 1 단독 배포 시 화면 기능 퇴행 (Phase 1 적대검증 발견)** — 상태 우선순위 정렬을 제거했는데 FE 는 아직 클라이언트 필터·KPI(현재 페이지 20건 기준)를 쓴다. 변경 전에는 CASE 정렬이 반려·검수대기를 page 0 상단에 올려줘 그나마 값이 나왔지만, 등록일 최신순이 되면 오래된 반려 건이 20행 안에 안 들어와 **"반려" 필터 결과 0행 · KPI 반려 카드 0** 이 된다 | **HIGH** | **Phase 1·2·3 을 동일 릴리스로 묶는다.** Phase 1 만 단독 머지·배포 금지. 커밋은 나눠도 되지만 배포 단위는 작업목록 BE+FE 를 함께 낸다 |

---

## 상세 계획

### Phase 1: 작업목록 BE — 정렬 정책 전환 + 필터 파라미터 신설 + 정렬 allowlist

**목표**: `GET /v1/tasks/board` 가 상태 우선순위 정렬을 버리고 시간축 단일 정렬로 동작하며, 워크플로 상태·검색어·이벤트유형·작업자 필터를 서버에서 처리한다. 기존 파라미터만 보낸 호출은 변경 전과 동일한 결과 집합을 낸다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/src/main/java/kr/co/cudo/authoring/common/util/SortAllowlist.java` | 생성 | FE 정렬 key → 엔티티 필드 매핑 검증 유틸. 미등록 키는 `CustomException(INVALID_INPUT)` |
| `backend/.../video/repository/VideoRepository.java` | 수정 | `findBoardOrderByStatusPriority` 의 `CASE` 정렬 제거 → 필터 조건을 받는 `searchBoard(...)` 로 대체. `countQuery` 동시 정의 |
| `backend/.../assignment/dto/TaskBoardSearchCondition.java` | 생성 | `record`(status, workStatus, q, eventTypeCd, workerId) |
| `backend/.../assignment/service/TaskBoardService.java` | 수정 | `listBoard` 에 검색 조건 전달. batch lookup 7종 구조 유지 |
| `backend/.../assignment/controller/TaskBoardController.java` | 수정 | optional 파라미터 4종 추가 + `sort` allowlist 적용 |
| `backend/src/test/.../assignment/TaskBoardControllerTest.java` | 수정/생성 | 하위호환·필터·정렬 테스트 |

**핵심 로직 / 시그니처**:
```java
// TaskBoardController — 기존 파라미터는 시그니처·기본값 그대로 두고 optional 만 추가
@GetMapping("/board")
@PreAuthorize("hasRole('REVIEWER')")
public ApiResponse<Page<TaskBoardItemResponse>> board(
    @RequestParam(defaultValue = "COMPLETED")
    @Pattern(regexp = "^(COMPLETED|UNASSIGNED|ASSIGNED|PENDING|IN_REVIEW|APPROVED|REJECTED)$") String status, // 기존 축(배치 상태) — 의미 불변
    @RequestParam(required = false)
    @Pattern(regexp = "^(UNASSIGNED|PENDING|REVIEW_PENDING|REJECTED|COMPLETED)$") String workStatus,          // 신규: 워크플로 축
    @RequestParam(required = false) @Size(max = 100) String q,           // 신규: 영상명·작업자명 부분일치
    @RequestParam(required = false) @Size(max = 20) String eventTypeCd,  // 신규
    @RequestParam(required = false) Long workerId,                        // 신규
    @PageableDefault(size = 20, sort = "regDt", direction = Sort.Direction.DESC) Pageable pageable, // 기본값 불변(R8)
    @AuthenticationPrincipal TokenClaims actor);

// SortAllowlist — 정렬 키 화이트리스트 (CWE-89 방어)
public final class SortAllowlist {
    public static final Map<String, String> TASK_BOARD = Map.of(
        "regDt", "regDt", "capturedAt", "shtDt", "shtDt", "shtDt", "rawSn", "rawSn", "videoId", "rawSn");
    public static final Map<String, String> REVIEW = Map.of(
        "submittedAt", "updDt", "updDt", "updDt", "videoId", "rawDataId", "status", "dataSttsCd");
    /** 미등록 key 는 CustomException(INVALID_INPUT) → 400. 정렬 미지정이면 fallback 반환. */
    public static Sort resolve(Sort raw, Map<String, String> allowlist, Sort fallback);
}
```

**정렬 정책 (R1·R8 동시 충족)**:
- `CASE` 우선순위 제거 → 1차 정렬은 **Pageable Sort**(기본 `regDt DESC`), tie-break 로 `rawSn DESC` 고정
- 기존 호출(`?status=COMPLETED&page=0&size=20`)은 `@PageableDefault` 가 그대로 `regDt DESC` 를 주므로 **결과 집합·건수는 동일하고 순서만 시간축으로 단순화**된다

> ⚠ **개발자 주의**: AC-8 의 "동일"은 **결과 집합(어떤 행이 몇 건 나오는가)** 기준이다. 정렬 순서는 R1 에 의해 의도적으로 바뀐다. 필터·페이징·특수값(`UNASSIGNED`) 동작은 완전히 동일해야 한다.

**테스트 케이스** (RED 단계에 그대로 사용):
- `기존_파라미터만_보내면_변경전과_동일한_결과집합_반환`
- `status_UNASSIGNED_는_배치상태_무관_미배정만_반환`
- `상태_우선순위_정렬이_적용되지_않고_등록일_최신순으로_반환`
- `workStatus_필터가_워크플로_상태로만_거른다`
- `workStatus_와_기존_status_는_서로_독립적으로_동작`
- `검색어가_영상명과_작업자명_양쪽에_부분일치`
- `검색어_LIKE_와일드카드_문자는_이스케이프되어_리터럴로_취급`
- `allowlist_밖_정렬키_요청시_400`
- `정렬키_미지정시_등록일_내림차순_폴백`
- `필터_적용시_totalElements_가_필터_결과_기준으로_계산`
- `REVIEWER_아닌_사용자_요청시_403`

**수용 기준**:
- [x] AC-8·AC-9(하위호환) 테스트 GREEN
- [x] AC-1 정렬 단순화 반영
- [x] AC-7 allowlist 400
- [x] N+1 batch lookup 7종 유지(쿼리 수 회귀 없음 — 정적 검토 기준, 자동 쿼리수 계측은 없음)

**의존성**: 없음

**Phase 1 완료 후 이월된 후속 항목** (차단 아님, 별도 처리):
- AC-8 **차등(golden) 테스트 부재** — 구 구현(`findBoardOrderByStatusPriority`/`findUnassigned`)이 삭제돼 두 구현 결과를 같은 픽스처로 비교할 수 없다. **Phase 6 실호출 회귀로 갈음**
- `LS_DATA_RAW.REG_DT`/`SHT_DT` **인덱스 부재** — 주 정렬축인데 인덱스가 없다(`IX_LS_DATA_RAW_CCTV`/`IX_LS_DATA_RAW_STTS` 만 존재). `LS_*` 는 저작도구 자체 소유라 관제 협의 불필요 → **별도 마이그레이션으로 후속 처리**
- `common/web/SortFieldMapper` 에 동일한 정렬 개수 무제한 갭 잔존(전사 공통, 이번 범위 밖)
- `GlobalExceptionHandler` 가 `propertyPath`(메서드명+파라미터명) 노출 — 전역 기존 패턴, 별도 과제
- deep offset(`page` 상한) 미제한 · 목록 조회 감사 로깅 부재 — 전사 공통 정책 사항
- `?status=%20`(공백)은 여전히 400 — `status` regex 불변 제약(R8)에 따라 미변경

---

### Phase 2: 작업목록 BE — KPI 집계 엔드포인트 + 이벤트유형 옵션 조회

**목표**: KPI 5종 카운트와 이벤트유형 옵션을 전체 기준으로 BE가 계산해 제공한다. FE가 페이지 20건으로 집계하던 것을 대체한다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/.../assignment/dto/TaskBoardSummaryResponse.java` | 생성 | `record(long total, long unassigned, long inProgress, long reviewPending, long rejected)` |
| `backend/.../assignment/controller/TaskBoardController.java` | 수정 | `GET /v1/tasks/board/summary`, `GET /v1/tasks/board/event-types` 추가 |
| `backend/.../assignment/service/TaskBoardService.java` | 수정 | `summary(...)`, `eventTypes(...)` 추가 |
| `backend/.../video/repository/VideoRepository.java` | 수정 | 워크플로 상태별 GROUP BY count + distinct 이벤트유형 쿼리 |

**핵심 로직**:
```java
// 상태별 카운트는 5회 count 가 아니라 GROUP BY 1회로 집계한다 (성능)
SELECT CASE WHEN NOT EXISTS (SELECT 1 FROM LsTaskAssignment a
                              WHERE a.rawDataId = v.rawSn AND a.taskTypeCd = 'LABELER') THEN 'UNASSIGNED'
            WHEN s.dataSttsCd = 'REJECTED'              THEN 'REJECTED'
            WHEN s.dataSttsCd IN ('PENDING','IN_REVIEW') THEN 'REVIEW_PENDING'
            WHEN s.dataSttsCd = 'APPROVED'              THEN 'COMPLETED'
            ELSE 'PENDING' END AS workStatus,
       COUNT(v)
  FROM LsDataRaw v LEFT JOIN LsRawDataStatus s ON s.rawDataId = v.rawSn
 WHERE v.dataSttsCd = :dataSttsCd   /* + q / eventTypeCd / workerId 조건 동일 적용 */
 GROUP BY 1
```

> ⚠ **위험 4 확인 필수**: 이 `CASE` 는 `TaskBoardService.mapBoardStatus` 와 **완전히 동일한 분기**여야 한다(중복 정의가 드리프트 원인). Phase 1 의 `workStatus` 필터 분기와 상수/헬퍼로 단일화하고, `mapBoardStatus` 와의 일치를 검증하는 테스트를 둔다.
> `mapBoardStatus` 는 `IN_PROGRESS` 를 반환하지 않으므로 KPI '작업중' 카드는 **`PENDING`(배정됨·검수 미제출)** 으로 정의한다. FE 라벨(`작업중`)과의 대응은 Phase 3 에서 맞춘다.

**테스트 케이스**:
- `summary_카운트가_페이지크기와_무관하게_전체기준으로_집계`
- `summary_의_상태분류가_목록의_status_매핑과_1대1로_일치`
- `summary_에_검색어_이벤트유형_필터가_동일하게_적용`
- `이벤트유형_옵션이_전체_대상에서_중복없이_반환`
- `REVIEWER_아닌_사용자_summary_요청시_403`

**수용 기준**:
- [x] AC-2(전체 기준 집계) 테스트 GREEN
- [x] 상태 분류가 목록 매핑과 일치함을 테스트로 고정(`BoardWorkStatus` 단일 원천 + 5종 전수 대조)
- [x] `database-reviewer` CONDITIONAL PASS — 정확성 결함 없음, 성능은 후속 과제

**의존성**: Phase 1 (필터 조건 재사용)

**★ Phase 3 인계 사항 (FE 연동 시 반드시 반영)**:
- **`GET /v1/tasks/board/event-types` 응답은 배열이 아니라 `{items: string[], truncated: boolean}` 객체다** (절단 은폐를 막기 위한 계약)
- **KPI 미배정 카드 클릭 시 `workStatus=UNASSIGNED` 를 보내라 — `status=UNASSIGNED` 가 아니다.** 두 축은 서로 다른 집합을 뜻한다(전자=배치 상태 필터 안의 미배정, 후자=배치 상태 무관 미배정 전체). 혼동하면 **카드 3 ↔ 목록 13** 같은 불일치가 화면에 그대로 노출된다
- KPI '작업중' 카드는 `workStatus=PENDING` 이다(`inProgress` 필드에 대응). `IN_PROGRESS` 라는 값을 보내면 400
- 신규 2개 엔드포인트는 `status=`(빈 문자열)를 기본값으로 폴백하지만, 기존 `GET /v1/tasks/board` 는 **여전히 400** 이다(R8 제약). FE 는 빈 값을 아예 안 보내는 쪽으로 통일할 것
- `event-types` 는 페이징이 없고 상한 500 이다. 호출 빈도 억제는 React Query `staleTime` 으로

**Phase 2 완료 후 이월된 후속 항목** (차단 아님):
- **행당 상관 서브쿼리 9회** — `LS_RAW_DATA_STATUS` 는 PK 1:1 이라 LEFT JOIN 해도 행 증식이 없으므로 상태 조건은 EXISTS 대신 조인 컬럼 비교로 바꿀 수 있고, 배정 테이블(1:N)만 EXISTS 가 필요하다. Phase 1 `search()` 구조까지 함께 바꿔야 해 **별도 과제**로 분리. `EXPLAIN ANALYZE` 실측 후 결정
- `LS_DATA_RAW(DATA_STTS_CD, EVNT_TYPE_CD)` 복합 인덱스 검토 — `LS_*` 자체 소유라 관제 협의 불필요
- `MAX_EVENT_TYPE_OPTIONS`(500)를 `LS_SYSTEM_CONFIG` 로 이동(운영 튜닝 필요 시)
- `/v1/tasks/board/summary` ↔ `/v1/tasks/{rawSn}/summary`(WORKER 허용) URI 인접 — 현재 라우팅은 안전하나 매핑 리팩터링 시 관찰 필요

---

### Phase 3: 작업목록 FE — 서버 필터·정렬·KPI 연동, 클라이언트 필터 제거

**목표**: 작업목록이 진입 시 전체·등록일 최신순으로 뜨고, KPI 5카드가 전체 기준 숫자를 보여주며 클릭하면 필터가 걸린다. 클라이언트 필터·집계 코드가 사라진다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/features/task/types.ts` | 수정 | `TaskBoardParams` 에 `workStatus`/`q`/`eventTypeCd`/`workerId` 추가, `TaskBoardSummary` 신규 |
| `frontend/src/features/task/api.ts` | 수정 | `getTaskBoardSummary`, `getTaskBoardEventTypes` 추가 |
| `frontend/src/features/task/hooks/useTaskBoardSummary.ts` | 생성 | React Query 훅 |
| `frontend/src/lib/queryKeys.ts` | 수정 | summary/event-types 키 추가 |
| `frontend/src/pages/TaskListPage.tsx` | 수정 | 클라이언트 필터(`:313-338`)·KPI(`:341-350`) 제거, 서버 파라미터 연동, KPI 5카드 + 클릭 필터 |
| `frontend/src/components/common/KpiCard.tsx` | 수정 | `onClick`/`selected` optional prop 추가(미전달 시 기존과 동일 렌더) |
| `frontend/src/features/task/components/TaskFilters.tsx` | 수정 | 상태 옵션에 '미배정' 추가, 이벤트유형 옵션을 서버 조회 결과로 |

**핵심 로직**:
```ts
// 진입 기본값은 FE 가 명시 전송 (R2·R8)
const boardParams = useMemo(() => ({
  status: 'COMPLETED',                     // 기존 축 유지
  workStatus: filters.status || undefined, // 신규 축
  q: filters.q || undefined,
  eventTypeCd: filters.eventType || undefined,
  workerId: filters.assigneeId || undefined,
  page, size: PAGE_SIZE,
  sort: sortParam ?? 'regDt,desc',         // 등록일 최신순 명시
}), [filters, page, sortParam]);
```

**KPI 카드 정의** (Phase 2 확정값과 일치):
| 카드 | workStatus 필터값 | 비고 |
|------|------------------|------|
| 전체 | (없음) | 필터 해제 |
| 미배정 | `UNASSIGNED` | **신규 카드** |
| 작업중 | `PENDING` | 배정됨·검수 미제출 (위험 4 반영) |
| 검수요청 | `REVIEW_PENDING` | |
| 반려 | `REJECTED` | |

**테스트 케이스**:
- `진입시_등록일_최신순_파라미터로_요청한다`
- `KPI_카드_클릭시_해당_workStatus_필터가_적용되고_페이지가_0으로_리셋`
- `필터_변경시_서버_요청_파라미터에_반영된다`
- `클라이언트에서_행을_다시_거르지_않는다`
- `헤더_정렬_클릭시_sort_파라미터가_서버로_전달된다`
- `WORKER_시각에서는_board_요청을_보내지_않는다`

**수용 기준**:
- [ ] AC-1·AC-3·AC-4 충족
- [ ] `TaskListPage.tsx` 에서 클라이언트 필터·KPI 계산 코드 제거 확인
- [ ] 기존 `TaskListPage.test.tsx` 회귀 0

**의존성**: Phase 1, 2

---

### Phase 4: 검수목록 BE — sort 수용 결함 수정 + 필터 서버화 + KPI 집계

**목표**: `GET /v1/reviews` 가 `sort` 를 실제로 반영하고, 검색어 필터와 KPI 집계를 서버에서 처리한다. 파라미터 없는 기존 호출은 변경 전과 동일하게 전체·`updDt DESC` 를 반환한다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/.../review/controller/ReviewController.java` | 수정 | `sort`·`q` optional 수용, allowlist 적용해 `PageRequest.of(page, size, sort)` 조립, `GET /v1/reviews/summary` 추가 |
| `backend/.../review/repository/ReviewRepository.java` | 수정 | JPQL 정적 `ORDER BY s.updDt DESC` 제거, 검색 조건 추가, `countQuery` 정의, 상태별 GROUP BY count |
| `backend/.../review/service/ReviewService.java` | 수정 | `list` 에 검색 조건 전달, `summary` 추가 |
| `backend/.../review/dto/ReviewSummaryResponse.java` | 생성 | `record(long pending, long inReview, long approved, long rejected)` |

**핵심 로직**:
```java
// 정렬 미지정 시 기존 동작 폴백 (R8)
Sort resolved = SortAllowlist.resolve(
        requestedSort,                                   // FE key: submittedAt / videoId / status
        SortAllowlist.REVIEW,
        Sort.by(Sort.Direction.DESC, "updDt"));          // 미지정 시 변경 전과 동일
Pageable pageable = PageRequest.of(page, size, resolved);
```

- 화이트리스트(`REVIEW_STATUS_WHITELIST`) 상시 적용은 **그대로 유지** — 검수 대상 아닌 배치 상태 누출을 계속 차단
- `status` 파라미터는 **BE 코드(`PENDING`/`IN_REVIEW`/`APPROVED`/`REJECTED`)만 수용**(R8). FE 코드 역매핑은 FE api 계층 책임(위험 6)

**테스트 케이스**:
- `sort_미지정시_변경전과_동일한_updDt_내림차순_반환`
- `sort_submittedAt_asc_요청시_제출일_오름차순으로_반환`
- `allowlist_밖_정렬키_요청시_400`
- `status_파라미터로_BE코드_지정시_해당_상태만_반환`
- `화이트리스트_밖_상태값_지정시_빈결과_반환`
- `검색어가_영상명과_작업자명에_부분일치`
- `summary_카운트가_전체기준으로_집계`
- `필터_적용시_totalElements_가_필터_결과_기준`

**수용 기준**:
- [ ] AC-6·AC-7·AC-8 충족
- [ ] 정적 `ORDER BY` 제거 후에도 기본 정렬 보존
- [ ] 검수 상태 화이트리스트 누출 방지 테스트 GREEN

**의존성**: Phase 1 (`SortAllowlist` 유틸) — 유틸만 의존하므로 Phase 2·3 과 **병렬 진행 가능**

---

### Phase 5: 검수목록 FE — 진입 기본값(FIFO) + 헤더 정렬 + 서버 필터 연동

**목표**: 검수목록이 진입 시 검수요청만·제출일 오래된 순으로 뜨고, KPI 4카드 클릭으로 필터가 걸리며, 헤더 정렬이 실제로 동작한다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/features/review/types.ts` | 수정 | `ReviewListParams` 에 `status`/`q` 추가, `ReviewSummary` 신규 |
| `frontend/src/features/review/api.ts` | 수정 | `listReviews` 에 FE→BE 상태 코드 역매핑 적용, `getReviewSummary` 추가 |
| `frontend/src/features/review/hooks/useReviewSummary.ts` | 생성 | React Query 훅 |
| `frontend/src/pages/ReviewListPage.tsx` | 수정 | 진입 기본값, 클라이언트 필터(`:86-98`) 제거, KPI 카드 클릭 필터, sortable 컬럼 확대 |

**핵심 로직**:
```ts
// 진입 기본값은 FE 가 명시 전송 (R4·R8) — BE 기본값은 '전체·updDt DESC' 유지
const params = {
  page, size,
  status: searchParams.get('status') ?? 'REVIEW_PENDING', // FE 코드 → api 계층에서 BE 'PENDING' 으로 역매핑
  sort:   searchParams.get('sort')   ?? 'submittedAt,asc', // 제출일 오래된 순(FIFO)
  q:      searchParams.get('q') || undefined,
};
```

**테스트 케이스**:
- `진입시_검수요청_상태와_제출일_오름차순으로_요청한다`
- `FE_상태코드가_BE_코드로_역매핑되어_전송된다`
- `KPI_카드_클릭시_해당_상태_필터가_적용되고_페이지가_0으로_리셋`
- `제출일_헤더_정렬_클릭시_방향이_토글되어_서버로_전달된다`
- `클라이언트에서_행을_다시_거르지_않는다`

**수용 기준**:
- [ ] AC-5·AC-6 충족
- [ ] `ReviewListPage.tsx` 클라이언트 필터 제거 확인
- [ ] 제출일 정렬 헤더가 실제로 목록 순서를 바꿈(기존 결함 해소)

**의존성**: Phase 4

---

### Phase 6: 하위호환 회귀 검증 + 문서 동기화

**목표**: R8 이 전 엔드포인트에서 실제로 지켜졌음을 실호출로 확인하고, 변경된 화면 동작·정책을 문서에 반영한다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/src/test/.../ListApiBackwardCompatibilityIT.java` | 생성 | 기존 파라미터 조합 실호출 회귀 IT |
| `docs/v2-wiki/04-screens-ia.md` | 수정 | 두 목록의 정렬·필터·KPI 동작 갱신 |
| `CLAUDE.md` | 수정 | **목록 정렬·필터 정책** 구속 규칙 신설(아래) |

**CLAUDE.md 추가 규칙 (구속)**:
> **목록 화면 정렬·필터 정책**: 목록의 정렬은 **시간축 단일 기준**으로 두고 상태 우선순위를 `ORDER BY CASE` 로 섞지 않는다. "지금 처리할 것"은 **필터·KPI 카드**로 표현한다. 필터·집계는 **BE에서 전체 기준으로** 처리하며 FE 클라이언트 필터로 대체하지 않는다(페이지 단위 집계는 오답). 목록 API에 필터·정렬 파라미터를 추가할 때는 **기존 호출 하위호환**(신규 파라미터 optional·BE 기본값 불변·축이 다른 필터는 별도 파라미터)을 지키고, 정렬 키는 **allowlist 매핑으로만** 해석한다.

**검증 항목** (실호출):
- [ ] `GET /v1/tasks/board?status=COMPLETED&page=0&size=20` — 변경 전 결과 집합·건수와 동일
- [ ] `GET /v1/tasks/board?status=UNASSIGNED&page=0&size=20` — 동일
- [ ] `GET /v1/tasks/board` (파라미터 없음) — 기본 `status=COMPLETED` 동작 동일
- [ ] `GET /v1/reviews?page=0&size=20` — 전체·`updDt DESC`(변경 전과 동일)
- [ ] `GET /v1/reviews?status=PENDING&page=0&size=20` — 동일
- [ ] `GET /v1/tasks/board?sort=nonExistentField,asc` — **400**(strict)
- [ ] `GET /v1/reviews?sort=labelPayload,asc` — **200 + 기본 정렬(`updDt DESC`) 폴백**(lenient). ⚠ 구 체크리스트는 400 이었으나 **AC-7 개정으로 정정**됨 — 이 엔드포인트는 변경 전 `sort` 를 버려 항상 200 이었고, 400 을 내면 FE 가 보존·재전송하는 `sort` 로 북마크·뒤로가기가 죽는다
- [ ] `GET /v1/reviews?sort=submittedAt,asc` — 200 + 제출일 오름차순(정렬이 실제로 적용됨)

**수용 기준**:
- [ ] AC-8·AC-9 실호출 PASS
- [ ] 문서 3종 갱신 완료
- [ ] 전체 테스트 GREEN(BE + FE), 회귀 0

**의존성**: Phase 1~5

---

## 실행 가이드

1. `/cc-tdd Phase 1: 작업목록 BE 정렬 정책 전환 + 필터 파라미터 신설 + 정렬 allowlist`
2. `/cc-tdd Phase 2: 작업목록 BE KPI 집계 엔드포인트 + 이벤트유형 옵션`
3. `/cc-tdd Phase 3: 작업목록 FE 서버 필터·정렬·KPI 연동`
4. `/cc-tdd Phase 4: 검수목록 BE sort 수용 결함 수정 + 필터 서버화 + KPI 집계`
5. `/cc-tdd Phase 5: 검수목록 FE 진입 기본값 + 헤더 정렬 + 서버 필터 연동`
6. `/cc-tdd Phase 6: 하위호환 회귀 검증 + 문서 동기화`

> Phase 4 는 `SortAllowlist` 유틸(Phase 1 산출)에만 의존하므로 Phase 2·3 과 병렬 진행 가능. 기본은 순차 진행.
