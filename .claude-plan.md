# 구현 계획: 백엔드 설정 프로퍼티 정리 + dev 프로파일 정상화

> 생성: 2026-07-27 (조사·요구확정·계획 승인 완료 — 구현 미착수)
> 상태: **전체 완료 (Phase 1~5, 미커밋)** — 2026-07-28
> 유형: REFACTOR(설정) + 환경 정상화 (BE 설정 위주 + 소량 Java, 규모 L)
> 작업 위치: **property 워크트리**(브랜치 `property`) — main 워크트리에는 변경을 만들지 않는다
> 조사 정본: `docs/operations/config-property-audit-20260727.md` (A/B/C/D/E 섹션 — 모든 근거·파일:라인 기재)
> 승인 요약: `docs/operations/config-property-plan-summary.md`
>
> **재개 방법**: 이 워크트리에서 `/cc-resume` → Phase 1부터 진행. 조사·요구 확정·계획 승인이 모두 끝났으므로 **재조사·재계획 불필요**.

## Phase 진행 현황

- [x] Phase 1: 동작 결함 4건 수정 (A1~A4) ✅ (A3 실측=중첩 미바인딩 확정→prd 20/5 발효, 신규 3테스트클래스)
- [x] Phase 2: 미사용 키 제거 + 환경 드리프트 정리 (B, C) ✅ (미사용 5키 제거, multipart 공통승격+prd 21MB/1100MB 명시 override, dev IP 평문 제거)
- [x] Phase 3: 암묵 기본값 명시 + portal 바인딩 통합 (D1, D2) ✅ (14키 yml 명시·드리프트 0, portal.upload @Value 0건. sweep.* 2키는 @Scheduled 제약으로 placeholder 유지)
- [x] Phase 4: dev 프로파일 전환 + mock-server 실연동 배선 (R6/E1~E3) ✅ (base=dev/local override=local, mock-server base 승격, jdbc-url 고정으로 관제 실DB 오접속 차단, WEBHOOK_CALLBACK_BASE_URL 신규 발견·주입)
- [x] Phase 5: 보안 리뷰 HIGH 3건 조치 ✅ (계획 외 추가 — mock-server 루프백+콜백 allowlist+경로경계 / DevProfileGuard 오배포 차단 / DeidentifyEndpointTrustGuard deny-known-mock prd 차단 / 문서 IP 마스킹. 재검증 PASS, 운영 기동 회귀 없음)

> 최종: 백엔드 **3056 GREEN** · mock-server pytest **108 passed** · 미커밋(수정 36 + 신규 14)
> **AC6/AC7 실기동 스모크는 미수행** — 사용자 수행 절차가 `docs/operations/config-key-changes-20260727.md` §4-2 에 있음

## 요구사항 요약

- 목적: 환경별로 갈라지고 사문화된 백엔드 설정을 정리해 ①설정이 실제로 먹는 상태로 되돌리고 ②dev(cudo_246)가 local 프로파일을 우회 사용하던 왜곡을 바로잡아 **내부 self-fill 없이 mock-server를 실제 HTTP로 바라보게** 한다.
- 범위(포함): `backend/src/main/resources/application{,-local,-dev,-stg,-prd}.yml`, `backend/src/test/resources/application-local.yml`(필요 시), 설정을 읽는 소량 Java(`BrampVideoProbe`, `PortalUploadProperties` 및 사용처), `docker-compose.yml`, `backend/.env.example`, 관련 문서
- 범위(제외): 증강(augment) 실서버 HTTP 클라이언트 신규 구현 / C2 prd `${VAR:?}` 강제 확대 / C5 test 프로파일 분리 / D3·D4 prefix 재편 / 시크릿 값 자체의 환경변수화 / ai-server·frontend 설정
- 주요 액터: 개발자(local), 운영자(cudo_246=dev 배포), 배치 파이프라인(비식별·VLM)

### 요구 목록

- **R1**: 대상은 백엔드 `application*.yml` 전체. dev 배포 정상화를 위해 `docker-compose.yml` 수정 허용(도커는 local/dev 전용, stg/prd 미사용이라 운영 오염 위험 없음) | 출처: 사용자 명시 + Q&A
- **R2 (A. 동작 결함)**:
  - **A1**: `application-local.yml`의 `kpst.augment.external.mode` → `authoring.augment.external.mode` 이동. 코드는 `authoring.*`를 읽어 local 증강 시뮬레이터가 무동작이었음 | 출처: 조사 실측
  - **A2**: ffprobe 키 일원화 — `BrampVideoProbe`가 읽는 미정의 키 `authoring.ffprobe.binary` → `authoring.ffmpeg.ffprobe-binary` | 출처: 조사 실측
  - **A3**: prd의 `spring.datasource.{control,portal}.hikari.*` 중첩 — **실측 검증 후** 바인딩되지 않으면 중첩 제거 | 출처: 조사 정황
  - **A4**: stg가 kpst 블록 부재로 공통 https 기본값을 상속 → `KpstWebClientConfig` fail-closed 기동 차단 조건. stg에 명시 배선 | 출처: 조사 실측
- **R3 (B. 미사용 키 6건 제거)**: `authoring.jwt.algorithm` / `authoring.batch.rate-per-min` / `authoring.batch.frame-interval-sec` / `authoring.integration.deidentify.timeout-ms` / `authoring.integration.ai-server.timeout-ms` / `kpst.augment.*`(A1로 이동). **키별 grep 재확인 후 제거** | 출처: 조사 실측
- **R4 (C. 드리프트 정리)**: C1 multipart 3중 복붙 공통 승격(**prd 값 보존**) · C3 dev의 내부 IP/벤더 IP 기본값 제거 · C4 `connection-init-sql` 의도 주석 명시 · C6 dev/stg의 `authoring.dev.login|upload.enabled` 하드코딩 `true` → `${DEV_LOGIN_ENABLED:true}` 형태로 override 가능화 · C7 stg 로깅 레벨 정합 | 출처: 조사 + 사용자 선택
- **R5 (D. 구조)**: D1 코드만 읽고 yml에 없는 암묵 기본값 12+개를 yml에 명시 · D2 `portal.upload.*` 바인딩 이원화(record 7필드 + 흩어진 `@Value` 6+개)를 `PortalUploadProperties` record로 통합 | 출처: 조사 + 사용자 선택
- **R6 (dev 환경 정상화)**:
  - `docker-compose.yml`의 `SPRING_PROFILES_ACTIVE: local` → `dev`. **`application-dev.yml`이 한 번도 로드된 적이 없으므로** dev가 요구하는 환경변수를 빠짐없이 주입해야 기동함
  - `docker-compose.yml`(base)에 `mock-server` 서비스 추가(`klid-mock-server:9400`, `klid-net`, storage 공유, `MOCK_OUTPUT_BASE=/app/storage/deidentified`)
  - **구속 원칙: 내부 self-fill 금지 — 모든 외부 연동은 HTTP로 실서버(dev=mock-server)를 바라본다**
  - **E1**: dev `deidentify.mock-mode` → `false` (내부 원본복사 self-fill 제거)
  - **E2**: dev `kpst.deid.base-url` → `http://klid-mock-server:9400`, `enabled=true`
  - **E3**: dev `vlm.client.enabled` → `true` + `vlm.client.url` → mock-server (현재 false라 VLM 단계가 SKIPPED로 통째로 빔)
  - **E6·E7 유지**: ai-server는 실추론(`AI_MOCK_MODE=false`), BE의 mock 응답 방어장치 4곳은 안전장치로 존치
  | 출처: 사용자 명시 + Q&A

### 수용 기준

- [ ] **AC1**: A1 수정 후 `authoring.augment.external.mode=dev`로 `DevAugmentCallbackSimulator` 빈이 실제 로드됨(빈 존재 검증)
- [ ] **AC2**: A2 수정 후 `FFPROBE_BIN` 주입이 `BrampVideoProbe` 포함 **모든** ffprobe 사용처에 반영됨(미정의 키 grep 0건)
- [ ] **AC3**: A3 실측으로 HikariCP 풀 설정의 정답 형식이 확정되고, prd가 의도한 20/5로 바인딩됨을 테스트로 검증
- [ ] **AC4**: 제거한 미사용 키 6건이 main+test 어디에서도 참조되지 않음(grep 0건) + 전체 테스트 GREEN
- [ ] **AC5**: multipart 공통 승격 후에도 **prd 한도가 상향되지 않음**(21MB/1100MB 유지 — OWASP API4 자원소진 방어)
- [ ] **AC6**: dev 프로파일 기동 성공 + 비식별/VLM이 mock-server로 **실제 HTTP 요청**을 보냄(내부 self-fill 경로 미진입 — 로그 확인)
- [ ] **AC7**: local 기동(`-f docker-compose.yml -f docker-compose.local.yml`) 정상 + 백엔드 전체 테스트 GREEN(회귀 0)
- [ ] **AC8**: 변경·신규·제거된 설정 키와 배포 환경변수 매핑이 산출물로 남음(`docs/operations/`) + `.env.example` 갱신

### 비기능 요구사항

- 보안: prd 노출 표면 불변(springdoc·actuator·multipart 한도), dev 토글이 prd로 새지 않음, 시크릿 평문 신규 유입 0
- 회귀: 기존 백엔드 테스트 전량 GREEN, local 자족 기동 유지
- 운영: 리포지토리 변경만으로 끝나지 않음 — **서버 `.env` 갱신 항목을 명시적으로 산출**

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | **없음** |
| 기존 API | **없음** (시그니처 변경 0) |
| 도메인 모듈 | `video.service.port.BrampVideoProbe`(키 1개), `portal.config.PortalUploadProperties` + 확장 필드 사용처(`portal.service.*`, `portal.config.*`) |
| 설정/환경 | `application.yml` + 4개 프로파일 + test yml, `docker-compose.yml`, `backend/.env.example` |
| 배포 | cudo_246 프로파일 `local`→`dev` 전환, `mock-server` 컨테이너 신규 기동, 서버 `.env` 키 추가 |
| 문서 | `docs/v2-wiki/00-local-setup.md` 등 설정 언급 문서, `docs/operations/` 산출물 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | dev 전환 시 필수 환경변수 누락 → 기동은 되나 기능이 죽음. `STREAM_SIGN_SECRET` 미설정=영상 스트리밍 전면 fail-closed, `CORS_ALLOWED_ORIGINS` 빈값=FE 전면 403, `ADMIN_CLAIM_PASSWORD_HASH` 빈값=관리자 클레임 불가 (local yml에만 기본값이 있고 dev엔 없음) | **HIGH** | Phase 4에서 dev 필수 env 체크리스트를 만들고 compose `environment`에 명시 주입 + 기동 후 스모크로 실제 동작 확인. 루트 `.env`엔 이미 4키 존재하나 **서버 `.env`는 별개**이므로 산출물로 전달 |
| 2 | multipart 공통 승격이 prd 한도를 의도치 않게 상향(21MB→500MB) | **HIGH** | 공통 승격 시 prd에 명시 override를 **같은 커밋에서** 추가. AC5로 게이트 |
| 3 | B 미사용 키 제거가 사실은 사용 중(relaxed binding·test `@TestPropertySource`·문서화된 운영 손잡이) | MED | 키별 grep(main+test+docs) 재확인 후 제거, 전체 테스트 GREEN 확인 |
| 4 | A3 hikari 중첩 제거가 오히려 회귀(실제로 바인딩되고 있었다면) | MED | **실측 선행** — 바인딩 테스트로 어느 형식이 먹는지 확정한 뒤에만 수정 |
| 5 | D2 record 통합 시 기존 `@Value` 기본값과 record `@DefaultValue` 불일치로 조용한 값 변경 | MED | 기존 기본값을 1:1 표로 옮기고 바인딩 테스트로 값 고정 |
| 6 | dev 전환으로 `DevSeedRunner`(`@Profile("local")`) 미실행 → 신규 환경 초기데이터 부재 | MED | 현행 cudo_246 DB엔 이미 적재됨을 전제. Phase 4에서 영향 명시하고, 필요 시 시드 경로를 산출물에 기록(코드 변경은 범위 외) |
| 7 | base compose에 mock-server 추가 시 local override와 **중복 정의**되어 설정이 갈라짐 / 포트 9400 충돌 | MED | base에 단일 정의하고 local override에서는 중복 제거(또는 차이분만 유지). 포트 점유는 기동 전 확인 |
| 8 | dev에서 KPST 실호출 경로가 처음 켜짐(E1 mock-mode=false) → 폴링 잡·타임아웃이 실제로 돌기 시작 | MED | mock-server가 즉시 응답하므로 정상 시나리오는 안전. 실패 시 `deIdntfYn='F'` + 원본 보존 원칙이 이미 있음. 기동 후 1건 파이프라인 스모크로 확인 |

---

## 상세 계획

### Phase 1: 동작 결함 4건 수정 (A1~A4)

**목표**: 설정이 "쓰여 있는데 안 먹는" 4곳을 실제로 먹게 만든다. 끝나면 증강 시뮬레이터가 로드되고, ffprobe 경로 주입이 전 사용처에 반영되며, prd 풀 설정 형식이 확정되고, stg가 기동 차단 조건에서 벗어난다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/src/main/resources/application-local.yml` | 수정 | A1 — `kpst.augment.external.mode` 블록을 `authoring.augment.external.mode`로 이동(값 `${AUGMENT_EXTERNAL_MODE:dev}` 유지). 오배치 교정 주석 갱신 |
| `backend/src/main/java/kr/co/cudo/authoring/video/service/port/BrampVideoProbe.java` | 수정 | A2 — `@Value("${authoring.ffprobe.binary:ffprobe}")` → `@Value("${authoring.ffmpeg.ffprobe-binary:ffprobe}")` |
| `backend/src/main/resources/application-prd.yml` | 수정 | A3 — 실측 결과에 따라 `hikari:` 중첩 제거(`maximum-pool-size`/`minimum-idle`를 `spring.datasource.{control,portal}` 직하로) |
| `backend/src/main/resources/application-stg.yml` | 수정 | A4 — `kpst.deid` 블록 신설(`base-url`을 환경변수 필수 형태로, ca-cert 조건 주석). mock-mode 의도 명시 |
| `backend/src/main/resources/application.yml` | 수정 | A4 부수 — `kpst.deid.enabled` 기본값(true)과 `KpstWebClientConfig` javadoc "기본 false" 주석 드리프트 정정 |
| `backend/src/test/java/.../config/DataSourcePoolBindingIT.java` | 생성 | A3 실측 — 어느 형식이 HikariDataSource에 바인딩되는지 검증 |
| `backend/src/test/java/.../config/PropertyKeyConsistencyTest.java` | 생성 | A1·A2 회귀 방지 — yml 키 집합과 코드 참조 키를 대조 |

**핵심 로직 / 시그니처**:
```java
// BrampVideoProbe (A2) — 키만 교체, 기본값·동작 불변
public BrampVideoProbe(@Value("${authoring.ffmpeg.ffprobe-binary:ffprobe}") String binary)

// DataSourcePoolBindingIT (A3) — 정답 형식 실측
// spring.datasource.control.maximum-pool-size=7 (중첩 없음) 주입 →
//   ((HikariDataSource) controlDataSource).getMaximumPoolSize() == 7  이어야 함
// spring.datasource.control.hikari.maximum-pool-size=7 (중첩) 주입 →
//   바인딩되지 않음(기본 10 유지)을 확인 → 이 결과로 prd 수정 여부 확정
```

```yaml
# application-local.yml (A1) — 이동 전/후
# BEFORE                          # AFTER
kpst:                             authoring:
  augment:                          augment:
    external:                         external:
      mode: ${AUGMENT_EXTERNAL_MODE:dev}   mode: ${AUGMENT_EXTERNAL_MODE:dev}
```

**테스트 케이스** (cc-tdd RED 단계에 그대로 사용):
- `hikari_중첩없는_형식이_실제_풀크기에_바인딩된다`
- `hikari_중첩_형식은_바인딩되지_않는다`  ← A3 판정 근거
- `local_프로파일에서_증강_시뮬레이터_빈이_로드된다`  ← A1 (`authoring.augment.external.mode=dev`)
- `ffprobe_경로_설정이_모든_사용처에_동일_키로_주입된다`  ← A2 (미정의 키 참조 0건)
- `코드가_참조하는_설정키가_yml에_정의되어_있다`  ← 회귀 방지

**수용 기준**:
- [ ] AC1 — `DevAugmentCallbackSimulator` 빈 로드 확인
- [ ] AC2 — `authoring.ffprobe.binary` 참조 grep 0건
- [ ] AC3 — 풀 설정 정답 형식 확정 + prd가 20/5로 바인딩됨을 테스트로 증명
- [ ] stg 프로파일이 KPST fail-closed 기동 차단 조건에서 벗어남(설정상 근거 명시)
- [ ] 백엔드 전체 테스트 GREEN

**의존성**: 없음

---

### Phase 2: 미사용 키 제거 + 환경 드리프트 정리 (B, C)

**목표**: 사문화된 키를 걷어내고 환경 간 복붙·기본값 불일치를 정리한다. 끝나면 yml에 남은 키는 전부 "실제로 읽히는 키"가 되고, multipart·로깅·dev토글이 환경별로 일관된 규칙을 갖는다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `application.yml` | 수정 | B: `jwt.algorithm`, `batch.rate-per-min`, `batch.frame-interval-sec`, `integration.deidentify.timeout-ms`, `integration.ai-server.timeout-ms` 제거 / C1: multipart 공통값 재정의 / C4: `connection-init-sql`이 control 전용인 의도 주석 |
| `application-prd.yml` | 수정 | C1: multipart 한도를 prd에서 **명시 override**(21MB/1100MB 유지 — 위험#2 방어) |
| `application-local.yml` | 수정 | C1: 공통 승격으로 중복 제거 |
| `application-dev.yml` | 수정 | C1 중복 제거 / C3: `${CONTROL_DB_HOST:192.168.102.101}`·`${CONTROL_DB_NAME:klid_system}`·`kpst.deid.base-url` 벤더 IP 기본값 제거 / C6: dev 토글을 `${DEV_LOGIN_ENABLED:true}` 형태로 |
| `application-stg.yml` | 수정 | C1 중복 제거 / C6 동일 / C7: `kr.co.cudo.authoring` 로깅 레벨 명시 |
| `docs/operations/config-key-changes-20260727.md` | 생성 | 제거·이동·신규 키와 배포 환경변수 매핑 산출물(AC8) |

**제거 대상 키 재확인 절차**(제거 전 필수):
```
키별로 main + test + docs 전 범위 grep → 0건 확인 → 제거
authoring.jwt.algorithm / authoring.batch.rate-per-min / authoring.batch.frame-interval-sec
authoring.integration.deidentify.timeout-ms / authoring.integration.ai-server.timeout-ms
kpst.augment.*  (Phase 1에서 이동 완료된 잔여)
```

**multipart 승격 규칙(C1)**:
| 프로파일 | 현행 | 변경 후 |
|---|---|---|
| 공통 | 21MB / 1100MB | **500MB / 1200MB** (local·dev·stg 공통 필요값) |
| local·dev·stg | 각자 500MB/1200MB 복붙 | 블록 제거(공통 상속) |
| prd | 공통 상속(21MB/1100MB) | **명시 override 21MB / 1100MB** ← 상향 방지 |

**테스트 케이스**:
- `제거된_설정키는_코드에서_참조되지_않는다`
- `prd_프로파일_multipart_한도는_21MB_1100MB로_유지된다`  ← AC5 게이트
- `local_dev_stg_프로파일_multipart_한도는_500MB_1200MB이다`
- `dev_프로파일에_내부IP_기본값이_남아있지_않는다`
- `dev토글은_환경변수로_override_가능하다`

**수용 기준**:
- [ ] AC4 — 제거 키 6건 grep 0건 + 전체 테스트 GREEN
- [ ] AC5 — prd multipart 한도 불변
- [ ] dev yml에 내부 IP/벤더 IP 리터럴 0건
- [ ] `docs/operations/config-key-changes-20260727.md` 생성

**의존성**: Phase 1 (A1 이동 완료 후 `kpst.augment.*` 잔여 제거)

---

### Phase 3: 암묵 기본값 명시 + portal 바인딩 통합 (D1, D2)

**목표**: 코드에만 존재하던 운영 손잡이를 설정 파일에서 발견 가능하게 만들고, `portal.upload.*`의 이원화된 바인딩을 record 하나로 합친다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `application.yml` | 수정 | D1 — 아래 12+개 키를 **현재 코드 기본값 그대로** 명시 + 용도 주석 |
| `backend/src/main/java/kr/co/cudo/authoring/portal/config/PortalUploadProperties.java` | 수정 | D2 — 흩어진 `@Value` 키를 record 필드로 흡수 |
| `.../portal/**` 사용처 | 수정 | D2 — `@Value` 주입을 `PortalUploadProperties` 참조로 교체 |
| `backend/src/test/java/.../portal/config/PortalUploadPropertiesTest.java` | 생성/수정 | 신규 필드 바인딩 + 기본값 고정 검증 |

**D1 명시 대상**(값은 현재 코드 기본값을 그대로 옮긴다 — 동작 변경 0):
```
authoring.control-notify.retry.initial-delay-ms / .interval-ms
authoring.meta-replication.enabled / .interval-sec / .batch-size / .max-retry
authoring.resolution.resize-max-concurrent / .resize-acquire-timeout-sec
authoring.sam2.max-image-bytes
authoring.upload.tus.cleanup.interval-ms / .initial-delay-ms
authoring.webhook.idempotency.in-memory
```

**D2 통합 대상**:
| 키 | 현행 | 변경 후 |
|---|---|---|
| `portal.upload.max-chunk-bytes` | `@Value` | record 필드 |
| `portal.upload.max-label-body-bytes` | `@Value` | record 필드 |
| `portal.upload.probe-timeout-sec` | `@Value` | record 필드 |
| `portal.upload.stuck-timeout-minutes` | `@Value` | record 필드 |
| `portal.upload.sweep.interval-ms` / `.initial-delay-ms` | `@Value` | 중첩 record 또는 평면 필드 |

> **기본값 보존 원칙**: 각 `@Value`의 현재 기본값을 `@DefaultValue`로 1:1 이관한다(위험#5). 값이 바뀌면 조용한 동작 변경이므로 테스트로 고정한다.

**테스트 케이스**:
- `portal_업로드_확장필드가_기본값으로_바인딩된다`
- `portal_업로드_확장필드를_yml로_override_할_수_있다`
- `암묵_기본값을_yml에_명시해도_기존_동작이_바뀌지_않는다`
- `sweep_주기_설정이_record로_주입된다`

**수용 기준**:
- [ ] D1 목록 전 키가 yml에 존재하고 값이 기존 코드 기본값과 동일
- [ ] `portal.upload.*`를 읽는 `@Value`가 0건(전부 record 경유)
- [ ] 백엔드 전체 테스트 GREEN(값 드리프트 0)

**의존성**: Phase 2 (yml 정리 후 진행해야 충돌 없음)

---

### Phase 4: dev 프로파일 전환 + mock-server 실연동 배선 (R6/E1~E3)

**목표**: cudo_246이 `dev` 프로파일로 뜨고, 비식별·VLM이 **내부 self-fill 없이** mock-server로 실제 HTTP 요청을 보낸다. 끝나면 `application-dev.yml`이 처음으로 실제 적용된다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `docker-compose.yml` | 수정 | `SPRING_PROFILES_ACTIVE: dev` / `mock-server` 서비스 추가 / dev 필수 env 주입 |
| `docker-compose.local.yml` | 수정 | base와 중복된 `mock-server` 정의 정리(차이분만 유지 — 위험#7) |
| `application-dev.yml` | 수정 | E1 `deidentify.mock-mode: ${DEIDENTIFY_MOCK_MODE:false}` / E2 `kpst.deid.base-url` mock-server 기본 / E3 `vlm.client.enabled: true` + `url` |
| `backend/.env.example` | 수정 | dev 필수 키 + 신규 키(`VLM_SERVICE_URL`, `KPST_DEID_BASE_URL`, `DEIDENTIFY_MOCK_MODE` 등) 반영 |
| `docs/operations/config-key-changes-20260727.md` | 수정 | **서버 `.env` 갱신 항목**을 배포 체크리스트로 추가(AC8) |
| `docs/v2-wiki/00-local-setup.md` 등 | 수정 | 프로파일·목업 기동 절차 동기화 |

**dev 필수 환경변수 체크리스트**(위험#1 — 누락 시 기능이 조용히 죽음):
| 키 | local엔 기본값 있음 | dev엔? | 미주입 시 증상 |
|---|:---:|:---:|---|
| `JWT_SECRET` | ✅ | ❌ | **부팅 실패**(공통에 기본값 없음) |
| `STREAM_SIGN_SECRET` | ✅ | ❌ | 영상 스트림 서명 전면 fail-closed(재생 불가) |
| `CORS_ALLOWED_ORIGINS` | ✅ | ❌ | FE 전면 403 |
| `ADMIN_CLAIM_PASSWORD_HASH` | ✅ | ❌ | 관리자 role-claim 불가 |
| `WEBHOOK_HMAC_SECRET_AUGMENT` | ✅ | ❌ | 증강 콜백 401 |
| `CONTROL_DB_*` / `PORTAL_DB_*` | ✅(local은 하드코딩) | compose가 주입 중 | 부팅 실패 |
| `VLM_SERVICE_URL` | — | **신규 필요** | 기본 `localhost:9400`=컨테이너 자기 자신 → VLM 호출 실패 |
| `KPST_DEID_BASE_URL` | — | **신규 필요** | 공통 `https://localhost:9201` → fail-closed |

> 루트 `.env`엔 `JWT_SECRET`·`STREAM_SIGN_SECRET`·`CORS_ALLOWED_ORIGINS`·`ADMIN_CLAIM_PASSWORD_HASH`·`WEBHOOK_HMAC_SECRET_AUGMENT`가 이미 존재하나, **cudo_246 서버의 `.env`는 별개 파일**이다. 산출물로 전달한다.

**mock-server 서비스 정의**(base로 승격 — `docker-compose.local.yml:76` 기준):
```yaml
  mock-server:
    image: klid-mock-server:latest        # 소스: mock-server/ (docker build -t klid-mock-server:latest mock-server/)
    container_name: klid-mock-server
    restart: unless-stopped
    environment:
      MOCK_OUTPUT_BASE: /app/storage/deidentified   # 미설정 시 파일 미생성(fail-closed)
    volumes:
      - <backend와 동일한 storage 마운트>            # BE가 넘긴 절대경로를 그대로 읽고 씀
    ports: ["9400:9400"]
    networks: [klid-net]
```
> ⚠ 로컬/dev 전용 — 무인증 + 콜백 SSRF 표면. stg/prd는 도커 미사용이라 전파되지 않음.

**self-fill 제거 확인 포인트**(E1~E3):
- `DeidentifyStep.runMock()` 경로 **미진입** (mock-mode=false)
- `KpstDeidentService.submit` → mock-server `POST /project` 실제 호출
- `VlmClient`가 `SKIPPED` 반환하지 않고 mock-server `POST /v1/videovlm/describe` 호출
- ai-server는 실추론 유지(`AI_MOCK_MODE=false`), BE mock 방어장치 4곳 존치

**테스트 케이스**:
- `dev_프로파일에서_비식별_mock모드가_기본_비활성이다`
- `dev_프로파일_kpst_base_url이_mock서버를_가리킨다`
- `dev_프로파일에서_VLM_클라이언트가_활성이다`
- `dev_프로파일_필수_환경변수_목록이_문서와_일치한다`
- (스모크) `dev 프로파일 컨테이너 기동 → /actuator/health/liveness UP`
- (스모크) `영상 1건 파이프라인 → mock-server 접근 로그에 KPST·VLM 요청이 남음`

**수용 기준**:
- [ ] AC6 — dev 기동 성공 + 비식별·VLM이 mock-server로 실제 HTTP 호출(로그 확인, self-fill 경로 미진입)
- [ ] AC7 — local 기동(두 파일 `-f` 동시 지정) 정상 + 전체 테스트 GREEN
- [ ] AC8 — 변경 키 매핑 + 서버 `.env` 갱신 체크리스트 산출
- [ ] 문서 동기화 완료

**의존성**: Phase 1~3 (dev yml 최종 상태 확정 후 배선)

---

## 실행 가이드

1. `/cc-tdd Phase 1: 동작 결함 4건 수정 (A1 augment 키 이동 · A2 ffprobe 키 통일 · A3 hikari 바인딩 실측 후 수정 · A4 stg kpst 배선)`
2. `/cc-tdd Phase 2: 미사용 키 6건 제거 + 드리프트 정리 (C1 multipart 공통 승격+prd override · C3 IP 기본값 제거 · C4 주석 · C6 dev토글 override · C7 로깅)`
3. `/cc-tdd Phase 3: 암묵 기본값 12+개 yml 명시 + PortalUploadProperties record 통합`
4. `/cc-tdd Phase 4: dev 프로파일 전환 + mock-server base compose 승격 + E1~E3 실연동 배선`
