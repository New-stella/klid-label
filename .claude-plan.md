# 구현 계획: 라벨링 갭 후속 백로그 (Phase 6~14)

> 생성: 2026-07-15 | 상태: 진행 중 | 브랜치: feat/labeling-gaps
> 선행: Phase 1~5(라벨링 갭 5기능) 완료·커밋(c4a3ded~42a8ed4). 전체 회귀 2,026/2,027 GREEN.

## Phase 진행 현황

- [x] Phase 6: BE 소규모 2종 — 예외 cause 분기(R2) + 작업락 만료 sweeper(R4) ✅ PASS (QA 5종+DEV_FIX 1회. adversarial BROKEN→sweep 정체성 오회수(rawSn→PK 스코프 원자 UPDATE)→ROBUST. 2041 GREEN)
- [x] Phase 7: 트랙 머지 재보간 범위 축소(R3) — 전체 트랙 → toTrackId만 ✅ PASS (QA 5종 무DEV_FIX. adversarial ROBUST·골든좌표 동등·stale from+to 고아0. 2050 GREEN)
- [x] Phase 8: F/Q 폴리곤 키보드 배선(R5) — OverlayLayer imperative handle ✅ PASS (QA 2종 무DEV_FIX. code 9.5/10 회귀0, tsc+vitest 1059 GREEN. 브라우저 실동작은 /cc-verify로 미룸)
- [x] Phase 9: 포털 도구 노출(R1-a) — 키포인트·SAM2 세그·SAM2 추적 포털 개방 ✅ PASS (★설계정정: 내부개방 폐기→포털 전용 non-persist 엔드포인트. QA 7종+DEV_FIX 1회. design DRIFT(키포인트 round-trip 절반)+보안 MED2(Log Forging·자원소진)→CLOSED. adversarial ROBUST 4대불변식. BE 2068·FE 1060 GREEN)
- [x] Phase 10: 포털 트랙 rename 오배선 버그 수정(R1-b 축소) — 포털엔 트랙 개념 부재라 rename 버튼 숨김 ✅ PASS (★사용자결정 축소. QA 2종 무DEV_FIX. 이중안전 버튼숨김+핸들러가드, BE무변경, FE 1063 GREEN)
- [x] Phase 11: 증강 프레임 비동기 재추출 + 상태전이 재설계(R6) ✅ PASS (QA 5종+DEV_FIX 1회. 동기 tx=부모잠금/게이트/멱등 유지·async=extractByFrameNumbers+videoFrameNo 라벨매핑+성공시만 finalize. database 🔴 CRITICAL(LS_DATA_META 레이스)→video.* 제외+upsert→CONFIRMED. adversarial ROBUST 4대불변식. AugmentDeidentConcurrencyIT 무변경. 2073 GREEN)
- [x] Phase 12: ai-server RTDETR 탐지 백엔드 제거(F1) — YOLOX 일원화 ✅ PASS (QA 2종+DEV_FIX 2회(문서·온프렘 동기화). pytest 84/84, RT-DETR 완전제거(코드+테스트+docs+온프렘 잔존0), YOLOX 실추론(mock아님), torchaudio ABI 소멸, torch/SAM2 유지, transformers 제거+huggingface-hub 승격)
- [x] Phase 13: (★재정의) mock/source를 FE 공개 API 계약에서만 제거 + 내부 mock 기능·안전장치 보존 ✅ PASS (원안 "ai-server mock모드 제거+503+CI" 폐기 — 사용자 확정 2026-07-15. ai-server 무변경, BE FE-facing DTO(Sam2SegmentResponse·AutolabelResponse) mock 제거·내부 resp.mock() 가드 유지·SAM2 빈폴리곤+message 재배선, FE .mock 참조0·message/빈폴리곤 기반 차단·오토라벨 message 대칭. QA 13a: build GREEN·adversarial ROBUST(안전장치 반증실패). 13b: code 9.5/10·req MET·fullstack PASS·vitest1074+gradle295 GREEN. memory: phase13-f2-mock-removal-decisions)
- [x] Phase 14: SAM2 Track 청크 코드리뷰 경미 3건 폴리싱(F3) ✅ PASS (QA 2종 무DEV_FIX. completedChunks c+1→c 실패구간 정확화·onError invalidate partial>0 가드·도달불가 주석. FE 1067 GREEN)

> **선행 완료(2026-07-15, 커밋 3802b43)**: SAM2 자동추적 "추적 실패" 버그 = FE nextSrcSns 무제한 vs BE @Size(max=50) 400. FE 50개 청크 순차호출로 수정·브라우저 검증 완료. 아래 F1~F3은 그 과정에서 발견된 후속. memory: sam2-track-nextsrcsns-contract-bug.

## 요구사항 요약

- 목적: 라벨링 갭 5-Phase 완료 후 식별된 후속 백로그(A/B/C/D-1) 구현. 사용자 결정 반영.
- 범위: 아래 R1~R6. 제외(out of scope): D-2 사후 재비식별 자식 cascade(보류, 논의 후). **F2/Phase 13은 재정의 후 완료(2026-07-15) — 아래 F2 항목 참조**.
- 주요 액터: WORKER/REVIEWER(내부), PORTAL_USER(포털), 배치 시스템.
- 요구 목록:
  - **R1 (A-1) 포털 게이팅 재정의** | 출처: 사용자 명시(2026-07-15)
    - R1-a: 포털에 키포인트(K)·SAM2 클릭/박스 세그(G)·SAM2 자동추적(T) 노출. YOLO 수동트리거만 포털 제외. SAM2 추론 엔드포인트 포털 채널 허용.
    - R1-b: 트랙 머지/rename 포털 전용 엔드포인트 신설(LS_PORTAL_USER_LABEL 대상). 내부 전용 경로는 유지.
    - > ADR-013 부분 override(구속). memory: labeling-gap-design-decisions.
  - **R2 (A-2) DataIntegrityViolation cause 분기** | 출처: 사용자 명시. 락 unique 충돌만 409, FK/NOT NULL 등은 구분 매핑.
  - **R3 (B-1) 머지 재보간 범위 축소** | 출처: 사용자 명시. 전체 트랙 → toTrackId만 재보간(락 유지시간 단축).
  - **R4 (B-2) 작업락 만료 sweeper** | 출처: 사용자 명시. EXPRY_DT 경과 LOCKED 락 자동 회수 스케줄.
  - **R5 (C) F/Q 폴리곤 키보드 배선** | 출처: 사용자 명시. no-op 해소(OverlayLayer state 노출).
  - **R6 (D-1) 증강 프레임 새 추출** | 출처: 사용자 명시. 부모 프레임 복사 → 증강 영상 FFmpeg 비동기 재추출. 라벨 좌표만 복사.
  - **F1 RTDETR 탐지 백엔드 제거** | 출처: 사용자 명시(2026-07-15). ai-server 에서 RT-DETRv2 완전 제거. 현재 `libtorchaudio.so: undefined symbol`(torch↔torchaudio ABI 붕괴)로 load 실패→mock 폴백 중. 탐지는 YOLOX(onnxruntime, permissive)로 일원화.
  - **F2 (★재정의 2026-07-15) mock/source를 FE 공개 API 계약에서만 제거** | 출처: 사용자 명시. 원안(ai-server `AI_MOCK_MODE`·`_should_mock`/`_mock_*` 전면 제거 + 503 전환 + Gitea CI 모델다운로드)은 **폐기**. 최종: ai-server·내부 client DTO(YoloResponse/Sam2Response) **무변경**(mock 판정 내부 신호 유지), FE-facing DTO(Sam2SegmentResponse·AutolabelResponse)에서 mock 필드만 제거. 안전장치 보존 — BE 내부 `resp.mock()`로 저장 스킵 유지 + SAM2 mock 시 빈폴리곤+`ApiResponse.message`("AI 모델 미로드") 재배선, FE는 빈폴리곤+message로 자동적용 차단·경고(오토라벨 message 대칭). source(라벨출처)·VLM·503·CI 전부 제외.
  - **F3 SAM2 Track 청크 코드리뷰 폴리싱** | 출처: 코드리뷰(2026-07-15, 커밋 3802b43 대상). ① `completedChunks` 실패구간 라벨 표기 일관성(도달불가 분기) ② 완전실패 시 불필요한 `LABEL_KEYS` invalidate skip ③ 도달불가 방어분기 주석 명시.
  - **(참고 — 코드 무관) 검은 프레임**: rawSn 11 앞부분 프레임(0~5)은 **원본 영상 자체가 검은 이미지**(데이터 특성). 코드 버그 아님 → 조치 없음. 검은 프레임에서 SAM2 실행 시 empty_mask 폴백은 정상 동작.
- 수용 기준: 각 Phase 하단 참조.
- 비기능: 기존 5-Phase 회귀 0. R6는 TOCTOU(CWE-359)·멱등 회귀 금지. 신규 물리명 표준용어 필수.

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | 없음(EXPRY_DT·LS_PORTAL_USER_LABEL 기존). R4 신규 인덱스만 검토. R6 신규 상태코드 없이 기존 PENDING/MARKING_READY/FAILED 재사용 |
| 기존 API | R1-b 포털 신규 엔드포인트(/v1/portal/**), R1-a SecurityConfig 채널 매처 조정. R2 예외 응답코드 정교화 |
| 도메인 모듈 | label(머지/보간/예외), auth(락), video/webhook/batch(증강), frontend(포털 게이팅·폴리곤) |
| 설정/환경 | R4 sweeper `@ConditionalOnProperty` 토글 프로퍼티 추가 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | R6 상태전이 비동기화 시 프레임 0건 MARKING_READY(반쪽 증강본 노출) | HIGH | MARKING_READY를 추출 성공 후로 이동, 실패 시 markBatchFailed. 동기 tx엔 PENDING/중간상태만 |
| 2 | R6 비동기화가 부모 비식별 TOCTOU(CWE-359) 재개방 | HIGH | 부모 findByRawSnForUpdate 잠금+게이트+멱등앵커는 동기 handle tx에 그대로 유지(비동기로 이동 금지) |
| 3 | R1-b 포털 머지가 LS_DATA_LBL 아닌 포털 테이블 대상 — 로직 이원화 | HIGH | 포털 전용 서비스/엔드포인트 분리, 내부 머지 로직 재사용 가능 부분만 공유 |
| 4 | R5 OverlayLayer 리팩터가 폴리곤/BBOX/SAM2/키포인트 마우스 흐름 회귀 | HIGH | imperative handle(최소 침습), 클릭/더블클릭 로직 함수 추출로 공유, 회귀 테스트 |
| 5 | R3 trackId 스코프 재보간이 idempotency 삭제/후보 조회(raw 단위)와 불일치 | MED | trackId 필터 오버로드 신설, 고아 프레임 0 테스트 |
| 6 | R1-a SAM2 포털 개방이 ai-server 자원/외부채널 보안 노출 | MED | 포털 채널 SAM2 추론만 허용(persist는 포털 저장경로), 인가·SSRF 유지 |

---

## 상세 계획

### Phase 6: BE 소규모 2종 — 예외 cause 분기(R2) + 작업락 만료 sweeper(R4)

**목표**: DB 무결성 위반을 원인별로 정확히 매핑(락 충돌만 409), 만료된 작업 락을 주기적으로 자동 회수.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `common/exception/GlobalExceptionHandler.java` | 수정 | `DataIntegrityViolationException` 핸들러(:102~108)에 cause 분기 추가 |
| `auth/repository/LsAuthWorkLockRepository.java` | 수정 | `findAllByLockSttsCdAndExpireDtBefore(String, LocalDateTime)` 추가 |
| `auth/service/WorkLockService.java` | 수정 | `sweepExpiredLocks()` 메서드 — 만료 LOCKED 락 release(REQUIRES_NEW) |
| `auth/scheduler/WorkLockSweepJob.java` | 생성 | `@Scheduled(fixedDelayString)` 만료 락 sweeper(TusUploadCleanupJob 패턴) |
| `auth/scheduler/WorkLockSweepConfig.java` | 생성 | `@ConditionalOnProperty` + `@EnableScheduling` 게이팅(ControlNotifySchedulingConfig 패턴) |
| `application.yml` | 수정 | `authoring.work-lock.sweep.enabled/interval-ms` 프로퍼티 |

**핵심 로직 / 시그니처**:
```java
// GlobalExceptionHandler — cause 분기
@ExceptionHandler(DataIntegrityViolationException.class)
ResponseEntity<ApiResponse<Void>> handleDataIntegrity(DataIntegrityViolationException e) {
  Throwable root = e.getMostSpecificCause();
  // ConstraintViolationException 캐스팅 → getConstraintName()
  //  - 락 partial unique index(V69, 예: uk_work_lock_*) → 409 CONFLICT(기존 유지)
  //  - 그 외(FK/NOT NULL/기타) → 500 INTERNAL 또는 400(제약명 비노출, CWE-209)
}

// WorkLockService — 만료 락 회수(정상 진행 락은 미회수)
@Transactional("controlTransactionManager") // REQUIRES_NEW 개별 release
int sweepExpiredLocks(); // findAllByLockSttsCdAndExpireDtBefore(LOCKED, now) → releaseRaw
```

**테스트 케이스**:
- `락_unique_충돌_DataIntegrityViolation은_409로_매핑된다`
- `FK_위반_DataIntegrityViolation은_409가_아닌_적정코드로_매핑된다`
- `만료된_LOCKED_락은_sweep에서_release된다`
- `만료되지_않은_진행중_락은_sweep에서_회수되지_않는다`
- `sweep_비활성_프로퍼티면_스케줄러_미기동`

**수용 기준**:
- [ ] 락 unique 위반→409, 그 외 무결성 위반→적정 코드(오분류 0)
- [ ] 응답에 제약명·스택 미노출(CWE-209)
- [ ] 만료 락이 sweep 주기 후 자동 release, 진행중 락은 미회수
- [x] sweeper `@ConditionalOnProperty`로 토글

**의존성**: 없음.

> 구현: GlobalExceptionHandler(cause 분기 work_lock unique→409·그외/불명 fail-closed 500) · LsAuthWorkLockRepository(findExpiredLocked·reclaimExpiredByPk @Modifying) · WorkLockService(sweepExpiredLocks NOT_SUPPORTED + reclaimExpiredLockInNewTx PK 스코프) · WorkLockSweepJob/Config(@Scheduled+@ConditionalOnProperty) · 테스트 4파일. DEV_FIX: sweep 정체성 오회수(rawSn→PK 원자 UPDATE)·토글 테스트·NOT_SUPPORTED.

### Phase 7: 트랙 머지 재보간 범위 축소(R3)

**목표**: 트랙 머지 후 영상 전체 재보간을 병합 결과 트랙(toTrackId)만 재보간으로 축소해 락 유지시간 단축.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `batch/step/TrackInterpolationStep.java` | 수정 | `interpolateSingleTrack(Long rawSn, String trackId)` 추가 — 기존 `interpolateTrack`(:195) 재사용, 해당 트랙만 stale 삭제+보간 |
| `batch/repository/LsDataLblRepository.java` | 수정 | `findInterpolatedLblSnsByRawSnAndTrackId`, `findAutoBboxByRawSnAndTrackId` (trackId 필터 오버로드) |
| `label/service/TrackMergeService.java` | 수정 | `doMerge`(:144)의 `interpolate(rawSn)` → `interpolateSingleTrack(rawSn, toTrackId)` |

**핵심 로직 / 시그니처**:
```java
// TrackInterpolationStep — 단일 트랙 재보간(머지 caller tx 참여)
public int interpolateSingleTrack(Long rawSn, String toTrackId);
// - findInterpolatedLblSnsByRawSnAndTrackId로 해당 트랙 stale만 삭제
// - findAutoBboxByRawSnAndTrackId 후보 조회 → interpolateTrack 1회
// - 반환: 보간 생성 row 수(기존 interpolatedRowCount 로그 유지)
```

**테스트 케이스**:
- `머지후_toTrackId만_재보간되고_다른_트랙_보간은_불변`
- `단일트랙_재보간_결과가_전체재보간과_동일_좌표`(정확성 회귀)
- `머지후_고아_프레임_0`
- `보간산출물_stale_삭제가_해당트랙만_적용`

**수용 기준**:
- [x] 머지 후 재보간이 toTrackId만 재생성 (★stale 삭제는 from+to — 시나리오 정정)
- [x] 결과 정확성 유지(고아 프레임 0, fromTrackId 보간잔재 0, 좌표 전체재보간과 동일-골든)
- [x] 응답 interpolatedRowCount·`[TrackMerge] merged` 로그 유지
- [x] 재보간 실패 시 머지 롤백(원자성, caller-tx 참여)

**의존성**: 없음(Phase 1 보간 완료분 재사용).

> 구현: TrackInterpolationStep.interpolateSingleTrack(rawSn,toTrackId,fromTrackId — caller-tx, interpolateTrack 재사용) · LsDataLblRepository 오버로드 2종(findAutoBboxByRawSnAndTrackId·findInterpolatedLblSnsByRawSnAndTrackId IN) · TrackMergeService:144. ★stale 삭제 from+to(fromTrackId 고아 방지, 계획 "toTrackId만"보다 정정). 전체경로 interpolate(rawSn) 무변경. 후속(결함 아님): 원자성 실DB IT·EXPLAIN 인덱스 확인·(RAW_SN,TRCK_ID) 복합인덱스.

### Phase 8: F/Q 폴리곤 키보드 배선(R5)

**목표**: F(점 추가)/Q(폴리곤 완성) 단축키가 실제 동작(현재 no-op). OverlayLayer 폴리곤 편집 상태를 imperative handle로 노출.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/features/label/canvas/OverlayLayer.tsx` | 수정 | `forwardRef`+`useImperativeHandle`로 `addPointAtPointer()`/`completePolygon()` 노출. onClick(:340~350)/onDblClick(:352~361) 로직을 명명 함수로 추출해 마우스·키보드 공유. 도구 이탈 시 polyPoints 초기화(:89~98 패턴) |
| `frontend/src/features/label/canvas/CanvasShell.tsx` | 수정 | `forwardRef`로 OverlayLayer ref 상위 중계(:44~52,:135~145). lazy/Suspense 경계 고려 |
| `frontend/src/features/label/LabelingPage.tsx` | 수정 | `onPolygonAddPoint`/`onPolygonComplete` 핸들러 추가해 ref 메서드 호출(:440~484 useLabelingShortcuts 인자) |
| `frontend/src/features/label/labelingKeymap.ts` | 수정 | :64~65 label의 "(후속 배선 대기)" 문구 제거 |

**핵심 로직**:
```ts
// OverlayLayer — imperative handle
useImperativeHandle(ref, () => ({
  addPointAtPointer: () => { /* pointerCanvas() 위치를 polyPoints에 push (onClick 로직 재사용) */ },
  completePolygon: () => { /* commitPolygon() (onDblClick 로직 재사용) */ },
}));
```

**테스트 케이스** (FE — ui-tester 브라우저 + 유닛):
- `F키_누르면_현재_포인터위치에_폴리곤_점_추가`
- `Q키_누르면_진행중_폴리곤_완성_저장`
- `폴리곤_마우스_그리기_기존동작_회귀없음`
- `BBOX_SAM2_키포인트_단축키_회귀없음`
- `폴리곤_도구_이탈시_진행중_점_초기화`

**수용 기준**:
- [x] F=점 추가, Q=폴리곤 완성 배선(no-op 해소, 마우스/키보드 로직 공유) — 브라우저 실동작은 /cc-verify
- [x] 기존 폴리곤 그리기·SAM2(G/Shift+T)·키포인트(K) 회귀 0 (vitest 1059 GREEN)
- [x] 키맵 label 문구 정리

**의존성**: 없음(FE 순수). 주의: 미사용 `PolygonTool.tsx`는 대상 아님(로직은 OverlayLayer).

> 구현: OverlayLayer.tsx(forwardRef+useImperativeHandle addPointAtPointer/completePolygon, onClick/onDblClick→addPolygonPointAt/tryCommitPolygon 추출, 도구이탈 POLYGON draft 초기화) · CanvasShell.tsx(forwardRef 중계, lazy 호환) · pages/label/LabelingPage.tsx(canvasHandleRef+F/Q 핸들러) · hooks/labelingKeymap.ts · 신규 OverlayLayerImperativePolygon.test.tsx(6).

### Phase 9: 포털 도구 노출(R1-a)

**목표**: 포털에 키포인트·SAM2 세그·SAM2 자동추적 노출. YOLO 수동트리거만 포털 제외 유지.

> **★설계 정정 (2026-07-15 시나리오 실측 — 계획의 "내부 엔드포인트 매처 개방" 폐기)**: `Sam2TrackService:130`이 SAM2 추적 결과를 서버에서 즉시 **내부 `LS_DATA_LBL`에 persist**하므로 내부 `/v1/frames/**` SAM2/키포인트를 포털에 열면 APPROVED 영상 라벨 오염·데이터마트 유출(CRITICAL). `/v1/frames/**` 매처 완화는 YOLO까지 노출(CRITICAL). LabelAccessGuard는 PORTAL_USER 미인식(WORKER 회귀 위험). → **정석: 내부 컨트롤러/가드 무변경 + 포털 전용 신규 엔드포인트**(persist 없이 좌표만). memory: labeling-gap-design-decisions R1-a 구현 정정.

**생성·수정 파일 (정정후)**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/.../portal/controller/PortalLabelController.java`(또는 신규 PortalSam2Controller) | 수정/생성 | `POST /v1/portal/frames/{srcSn}/sam2-track`·`sam2-segment` — persist 없이 좌표만 반환. `@PreAuthorize(ROLE_PORTAL_USER)`+CHANNEL_PORTAL. IDOR 재검증(APPROVED 소속 srcSn) |
| `backend/.../portal/service/PortalLabelService.java` (또는 신규) | 수정/생성 | 포털 SAM2 추론 위임(Sam2*Service 추론부 재사용, persist 제거 경로) + SAM2에 bulkhead(aiOnlineBulkhead/포털 전용). 포털 키포인트(SKELETON) 저장을 `PortalUserLabelRequest`/`LS_PORTAL_USER_LABEL`에 지원 |
| `backend/.../portal/dto/PortalUserLabelRequest.java` | 수정 | SKELETON(키포인트) 케이스 명시 검증 |
| `frontend/src/features/label/types.ts` | 수정 | `PORTAL_HIDDEN_TOOLS`(:185~189)에서 SAM_SEGMENT·TRACK·KEYPOINT 제거. 단일 소스라 툴바+단축키 동시 개방(YOLO는 독립 플래그라 계속 숨김) |
| `frontend/src/pages/label/LabelingPage.tsx` | 수정 | SAM2 자동추적 패널 portalMode 분기(:854~864) 노출 |
| `frontend/src/features/label/**/api.ts`(또는 훅) | 수정 | portalMode면 SAM2/키포인트 저장 URL을 `/v1/portal/frames/...`·포털 user-label로 분기 |
| `docs/v2-wiki/16-portal.md`·`10-labeling.md`·`README.md` | 수정 | ADR-013 override 반영(활성 문서만) |

**테스트 케이스**:
- `포털_채널_SAM2추적_좌표만_반환_내부_LS_DATA_LBL_불변`(CRITICAL #1)
- `포털_채널_SAM2세그먼트_추론_호출_허용`
- `포털_채널_YOLO_autolabel_및_내부_sam2track은_여전히_403`(CRITICAL #3)
- `포털_SAM2_bulkhead_초과시_429`(HIGH #4)
- `포털_키포인트_SKELETON_LS_PORTAL_USER_LABEL_저장`
- `포털_타APPROVED영상_srcSn_조작_거부`(HIGH #5 IDOR)
- `내부_WORKER_REVIEWER_sam2track_labels_회귀없음`

**수용 기준**:
- [ ] 포털 로그인 시 키포인트·SAM2 세그·SAM2 추적 버튼+단축키 노출·동작(포털 전용 엔드포인트)
- [ ] YOLO 오토라벨 + 내부 sam2-track 엔드포인트는 포털 403 유지(CRITICAL 채널 경계)
- [ ] 포털 SAM2 결과 persist 없음(내부 LS_DATA_LBL 불변), 저장은 LS_PORTAL_USER_LABEL
- [ ] SAM2 bulkhead + 포털 IDOR 재검증
- [ ] 내부 채널 회귀 0 + ADR-013 문서 갱신

**의존성**: 없음. **설계 문서(ADR-013) 갱신 동반**(design baseline drift 방지). Phase 10(포털 머지)과 포털 전용 엔드포인트 패턴 공유.

### Phase 10: 포털 트랙 rename 오배선 버그 수정 (R1-b 축소)

> **★사용자 결정(2026-07-15): 축소.** 시나리오 실측 — 포털엔 트랙 개념이 없음(`LsPortalUserLabel`에 trackId 컬럼 부재, 저장은 새 row insert, loadFrameLabels가 trackId 스트리핑, SAM2 track은 FE 세션 opaque 미저장, 재보간 대상 부재). "포털 트랙 머지+재보간"은 데이터모델 근본 불일치라 TRACK_ID 스키마+트랙 인프라 신설은 투기적. → **포털에서 트랙 rename 버튼 숨김/비활성(FE 오배선 버그 제거) + 문서 정리**로 축소. 스키마 변경·신규 BE 엔드포인트 없음.

**목표(축소)**: 포털 라벨링 화면의 트랙 rename 오배선 버그 제거. 현재 `handleRenameTrack`이 portalMode 무분기로 내부 전용 `mergeTracks`(`/v1/videos/{rawSn}/tracks/merge`, 포털 403)를 호출 — 2회차 rename 시 trackId가 store에 세팅돼 실제 403 발생. 포털엔 트랙이 없으므로 rename UI를 포털에서 숨긴다.

**생성·수정 파일(축소)**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/features/label/components/ObjectClassTree.tsx` | 수정 | 트랙 rename(연필) 버튼을 portalMode에서 숨김/비활성 |
| `frontend/src/pages/label/LabelingPage.tsx` | 수정 | `ObjectClassTree`에 portalMode 전달, `handleRenameTrack` 포털 no-op 가드(내부 API 미호출) |
| `docs/v2-wiki/16-portal.md` | 수정 | 포털은 트랙 rename/머지 미제공(트랙 데이터모델 부재) 명시 |

**목표(원안, 폐기)**: ~~포털 전용 `/v1/portal/videos/{rawSn}/tracks/merge` 신설, LS_PORTAL_USER_LABEL 대상~~ — 포털 트랙 부재로 폐기.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/.../label/controller/PortalTrackMergeController.java` | 생성 | `POST /v1/portal/videos/{rawSn}/tracks/merge` — `@PreAuthorize(ROLE_PORTAL_USER)` + CHANNEL_PORTAL |
| `backend/.../label/service/PortalTrackMergeService.java` | 생성 | LS_PORTAL_USER_LABEL 대상 머지/rename. 내부 TrackMergeService 로직 중 재사용 가능 부분 공유(겹침 409·재배정), 저장소만 포털 |
| `backend/.../portal/repository/LsPortalUserLabelRepository.java` | 수정 | 트랙 조회·재배정 쿼리 추가 |
| `frontend/.../label/api.ts` | 수정 | portalMode면 `/v1/portal/...` 머지 경로 사용(`mergeTracks`/rename 분기) |

**핵심 로직**:
```java
// PortalTrackMergeService — 포털 데이터 대상
@Transactional("portalTransactionManager")
PortalTrackMergeResponse merge(Long rawSn, String fromTrackId, String toTrackId, Long portalUserNo);
// - 포털 사용자 소유 라벨(LS_PORTAL_USER_LABEL)만 대상(IDOR)
// - 겹침 frame 교집합 409(내부 로직 공유)
// - 포털 라벨은 데이터마트 미반영(단방향) — 재보간 필요 여부 설계 판정
```

**테스트 케이스**:
- `포털_머지_본인라벨만_대상_타인_403`
- `포털_머지_겹침프레임_409`
- `포털_rename_미사용번호_병합_동작`
- `포털_머지가_LS_DATA_LBL_내부데이터_미변경`
- `내부_머지_엔드포인트_회귀없음`

**수용 기준**:
- [ ] 포털 머지/rename이 LS_PORTAL_USER_LABEL에 정합 동작
- [ ] 내부 라벨(LS_DATA_LBL) 불변, 내부 경로 회귀 0
- [ ] 포털 IDOR/채널 인가 준수

**의존성**: 약한 의존 — Phase 7(재보간) 로직 참고. 포털 재보간 정책 확정 필요(계획 중 설계 판정).

### Phase 11: 증강 프레임 비동기 재추출 + 상태전이 재설계(R6)

**목표**: 증강본 프레임을 부모 복사 대신 증강 영상에서 FFmpeg로 비동기 재추출. 라벨 좌표만 복사. TOCTOU·멱등 안전 유지.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `webhook/service/AugmentResultService.java` | 수정 | `createAugmentedVideo`(:159~248)에서 프레임/라벨 복사 블록(:197~233) 제거. 동기 tx엔 부모잠금(:169)+게이트(:180)+멱등앵커(:104~135)+`deIdntfYn='Y'`+SUCCESS procLog 유지, 새 RAW는 PENDING 커밋. `markMarkingReady()` 제거 |
| `webhook/runner/AsyncAugmentFrameRunner.java` | 생성 | `@Async("batchAsyncExecutor")` REQUIRES_NEW — newRaw 재조회 → 프레임 재추출 → 라벨 srcSnMap 재매핑+AugLblMap → 성공 시 `markMarkingReady()`, 실패 시 `markBatchFailed()` |
| `batch/step/FfmpegFrameExtractor.java` | 수정 | `extractByFrameNumbers(LsDataRaw, List<Long> videoFrameNos)` 추가 — `writeFrameByNumber`(:335) 활용 frame-exact 추출. 증강본은 RAW=DEID 동일 취급 |
| `webhook/service/AugmentResultService.java` | 수정 | `triggerMetaExtractionAfterCommit`(:269~280) 패턴으로 프레임 추출 러너 AFTER_COMMIT 트리거 |

**핵심 로직 / 순서**:
```
[동기 handle tx] 부모 findByRawSnForUpdate 잠금 → deIdntfYn=='Y' 게이트 → 멱등 앵커 →
  새 RAW createFromAugment(PENDING, rawFilePathNm=증강영상경로) →
  deIdntfYn='Y'+SUCCESS procLog(추출러너 게이트 통과용) → AFTER_COMMIT: AsyncAugmentFrameRunner.runAsync
[비동기 러너 REQUIRES_NEW] newRaw 재조회 → 부모 videoFrameNo 리스트로 extractByFrameNumbers →
  새 SRC_SN ↔ 부모 프레임 zip으로 srcSnMap → 라벨 copyForNewSrc + AugLblMap(COORD_RECALC_YN='N') →
  성공: markMarkingReady() / 실패: markBatchFailed()
```

**테스트 케이스**:
- `증강본_프레임이_증강영상에서_재추출됨_부모경로_복사아님`
- `증강본_라벨좌표_새SRC_SN에_정확_재매핑`
- `프레임추출_성공전_MARKING_READY_미전이`(반쪽 증강본 차단)
- `프레임추출_실패시_FAILED_전이_마킹대기_고착없음`
- `부모_비식별_TOCTOU_동기tx_유지_회귀없음`(AugmentDeidentConcurrencyIT 확장)
- `중복콜백_멱등_이중처리_0_회귀없음`

**수용 기준**:
- [ ] 증강 프레임이 증강 파일에서 재추출(원본 경로 복사 아님), 비동기 처리
- [ ] 라벨 좌표 정확 매핑(해상도 동일, COORD_RECALC_YN='N')
- [ ] MARKING_READY는 추출 성공 후 전이, 실패 시 FAILED
- [ ] 부모 TOCTOU(CWE-359)·콜백 멱등 회귀 0
- [ ] 기존 증강 라벨/메타 복사·상태전이 테스트 GREEN

**의존성**: 없음(독립). 최고 위험 → 마지막. Phase 5 코드 직접 수정하므로 TOCTOU·멱등 회귀 집중 검증. AiInfo 승계 필요 여부는 구현 중 판단(현재 미복사).

---

### Phase 12: ai-server RTDETR 탐지 백엔드 제거(F1)

**목적**: RT-DETRv2 탐지 백엔드가 `libtorchaudio.so: undefined symbol`(torch↔torchaudio ABI 불일치)로 load 실패 → mock 폴백 중. 탐지를 YOLOX 단일 백엔드로 일원화하여 ABI 취약 의존을 제거.

| 파일 | 변경 | 내용 |
|------|------|------|
| `ai-server/app/models/rtdetr_loader.py` | 삭제 | RT-DETR 로더 제거 |
| `ai-server/app/routers/yolo.py` | 수정 | `DETECTOR_BACKEND` 분기에서 rtdetr 경로 제거, YOLOX 단일화 |
| `ai-server/app/config.py` | 수정 | `detector_backend`/rtdetr 관련 설정 제거(또는 yolo 고정) |
| `ai-server/requirements.txt` | 수정 | transformers/torchaudio 등 RT-DETR 전용 의존 제거(YOLOX onnxruntime만 필요 시) |
| `docker-compose.local.yml` | 수정 | `DETECTOR_BACKEND: rtdetr` 제거 |
| `ai-server/tests/test_rtdetr_real.py`·`test_yolo_dispatch.py` | 수정/삭제 | RT-DETR 테스트 정리, 디스패치 테스트 YOLOX 단일 반영 |

**수용 기준**: [ ] RT-DETR import·설정·테스트 완전 제거 [ ] 탐지/오토라벨이 YOLOX로 정상 동작(mock 폴백 아님) [ ] torchaudio ABI 에러 소멸 [ ] ai-server 기동·기존 YOLO 테스트 GREEN
**위험**: YOLOX 미커버 클래스/성능 차이 → 제거 전 YOLOX 탐지 실동작 확인. **의존성**: F2와 함께 진행 시 효율적(둘 다 ai-server).

### Phase 13: ai-server mock 모드 자체 제거(F2)

**목적**: `AI_MOCK_MODE` 및 `_should_mock`/`_mock_*` 폴백 경로를 전면 제거. 실추론 전제로 단순화하고, 모델 미로드 시 조용한 mock 대신 명시적 에러로 실패.

| 파일 | 변경 | 내용 |
|------|------|------|
| `ai-server/app/config.py` | 수정 | `ai_mock_mode` 설정 제거 |
| `ai-server/app/routers/sam2.py`·`yolo.py`·`vlm.py` | 수정 | `_should_mock`/`_mock_track`/`_mock_segment`/mock 응답 분기 제거. 모델 None 시 503 등 명시적 에러 |
| `ai-server/app/models/*_loader.py` | 수정 | `mock_reason`/load 실패 시 None 반환 → 예외 전파로 변경 |
| `docker-compose.yml`·`.local.yml`·`.env.example` | 수정 | `AI_MOCK_MODE` 환경변수 제거(기본 `${AI_MOCK_MODE:-true}` 삭제) |
| `ai-server/tests/test_mock_indicator.py` | 삭제 | mock 지표 테스트 제거 |
| `ai-server/tests/test_*_real.py` | 수정 | mock 스킵 전제 제거, 실모델 필수화(CI 모델 준비 전략 결정 필요) |
| (BE) `Sam2TrackResponse`·`Sam2SegmentResponse` mock 필드 | 검토 | ai-server가 mock 필드를 더 안 보내면 BE DTO도 정리(부수 발견 #3과 연계) |

**수용 기준**: [ ] AI_MOCK_MODE·mock 분기·mock 테스트 완전 제거 [ ] 모델 미로드 시 명시적 에러(조용한 mock 저장 0) [ ] CI/로컬 모델 준비 전략 문서화 [ ] 실추론 경로 회귀 0
**위험(HIGH)**: mock은 모델 없는 CI·의존성검증 기동의 전제 — 제거 시 CI에서 모델 다운로드(HF) 또는 ai-server 테스트 분리 필요. 제거 범위·CI 영향 먼저 합의. **의존성**: Phase 12와 동일 파일군(sam2/yolo 라우터) → 순차 권장.

### Phase 14: SAM2 Track 청크 코드리뷰 경미 3건 폴리싱(F3)

**목적**: 커밋 3802b43(SAM2 Track 청크) 코드리뷰 LOW/MEDIUM 3건 정리. 기능 무해하나 사용자 안내 정확성·유지보수성 개선.

| 파일:라인 | 변경 |
|----------|------|
| `frontend/src/features/label/api.ts:433~441` | 빈 tracked 방어분기의 `completedChunks` 라벨을 실패 catch 분기와 일관되게 통일(실패구간 번호 오표시 방지) + "BE 계약상 도달 불가, 향후 계약변경 대비" 주석 명시 |
| `frontend/src/features/label/hooks/useSam2Track.ts:33~37` | `onError`의 `LABEL_KEYS` 무효화를 `err instanceof Sam2TrackChunkError && err.partial.length > 0`일 때만(완전 실패 시 불필요 리페치 skip) |

**수용 기준**: [ ] 실패구간 안내가 실제 완료/실패 청크와 일치 [ ] 완전 실패 시 불필요 invalidate 없음 [ ] 도달불가 분기 주석 명시 [ ] 기존 sam2-track 테스트 GREEN + 해당 케이스 테스트 보강
**위험**: 낮음(경미 폴리싱). **의존성**: 없음(독립).

---

## 실행 가이드

1. `/cc-tdd Phase 6: 예외 cause 분기 + 작업락 만료 sweeper`
2. `/cc-tdd Phase 7: 트랙 머지 재보간 범위 축소`
3. `/cc-tdd Phase 8: F/Q 폴리곤 키보드 배선`
4. `/cc-tdd Phase 9: 포털 도구 노출(키포인트·SAM2)` (+ ADR-013 문서 갱신)
5. `/cc-tdd Phase 10: 포털 전용 트랙 머지/rename 엔드포인트`
6. `/cc-tdd Phase 11: 증강 프레임 비동기 재추출 + 상태전이 재설계`
7. `/cc-hotfix Phase 12: ai-server RTDETR 탐지 백엔드 제거`
8. `/cc-hotfix Phase 13: ai-server mock 모드 자체 제거` (CI/모델 준비 전략 선합의)
9. `/cc-tdd Phase 14: SAM2 Track 청크 코드리뷰 경미 3건 폴리싱`
