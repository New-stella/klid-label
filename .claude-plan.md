# CVAT-Like 라벨 마스터·속성 풀 포팅 (Plan)

## Context

**시작**: 2026-05-15
**동기**: 작업자(WORKER) 라벨링 시 객체명이 `Object` 고정 + 수정 불가. CVAT 와 비교 분석 결과 — 라벨 마스터 정의 부재, 클래스 선택 UX 누락, AI 자동 라벨 매핑 부재가 근본 원인 (옵션 C 사용자 결정).

**범위**: CVAT 동등 라벨 시스템 풀 포팅
- 라벨 마스터(이름·색상·타입) — 프로젝트 단위
- 속성(Attribute) — 라벨별 옵션값(가려짐/방향 등)
- 자동 라벨 preset 매핑 — YOLO 클래스 → 라벨 FK
- FE LabelSidebar + 단축키 + 색상 + ObjectAttributePanel 드롭다운

**완료 기준** (Done Definition)
- 작업자가 BBOX 그릴 때 드롭다운 또는 단축키(1~9)로 클래스 선택 가능
- 이미 그려진 객체 더블클릭 → 클래스/속성 수정
- YOLO 자동 라벨이 LS_LABEL FK 와 매칭되어 한글 이름·색상으로 표시
- `Object` fallback 텍스트 화면에서 사라짐

**이전 plan 보존**: `.claude-archive/plan-v2-policy-2026-05-15.md` (V2 정책 작업 완료본)

---

## 핵심 아키텍처 결정

| 항목 | 결정 |
|------|------|
| 라벨 마스터 스코프 | **프로젝트(PJT_ID) 단위** — 현재 단일 PJT_ID=1 운영. 향후 멀티 프로젝트 시 자연스럽게 확장 |
| 신규 테이블 | `LS_LABEL`, `LS_LABEL_ATTR`, `LS_DATA_LBL_ATTR_VAL` |
| LS_DATA_LBL.LABEL 컬럼 | **deprecated** — 신규 `LABEL_ID` FK 컬럼 추가. 기존 텍스트는 일회성 마이그레이션으로 best-effort 변환 후 컬럼 유지(읽기 전용) |
| LS_LABEL_PRESET 기존 테이블 | **존속하되 활용 안 함** — V21 분리 후 BBOX/POLYGON 토글 필터로만 사용 중. 라벨 마스터 역할은 LS_LABEL 이 흡수 |
| 색상 형식 | `#RRGGBB` 7자 (대문자 hex) |
| 단축키 | 1~9 (10번째 이상은 드롭다운만) — sort_no 순서 기준 |
| AI 매핑 전략 | `LS_LABEL.NAME` 과 YOLO 응답 label 의 case-insensitive 매칭 (예: "person" → name="person" 라벨 찾기). 매칭 실패 시 LABEL_ID=NULL 저장 + FE 가 "라벨 필요" 표시 |
| 권한 | 라벨 CRUD = REVIEWER. 라벨 조회 = REVIEWER+WORKER+PORTAL_USER. 객체별 클래스 변경 = REVIEWER+WORKER |

---

## 진행 현황 (Phase Checklist)

- [x] **Phase 1**: 라벨 마스터 도메인 — `LS_LABEL` DDL + Entity/Repo/Service/Controller + CRUD API + 시드 13건 (2026-05-15 완료)
- [x] **Phase 2**: `LS_DATA_LBL.LABEL_ID` FK 연결 — V32 마이그레이션 + 기존 텍스트 best-effort 변환 (2026-05-15 완료)
- [ ] **Phase 3**: 속성(Attribute) 도메인 — `LS_LABEL_ATTR` + `LS_DATA_LBL_ATTR_VAL` + CRUD
- [ ] **Phase 4**: AutoLabel preset 매핑 — `YoloAutolabelStep` 에서 YOLO label → LS_LABEL FK 룩업
- [ ] **Phase 5**: FE LabelSidebar — CVAT 스타일 사이드바 + 색상 + 단축키 1~9
- [ ] **Phase 6**: FE ObjectAttributePanel — 드롭다운 + 속성 편집 + 즉시 반영 + `'Object'` fallback 제거
- [ ] **Phase 7**: 통합 시나리오 검증 (`/cc-verify`) + 회귀 테스트

---

## 실행 가이드 (각 Phase 자기-완결, 다른 세션에서 재개 가능)

```
/cc-tdd Phase 1: 라벨 마스터 도메인
/cc-tdd Phase 2: LS_DATA_LBL FK 연결
/cc-tdd Phase 3: 속성 도메인
/cc-tdd Phase 4: AutoLabel preset 매핑
/cc-tdd Phase 5: FE LabelSidebar
/cc-tdd Phase 6: FE ObjectAttributePanel
/cc-verify Phase 7: 통합 시나리오
```

**세션 중단 시 재개**: 새 세션에서 `/cc-resume` 입력 → 이 plan 의 미체크 Phase 부터 자동 진행. 또는 `/cc-tdd Phase N: ...` 로 특정 Phase 만 실행.

---

## Phase 1: 라벨 마스터 도메인

### 목적
프로젝트 단위 라벨 마스터(이름·색상·타입) 정의 + REVIEWER 가 CRUD. WORKER 는 조회만.

### 변경/생성 파일

| 분류 | 경로 | 동작 |
|------|------|------|
| 마이그레이션 | `backend/src/main/resources/db/migration/V31__create_ls_label.sql` | 신규 |
| Entity | `backend/src/main/java/kr/co/cudo/authoring/label/entity/LsLabel.java` | 신규 |
| Repository | `backend/src/main/java/kr/co/cudo/authoring/label/repository/LsLabelRepository.java` | 신규 (`@ControlRepo`) |
| Service | `backend/src/main/java/kr/co/cudo/authoring/label/service/LabelMasterService.java` | 신규 |
| Controller | `backend/src/main/java/kr/co/cudo/authoring/label/controller/LabelMasterController.java` | 신규 |
| DTO | `backend/src/main/java/kr/co/cudo/authoring/label/dto/LabelMasterRequest.java` | 신규 |
| DTO | `backend/src/main/java/kr/co/cudo/authoring/label/dto/LabelMasterResponse.java` | 신규 |
| 시드 | `backend/src/main/resources/db/seed/dev-seed.sql` | LS_LABEL INSERT 섹션 추가 |
| 테스트 | `backend/src/test/java/kr/co/cudo/authoring/label/LabelMasterServiceTest.java` | 신규 |
| 테스트 | `backend/src/test/java/kr/co/cudo/authoring/label/LabelMasterControllerTest.java` | 신규 |

### DDL (V31)

```sql
CREATE TABLE IF NOT EXISTS LS_LABEL (
    LABEL_ID  BIGINT       NOT NULL AUTO_INCREMENT,
    PJT_ID    BIGINT       NOT NULL,
    NAME      VARCHAR(64)  NOT NULL,
    COLOR     VARCHAR(7)   NOT NULL,
    TYPE      VARCHAR(16)  NOT NULL,
    SORT_NO   INT          NOT NULL DEFAULT 0,
    USE_YN    VARCHAR(1)   NOT NULL DEFAULT 'Y',
    REG_ID    VARCHAR(30)  NULL,
    REG_DT    TIMESTAMP    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    MDFCN_ID  VARCHAR(30)  NULL,
    MDFCN_DT  TIMESTAMP    NULL,
    PRIMARY KEY (LABEL_ID),
    UNIQUE KEY UK_LS_LABEL_NAME (PJT_ID, NAME),
    KEY IDX_LS_LABEL_PJT_USE (PJT_ID, USE_YN, SORT_NO)
);
```

### 시드 라벨 13건 (dev-seed.sql 추가)
person, car, bicycle, motorbike, bus, truck, animal, fire, smoke, water, fallen-person, vehicle-accident, object(fallback) — 색상은 distinct, sort_no 1~13

### API 계약

| Method | Path | 권한 | 설명 |
|--------|------|------|------|
| GET | `/v1/manage/labels?pjtId=1` | REVIEWER+WORKER+PORTAL_USER | 라벨 목록 (sort_no ASC) |
| POST | `/v1/manage/labels` | REVIEWER | 신규 라벨 |
| PUT | `/v1/manage/labels/{id}` | REVIEWER | 수정 (name/color/type/sortNo) |
| DELETE | `/v1/manage/labels/{id}` | REVIEWER | soft delete (USE_YN='N') |

### 테스트 케이스 (RED)

- 빈 name 입력 → 400
- 동일 PJT_ID 내 중복 name → 409
- 잘못된 컬러 형식(#FFF 또는 'red') → 400
- WORKER 가 POST → 403
- 조회는 REVIEWER+WORKER 둘 다 200
- 시드 적용 후 GET 결과 13건

### 수용 기준
- Swagger `/v1/manage/labels` 4개 endpoint 노출
- BE `./gradlew test` 전체 통과
- 시드 재실행 시 `SELECT COUNT(*) FROM LS_LABEL WHERE PJT_ID=1 AND USE_YN='Y'` = 13

> 구현 (2026-05-15): V31 + LsLabel/Repository/Service/Controller + LabelMasterRequest/Response + 16개 신규 테스트(서비스 7 + 컨트롤러 9) + dev-seed.sql LS_LABEL 13건 + SecurityConfig 매처 조정(GET은 인증 사용자 전체, POST/PUT/DELETE는 REVIEWER). BE 전체 598 tests, 0 fail.

---

## Phase 2: LS_DATA_LBL FK 연결

### 목적
기존 `LS_DATA_LBL.LABEL` (varchar) → 신규 `LABEL_ID` FK 로 연결. 기존 데이터는 best-effort 일회성 마이그레이션.

### 변경 파일
- `db/migration/V32__add_ls_data_lbl_label_id.sql` — LABEL_ID 컬럼 ADD + FK + 인덱스. 기존 row 의 `LABEL` 텍스트와 일치하는 LS_LABEL.NAME 찾아 LABEL_ID 채움(없으면 NULL)
- `LsDataLbl` Entity — `labelId: Long` 필드 추가. `label` (텍스트) 컬럼은 `@Deprecated` 주석 + 쓰기 경로 차단
- `LsDataLbl.createAutoBbox(...)` / `createUserBbox(...)` 시그니처 변경: `String label` 대신 `Long labelId`
- `LabelService.updateUserContent(...)` 시그니처에 `labelId` 추가
- LabelResponse DTO — `labelId` + `label` (LS_LABEL.NAME) + `color` 함께 반환

### 테스트 케이스
- labelId=NULL 인 row 조회 시 응답에서 label=null, color=null
- labelId 변경 PUT 후 GET 시 새 label/color 표시
- WORKER 가 labelId 변경 가능 (REVIEWER 도 동일)

### 수용 기준
- 마이그레이션 후 기존 시드 라벨 데이터 100% 매핑 또는 NULL (검증 SELECT)
- 신규 라벨 INSERT 시 LABEL_ID 필수 검증

> 구현 (2026-05-15): V32 (LABEL_ID + FK + 인덱스 + best-effort UPDATE) + LsDataLbl entity (labelId 필드 + 4종 factory 오버로드 + updateUserContent 6-arg) + LabelItemDto/LabelResponse (labelId/labelName/color) + LabelService (LsLabel 검증 + 응답 enrichment + N+1 회피). 기존 시그니처 모두 @Deprecated 유지 — 호출자(YOLO/SAM2/Track/etc) 무수정. 신규 테스트 18 (Entity 8 + DTO 3 + Controller 7), 전체 616 tests, 0 fail.

---

## Phase 3: 속성(Attribute) 도메인

### 목적
라벨별 추가 속성(예: 가려짐 yes/no, 방향 N/S/E/W) 정의 + 객체별 속성값 저장. CVAT AttributeSpec 동등.

### 변경 파일
- `db/migration/V33__create_ls_label_attr.sql` — LS_LABEL_ATTR (id, labelId, name, valuesJson, mutable, sortNo) + LS_DATA_LBL_ATTR_VAL (id, dataLblSn, attrId, value)
- Entity/Repo/Service/Controller 일괄 (LabelAttrController, LabelAttrService)
- API: `GET /v1/manage/labels/{labelId}/attrs`, POST/PUT/DELETE 동일 패턴

### 수용 기준
- 라벨 1개에 N개 속성 정의 가능
- 객체 1개에 라벨 속성 N개 값 저장
- mutable=false 속성은 트랙 전체에서 동일 값 (검증)

> 구현: (Phase 진행 시 채움)

---

## Phase 4: AutoLabel preset 매핑

### 목적
`YoloAutolabelStep` 에서 YOLO 응답 `label` ("person") → `LS_LABEL.NAME` case-insensitive 매칭하여 `LABEL_ID` 저장. 매칭 실패 시 LABEL_ID=NULL.

### 변경 파일
- `LabelMasterService.findLabelIdByName(Long pjtId, String name)` — case-insensitive 룩업
- `YoloAutolabelStep.java` — `LsDataLbl.createAutoBbox(..., labelId)` 사용
- `Sam2SegmentStep.java` — 동일 패턴

### 테스트 케이스
- YOLO "person" → LS_LABEL person 매칭 (labelId 채워짐)
- YOLO "UNKNOWN_CLASS" → labelId=NULL
- 시드 라벨 + 자동 라벨 시나리오 통합

### 수용 기준
- 영상업로드 페이지에서 YOLO 자동 라벨 결과가 한글 이름·색상으로 표시
- BE 로그에 "[Batch][YOLO] mapped label name=person labelId=1" 같은 로그

> 구현: (Phase 진행 시 채움)

---

## Phase 5: FE LabelSidebar

### 목적
CVAT 스타일 라벨 사이드바 — 라벨 목록(색상 박스 + 이름 + 단축키), 현재 선택 라벨 강조, 클릭 시 활성화.

### 변경 파일
- `frontend/src/features/label/api/labelMaster.ts` (신규) — `GET /v1/manage/labels` 호출
- `frontend/src/features/label/hooks/useLabelMaster.ts` (신규) — TanStack Query
- `frontend/src/features/label/components/LabelSidebar.tsx` (신규)
- `frontend/src/pages/labeling/LabelingPage.tsx` — Sidebar 통합 + 단축키 핸들러 (1~9 → 라벨 활성화)
- `frontend/src/features/label/components/OverlayLayer.tsx` — DEFAULT_CLASS_NAME 제거, 현재 활성 라벨 사용

### 수용 기준
- 라벨링 화면 좌측에 LabelSidebar 표시 (13개 라벨, 색상 박스 + 한글 이름 + 단축키 번호)
- 단축키 1~9 누르면 해당 라벨 활성화
- BBOX 새로 그리면 활성 라벨로 자동 적용 (DB에 labelId 저장)

> 구현: (Phase 진행 시 채움)

---

## Phase 6: FE ObjectAttributePanel

### 목적
이미 그려진 객체 클릭 시 우측 패널에 라벨 드롭다운 + 속성 편집 + 변경 즉시 PUT 반영. `'Object'` fallback 텍스트 완전 제거.

### 변경 파일
- `frontend/src/features/label/components/ObjectAttributePanel.tsx` — `availableLabels` prop 사용 + 드롭다운 + onChange 시 PUT
- `frontend/src/features/label/components/LabelPanel.tsx` — labelId 기반 그룹화 + 색상 표시
- `frontend/src/pages/labeling/LabelingPage.tsx` — `availableLabels` 전달
- `frontend/src/features/label/api.ts` — `normalizeLabel` 에서 `Object` fallback 제거, labelId null 이면 빈 표시

### 수용 기준
- 자동 라벨 + 수동 라벨 모두 한글 이름·색상으로 표시
- 객체 클릭 → 드롭다운에서 다른 라벨 선택 → 즉시 캔버스 색상/이름 갱신
- `Object` 텍스트 화면 어디서도 안 보임
- Vitest 시나리오: "labelId 변경 후 화면 즉시 반영"

> 구현: (Phase 진행 시 채움)

---

## Phase 7: 통합 시나리오 검증

### 목적
`/cc-verify` 로 사용자 시나리오(영상 업로드 → 자동 라벨 → 라벨 수정 → 저장) 전체 통과 확인. 회귀: 기존 라벨링/검수 플로우.

### 검증 시나리오
1. 영상업로드 페이지에서 mp4 업로드 + 전체 단계 ON → 자동 라벨 결과가 한글 이름·색상으로 표시
2. WORKER 가 객체 클릭 → 드롭다운에서 라벨 변경 → 저장 → 새로고침 후 유지
3. 단축키 1~9 로 신규 BBOX 그리기 → 활성 라벨로 저장
4. REVIEWER 가 `/v1/manage/labels` 에서 새 라벨 추가 → 라벨링 화면 새로고침 후 드롭다운에 노출
5. 회귀: 검수(/v1/reviews/*) / 비식별 / 통계 / 포털 화면 정상

> 구현: (Phase 진행 시 채움)

---

## 향후 검토 후보 (Future Cleanup Candidates)

### 프로젝트 개념 자체 제거 (2026-05-15 기록)
- 현재 PJT_ID=1 단일 프로젝트만 사용하므로 LS_PJT 테이블 자체 + 전 도메인의 PJT_ID 컬럼을 제거하는 옵션 고려 가능
- 영향 범위: LS_PJT_DATA_MPNG, LS_PJT_DATA_STTS, LS_PJT_USER_AUTHRT, LS_PJT_USER_AUTHRT_HSTRY, LS_PJT_META, LS_PJT_DDLN, LS_PJT_TASK_EVENT_LOG, **LS_LABEL** (Phase 1에서 추가됨) 등
- 결정 시점: 단일 프로젝트 전제가 확정되면 진행. 멀티 프로젝트 가능성 남아있으면 보류
- 선행 작업 (2026-05-15 완료): `/v1/projects/*` API + `/v1/generate/background/*` API 제거

### V2 정책 작업 (완료 보관)
이전 plan(V2 정책 9건 반영 + LS_DEIDENT_REPORT 분리 + 시드 정리 + M2M 제거 + projects/generate API 제거 + 영상업로드 명칭/4단계 토글) 은 `.claude-archive/plan-v2-policy-2026-05-15.md` 에 보존.
