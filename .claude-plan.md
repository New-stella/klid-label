# 구현 계획: 포털 전용 이미지/영상 업로드 + 수동 라벨링

> 생성: 2026-07-17 | 상태: **전체 완료** ✅ | 브랜치: feat/portal-image-upload (worktree 격리)
> 🎯 목표 달성: 포털 사용자가 본인 이미지·영상을 직접 업로드하고, 수동 라벨링(bbox/폴리곤) 후 본인 데이터를 다운로드. 내부 파이프라인과 완전 분리.

## Phase 진행 현황

- [x] Phase 1: DB 마이그레이션 + 도메인 골격 ✅ (DEV_FIX 1회)
- [x] Phase 2: 이미지 업로드 + 자산 관리 API ✅ (QA 7종, DEV_FIX 2회)
- [x] Phase 3: 영상 TUS 업로드 + 비동기 프레임 추출 ✅ (QA 6종+재반증 FIXED×2, DEV_FIX 2회)
- [x] Phase 4: 업로드 라벨 CRUD + export/다운로드 ✅ (QA 5종, DEV_FIX 1회, 동시성 실DB IT)
- [x] Phase 5: FE 업로드 화면 ✅ (fullstack 계약 정합, DEV_FIX 1회)
- [x] Phase 6: FE 업로드 라벨링 화면 ✅ (코어 무수정 확인, 테스트 보강 1회)
- [x] Phase 7: 문서 동기화 + 최종 통합 검증 ✅ (requirements CONFORM · scenario PASS)

## 최종 검증 요약

- **requirements-verifier(전체)**: CONFORM — R1~R5·AC1~AC7 전부 MET (FE-BE 끝-끝 배선, 데이터마트 View SQL 무접촉, 이벤트 구독 전수, 비식별 미참조 실증)
- **scenario-verifier**: PASS — BE 2377 tests(portal 202 GREEN), FE 1125/1125 GREEN. 실패 3건은 pre-existing `DatasetExportServiceIT`(이번 작업 무관, clean tree 입증)
- **미수행**: 실브라우저 E2E(라이브 서버 부재 — dev 서버 기동 후 `/cc-verify` 또는 e2e/specs/portal-upload.spec.ts 실행 권장)

## 요구사항 최종 상태 (baseline)

- R1 이미지+영상 업로드 ✅ / R2 수동 라벨링만(3중 방어) ✅ / R3 LS_PORTAL_* 분리 ✅ / R4 본인 데이터만 ✅ / R5 비식별 미적용+간격/상한 ✅
- AC1~AC7 전부 충족 (AC7: CLAUDE.md·PortalLabelService 주석·04-screens-ia(SC-034/035)·18-database(4테이블)·19-v2-gap-checklist 갱신)

## 산출물 요약

- **DB**: V107(LS_PORTAL_ULD/FRME/LBL + FRME SEQUENCE(50) + 인덱스), V108(LS_PORTAL_TUS_ULD + 부분 인덱스), sysconfig `portal.upload.frame-interval-sec`(기본 5s)
- **BE**(portal 패키지): PortalUploadController/Service(이미지 multipart, all-or-nothing, 매직바이트+EOI/IHDR, RateLimiter), PortalTusUploadController/PortalVideoUploadService(+TxService — 락 최소화·검증 tx밖·finalize 조건부), PortalVideoUploadedEvent(관제 이벤트 완전 분리)+Bridge+PortalFrameExtractRunner(+TxService — 조건부 전이·하트비트·전용 executor), PortalUploadSweepJob(+TxService — 멱등 스윕), PortalUploadLabelController/Service(비관 락 교체·CWE-113·본문 상한 필터), upload/TusChunkStore(코어 추출)+관제 cancel 락 수정
- **FE**: PortalUploadPage(이미지 multipart+영상 TUS endpointBase 주입+폴링/409 alert), PortalUploadLabelingPage(CanvasShell 조립 — 코어 무수정, 도구 4종만, raw 배열 PUT, export/원본 다운로드), features/portal/uploads/{api,types,validation,hooks}
- **설정**: multipart 전역 활성(21MB/1100MB/threshold 1MB — TUS 무영향 검증), portal.upload 이미지 정책, portalUpload RateLimiter

## 백로그 (비게이팅 — 후속 검토)

1. finalize 전 크래시 완료-유실(N3): 재구동 설계 / 2. ffprobe 타임아웃 시 OS 프로세스 정리(destroyForcibly) / 3. 404·403 열거 응답 통일 / 4. 전역 스토리지 쿼터·사용자 누적 상한 / 5. TUS 종료행 아카이빙 / 6. ShedLock(스윕은 멱등화로 완화됨) / 7. 이미지 커밋시점 고아 스캐너 / 8. 심링크 toRealPath / 9. Idempotency-Key / 10. 전역 Pageable size 캡(프로젝트 공통) / 11. AC6 ArchUnit 게이트 / 12. OverlayLayer BBOX 마스터 미로드 시 무안내(코어 소관 — 다른 세션 조율) / 13. label 80자 절단 무경고 / 14. PortalUploadPage 섹션 분리·삭제버튼 항목 스코프 / 15. 16-portal.md 업로드 서술 보완 / 16. dev/stg multipart 500MB 수용 / 17. PORTAL 사업 단어 등록(LogiCraft — 사용자 논의) / 18. ADR-013 LogiCraft ITEM·D-산출물 갱신(사용자 논의) / 19. **DatasetExportServiceIT 3건 pre-existing 실패(이번 작업 무관 — 별도 수정 필요)** / 20. e2e/specs/portal-upload.spec.ts 실브라우저 실행

## 격리·병합 참고

- worktree: `.claude/worktrees/portal-image-upload`, base origin/main 7be0d6e — 다른 세션(feat/label-v1-adopt)과 무간섭
- FE 공유 파일 수정 최소(router/queryKeys/PortalHomePage/tusClient — features/label/** 코어 0건 수정, git diff 확인)
