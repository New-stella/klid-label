# 구현 계획: 영상 목록 size:999 패턴 전면 재설계 (만 건 대비)

> 생성: 2026-05-18 16:35
> 상태: 진행 중

## Phase 진행 현황

- [x] Phase 1: ReviewResponse enrich에 eventName 추가 + ReviewListPage useVideos 제거
  > 구현: VideoRepository.findEventInfoByRawSns / ReviewResponse 7-arg / ReviewService.lookupEventByVideo / ReviewListPage useVideos 제거 (페이지당 쿼리 6건)
- [x] Phase 2: TaskResponse enrich + TaskListPage WORKER 시각 페이징·useVideos 제거
  > 구현: AssignmentResponse.Item에 eventName/eventTypeCd/status / AssignmentService.lookupEventInfoByVideo + lookupDataSttsByVideo / TaskListPage WORKER 분기 BE 페이징 + useVideos enabled 게이트 / DEV_FIX: requireReviewer null 가드 + status 정합 + WORKER 이벤트 필터 분기
- [ ] Phase 3: REVIEWER 작업 목록 통합 BE 엔드포인트 (`/v1/tasks/board`) + FE 전환
- [ ] Phase 4: AugmentRequestPage 영상 선택 패턴 재설계

## 요구사항 요약

- **목적**: 영상 수가 만 건 이상으로 늘어나도 목록 화면이 정상 동작하도록 size:999 일괄 조회 패턴을 BE 페이징·enrich 패턴으로 전환
- **범위**:
  - 포함: 4개 페이지(`ReviewListPage`, `TaskListPage` WORKER/REVIEWER 시각, `AugmentRequestPage`)의 데이터 로딩 패턴 재설계
  - 제외: 대시보드(`DashboardPage.recentPage`)는 별도 size 정책으로 제외. `VideoListPage`는 이미 페이징 사용 중이라 제외
- **주요 액터**: REVIEWER(작업 배정·검수), WORKER(본인 배정 작업·라벨링), 포털 사용자(증강 요청 시 영상 선택)
- **비기능 요구사항**:
  - BE 페이지 size 20 기준 응답 시간 100ms 이하
  - 페이지당 추가 쿼리 ≤ 5건 (직전 hotfix의 N+1 회피 패턴 유지)
  - 기존 API/응답 형식 backward-compat (필드 추가만, 제거·이름변경 금지)
  - 보안: REVIEWER 권한 가드(`requireReviewer`) 보존, IDOR/Mass Assignment 방지(응답 전용 DTO)

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | 없음 (DDL 변경 0건) |
| 기존 API | `ReviewResponse`/`TaskResponse`에 `eventName` 필드 추가(backward-compat), `/v1/tasks/board` 신설 |
| 도메인 모듈 | `review`, `assignment`, `video` |
| 설정/환경 | 없음 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | TaskResponse 매핑하는 다른 클라이언트가 추가 필드 강한 검증 시 깨짐 | LOW | record + `@JsonIgnoreProperties(ignoreUnknown=true)` 보장. 필드 **추가만** 허용 |
| 2 | `/v1/tasks/board` 신규 엔드포인트 권한 누락 → 권한 상승 | MED | `@PreAuthorize("hasRole('REVIEWER')")` + 서비스 `requireReviewer(actor)` 이중 가드 (CWE-862/863) |
| 3 | enrich lookup 추가로 페이지당 쿼리 5건 초과 | MED | 기존 batch helper(`lookupCctvNames` 등) 패턴 재사용. 페이지당 추가 1건(eventName)만 |
| 4 | TaskListPage 두 시각(WORKER/REVIEWER)이 동일 컴포넌트에서 다른 데이터 로딩이라 분기 누락 시 깨짐 | MED | Phase 2(WORKER) → Phase 3(REVIEWER) 순차 진행. 각 Phase 종료 시 ui-tester 실제 화면 검증 |
| 5 | AugmentRequestPage 영상 선택 UX 변경이 사용자 흐름 단절 가능 | MED | Phase 4 시작 전 영상 선택 방식(검색·페이징·자동완성) 옵션을 사용자에게 확인 |

---

## 상세 계획

### Phase 1: ReviewResponse enrich에 eventName 추가 + ReviewListPage useVideos 제거

**목표**: 검수 목록이 BE 단일 응답으로 영상명·CCTV명·작업자·라벨 수·이벤트명을 모두 받도록 하여 FE의 `useVideos({size:999})` 호출을 제거한다. 영상 수가 만 건 이상이어도 검수 목록 진입 시 BE 페이징 호출 1건만 발생.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/src/main/java/kr/co/cudo/authoring/review/dto/ReviewResponse.java` | 수정 | `String eventName`, `String eventTypeCd` 필드 추가. 5인자 → 7인자 `from()` 오버로드 확장. 폴백: null/"" |
| `backend/src/main/java/kr/co/cudo/authoring/review/service/ReviewService.java` | 수정 | `lookupEventByVideo` private helper 추가. `list()`/`enrichOne()` 에서 eventName 매핑 |
| `backend/src/main/java/kr/co/cudo/authoring/video/repository/VideoRepository.java` | 수정 | `findEventInfoByRawSns(Collection<Long>)` 메서드 신설 — `(rawSn, eventName, eventTypeCd)` 튜플 IN-쿼리 (interface projection) |
| `frontend/src/features/review/types.ts` | 수정 | `Review` interface에 `eventName?: string`, `eventTypeCd?: string` 추가 |
| `frontend/src/pages/ReviewListPage.tsx` | 수정 | `useVideos` import·호출 제거. `videoById` Map 제거. 이벤트 컬럼이 `r.eventName` 직접 참조 |
| `backend/src/test/java/kr/co/cudo/authoring/review/ReviewControllerTest.java` | 수정 | eventName enrich 검증 케이스 추가 |
| `frontend/src/pages/__tests__/ReviewListPage.test.tsx` | 수정 | `/videos` mock 제거, `eventName` 응답 직접 mock |

**핵심 로직 / 시그니처**:
```java
// ReviewResponse.java
public record ReviewResponse(
    Long id, String cctvName, Long workerId, String workerName,
    LocalDateTime submittedAt, Long labelCount, String status,
    String eventName, String eventTypeCd,                // ← 추가
    Long videoId, String dataSttsCd, Long version, LocalDateTime updDt
) {
    public static ReviewResponse from(LsRawDataStatus stts) {
        return from(stts, null, null, null, 0L, null, null);
    }
    public static ReviewResponse from(LsRawDataStatus stts,
                                      String cctvName, Long workerId,
                                      String workerName, Long labelCount,
                                      String eventName, String eventTypeCd) { /* enrich */ }
}

// VideoRepository.java
@Query(value = """
    SELECT s.RAW_SN AS rawSn, c.EVENT_NM AS eventName, s.EVENT_TYPE_CD AS eventTypeCd
      FROM LS_DATA_RAW s
 LEFT JOIN MNG_RESOURCE_CCTV c ON c.CCTV_ID = s.VMS_CCTV_ID
     WHERE s.RAW_SN IN (:rawSns)
""", nativeQuery = true)
List<VideoEventInfoRow> findEventInfoByRawSns(@Param("rawSns") Collection<Long> rawSns);
```

**테스트 케이스** (RED):
- `검수목록_응답에_eventName과_eventTypeCd가_채워진다`
- `영상_메타_없는_경우_eventName_null_허용`
- `ReviewListPage_렌더_시_useVideos를_호출하지_않는다`
- `ReviewListPage_이벤트_컬럼_BE_eventName_직접_렌더`

**수용 기준**:
- [ ] BE: `ReviewResponse`에 `eventName`, `eventTypeCd` 필드 존재
- [ ] BE: `ReviewService.list/enrichOne` 결과에 eventName 채워짐, 메타 없으면 null 폴백
- [ ] BE: 페이지당 추가 쿼리 ≤ 5건 (기존 4건 + eventName 1건)
- [ ] FE: `ReviewListPage`가 `useVideos`를 호출하지 않음 (grep 0건)
- [ ] FE: 이벤트 컬럼이 `r.eventName` 기반 렌더 (메타 없으면 `-`)
- [ ] BE/FE 빌드 + 테스트 PASS

**의존성**: 없음 (Phase 2와 동시 진행 가능)

---

### Phase 2: TaskResponse enrich + TaskListPage WORKER 시각 페이징·useVideos 제거

**목표**: WORKER 가 작업 목록 진입 시 본인 배정 task 페이지(`/v1/tasks?page=&size=20`) 호출 1건만으로 전체 row 데이터가 채워지도록 한다. WORKER 시각에서 `useVideos({size:999})` 의존을 끊는다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/src/main/java/kr/co/cudo/authoring/assignment/dto/TaskResponse.java` | 수정 | `String eventName`, `String eventTypeCd` 필드 추가 (backward-compat) |
| `backend/src/main/java/kr/co/cudo/authoring/assignment/service/AssignmentService.java` (또는 TaskQueryService) | 수정 | `listTasks` batch lookup 에 `lookupEventByVideo` 추가 (Phase 1의 VideoRepository 메서드 재사용) |
| `frontend/src/features/task/types.ts` | 수정 | `Task` interface에 `eventName?: string`, `eventTypeCd?: string` 추가 |
| `frontend/src/features/task/hooks/useTasks.ts` | 수정 (필요 시) | 페이지 size 파라미터 기본값 20 |
| `frontend/src/pages/TaskListPage.tsx` | 수정 | **WORKER 분기**에서 `taskParams.size: 20` + 페이징 UI. WORKER 시각의 `useVideos` 호출 제거 (REVIEWER 시각의 useVideos 는 Phase 3에서 제거) |
| `backend/src/test/java/.../assignment/*Test.java` | 수정 | TaskResponse eventName 검증 |
| `frontend/src/pages/__tests__/TaskListPage.test.tsx` | 수정 | WORKER 케이스 `eventName` 응답 mock, `useVideos` 미호출 단언 |

**핵심 로직 / 시그니처**:
```java
public record TaskResponse(
    Long id, Long videoId, String cctvName,
    Long workerId, String workerName,
    Long reviewerId, String reviewerName,
    String status, LocalDateTime assignedAt, Long firstSrcSn,
    String eventName, String eventTypeCd     // ← 추가
) { ... }

// AssignmentService.listTasks(...)
// 기존 cctvName/workerName 매핑 후 → videoIds 로 lookupEventByVideo 추가
Map<Long, VideoEventInfo> eventMap = lookupEventByVideo(videoIds);
```

**TaskListPage WORKER 분기 변경**:
```tsx
// 변경 전: { page: 0, size: 999, workerId }  + useVideos({ size: 999 })
// 변경 후: { page: workerPage, size: 20, workerId }  + useVideos 는 isReviewer 분기 내에서만
```

**테스트 케이스** (RED):
- `TaskResponse_응답에_eventName_eventTypeCd_채워진다`
- `미배정_영상은_eventName_null_허용`
- `WORKER_TaskListPage_렌더_시_useVideos_미호출`
- `WORKER_taskParams_size_20으로_페이징_호출`

**수용 기준**:
- [ ] BE: `TaskResponse`에 `eventName`, `eventTypeCd` 필드 (record, null 허용)
- [ ] BE: AssignmentService 가 페이지당 eventName lookup 1회 추가 (총 ≤ 5건)
- [ ] FE: WORKER 시각의 TaskListPage 가 `useVideos` 미호출
- [ ] FE: WORKER 시각이 size 20 페이징 사용 (페이지네이션 UI 노출)
- [ ] BE/FE 빌드 + 테스트 PASS

**의존성**: 없음 (Phase 1과 동시 진행 가능)

---

### Phase 3: REVIEWER 작업 목록 통합 BE 엔드포인트 (`/v1/tasks/board`) + FE 전환

**목표**: REVIEWER 의 작업 목록("처리 완료 영상 + left-join task")을 BE에서 페이징으로 처리하는 통합 엔드포인트를 신설한다. FE 의 `useVideos({size:999})` 호출을 완전히 제거한다. 미배정 영상도 포함되어 배정 액션이 가능해야 한다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/src/main/java/kr/co/cudo/authoring/assignment/controller/TaskBoardController.java` | 생성 | `GET /v1/tasks/board` Controller. `@PreAuthorize("hasRole('REVIEWER')")` |
| `backend/src/main/java/kr/co/cudo/authoring/assignment/service/TaskBoardService.java` | 생성 | "처리완료 영상 페이징 + left-join task" 비즈니스 로직 |
| `backend/src/main/java/kr/co/cudo/authoring/assignment/dto/TaskBoardItemResponse.java` | 생성 | row DTO — video 메타(cctvName, eventName, eventTypeCd, frameCount, capturedAt) + optional task (workerId/Name, reviewerId/Name, status, assignedAt, firstSrcSn) |
| `backend/src/main/java/kr/co/cudo/authoring/video/repository/VideoRepository.java` | 수정 | `findByStatusOrderByCapturedAtDesc(String, Pageable)` 페이징 메서드 신설 (기존 있으면 재사용) |
| `frontend/src/features/task/hooks/useTaskBoard.ts` | 생성 | `useQuery` 훅. queryKey `['task-board', params]` |
| `frontend/src/features/task/types.ts` | 수정 | `TaskBoardItem` interface 추가 |
| `frontend/src/pages/TaskListPage.tsx` | 수정 | **REVIEWER 분기**에서 `useTaskBoard({page, size:20, status:'COMPLETED'})` 사용. `useVideos` 호출 완전 제거 |
| `backend/src/test/java/.../assignment/TaskBoardControllerTest.java` | 생성 | 보안·페이징·left-join 검증 |
| `frontend/src/pages/__tests__/TaskListPage.test.tsx` | 수정 | REVIEWER 케이스에서 `/tasks/board` mock, `useVideos` 미호출 단언 |

**핵심 로직 / 시그니처**:
```java
@RestController
@RequestMapping("/v1/tasks/board")
@RequiredArgsConstructor
public class TaskBoardController {
    private final TaskBoardService service;

    @GetMapping
    @PreAuthorize("hasRole('REVIEWER')")
    public ApiResponse<Page<TaskBoardItemResponse>> list(
            @RequestParam(defaultValue = "COMPLETED") String status,
            @PageableDefault(size = 20, sort = "capturedAt", direction = DESC) Pageable pageable,
            @AuthenticationPrincipal TokenClaims actor) {
        return ApiResponse.ok(service.list(status, pageable, actor));
    }
}

// TaskBoardService.list(...)
//   1) videoRepo.findByStatusOrderByCapturedAtDesc(status, pageable)
//   2) videoIds = page.content().map(Video::getId)
//   3) batch lookup: assignments(LABELER) + workerNames + reviewerNames + eventName (Phase 1 재사용)
//   4) page.map(v -> TaskBoardItemResponse.from(v, taskByVideoId.get(v.id), ...))
```

**TaskListPage REVIEWER 분기 변경**:
```tsx
// 변경 전:
//   if (isReviewer) const { data: videosPage } = useVideos({ size: 999 });
//   completedVideos = videos.filter(v => v.status === 'COMPLETED')
//   completedVideos.map(v => { const task = taskByVideoId.get(v.id); ... })
// 변경 후:
//   const { data: boardPage } = useTaskBoard({ page, size: 20, status: 'COMPLETED' });
//   rows = boardPage?.content ?? []
```

**테스트 케이스** (RED):
- `GET_v1_tasks_board_status_COMPLETED_size_20_정상_페이징_응답`
- `미배정_영상도_포함_left-join_task_null`
- `REVIEWER_외_역할은_403`
- `페이지당_쿼리_5건_이내`
- `REVIEWER_TaskListPage_useVideos_미호출_useTaskBoard_사용`

**수용 기준**:
- [ ] BE: `GET /v1/tasks/board?status=COMPLETED&page=0&size=20` 정상 응답
- [ ] BE: REVIEWER 외 403, `requireReviewer(actor)` 이중 가드
- [ ] BE: 응답에 video 메타 + optional task 정보 enriched
- [ ] BE: 페이지당 추가 쿼리 ≤ 5건
- [ ] FE: REVIEWER 시각 TaskListPage 가 useTaskBoard 사용, useVideos 호출 0건
- [ ] BE/FE 빌드 + 테스트 PASS

**의존성**: Phase 2 완료 후 (TaskResponse·eventName lookup 패턴 재사용)

---

### Phase 4: AugmentRequestPage 영상 선택 패턴 재설계

**목표**: AugmentRequestPage의 `useVideos({size:999})` 호출이 영상 수 만 건 시 무력화되지 않도록 영상 선택 UX 를 페이징·검색 기반으로 전환한다.

**선결 결정**: Phase 4 시작 시 사용자에게 영상 선택 UX 옵션 확인:
1. 검색 + 페이징 (영상명 입력 → 페이징 결과, 다중 선택 칩)
2. 자동완성 콤보박스 (입력 시 BE 검색 API 호출)
3. 영상 리스트 페이징 (기존 size:999 → size:20)

기본 방향(미응답 시): 옵션 3 — 가장 변경 적음. 검색 UX 강화는 후속 PR.

**생성·수정 파일** (옵션 3 기준):
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/pages/AugmentRequestPage.tsx` | 수정 | `useVideos` size 999 → size 20 + 페이지 state + 페이지네이션 UI |
| 기존 `VideoController` | 변경 없음 | 이미 페이징 지원 |
| `frontend/src/pages/__tests__/AugmentRequestPage.test.tsx` | 수정 | 페이징 mock |

**테스트 케이스** (RED):
- `AugmentRequestPage_영상_목록_size_20_페이지_로드`
- `다음_페이지_클릭_시_BE_호출_page=1`

**수용 기준**:
- [ ] FE: AugmentRequestPage 가 size:999 호출 금지
- [ ] 화면에서 페이지 이동 가능
- [ ] 테스트 PASS

**의존성**: Phase 1~3과 독립. 사용자 UX 결정에 따라 작업량 변동.

---

## 실행 가이드

1. `/cc-tdd Phase 1: ReviewResponse enrich에 eventName 추가 + ReviewListPage useVideos 제거`
2. `/cc-tdd Phase 2: TaskResponse enrich + TaskListPage WORKER 시각 페이징·useVideos 제거`
3. `/cc-tdd Phase 3: REVIEWER 작업 목록 통합 BE 엔드포인트 + FE 전환`
4. `"Phase 4 진행해줘"` — AugmentRequestPage UX 옵션 사용자 확인 후 구현

Phase 1·2는 도메인 분리 → 동시 진행 가능. Phase 3은 Phase 2 패턴 재사용이라 순차. Phase 4는 별도 영역.
