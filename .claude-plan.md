# 구현 계획: 1차 검증 이슈 74건 수정 (CRITICAL 2 + HIGH 26 + MEDIUM 46)

> 생성: 2026-07-25 | 유형: ISSUE (규모 XL, BE 중심 + 일부 FE/DB) | 상태: 착수 대기
> 🎯 목표: 1차 검증(A~E 878건)에서 나온 결함 74건을 **뿌리 단위로** 해소해, 인증 우회가 막히고 · PII 격리가 파생영상 포함 fail-closed 가 되며 · 승인 상태가 조용히 강등되지 않고 · 관제 통지가 실측값을 실어 나르고 · 외부 연동이 목업서버를 실제로 경유하는 상태로 만든다.

## Phase 진행 현황

- [x] Phase 1: 웹훅 인증 우회 차단 — HMAC 경로 정규화 + 시크릿 + replay/rate-limit (7건) ✅ PASS
  > 구현: `common/security/webhook/` 신규 12파일(`WebhookProtectedPaths`·`WebhookGuardedRequest`·`WebhookGateInterceptor`·`WebhookGateConfig`·`WebhookNonceStore`·`WebhookRateLimitStore`·`JdbcWebhookGuardStore`·`WebhookRateLimiter`·`ClientIpResolver`·`WebhookIpAllowlist`·`WebhookCidrParser`·`WebhookConfigProfiles`·`WebhookGuardPurgeJob`) + `HmacWebhookFilter`·`SecurityConfig`·컨트롤러2 + **V131** + `application-stg.yml` + env 4벌/compose + v2-wiki 3종 + onprem 문서
  > 사이클: 구현 → QA 5종(HIGH 5) → DEV_FIX 1차(16항목) → 적대검증 WEAK(HIGH 2) → DEV_FIX 2차 → 적대검증 **BROKEN**(신규 HIGH 3) → **REDESIGN**(PM 설계 오류 정정) → 적대검증 **WEAK**(HIGH 0)
  > 테스트: 3013 → **3080** (회귀 0). 위양성 테스트 **3건 폐기**
  > 실동작 검증 17항목 PASS → `docs/검증결과/2026-07-25/2차/Phase1-웹훅인증-부분재검증.md`. **`%61ug` 무인증 침투 200→401**, 동일 서명 인코딩 변형 재전송 409, XFF 부착 위조 15연타 6회째 429
  > ★설계 정정: "XFF 관측 시 하류 집계 비활성"(PM 지시)이 **공격자 제어 헤더로 보안 통제를 끄는 fail-open** → 게이팅 완전 제거, 가용성은 설정 강제(prd·stg 기동 차단)로 해결
  > ★별건 발견: `${VAR:?}` 는 Spring 에서 fail-fast 가 아님(`"?메시지"` 주입, 46B라 길이검사 통과) → **prd 도 커밋 placeholder 로 기동 중이었음**. `${VAR:}` + `"?"` 시작값 차단으로 수정
  > 잔여(2차 확인): ①차단 지속 최대 W+180s(javadoc 은 W+120s 단정, 가용성) ②`liveRowsSurvivePurge_*` IT 가 `assumeTrue` 로 초록 skip 가능(위양성 안전망 구멍) ③`isProxyAware()` 죽은 API ④분경계 플레이키 3건 ⑤슬라이딩 윈도우 전환 ⑥`TIMESTAMPTZ` 전환(V132) ⑦VLM/프록시 CIDR 실값 주입
- [x] Phase 2: 권한·인가 표면 정리 — 자가부여·role=null·dev토큰·스트림 인가 + 로그 마스킹 배선 (8건) ✅ PASS
  > 사이클: 구현 → QA6종 → DEV_FIX1(HIGH5+MED4) → 적대 **BROKEN** → **REDESIGN** → 적대 WEAK → DEV_FIX2 → 실HTTP 4회차
  > 테스트 3080 → **3143**(회귀 0) · 재검증 `docs/검증결과/2026-07-25/2차/Phase2-권한인가-부분재검증.md`
  > ⚠ **PM 설계 오류로 Phase 1 XFF 방어를 일시 무력화**(`forward-headers-strategy`) → REDESIGN 복구 + `ForwardedHeadersConfigGuard` 로 전 축(환경변수 포함) 재도입 차단, 실측 확인
  > 🔴 **신규 발견(범위 밖·미해결)**: `GET/PUT /v1/labels/{lblSn}/attrs` IDOR — 타 영상 라벨 속성 읽기+수정 가능. 포털 공용 경로라 내부/포털 축 분기 설계 선행 필요
- [x] Phase 3: 해상도 파생 PII 격리 통합 수정 — deid base 이동 5단계 + 백필 + 위양성 테스트 정정 (9건) ✅ PASS
  > 사이클: 구현 → QA6종 → DEV_FIX1(HIGH5+GAP2+MED7) → 재QA3 → DEV_FIX2(A-1~A-6) → 캐시수정 → 적대 **BROKEN**
  > 테스트 3143 → **3204**(회귀 0) · 재검증 `docs/검증결과/2026-07-25/2차/Phase3-해상도파생PII격리-부분재검증.md`
  > **백필 실행 완료** — 파생 4건 이관, 감사 위반 0, `rawSn=19` 스트리밍 **403→206**, `V_COMPLETED_FRAME` 19 복귀, raw base 잔존 0, 재실행 멱등
  > 계획 정정 3건: 마이그레이션 **V133**(V131 오기) · 백필은 **Flyway 아닌 Java 배치**(SQL 로는 파일을 못 옮겨 `B-61` 재발) · 실패 시 **raw 유지**(NULL 이면 서빙 중 4건 즉시 404)
  > 신규범위: 동일-base prd 에서 **kind별 subtree 검사** 필요(폴백 제거만으론 `E-22` 미해소) / 범위축소: 뷰 게이트를 `SRC=DEID` 로 좁혀 **증강파생·`rawSn 5·6·13` 소실 방지**
  > 캐시 stale HIGH 2건(2노드 peer stale · evict 후 재설치) = **유예 삭제로 해소**(사용자 결정: Redis 미도입). 구 파일을 즉시 지우지 않아 stale 노드도 정상 재생 → 공유 캐시·운영 절차 불필요. 대기열=파일 마커(DB 스키마 무변경), 정리=주기 잡+백필 재실행, 유예<TTL 이면 **기동 거부**(실측 확인). 최종 테스트 **3215**
  > 잔여(무해·후속): 2노드 TTL≤5분 구 경로 서빙 · `sync=true` 미적용(`VideoStreamService` 수정 금지) · 유예 중 저장소 2배 · 오프셋 미저장 · 기존 파생 4건 왜곡 존치 · `unresolvedPresetRawSns` 12건
- [x] Phase 4: 상태머신 게이트 — 승인 강등 차단 + 동시성 직렬화 + 입력 검증 (8건) ✅ **PASS** (2026-07-27, `2차/Phase4-상태머신게이트-부분재검증.md`)
  - 사이클: 구현(4-A 3건 + 4-B 5건) → QA **BROKEN ×2** → DEV_FIX 1차 → QA **WEAK** → DEV_FIX 2차 → 실HTTP PASS
  - ⚠ **이 Phase 는 방어를 "추가"하는 작업이라 단위 GREEN 이 실경로를 보증하지 않았다** — 적대검증이 두 번 모두 "구현됐으나 실경로 미발화"를 잡음 (①배치 입구 미차단 ②FE 미전송 ③BE·FE fps 불일치)
  - 사용자 확정: C-21=버전 409(optional 하위호환) · C-22=400 거부 · D-04=승인차단+export회수 **둘 다** · **negative sample=검수자 명시 확인 시 허용** · **FE 이번 Phase 포함**
  - 교훈 갱신: 인벤토리 표 요구만으론 부족 — **"스코프 밖" 사유 자체를 코드 근거로 검증**해야 함(4-A 가 표는 냈으나 허위 사유로 수정 전체가 우회 가능했음)
  - 스키마 V134(`LS_TASK_ASSIGNMENT.VER`)·V135(`LS_DATA_SRC.LBL_VER`)·V136(`LS_DATASET_EXPORT.RTY_NMTM/RTY_DT`). BE 3278 / FE 1525 GREEN
  - **ESCALATE 없음** — 검증 중 올렸던 '반려 후 재마킹 동선 부재'는 **반려 후 마킹 시나리오 자체가 없음**(사용자 확인 2026-07-27)이라 공백 아님. `REJECTED` 차단 유지가 정답(빼면 `markRawDataCompleted` 가 ASSIGNED 로 되돌려 반려 사실 소실)
- [x] Phase 5: 관제 연동 계약 이행 — **5A ✅** 산출 트리 co-locate 복구 · **5B ✅** 통지 계약 정합 + 기존 7건 · **5C ✅** 재export 트리거 확대 + 통지 순서 보장 ✅ **PASS**
  > **5C ✅ PASS**(2026-07-27) — 빌드 GREEN **3428/0/5**(5A 3413 대비 +15, 회귀 0). DEV_FIX 2회 + 테스트 사각 보강 1회.
  > QA: code-review **FAIL**(HIGH-E) · adversarial **BROKEN**(HIGH 4) → DEV_FIX1 → adversarial **WEAK**(MED 1) → DEV_FIX2 → adversarial **WEAK**(HIGH 0) · requirements 충족 · coverage 보강권장→해소
  > ★HIGH-E(구조적): 재export 트리거가 `control-notify.enabled`(dev/stg/prd=false) 종속이라 **운영 환경에서 재생성이 아예 안 됨** → 승인 경로 구조로 대칭화(항상활성 `TaskModifiedAccumulateListener` + `@Nullable` 통지 주입 debouncer)
  > ★HIGH-A/B/C: 롤백(`VersionService`)·프레임설명·개인정보메타 3경로가 4-arg(regen=false) 발행이라 export JSON 고착 → 5-arg true
  > ★HIGH-D: export 실패에도 통지 발송 + 회수 후 재통지 없음 → 성공 시에만 통지, 회수기 `runApprovalAsync` 로 재통지
  > ★MED-1(2차): flush `@Scheduled` 가 무관한 스케줄러 토글 3곳 종속 → 전용 daemon `ScheduledExecutorService`(`dataset-export.regen-flush.enabled`). `@EnableScheduling` 추가는 게이팅 없는 Portal/TUS 잡을 깨우는 함정이라 폐기
  > **이월 → Phase 9**: 2노드 Active-Active 에서 debouncer flush 가 노드별 독립(JVM-local 윈도우) → 같은 rawSn 이 양 노드 축적 시 export·통지 2회. 구 `@Scheduled` 도 동일 기제였으나 이제 **도달 가능**해짐
  > **5A ✅ PASS**(2026-07-27) — 빌드 GREEN **3413/0/5**(+12, 회귀 0). DEV_FIX 2회(1차 A-1~A3·B-1~B5 / 2차 공백 rawFilePathNm 폴백). QA 6종: code-review PASS·DB PASS·security 차단없음·requirements GAP(문서) 해소·coverage(A-3) 해소·adversarial **WEAK**(MEDIUM 1건 DEV_FIX 2차 해소, HIGH 0). 신규 `VideoArtifactRootResolver`·`ArtifactRootTestSupport`·V138 뷰. 산출 트리 `dirname(RAW_FILE_PATH_NM)/{rawSn}/` co-locate 복구
  > ⏸ **착수 게이트** — `docs/관제팀-연동정합-확인요청-20260727.md` 의 **B-1**(픽업 방식·원본 영상 포함) · **B-3**(DB fetch 유효성) · **B-4**(JWT 발급 주체) 답변 대기. 5B 는 B-4 제외 범위에서 선행 가능
  > 📌 2026-07-27 관제 설계(LogiCraft) 대조로 **스코프 확대** — 기존 7건만으로는 통지가 404/401/422 로 죽어 무의미함이 확인됨
- [x] Phase 6: 롤백 경로 재구성 — dead branch 제거 + LBL_SN 보존 + AI메타 복원 (6건) ✅ **PASS**
  > 빌드 GREEN **3486/0/5**(+58, 회귀 0). 사이클: 구현(A1·A2·B+C 3분할) → QA4종 → **DEV_FIX-A**(PII 게이트 10곳) → **DEV_FIX-B**(동시성·멱등·정합) → **REDESIGN**(export 단일 게이트) → **FIX-D**(TOCTOU·복구경로) → adversarial **ROBUST**
  > ★사용자 확정 설계 4건: D-21=**재활성 유지 + `LS_DATA_LBL_HSTRY` 봉투에 롤백 actor·시각·대상해시**(스키마 무변경) · D-22=**옛 `LBL_SN` 그대로 복원**(native insert + `setval` + advisory lock) · D-25=**정책 반전**(신고 시 라벨·스냅샷 미삭제, `deIdntfYn='F'` 게이트로 대체) · 기존 삭제분(rawSn=8) 존치
  > ★적대검증이 3회 연속 BROKEN — 전부 **D-25 정책 반전이 파생시킨 PII 표면**이었다: ①게이트가 `getByFrame` 1곳만 배선(포털·관제·이력·diff·프레임이미지 우회) ②**export 재생성 경로가 게이트 밖**이라 PII 프레임이 `v{n+1}` 폴더로 복사+통지 ③게이트가 export 시작 1회 읽기라 **수분짜리 복사 중 신고 TOCTOU**
  > ★교훈: 게이트를 호출처마다 붙이면 반드시 샌다 → **산출 진입점 1곳 fail-closed**(`DatasetExportService.export`) + 판정 단일 원천(`DeidentReportGate`)로 정정. 마감은 RAW 락 안에서 재판정
  > 신규 `DeidentReportGate`·`DeidentReportResolvedEvent`·`LsDataLblRepositoryCustom/Impl` + **V139**(뷰 0건 행 필터). 부수 해소: 속성값 FK 위반 500(pre-existing)·승인 스냅샷 AI메타 누락·items 정렬 비결정성
  > 잔여(별건): 속성값 롤백 복원 불가(스냅샷 스키마 확장 필요) · `FrameImageController` 원본 서빙 정책 · `applyRedeidentCompletion` 은 resolve 재트리거 없음(운영자 1스텝 필요, 유실 아님)
- [ ] Phase 7: 외부 연동 실배선 — 증강 HTTP 실구현 + VLM WebClient 정책 + KPST 무결성 (10건)
- [ ] Phase 8: 증강 콜백 견고성 — 멱등·dead-letter·부분실패 계약 (6건)
- [ ] Phase 9: 2노드 Active-Active 안전성 — Quartz 클러스터링 + 원자 클레임 (4건)
- [ ] Phase 10: 잔여 — self-fill 원천 매핑 + 오토라벨 좌표정책 + 무결성·성능 (9건)

## 요구사항 요약

- **목적**: 1차 검증 결과 `docs/검증결과/2026-07-25/1차/ISSUES.md` 115건 중 실동작·보안에 영향을 주는 74건을 해소하고, 2차 재검증이 1:1 대조로 통과 가능한 상태를 만든다.
- **범위**
  - 포함: CRITICAL 2 + HIGH 26 + MEDIUM 46 = **74건**
  - 제외(out of scope): LOW 39건(ISSUES.md 에 열린 채 보존) · F/G/H 클러스터 최초 검증(2차) · `A-ISSUE-06`(HSTS 프록시 런타임 확인)
- **주요 액터**: WORKER / REVIEWER / 관제서버(통지 수신·조회) / 외부 벤더(KPST 비식별·VLM·증강, 로컬은 mock-server:9400)

### 요구 목록

| ID | 요구 | 출처 |
|---|---|---|
| R1 | ISSUES.md 74건(CRITICAL+HIGH+MEDIUM) 수정. LOW 39건은 범위 밖 | 사용자 명시(AskUserQuestion) |
| R2 | 개별 이슈가 아닌 **SUMMARY.md §3 뿌리별 그룹 단위**로 Phase 구성. 특히 "해상도 파생 raw base"는 fail-closed/fail-open 이 섞여 있어 개별 수정 시 악화 | 사용자 명시 |
| R3 | 각 Phase 종료 시 **그 그룹에 걸린 ISSUES.md 블록만 1:1 부분 재검증** + 목업서버(:9400) 실동작 확인 | 사용자 명시(AskUserQuestion) |
| R4 | **테스트 위양성 6건**(SUMMARY.md §5)을 해당 결함 Phase 안에서 함께 정정 | 사용자 명시 |
| R5 | 외부 연동은 전부 mock-server 경유. **self-fill(외부 응답 없이 값 자체 생성)은 결함** | 사용자 구속 지시(memory: `local-must-use-mock-server-no-selffill`) |
| R6 | 수정은 전부 `developer-*` Agent 위임. PM 직접 Edit 금지 | 하네스 역할 격리 |

### 수용 기준 (requirements-verifier 역추적 기준)

- [ ] **AC-1**: 74건 각각이 `ISSUES.md` 해당 블록의 "기대효과" 문장을 충족한다 (Phase별 부분 재검증에서 1:1 대조)
- [ ] **AC-2**: BE(`./gradlew cleanTest test`) · FE(`npm run build && npm test`) 전건 GREEN, 회귀 0
- [ ] **AC-3**: `POST /api/v1/%61ug/callback` 및 slash·`//`·`;param`·대문자·이중인코딩 변형 **전부 401**
- [ ] **AC-4**: 해상도 파생영상의 프레임/비디오 산출물이 **deid base 하위**에 존재하고, `SRC_FILE_PATH_NM ≠ DE_IDNTF_SRC_FILE_PATH_NM`, `V_COMPLETED_FRAME` 이 원본 경로를 비식별 컬럼에 노출하지 않는다
- [ ] **AC-5**: `FrameSource` 의 DEIDENTIFIED rawBase 폴백이 제거되고, 비식별본 부재 시 fail-closed 로 동작한다
- [ ] **AC-6**: APPROVED 영상에 신규 배정/배치 재트리거를 걸어도 상태가 강등되지 않는다(409 또는 무전이)
- [ ] **AC-7**: `TASK_COMPLETED` 페이로드의 프레임수·라벨수·검수자·영상메타가 **DB 실측값**이며 하드코딩 0/null 이 없다
- [ ] **AC-8**: 증강 요청이 mock-server(:9400) 에 **실제 HTTP 요청으로 도달**하고, 그 응답 기반으로 콜백이 처리된다(내부 시뮬레이터 자체 생성 아님)
- [ ] **AC-9**: VLM 위탁이 mock-server 에 도달한다(부팅 크래시 없이)
- [ ] **AC-10**: 위양성 테스트 6건이 정정되어, 결함을 되돌리면 **테스트가 실패한다**

### 비기능 요구사항

- 보안: OWASP A01(접근통제)·A05(주입)·A07(인증) 기준. CWE-436(해석충돌)·CWE-639(IDOR)·CWE-862 재발 방지 회귀 테스트 필수
- 표준용어: 신규 DB 컬럼·테이블은 **물리명(표준단어 조합) + 타입·크기(표준도메인)** 준수 (`docs/표준용어/` CSV + LogiCraft MCP 조회 후 확정)
- 무중단: MNG_* 공유 테이블 변경 금지. LS_* 전용 테이블만 Flyway 자체 관리 (다음 버전 **V131**부터)
- 문서 동기화: 기능·계약 변경 시 `docs/v2-wiki/` 해당 페이지 같은 커밋에서 갱신

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | **V131~**: 데이터마트 뷰 재정의(`V_COMPLETED_FRAME`·`V_COMPLETED_LABEL_CHANGE`) · 해상도 파생 경로 백필 · `ls_data_raw` 자식 FK 보강 · 재시도/클레임 컬럼(선택) |
| 기존 API | `POST /api/v1/aug/callback`(인증 강화, 시그니처 불변) · `POST /v1/assignments`(APPROVED 거부 409 추가) · `POST /v1/videos/{rawSn}/resolution`(응답에 확정 상태 반영) · 관제 조회 API 페이징 추가 |
| 도메인 모듈 | `common.security` · `common.config` · `common.logging` · `video`(해상도) · `dataset.export` · `assignment` · `label` · `version` · `controlnotify` · `augment` · `batch` · `auth` |
| 설정/환경 | `.env`(`WEBHOOK_HMAC_SECRET_AUGMENT`) · `application-local.yml`(augment 오중첩 교정, VLM base-url) · `application-prd.yml`(Quartz `isClustered`) · `docker-compose.local.yml` |
| 프론트엔드 | Phase 3(파생 스트리밍 재확인) · Phase 4(동시 저장 충돌 UX) · Phase 8(증강 부분실패 표시) 범위에서 소폭 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | **`B-61` 을 "raw base 도 허용"으로 고치면 `D-46`·`E-22` PII 노출이 확대** | **HIGH** | `VideoStreamService` 를 **손대지 않는다**. 파생 산출물을 deid base 로 이동시키면 스트리밍 403 이 자동 해소. Phase 3 수용 기준에 "VideoStreamService diff 0" 명시 |
| 2 | `ResolutionFileMaterializerTest` 가 raw base 를 기대값으로 고정 → 올바른 수정이 테스트를 깨뜨려 **되돌릴 유인** 발생 | HIGH | Phase 3 안에서 테스트를 **먼저** 정정(RED) 후 프로덕션 수정(GREEN). 테스트 정정을 별도 Phase 로 미루지 않음 |
| 3 | 기존 파생 4건이 raw base 에 이미 존재 → 코드만 고치면 **기존 데이터는 여전히 노출** | HIGH | Phase 3 에 **백필 마이그레이션 + 파일 이동 배치** 포함. 이동 실패 시 fail-closed(경로 NULL 처리) |
| 4 | VLM 목업 배선이 `WebClientConfig` HTTPS-only + 사설IP 차단 때문에 **부팅 크래시** | HIGH | Phase 7 에서 정책을 프로파일 기반으로 분리(prd 는 강제 유지, local/dev 는 `KpstWebClientConfig` 와 동일 완화). 정책 완화가 prd 로 새지 않도록 `@Profile` + 기동 시 assert |
| 5 | 롤백 재구성이 `replaceFrameLabels`(delete+recreate) 를 건드려 **라벨 저장 전반에 회귀** | HIGH | Phase 6 에서 롤백 전용 경로를 분기(기존 저장 경로 시그니처 불변). 기존 라벨 저장 IT 전건 GREEN 을 수용 기준에 포함 |
| 6 | 상태머신 게이트 추가가 **정상 플로우를 막아** 배치·검수가 멈춤 | HIGH | Phase 4 에서 전이표를 먼저 문서화(허용 전이 화이트리스트) 후 적용. `rawSn=26` 전체 파이프라인 재주행을 수용 기준에 포함 |
| 7 | 74건 대규모 변경으로 Phase 간 **회귀 누적** | MED | R3(뿌리 그룹마다 부분 재검증) + Phase 시작 시 baseline 테스트 재확인. gradle 은 `cleanTest test`, 테스트 동시 실행 금지 |
| 8 | ~~통지 페이로드 필드 추가가 관제 수신측 계약 변경~~ → **전제 폐기(2026-07-27)**. 관제 실계약(API-251)은 6필드 **평면** 구조라 현 `{eventType, payload:{...}}` 2단 중첩과 **교집합 0** — 추가가 아니라 **전면 교체** | MED | 깨질 하위호환이 애초에 없음(현 페이로드를 관제가 수신하지 않음). Phase 5B 에서 페이로드·경로를 관제 계약으로 교체하고 `docs/관제팀-연동정합-확인요청-20260727.md` A 항목으로 통보 |
| 10 | ~~관제 답변 전 착수 시 재작업~~ → **해소(2026-07-27)**. 관제가 "**원본 영상 경로 하위 폴더 구축 가능**" 회신 → 5A 는 원안 co-locate 로 확정 | — | 게이트 해제. 방향 4회 정정 이력(co-locate→하드링크→별도루트→co-locate 복귀)의 종착점이므로 **이 결정을 memory 에 고정**하고 재논의하지 않는다 |
| 11 | **동적 base 경로 주입(CWE-22)** — 산출 base 가 DB 값 `RAW_FILE_PATH_NM` 에서 도출되므로, 그 값이 오염되면 산출이 임의 경로로 나감. 고정 루트 `startsWith` 가드가 더는 성립하지 않음 | **HIGH** | 3중 가드: ①허용 루트 allowlist(`STORAGE_RAW_PATH` 등) 교집합 ②`normalize()` 로 `..` 차단 ③`toRealPath()` 심링크 해석 후 **재검증**. null/빈값은 기본 루트로 새지 말고 **명시 실패**. 음성 테스트 필수 + security-reviewer 필수 |
| 11-1 | 비식별 출력 가드가 `STORAGE_DEIDENTIFIED_PATH` 하위를 강제하는데 산출 위치가 원본 base 로 이동 | **HIGH** | allowlist 를 **명시 확장**. 현재 `STORAGE_RAW_PATH == STORAGE_DEIDENTIFIED_PATH == /nas-storage` 라 **우연히 통과**할 수 있으므로, 두 설정을 분리해도 성립하는지 테스트로 고정 |
| 11-2 | 원본 영상이 있는 **관제 NAS 디렉터리에 저작도구가 쓰기** — 권한·쿼터·원본 훼손 위험 | **HIGH** | 착수 전 mkdir 가능 여부 실측(불가 시 ESCALATE). 런타임 fail-closed(승인은 롤백 안 함). **원본 파일 삭제·변경·덮어쓰기 금지**를 테스트로 단언 |
| 12 | 비식별 영상 산출 위치 변경이 **마킹 스트리밍·PII 격리 뷰**에 회귀 | MED | 경로는 `LS_DEIDENT_PROC_LOG.DE_IDNTF_FILE_PATH_NM` 값 기반이라 위치 무관 동작이 원칙 — 다만 신규만 새 위치라 **경로 혼재** 발생. 기존 건 백필 여부를 5A 에서 결정(기본: 백필 없이 신규만, 재승인 시 자연 이관) |
| 9 | Quartz 클러스터링 활성화가 기존 잡 스케줄에 영향 | MED | Phase 9 에서 `qrtz_scheduler_state` 런타임 확인 + 단일노드에서도 정상 동작 검증 후 적용 |

---

## 상세 계획

### Phase 1: 웹훅 인증 우회 차단 — HMAC 경로 정규화 + 시크릿 + replay/rate-limit

**목표**: `POST /api/v1/%61ug/callback` 을 포함한 모든 경로 변형이 401 로 차단되고, 정상 콜백은 유효 서명으로 통과한다. "정규 경로는 죽고 우회 경로만 열린" 상태를 해소한다.

**대상 이슈 (7건)**: `A-ISSUE-13`(HIGH) · `E-ISSUE-01`(**CRITICAL**) · `E-ISSUE-04`(HIGH) · `A-ISSUE-12`(MEDIUM) · `A-ISSUE-14`(MEDIUM) · `A-ISSUE-15`(MEDIUM) · `B-ISSUE-25`(MEDIUM)

> **⚠ 시나리오 분석(2026-07-25) 결과 계획 전제 3건 정정 — 착수 전 반드시 반영**
>
> 1. **`HmacWebhookFilter` 단위 테스트는 0건이 아니라 17건**이다 (`src/test/java/kr/co/cudo/authoring/webhook/HmacWebhookFilterTest.java`). 1차 `SUMMARY.md`·`ISSUES.md` 의 "0건" 서술은 **오류**(경로를 `common/security/` 로 가정해 놓침) → 문서 정정 필요. 단 **경로 변형 케이스 0건 + 전부 `MockHttpServletRequest` 기반**이라, 판정을 `ServletRequestPathUtils` 기반으로 바꾸면 **필터가 통째로 무력화돼도 17건이 전부 GREEN** 이 될 수 있다(위양성). 결함 자체는 유효.
> 2. **VLM 콜백을 HMAC 에 편입하지 않는다** — 벤더 확정 계약(IntelliVIX Video VLM API v2.0.1)이 **무서명 규격**이며 `HmacWebhookFilter:32-34` · `application.yml:395-396` · `VlmResultController` 3곳에 "2026-07-07 승인"으로 기록돼 있다. 편입 시 실 VLM 콜백 전건 401 + mock-server 에 서명 기능이 없어 로컬 E2E 도 붕괴. **`B-ISSUE-25` 는 ①IP allowlist(prd) + ②기존 `request_id` 발급 게이트 존치 + ③rate limit·본문 size cap 3계층으로 종결**하고, HMAC 은 `webhook.hmac.secret.vlm` 이 설정된 경우에만 요구하는 **opt-in** 으로 남긴다(벤더 규격 변경 시 무중단 전환). *(사용자 확정 2026-07-25)*
> 3. **노드 간 공유 상태 인프라가 이미 있다** — `ls_webhook_idempotency`(V39/V43/V78) + `PersistentWebhookIdempotencyLedger`(PostgreSQL, 비관적 락·UNIQUE 멱등). nonce·rate-limit 공유 저장소를 신규 도입하지 말고 **이 축을 재사용**한다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/src/main/java/kr/co/cudo/authoring/common/security/HmacWebhookFilter.java` | 수정 | `shouldNotFilter` 를 raw URI 정확일치 → **디코딩·정규화 경로 매칭**으로 교체 |
| `backend/src/test/java/kr/co/cudo/authoring/common/security/HmacWebhookFilterTest.java` | **생성** | 현재 단위 테스트 0건 — 우회 변형 회귀 스위트 신설 |
| `backend/src/main/java/kr/co/cudo/authoring/common/security/` (nonce 저장소) | 생성 | replay 방지용 nonce/서명 1회성 저장 (DB 또는 공유 캐시) |
| `.env` / `.env.example` | 수정 | 빈 `WEBHOOK_HMAC_SECRET_AUGMENT=` 제거 또는 실값 주입 |
| `backend/src/main/resources/application*.yml` | 수정 | 시크릿 미설정 시 **기동 실패(fail-closed)** — 조용한 401 대신 명시적 오류 |
| VLM 콜백 컨트롤러 (`/v1/vlm/callback`) | 수정 | HMAC 필터 적용 대상에 편입 + rate limit |

**핵심 로직 / 시그니처**:
```java
// HmacWebhookFilter — 경로 판정을 서블릿 정규화 경로 기준으로
@Override
protected boolean shouldNotFilter(HttpServletRequest request) {
    // AS-IS: request.getRequestURI() 정확일치  → %61ug 관통 (CWE-436)
    // TO-BE: 디코딩·정규화 경로 + PathPatternParser 매칭
    //        + 이중 인코딩·경로 순회 방지 (디코딩 후 재검증)
}
```
- 매칭은 **allowlist(보호 대상 경로)** 방식 — 신규 웹훅 추가 시 기본이 "보호됨"이어야 함
- replay: `(timestamp, signature)` 를 nonce 로 1회성 소비. 2노드 공유 필요 → JVM-local 금지
- rate limit: JVM-local → **공유 저장소 기반**으로 이전. `clientIp` 는 `X-Forwarded-For` 신뢰 체인 적용(프록시 뒤 전체 차단 방지)

**테스트 케이스** (RED 에 그대로 사용):
- `퍼센트인코딩_경로_%61ug_callback_은_401`
- `이중_퍼센트인코딩_경로_는_401`
- `대문자_trailing슬래시_이중슬래시_세미콜론파라미터_변형_전부_401`
- `유효_서명과_타임스탬프면_정상_통과`
- `시크릿이_빈값이면_애플리케이션_기동이_실패`
- `동일_서명_재전송시_윈도우_내라도_401`(replay)
- `X_Forwarded_For_뒤_클라이언트별로_rate_limit_이_독립_적용`
- `VLM_콜백도_서명_없으면_401`

**시나리오 방어 (HIGH 15건 중 필수 반영분)**:
| # | 시나리오 | 방어 |
|---|---|---|
| S-18 | **경로 정규화만으로는 부족** — 정규화 규칙이 필터/Security/MVC **3벌** 존재하는 한 불일치는 형태만 바꿔 재발 | **positive marker 이중 게이트 병행(필수)**: 컨트롤러가 "필터를 통과했다는 증거"(필터가 감싼 `CachedBodyHttpServletRequest` 래퍼 타입) 없으면 401. 경로 문자열 논쟁에서 구조적으로 벗어남 |
| S-17 | 필터가 자체 디코딩·정규화를 구현하면 컨테이너·Security firewall·MVC 와 규칙이 또 어긋남 | 직접 구현 금지 — Spring Security 와 **동일 매처 객체** 재사용 또는 `ServletRequestPathUtils` 로 MVC 와 같은 `RequestPath` 사용 |
| S-13 | 경로 판정 예외 시 기본값이 "스킵"이면 예외 유발 URI 하나로 우회 재현 | 예외·판정불가 = **보호(필터 적용)** 로 고정 + 테스트 |
| S-07 | nonce 를 JPA/트랜잭션 위에 올리면 PG UNIQUE 위반이 tx 전체 abort(25P02) → 필터 계층에 500 재생산 | `JdbcTemplate` 단문 **`INSERT … ON CONFLICT DO NOTHING` + `updateCount==0 ⇒ replay`** (예외 경로 자체를 없앰) |
| S-08 | nonce 를 서명 검증 **전에** 소비하면 무인증 공격자가 테이블을 무한 팽창(pre-auth write DoS) | 순서 고정: rate-limit → size cap → timestamp window → **서명 검증 성공 → nonce INSERT** |
| S-10 | 벤더의 **정상 재시도**를 nonce 가 401 로 거부 → 우리는 성공인데 벤더는 실패로 인지 | nonce 중복은 401 이 아니라 **200 `{"applied":false}` 또는 409** — 인증 실패와 replay 흡수를 응답으로 구분 |
| S-11 | nonce 소비 후 처리 실패 시 벤더 재시도가 막혀 **결과 영구 유실** | nonce 는 인증 계층 한정, 비즈니스 멱등은 `otsd_job_id` 계층이 담당. "nonce 소비 후 5xx" 통합 테스트 1건 필수 |
| S-22 | **`.env` 4곳이 전부 빈 값** — fail-closed 기동을 넣는 순간 로컬·검증·온프렘이 동시 기동 불가, 2노드 롤링 중 전면 중단 | **env 선반영이 코드보다 먼저**: 같은 커밋에서 `.env`·`.env.example`(3벌)·compose 에 32B 이상 값 채움. 기동 실패 메시지에 변수명·최소길이·설정위치 명시 |
| S-24 | `X-Forwarded-For` 무조건 신뢰 시 헤더 변조로 **rate limit 무한 우회**(CWE-348) | `server.forward-headers-strategy` + **신뢰 프록시 CIDR allowlist** 기반 파싱. 로컬/dev 는 XFF 신뢰 기본 off |
| S-21 | **기존 17개 테스트가 위양성** — 판정 방식을 바꾸면 필터가 무력화돼도 GREEN | 필터 단위테스트에 **"보호 대상 경로에서 401" negative 단정** 추가(스킵되면 실패) + **`MockMvc`/`@SpringBootTest` 실 Security 체인** 검증 |
| S-23 | 로컬이 안 뜨면 "빈 시크릿이면 WARN 후 통과" 임시 완화가 들어가고 prd 로 누수 | 완화 분기 금지. 불가피하면 `LocalProfileGuard` 패턴으로 local 한정 + "prd 에서 완화 켜지면 기동 실패" 테스트 |
| S-26 | `deploy/onprem/src/` 스테일 복사본이 **구버전 필터**를 담고 있어 폐쇄망 패키지에 취약 코드가 배포될 수 있음 | 패키징이 `backend/` 정본에서 복사하는지 확인. 배포 후 `%61ug` 우회 재현 검사를 릴리스 체크리스트에 고정 |
| S-14 | 침투로 생성된 오염 데이터 잔존 | **인벤토리 후 격리** (아래 별도 항목, 사용자 확정) |

**★ 설계 정정 (REDESIGN, 2026-07-25) — `A-ISSUE-15`/`B-ISSUE-25` 접근법 폐기·대체**

> **폐기되는 설계**: DEV_FIX 2차에서 PM이 지시한 *"`trusted-proxy-cidrs` 미설정 + XFF 관측 시 하류 401 집계 비활성(`isClientIpAttributable()` 게이팅)"*.
>
> **폐기 근거 (런타임 실측 확정)**: XFF 는 **공격자가 임의로 붙일 수 있는 헤더**인데 이를 보안 통제를 끄는 트리거로 삼아 **fail-open** 이 됐다. 실측:
> | 조건 | 결과 |
> |---|---|
> | XFF 없음 | `401×5 → 429×10` (정상 차단) |
> | `X-Forwarded-For: 1.2.3.4` 추가 | **15회 전부 401, 429 없음** |
>
> 게다가 이 가드는 **자기 목적(가용성)도 달성하지 못했다** — 411/413(필터 단계 실패)과 HMAC 서명 실패는 `attributable` 과 무관하게 수렴된 IP 로 집계되므로 "공격자 5회 → 정상 벤더 전건 차단"이 그대로 성립. **보안을 내주고 가용성도 못 산 순손실.**
>
> **대체 설계 (확정)**:
> 1. **`isClientIpAttributable()` 게이팅을 완전히 제거한다.** 하류 401/403 집계를 **항상** 수행. 런타임에 공격자 제어 입력으로 보안 통제를 끄지 않는다.
> 2. IP 산출은 기존 `ClientIpResolver` 로직 유지 — 신뢰 프록시 CIDR 안에서 온 요청만 XFF 해석, 밖이면 **XFF 전면 폐기 후 `remoteAddr` 사용**(이미 그렇게 동작). `none`/미설정은 "프록시 없음 = `remoteAddr` 이 곧 진짜 클라이언트"라는 뜻이므로 귀속 불가 상태가 아니다.
> 3. LB IP 수렴으로 인한 자기유발 DoS 는 **런타임 우회가 아니라 설정 강제로** 막는다 — 미설정 시 기동 차단(M-3)을 **prd 뿐 아니라 stg 에도 적용**(현재 `contains("prd")` 라 stg 가 조용히 빈 값으로 기동). 오설정은 fail-fast 로 드러내는 것이 정답이며, 보안 통제를 끄는 것으로 감추지 않는다.
> 4. 잔여 가용성 리스크(정상 벤더가 5회 실패 시 최대 2분 격리)는 **차단 지속시간 정정**(아래 R-5)으로 완화한다.

**오염 데이터 처리 (사용자 확정 — 인벤토리 후 격리)**:
1. 영향 범위 조회 SQL 로 **inventory 먼저 확정** — `raw_sn=29·31` 및 `data_aug_sn=8/9/10/16` 의 자식 행 전수(`ls_data_src`·`ls_data_lbl`·`ls_data_aug`·`ls_data_aug_lbl_map`·`ls_raw_data_status`·`ls_batch_proc_log`·`ls_dataset_export`) + `TASK_COMPLETED` 발송 이력
2. **삭제하지 않고 격리** — 상태 마킹 + 데이터마트 뷰 제외 (FK 미강제라 삭제 시 고아행 발생)
3. 정리는 Flyway 마이그레이션이 아니라 **일회성 운영 스크립트 + 실행 로그**(재실행 안전)
4. 재검증은 정리 후 **신규 rawSn** 으로 수행 — 오염 상태 위에서 PASS 판정 방지

**수용 기준**:
- [ ] AC-3 충족 — 우회 변형(`%61ug`·`%2561ug`·`AUG`·`//`·`;a=b`·trailing `/`·`..`·후행 점·`%00`) 전부 **401 또는 404 이며 DB 무변경**(before/after count 동일). **실 Security 체인(MockMvc)** 으로 확인 — 필터 단위테스트로 갈음 금지
- [ ] 유효 서명 콜백이 200 으로 처리되어 증강 상태가 정상 전이
- [ ] **VLM 콜백이 서명 없이도 정상 동작**(계약 준수) + rate limit·size cap·IP allowlist 적용
- [ ] `.env` 3벌 + compose 에 시크릿 선반영 완료 — 로컬 스택이 정상 기동
- [ ] `ISSUES.md` A-13 / E-01 / E-04 / A-12 / A-14 / A-15 / B-25 블록 기대효과 1:1 충족
- [ ] 오염 데이터 inventory 보고 + 격리 완료
- [ ] `SUMMARY.md`·`ISSUES.md` 의 "`HmacWebhookFilter` 단위 테스트 0건" 서술 정정

**의존성**: 없음 (최우선)

---

### Phase 2: 권한·인가 표면 정리 — 자가부여·role=null·dev토큰·스트림 인가 + 로그 마스킹 배선

**목표**: 인증은 됐으나 권한이 없는 사용자가 최고권한을 얻거나 남의 자원에 닿는 경로를 닫는다. 로그에 토큰·패스워드가 평문으로 나가는 것을 실제로 막는다.

**대상 이슈 (8건)**: `A-ISSUE-17`(HIGH) · `A-ISSUE-02`(MEDIUM) · `A-ISSUE-05`(MEDIUM) · `A-ISSUE-11`(MEDIUM) · `A-ISSUE-18`(MEDIUM) · `A-ISSUE-21`(MEDIUM) · `A-ISSUE-24`(MEDIUM) · `B-ISSUE-63`(MEDIUM)

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `.../auth/controller/RoleClaimController.java` + 서비스 | 수정 | 공유 정적 패스워드 단독 승격 제거 — 관리자 승인/화이트리스트 기반으로 전환 |
| — | ⚠ 방향 정정 | **`A-17` 을 "local 프로파일 전용" 으로 막는 원안 철회**(2026-07-25, PM). 근거: 프로파일 게이트는 dev/stg 의 정상 WORKER 온보딩까지 차단한다. 대신 **`allowedClaimRoles()` 를 `claim()` 최상단에 실제 연결 + REVIEWER 를 자가부여 대상에서 제거**(ISSUES.md 제안 ②) — 최고권한을 닫으면서 정상 경로는 유지 |
| `.../common/security/SecurityConfig.java` | 수정 | `role=null` INTERNAL 사용자의 내부 조회 API 전건 통과 차단 · dev 토큰 엔드포인트 permitAll 제거 |
| `.../common/security/StreamSignatureFilter.java` | 수정 | 서명 URL 의 `userNo` 바인딩을 실제 검증에 사용(재사용 차단) |
| `.../video/controller/…/stream` | 수정 | `/stream` 에 **영상 단위 배정 인가** 추가 (IDOR, CWE-639) |
| `backend/src/main/resources/logback-spring.xml` (또는 `application-local.yml` 로깅 패턴) | 수정 | `%msg` → `%mask` 로 교체해 `MaskingPatternLayout` 을 실제 파이프라인에 연결 |
| `.../common/security/LocalProfileGuardTest.java` | **생성** | 자동 회귀 테스트 전무 → 신설 |

**핵심 로직 / 시그니처**:
```java
// 역할 자가부여 — 공유 시크릿 단독 승격 금지
// AS-IS: 정적 패스워드 일치 → REVIEWER 즉시 부여
// TO-BE: 프로파일 제한(local 전용) + 계정 화이트리스트 + 감사 로그 + 공유 저장소 rate limit
//        attempts 맵 무한 증가 → TTL 캐시(Caffeine)로 교체
```
- `/stream` 인가: `rawSn` 에 대해 요청자가 (WORKER=본인 배정) 또는 (REVIEWER=전체) 인지 검증. 미배정 WORKER 는 403

**테스트 케이스**:
- `공유_패스워드만으로는_REVIEWER_승격_불가`
- `local_프로파일_외에서_자가부여_엔드포인트_404_또는_403`
- `자가부여_실패_시도_맵이_무한증가하지_않고_TTL_로_정리됨`
- `role_null_INTERNAL_사용자는_내부_조회_API_403`
- `dev_토큰_발급은_미인증_접근_불가`
- `배정되지_않은_WORKER_가_임의_rawSn_스트리밍시_403`
- `서명_URL_을_다른_userNo_가_재사용하면_403`
- `로그_출력에_토큰_패스워드가_마스킹되어_기록됨`(실제 로그 파일 검증 — 클래스 단위테스트로 갈음 금지)

**수용 기준**:
- [ ] `A-24` — 실제 local 로그 파일에 토큰·패스워드 평문이 남지 않음(실행 후 grep 으로 확인)
- [ ] 미배정 WORKER 의 타 영상 스트리밍이 403
- [ ] `ISSUES.md` A-17/A-02/A-05/A-11/A-18/A-21/A-24/B-63 블록 기대효과 1:1 충족
- [ ] 정상 사용자(배정된 WORKER, REVIEWER)의 기존 플로우 회귀 0

**의존성**: Phase 1 (같은 보안 표면, 필터 체인 충돌 방지)

---

### Phase 3: 해상도 파생 PII 격리 통합 수정 ⚠ 가장 위험

**목표**: 해상도 파생 산출물이 **deid base 하위**에 생성되어, 파생영상 스트리밍이 되살아나고(403 해소) 동시에 데이터마트·export 의 PII 격리가 fail-closed 가 된다. 기존 파생 4건도 백필한다.

> **⚠ 이 Phase 는 반드시 5단계를 한 릴리스에 묶어서 수행한다.** 개별 수정 시 `B-61`(fail-closed)과 `D-46`·`E-22`(fail-open)가 반대 방향이라 한쪽만 고치면 악화된다.
> **⚠ `VideoStreamService` 를 수정하지 않는다.** 파생을 deid base 로 옮기면 403 이 자동 해소된다.

**대상 이슈 (9건)**: `E-ISSUE-21`(HIGH) · `E-ISSUE-22`(HIGH) · `E-ISSUE-41`(HIGH) · `D-ISSUE-46`(HIGH) · `B-ISSUE-61`(HIGH) · `E-ISSUE-23`(MEDIUM) · `E-ISSUE-24`(MEDIUM) · `E-ISSUE-26`(MEDIUM) · `E-ISSUE-29`(MEDIUM)

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `.../video/service/ResolutionReservationPersister.java` (82-84) | 수정 | 비디오 출력 base 를 raw → **deid base** |
| `.../video/service/ResolutionSnapshotService.java` (177-178, `resolveSafeFile`) | 수정 | 프레임 출력 base 및 경로 안전검증 기준을 deid base 로 |
| `.../video/service/ResolutionPersistService.java` (243-247) | 수정 | `(dst, dst)` 동일값 기록 → **`SRC`/`DE_IDNTF` 컬럼 분리** |
| `.../video/service/ResolutionFileMaterializer.java` | 수정 | cleanup base 동반 이동 |
| `.../dataset/export/FrameSource.java` (68-75) | 수정 | **DEIDENTIFIED rawBase 폴백 제거** — 비식별본 부재 시 fail-closed |
| `backend/src/test/java/kr/co/cudo/authoring/video/ResolutionFileMaterializerTest.java` | 수정 | **raw base 기대값 → deid base 로 정정** (R4, 위양성 #6) |
| `backend/src/main/resources/db/migration/V131__backfill_resolution_derivative_deid_path.sql` | **생성** | 기존 파생 4건 경로 백필 + `V_COMPLETED_FRAME` 재정의 |
| 파일 이동 유틸/배치 | 생성 | 기존 raw base 산출물을 deid base 로 물리 이동, 실패 시 경로 NULL(fail-closed) |
| `.../video/service/…`(예약/확정 상태) | 수정 | `E-23` 확정 실패 시 고아 `LS_DATA_RAW` 정리 · `E-24` 확정 상태 조회 경로 |

**핵심 로직 / 시그니처**:
```java
// ResolutionSnapshotService — 출력 base 결정
// AS-IS: storageProperties.getRawPath()  ← 주석에 의도 명시돼 있음
// TO-BE: storageProperties.getDeidentifiedPath()
//        (local 은 두 값이 같지만 videos/{rawSn} 하위 규약으로 구분 — CLAUDE.md 참조)

// FrameSource:68-75 — 폴백 제거
// AS-IS: DEIDENTIFIED 인데 deid 파일 없으면 rawBase 폴백 (주석: "해상도 파생이 raw base 에 기록하므로")
// TO-BE: 폴백 없음. deid 파일 부재 시 해당 프레임 export 제외 + 사유 기록 (fail-closed)
```

**테스트 케이스**:
- `해상도_파생_비디오_산출물이_deid_base_하위에_생성됨`
- `해상도_파생_프레임_이미지가_deid_base_하위에_생성됨`
- `파생영상의_SRC_경로와_DE_IDNTF_경로가_서로_다름`
- `파생영상_orgnl_deid_export_산출물의_해시가_서로_다름`(E-41 — 현재 md5 동일)
- `비식별본이_없는_프레임은_DEIDENTIFIED_export_에서_제외되고_rawBase_로_대체되지_않음`(E-22 fail-closed)
- `해상도_파생영상_스트리밍이_200_으로_재생됨`(B-61 — VideoStreamService 무수정으로 해소)
- `V_COMPLETED_FRAME_이_원본경로를_비식별컬럼에_노출하지_않음`(D-46)
- `파생_확정_실패시_LS_DATA_RAW_고아행이_남지_않음`(E-23)
- `비16대9_원본의_종횡비가_보존되거나_문서화된_정책대로_동작`(E-26)
- `확정_게이트_3종_각각에_대한_전용_테스트가_존재`(E-29)

**수용 기준**:
- [ ] AC-4 · AC-5 충족
- [ ] **`git diff` 에 `VideoStreamService.java` 가 포함되지 않는다** (위험 #1 방어)
- [ ] `ResolutionFileMaterializerTest` 가 deid base 를 기대값으로 하고, raw base 로 되돌리면 **실패한다** (AC-10)
- [ ] 기존 파생 4건이 백필되어 `V_COMPLETED_FRAME` 에서 원본/비식별 경로가 상이
- [ ] `ISSUES.md` E-21/E-22/E-41/D-46/B-61/E-23/E-24/E-26/E-29 블록 기대효과 1:1 충족
- [ ] `rawSn=26`(정상 영상) export 가 여전히 SUCCEEDED — 폴백 제거가 정상 경로를 깨지 않음

**의존성**: 없음 (Phase 1·2와 독립. 단 규모가 커 단독 진행 권장)

---

### Phase 4: 상태머신 게이트 — 승인 강등 차단 + 동시성 직렬화 + 입력 검증

**목표**: APPROVED(검수완료) 영상이 배정·배치 재트리거로 조용히 강등되지 않는다. 동시 편집·동시 재배정이 직렬화되고, 라벨·마킹 입력이 검증된다.

**대상 이슈 (8건)**: `D-ISSUE-01`(HIGH) · `C-ISSUE-21`(HIGH) · `B-ISSUE-03`(MEDIUM) · `D-ISSUE-02`(MEDIUM) · `C-ISSUE-01`(MEDIUM) · `C-ISSUE-22`(MEDIUM) · `C-ISSUE-25`(MEDIUM) · `D-ISSUE-04`(MEDIUM)

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `.../assignment/service/AssignmentService.java` (75 `markAssigned`) | 수정 | APPROVED 무검증 강등 차단 — 재배정에만 있던 가드를 **신규 배정에도** 적용 |
| `.../batch/…` 상태 전이 (`transitionTo`/`changeStatus`) | 수정 | 무검증 setter → **허용 전이 화이트리스트** 기반 상태머신 |
| 상태 전이 정의 (신규 enum/정책 클래스) | **생성** | `LsRawDataStatus` 허용 전이표를 코드로 단일화 |
| `.../assignment/…` 재배정 | 수정 | `@Version` 낙관적 잠금 또는 DB 유니크/advisory lock 으로 직렬화 |
| `.../label/service/…`(라벨 저장) | 수정 | full-replace lost update 방어 — 버전 토큰/`@Version` 기반 충돌 감지 |
| `.../label/…` 좌표 검증 | 수정 | 이미지 경계 초과 좌표 거부(javadoc 이 이미 "차단"이라 기술) |
| `.../marking/…` MANUAL marks | 수정 | 중복 시점·영상길이 초과·음수 frameIndex 검증 |
| `.../label/…` 마스터 soft delete | 수정 | 사용 중 라벨 soft delete 시 저장 전면 차단 → 기존 라벨 유지 정책으로 완화 |
| `.../review/service/ReviewService.java` | 수정 | 라벨 0건·프레임 이미지 부재 영상 승인 차단(또는 경고 + 뷰 제외) |

**핵심 로직 / 시그니처**:
```java
// 상태 전이 화이트리스트 — 단일 진실원
// APPROVED 는 종결 상태: ASSIGNED/PENDING 으로의 하향 전이는 명시적 재검수 요청 경로에서만 허용
public void transitionTo(LsRawDataStatus target) {
    if (!TransitionPolicy.isAllowed(this.dataSttsCd, target)) {
        throw new CustomException(ErrorCode.INVALID_STATUS_TRANSITION); // 409
    }
}
```

**테스트 케이스**:
- `APPROVED_영상에_신규배정시_409_이고_상태가_강등되지_않음`(D-01)
- `APPROVED_영상에_배치_재트리거시_상태가_유지됨`(B-03)
- `동시_재배정_4병렬시_1건만_성공하고_이력이_중복되지_않음`(D-02)
- `두_작업자가_동시에_라벨_저장시_나중_저장이_앞선_라벨을_침묵_삭제하지_않음`(C-21)
- `이미지_경계를_벗어난_좌표_저장시_400`(C-22)
- `중복시점_영상길이초과_음수frameIndex_마킹은_400`(C-01)
- `사용중_마스터라벨_soft_delete_후에도_기존_프레임_라벨_저장이_가능`(C-25)
- `라벨_0건_영상은_승인되지_않거나_데이터마트_뷰에_노출되지_않음`(D-04)

**수용 기준**:
- [ ] AC-6 충족
- [ ] `rawSn=26` 전체 파이프라인(적재→비식별→마킹→배치→라벨→검수승인→export) **재주행 성공** — 게이트 추가가 정상 플로우를 막지 않음(위험 #6)
- [ ] `V_COMPLETED_VIDEO` 와 `LS_LABEL_VERSION` 스냅샷 불일치가 재현되지 않음
- [ ] `ISSUES.md` D-01/C-21/B-03/D-02/C-01/C-22/C-25/D-04 블록 기대효과 1:1 충족

**의존성**: 없음

---

### Phase 5: 관제 연동 계약 이행 — 5A 산출 트리 재편 + 5B 통지 계약 정합

> **⚠ 스코프 확대 근거 (2026-07-27)**: LogiCraft 관제 설계(`EXTSYS-006`/`INT-015~018`/`API-251`·`285`/`ADR-067`/`ERD-010`)를 우리 코드와 1:1 대조한 결과, **기존 7건만 고쳐도 통지는 404/401/422 로 죽는다**(경로·인증·페이로드가 전부 불일치, 교집합 0). 실측값을 정확히 싣는 작업 자체가 무의미해지므로 계약 정합을 같은 Phase 에 편입한다.
>
> **✅ 착수 게이트 해제 (2026-07-27)**: 관제 측이 **"영상 원본 경로 하위에 폴더를 구축해도 된다"** 고 답변. 원본 영상을 다른 용도로 쓰지 않아 충돌 없음. → **5A 착수 가능**. 단 `gradle` 산출물 충돌 방지를 위해 **5B 완료 후 순차 진행**(동시 실행 금지).

---

#### Phase 5A — 산출 트리 재편 (원안 co-locate 복구)

**목표**: 검수 승인 산출물이 **원본 영상이 있는 디렉터리 하위 `{rawSn}/`** 한 루트에 모이고 비식별 영상이 그 안에 함께 놓여, 관제가 **경로 1개**로 전부 픽업할 수 있다.

**확정 구조** (관제 답변 반영 2026-07-27 — 원안 복구):
```
/nas-storage/.../                    ← dirname(LS_DATA_RAW.RAW_FILE_PATH_NM) — 관제 NAS
    clip-001.mp4                     ← 원본. 관제 소유. 저작도구는 읽기만·절대 변경 안 함
    {rawSn}/                         ← 저작도구가 신규 생성 (rawSn == job_id)
        deid/{비식별영상}             ← 파일명 고정 아님(⚠ 아래). 버전 무관
        v1/orgnl/NNNN.jpg | NNNN.json
        v1/deid/NNNN.jpg  | NNNN.json
        v2/orgnl/NNNN.jpg | NNNN.json
        v2/deid/NNNN.jpg  | NNNN.json
```

**파일명 규칙 (사용자 확정 2026-07-27)** — 구 `frame-{N}.jpg` 접두사 형식 **폐기**:
- `NNNN` = **`LS_DATA_SRC.FRM_NO`(추출 순번) 를 4자리 zero-pad** (`String.format("%04d", frmNo)`)
- `FRM_NO` 는 **0부터 시작**(실측) → `0000.jpg` 가 첫 프레임. 1-based 로 바꾸지 않는다(DB 값과 일치 유지)
- **10000 이상은 자연 확장**(`10000.jpg`) — 상한을 코드에 박지 않는다
- **왜 4자리인가**(3자리에서 상향): `%0Nd` 는 최소 폭이라 초과분이 잘리진 않지만, 폭이 바뀌는 순간 **사전순 정렬이 깨진다**(3자리면 `1000.jpg` < `999.jpg`). 실측 최대 `FRM_NO` = 338(rawSn=11)이나 목표가 영상 5,000건·30초 이상이고 자동 마킹이 프레임 간격 기반이라 여유를 둔다 — 정렬 파손 지점을 999 → 9999 로 밀어둔 것
- 이미지·JSON은 **동일 basename 페어**(`0007.jpg` ↔ `0007.json`)
- ⚠ 파일명 생성은 **단일 지점**(예: `ExportFileNaming`)에서만 — 통지 페이로드(5B `changed_items`)와 실제 파일(5A writer)이 **같은 규칙을 공유**해야 한다. 흩어지면 관제가 받은 파일명과 디스크 파일명이 어긋난다

**★ `frame_num` 의미 교정 (사용자 확정 2026-07-27 — 결함 수정)**:

`LS_DATA_SRC` 는 두 값을 **별개 컬럼**으로 보유한다(`FfmpegFrameExtractor:229-256` 이 `create(rawSn, i, mark.frameIndex(), ...)` 로 각각 저장):

| 컬럼 | 의미 | 실측(rawSn=11 / 19) |
|---|---|---|
| `FRM_NO` | 추출 순번(루프 인덱스) | 0~338 / 0~11 |
| `VDO_FRM_NO` | **실제 영상 디코더 0-base 프레임 위치** | 0~**3380** / 0~**3300** |

- **현행 결함**: `NiaJsonBuilder:162` 가 `frame_num` 에 **`FRM_NO`(추출순번)** 를 넣고 있고, 실제 영상 위치인 `VDO_FRM_NO` 는 **export JSON 에 전혀 나가지 않는다** → 학습데이터 소비자가 JSON 만으로 원본 영상의 어느 지점인지 알 수 없음
- **교정**: `frame_num` = **`VDO_FRM_NO`**. 파일명은 그대로 `FRM_NO` 3자리(정렬·페어링 안정성 유지)
- `NiaJsonBuilder:141` 의 `fileName = "frame-" + frameNo + ".jpg"` 도 새 파일명 규칙(`%04d`)으로 교체 — **디스크 파일명과 JSON `file_name` 이 반드시 일치**해야 한다
- ⚠ `VDO_FRM_NO` 는 **nullable** — null 이면 `frame_num` 도 **null 로 내보낸다**(키는 유지). `FRM_NO` 로 폴백하면 의미가 뒤섞이므로 금지. 실측상 현재 전건 not null
- **`export_root` = `dirname(RAW_FILE_PATH_NM)`** — 영상마다 다른 **동적 base**. 고정 `labeling-path` 루트는 폐기.
- **원본 영상 파일은 복사하지 않는다** — 이미 `{rawSn}/` 의 **바로 상위 형제**로 존재하므로 co-locate 목적이 달성된다. 5GB 급 중복 저장 회피. 경로는 `V_COMPLETED_VIDEO.ORIGINAL_VIDEO_PATH` 로 계속 제공.
- **원본 영상을 삭제·변경·덮어쓰지 않는다** (R4). 산출은 반드시 신규 `{rawSn}/` 하위에만. 테스트로 단언.
- **비식별 영상은 버전 무관**이라 `v{n}` 밖 `{rawSn}/deid/` 에 둔다. 비식별 시점(적재 직후)에 `rawSn` 이 확정돼 있어 **그 시점에 경로 지정이 가능** → 링크·복사 불필요.
- ⚠ **비식별 영상 파일명은 고정이 아니다** — 우리가 지정하는 건 **디렉터리(`export_path`)까지**이고 파일명은 외부 솔루션이 정한다: mock=`deidentified.mp4`(`DeidentifyStep:367`), **KPST 실연동=`{원본stem}-mask{ext}`**(`KpstDeidentService.MASK_SUFFIX`, 예 `001.mp4`→`001-mask.mp4`) 로 **영상마다 다름**. **파일명을 조합·추측하지 말고 `LS_DEIDENT_PROC_LOG.DE_IDNTF_FILE_PATH_NM` 를 읽는다.** 관제도 고정 파일명을 기대할 수 없으므로 **A-5(`V138`) 뷰 컬럼이 관제의 유일한 탐색 수단**이 된다(중요도 상승).
- **`{rawSn}` 폴더명이 충돌 방어**: 한 디렉터리에 여러 원본 영상이 섞여 있어도 `rawSn` 이 유니크하므로 산출 루트가 겹치지 않는다.

**폐기된 대안 (기록)**:
- **별도 `labeling-path` 루트** (구현 커밋 `7f0f64b5`, 2026-07-16 ~ 현재) → **폐기**. 요구 이탈이었음이 확인됨(원안 = co-locate).
- **영상 하드링크** → 원본·비식별 둘 다 저작도구 소유가 아니라(`fs.protected_hardlinks`) 성립 불확실 → 불필요해짐(co-locate 로 목적 달성).
- **별도 `export_root` 신설** (2026-07-27 오전 안) → 관제가 co-locate 를 허용하면서 불필요해짐.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `.../dataset/export/DatasetExportPathResolver.java` | 수정 | 고정 `labelingRoot` → **동적 base `dirname(RAW_FILE_PATH_NM)/{rawSn}`**. 가드 3중(허용 루트 allowlist → `normalize()` `..` 차단 → `toRealPath()` 심링크 해석 후 재검증) |
| `.../dataset/export/ExportBaseResolver.java` (신규 예상) | 생성 | `RAW_FILE_PATH_NM` → 산출 base 도출 + 검증을 **한 곳에 집중**. DB 값을 신뢰하지 않음 |
| 비식별 `export_path` 지정부 (KPST 클라이언트) | 수정 | 출력 경로를 `dirname(원본)/{rawSn}/deid/` 로 지정 |
| 비식별 출력 경로 가드 | 수정 | `STORAGE_DEIDENTIFIED_PATH` 하위 강제 → **allowlist 확장**(원본 base 도 허용). 현재 `STORAGE_RAW_PATH == STORAGE_DEIDENTIFIED_PATH == /nas-storage` 라 우연히 통과할 수 있으므로 **설정 분리 시에도 성립하도록 명시 확장** (**security-reviewer 필수**) |
| `.../dataset/export/DatasetExportTxService.java` | 수정 | `EXPORT_PATH_NM` = **`dirname(원본)/{rawSn}`**(영상 루트, 버전 루트 아님) |
| `db/migration/V138__expose_deid_video_path_in_video_view.sql` | 생성 | `V_COMPLETED_VIDEO` 에 **비식별 영상 경로 컬럼 추가**(`LS_DEIDENT_PROC_LOG.DE_IDNTF_FILE_PATH_NM` — 현재 어느 뷰에도 미노출). 관제가 (a)폴더 픽업/(b)경로별 픽업 중 무엇을 택해도 성립하게 함 |
| `application.yml` | 수정 | `authoring.dataset-export.base-strategy: co-locate \| labeling-root` 롤백 플래그(기본 `co-locate`) |
| `docs/v2-wiki/24-dataset-export.md` · `CLAUDE.md` | 수정 | 폴더 구조·설정표·뷰 서술 동기화 (R7) |

**선행 실측 결과 (2026-07-27, local 컨테이너 — 이미 수행됨)**:

| 확인 | 결과 | 의미 |
|---|---|---|
| 쓰기 권한 | `/app/storage/raw` = `app:app drwxrwxr-x`, **mkdir OK** | co-locate 성립. dev/stg/prd NAS 는 배포 전 동일 확인 필요 |
| `RAW_FILE_PATH_NM` 형태 | 33행 중 **컨테이너 경로 27 / 호스트 절대경로 4 / 기타 2** | ⚠ **`/Users/ck/...` 4건은 컨테이너에 존재하지 않는 경로** — base 도출 불가. **기본 루트로 새지 말고 fail-closed 명시 실패** |
| null·공백 | **0건** | 하지만 방어는 유지 |
| **경로 공유** | `resolution/13/RESL_1080P/video/RESL_1080P.mp4` 를 **rawSn 5건이 공유**(20,30,33,36,39). 720P 3건·480P 4건도 동일 | `{rawSn}/` 하위 분리라 **충돌 없음**. 다만 한 디렉터리에 여러 job 산출 루트가 나란히 생기므로 관제 "폴더 통째 픽업"은 `{rawSn}` 단위여야 함 → `EXPORT_PATH_NM` 이 `{rawSn}` 까지 가리키므로 성립 |
| 파생영상 base | 파생은 `raw/resolution/{원본}/RESL_*/video/` 라 원본과 다른 트리 | 파생도 자기 영상 옆에 산출됨. 동작 정상 |

**런타임 정책**: 쓰기 실패·base 도출 실패 시 **fail-closed** — export 를 FAILED 로 기록하고 승인 트랜잭션은 롤백하지 않는다(기존 정합 불변). 조용히 기본 루트로 fallback 하지 않는다.

**테스트 케이스**:
- `승인시_산출물이_원본영상_디렉터리_하위_rawSn_에_생성됨`
- `프레임_파일명이_FRM_NO_4자리_zero_pad_형식이다`(`0000.jpg`·`0007.jpg`·`0338.jpg`)
- `FRM_NO_가_10000_이상이면_잘리지_않고_5자리로_확장된다`
- `이미지와_JSON_이_동일_basename_페어로_생성된다`
- `통지_changed_items_파일명과_실제_생성_파일명이_일치한다`(5B↔5A 규칙 단일화 검증)
- `JSON_frame_num_이_추출순번이_아니라_VDO_FRM_NO_실제영상프레임위치다`(예: 파일 `0002.json` → `frame_num=20`)
- `JSON_file_name_이_디스크_실제_파일명과_일치한다`
- `VDO_FRM_NO_가_null_이면_frame_num_이_null_이고_FRM_NO_로_폴백하지_않는다`
- `비식별_영상이_rawSn_deid_하위에_존재하고_버전폴더_밖에_있음`
- `EXPORT_PATH_NM_이_영상루트를_가리켜_한_경로로_전_버전이_커버됨`
- `RAW_FILE_PATH_NM_에_상위경로순회가_들어와도_산출이_허용루트_밖으로_안나감`(음성)
- `심링크로_허용루트_밖을_가리켜도_toRealPath_재검증에서_차단됨`(음성)
- `RAW_FILE_PATH_NM_이_null_이거나_빈값이면_산출이_기본루트로_새지_않고_명시_실패`(음성)
- `원본_영상_파일이_산출_전후로_내용도_존재도_변하지_않음`
- `같은_디렉터리에_여러_원본영상이_있어도_rawSn_별로_산출루트가_분리됨`
- `파생영상은_deid_1벌만_산출되는_기존_분기가_유지됨`
- `산출_디렉터리_생성_실패시_승인은_롤백되지_않고_export만_FAILED`

**수용 기준**:
- [ ] `dirname(원본영상)/{rawSn}/` 아래에 비식별 영상 + 전 버전 프레임/JSON 이 모두 존재
- [ ] `V_COMPLETED_VIDEO.EXPORT_PATH_NM` 이 영상 루트를 담고, 그 하위만으로 관제 픽업이 성립
- [ ] 비식별 영상 경로가 뷰로도 노출됨 (픽업 방식 (b) 대비)
- [ ] 경로 조작 음성 테스트 전건 차단, **원본 영상 불변** 단언 통과
- [ ] 롤백 플래그로 구 구조(`labeling-root`) 환원 가능
- [ ] 기존 `./storage/labeling` 산출물은 **이관하지 않음** — 재승인 시 새 위치에 재생성(재생성 정책이 이미 "승인마다 전량 재생성")

**의존성**: **Phase 5B 완료 후 순차**(동일 레포 gradle 충돌 방지) · Phase 3(PII 격리 정책 불변 확인)

---

#### Phase 5B — 통지 계약 정합 + 기존 7건 (선행 가능)

**목표**: 관제가 정의한 경로·페이로드로 통지가 실제 도달하고, 그 내용이 **DB 실측값**이며, 변경 통지가 유실되지 않는다.

**대상 이슈 (기존 7건)**: `D-ISSUE-41`(**CRITICAL**) · `D-ISSUE-42`(HIGH) · `D-ISSUE-43`(HIGH) · `D-ISSUE-47`(HIGH) · `D-ISSUE-44`(MEDIUM) · `D-ISSUE-45`(MEDIUM) · `D-ISSUE-48`(MEDIUM)

**신규 범위 (관제 계약 정합)**:
| # | 항목 | 근거 |
|---|---|---|
| N-1 | 경로 분리 — `POST /api/data-set/v2/jobs/{job_id}/notify-completed` · `.../notify-updated` (현 단일 `/api/v1/notify` 폐기) | API-251 / API-285 |
| N-2 | 완료 페이로드 **6필드 평면 교체** — `job_id`·`event_type_cd`·`lclgv_cd`·`lclgv_nm`·`duration_sec`·`image_count` (현 2단 중첩 폐기) | API-251 |
| N-3 | 수정 페이로드 `changed_items{images[],jsons[]}` — `SRC_SN`→`FRM_NO`→`frame-N.jpg\|.json` 변환 | API-285 |
| N-4 | 최초/재통지 분기 — 최초 `completed`, 재승인 `updated` (현재는 매 승인 completed 발행 → 관제 409) | API-251 409 계약 |
| N-5 | `resilience4j.retry.controlNotify` 에 **4xx `ignore-exceptions`** — 409/422 가 5회 재시도 후 DEAD_LETTER 로 쌓이는 것 차단 | 실측 `application.yml` |
| N-6 | `image_count` 를 **`COUNT(LS_DATA_SRC)`** 로 조달 — export `@Async` 와 같은 이벤트를 소비해 `FRAME_CNT` 가 아직 없는 경합 회피 | 실측 |
| N-7 | `job_id` = `CAST(RAW_SN AS VARCHAR)` | 관제 확인 대기(A-5) |

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `.../common/client/ControlNotifyClient.java` | 수정 | 단일 경로 상수 → **경로 2종 분리**, 응답 202 수용 |
| `.../controlnotify/dto/TaskCompletedPayload.java` | **교체** | 6필드 평면 구조 (2단 중첩 폐기 — 위험 #8 정정) |
| `.../controlnotify/dto/TaskModifiedPayload.java` | **교체** | `changed_items{images[],jsons[]}` |
| `.../controlnotify/service/ControlNotifyService.java` (93-115) | 수정 | `totalFrames=0, labeledFrames=0, reviewerName=null` **하드코딩 제거 → DB 실측 조회**(D-41) + 최초/재통지 분기(N-4) |
| `.../controlnotify/service/ControlNotifyDebouncer.java` | 수정 | `srcSn=null` 변경 통지 **NPE 유실** 수정(D-43) + 프레임↔변경종류 **페어링 복원**(D-42, 현 평행 Set 폐기) |
| `.../controlnotify/event/ChangeType.java` + 발행부 | 수정 | `LABEL_ADDED` dead 계약값 실제 발행 배선(D-44) |
| `.../controlnotify/controller/TaskQueryController.java` + `TaskQueryService` | 수정 | 라벨/프레임 조회 **페이징·상한**(D-45, CWE-770) |
| `application.yml` (`resilience4j.retry.instances.controlNotify`) | 수정 | 4xx `ignore-exceptions`(N-5) |
| `db/migration/V137__remove_label_body_from_change_view.sql` | **생성** | `V_COMPLETED_LABEL_CHANGE` 라벨 본문 제거(D-47) — ※ V132 → **V137**(V134~136 선점됨) |
| `.env` / `application-local.yml` | 수정 | `CONTROL_NOTIFY_ENABLED=true` 실동작 검증(D-48) |

**테스트 케이스**:
- `완료통지가_notify_completed_경로로_6필드_평면_바디를_보낸다`(N-1·N-2)
- `재승인은_notify_updated_경로로_changed_items_를_보낸다`(N-3·N-4)
- `changed_items_파일명이_SRC_SN_이_아니라_frame_FRM_NO_형식이다`(N-3)
- `409_응답이_재시도되지_않고_데드레터로_쌓이지_않는다`(N-5)
- `image_count_가_export_완료여부와_무관하게_실측_프레임수를_싣는다`(N-6)
- `TASK_COMPLETED_페이로드의_프레임수_라벨수가_DB_실측값과_일치`(D-41)
- `검수자명이_null_하드코딩이_아니라_실제_승인자`(D-41)
- `srcSn_이_null_인_영상단위_변경통지가_디바운서에서_유실되지_않음`(D-43)
- `TASK_MODIFIED_가_프레임ID와_변경종류를_페어로_전달`(D-42)
- `라벨_추가시_LABEL_ADDED_가_실제로_발행됨`(D-44)
- `관제_조회_API_가_페이징_없이_전건을_반환하지_않음`(D-45)
- `V_COMPLETED_LABEL_CHANGE_에_라벨_좌표_본문이_없음`(D-47)

**수용 기준**:
- [ ] **AC-7** 충족 — 하드코딩 0/null 이 코드에서 사라짐
- [ ] 통지가 관제 계약 경로·바디로 발신됨 (mock 수신 검증)
- [ ] **409 는 데드레터를 만들지 않음**(자기치유로 `updated` 전환). ~~422 포함~~ → **사양 정정(2026-07-27)**: 422 는 *우리 페이로드가 관제 스키마 검증에 걸린 것*이라 재시도해도 영구 실패 → **재시도는 억제하되 dead-letter 는 남겨야** 운영자가 우리 쪽 버그를 발견한다. 조용히 폐기하면 통지 유실이 은폐됨. 현행 구현(재시도 0회 + 폴백큐 적재 → 소진 시 dead-letter)이 정답
- [ ] `CONTROL_NOTIFY_ENABLED=true` 상태에서 통지 빈 7종 등록 + `/v1/tasks/**` 정상 응답(D-48)
- [ ] `ISSUES.md` D-41/42/43/44/45/47/48 블록 기대효과 1:1 충족
- [ ] `docs/v2-wiki/` 통지 계약 문서 동기화

**제외**: 인증(`x-access-token`) 적용 — 사용자 확정 "차후 진행". **JWT 는 관제 발급분 사용**(저작도구는 발급 기능 없음, 관제 ADR-027 표현 정정 요청 = 질의 B-4)

**의존성**: Phase 4 (승인 상태가 안정돼야 통지 시점이 정확)

> 구현 완료 (2026-07-27) — 빌드 GREEN **3363건 / 실패 0 / 스킵 5**. 마이그레이션 **V137**(스키마 추가 없음). 신규 `ExportFileNaming`·`ControlNotifyPayloadFactory`·`FrameChangeSet`·`ControlNotifyStatusException`·`MngExLocalGovRepository` + `mock-server/app/routers/control.py`.
> QA: code-review CONDITIONAL PASS(HIGH 시나리오 7/7) · api PASS · db CONDITIONAL PASS · security 차단없음 · requirements **전체 충족** · adversarial **WEAK**(HIGH 0). DEV_FIX 2회(한도 내).
> 이월 MEDIUM 3건 → Phase 5C 에서 처리: ①`buildModified` 프레임 필터가 "한쪽 벌만 보유"(증강 파생 `AugmentExtractPersist:103-106`, deid=null)를 못 걸러 관제 404 가능 ②폴백 큐 **재조립 경로에서 `exportRegenerated` 플래그 소실** → 재시도 시 무조건 전 프레임 발송 ③`claimAndSendIsolated` 가 소실 윈도우를 `debounce.flush`(성공)로 계상해 `control.notify.dropped` 가 0 유지
> 이월 LOW: `FrameChangeSet.changeTypes` 가 전송 계약에 미반영(페어링 보존이 사실상 무용) · `ChangeType.java:9` javadoc 이 삭제된 `TaskModifiedPayload#changeTypes()` 참조

---

#### Phase 5C — 재export 트리거 확대 + 통지 순서 보장 (2026-07-27 신설)

**목표**: 승인 후 라벨을 수정하면 export 가 **새 버전으로 재생성**되고, **그 완료 후에** 통지가 나가 관제가 픽업한 파일이 항상 최신이다.

> **★ 요구 출처 (사업 요구사항 원문 — 사용자 제시 2026-07-27)**
> ```
> o 기 구축된 데이터 마트 연계 관리기능 개발
>   - 데이터마트로 구축이 완료된 학습데이터의 라벨링 수정시 기존 데이터마트
>     학습데이터셋의 라벨링 정보 동기화 기능 개발
> ```
> → **Phase 5C 는 선택적 개선이 아니라 명시된 요구사항이다.** 미충족 = 요구 위반. `CLAUDE.md` 도 "데이터마트 동기화(SFR-08)는 검수완료 후 수정 시 `TASK_MODIFIED` 통지로 연계"로 이미 매핑해 두었으나, **파일이 재생성되지 않으면 '동기화'가 성립하지 않는다**(관제가 픽업해도 수정 전 내용).

> **정책 반전 근거 (사용자 확정 2026-07-27)**: 구 정책(2026-07-24)은 "재생성은 승인 시점에만, 승인 후 편집분은 다음 재승인에서 재산출"이었다. 그러나 위 요구("라벨링 정보 동기화")와 새 통지 계약 `TASK_MODIFIED.changed_items`(**변경 파일명 목록**)를 함께 놓고 보면, 재생성을 안 하면 동기화 요구 자체가 미충족이다. 사용자가 **저장소 증폭을 감수하고 정합을 택함**.

**범위**
| # | 항목 |
|---|---|
| C-1 | **승인 후 라벨 수정 → 재export**(새 버전 폴더 `v{n+1}`, 이미지 2벌 포함 — 승인 경로와 동일 산출) |
| C-1b | **승인 후 촬영환경(날씨/시간대/계절) 수정 → 재export** (사용자 확정 2026-07-27). `EnvironmentMetaService:111` 이 이미 `reFreezeApprovedSnapshot`→`materialize(rawSn, reviewCompletedAt)` 로 **동결 스냅샷을 재동결**하므로 재export 를 붙이면 새 값이 실제로 JSON `video` 블록에 반영된다. 같은 파일 javadoc `:76` 의 "**export 폴더 재생성은 트리거하지 않는다**" 서술을 **뒤집어야 함**(문서·주석 동기화 필수). `TaskModifiedEvent` 를 `exportRegenerated=true` 로 발행 |
| C-2 | **통지는 export 완료 후 발송** — 승인 경로(`TASK_COMPLETED`)·수정 경로(`TASK_MODIFIED`) **양쪽 모두**. 현재 export 는 `@Async` 라 통지가 앞설 수 있고, 관제가 `EXPORT_PATH_NM`(직전 SUCCEEDED)로 **구 버전 폴더**를 픽업한다(적대검증 N-3) |
| C-3 | `changed_items` 가 새로 생성된 `v{n+1}` 파일을 가리키고 `EXPORT_PATH_NM` 도 함께 갱신 |
| C-4 | `TaskModifiedEvent.exportRegenerated` 가 라벨 수정 경로에서도 **true** |
| C-5 | 5B 이월 MEDIUM 3건 + LOW 2건 (위 5B 하단 목록) |

**정책 근거 (사용자 확정 2026-07-27)**
- ①**검수 완료 영상의 재검수 빈도가 낮다** — 저장소 증폭 우려가 과대평가였음 ②**관제 데이터마트에 rollback 시나리오가 필요**하므로 각 버전 폴더가 **자기완결(전 프레임 이미지+JSON)** 이어야 한다. 델타만 두면 롤백이 성립하지 않는다
- **보존정책 확정 = 전 버전 보존(삭제 안 함)** — 롤백 대상이 사라지면 안 됨. 구 버전 정리 로직을 만들지 않는다
- 이 근거는 5A 의 **`EXPORT_PATH_NM` = `{rawSn}` 영상 루트**(버전 루트 아님) 결정과 정합 — 관제가 `v1`·`v2` 를 한 경로 아래에서 보고 골라야 롤백 가능

**미확정 (착수 전 확인 필요)**
- **`EvntAnnoService`(event_annotation 수정)** 만 미확정으로 남음. ⚠ **촬영환경과 사정이 다르다** — `EvntAnnoService` 는 `DatasetVideoMetaSnapshotService` 를 주입받지 않아 **재동결(materialize)을 하지 않는다**. `DatasetExportTxService:123` 이 export JSON 의 `event_annotation` 을 **승인 시점 동결본(`EVNT_ANNO_CN`)에서만** 읽으므로, **재export 만 붙이면 옛 값이 그대로 나간다**. 적용하려면 **재동결을 먼저 배선**해야 하며 그건 별도 작업이다. (촬영환경은 재동결이 이미 있어 재export 만으로 성립 — C-1b)

**의존성**: **Phase 5A 완료 후**(5A 가 export 트리 구조를 바꾸므로 그 위에서 트리거·순서를 손대야 충돌이 없다)

---

### Phase 6: 롤백 경로 재구성 — dead branch 제거 + LBL_SN 보존 + AI메타 복원

**목표**: 롤백이 실제로 동작하고 감사 추적에 남으며, 롤백 후 diff 가 왜곡되지 않고 AI 메타·트랙 ID 가 고아가 되지 않는다.

**대상 이슈 (6건)**: `D-ISSUE-21`(HIGH) · `D-ISSUE-22`(HIGH) · `D-ISSUE-23`(HIGH) · `D-ISSUE-24`(MEDIUM) · `D-ISSUE-25`(MEDIUM) · `D-ISSUE-26`(MEDIUM)

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `.../version/service/VersionService.java` | 수정 | `SAVE_REASON_ROLLBACK` dead branch 제거/재설계 — 롤백을 명시 플래그로 식별 |
| `.../label/…` `replaceFrameLabels` | 수정 | 롤백 전용 경로 분기 — **`LBL_SN` 보존 복원**(delete+recreate 대신 upsert) |
| `.../label/…` AI 메타 복원 | 수정 | `LS_DATA_LBL_AI_INFO`·`TRCK_ID` 동반 복원, 고아 방지 |
| `.../version/controller/VersionController.java` | 수정 | `DATA_SRC_SN=NULL` 스냅샷 diff 호출 시 500 → 명시적 4xx |
| `.../version/…` 비식별 신고 스냅샷 | 수정 | `DEIDENT_REPORT` 스냅샷의 list·rollback 도달 경로 확보 |
| `backend/src/test/java/.../version/VersionServiceTest.java` | 수정 | **40자 가짜 해시 픽스처 제거** (R4, 위양성 #2) — 실제 스냅샷으로 교체 |

**핵심 로직 / 시그니처**:
```java
// 롤백 — LBL_SN 보존
// AS-IS: 전량 delete → recreate (lbl_sn 재발급) → 좌표 동일한데 diff 가 REMOVED×N + ADDED×N
// TO-BE: 스냅샷의 lbl_sn 을 키로 upsert. 삭제분만 delete, 신규분만 insert
//        AI 메타·TRCK_ID 는 lbl_sn 기준으로 함께 복원
```

**테스트 케이스**:
- `롤백_수행시_LS_LABEL_VERSION_에_ROLLBACK_사유_행이_실제로_기록됨`(D-21)
- `좌표가_동일한_v1_v3_롤백_diff_가_변경없음으로_판정`(D-22 — 현재 8건 오분류)
- `롤백_후_LS_DATA_LBL_AI_INFO_와_TRCK_ID_가_고아가_되지_않음`(D-23)
- `속성값을_보유한_프레임_롤백시_FK_위반_500_이_발생하지_않음`
- `멱등_롤백_후_active_스냅샷과_실제_라벨_ID_가_일치`(D-24)
- `비식별신고_스냅샷도_목록조회와_롤백이_가능`(D-25)
- `DATA_SRC_SN_이_NULL_인_해시로_diff_호출시_500_이_아닌_4xx`(D-26)

**수용 기준**:
- [ ] `rawSn=26`(AI메타 131행 1:1 보유) 롤백 후 고아 0행
- [ ] 기존 라벨 저장 IT 전건 GREEN — 롤백 분기 추가가 저장 경로에 회귀를 내지 않음(위험 #5)
- [ ] 40자 가짜 해시 픽스처가 제거되고, dead branch 를 되살리면 테스트가 실패(AC-10)
- [ ] `ISSUES.md` D-21~D-26 블록 기대효과 1:1 충족

**의존성**: Phase 4 (라벨 저장 동시성 가드가 먼저 있어야 롤백 경합이 안전)

---

### Phase 7: 외부 연동 실배선 — 증강 HTTP 실구현 + VLM WebClient 정책 + KPST 무결성

**목표**: 증강·VLM 이 **실제로 mock-server(:9400) 에 HTTP 요청을 보내고 그 응답으로 동작**한다. 내부 시뮬레이터가 결과를 자체 생성하는 self-fill 을 제거한다. (R5 · 2차 실검증의 전제 조건)

**대상 이슈 (10건)**: `ENV-ISSUE-01`(HIGH) · `E-ISSUE-02`(HIGH) · `E-ISSUE-03`(HIGH) · `VlmClient WebClient 결함`(HIGH) · `B-ISSUE-21`(HIGH) · `B-ISSUE-01`(HIGH) · `ENV-ISSUE-02`(MEDIUM) · `B-ISSUE-22`(MEDIUM) · `B-ISSUE-24`(MEDIUM) · `B-ISSUE-84`(MEDIUM)

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `.../augment/integration/HttpExternalAugmentClient.java` | **생성** | 실 HTTP 구현 (WebClient + Resilience4j). `NoopExternalAugmentClient` 는 테스트/명시 비활성 전용으로 강등 |
| `.../augment/integration/NoopExternalAugmentClient.java` | 수정 | 기본 빈에서 제외 — 기본이 "실호출"이어야 함 |
| `.../augment/dev/DevAugmentCallbackSimulator.java` | 수정/제거 | 자체 SUCCESS 생성 제거 (self-fill 근원) |
| `.../common/config/WebClientConfig.java` | 수정 | **HTTPS-only + 사설IP 차단을 프로파일 기반으로 분리** — prd 강제 유지, local/dev 완화 |
| `.../common/config/KpstWebClientConfig.java` | 참조 | 동일 목적인데 정책이 달랐음 → 두 설정의 정책 근거를 일원화 |
| `backend/src/main/resources/application-local.yml` (95-105) | 수정 | `augment:` 블록이 `kpst:` 하위에 **오중첩** → 최상위로 교정 |
| `.../batch/…` KPST 위탁 | 수정 | **원본 존재 검증** + 산출물 무결성("존재+>0바이트" → 크기·형식 검증) |
| `.../batch/pipeline/VlmTimeseriesStep` 등 | 수정 | `B-22` 동일 rawSn 다건 마킹 시 전건 위탁 · `B-24` skip 을 DB 에 기록 |
| `mock-server/` | 수정 | `retrieve_progress.fileName` 계약을 실서버와 일치시켜 1차 회수 경로를 로컬에서 검증 가능하게(`B-84`) |

**핵심 로직 / 시그니처**:
```java
// 증강 외부 연동 — 실구현
public interface ExternalAugmentClient {
    boolean requestAugment(AugmentRequest req);  // AS-IS: Noop 이 HTTP 없이 true 반환
}
@Profile("!test")
public class HttpExternalAugmentClient implements ExternalAugmentClient {
    // WebClient POST → 외부(로컬=mock-server:9400) → 응답 기반 상태 전이
    // 재시도/타임아웃/서킷브레이커는 Resilience4j 정책
}
```
- `WebClientConfig` 정책 분리 시 **prd 로 완화가 새지 않도록** 기동 시 assert (`prd` 프로파일에서 평문 HTTP/사설IP 허용이면 기동 실패)

**테스트 케이스**:
- `증강_요청시_외부_HTTP_엔드포인트로_실제_요청이_전송됨`(ENV-01, E-02 — mock-server 액세스 로그로 확인)
- `외부_응답_없이_내부에서_SUCCESS_를_생성하지_않음`(self-fill 금지)
- `application_local_yml_의_augment_설정이_최상위에서_로드됨`(E-03)
- `VLM_클라이언트가_평문_HTTP_사설IP_목업에_로컬에서_연결됨`(부팅 크래시 없음)
- `prd_프로파일에서_평문_HTTP_VLM_URL_이면_기동이_실패`(정책 누수 방지)
- `VLM_위탁이_mock_server_에_도달하고_콜백이_LS_DATA_META_에_적재됨`(B-21)
- `동일_rawSn_에_마킹_N건이면_전건이_VLM_위탁됨`(B-22)
- `VLM_단계_skip_이_DB_에_사유와_함께_기록됨`(B-24)
- `원본_파일이_없는_영상은_비식별_완료_Y_로_승인되지_않음`(B-01 — 18바이트 스텁 차단)
- `mock_server_의_fileName_계약이_실서버와_동일해_1차_회수경로가_검증됨`(B-84)

**수용 기준**:
- [ ] AC-8 · AC-9 충족 — mock-server 액세스 로그에 증강·VLM 요청이 **실제로 기록**됨
- [ ] `DevAugmentCallbackSimulator` 의 자체 SUCCESS 생성 경로가 제거됨
- [ ] prd 프로파일 보안 정책(HTTPS-only + 사설IP 차단)이 유지됨 (위험 #4)
- [ ] `ISSUES.md` ENV-01/ENV-02/E-02/E-03/VLM WebClient/B-21/B-22/B-24/B-84/B-01 블록 기대효과 1:1 충족

**의존성**: Phase 1 (콜백 인증이 먼저 정상화돼야 실연동 콜백이 통과)

---

### Phase 8: 증강 콜백 견고성 — 멱등·dead-letter·부분실패 계약

**목표**: 증강 콜백이 동시·중복·부분실패 상황에서 계약대로 동작한다. "테스트는 GREEN 인데 프로덕션 도달 불가"인 경로를 실제 경로로 만든다.

**대상 이슈 (6건)**: `E-ISSUE-05`(HIGH) · `E-ISSUE-06`(MEDIUM) · `E-ISSUE-07`(MEDIUM) · `E-ISSUE-08`(MEDIUM) · `E-ISSUE-09`(MEDIUM) · `E-ISSUE-11`(MEDIUM)

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `.../augment/service/…` 콜백 처리 | 수정 | UNIQUE 위반 "멱등 흡수"가 PostgreSQL 에서 500 — **별도 트랜잭션 선조회 방식**으로 전환 |
| `.../augment/…` dead-letter | 수정 | `markDeadLetter()` **프로덕션 호출자 0건** → 재시도 소진 시 실제 호출 |
| `.../augment/…` 멱등 원장 | 수정 | write-only → 콜백에서 **조회하여 중복 판정** |
| `.../augment/dto/…` | 수정 | `@Size(max=1)` 단건 계약 vs 다건 distinct 로직 불일치 해소 |
| `.../augment/…` 응답 | 수정 | 생성 0건이면 200 silent no-op → 명시적 상태/사유 반환 |
| `.../augment/…` PII 게이트 | 수정 | 영상 생성 보류인데 aug 행이 `ACCEPTED`+`applied:true` → 보류 상태 반영 |
| 관련 테스트 | 수정 | **Mockito 스텁 → Testcontainers 실 DB** (R4, 위양성 #3·#4) |

**핵심 로직**:
```java
// 멱등 흡수 — PostgreSQL 은 UNIQUE 위반 시 트랜잭션 전체 abort (25P02)
// AS-IS: try { insert } catch (DataIntegrityViolation) { /* 흡수 */ }  → 같은 tx 에서 후속 쿼리 전부 실패
// TO-BE: 선조회 → 없으면 insert, 충돌 시 별도 트랜잭션에서 재조회
//        (memory: marking-batch-trigger-row-and-duration 의 동일 함정)
```

**테스트 케이스**:
- `동일_콜백_동시_2건_수신시_1건만_반영되고_500_이_발생하지_않음`(E-05, Testcontainers 실 DB)
- `재시도_소진시_dead_letter_가_실제로_기록됨`(E-06)
- `이미_처리된_요청ID_로_콜백_재수신시_멱등_원장_조회로_중복_차단`(E-07)
- `단건_계약과_다건_처리_로직이_일치`(E-08)
- `생성_0건이면_200_이_아니라_사유가_담긴_명시적_응답`(E-09)
- `PII_게이트로_보류된_증강은_ACCEPTED_applied_true_로_종결되지_않음`(E-11)

**수용 기준**:
- [ ] Mockito 스텁 기반 위양성 테스트 2건이 실 DB 테스트로 교체(AC-10)
- [ ] `markDeadLetter()` 프로덕션 호출자 ≥ 1
- [ ] `ISSUES.md` E-05~E-11 블록 기대효과 1:1 충족

**의존성**: Phase 7 (실연동이 배선돼야 콜백 경로가 실제로 돌아감)

---

### Phase 9: 2노드 Active-Active 안전성 — Quartz 클러스터링 + 원자 클레임

**목표**: 배포 토폴로지(2노드 Active-Active) 전제와 코드가 일치한다. 두 노드가 같은 작업을 중복 처리하지 않는다.

**대상 이슈 (4건)**: `B-ISSUE-81`(HIGH) · `B-ISSUE-82`(HIGH) · `B-ISSUE-02`(MEDIUM) · `B-ISSUE-83`(MEDIUM)

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/src/main/resources/application*.yml` | 수정 | Quartz `isClustered=true` + `clusterCheckinInterval` (PostgreSQL JobStore) |
| 온프렘 배포 템플릿 (`deploy/`) | 수정 | 템플릿도 `false` → `true` |
| `.../batch/…` KPST 폴링 | 수정 | **원자적 클레임** (`UPDATE ... WHERE status=? RETURNING` 또는 advisory lock) |
| `.../batch/…` `finishDownloadAndComplete` | 수정 | **멱등 가드** 추가 — 프레임 attach 이중 실행 방지 |
| `.../batch/…` 재시도 큐 | 수정 | `RETRYING` 클레임 후 노드 사멸 시 **stale 클레임 회수**(타임아웃 기반 복구) |
| 동시성 IT | **생성** | Testcontainers 2-스레드 동시 폴링/클레임 검증 |

**핵심 로직**:
```sql
-- 원자적 클레임 (두 노드가 같은 건을 집지 못하게)
UPDATE ls_deident_proc_log
   SET proc_stts_cd = 'POLLING', claim_node_nm = :node, claim_dt = now()
 WHERE log_sn = (SELECT log_sn FROM ... WHERE proc_stts_cd = 'REQUESTED'
                  ORDER BY log_sn FOR UPDATE SKIP LOCKED LIMIT 1)
RETURNING log_sn;
```

**테스트 케이스**:
- `2스레드_동시_폴링시_동일_위탁건이_한_번만_클레임됨`(B-82)
- `finishDownloadAndComplete_를_두_번_호출해도_프레임이_이중_생성되지_않음`
- `Quartz_클러스터링_활성시_qrtz_scheduler_state_에_인스턴스가_등록됨`(B-81, B-02 — 런타임 확인)
- `RETRYING_클레임_후_타임아웃이_지나면_다른_노드가_회수함`(B-83)
- `단일노드_환경에서도_기존_스케줄이_정상_동작`(회귀 방어)

**수용 기준**:
- [ ] 런타임 확인: `qrtz_scheduler_state` ≥ 1행, `qrtz_fired_triggers.instance_name ≠ "NON_CLUSTERED"`
- [ ] 동시성 IT 가 Testcontainers 실 DB 에서 GREEN (테스트 동시 실행 금지 — 순차)
- [ ] `ISSUES.md` B-81/B-82/B-02/B-83 블록 기대효과 1:1 충족
- [ ] `CLAUDE.md` 문서 드리프트 해소 — 코드와 문서가 일치

**의존성**: 없음

---

### Phase 10: 잔여 — self-fill 원천 매핑 + 오토라벨 좌표정책 + 무결성·성능

**목표**: 남은 self-fill(추정값 생성)을 원천 매핑으로 대체하고, 오토라벨이 실모델 출력으로 정상 동작하며, 참조 무결성·성능 결함을 정리한다.

**대상 이슈 (9건)**: `C-ISSUE-41`(HIGH) · `E-ISSUE-42`(MEDIUM) · `E-ISSUE-43`(MEDIUM) · `B-ISSUE-04`(MEDIUM) · `B-ISSUE-42`(MEDIUM) · `B-ISSUE-28`(MEDIUM) · `B-ISSUE-62`(MEDIUM) · `C-ISSUE-81`(MEDIUM) · `DB-ISSUE-01`(MEDIUM)

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `.../label/service/AutolabelOnlineService.java` | 수정 | **실모델 YOLO 의 경계 음수 좌표**로 온라인 오토라벨이 400 전량 거부 → 배치 경로와 검증 정책 일원화(클램핑) |
| `.../dataset/export/…`(`NiaJsonBuilder` 등) | 수정 | `time_of_day`·`season` self-fill → **관제 `MNG_CLIP_EVNT_LST.HR_TYPE_CD`/`SESN_CD` 매핑**. 원천 없으면 null 유지 + MANUAL/DERIVED 구분자 노출 |
| `.../review/…` export 상태 | 수정 | 라벨/프레임 산출 불가 영상이 승인만 되고 export FAILED → 승인 차단 또는 상태 반영(E-43) |
| `.../batch/…` 학습용 클립 스캔 | 수정 | 매 tick 전체 조회 → **페이징 + 미적재 필터** (B-04) |
| `.../batch/…` YOLO/SAM2 저장 | 수정 | 루프 내 개별 `save()` → `saveAll()`/batch insert (B-42) |
| 마킹 단계 비식별 신고 컨트롤러 | **생성** | `POST /v1/videos/{rawSn}/deident-report` 미구현(404) → 구현 (B-28) |
| `MaskRleConverter`·`CoordinateTransformer` 테스트 | **생성** | 경계/보안 가드 커버리지 0% → 신설 (C-81) |
| `db/migration/V13x__add_ls_data_raw_child_fks.sql` | **생성** | 자식 테이블 FK 보강 — 고아 행 방지 (DB-01) |
| 재검증 | — | `B-62`(HEAD 미반영으로 미검증)는 V130 반영 백엔드에서 **실동작 재확인** |

**핵심 로직**:
```java
// self-fill 제거 (R5) — 원천이 있으면 원천을, 없으면 null
// AS-IS: season = deriveFromShtDt(shtDt);  time_of_day = deriveFromHour(shtDt);
//        → rawSn=26 은 18:00 촬영인데 NGT(야간) 오분류 (7월 일몰 19:50)
// TO-BE: 관제 MNG_CLIP_EVNT_LST 의 SESN_CD/HR_TYPE_CD 를 매핑. 미보유 시 null.
//        export/뷰에 값 출처(MANUAL/DERIVED/OBSERVED) 구분자 동반
```

**테스트 케이스**:
- `실모델_YOLO_의_경계_음수좌표_응답이_온라인_오토라벨에서_거부되지_않고_클램핑됨`(C-41)
- `배치_오토라벨과_온라인_오토라벨의_좌표_검증정책이_동일`
- `season_time_of_day_가_관제_원천값에서_매핑됨`(E-42)
- `원천값이_없으면_추정하지_않고_null_을_유지`
- `export_에_값_출처_구분자가_포함됨`
- `18시_촬영_영상이_야간으로_오분류되지_않음`(E-42 실측 재현)
- `라벨_산출_불가_영상이_검수완료로_노출되지_않음`(E-43)
- `학습용_클립_스캔이_페이징으로_동작하고_이미_적재된_건을_재조회하지_않음`(B-04)
- `마킹_단계에서_rawSn_기준_비식별_신고가_가능`(B-28)
- `ls_data_raw_삭제시_자식행이_고아로_남지_않음`(DB-01)

**수용 기준**:
- [ ] `E-42` self-fill 제거 — 원천 매핑 또는 null, 추정값 생성 0
- [ ] `rawSn=26` 의 `time_of_day` 가 실제 촬영시각과 정합
- [ ] 온라인 오토라벨이 실모델 응답으로 프레임 대부분에서 성공
- [ ] `ISSUES.md` C-41/E-42/E-43/B-04/B-42/B-28/B-62/C-81/DB-01 블록 기대효과 1:1 충족

**의존성**: Phase 5 (원천 매핑이 통지·뷰와 함께 정합돼야 함)

---

## 실행 가이드

1. `/cc-tdd Phase 1: 웹훅 인증 우회 차단 — HMAC 경로 정규화 + 시크릿 + replay/rate-limit`
2. `/cc-tdd Phase 2: 권한·인가 표면 정리 — 자가부여·role=null·dev토큰·스트림 인가 + 로그 마스킹 배선`
3. `/cc-tdd Phase 3: 해상도 파생 PII 격리 통합 수정 — deid base 이동 5단계 + 백필 + 위양성 테스트 정정`
4. `/cc-tdd Phase 4: 상태머신 게이트 — 승인 강등 차단 + 동시성 직렬화 + 입력 검증`
5. `/cc-tdd Phase 5B: 관제 통지 계약 정합 — 경로·페이로드 교체 + 실측값 + 디바운서 NPE + 뷰 본문 제거` → `/cc-tdd Phase 5A: 산출 트리 co-locate 복구 — 원본영상 디렉터리 하위 {rawSn}/ + 비식별 영상 동거 + 동적 경로 가드`
6. `/cc-tdd Phase 6: 롤백 경로 재구성 — dead branch 제거 + LBL_SN 보존 + AI메타 복원`
7. `/cc-tdd Phase 7: 외부 연동 실배선 — 증강 HTTP 실구현 + VLM WebClient 정책 + KPST 무결성`
8. `/cc-tdd Phase 8: 증강 콜백 견고성 — 멱등·dead-letter·부분실패 계약`
9. `/cc-tdd Phase 9: 2노드 Active-Active 안전성 — Quartz 클러스터링 + 원자 클레임`
10. `/cc-tdd Phase 10: 잔여 — self-fill 원천 매핑 + 오토라벨 좌표정책 + 무결성·성능`

> 각 Phase 종료 시 **R3 부분 재검증**: 해당 Phase 대상 이슈의 `ISSUES.md` 블록만 1:1 대조 + 목업서버(:9400) 실동작 확인. 결과는 `docs/검증결과/2026-07-25/2차/` 에 Phase 단위로 누적.

## 검증 환경 (모든 Phase 공통)

- 스택: `docker compose` — postgres(5432) / backend(8080) / ai-server(9300) / **mock-server(9400)** / frontend(5173)
- DB 실제 스키마 **`public`** (klid_at 아님), 계정 `klid_user` / `klid_system`
- 참조 데이터 **`rawSn=26`** — 프레임 16 / 라벨 131 / APPROVED / export SUCCEEDED
- 다음 Flyway 버전 **V131**부터
- ⚠ 함정: gradle 은 `cleanTest test` 필수 · **테스트 동시 실행 금지**(test-results XML 충돌로 위양성 실패) · macOS 에 `timeout` 없음
