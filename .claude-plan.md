# CVAT-Like 라벨 마스터·속성 풀 포팅 + PJT_ID 전면 제거 (Plan)

## Context

**시작**: 2026-05-15
**갱신**: 2026-05-15 (Phase 4~5 신설 — PJT_ID 전면 제거)

**동기**:
1. 작업자(WORKER) 라벨링 시 객체명이 `Object` 고정 + 수정 불가 → CVAT 동등 라벨 시스템 풀 포팅 (Phase 1~3 완료, 후속 Phase 6~9)
2. 단일 프로젝트 전제 확정 — 멀티 프로젝트 가능성 닫힘 → 프로젝트 개념 자체 제거 (Phase 4~5)

**완료 기준 (라벨 풀 포팅)**:
- 작업자가 BBOX 그릴 때 드롭다운 또는 단축키(1~9)로 클래스 선택 가능
- 이미 그려진 객체 더블클릭 → 클래스/속성 수정
- YOLO 자동 라벨이 LS_LABEL FK 와 매칭되어 한글 이름·색상으로 표시
- `Object` fallback 텍스트 화면에서 사라짐

**완료 기준 (PJT_ID 제거)**:
- DB 에서 LS_PJT 본체 DROP + 모든 LS_PJT_*/도메인 테이블의 PJT_ID 컬럼/PK·UK 제거
- BE 17개 파일에서 `pjtId` 필드/파라미터/시그니처 제거
- FE 4개 파일에서 `pjtId` 정리
- 시드 데이터 정리
- 전체 테스트 통과 (BE/FE)

**이전 plan 보존**: `.claude-archive/plan-v2-policy-2026-05-15.md` (V2 정책 작업 완료본)

---

## 핵심 아키텍처 결정

| 항목 | 결정 |
|------|------|
| 프로젝트 개념 | **제거** — LS_PJT 본체 DROP, 도메인 테이블의 PJT_ID 컬럼 모두 삭제 |
| DDL 전략 | **혼합 (Phase 4 결정)** — LS_PJT 본체는 DROP, LS_PJT_* 접두사 테이블은 ALTER DROP COLUMN (테이블명 유지: V1 base 파일이 거대해서 rename 영향 큼). 단 LS_LABEL/LS_DATA_SET 같은 신규 테이블은 컬럼만 DROP |
| 외부 시스템 위험 | 사용자 명시: 관제서버 양방향 deprecated 상태 — LS_PJT 외부 의존 없음으로 판단 |
| 라벨 마스터 스코프 | **전역 (단일)** — UK (PJT_ID, NAME) → UK (NAME) 단독 변경 |
| 신규 테이블 | `LS_LABEL`, `LS_LABEL_ATTR`, `LS_DATA_LBL_ATTR_VAL` (Phase 1~3) |
| LS_DATA_LBL.LABEL 컬럼 | deprecated — `LABEL_ID` FK 컬럼 사용. 기존 텍스트는 일회성 마이그레이션으로 best-effort 변환 후 컬럼 유지(읽기 전용) |
| LS_LABEL_PRESET | 존속하되 활용 안 함 — BBOX/POLYGON 토글 필터로만 사용 |
| 색상 형식 | `#RRGGBB` 7자 (대문자 hex) |
| 단축키 | 1~9 (10번째 이상은 드롭다운만) — sort_no 순서 기준 |
| AI 매핑 전략 | `LS_LABEL.NAME` 과 YOLO 응답 label 의 case-insensitive 매칭. 매칭 실패 시 LABEL_ID=NULL + FE "라벨 필요" |
| 권한 | 라벨 CRUD = REVIEWER. 라벨 조회 = REVIEWER+WORKER+PORTAL_USER. 객체별 클래스 변경 = REVIEWER+WORKER |

---

## 진행 현황 (Phase Checklist)

- [x] **Phase 1**: 라벨 마스터 도메인 — `LS_LABEL` DDL + Entity/Repo/Service/Controller + CRUD API + 시드 13건 (2026-05-15 완료)
- [x] **Phase 2**: `LS_DATA_LBL.LABEL_ID` FK 연결 — V32 마이그레이션 + 기존 텍스트 best-effort 변환 (2026-05-15 완료)
- [x] **Phase 3**: 속성(Attribute) 도메인 — `LS_LABEL_ATTR` + `LS_DATA_LBL_ATTR_VAL` + CRUD (2026-05-15 완료)
- [x] **Phase 4**: PJT_ID 제거 — DB·시드 (V34 마이그레이션 + dev-seed.sql 정리) (2026-05-16 완료)
- [x] **Phase 5**: PJT_ID 제거 — BE·FE 코드 (Entity/Repo/Service/Controller/DTO/테스트 + FE 4파일) (2026-05-16 완료)
- [x] **Phase 6**: AutoLabel preset 매핑 — `YoloAutolabelStep` / `Sam2SegmentStep` 에서 label → LS_LABEL FK 룩업 (2026-05-16 완료)
- [x] **Phase 7**: FE LabelSidebar — CVAT 스타일 사이드바 + 색상 + 단축키 1~9 (2026-05-16 완료)
- [x] **Phase 8**: FE ObjectAttributePanel — 드롭다운 + 속성 편집 + 즉시 반영 + `'Object'` fallback 제거 (2026-05-16 완료, 2026-05-18 LabelingPage 통합 보강)
- [x] **Phase 9**: 통합 시나리오 검증 (`/cc-verify`) + 회귀 테스트 (2026-05-18 완료)
- [x] **Phase 10**: 신규 LS_* 테이블 DDL + 데이터 마이그레이션 (V36/V37, 코드 무수정) (2026-05-18 완료)
- [x] **Phase 11**: Entity/Repository 클래스명 일괄 변경 + Service/DTO/JPQL 적응 (2026-05-18 완료)
- [x] **Phase 12**: 시드 / 테스트 / FE 호환 정리 (2026-05-18 완료)
- [ ] **Phase 13**: 통합 검증 (`/cc-verify`) + 회귀

---

## 실행 가이드 (각 Phase 자기-완결, 다른 세션에서 재개 가능)

```
Phase 1~3 (완료)

"Phase 4 진행해줘"                                  — DB 마이그레이션 (코드 생성 없음)
/cc-tdd Phase 5: PJT_ID 제거 BE·FE 코드
/cc-tdd Phase 6: AutoLabel preset 매핑
/cc-tdd Phase 7: FE LabelSidebar
/cc-tdd Phase 8: FE ObjectAttributePanel
/cc-verify Phase 9: 통합 시나리오

"Phase 10 진행해줘"                                — V36 (신규 LS_* 테이블 7개 생성) + V37 (데이터 마이그레이션)
/cc-tdd Phase 11: Entity/Repository 클래스명 일괄 변경 + Service/DTO/JPQL 적응
/cc-tdd Phase 12: 시드/테스트/FE 호환 정리
/cc-verify Phase 13: 통합 검증
```

**세션 중단 시 재개**: `/cc-resume` → 미체크 Phase 부터 자동 진행. 또는 `/cc-tdd Phase N: ...` 로 특정 Phase 만 실행.

---

## 영향 범위

| 영역 | 변경 내용 |
|------|-----------|
| DB 스키마 | LS_PJT DROP / LS_PJT_DATA_MPNG, LS_PJT_DATA_STTS, LS_PJT_USER_AUTHRT, LS_PJT_USER_AUTHRT_HSTRY, LS_PJT_META, LS_PJT_DDLN, LS_PJT_TASK_EVENT_LOG, LS_LABEL, LS_DATA_SET 에서 PJT_ID 컬럼/PK/UK 제거 |
| BE 엔티티 | 10개 (LsPjt, LsPjtDataMpng, LsPjtDdln, LsPjtMeta, LsPjtDataStts, LsPjtUserAuthrt, LsPjtUserAuthrtHstry, LsPjtTaskEventLog, LsLabel, LsDataSet) |
| BE Repository | 2개 (LsLabelRepository 3개 메서드, LsPjtTaskEventLogRepository 1개 메서드) |
| BE Service | 1개 (LabelMasterService) |
| BE Controller | 1개 (LabelMasterController — `?pjtId=` 쿼리 파라미터 제거) |
| BE DTO | LabelMasterRequest (pjtId 필드 제거) |
| 시드 | dev-seed.sql 14 라인 |
| BE 테스트 | LabelMasterServiceTest, LabelMasterControllerTest |
| FE | types.ts, AssignModal.tsx, api.test.ts, AssignModal.test.tsx (4파일 8라인) |
| 설정/환경 | 없음 |

---

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | LS_PJT 가 관제서버에서 사용될 가능성 | HIGH | 사용자 명시 — 관제 양방향 deprecated 상태로 외부 의존 없음 판단. 마이그레이션 전 한 번 더 grep 으로 외부 참조 확인 |
| 2 | LsPjtUserAuthrt UK (PJT_ID, RAW_DATA_ID, USER_NO, TASK_TYPE_CD) 에서 PJT_ID 빠지면 단일 (RAW_DATA_ID, USER_NO, TASK_TYPE_CD) 로 UK 충분한가? | MED | 현 운영 데이터는 PJT_ID=1 단일이므로 (RAW_DATA_ID, USER_NO, TASK_TYPE_CD) UK 가 자연 키. 신규 UK 로 동일 의미 보존 |
| 3 | LS_PJT_DDLN / LS_PJT_META 처럼 LS_PJT 가 의미적 컨테이너인 테이블들이 PJT_ID 없으면 정체성 모호 | MED | "전역 마감일정", "전역 메타데이터" 로 재해석. DDLN_SEQ, META_KEY 단독 PK |
| 4 | Phase 4 마이그레이션 도중 BE 가 기동되면 Entity 매핑 오류 | HIGH | Phase 4 (DDL) → Phase 5 (Entity) 분리. Phase 4 완료 직후 BE 재시작 금지. Phase 5 동일 PR 또는 동일 작업 흐름에서 즉시 진행 |
| 5 | Flyway 마이그레이션 한 번 적용되면 rollback 어려움 | HIGH | V34 단일 파일이 아닌 여러 V 파일로 분리 (테이블별). 실패 시 V35, V36 별도 작성으로 회복 가능하게 |
| 6 | 시드 재실행 시 PJT_ID 참조로 실패 | MED | Phase 4 의 시드 정리 단계에 포함. 시드는 DROP/CREATE 후 새 INSERT 작성 |

---

## Phase 1: 라벨 마스터 도메인 (완료 ✅)

> 구현 (2026-05-15): V31 + LsLabel/Repository/Service/Controller + LabelMasterRequest/Response + 16개 신규 테스트(서비스 7 + 컨트롤러 9) + dev-seed.sql LS_LABEL 13건 + SecurityConfig 매처 조정. BE 전체 598 tests, 0 fail.

---

## Phase 2: LS_DATA_LBL FK 연결 (완료 ✅)

> 구현 (2026-05-15): V32 (LABEL_ID + FK + 인덱스 + best-effort UPDATE) + LsDataLbl entity (labelId 필드 + 4종 factory 오버로드 + updateUserContent 6-arg) + LabelItemDto/LabelResponse (labelId/labelName/color) + LabelService (LsLabel 검증 + 응답 enrichment + N+1 회피). 기존 시그니처 모두 @Deprecated 유지. 신규 테스트 18 (Entity 8 + DTO 3 + Controller 7), 전체 616 tests, 0 fail.

---

## Phase 3: 속성(Attribute) 도메인 (완료 ✅)

> 구현 (2026-05-15): V33 (LS_LABEL_ATTR + LS_DATA_LBL_ATTR_VAL, `VALUE` reserved keyword 회피로 컬럼명 `ATTR_VAL`) + Entity 2개 + Repository 2개 + LabelAttrService(정의 CRUD) + LabelAttrValueService(객체별 upsert/조회) + LabelAttrController(6개 endpoint) + DTO 4개. SecurityConfig 매처 와일드카드 확장. 신규 테스트 32, 전체 649 / 0 fail.

---

## Phase 4: PJT_ID 제거 — DB · 시드

### 목표
LS_PJT 본체 DROP + 도메인 9개 테이블의 PJT_ID 컬럼/PK/UK 제거. 시드 데이터 정리. **이 Phase 는 DDL/시드 변경만 수행하고 BE 코드는 건드리지 않는다.** Phase 5 와 같은 작업 흐름에서 즉시 이어 진행해야 BE 기동 가능.

### 생성·수정 파일

| 파일 | 동작 | 내용 |
|------|------|------|
| `backend/src/main/resources/db/migration/V34__drop_pjt_id_columns.sql` | 신규 | 9개 테이블 PJT_ID 컬럼/PK/UK/FK/INDEX 일괄 제거 |
| `backend/src/main/resources/db/migration/V35__drop_ls_pjt.sql` | 신규 | LS_PJT 본체 DROP TABLE IF EXISTS |
| `backend/src/main/resources/db/seed/dev-seed.sql` | 수정 | LS_PJT INSERT 제거 + 모든 PJT_ID 참조 컬럼 제거 + 14라인 정리 |

### 단계별 DDL (V34 상세 — 단일 파일에 트랜잭션 분리)

```sql
-- 1. LS_LABEL: UK (PJT_ID, NAME) → UK (NAME)
ALTER TABLE LS_LABEL DROP INDEX UK_LS_LABEL_NAME;
ALTER TABLE LS_LABEL DROP INDEX IDX_LS_LABEL_PJT_USE;
ALTER TABLE LS_LABEL ADD UNIQUE KEY UK_LS_LABEL_NAME (NAME);
ALTER TABLE LS_LABEL ADD KEY IDX_LS_LABEL_USE (USE_YN, SORT_NO);
ALTER TABLE LS_LABEL DROP COLUMN PJT_ID;

-- 2. LS_PJT_DATA_MPNG: PK (PJT_ID, RAW_DATA_ID) → PK (RAW_DATA_ID)
ALTER TABLE LS_PJT_DATA_MPNG DROP PRIMARY KEY, ADD PRIMARY KEY (RAW_DATA_ID);
ALTER TABLE LS_PJT_DATA_MPNG DROP COLUMN PJT_ID;

-- 3. LS_PJT_DATA_STTS: PK (PJT_ID, RAW_DATA_ID) → PK (RAW_DATA_ID)
ALTER TABLE LS_PJT_DATA_STTS DROP PRIMARY KEY, ADD PRIMARY KEY (RAW_DATA_ID);
ALTER TABLE LS_PJT_DATA_STTS DROP COLUMN PJT_ID;

-- 4. LS_PJT_USER_AUTHRT: UK (PJT_ID, RAW_DATA_ID, USER_NO, TASK_TYPE_CD) → UK (RAW_DATA_ID, USER_NO, TASK_TYPE_CD)
ALTER TABLE LS_PJT_USER_AUTHRT DROP INDEX UK_LS_PJT_USER_AUTHRT;
ALTER TABLE LS_PJT_USER_AUTHRT ADD UNIQUE KEY UK_LS_PJT_USER_AUTHRT (RAW_DATA_ID, USER_NO, TASK_TYPE_CD);
ALTER TABLE LS_PJT_USER_AUTHRT DROP COLUMN PJT_ID;

-- 5. LS_PJT_USER_AUTHRT_HSTRY: 카테고리 컬럼 제거
ALTER TABLE LS_PJT_USER_AUTHRT_HSTRY DROP COLUMN PJT_ID;

-- 6. LS_PJT_META: PK (PJT_ID, META_KEY) → PK (META_KEY)
ALTER TABLE LS_PJT_META DROP PRIMARY KEY, ADD PRIMARY KEY (META_KEY);
ALTER TABLE LS_PJT_META DROP COLUMN PJT_ID;

-- 7. LS_PJT_DDLN: PK (PJT_ID, DDLN_SEQ) → PK (DDLN_SEQ)
ALTER TABLE LS_PJT_DDLN DROP PRIMARY KEY, ADD PRIMARY KEY (DDLN_SEQ);
ALTER TABLE LS_PJT_DDLN DROP COLUMN PJT_ID;

-- 8. LS_PJT_TASK_EVENT_LOG: 카테고리 컬럼 + 인덱스 제거
ALTER TABLE LS_PJT_TASK_EVENT_LOG DROP INDEX IDX_LS_PJT_TASK_EVENT_LOG_PJT_RAW;
ALTER TABLE LS_PJT_TASK_EVENT_LOG ADD KEY IDX_LS_PJT_TASK_EVENT_LOG_RAW (RAW_DATA_ID, OCCURRED_AT);
ALTER TABLE LS_PJT_TASK_EVENT_LOG DROP COLUMN PJT_ID;

-- 9. LS_DATA_SET: 카테고리 컬럼 + 인덱스 제거
ALTER TABLE LS_DATA_SET DROP INDEX IDX_LS_DATA_SET_PJT;
ALTER TABLE LS_DATA_SET DROP COLUMN PJT_ID;
```

### 시드 데이터 정리

- `dev-seed.sql` 에서 `INSERT INTO LS_PJT` 섹션 전체 제거
- `INSERT INTO LS_PJT_DATA_MPNG`, `LS_PJT_DATA_STTS`, `LS_PJT_USER_AUTHRT`, `LS_PJT_USER_AUTHRT_HSTRY`, `LS_PJT_META`, `LS_PJT_DDLN`, `LS_PJT_TASK_EVENT_LOG`, `LS_LABEL`, `LS_DATA_SET` 등의 INSERT 문에서 PJT_ID 컬럼 제거
- 정리 후 `SELECT COUNT(*) FROM LS_PJT` 같은 검증 라인 제거

### 수용 기준
- [ ] `SHOW TABLES LIKE 'LS_PJT'` 결과 0건
- [ ] `INFORMATION_SCHEMA.COLUMNS WHERE COLUMN_NAME='PJT_ID'` 결과 0건
- [ ] Flyway 마이그레이션 V34, V35 적용 후 충돌 없음
- [ ] dev-seed.sql 재실행 후 ERROR 0건

### 의존성
- 이 Phase 완료 직후 즉시 Phase 5 진행 — BE 코드가 PJT_ID 참조 중이므로 단독 적용 시 BE 기동 실패

> 구현 (2026-05-16): V34 (9개 테이블 ALTER — LS_LABEL/LS_PJT_DATA_MPNG/LS_PJT_DATA_STTS/LS_PJT_USER_AUTHRT/LS_PJT_USER_AUTHRT_HSTRY/LS_PJT_META/LS_PJT_DDLN/LS_PJT_TASK_EVENT_LOG/LS_DATA_SET) + V35 (LS_PJT DROP) + dev-seed.sql / test-data.sql 정리. H2(MODE=MySQL) 호환을 위해 `DROP PRIMARY KEY, ADD PRIMARY KEY` 복합문은 두 문장으로 분리(LS_PJT_DATA_MPNG/STTS/META/DDLN 4건). 실제 인덱스명은 V14의 `IDX_PJT_TASK_EVENT_VIDEO`(plan 가정과 달랐던 항목) 로 교정.

---

## Phase 5: PJT_ID 제거 — BE · FE 코드

### 목표
Phase 4 의 DDL 변경에 BE 17개 파일 + FE 4개 파일을 적응시켜 빌드/테스트 통과. 시드/마이그레이션은 Phase 4 에서 완료된 상태.

### 생성·수정 파일

#### BE Entity (10개)
| 파일 | 동작 |
|------|------|
| `project/entity/LsPjt.java` | **삭제** (테이블 DROP) |
| `project/entity/LsPjtDataMpng.java` | `@EmbeddedId Pk` 단순화 → `@Id @Column private Long rawDataId` |
| `project/entity/LsPjtDdln.java` | 단일 PK `DDLN_SEQ` |
| `project/entity/LsPjtMeta.java` | 단일 PK `META_KEY` |
| `assignment/entity/LsPjtDataStts.java` | 단일 PK `RAW_DATA_ID` |
| `assignment/entity/LsPjtUserAuthrt.java` | `pjtId` 필드 제거 + UK 매핑 변경 |
| `assignment/entity/LsPjtUserAuthrtHstry.java` | `pjtId` 필드 제거 |
| `assignment/entity/LsPjtTaskEventLog.java` | `pjtId` 필드 제거 |
| `label/entity/LsLabel.java` | `pjtId` 필드 제거 + UK 매핑 변경 |
| `export/entity/LsDataSet.java` | `pjtId` 필드 제거 |

#### BE Repository (2개)
| 파일 | 변경 메서드 |
|------|-----------|
| `label/repository/LsLabelRepository.java` | `findByPjtIdAndUseYnOrderBySortNoAsc(pjtId, useYn)` → `findByUseYnOrderBySortNoAsc(useYn)`<br>`existsByPjtIdAndName(pjtId, name)` → `existsByName(name)`<br>`existsByPjtIdAndNameAndLabelIdNot(pjtId, name, id)` → `existsByNameAndLabelIdNot(name, id)` |
| `assignment/repository/LsPjtTaskEventLogRepository.java` | `findByPjtIdAndRawDataIdOrderByOccurredAtAsc(pjtId, rawDataId)` → `findByRawDataIdOrderByOccurredAtAsc(rawDataId)` |

#### BE Service / Controller / DTO
| 파일 | 변경 |
|------|------|
| `label/service/LabelMasterService.java` | `list(pjtId)` → `list()`, `create(req, regId)` 에서 `req.pjtId()` 사용 제거, `update`/`delete` 도 동일 |
| `label/controller/LabelMasterController.java` | GET/POST/PUT 의 `@RequestParam @NotNull Long pjtId` 제거. 경로: `GET /v1/manage/labels` (pjtId 쿼리 없음) |
| `label/dto/LabelMasterRequest.java` | `pjtId` 필드 제거 |

#### BE 테스트 (2개)
| 파일 | 변경 |
|------|------|
| `label/LabelMasterServiceTest.java` | pjtId 인자 제거, mock 시그니처 일치 |
| `label/LabelMasterControllerTest.java` | `?pjtId=1` 쿼리 제거, request body 에서 `pjtId` 필드 제거 |

#### FE (4개)
| 파일 | 변경 |
|------|------|
| `frontend/src/.../types.ts` (line 47) | `AssignTaskRequest.pjtId` 필드 제거 |
| `frontend/src/.../AssignModal.tsx` (line 170, 172) | `pjtId: 1` 하드코딩 제거 + 주석 제거 |
| `frontend/src/.../api.test.ts` (line 53, 56, 68) | 테스트에서 `pjtId: 1` 제거 |
| `frontend/src/.../AssignModal.test.tsx` (line 137, 138) | 테스트에서 `pjtId: 1` 제거 |

### 핵심 시그니처 변경

```java
// LabelMasterService
public List<LabelMasterResponse> list();
public LabelMasterResponse create(LabelMasterRequest req, String regId);
public LabelMasterResponse update(Long labelId, LabelMasterRequest req, String mdfcnId);
public void delete(Long labelId, String mdfcnId);

// LabelMasterController
@GetMapping
public ApiResponse<List<LabelMasterResponse>> list();
@PostMapping
public ApiResponse<LabelMasterResponse> create(@RequestBody @Valid LabelMasterRequest req);
```

```typescript
// FE types.ts
export interface AssignTaskRequest {
  rawDataId: number;
  userNo: number;
  taskTypeCd: string;
  // pjtId 제거
}
```

### 테스트 케이스 (cc-tdd RED 단계)

- `pjtId_쿼리_없이_라벨_목록_200_반환` — `GET /v1/manage/labels` (쿼리 파라미터 없음) 200
- `라벨_생성_시_pjtId_없이_NAME_단독_UK_검증` — 동일 NAME 중복 시 409
- `라벨_NAME_단독_UK_제약_위반_시_409` — DB 에러 → 409 처리
- `assignTask_API_pjtId_필드_없이_정상_요청` — FE: AssignTask 요청 body 에 pjtId 없음
- `LabelMasterControllerTest 전체 회귀` — 기존 16개 시나리오 PJT_ID 없는 형태로 재작성

### 수용 기준
- [ ] BE `./gradlew test` 전체 통과 (Phase 3 기준 649 + Phase 4/5 신규 변경 후 회귀 0)
- [ ] FE `npm test` 전체 통과
- [ ] `git grep "pjtId\|PJT_ID\|pjt_id"` 결과 0건 (docs/ 제외)
- [ ] BE 부트런 → Hibernate `ddl-auto=validate` 통과 (Entity ↔ DB 스키마 일치)

### 의존성
- Phase 4 (DDL 완료) → Phase 5

> 구현 (2026-05-16): Entity 10 (LsPjt 삭제 + 9개 수정) + Repository 3 (LsLabelRepository / LsPjtTaskEventLogRepository / LsPjtDataSttsRepository 제네릭 Long 단일화) + Service/Controller/DTO 다수 + BE 테스트 11건 + FE 4파일. 부수 변경: `ReviewService.loadByVideoId` Optional 단순화, `StatsQueryRepository` JPQL join 단순화, `VideoRepository.findLatestExportsByRawSns` 영상별 export 매핑 단절로 빈 리스트 반환(향후 매핑 테이블 신설 시 복원). `LabelControllerTest` 에 `@Transactional("controlTransactionManager")` 추가로 LS_LABEL 시드 leak 차단. 전체 649 tests / 648 passed / 0 fail / 1 skipped.

> 잔존 후보: `LS_DATA_SET` 영상-export 매핑 부재 — 향후 매핑 컬럼/테이블 신설 시 `VideoRepository.findLatestExportsByRawSns` 복원

---

## Phase 6: AutoLabel preset 매핑

### 목적
`YoloAutolabelStep` / `Sam2SegmentStep` 에서 YOLO/SAM2 응답 `label` ("person") → `LS_LABEL.NAME` case-insensitive 매칭하여 `LABEL_ID` 저장. 매칭 실패 시 LABEL_ID=NULL.

Phase 4~5 이후 lookup 시그니처가 단순해진 형태로 구현 — `findLabelIdByName(String name)` (pjtId 인자 없음).

### 변경 파일
| 파일 | 변경 |
|------|------|
| `backend/.../label/repository/LsLabelRepository.java` | `Optional<LsLabel> findByNameIgnoreCaseAndUseYn(String name, String useYn)` 추가 |
| `backend/.../label/service/LabelMasterService.java` | `Optional<Long> findLabelIdByName(String name)` 추가 (USE_YN='Y' 활성 라벨만, null/blank safe) |
| `backend/.../batch/step/YoloAutolabelStep.java` | `LabelMasterService` 주입 + `createAutoBbox(srcSn, labelId, label, ...)` 6-arg 사용 + 로그 `[Batch][Yolo] mapped label name={} labelId={}` |
| `backend/.../batch/step/Sam2SegmentStep.java` | 동일 패턴, `createAutoPolygon(srcSn, labelId, label, ...)` 5-arg |
| `backend/.../label/service/LabelMasterServiceTest.java` | `findLabelIdByName` 단위 테스트 추가 |
| `backend/.../batch/step/YoloAutolabelStepTest.java` | LabelMasterService mock + 매칭/미매칭 케이스 추가 |
| `backend/.../batch/step/Sam2SegmentStepTest.java` | 동일 |

### 테스트 케이스
- `YOLO_person_응답_시_LABEL_ID_매핑` — `findByNameIgnoreCaseAndUseYn("PERSON", "Y")` → labelId=1
- `YOLO_unknown_label_시_LABEL_ID_null` — 매칭 실패 시 null 반환 + 저장 시 LABEL_ID=NULL
- `findLabelIdByName_null_입력_시_Optional_empty` — null/blank safe
- `findLabelIdByName_use_yn_N_라벨은_제외` — soft-deleted 라벨 무시
- `YoloAutolabelStep_매핑_로그_검증` — `ListAppender` 로 mapped 로그 확인
- `Sam2SegmentStep_매핑_로그_검증` — 동일

### 수용 기준
- [ ] 영상업로드 페이지에서 YOLO 자동 라벨 결과가 한글 이름·색상으로 표시
- [ ] BE 로그에 `[Batch][Yolo] mapped label name=person labelId=1` 같은 로그
- [ ] 매칭 실패 라벨은 응답 enrichment 에서 labelName/color 가 null

> 구현 (2026-05-16): LsLabelRepository.findByNameIgnoreCaseAndUseYn derived query + LabelMasterService.findLabelIdByName (null/blank guard + trim + use_yn='Y') + YoloAutolabelStep / Sam2SegmentStep 에 LabelMasterService 주입 + 6-arg createAutoBbox / 5-arg createAutoPolygon 사용 + `[Batch][Yolo|Sam2] mapped label name={} labelId={}` 로그 (LogSanitizer 적용, CWE-117 방어). 신규 테스트 13 (Service 7 + Yolo 3 + Sam2 3). 전체 662 / 0 fail.

> 잔존 후보 (별도 phase 권장): Sam2TrackService.java:117 가 여전히 deprecated 4-arg createAutoPolygon 사용 — 트랙 보간 경로도 6-arg 로 마이그레이션 필요

---

## Phase 7: FE LabelSidebar

### 목적
CVAT 스타일 라벨 사이드바 — 라벨 목록(색상 박스 + 이름 + 단축키), 현재 선택 라벨 강조, 클릭 시 활성화.

### 변경 파일
- `frontend/src/features/label/api/labelMaster.ts` (신규) — `GET /v1/manage/labels` 호출 (pjtId 쿼리 없음)
- `frontend/src/features/label/hooks/useLabelMaster.ts` (신규) — TanStack Query
- `frontend/src/features/label/components/LabelSidebar.tsx` (신규)
- `frontend/src/pages/labeling/LabelingPage.tsx` — Sidebar 통합 + 단축키 핸들러 (1~9 → 라벨 활성화)
- `frontend/src/features/label/components/OverlayLayer.tsx` — DEFAULT_CLASS_NAME 제거, 현재 활성 라벨 사용

### 테스트 케이스
- `LabelSidebar_렌더링_시_13개_라벨_표시`
- `단축키_1_누르면_첫번째_라벨_활성화`
- `클릭_시_활성_라벨_강조_표시`
- `useLabelMaster_훅_API_호출_검증`

### 수용 기준
- [ ] 라벨링 화면 좌측에 LabelSidebar 표시 (13개 라벨, 색상 박스 + 한글 이름 + 단축키 번호)
- [ ] 단축키 1~9 누르면 해당 라벨 활성화
- [ ] BBOX 새로 그리면 활성 라벨로 자동 적용 (DB에 labelId 저장)

> 구현 (2026-05-16): `features/label/api/labelMaster.ts` (GET /v1/manage/labels) + `hooks/useLabelMasters.ts` (TanStack Query, staleTime 5분) + `components/LabelSidebar.tsx` (좌측 w-56 신규 패널, sortNo asc, 색상 박스 + 한글 이름 + 단축키 1~9 뱃지, a11y aria-label/role) + `stores/useLabelStore.ts` 에 activeLabelId/setActiveLabelId 추가 + `useLabelingShortcuts.ts` 에 1~9 키 바인딩 (INPUT 포커스 회피) + LabelingPage 좌측 통합. 신규 테스트 15건 (api 2 + Sidebar 5 + store 4 + 단축키 4). LabelSidebar 폭 w-56, sortNo 10 이상은 드롭다운만으로 활성화.

---

## Phase 8: FE ObjectAttributePanel

### 목적
이미 그려진 객체 클릭 시 우측 패널에 라벨 드롭다운 + 속성 편집 + 변경 즉시 PUT 반영. `'Object'` fallback 텍스트 완전 제거.

### 변경 파일
- `frontend/src/features/label/components/ObjectAttributePanel.tsx` — `availableLabels` prop 사용 + 드롭다운 + onChange 시 PUT
- `frontend/src/features/label/components/LabelPanel.tsx` — labelId 기반 그룹화 + 색상 표시
- `frontend/src/pages/labeling/LabelingPage.tsx` — `availableLabels` 전달
- `frontend/src/features/label/api.ts` — `normalizeLabel` 에서 `Object` fallback 제거, labelId null 이면 빈 표시

### 테스트 케이스
- `객체_클릭_시_드롭다운에_13개_라벨_표시`
- `드롭다운_변경_시_PUT_호출_검증`
- `labelId_null_객체는_빈_라벨_표시`
- `Object_fallback_텍스트_제거_검증`

### 수용 기준
- [ ] 자동 라벨 + 수동 라벨 모두 한글 이름·색상으로 표시
- [ ] 객체 클릭 → 드롭다운에서 다른 라벨 선택 → 즉시 캔버스 색상/이름 갱신
- [ ] `Object` 텍스트 화면 어디서도 안 보임
- [ ] Vitest 시나리오: "labelId 변경 후 화면 즉시 반영"

> 구현 (2026-05-16): `ObjectAttributePanel.tsx` 에 useLabelMasters 자동 채움 + 드롭다운 + dirty 마킹 + 색상 박스 + `OverlayLayer.tsx` 의 `DEFAULT_CLASS_NAME='object'` 완전 제거 → `resolveDefaultLabel(masters, activeLabelId)` 패턴 도입 (activeLabelId null/미존재 시 sortNo 최소 활성 라벨 자동 선택, 라벨 0건이면 BBOX 생성 거부 안전장치). `api.ts` normalizeLabel 의 Object fallback 제거. 신규 테스트 7건 (ObjectAttributePanel Phase 8 2 + resolveDefaultLabel 5). 전체 npm test 607/607 PASS, npm run build PASS, `grep DEFAULT_CLASS_NAME` 0건.

---

## Phase 9: 통합 시나리오 검증

### 목적
`/cc-verify` 로 사용자 시나리오(영상 업로드 → 자동 라벨 → 라벨 수정 → 저장) 전체 통과 확인. 회귀: 기존 라벨링/검수 플로우.

### 검증 시나리오
1. 영상업로드 페이지에서 mp4 업로드 + 전체 단계 ON → 자동 라벨 결과가 한글 이름·색상으로 표시
2. WORKER 가 객체 클릭 → 드롭다운에서 라벨 변경 → 저장 → 새로고침 후 유지
3. 단축키 1~9 로 신규 BBOX 그리기 → 활성 라벨로 저장
4. REVIEWER 가 `/v1/manage/labels` (pjtId 쿼리 없음) 에서 새 라벨 추가 → 라벨링 화면 새로고침 후 드롭다운에 노출
5. 회귀: 검수(/v1/reviews/*) / 비식별 / 통계 / 포털 화면 / 작업 배정 (pjtId 없는 형태) 정상

> 구현 (2026-05-18): API 검증 ALL PASS (BE 662 tests, 5개 시나리오 모두 기존 테스트 커버 — 흐름 A/B/C 분석 매끄러움). UI 검증 — LabelSidebar 렌더링/단축키 1~9/Object fallback 미노출/회귀(검수·통계·작업·비식별·포털) 모두 PASS. 초기 검증에서 ObjectAttributePanel 미통합 발견 → Phase 8 보강 (LabelingPage.tsx 의 ClassAttributePanel → ObjectAttributePanel 교체, 구버전 + 테스트 파일 삭제) 후 시나리오 C(드롭다운 변경 + 즉시 반영) PASS. 종합 ALL PASS.

> 후속 처리 (2026-05-18): 캔버스 BBox 색상이 LS_LABEL.color 와 연결되는지 검증 → **수정 완료**. `getLabelDisplayColor` 신규 유틸 + `LabelsLayer` 에서 useLabelMasters 활용. 우선순위: `label.color` → labelMasters lookup (labelId) → trackId 해시 → source fallback. Playwright 시각 확인: 사람=빨강, 차량=청록 LabelSidebar 와 1:1 일치. FE 620 tests / 0 fail.

> 후속 처리 (2026-05-18): ObjectAttributePanel 다크톤 일관성 → **수정 완료**. bg-white → bg-gray-800 / text-gray-100 / input·select 다크톤(bg-gray-700, border-gray-600, text-white, focus:ring-blue-500) 일괄 적용. LabelSidebar/RightContainer 와 시각 통일. WCAG AA 대비 4.5:1 보장.

> 후속 처리 (2026-05-18): Sam2TrackService:123 deprecated 4-arg createAutoPolygon → 5-arg + LabelMasterService 주입 + 매핑 로그 → **수정 완료**. LsDataLbl 의 deprecated wrapper 는 dead code 로 유지. 신규 테스트 3건. 전체 665 / 0 fail. git grep deprecated 호출처 0건.

> 잔존 후속 후보: LS_PJT_* 테이블명 LS_* 리네이밍 → **Phase 10~13 으로 신설** (RENAME 대신 신규 테이블 생성 + 기존 readonly 방치 방식)

---

## Phase 10~13 — 신규 LS_* 테이블 전환 (Plan)

### 동기
사용자 결정: 기존 LS_PJT_* 7개 테이블은 외부 시스템(관제서버) 호환을 위해 **readonly 로 그대로 두고**, 저작도구가 사용할 테이블만 신규 LS_* 이름으로 생성하여 사용. RENAME TABLE 대비 위험 회피 (외부 코드 영향 0).

### 결정 사항 (권장안 확정)

| # | 항목 | 권장안 | 이유 |
|---|------|--------|------|
| A | 데이터 마이그레이션 | **A1 (V37 일괄 복사)** | 운영 데이터 보존 + 단일 phase 완결 + 빈 신규 환경에서도 INSERT 0건이라 안전 |
| B | 기존 LS_PJT_* 운명 | **B1 (그대로 방치)** | 외부 호환 + 롤백 안전성. 향후 DROP 은 별도 phase |
| C | Entity 클래스명 | **C1 (전체 변경)** | LsPjtUserAuthrt → LsTaskAssignment 식으로 명확화. C2(이름만 @Table 변경) 는 가독성 저하 |
| D | 패키지 구조 | **D1 (기존 유지)** | 변경 범위 최소화. project/, assignment/ 그대로 |

### 신규 테이블 명명 (확정)

| 기존 (readonly 잔존) | 신규 | Entity 클래스명 |
|---|---|---|
| LS_PJT_DATA_MPNG | **LS_RAW_DATA_ENROLLMENT** | LsRawDataEnrollment |
| LS_PJT_DATA_STTS | **LS_RAW_DATA_STATUS** | LsRawDataStatus |
| LS_PJT_USER_AUTHRT | **LS_TASK_ASSIGNMENT** | LsTaskAssignment |
| LS_PJT_USER_AUTHRT_HSTRY | **LS_TASK_ASSIGN_HISTORY** | LsTaskAssignHistory |
| LS_PJT_META | **LS_META** | LsMeta |
| LS_PJT_DDLN | **LS_DEADLINE** | LsDeadline |
| LS_PJT_TASK_EVENT_LOG | **LS_TASK_EVENT_LOG** | LsTaskEventLog |

### 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | 외부 시스템(관제서버) 이 LS_PJT_DATA_MPNG 에 영상 INSERT 시 저작도구가 못 읽음 | HIGH | 사용자 명시: 관제 양방향 deprecated. 외부 INSERT 없음 전제. 실제로 발생 시 별도 동기화 batch 필요 |
| 2 | 운영 데이터 마이그레이션 V37 실패 | MED | V36(테이블 생성) / V37(데이터 복사) 분리. V37 실패해도 빈 신규 테이블로 시작 가능 (운영 시 별도 검증) |
| 3 | Entity 클래스명 일괄 변경 시 import 누락 | MED | 컴파일 단계에서 빠짐없이 검출. 자동화된 sed/Edit replace_all 활용 |
| 4 | Phase 11/12 분리 시 BE 컴파일 실패 구간 | HIGH | 한 작업 흐름에서 연속 처리. Phase 11+12 사이 BE 기동 금지 |
| 5 | H2 Testcontainers 호환 | LOW | V36 단순 CREATE TABLE 이라 호환성 위험 낮음. V37 의 INSERT INTO SELECT 는 표준 SQL |

---

## Phase 10: 신규 LS_* 테이블 DDL + 데이터 마이그레이션

**목표**: 신규 LS_* 7개 테이블 생성 + 기존 LS_PJT_* 데이터 복사. DDL/시드만 변경, 코드 무수정. Phase 11 직전 단계.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/src/main/resources/db/migration/V36__create_new_ls_tables.sql` | 신규 | 7개 신규 테이블 CREATE TABLE + PK/UK/INDEX |
| `backend/src/main/resources/db/migration/V37__migrate_data_to_new_tables.sql` | 신규 | INSERT INTO new SELECT FROM old (7개 테이블) |

**V36 (스키마 — 기존 LS_PJT_* 의 PJT_ID 제거 후 컬럼 그대로 복제)**:
```sql
-- 1. LS_RAW_DATA_ENROLLMENT (영상 등록 매핑)
CREATE TABLE IF NOT EXISTS LS_RAW_DATA_ENROLLMENT (
    RAW_DATA_ID  BIGINT     NOT NULL,
    REG_ID       VARCHAR(30) NULL,
    REG_DT       TIMESTAMP  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (RAW_DATA_ID)
);

-- 2. LS_RAW_DATA_STATUS (영상별 진행 상태)
CREATE TABLE IF NOT EXISTS LS_RAW_DATA_STATUS (
    RAW_DATA_ID    BIGINT       NOT NULL,
    LABEL_STTS_CD  VARCHAR(20)  NULL,
    REVIEW_STTS_CD VARCHAR(20)  NULL,
    -- (기존 LS_PJT_DATA_STTS 컬럼 그대로)
    PRIMARY KEY (RAW_DATA_ID)
);

-- 3. LS_TASK_ASSIGNMENT (작업자 배정)
CREATE TABLE IF NOT EXISTS LS_TASK_ASSIGNMENT (
    ASSIGNMENT_ID BIGINT       NOT NULL AUTO_INCREMENT,
    RAW_DATA_ID   BIGINT       NOT NULL,
    USER_NO       BIGINT       NOT NULL,
    TASK_TYPE_CD  VARCHAR(20)  NOT NULL,
    -- (기존 LS_PJT_USER_AUTHRT 컬럼)
    PRIMARY KEY (ASSIGNMENT_ID),
    UNIQUE KEY UK_LS_TASK_ASSIGNMENT (RAW_DATA_ID, USER_NO, TASK_TYPE_CD)
);

-- 4. LS_TASK_ASSIGN_HISTORY
-- 5. LS_META (PK: META_KEY)
-- 6. LS_DEADLINE (PK: DEADLINE_SEQ)
-- 7. LS_TASK_EVENT_LOG (PK: EVENT_SEQ)
```
(정확한 컬럼은 기존 LS_PJT_* 의 V1/V14 정의를 그대로 복사 — Phase 10 진입 시 BE Agent 가 확인)

**V37 (데이터 복사)**:
```sql
INSERT INTO LS_RAW_DATA_ENROLLMENT (RAW_DATA_ID, REG_ID, REG_DT)
  SELECT RAW_DATA_ID, REG_ID, REG_DT FROM LS_PJT_DATA_MPNG;
INSERT INTO LS_RAW_DATA_STATUS (RAW_DATA_ID, LABEL_STTS_CD, REVIEW_STTS_CD, ...)
  SELECT RAW_DATA_ID, LABEL_STTS_CD, REVIEW_STTS_CD, ... FROM LS_PJT_DATA_STTS;
-- (7개 테이블 동일 패턴)
```

**테스트 케이스**:
- `V36_적용_후_LS_RAW_DATA_ENROLLMENT_존재_확인` (Flyway info / SHOW TABLES)
- `V37_적용_후_데이터_복사_확인` (SELECT COUNT 비교)

**수용 기준**:
- [x] Flyway V36/V37 적용 성공
- [x] `SHOW TABLES LIKE 'LS_PJT_%'` 7건 (그대로 유지)
- [x] `SHOW TABLES LIKE 'LS_%'` 신규 7건 추가
- [x] `SELECT COUNT(*) FROM LS_TASK_ASSIGNMENT = SELECT COUNT(*) FROM LS_PJT_USER_AUTHRT` (모든 7쌍 일치)

**의존성**: Phase 9 완료 (선행). Phase 11 직전 단계 (BE 코드 무수정, BE 기동 금지)

> 구현 (2026-05-18): V36 (133L — 7개 테이블 `CREATE TABLE IF NOT EXISTS` + PK/UK/INDEX, V34 적용 후 LS_PJT_* 컬럼 1:1 복제) + V37 (54L — `INSERT INTO new (cols) SELECT cols FROM old` 7쌍, 컬럼 명시). 신규 테이블: LS_RAW_DATA_ENROLLMENT(2), LS_RAW_DATA_STATUS(6 — V5 의 VERSION 포함), LS_TASK_ASSIGNMENT(6 — `ASSIGNMENT_ID` AUTO_INCREMENT 신규 PK + UK(RAW_DATA_ID, USER_NO, TASK_TYPE_CD)), LS_TASK_ASSIGN_HISTORY(8 — `AUTHRT_SEQ` 컬럼명 유지), LS_META(2), LS_DEADLINE(6 — DDLN_SEQ 비 AUTO_INCREMENT 유지), LS_TASK_EVENT_LOG(8). V37 INSERT 시 ASSIGNMENT_ID 만 생략(자동 부여), HSTRY_SEQ/EVENT_SEQ 는 기존값 보존 위해 명시 INSERT. H2(MODE=MySQL) 호환 (표준 SQL). 빌드/테스트 PASS — 665 tests / 0 fail (Phase 9 기준 동일, 회귀 0). 리뷰 CONDITIONAL PASS — CRITICAL 0 / HIGH 0 / MEDIUM 1 (V37 INSERT 멱등성 — Flyway baseline 보호로 운영 무위험) / LOW 2 (인덱스명 접두사 IDX_↔IX_ 혼용, LS_TASK_ASSIGN_HISTORY.AUTHRT_SEQ ↔ 신규 ASSIGNMENT_ID 단절 — **Phase 11 인계 사항**).

> Phase 11 인계 사항 (HIGH): V37 데이터 복사 시 기존 `LS_PJT_USER_AUTHRT.AUTHRT_SEQ` → 신규 `LS_TASK_ASSIGNMENT.ASSIGNMENT_ID` 매핑이 단절됨 (AUTO_INCREMENT 신규 부여). `LS_TASK_ASSIGN_HISTORY.AUTHRT_SEQ` 가 기존 값을 보존하므로, Phase 11 Entity 매핑 시 `@JoinColumn` 정책 결정 필요 (운영 DB 빈 상태에서는 무영향). 신규 LS_TASK_ASSIGN_HISTORY 엔티티에서 AUTHRT_SEQ → ASSIGNMENT_ID 재매핑 backfill UPDATE 또는 컬럼명 변경 결정 필요.

---

## Phase 11: Entity/Repository 일괄 변경 + Service/DTO/JPQL 적응

**목표**: Phase 10 의 신규 테이블 7개에 맞춰 Entity 클래스명 일괄 변경 + Repository 클래스명 변경 + 의존 Service/DTO/JPQL/시드 동시 적응. 빌드/테스트 통과까지.

**생성·수정 파일**:
| 파일 | 동작 |
|------|------|
| Entity 7개 rename | `LsPjtDataMpng.java` → `LsRawDataEnrollment.java`, `LsPjtDataStts.java` → `LsRawDataStatus.java`, `LsPjtUserAuthrt.java` → `LsTaskAssignment.java`, `LsPjtUserAuthrtHstry.java` → `LsTaskAssignHistory.java`, `LsPjtMeta.java` → `LsMeta.java`, `LsPjtDdln.java` → `LsDeadline.java`, `LsPjtTaskEventLog.java` → `LsTaskEventLog.java` |
| Entity 내부 | `@Table(name = "LS_*")` 갱신, 클래스명 변경, factory 메서드명 그대로 (createLabeler 등) |
| Repository 7개 rename | `LsPjtDataMpngRepository` → `LsRawDataEnrollmentRepository` 등 |
| Service | AssignmentService, ReviewService, StatsQueryRepository, VideoRepository 등 `LsPjt*` import 갱신 |
| DTO | AssignmentResponse, ReviewResponse 등 — Entity 메서드 호출 import 갱신 |
| JPQL/Native Query | StatsQueryRepository, VideoRepository 의 SQL/JPQL 의 `LS_PJT_*` → `LS_*` 테이블명 갱신 |
| 시드 (dev-seed.sql, test-data.sql) | INSERT 문 테이블명 변경 (LS_PJT_* → LS_*) |

**핵심 변경 절차**:
1. Entity 파일 7개를 신규 이름으로 복사 (또는 mv) — 클래스 선언/@Table/factory 메서드 시그니처 모두 갱신
2. Repository 7개 동일 패턴
3. `git grep "LsPjt"` + Edit replace_all 로 의존 import 일괄 갱신 (`LsPjtDataMpng` → `LsRawDataEnrollment` 등 7쌍)
4. JPQL/Native Query 의 테이블명 `LS_PJT_*` → `LS_*` 갱신
5. 시드 데이터의 INSERT 문 갱신

**테스트 케이스** (기존 테스트가 모두 회귀 0 으로 통과해야 함):
- `LsTaskAssignment_엔티티_생성_시_LS_TASK_ASSIGNMENT_테이블에_저장`
- `AssignmentService_assign_시_신규_테이블_사용`
- `StatsQueryRepository_JPQL_신규_테이블_조회_정상`

**수용 기준**:
- [x] BE 컴파일 통과 (`compileJava` + `compileTestJava`)
- [x] `git grep "LsPjt\|LS_PJT_"` (테스트 데이터 SQL 제외) → 0건
- [x] `./gradlew test` 회귀 0 (Phase 9 기준 665 → 665+ 유지)
- [x] Hibernate `ddl-auto=validate` 통과

**Phase 10 인계 사항 (HIGH)**: `LS_TASK_ASSIGN_HISTORY.AUTHRT_SEQ` ↔ 신규 `LS_TASK_ASSIGNMENT.ASSIGNMENT_ID` 단절 — Entity 매핑 시 (a) 컬럼명 ASSIGNMENT_ID 로 변경 + backfill UPDATE, (b) AUTHRT_SEQ 유지 + 별도 정책 문서화 중 결정 필요.

**의존성**: Phase 10 완료 (DDL/데이터 적용). 동일 작업 흐름에서 즉시 진행 — BE 기동 금지

> 구현 (2026-05-18): Entity 7쌍 rename (LsPjtDataMpng→LsRawDataEnrollment / LsPjtDataStts→LsRawDataStatus / LsPjtUserAuthrt→LsTaskAssignment / LsPjtUserAuthrtHstry→LsTaskAssignHistory / LsPjtMeta→LsMeta / LsPjtDdln→LsDeadline / LsPjtTaskEventLog→LsTaskEventLog) + Repository 4쌍 rename (Mpng/Meta/Ddln 은 Repository 부재) + Service 15개 () + Test 12개 + 시드 3개 (dev-seed.sql/test-data.sql/test-data-stats-clean.sql DELETE 갱신). LsTaskAssignment 는 PK 컬럼 `AUTHRT_SEQ` → `ASSIGNMENT_ID` + `@GeneratedValue(IDENTITY)` 도입, 필드 `assignmentId` (의존처 `getAuthrtSeq()` → `getAssignmentId()` 갱신). **Phase 10 인계 (b) 채택** — LsTaskAssignHistory.authrtSeq 필드/컬럼명 보존 (운영 빈 DB 무영향, 실 마이그레이션 시점에 backfill 결정). AssignmentResponse.Item.authrtSeq JSON 직렬화 필드명 보존 → FE 무영향. 빌드/테스트 BUILD SUCCESSFUL — 665 tests / 0 fail / 1 skipped (Phase 10 기준 동일, 회귀 0, 첫 시도 성공). `git grep "LsPjt\|LS_PJT_"` 0건 (V36/V37 + V1/V5/V14/V34 마이그레이션 파일 + docs/plan/archive/cvat 제외). CLAUDE.md 자동 갱신 반영 (LS_TASK_ASSIGNMENT/LS_TASK_ASSIGN_HISTORY 표기).

> Phase 12 인계 후보: (1) 운영 데이터 마이그레이션 시 AUTHRT_SEQ → ASSIGNMENT_ID 재매핑 backfill batch (옵션 a 전환 결정 필요) (2) Hibernate validate 실 MariaDB 적용 후 bootRun 재검증 (3) 인덱스명 접두사 IDX_↔IX_ 일관성 정리 (Phase 10 LOW 잔존).

---

## Phase 12: 시드 / 테스트 / FE 호환 정리

**목표**: 누락된 테스트 보강 + FE 가 변경된 응답 형태(있다면) 적응 + 회귀 sweep. Phase 11 이 컴파일·단위 테스트 PASS 까지 보장하지만 일부 통합 시나리오 누락 가능성 점검.

**생성·수정 파일**:
| 파일 | 동작 |
|------|------|
| `dev-seed.sql` / `test-data.sql` | Phase 11 에서 1차 갱신됨. 누락 INSERT 보강 |
| 신규 테스트 | `NewLsTablesIntegrationTest` (선택) — V36/V37 적용 후 신규 테이블에 데이터 정상 INSERT/SELECT 검증 |
| FE | DTO 응답 형태 변경 없음 (Entity 만 변경) → 0 파일 예상. 단 grep 으로 잔존 키워드 확인 |

**수용 기준**:
- [ ] `./gradlew test` 전체 통과
- [ ] FE `npm test` 전체 통과 + `npm run build` PASS
- [ ] `git grep "LsPjt\|LS_PJT_"` (LS_PJT_*** 마이그레이션 파일 제외) → 0건

**의존성**: Phase 11 완료

> 구현 (2026-05-18): 신규 코드 변경 0건 — 회귀 sweep 단독 수행. 조사 결과 `dev-seed.sql`/`test-data*.sql` 의 `LsPjt`/`LS_PJT_` 잔존 0건 (Phase 11 에서 1차 정리 완료), FE/Java 코드 0건. `NewLsTablesIntegrationTest` 는 (선택) 항목으로 표기되었으나 Phase 11 의 665 tests 가 이미 AssignmentService/ReviewService/StatsQueryRepository/MetaService 경로로 신규 7개 테이블 전체 read/write 를 커버하여 중복 회피로 skip. BE `./gradlew test` 665 / 664 passed / 0 fail / 1 skipped (Phase 11 기준 동일, 회귀 0). FE `npm test --run` 620 / 620 PASS + `npm run build` PASS (2.69s, main chunk gzip 94KB). 잔존 키워드는 마이그레이션 SQL (V1/V5/V14/V34/V36/V37) 에 한정 — 수용 기준의 명시적 제외 범위 충족. 인덱스명 접두사 IDX_↔IX_ 정리 (LOW) 는 Phase 12 보강 작업 미발생으로 후속 이관.

---

## Phase 13: 통합 검증

**목표**: `/cc-verify` 로 Phase 10~12 의 end-to-end 검증. Phase 9 와 동일한 시나리오 5종 + 신규 테이블 동작 검증.

**검증 시나리오**:
1. 영상 자동 라벨 한글 이름·색상 (Phase 6 회귀)
2. 객체 라벨 수정 영속화 (Phase 8 회귀)
3. 단축키 + 신규 BBOX (Phase 7 회귀)
4. 라벨 마스터 CRUD (Phase 1+5 회귀)
5. **신규 추가** — 작업 배정 (LS_TASK_ASSIGNMENT) / 상태 추적 (LS_RAW_DATA_STATUS) / 이벤트 로그 (LS_TASK_EVENT_LOG) 정상 동작

**수용 기준**:
- [ ] 시나리오 5개 모두 PASS
- [ ] BE 로그에 LS_TASK_ASSIGNMENT / LS_RAW_DATA_STATUS 등 신규 테이블 쿼리 출현
- [ ] reports/phase-13-verification.md 작성

**의존성**: Phase 12 완료

---

## 향후 검토 후보 (Future Cleanup Candidates)

### LS_PJT_* 테이블 DROP (B2 옵션, 별도 phase)
Phase 10~13 완료 후 일정 기간(예: 1~2주) 운영 안정 확인 후 LS_PJT_* 7개 테이블 DROP. 운영 환경 외부 시스템(관제서버) 의 미사용 확인 필수.

### V2 정책 작업 (완료 보관)
이전 plan(V2 정책 9건 반영 + LS_DEIDENT_REPORT 분리 + 시드 정리 + M2M 제거 + projects/generate API 제거 + 영상업로드 명칭/4단계 토글) 은 `.claude-archive/plan-v2-policy-2026-05-15.md` 에 보존.
