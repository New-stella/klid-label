# 구현 계획: KPST 비식별 솔루션 폴링 연동 재설계

> 생성: 2026-06-10
> 상태: 진행 중
> 규격서: docs/v2-wiki/22-deid-solution-api.md (KPST 비식별화 솔루션 API v1.0, 13 엔드포인트)
> 사용자 확정: ① 영상 1건 = KPST 프로젝트 1개 ② 기존 콜백 인프라 유지 + 폴링 경로 병행 신설

## 배경 (Context)
현행 비식별 연동(`DeidentifyClient` `POST /api/v1/deidentify` + `DeidentifyResultController` HMAC 콜백)은 **가상 콜백 모델**로 구현돼 실제 KPST API와 불일치. 규격서 본문이 "폴링 어댑터로 재설계 필요(별도 DEV 과제)"로 명시. 실제 KPST는 **콜백 없음 — 업로드→프로젝트→진행률 폴링→다운로드** 흐름.

## Phase 진행 현황

- [x] Phase 1: KPST API 클라이언트 신설 (TLS 자체CA + Resilience4j) ✅ 완료 (2026-06-10)
  > 구현: KpstDeidentifyClient(6메서드)+5 DTO+KpstWebClientConfig(자체CA TLS)+Resilience4j kpstDeid. download atomic move/부분파일 정리, upload defer+검증, 경로순회 강화. 1307 GREEN. QA: code 9/10·security CONDITIONAL·build PASS. 잔여: CWE-22 leaf-symlink(Low~Med 후속)
- [x] Phase 2: RAW_SN↔prj_id/dataset_id 매핑 + LS_DEIDENT_PROC_LOG 확장 (마이그레이션) ✅ 완료 (2026-06-10)
  > 구현: V64(KPST_PRJ_ID/DATASET_ID/POLL_STTS_CD/POLL_LAST_DT/POLL_ATTEMPT_CNT nullable)+LsDeidentProcLog markKpstSubmitted/markPolling/markDownloaded+findByPollSttsCdIn. 콜백 경로 무영향. 1312 GREEN. QA PASS(9/10). 잔여: markDownloaded null가드·findByPollSttsCdIn 빈리스트(Phase 3 호출부 처리)
- [x] Phase 3: 비식별 폴링 오케스트레이션 (DeidentifyStep 위탁 + KpstDeidentPollJob 폴링/다운로드) ✅ 완료 (2026-06-10)
  > 구현: KpstDeidentService(위탁+폴링)+KpstDeidentTxService(REQUIRES_NEW, finishDownloadAndComplete 원자화)+KpstDeidentPollJob(Quartz)+DeidentifyStep 토글. FAILED→POLL_FAILED 종료값, 다운로드 size>0+파일실재 검증(불완전 비식별 차단), 다중셋 전체완료, 타임아웃(240/180분). 1343 GREEN. QA: code 8.0·security CONDITIONAL·build PASS
- [x] Phase 4: 통합 검증 + 기존 비식별 테스트 정합 ✅ 완료 (2026-06-10)
  > 구현: KPST 폴링 end-to-end 통합 테스트(submit→poll→download→Y / 타임아웃→F / 재기동 / enabled=false 회귀) + 심링크 거부 회귀 + build.gradle heap 2g. 1348 GREEN(회귀 0). build-validator PASS

> ✅ 전체 완료 (2026-06-10): KPST 비식별 폴링 연동 4 Phase 전부 PASS. BE 1348 GREEN. 잔여 후속(비차단): v2-wiki(07/08) 폴링 잡 문서동기화, 콜백/폴링 완료로직 일원화 검토.

## 요구사항 요약
- **표준 흐름(부록 B)**: `GET /`(연결확인) → `POST /upload`(multipart, 영상 1개) → `POST /project`(1 video=1 project, prj_id 획득) → `GET /retrieve_progress`(폴링, dsStatus.procState=완료 감지) → `GET /download?dataset_id=`(mask 파일) → `STORAGE_DEIDENTIFIED_PATH` 저장 → `LS_DATA_RAW.DE_IDENT_YN='Y'`
- **프로젝트 단위**: 영상 1건 = 프로젝트 1개 (선두 비식별 타이밍 유지, RAW_SN↔prj_id 1:1)
- **콜백 병행**: 기존 `DeidentifyResultController`/HMAC webhook/`WebhookIdempotencyLedger`는 **무변경 유지**(향후 타 솔루션 대비). 신규 KPST 경로는 폴링으로 독립
- **TLS**: 자체 CA `ca.crt` 신뢰 저장소를 WebClient에 구성(시스템 기본 신뢰 체인 아님)
- **상태 코드**: 데이터셋 처리완료 state=2(다운로드 대상), 프로젝트 수동대상 state=3
- **출력 포맷**: export_path 아래 `원본명-mask.mp4`

## 범위 (제외)
- 수동 비식별 연계(manual_deid_info/dataset_frames → 누락 신고 워크플로) = 후속(Phase 4 이후 별도)
- retrieve_report faceCount/lpCount 집계 적재 = 후속(선택)
- KPST 코드표(masking_type 등) 상세 매핑 = 요청 시 별도 제공(기본값 사용)
- 콜백 인프라 제거 = 안 함(병행 유지)

## 위험 요소
| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | 폴링이 async 스레드를 장시간 점유 / 재기동 시 유실 | HIGH | **Quartz 잡(KpstDeidentPollJob)으로 폴링 분리** — DeidentifyStep은 upload+project 위탁만, 완료감지·다운로드는 주기 잡. 매핑 테이블로 진행 추적(재기동 안전) |
| 2 | 외부 KPST 서버 장애·타임아웃·부분실패 | HIGH | Resilience4j(타임아웃/재시도/서킷) + 폴링 최대 시도·타임아웃 → 초과 시 DE_IDENT_YN='F' + 로그 |
| 3 | TLS 자체CA 미구성 시 핸드셰이크 실패 | HIGH | ca.crt 신뢰 저장소 WebClient 구성, 경로 환경변수. 미설정 시 명확한 부팅/호출 오류 |
| 4 | multipart 대용량 업로드 메모리/타임아웃 | MED | 스트리밍 업로드, 업로드 전용 타임아웃 별도 |
| 5 | 기존 DeidentifyStep/Client 교체로 배치·비식별 테스트 회귀 | MED | 기존 인터페이스 보존 또는 어댑터 분리, 테스트 신 흐름 정합 |
| 6 | SSRF/경로순회 (업로드·다운로드 경로) | MED(보안) | 베이스 URL application.yml 고정, 다운로드 저장경로 base 검증(normalize) |

---

## 상세 계획

### Phase 1: KPST API 클라이언트 신설
**목표**: KPST 13 엔드포인트 중 핵심 6종을 호출하는 `KpstDeidentifyClient`(WebClient, TLS 자체CA, Resilience4j) + 요청/응답 DTO 신설. 기존 `DeidentifyClient`는 콜백 경로용으로 보존.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `common/client/KpstDeidentifyClient.java` | 생성 | GET `/`(connect), POST `/upload`(multipart), POST `/project`, GET `/retrieve_progress`, GET `/download`, POST `/delete_project_id`. Resilience4j(`kpstDeid` 인스턴스) + ca.crt WebClient |
| `common/client/dto/Kpst*.java` | 생성 | UploadResponse(inputPath/files/count), ProjectRequest/Response(prj_id), ProgressResponse(prjStatus/dsStatus.procState), DownloadResult |
| `common/config/KpstWebClientConfig.java` | 생성 | 자체CA(ca.crt) SslContext + WebClient 빈(업로드 타임아웃 별도) |
| `application.yml` | 수정 | `kpst.deid.base-url`, `kpst.deid.ca-cert-path`, `creator-id`, `export-path-base`, 폴링 주기·타임아웃, Resilience4j `kpstDeid` |

**테스트 케이스(RED)**:
- `업로드_응답의_inputPath와_files를_파싱한다`
- `프로젝트_생성_응답에서_prjId를_획득한다`
- `진행률_조회에서_dataset_procState_완료를_판정한다`
- `다운로드가_mask파일을_지정_경로에_저장한다`
- `연결확인이_Connect를_반환한다`
- (보안) `베이스URL은_설정값만_사용한다(SSRF 차단)`

**수용 기준**: MockWebServer로 6 엔드포인트 호출/파싱 검증 · TLS 설정 빈 구성 · Resilience4j 적용 · 빌드+테스트 GREEN

### Phase 2: 매핑·로그 확장
**목표**: 영상 1건↔KPST 프로젝트/데이터셋 추적을 위해 `LS_DEIDENT_PROC_LOG`에 prj_id/dataset_id + 폴링 상태 컬럼 추가(또는 매핑 테이블 신설). 마이그레이션 + 엔티티.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `db/migration/V64__deident_kpst_mapping.sql` | 생성 | LS_DEIDENT_PROC_LOG에 KPST_PRJ_ID·KPST_DATASET_ID·POLL_STTS_CD(WAITING/POLLING/DOWNLOADED) 등 추가(또는 LS_KPST_DEID_JOB 매핑 테이블) |
| `batch/entity/LsDeidentProcLog.java` | 수정 | 신규 컬럼 매핑 + 상태 전이 메서드(위탁/폴링/다운로드/완료/실패) |

**테스트 케이스(RED)**: `위탁시_prjId가_기록된다` · `폴링완료시_datasetId와_다운로드경로가_기록된다` · `상태전이_WAITING_POLLING_DOWNLOADED_SUCCEEDED`

**수용 기준**: validate 통과 · 상태 전이 멱등 · 빌드+테스트 GREEN

### Phase 3: 비식별 폴링 오케스트레이션 (핵심)
**목표**: 비식별 선두 단계를 KPST 흐름으로 재설계. **DeidentifyStep = 위탁(connect→upload→project, WAITING 기록)** 까지만 동기 수행. **KpstDeidentPollJob(Quartz 주기) = retrieve_progress 폴링 → 완료(state=2) 감지 → download → STORAGE_DEIDENTIFIED_PATH 저장 → DE_IDENT_YN='Y' + 락해제/신고해소(기존 로직 재사용)**. 타임아웃/실패 시 'F'.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `batch/step/DeidentifyStep.java` | 수정 | 단일 `deidentify()` 호출 → KPST 위탁(upload+project)으로 교체, WAITING 기록 후 반환(완료대기는 폴링 잡) |
| `batch/scheduler/KpstDeidentPollJob.java` + `*TriggerConfig` | 생성 | WAITING/POLLING 작업 조회 → retrieve_progress 폴링 → 완료 시 download+저장+상태전이, 타임아웃 시 'F'. @DisallowConcurrentExecution |
| `batch/service/KpstDeidentService.java` | 생성 | 위탁/폴링/다운로드 오케스트레이션 + 락해제(`WorkLockService`)·신고해소(`DeidentReportService.resolveOpenReports`)·알림 기존 로직 재사용 |
| 관련 테스트 | 생성/수정 | DeidentifyStep·PollJob·Service + 기존 비식별/배치 테스트 정합 |

**시나리오 방어(HIGH)**: 폴링 타임아웃→'F' · 부분 실패 격리 · download 경로 base 검증 · 재기동 시 WAITING 작업 폴링 재개 · 마킹 진입 가드(DE_IDENT_YN='Y' 선행)

**테스트 케이스(RED)**:
- `적재시_DeidentifyStep이_KPST_업로드_프로젝트생성까지_위탁한다`
- `폴링잡이_완료감지시_download하고_비식별경로_저장_Y전이한다`
- `폴링_타임아웃시_F로_마킹하고_재시도큐는_신설하지_않는다`
- `비식별_완료시_OPEN신고가_RESOLVED되고_락이_해제된다`(기존 로직 보존)
- `재기동후_WAITING작업_폴링이_재개된다`

**수용 기준**: upload→project→poll→download 전체 흐름 동작 · 영상1건=프로젝트1개 · 콜백 인프라 무변경 · 기존 락/신고/알림 보존 · 빌드+전체 테스트 GREEN

### Phase 4: 통합 검증 + 테스트 정합
**목표**: 영향받은 비식별/배치 테스트 전수 정합 + 신 흐름 통합 검증(mock KPST 서버).

**수용 기준**: 기존 비식별·배치 테스트 신 흐름 GREEN · 통합 시나리오 PASS · code-review/security-reviewer(외부연동·TLS·SSRF) PASS

---

## 실행 가이드
1. `/cc-tdd Phase 1: KPST API 클라이언트 신설 (TLS 자체CA + Resilience4j)`
2. `/cc-tdd Phase 2: RAW_SN↔prj_id 매핑 + LS_DEIDENT_PROC_LOG 확장`
3. `/cc-tdd Phase 3: 비식별 폴링 오케스트레이션 (위탁 + KpstDeidentPollJob)`
4. `/cc-tdd Phase 4: 통합 검증 + 비식별 테스트 정합` → `/cc-verify`
