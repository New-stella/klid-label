# 구현 계획: 영상 배정 → WORKER 마킹 → 배치 트리거 시나리오 완성

> 생성: 2026-05-27 16:30
> 상태: 진행 중

## Phase 진행 현황

- [x] Phase 1: 영상 상태 전이 확장 + 마킹→배치 트리거 연결 (BE)
> 구현: STTS_BATCH_QUEUED 상수 + MarkingCompletedEvent + MarkingBatchBridge(@TransactionalEventListener AFTER_COMMIT) + AsyncBatchRunner(@Async) + Flyway V50 | 신규 9테스트, 993/994 PASS
- [x] Phase 2: TaskList 마킹 네비게이션 + 마킹 완료 피드백 UI (FE)
> 구현: TaskListPage 마킹/작업 버튼 분기 + MarkingPage 토스트+리다이렉트 + useMarkings TASK_BOARD_KEYS 무효화 | 신규 5테스트, 655/655 PASS

## 요구사항 요약

- 목적: 영상 배정 후 WORKER가 마킹하고, 마킹 완료 시 자동으로 배치 파이프라인이 시작되는 end-to-end 시나리오 완성
- 범위:
  - 포함: 영상 상태 전이 확장, 마킹 완료 → 배치 큐 등록, TaskList 마킹 네비게이션, 마킹 완료 피드백 UI
  - 제외: VLM 본체 구현, 마킹 API 자체 (이미 완성), 배치 파이프라인 내부 (이미 완성)
- 주요 액터: REVIEWER(배정), WORKER(마킹), 배치 시스템(자동 트리거)

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | CM_CODE에 DATA_STTS_CD 추가 값 seed (MARKING_IN_PROGRESS, BATCH_QUEUED) |
| 기존 API | MarkingService.create()에 배치 트리거 추가 |
| 도메인 모듈 | marking, assignment, batch |
| 설정/환경 | 없음 |
| FE | TaskListPage에 마킹 버튼, MarkingPage에 완료 피드백 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | VLM 외부 서비스 미연동 상태에서 배치 진행 | MED | VlmTimeseriesStep.enabled=false 시 VLM 스킵 후 바로 배치 진행 (기존 구현) |
| 2 | 마킹 완료 후 배치 중복 실행 | HIGH | dataSttsCd 상태 체크 + LsBatchProcLog로 중복 방지 |

---

## 상세 계획

### Phase 1: 영상 상태 전이 확장

**목표**: 영상의 전체 라이프사이클 상태(PENDING → ASSIGNED → MARKING_IN_PROGRESS → BATCH_QUEUED → PROCESSING → COMPLETED)가 정의되어 LsRawDataStatus로 추적 가능

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `assignment/entity/LsRawDataStatus.java` | 수정 | STTS_MARKING_IN_PROGRESS, STTS_BATCH_QUEUED 상수 + 전이 메서드 추가 |
| `db/migration/V50__seed_marking_batch_status_codes.sql` | 생성 | CM_CODE에 MARKING_IN_PROGRESS, BATCH_QUEUED 값 INSERT |

**핵심 로직 / 시그니처**:
```java
// LsRawDataStatus — 신규 상수 + 전이 메서드
public static final String STTS_MARKING_IN_PROGRESS = "MARKING_IN_PROGRESS";
public static final String STTS_BATCH_QUEUED = "BATCH_QUEUED";

public void markMarkingInProgress() {
    this.dataSttsCd = STTS_MARKING_IN_PROGRESS;
    this.updDt = LocalDateTime.now();
}

public void markBatchQueued() {
    this.dataSttsCd = STTS_BATCH_QUEUED;
    this.updDt = LocalDateTime.now();
}
```

**테스트 케이스**:
- `ASSIGNED에서_markMarkingInProgress_호출시_MARKING_IN_PROGRESS_전이`
- `MARKING_IN_PROGRESS에서_markBatchQueued_호출시_BATCH_QUEUED_전이`
- `상태_상수_7종_모두_정의_확인`

**수용 기준**:
- [ ] LsRawDataStatus에 7개 상태 상수 정의
- [ ] 각 전이 메서드가 dataSttsCd를 올바르게 갱신
- [ ] Flyway V50 마이그레이션 통과

**의존성**: 없음

### Phase 2: 마킹 완료 → 배치 트리거 연결

**목표**: WORKER가 마킹 제출 시 영상 상태가 BATCH_QUEUED로 전이되고 배치 큐에 등록되어 파이프라인이 자동 시작

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `marking/service/MarkingService.java` | 수정 | create()에서 TODO 교체 → 상태 전이 + 배치 트리거 호출 |
| `marking/service/MarkingBatchTrigger.java` | 생성 | 마킹 완료 후 상태 전이 + BatchOrchestrator 비동기 트리거 |
| `assignment/repository/LsRawDataStatusRepository.java` | 수정 | 없으면 생성. findByRawDataId() 메서드 |

**핵심 로직 / 시그니처**:
```java
// MarkingBatchTrigger — 마킹 완료 후 배치 파이프라인 트리거
@Service @RequiredArgsConstructor
public class MarkingBatchTrigger {
    private final LsRawDataStatusRepository statusRepository;
    private final BatchStatusService batchStatusService;

    /**
     * 마킹 완료 후: 상태 전이(BATCH_QUEUED) + 배치 로그 등록.
     * BatchOrchestrator.run(rawSn)은 Quartz 스케줄러가 큐에서 꺼내어 비동기 실행.
     * (local 에서는 batch.enabled=false 이므로 수동 트리거로 테스트)
     */
    @Transactional("controlTransactionManager")
    public void triggerAfterMarking(Long rawSn) {
        // 1. 상태 전이: ASSIGNED/MARKING_IN_PROGRESS → BATCH_QUEUED
        LsRawDataStatus status = statusRepository.findById(rawSn)
                .orElseThrow(() -> new CustomException(ErrorCode.NOT_FOUND));
        status.markBatchQueued();
        // 2. 배치 로그 등록 (BatchStatusService.startNew 패턴)
        batchStatusService.startNew(rawSn);
    }
}

// MarkingService.create() L77 TODO 교체:
markingBatchTrigger.triggerAfterMarking(rawSn);
```

**테스트 케이스**:
- `마킹_제출_시_영상_상태_BATCH_QUEUED_전이`
- `마킹_제출_시_LsBatchProcLog_신규_레코드_생성`
- `이미_BATCH_QUEUED인_영상에_마킹_재제출시_중복_방지_또는_무시`
- `영상_미존재시_NOT_FOUND_예외`

**수용 기준**:
- [ ] 마킹 API 호출 성공 시 LsRawDataStatus.dataSttsCd == BATCH_QUEUED
- [ ] LsBatchProcLog에 새 로그 생성
- [ ] MarkingService.create()의 TODO 주석 완전 교체

**의존성**: Phase 1

### Phase 3: TaskList → 마킹 화면 네비게이션 + 마킹 완료 피드백 UI

**목표**: WORKER가 배정 목록에서 마킹 화면으로 진입할 수 있고, 마킹 제출 후 "배치 처리 시작됨" 피드백을 받음

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/pages/TaskListPage.tsx` | 수정 | WORKER 행에 상태별 버튼: ASSIGNED→마킹, COMPLETED→작업, 그 외→뱃지 |
| `frontend/src/pages/MarkingPage.tsx` | 수정 | 제출 성공 후 토스트 + navigate('/tasks') 리다이렉트 |
| `frontend/src/features/marking/api.ts` | 수정 | createMarking onSuccess에서 TASK_KEYS 쿼리 무효화 |

**핵심 UI 흐름**:
```
TaskListPage (WORKER)
├── 영상 [ASSIGNED, firstSrcSn 없음]  → [마킹 시작] 버튼 → /marking/:rawSn
├── 영상 [BATCH_QUEUED/PROCESSING]     → "처리 중" 뱃지 (비활성)
└── 영상 [COMPLETED, firstSrcSn 있음] → [작업 시작] 버튼 → /label/:srcSn (기존)
```

**MarkingPage 수정 — 제출 성공 피드백**:
```typescript
// createMarking onSuccess 콜백
onSuccess: () => {
  pushToast({ variant: 'success', message: '마킹 제출 완료 — 배치 처리가 시작됩니다' });
  navigate('/tasks');
}
```

**테스트 케이스**:
- `WORKER_배정목록_ASSIGNED_영상에_마킹_버튼_렌더링`
- `WORKER_마킹_버튼_클릭시_marking_페이지_이동`
- `마킹_제출_성공시_토스트_표시_후_tasks_리다이렉트`
- `BATCH_QUEUED_영상은_처리중_뱃지_표시_버튼_비활성`

**수용 기준**:
- [ ] ASSIGNED 상태 영상에 "마킹 시작" 버튼 렌더링
- [ ] 마킹 제출 후 성공 토스트 + /tasks 리다이렉트
- [ ] BATCH_QUEUED/PROCESSING 상태 영상은 "처리 중" 뱃지

**의존성**: Phase 2
