# 구현 계획: ccarch-authoring 정합성 정렬 (외부 시스템 비동기 패턴 + VLM 외부 위탁)

> 생성: 2026-05-19
> 상태: Phase 1, 2 완료, Phase 3 대기
> 입력: `docs/ccarch-authoring/` 13개 문서 vs 현재 구현 Gap Report 14건
> 대체: 직전 `.claude-plan.md` (size:999 재설계 계획)은 완료되어 git history에 보존됨

## Phase 진행 현황

- [x] **Phase 1**: VLM 외부 위탁 전환 (CRITICAL — A-2 / C-2 / D-1 / B-1 / A-3 일부)
  > 구현: VlmClient(SSRF allowlist + HTTPS 강제 + 응답 5중 검증 + safeForLog) / VlmTimeseriesRequest+Response / VlmTimeseriesStep(externalJobId LS_BATCH_PROC_LOG JSON 영속화) / AiServerClient.extractVideoMeta 제거 / VlmMetaStep NO-OP화(@Deprecated, Phase 5 폐기 예정) / BatchOrchestrator 스텝 교체 / WebClientConfig.validateExternalUrl / Resilience4j vlmClient + BatchStatusService.recordVlmTimeseriesResult / application.yml vlm.client.* (enabled=false 기본) / .env.example / ai-server tests/test_vlm.py 회귀 가드. DEV_FIX 1회(HIGH 3 + MEDIUM 4 해결). 신규 25 + 기존 729 = 754 PASS
- [x] **Phase 2**: 비동기 결과 수신 콜백 엔드포인트 3종 신설 (CRITICAL — D-2)
  > 구현: 3 webhook 컨트롤러 (Deidentify/Vlm/Augment Result) + 3 Service + HmacWebhookFilter (Content-Length 1MB 가드 + 분당 5회 실패 backoff + FailureTracker hard cap 4096) + SecretKeyResolver + 영속 IdempotencyLedger (V39 LS_WEBHOOK_IDEMPOTENCY + JPA Entity/Repo + Persistent/InMemory 분리, @Primary=Persistent) + LS_DEIDENT_PROC_LOG.EXTERNAL_JOB_ID UNIQUE (V40) + ExternalUrlValidator 강화 (CGNAT/IPv4-mapped IPv6/IPv6 ULA/IDN punycode/multi-A) + DTO @Size(500/1000/1000) 상한 + application-prd.yml 환경변수 강제 (`${VAR:?msg}`) + DataIntegrityViolationException race 흡수 (Deident + Vlm Service). DEV_FIX 2회 (CRITICAL 1 + HIGH 4 + MEDIUM 4 + footgun 1 해결). 신규 40 + 기존 754 = 794 PASS. 운영 가이드 잔존: LB X-Forwarded-For / 분산 rate limit / 시크릿 회전 SOP
- [ ] **Phase 3**: 외부 시스템 비동기 정합 완성 (HIGH — A-1 / A-4) — Gitea fallback 큐 마이그레이션은 V41
- [ ] **Phase 4**: 비동기 표준 필드 + 범위·책임 문서 정리 (MEDIUM — E-1 / C-3 / G-1) — V42/V43
- [ ] **Phase 5**: ccarch 워크스페이스 업로드 실행 (LOW — H-1) + VlmMetaStep cleanup

## 요구사항 요약

- **목적**: `docs/ccarch-authoring/` 13개 문서가 정의한 **목표 아키텍처**(외부 시스템 비동기 위탁 + VLM 시계열 외부 책임 + 결과 수신 콜백 패턴)와 현재 구현 사이의 14건 GAP을 해소하여, ccarch 추적성과 코드 구현을 일치시킨다.
- **범위**:
  - 포함: BE common.client·batch.step·신규 webhook 컨트롤러, ai-server vlm.py 축소, Flyway 신규 마이그레이션(비동기 표준 컬럼), ccarch 노드 업로드
  - 제외: 외부 VLM 서비스 본체 / Deidentify SW 본체 / 생성형 AI 모델 본체 (모두 외부 책임). 본 도구는 클라이언트·SPI만 책임
- **주요 액터**: BATCH_SYSTEM (Quartz Job — VlmTimeseriesStep / DeidentifyStep 등), REVIEWER (시계열 메타·증강 결과 검토), 외부 시스템 (Deidentify SW / 외부 VLM 서비스 / 외부 생성형 AI / Gitea)
- **비기능 요구사항**:
  - 비동기 위탁: 요청 ID(idempotencyKey) 발급 + 중복 인계 시 멱등 처리 + dead-letter
  - 동기 Gitea: timeout 70s + CircuitBreaker 50%/window10 + Retry max=3 + fallback 큐 (stg/prd)
  - 콜백 수신: HMAC 또는 사전 토큰 인증 + 페이로드 검증
  - 보안: 콜백 엔드포인트 OWASP A01 (Broken Access Control) 가드 + 페이로드 SSRF 차단
  - 기존 도메인 API backward-compat (필드 추가만, 제거·이름 변경 금지)

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | Phase 2 적용 완료: V39 LS_WEBHOOK_IDEMPOTENCY + V40 LS_DEIDENT_PROC_LOG.EXTERNAL_JOB_ID UNIQUE. Phase 3 (V41 LS_GITEA_FALLBACK_QUEUE) + Phase 4 (V42 비동기 표준 컬럼 + V43 backfill) 예정 |
| 기존 API | 변경 없음. 신규 webhook 3종 적용 완료 (`POST /v1/deidentify/result`, `POST /v1/vlm/result`, `POST /v1/augments/result`) |
| 도메인 모듈 | `batch.step` (Phase 1 완료), `common.client` (VlmClient Phase 1 완료, DeidentifyClient/GiteaClient Phase 3), 신규 `webhook` 패키지 (Phase 2 완료) |
| 설정/환경 | Phase 2 적용 완료 (WEBHOOK_HMAC_SECRET_* + 환경변수 강제). Phase 3 (GITEA_FALLBACK_*) 예정 |
| ccarch 메타 | 노드 76개 + 링크 130개 업로드 (Phase 5 12-upload-plan.md 체크리스트) |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | 외부 VLM 서비스 계약(URL/스키마/인증) 미확정 → VlmClient 구현 보류 | HIGH | Phase 1에서 **인터페이스만 먼저 정의** (인터페이스 + DTO + Stub). 실 서비스 연동은 환경변수 토글 (`vlm.client.enabled=false` 기본). 외부 계약 확정 후 활성화 |
| 2 | ai-server vlm.py 의 `extractVideoMeta` 제거로 VlmMetaStep 깨짐 | HIGH | Phase 1 동시 진행. 마이그레이션 순서: (1) VlmTimeseriesStep 추가 (2) VlmMetaStep을 외부 위탁용으로 리팩토링 또는 폐기 (3) ai-server vlm.py 의 영상 메타 라우터만 제거 (객체 검증 라우터는 유지) |
| 3 | 비동기 webhook 인증 부재 시 외부에서 결과 위조 가능 | CRITICAL | Phase 2에서 HMAC 헤더(`X-Signature: hmac-sha256=...`) 필수 + 요청 ID 사전 발급분만 수락 (allowlist) + Replay 방지(처리 완료 idempotencyKey 재인계 시 200 OK 멱등 반환, 미발급 ID는 401) ✅ 적용 완료 |
| 4 | idempotencyKey 컬럼 추가 마이그레이션이 기존 데이터에 NULL → 외부 결과 매핑 불가 | MEDIUM | 마이그레이션 시 기존 행은 `LS_DEIDENT_REPORT.PK` 또는 `LS_DATA_RAW.RAW_SN` 기반 결정적 UUID로 backfill. 신규 행만 새 키 발급 |
| 5 | Gitea fallback 큐 동작이 라벨 저장 사용자 경험에 영향 | LOW | 정상 흐름은 동기 유지. CircuitBreaker open 시에만 큐 진입 + FE에 "버전 저장은 잠시 후 자동 재시도됩니다" 안내 |
| 6 | ccarch 노드 업로드 멱등성 위반 — 같은 `_handle` 중복 시 충돌 | MEDIUM | `mcp__ccarch__ccarch_create_node` 호출 시 idempotencyKey 사용, 실패 시 `ccarch_get_node`로 사전 확인 |

---

## Phase 1: VLM 외부 위탁 전환 ✅ 완료

(상세는 Phase 진행 현황 라인에 요약, 변경 파일과 시나리오 적용은 직전 커밋 `c6c7fd1` 참조)

---

## Phase 2: 비동기 결과 수신 콜백 엔드포인트 3종 ✅ 완료

### 최종 구현
- 3 컨트롤러 + 3 Service + 3 DTO + HmacWebhookFilter + SecretKeyResolver + WebhookIdempotencyLedger(인터페이스) + InMemory/Persistent 구현 + LsWebhookIdempotency 엔티티/Repo
- Flyway: V39 LS_WEBHOOK_IDEMPOTENCY / V40 LS_DEIDENT_PROC_LOG.EXTERNAL_JOB_ID UNIQUE
- 보안: HMAC-SHA256 (32B 이상 시크릿 강제, MessageDigest.isEqual 상수시간) + X-Timestamp ±5분 윈도우 + idempotencyKey allowlist (DB 영속) + SSRF 강화 (CGNAT/IPv4-mapped IPv6/IPv6 ULA/IDN punycode/multi-A) + Content-Length 1MB 가드 + 분당 5회 실패 backoff (FailureTracker hard cap 4096) + DataIntegrityViolationException race 흡수
- application-prd.yml: `${WEBHOOK_HMAC_SECRET_*:?msg}` 환경변수 강제 (미설정 시 부팅 실패)
- DTO Size 상한: vlmMetaItems(500) / labelMapping(1000) / processedRegions(1000)
- PersistentLedger.clear() = UnsupportedOperationException (footgun 차단)
- 794/794 테스트 PASS (Phase 1 754 + Phase 2 신규 40)

### Phase 3+ 로 넘긴 운영 가이드 영역 (코드 변경 범위 외)
- LB X-Forwarded-For trust 정책 (인프라 설정)
- 분산 rate limit (Redis 기반, 멀티 인스턴스 운영 시)
- 시크릿 회전 SOP 문서화
- Augment race 흡수 (UNIQUE 컬럼 추가 시 동일 패턴 적용 — Phase 4 와 연계)

---

## Phase 3: 외부 시스템 비동기 정합 완성

### 목적
**Deidentify**: ccarch `if-deidentify-spi` 정의에 맞춰 비동기 위탁 패턴 완성 (요청 ID 발급 + 결과 매핑). **Gitea**: ccarch `if-gitea-contents` 정의(동기 REST + CircuitBreaker + fallback 큐)에 맞춰 fallback 큐 동작 검증/구현.

### 변경 파일

| 영역 | 파일 | 동작 |
|---|---|---|
| BE common.client | `common/client/DeidentifyClient.java` | **수정** — `submit(VideoSn, options): JobId` 형태로 정리. idempotencyKey 발급 + 헤더 포함 (Phase 2 영속 ledger 의 `recordIssued` 호출 포함) |
| BE batch.step | `batch/step/DeidentifyStep.java` | **수정** — 위탁 완료 후 결과 대기(non-blocking). 결과는 Phase 2 webhook (`POST /v1/deidentify/result`)으로 수신 |
| BE common.client | `common/client/GiteaClient.java` | **수정** — CircuitBreaker open 시 fallback method 에서 `LS_GITEA_FALLBACK_QUEUE` 적재 + 환경별 활성화 플래그(`gitea.fallback.enabled`, 기본 dev:false, stg/prd:true) |
| BE 신규 모듈 | `version/fallback/GiteaFallbackQueue.java` + `GiteaFallbackRetryJob.java` (Quartz) | **신설** — 큐 처리 워커. 백오프 재시도, dead-letter 임계치 |
| DB 마이그레이션 | `db/migration/V41__create_ls_gitea_fallback_queue.sql` | **신설** — fallback 큐 테이블 (idempotencyKey, payload, retryCount, status, deadLetterAt) |
| 환경 설정 | `application-{dev,stg,prd}.yml` | `gitea.fallback.enabled` 환경별 설정 |

### 시나리오 (HIGH 방어)

| # | 시나리오 | 심각도 | 방어 전략 |
|---|---|:---:|---|
| S-1 | Deidentify 위탁 후 결과 영구 미수신 → 영상 PENDING 잠김 | HIGH | `LS_DEIDENT_REPORT` 에 SLA 타임아웃(예: 24h) 적용. 초과 시 `LS_DATA_RAW.DE_IDNTF_YN='F'` + 재등록 큐. 원본 절대 삭제 금지 (CLAUDE.md 규칙) |
| S-2 | Gitea fallback 큐가 무한 적재되어 큐 폭주 | HIGH | 최대 retry 5회, deadLetterAt 마킹 후 운영 알림. 큐 크기 모니터링 메트릭 추가 |
| S-3 | Gitea CircuitBreaker open 인지 사용자가 모름 | MED | FE에 응답으로 `versionStatus: "QUEUED_FOR_RETRY"` 반환 + 배지 표시 |

### 테스트 케이스 (RED)
- `DeidentifyClient_위탁_시_idempotencyKey_헤더_포함됨_그리고_recordIssued_호출()`
- `DeidentifyStep_위탁_완료_후_LS_BATCH_PROC_LOG_PENDING_상태()`
- `GiteaClient_CircuitBreaker_open_시_fallback_큐에_적재()`
- `GiteaFallbackRetryJob_재시도_성공_시_큐에서_제거()`
- `GiteaFallbackRetryJob_최대_재시도_초과_시_deadLetterAt_마킹()`

### 수용 기준
- DeidentifyClient 호출 시 idempotencyKey 가 항상 발급되고 `WebhookIdempotencyLedger.recordIssued` 호출됨
- Gitea CircuitBreaker open 시 (dev: 직접 실패, stg/prd: 큐 적재) 환경별 동작 검증
- 큐 retry 워커가 백오프 + 최대 횟수 + dead-letter 로 종료
- 기존 라벨 저장 흐름(정상 케이스) 영향 0건

### 실행 가이드
`/cc-tdd Phase 3: Deidentify 비동기 위탁 + Gitea fallback 큐` → `/cc-code-review`

---

## Phase 4: 비동기 표준 필드 + 범위·책임 문서 정리

### 목적
**E-1**: 비동기 위탁/수신 공통 컬럼 표준화(`idempotencyKey`, `externalJobId`, `retryCount`, `deadLetterAt`). **C-3**: CLAUDE.md V1.8 정리 사항과 코드/문서 일치도 마무리. **G-1**: 패키지 구조 vs ccarch COMPONENT 매핑 부드러운 정렬. **Augment race 흡수** 도 함께 (Phase 2 운영 가이드 인계 항목).

### 변경 파일

| 영역 | 파일 | 동작 |
|---|---|---|
| DB 마이그레이션 | `db/migration/V42__add_async_standard_columns.sql` | `LS_DATA_META`, `LS_DATA_AUG` 에 `IDEMPOTENCY_KEY VARCHAR(64) UNIQUE`, `EXTERNAL_JOB_ID VARCHAR(128) UNIQUE`, `RETRY_COUNT INT DEFAULT 0`, `DEAD_LETTER_AT DATETIME NULL` 추가. (`LS_DEIDENT_REPORT` 는 별도 검토 — `LS_DEIDENT_PROC_LOG.EXTERNAL_JOB_ID` 는 Phase 2 V40 으로 이미 추가됨) |
| DB 마이그레이션 | `db/migration/V43__backfill_idempotency_keys.sql` | 기존 행에 결정적 UUID backfill (`UUID(VARCHAR_FIELD)` 또는 SHA-256(PK)) |
| BE 엔티티 | `LsDataMeta.java`, `LsDataAug.java` (LsDeidentReport 는 Phase 2 V40 으로 처리됨) | 신규 컬럼 매핑 추가. setter 금지, 생성 시 자동 발급 메서드 |
| BE Service | `webhook/service/AugmentResultService.java` | UNIQUE 컬럼 추가 후 Deident/Vlm 와 동일하게 `DataIntegrityViolationException` race 흡수 catch 추가 (Phase 2 잔존 항목) |
| 문서 | `docs/ccarch-authoring/07-entities.md` | 엔티티별 신규 컬럼 반영 (멱등성 + 외부 작업 ID 표준화 명시) |
| 문서 | `CLAUDE.md` 의 V1.8 섹션 | "VLM 외부 위탁 + 비동기 결과 수신 webhook 완료 (2026-05-19 Phase 1~3)" 한 줄 추가, 잔존 모호성 정리 |
| 문서 | `docs/ccarch-authoring/08-components.md` | 본 사업 5/19 Phase 결과 (VlmClient 신설, webhook 패키지 추가) 반영 |

### 시나리오

| # | 시나리오 | 심각도 | 방어 전략 |
|---|---|:---:|---|
| S-1 | backfill 마이그레이션이 대량 행에서 느림 | MED | 인덱스 추가 전에 backfill → 인덱스 추가 순서. 또는 batch update |
| S-2 | UNIQUE 제약 충돌 (기존 데이터 중 동일 키) | MED | backfill 시 `(table, pk)` 조합 기반 결정적 UUID 사용 → 충돌 0 보장 |

### 테스트 케이스
- `LsDataMeta_VLM_타입_생성_시_externalJobId_NULL_허용()`
- `LsDataAug_생성_시_idempotencyKey_자동발급()`
- `AugmentResultService_동시_webhook_인계_시_DataIntegrityViolation_멱등_흡수()`
- `Flyway_V42_V43_마이그레이션_idempotent_재실행_안전()`

### 수용 기준
- 2개 테이블 + 도메인 엔티티 추가 컬럼, 기존 데이터 backfill 완료
- AugmentResultService race 흡수 보강 (Deident/Vlm 와 일관)
- ccarch 문서(07-entities.md, 08-components.md, CLAUDE.md V1.8) 와 코드 일치

### 실행 가이드
`/cc-generate Phase 4: 비동기 표준 컬럼 추가 (V42 + V43 + 엔티티 수정 + AugmentResultService race 흡수)` → 문서 수정은 PM 직접 (`Edit`만)

---

## Phase 5: ccarch 워크스페이스 업로드 + cleanup

### 목적
`docs/ccarch-authoring/12-upload-plan.md` 체크리스트 순차 실행으로 ccarch 워크스페이스에 노드 76개 + 링크 130개 등록. `mcp__ccarch__ccarch_get_wiki_stats` 로 검증. **추가**: Phase 1에서 보류한 VlmMetaStep 클래스 완전 폐기.

### 변경 파일
- ccarch MCP 도구 호출 (코드 변경 없음). 실행 로그는 `reports/ccarch-upload-{timestamp}.md` 에 저장
- `backend/.../batch/step/VlmMetaStep.java` 삭제 (Phase 1 NO-OP 처리분)
- `backend/.../batch/step/VlmMetaStepTest.java` 삭제

### 시나리오

| # | 시나리오 | 심각도 | 방어 전략 |
|---|---|:---:|---|
| S-1 | 같은 `_handle` 중복 업로드 시 충돌 | MED | `mcp__ccarch__ccarch_create_node` 의 idempotencyKey 사용. 실패 시 `ccarch_get_node` 로 사전 확인 후 스킵 |
| S-2 | 링크 생성 시 source/target 노드 미존재 | MED | 12-upload-plan.md 순서 엄수 (ACTOR → ENTITY → COMPONENT → INTERFACE → FEATURE → USECASE → REQUIREMENT → ARCHITECTURE → 링크) |
| S-3 | VlmMetaStep 폐기 시 잔존 호출처 | LOW | grep 으로 0건 재확인. BatchOrchestrator 에서 이미 호출 제거됨 (Phase 1) |

### 수용 기준
- `ccarch_get_wiki_stats` 결과: 노드 ≥ 76, 링크 ≥ 130
- 업로드 결과 리포트(`reports/ccarch-upload-*.md`) 가 reports/ 디렉토리에 생성됨
- VlmMetaStep 클래스 + 테스트 파일 git에서 삭제됨

### 실행 가이드
PM이 직접 `mcp__ccarch__ccarch_*` 도구 순차 호출 + VlmMetaStep 삭제 (developer Agent 불필요)

---

## 실행 가이드 (요약)

1. **Phase 1** ✅ 완료 — 커밋 `c6c7fd1`
2. **Phase 2** ✅ 완료 — 커밋 대기
3. **Phase 3**: `/cc-tdd Phase 3: Deidentify 비동기 + Gitea fallback 큐 (V41)` → `/cc-code-review` → `/cc-commit`
4. **Phase 4**: `/cc-generate Phase 4: 비동기 표준 컬럼 (V42/V43) + AugmentResultService race 흡수` → 문서 수정 (PM 직접) → `/cc-commit`
5. **Phase 5**: ccarch 업로드 + VlmMetaStep 삭제 → `/cc-commit`
6. 전체 완료 후: `/cc-verify` (통합 시나리오 검증) → `/cc-deploy-check`

## 결정 사항 (요약)

| 항목 | 결정 | 근거 |
|---|---|---|
| VLM 책임 | **외부 위탁 전환** | CLAUDE.md V1.8 방침 + ccarch 정의 + Gap A-2/C-2 일치 |
| 비동기 결과 수신 | **콜백 웹훅** | 즉시 구현 가능, 큐 인프라 협의 불필요 |
| 인증 방식 | **HMAC 시그니처 + timestamp + allowlist** | OWASP A07 + A08 (Data Validation) 준수 |
| 범위 | **전체 14건 (5 Phase)** | CRITICAL + HIGH + MEDIUM + LOW + ccarch 업로드 |
| ai-server vlm.py | **객체 검증만 유지, 영상 메타 추출 제거** | V1.8 + ccarch `comp-ai-server` 정의 |
| Migration 번호 | V39/V40 (Phase 2) → V41 (Phase 3) → V42/V43 (Phase 4) | 충돌 방지 + 순차 배포 안전성 |
