# 구현 계획: R1(v1.16) ↔ 코드 정합 — 해상도·올가미·SAM2 분할·비식별 신고

> 생성: 2026-06-04
> 상태: 완료 ✅ (Phase 1·2·3R·4·5 — Phase 3 올가미는 08-01 VOS 재정정으로 구현 후 제거)
> 기준 문서: docs/design/R1-사용자요구사항정의서.md (v1.16) / R2-유스케이스명세서.md
> 사용자 확정: ① 해상도 화이트리스트 = RES_1080P/RES_720P/RES_480P ② 신고 시 삭제 범위 = 영상(rawSn) 전체 프레임 라벨

## Phase 진행 현황

- [x] Phase 1: 해상도 변경 R1 정합 (BE) — 표준 하위 해상도·이미지셋만·라벨 미제공
- [x] Phase 2: 비식별 신고 흐름 R1 정합 (BE) — 신고 시 영상 전체 라벨 삭제 + 수동 비식별화 RESOLVED API
- [x] Phase 3: 자석 올가미 도구 신규 (FE) — ⚠ 08-01 VOS 재정정으로 폐기 → Phase 3R에서 제거
- [x] Phase 3R: 올가미 전면 제거 (FE) — 08-01 재정정(올가미→VOS), LASSO 도구·scissors·opencv.js 제거
- [x] Phase 4: SAM2 클릭/박스 분할 UI 완성 (BE 프록시 + FE 도구)
- [x] Phase 5: 문서·주석 정합 (CLAUDE.md + 엔티티 주석) — 08-01=VOS(기존 SAM2 Track+분할 매핑) 반영

## 요구사항 요약

- 목적: R1 v1.8~1.16에서 재정의된 요구사항(해상도 변경 정책, 자석 올가미, SAM2 분할, 비식별 신고 흐름)을 코드에 반영
- 범위: RQ-SFR-06-03 / 08-01 / 08-02 / 09-03 관련 코드. (08-03/04/05, 07-01/02/03은 갭 분석 결과 이미 정합 — 변경 없음)
- 주요 액터: REVIEWER(해상도 변경 요청·신고 해소), WORKER(올가미·SAM2 라벨링·비식별 신고)
- 비기능: CWE-20 좌표 검증, CWE-209 내부 경로 미노출, IDOR 차단, OpenCV.js 지연 로딩(번들 영향 0)

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | `LS_RESOLUTION_EXPORT` 신규 (Flyway V55 — 적용됨). 기존 테이블 변경 없음 |
| 기존 API | `POST /v1/videos/{rawSn}/resolution` 계약 변경 완료. 신규 예정: `POST /v1/deident-reports/{rprtSn}/resolve`, `POST /v1/frames/{srcSn}/sam2-segment` |
| 도메인 모듈 | `video`(해상도 — 완료), `label`(신고·라벨 삭제), `frontend/features/label`(올가미·SAM2 도구), `webhook` 변경 없음 |
| 설정/환경 | frontend: opencv.js(WASM) 에셋 추가 (동적 import, public asset) |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | 해상도 변경 기존 데이터(P75 등 자식 영상) 잔존 | MED | 기존 RESOLUTION 자식 영상은 그대로 두고 신규 경로만 변경 (마이그레이션 강제 없음) |
| 2 | OpenCV.js WASM(~8MB) 초기 번들 오염 | HIGH | LASSO 도구 최초 활성화 시 동적 import — 초기 번들 0 영향. 로딩 중 스피너 |
| 3 | 신고 시 영상 전체 라벨 삭제 — 오신고 시 라벨 유실 | HIGH | 삭제 전 전 건 `LS_DATA_LBL_HSTRY` 이력 기록(기존 패턴) — 복구 가능성 보존 |
| 4 | Flyway 기적용 V7 주석 수정 시 checksum 깨짐 | HIGH | 기적용 마이그레이션 파일은 절대 수정 금지 — 엔티티/문서 주석만 정정 |
| 5 | 자동 재비식별 큐 제거 → 기존 DeidentifyStep 자동 resolve 경로 영향 | MED | 신고 경로의 큐 적재만 제거, 배치 재시도 인프라(BatchRetryQueue) 자체는 유지 |

---

## 상세 계획

### Phase 1: 해상도 변경 R1 정합 (BE) ✅

**목표**: 해상도 변경이 표준 하위 해상도(1080p/720p/480p) 화이트리스트 기반 **프레임 이미지셋 다운스케일**로 동작하고, 영상 재생성·라벨 복사·새 PENDING 영상 생성이 제거된다. (RQ-SFR-06-03 v1.8/1.10)

> 구현: ResolutionPreset(RES_1080P/720P/480P) · VideoResolutionService(첫 프레임 실측 업스케일 거부, 프레임 청크 다운스케일, 디렉토리 멱등 정리, DIV→409) · VideoResolutionPersister(EXPORT 1행만, REQUIRES_NEW) · ImageResizer+Java2DImageResizer 신규 · LsResolutionExport+Repository 신규 · V55 마이그레이션 · VideoController 정합 | 삭제: LabelCoordinateScaler, VideoResizer/BrampVideoResizer, LsDataRaw.createFromResolution, LsDataLbl.copyForNewSrcScaled
> QA: build PASS (테스트 실패는 전부 DOCKER_DOWN 환경 이슈, VideoResolutionServiceTest 13건 등 단위 PASS) · code-review CONDITIONAL PASS (CRITICAL/HIGH 0)
> 잔여 개선(비차단): ① MEDIUM — VideoResolutionService.loadAndValidate self-invocation으로 @Transactional(readOnly) 미적용 (기존 패턴 동일) ② LOW — resizeMaxConcurrent=0 시 전면 차단 의도 주석 ③ LOW — @ResponseStatus(CREATED)+ApiResponse.ok() 불일치 ④ VideoProbe/BrampVideoProbe dead 컴포넌트 잔존(미주입) ⑤ ControllerTest 6건 Docker 기동 후 재검증 필요

### Phase 2: 비식별 신고 흐름 R1 정합 (BE) ✅

**목표**: 라벨 작업 중 비식별 누락 신고 시 **해당 영상(rawSn) 전체 프레임 라벨이 이력 기록 후 삭제**되고, 작업자/검수자가 외부 솔루션 수동 비식별화 완료 후 **신고를 RESOLVED로 수동 전이**할 수 있다. (RQ-SFR-09-03 v1.14, 삭제 범위 = 사용자 확정: 영상 전체)

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/.../label/service/DeidentReportService.java` | 수정 | ① `report()`: 신고 직후 해당 영상(rawSn) 전체 프레임 라벨을 이력 기록 후 삭제 + 자동 재비식별 큐 적재 제거 ② `resolveManually(Long rprtSn, TokenClaims actor)` 신규 |
| `backend/.../label/controller/DeidentReportController.java` | 수정 | `POST /v1/deident-reports/{rprtSn}/resolve` 추가 (WORKER/REVIEWER), 주석 정정 |
| `backend/.../label/service/DeidentReportServiceTest.java` | 수정 | 작업 삭제·수동 resolve 케이스 추가 |

**핵심 로직 / 시그니처**:
```java
// DeidentReportService
@Transactional
public DeidentReportResponse report(Long srcSn, DeidentReportRequest req, TokenClaims actor);
// 변경: 신고 저장(OPEN) → 작업락(유지) → DE_IDNTF_YN='F'(유지)
//   + 해당 영상(rawSn) 전체 프레임 라벨을 LS_DATA_LBL_HSTRY 이력 기록 후 일괄 삭제 (신규)
//   - retryQueue.enqueueIfRetryable(...) 호출 제거 (자동 재비식별 폐기 — R1 v1.14 수동 흐름 대체)

@Transactional
public void resolveManually(Long rprtSn, TokenClaims actor);
// OPEN 상태만 RESOLVED 전이 (그 외 409), 작업락 해제, 권한 검증 (WORKER/REVIEWER)
```

**테스트 케이스** (RED):
- `신고시_영상_전체_라벨이_이력_기록_후_삭제됨`
- `신고시_재비식별_큐에_적재되지_않음`
- `수동_비식별화_완료시_신고가_RESOLVED로_전이되고_작업락_해제`
- `OPEN이_아닌_신고_resolve_요청시_409`
- `미인증_사용자_resolve_요청시_401` / `권한없는_역할_403`
- `원본_영상_경로는_변경되지_않음` (기존 유지)

**수용 기준**:
- [ ] 신고 → 영상 전체 라벨 삭제(이력 보존) + 작업락 + DE_IDNTF_YN='F'
- [ ] `POST /v1/deident-reports/{rprtSn}/resolve` 로 RESOLVED 전이
- [ ] 신고 경로에서 자동 재비식별 큐 적재 코드 제거 (DeidentifyStep 자동 resolve 등 배치 인프라는 유지)

**의존성**: 없음 (Phase 1과 독립 — 순차 진행)

> 구현: DeidentReportService(영상 전체 라벨 스냅샷+ATTR→AI→LBL 순 삭제+TASK_MODIFIED 통지+retryQueue 제거+resolveManually) · DeidentReportController(POST /v1/deident-reports/{rprtSn}/resolve) · LabelAccessGuard.verifyRawAccess · VersionService.snapshotDeidentReport(ACTIVE_N, SAVE_REASON=DEIDENT_REPORT) · LsDataLblRepository.deleteAllByRawSn 등 | 마이그레이션 불필요
> QA: build PASS (Phase1+2 단위 41/41 PASS, 실패 전부 DOCKER_DOWN) · code-review CONDITIONAL PASS (CRITICAL/HIGH 0)
> 잔여 개선(비차단): ① MEDIUM — DeidentifyStep.run()과 resolveOpenReports 간 WorkLock 해제 이중 책임 (멱등이라 무해) ② LOW — LsDataLblAttrValRepository.deleteByLblSnIn에 @Query bulk JPQL 추가 권장 ③ Controller 테스트 Docker 기동 후 재검증

### Phase 3: 자석 올가미 도구 신규 (FE) ✅→⚠ 폐기 (2026-06-04 사용자 재정정: 08-01 올가미→VOS(추적+분할). 기존 SAM2 Track+Phase 4 분할이 VOS 커버. 구현물은 Phase 3R에서 제거)

**목표**: WORKER가 LASSO 도구로 객체 외곽을 따라 그리면 엣지 검출 기반으로 경로가 경계에 자동 스냅되어 정밀 폴리곤이 생성·저장된다. (RQ-SFR-08-01 v1.15, CVAT intelligent scissors 패턴 — OpenCV.js `IntelligentScissorsMB` 클라이언트 측, BE 변경 불요)

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/features/label/types.ts` | 수정 | `ToolType`에 `LASSO: 'LASSO'` 추가 |
| `frontend/src/features/label/components/ToolBar.tsx` | 수정 | 올가미 도구 버튼 (단축키 L) |
| `frontend/src/features/label/canvas/tools/MagneticLassoTool.tsx` | 생성 | 도구 토글 + OpenCV.js 지연 로딩 상태 표시 |
| `frontend/src/features/label/canvas/scissors/intelligentScissors.ts` | 생성 | IntelligentScissorsMB 래퍼 — `applyImage(imageData)`, `buildMap(anchor)`, `getContour(cursor): Point[]` |
| `frontend/src/features/label/canvas/scissors/loadOpenCv.ts` | 생성 | opencv.js 동적 import 싱글톤 (최초 LASSO 활성화 시만 로드) |
| `frontend/src/features/label/canvas/layers/OverlayLayer.tsx` | 수정 | LASSO 케이스: 클릭=앵커 추가 → 커서 이동 시 스냅 경로 미리보기 → 더블클릭/시작점 근접 시 폴리곤 확정 |
| `frontend/public/opencv/opencv.js` | 생성 | OpenCV.js 4.x 에셋 (CDN 의존 금지 — 로컬 서빙) |
| `frontend/src/features/label/canvas/scissors/__tests__/intelligentScissors.test.ts` | 생성 | 래퍼 단위 테스트 (cv mock) |

**핵심 로직**:
```typescript
// intelligentScissors.ts — CVAT 패턴
// 1) 프레임 이미지 → cv.Mat → scissors.applyImage(mat) (도구 활성화/프레임 전환 시 1회)
// 2) 클릭(앵커): scissors.buildMap(anchorPoint)
// 3) mousemove: scissors.getContour(cursorPoint) → 스냅 경로 폴리라인 미리보기
// 4) 다음 클릭: 현재 스냅 경로를 확정 세그먼트로 누적
// 5) 더블클릭/시작점 근접: 폴리곤 닫기 → 기존 폴리곤 저장 흐름 재사용 (CWE-20: 좌표 캔버스 범위 클램프)
// 미세조정: 확정 후 SELECT 도구의 기존 정점 편집 그대로 사용
```

**테스트 케이스**:
- `올가미_도구_활성화시_OpenCV가_지연_로딩됨` (초기 번들에 opencv 미포함)
- `앵커_클릭과_커서_이동시_스냅_경로가_미리보기됨` (cv mock)
- `더블클릭시_폴리곤이_닫히고_라벨_저장_요청_발생`
- `좌표가_이미지_범위로_클램프되어_저장됨`
- `OpenCV_로딩_실패시_일반_폴리곤_도구로_폴백_안내`

**수용 기준**:
- [ ] LASSO 도구로 그린 경로가 엣지에 스냅된 폴리곤으로 저장됨
- [ ] 스냅 결과를 기존 정점 편집으로 미세조정 가능
- [ ] 초기 JS 번들 크기 증가 없음 (동적 로딩)
- [ ] 본인 미배정 프레임 접근 차단은 기존 라벨 저장 API IDOR 가드 재사용

**의존성**: 없음

> 구현: scissors/{cvTypes,loadOpenCv(싱글턴+즉시초기화 분기),intelligentScissors(Mat 추적 dispose, 2MP 다운스케일+역산, CORS catch)} · MagneticLassoTool(지연 로딩+cleanup) · OverlayLayer LASSO 케이스(clampToImage+simplifyPolygon 1000상한) · polygonHelpers.simplifyPolygon(D-P) · ToolBar(L, error시 disabled) · useLabelingShortcuts(L+cvStatus 가드) · useLabelStore.cvStatus · vite.config CSP wasm-unsafe-eval · public/opencv/opencv.js(4.10.0, 10MB)
> QA: build PASS · vitest 672/672 PASS(회귀 0, 번들 opencv 미포함) · code-review CONDITIONAL PASS → DEV_FIX 1회(loadOpenCv pending 버그+단축키 가드) 후 PASS
> 잔여(비차단): ① CSP 'unsafe-inline' 기존 설정 — 운영 빌드 정책 별도 검토 ② opencv.js 10MB 레포 추가 — Git LFS/배포 다운로드 전환 검토 ③ 실 OpenCV 런타임 시각 검증은 서버 기동 후 /cc-verify (ui-tester 환경 ESCALATE) ④ Canny 파라미터(32/100/200) 실데이터 튜닝 여지

### Phase 4: SAM2 클릭/박스 분할 UI 완성 (BE 프록시 + FE 도구) ✅

**목표**: 사용자가 캔버스에서 클릭/박스로 객체를 지목하면 SAM2 분할 폴리곤+신뢰도가 반환되어 라벨로 적용된다. (RQ-SFR-08-02)

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/.../label/controller/Sam2SegmentController.java` (또는 기존 컨트롤러) | 생성/수정 | `POST /v1/frames/{srcSn}/sam2-segment` — 배정 검증(IDOR) → AiServerClient `/infer/sam2/segment` 프록시 → 좌표 검증(CWE-20) 후 반환 |
| `backend/.../label/service/Sam2SegmentService.java` | 생성 | 프레임 이미지 로드 → ai-server 호출(Resilience4j 60s) → 폴리곤 좌표 이미지 범위 검증 |
| `backend/.../label/dto/Sam2SegmentRequest/Response.java` | 생성 | `{points?, box?}` → `{polygon, score}` (@Valid, 최소 1개 프롬프트 필수) |
| `frontend/src/features/label/types.ts` | 수정 | `ToolType.SAM_SEGMENT` 추가 |
| `frontend/src/features/label/canvas/tools/Sam2SegmentTool.tsx` | 생성 | 클릭(포인트)/드래그(박스) → segment 호출 → 폴리곤 적용 + 신뢰도 표시 |
| `frontend/src/features/label/api.ts` | 수정 | `requestSam2Segment(srcSn, {points?, box?})` 추가 |
| `frontend/.../components/ToolBar.tsx` | 수정 | 도구 버튼 추가 |

**테스트 케이스** (RED):
- `클릭_프롬프트로_폴리곤과_신뢰도가_반환됨`
- `포인트와_박스_모두_없으면_400`
- `본인_미배정_프레임_요청시_403` (IDOR)
- `ai서버_폴리곤_좌표가_이미지_범위_밖이면_검증_후_클램프_또는_400`
- `ai서버_타임아웃시_502_매핑_및_서킷브레이커_동작`

**수용 기준**:
- [ ] 클릭/박스 입력 → SAM2 폴리곤이 경계에 밀착되어 라벨로 저장
- [ ] 응답 좌표 검증(CWE-20) 후 저장, 미배정 프레임 403
- [ ] Resilience4j 타임아웃/재시도 적용

**의존성**: Phase 3 (ToolBar/types 동시 수정 충돌 방지 — 순차)

> 구현: BE — Sam2SegmentService(IDOR verifyAccess, resolveSafe, 20MB 상한, 좌표 실측 상한 검증, 3점 미만 거부, mock 전파) + Sam2SegmentRequest(@AssertTrue points/box 배타)/Response + LabelController POST /v1/frames/{srcSn}/sam2-segment + Sam2Response mock/source 필드(2-arg 호환 생성자) | FE — ToolType.SAM_SEGMENT(단축키 G), useSam2Segment(isSegmenting+stale srcSn 폐기), OverlayLayer 클릭/드래그 케이스, mock 경고+자동적용 차단, CanvasShell 배너
> QA: build PASS (BE 컴파일+신규 단위 13건, FE 683/683 회귀 0) · code-review PASS (LOW 1) · fullstack-coordinator 계약 PASS (URL/필드/에러코드 일치)
> 잔여(비차단): ① WARN — FE 413/502 에러코드별 사용자 안내 분기 미구현(에러는 throw됨, UX 보완) ② LOW — Sam2SegmentService IOException 원문 메시지 고정 문자열 교체 권장(CWE-209 잠재) ③ 정보 — ImageIO 힙·block() 동기화 성능은 트래픽 증가 시 프로파일링 ④ mock시 "그래도 적용" 명시 버튼은 후속 UX

### Phase 5: 문서·주석 정합

**목표**: CLAUDE.md·코드 주석의 구 정책 서술을 R1 v1.16 기준으로 갱신 (코드 로직 변경 없음).

**수정 파일**:
| 파일 | 변경 |
|------|------|
| `CLAUDE.md` | ① "증강 4종 WINTER/NIGHT/RAIN/RESOLUTION" → 외부 증강 3종 + 해상도 변경은 저작도구 내부(이미지셋·라벨 미제공) ② "해상도 변경 증강은 이미지 해상도만 변경하여 저장" 서술 정정 ③ 비식별 신고: "작업락+재비식별 큐+자동 RESOLVED" → "작업락+영상 전체 라벨 삭제(이력 보존)+수동 비식별화→RESOLVED" ④ 라벨링 도구에 자석 올가미 명시 |
| `backend/.../augment/entity/LsDataAug.java` | 주석 "4종" → 신규 3종(기존 RESOLUTION 데이터 호환 상수 유지 명시) |
| ⚠️ 기적용 Flyway 파일(V7 등) | **수정 금지** (checksum) — 주석 정정 대상에서 제외 |

**수용 기준**:
- [ ] CLAUDE.md가 R1 v1.16 정책과 일치 (증강 3종, 해상도 내부 다운스케일, 신고 수동 흐름, 올가미)

**의존성**: Phase 1~4 완료 후

> 구현: CLAUDE.md ① 증강 4종→외부 3종+해상도 내부(SFR-06-03 다운스케일 이미지셋·라벨 미제공·LS_RESOLUTION_EXPORT) ② SFR-08-01=VOS(추적+분할) 해석 명시(올가미 폐기, sam2-track+sam2-segment 매핑) ③ 비식별 신고 수동 흐름(영상 전체 라벨 삭제+스냅샷+resolve API+TASK_MODIFIED) | LsDataAug javadoc 3종+RESOLUTION 호환 상수 사유
> QA: build-validator compileJava/TestJava PASS (문서·주석 Phase — code-review 스킵)

---

## 실행 가이드

1. `/cc-tdd Phase 1: 해상도 변경 R1 정합` ✅ → `/cc-tdd Phase 2: 비식별 신고 흐름 R1 정합` ✅
2. `/cc-tdd Phase 3: 자석 올가미 도구` ✅ → `/cc-tdd Phase 4: SAM2 클릭/박스 분할 UI` ✅
3. "Phase 5 진행해줘" — 문서·주석 정합 (코드 없음) ✅

## 전체 완료 후 잔여 항목 (커밋 후 처리 권장)
- Docker 기동 후 `./gradlew test` 전체 재실행 (@SpringBootTest/Testcontainers 슬라이스 — 현재 전부 DOCKER_DOWN 스킵 상태)
- FE/BE 서버 기동 후 `/cc-verify` 통합 시나리오 검증 (SAM2 분할 실런타임, 해상도 변경, 신고 흐름)
- 잔여 개선(비차단): Phase 1·2·4 각 섹션의 MEDIUM/LOW 항목 참조
