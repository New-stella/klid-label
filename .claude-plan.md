# 구현 계획: 게시판(공지사항/가이드라인) 기능 추가

> 생성: 2026-06-04
> 상태: **전체 완료 (2026-06-05)** — Phase 1~4 QA PASS
> 성격: **R1 요구사항(SFR-06~09) 외 추가 기능** — v1 대비 누락 기능(`docs/v1-wiki/19-v2-gap-checklist.md` 🟡 후보) 중 "게시판"을 우선 구현. v1 `LS_NTC_BBS`/[v1-wiki 13](docs/v1-wiki/13-board-practice.md) 참고.

## Phase 진행 현황

- [x] Phase 1: 게시판 BE — LS_NOTICE CRUD·발행상태·고정·검색·페이징·권한 ✅ 2026-06-05 (QA PASS — 리뷰 0C/0M/5m, 전체 1,044 테스트 통과)
- [x] Phase 2: 첨부파일 BE — LS_NOTICE_ATTACH 업로드/다운로드/삭제 + 검증 ✅ 2026-06-05 (QA PASS — DEV_FIX 1회 후 재리뷰 0C/0M, 전체 1,055 테스트 통과)
- [x] Phase 3: 게시판 FE — 목록/상세/작성 화면 + API 클라이언트 + 라우트/LNB ✅ 2026-06-05 (QA PASS — DEV_FIX 1회 후 재리뷰 0C/0M, 빌드+notice 테스트 통과)
- [x] Phase 4: 문서 — 위키 갱신(v2-wiki 신규 + v1-wiki/19 체크) + 화면 채번 ✅ 2026-06-05 (PM 직접 — 링크 검증 0건 깨짐)

## 진행 로그

- 2026-06-05 Phase 4 완료(PM 직접): v2-wiki/20-notice-board.md 신규(개요/권한/상태전이/API/첨부보안/테이블/화면/v1대비) + README 목차·빠른찾기·화면ID + 04-screens-ia(SC-030/031/032, 활성 26개) + 18-database(V56, LS_NOTICE 행) + v1-wiki/19 게시판 체크(R1 외 추가 구현 명시, 잔여 후보 4개). 내부 링크 검증 통과.
- 2026-06-05 Phase 3 완료: features/notice(types/schemas/api/hooks×2/EditModal/parseNoticeListParams) + NoticeList/DetailPage + router(`/notice`,`/notice/:id` InternalRoute)/Lnb('게시판')/queryKeys(NOTICE_KEYS). 신규 테스트 12건 PASS, 빌드·lint PASS. 1차 리뷰 FAIL(MAJOR 2 — 이중 단언, useState→useSearchParams 컨벤션) → DEV_FIX → 재리뷰 PASS. 이월 MINOR: size URL 미반영, applySearch replace, 입력 UI 뒤로가기 재동기화, tr onClick 접근성, -1 placeholder 캐시 키. **FE 사전 존재 실패 11건(features/auth — jsdom localStorage shim)은 게시판과 무관 — 별도 수정 후보.**
- 2026-06-05 Phase 2 완료: LsNoticeAttach 엔티티/Repository/Service/Controller/DTO + NoticeService.delete 파일정리 연계 + NoticeResponse.attachments 추가. 테스트 11건 신규(확장자/크기/경로순회/IDOR/DRAFT 가시성/물리파일 정리) + Phase 1 회귀 13건 PASS. 1차 리뷰 FAIL(MAJOR 2 — 롤백 고아파일, delete 순서 역전) → DEV_FIX: afterCompletion(ROLLED_BACK) 고아파일 정리 + DB 삭제 선행·afterCommit 파일 삭제 + cleanup 일원화(@Lazy) → 재리뷰 PASS. 신규 MINOR 2건 이월(cleanupPhysicalFiles @Transactional 명시, @Lazy 순환을 existsById로 제거 가능).

- 2026-06-05 Phase 1 완료: notice 도메인 신규(entity/repository×2/service/controller/dto×4) + V56(LS_NOTICE+LS_NOTICE_ATTACH) + SecurityConfig `/v1/notices/**`→hasAnyRole(REVIEWER,WORKER) 매처 추가. 테스트 13건(권한·DRAFT 가시성·발행 멱등·검색 이스케이프·페이징) PASS. 시나리오 HIGH 8건 전부 방어. 리뷰 MINOR 5건(JPAQueryFactory 재사용, 목록 쿼리 TEXT 컬럼 로드, pg_trgm 인덱스 후보, delete TOCTOU, PUT/DELETE/unpublish 테스트 누락)은 비차단 — Phase 2 또는 후속 개선에서 반영 검토.

## 요구사항 요약

- **목적**: 관리자(REVIEWER)가 라벨러에게 공지/가이드라인을 게시·배포하고, 작업자(WORKER)가 열람·첨부 다운로드. v1 게시판 기능의 v2 재구현.
- **범위(포함)**: 공지 CRUD, 임시저장/발행 상태, 상단 고정, 제목/내용 검색, 페이징, 첨부파일(가이드 다운로드), 권한 분기.
- **범위(제외)**: 댓글, 조회수, 카테고리, 포털 채널 노출(PORTAL_USER 제외 — 내부 채널 전용).
- **주요 액터**: REVIEWER(작성/수정/삭제/발행/첨부), WORKER(발행글 열람·다운로드).
- **비기능**: 권한 경계(쓰기=REVIEWER), 파일 업로드 보안(확장자 allowlist·크기·경로순회·MIME), 페이징 필수.

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | **신규** `LS_NOTICE`, `LS_NOTICE_ATTACH` (V56 마이그레이션 — 적용됨) |
| 기존 API | 없음 (신규 `/v1/notices*`) |
| 도메인 모듈 | **신규** `kr.co.cudo.authoring.notice` |
| 보안 설정 | `SecurityConfig` — `/v1/notices/**` hasAnyRole(REVIEWER,WORKER) 매처 추가됨 (PORTAL_USER 차단) |
| 설정/환경 | 첨부 저장 경로 = `STORAGE_RAW_PATH` 하위 `notice-attach/` 서브디렉토리 재사용 (신규 env 없음) |
| FE | 신규 `features/notice`, `pages` 3종, `router`·`Lnb`·`queryKeys` 수정 |
| 문서 | v2-wiki 신규 페이지(게시판) + README, v1-wiki/19 체크박스 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | 파일 업로드 취약점(확장자·경로순회·크기) | HIGH | allowlist(pdf/doc/docx/xlsx/png/jpg/zip/hwp) + UUID 재명명 + `normalize()` + 크기 상한(20MB) + MIME 검증 |
| 2 | DRAFT(임시저장) 글이 WORKER에 노출 | MED | ✅ Phase 1에서 방어 — 목록/상세 역할 분기 (WORKER=PUBLISHED만, DRAFT 상세 404) |
| 3 | 첨부 다운로드 IDOR/경로노출 | MED | 다운로드 시 attach가 path notice 소속 검증, 응답에 내부 절대경로 미노출(CWE-209) |
| 4 | 검색어 LIKE 인젝션/로그 인젝션 | LOW | ✅ Phase 1에서 방어 — QueryDSL 바인딩 + `% _ \` 이스케이프 + field enum 화이트리스트 |

---

## 상세 계획

### Phase 1: 게시판 BE — LS_NOTICE CRUD·발행상태·고정·검색·페이징·권한 ✅ 완료

**목표**: 첨부 제외 게시판 CRUD가 동작. REVIEWER 작성/수정/삭제/발행, WORKER 발행글 열람. 제목/내용 검색 + 페이징.

**생성·수정 파일** (전부 완료):
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/.../notice/entity/LsNotice.java` | 생성 ✅ | Aggregate Root, 정적 팩토리, publish/unpublish 멱등, NOTICE_CN columnDefinition=TEXT |
| `backend/.../notice/repository/LsNoticeRepository.java` | 생성 ✅ | `@ControlRepo` JpaRepository |
| `backend/.../notice/repository/LsNoticeQueryRepository.java` | 생성 ✅ | QueryDSL 검색(SearchField enum + LIKE 이스케이프) + 고정 우선 정렬(3차 키 PK) + 페이징 |
| `backend/.../notice/service/NoticeService.java` | 생성 ✅ | `@Transactional("controlTransactionManager")` CRUD·발행·역할 가시성 판정 |
| `backend/.../notice/controller/NoticeController.java` | 생성 ✅ | `/v1/notices` REST, ApiResponse, 쓰기 @PreAuthorize(REVIEWER) |
| `backend/.../notice/dto/Notice{Create,Update}Request.java` | 생성 ✅ | record + @Valid |
| `backend/.../notice/dto/Notice{Response,SummaryResponse}.java` | 생성 ✅ | 목록 요약은 내용 제외 |
| `backend/.../common/security/SecurityConfig.java` | 수정 ✅ | `/v1/notices/**` hasAnyRole(REVIEWER,WORKER) — `/v1/**` 위에 배치 |
| `backend/src/main/resources/db/migration/V56__create_ls_notice.sql` | 생성 ✅ | LS_NOTICE + LS_NOTICE_ATTACH DDL |

**테스트 결과**: 13/13 PASS — REVIEWER 작성 DRAFT 생성, WORKER 쓰기 403, PORTAL_USER 목록 403, WORKER 목록 DRAFT 제외, REVIEWER 고정글 상단정렬, WORKER DRAFT 상세 404, 발행 시 PUB_DT 설정, 재발행 멱등, 제목 검색, `%` 와일드카드 무력화, 페이징 분할, 미존재 404, 잘못된 field 400.

**수용 기준**:
- [x] REVIEWER 작성/수정/삭제/발행 200·201·204, WORKER 쓰기 403
- [x] WORKER 목록·상세에 DRAFT 미노출, REVIEWER 전체 노출
- [x] 고정글이 목록 최상단, 이후 최신순
- [x] 제목/내용 검색 + 페이징 동작
- [x] V56 마이그레이션이 validate 모드에서 엔티티와 정합 (전체 테스트 기동 통과로 확인)

**리뷰 MINOR 이월 항목** (비차단): JPAQueryFactory 호출별 생성 → 재사용, 목록 쿼리 Projection으로 TEXT 미로드, pg_trgm GIN 인덱스(차기 마이그레이션), delete의 existsById+deleteById → findById+delete, PUT/DELETE/unpublish 테스트 보강.

---

### Phase 2: 첨부파일 BE — LS_NOTICE_ATTACH 업로드/다운로드/삭제 + 검증

**목표**: 공지에 가이드라인 파일 첨부·다운로드. 업로드 보안 가드 적용.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/.../notice/entity/LsNoticeAttach.java` | 생성 | 첨부 엔티티(FK NOTICE_SN), 저장/원본 파일명·크기·경로 |
| `backend/.../notice/repository/LsNoticeAttachRepository.java` | 생성 | `@ControlRepo` |
| `backend/.../notice/service/NoticeAttachService.java` | 생성 | MultipartFile 검증·저장(UUID)·다운로드 리소스·삭제 |
| `backend/.../notice/controller/NoticeAttachController.java` | 생성 | 업로드(multipart)/다운로드/삭제 |
| `backend/.../notice/dto/NoticeAttachResponse.java` | 생성 | attachId·원본명·크기 (절대경로 미노출) |

> `LS_NOTICE_ATTACH` DDL은 V56(Phase 1)에 포함 완료 — 추가 마이그레이션 불요.
> 공지 삭제 시 첨부 파일(물리 파일) 정리 — NoticeService.delete와 연계 필요.

**API**:
| 메서드 | 경로 | 권한 |
|--------|------|------|
| POST | `/v1/notices/{id}/attachments` (multipart) | REVIEWER |
| GET | `/v1/notices/{id}/attachments/{attachId}/download` | authenticated(REVIEWER/WORKER) — DRAFT 공지의 첨부는 REVIEWER만 |
| DELETE | `/v1/notices/{id}/attachments/{attachId}` | REVIEWER |

**보안 가드 (DevAutolabelTestService 패턴 참고)**:
- 확장자 allowlist: `pdf,doc,docx,xls,xlsx,ppt,pptx,png,jpg,jpeg,zip,hwp,hwpx` (CWE-434)
- 크기 상한 20MB (`PAYLOAD_TOO_LARGE`)
- UUID 재명명 + `Path.normalize()` + 기준경로 startsWith 검증 (CWE-22)
- 다운로드 시 attach의 notice 소속 검증(IDOR), 절대경로 미노출(CWE-209)
- 원본 파일명 로그 출력 금지(UUID만, CWE-117)
- DRAFT 공지 첨부의 WORKER 다운로드 차단(404) — Phase 1 가시성 규칙과 일관

**테스트 케이스 (cc-tdd RED)**:
- `허용확장자_파일_업로드시_저장되고_원본명_보존`
- `비허용확장자_exe_업로드시_400`
- `크기초과_파일_업로드시_413`
- `경로순회_파일명_업로드시_차단`
- `WORKER_업로드_시도시_403`
- `다른_공지의_첨부ID로_다운로드시_404`
- `WORKER가_발행글_첨부_다운로드_성공`
- `WORKER가_DRAFT_공지_첨부_다운로드시_404`

**수용 기준**:
- [ ] 허용 확장자만 저장, 그 외 400/413
- [ ] 다운로드 시 원본 파일명으로 응답(Content-Disposition), 절대경로 미노출
- [ ] 공지 삭제 시 첨부 cascade + 파일 정리
- [ ] WORKER 업로드/삭제 403, 발행글 첨부 다운로드 200

**의존성**: Phase 1 ✅

---

### Phase 3: 게시판 FE — 목록/상세/작성 화면 + API 클라이언트 + 라우트/LNB

**목표**: 내부 사용자가 게시판 목록·상세 열람, REVIEWER가 작성/수정/발행/첨부 수행.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/features/notice/api.ts` | 생성 | apiClient CRUD + 첨부 multipart/다운로드 |
| `frontend/src/features/notice/types.ts` | 생성 | Notice/NoticeForm/Summary/Attach 타입 |
| `frontend/src/features/notice/schemas.ts` | 생성 | zod 검증 |
| `frontend/src/features/notice/hooks/useNotices.ts` | 생성 | 목록/상세 useQuery |
| `frontend/src/features/notice/hooks/useNoticeActions.ts` | 생성 | create/update/delete/publish/첨부 useMutation |
| `frontend/src/features/notice/components/NoticeEditModal.tsx` | 생성 | 작성/수정 모달(제목·내용·고정·첨부) |
| `frontend/src/pages/NoticeListPage.tsx` | 생성 | 목록(검색·페이징·고정 배지·발행상태) |
| `frontend/src/pages/NoticeDetailPage.tsx` | 생성 | 상세(내용·첨부 다운로드 + REVIEWER 수정/발행/삭제) |
| `frontend/src/router/index.tsx` | 수정 | `/notice`, `/notice/:id` lazy 라우트(internal 전체 역할) |
| `frontend/src/components/layout/Lnb.tsx` | 수정 | 공통 그룹에 '게시판' 메뉴(전체 내부 역할 노출) |
| `frontend/src/lib/queryKeys.ts` | 수정 | NOTICE_KEYS 추가 |

**화면 채번 (D2 KLID-AT-SC)**:
- `KLID-AT-SC-030` 게시판 목록 `/notice`
- `KLID-AT-SC-031` 게시판 상세 `/notice/:id`
- `KLID-AT-SC-032` 게시판 작성/수정 (모달)

**테스트 케이스 (cc-tdd RED — FE)**:
- `목록_조회시_고정글이_상단에_표시됨`
- `REVIEWER에게만_작성버튼_노출`
- `WORKER에게는_작성수정버튼_미노출`
- `작성_폼_제목_미입력시_검증오류`
- `상세에서_첨부_다운로드_링크_표시`

**수용 기준**:
- [ ] 목록 검색/페이징/고정 배지/발행상태 표시
- [ ] REVIEWER만 작성·수정·발행·삭제·첨부 UI 노출, WORKER 열람 전용
- [ ] 작성/수정 모달 zod 검증
- [ ] 상세에서 첨부 다운로드 동작
- [ ] LNB '게시판' 메뉴가 내부 전체 역할에 노출

**의존성**: Phase 1 ✅, 2

---

### Phase 4: 문서 — 위키 갱신 + 화면 채번

**목표**: 게시판 추가를 위키·체크리스트에 반영(CLAUDE.md 문서 동기화 규칙 준수).

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `docs/v2-wiki/20-notice-board.md` | 생성 | 게시판 기능 페이지(권한·상태·첨부·API·테이블·화면ID) |
| `docs/v2-wiki/README.md` | 수정 | 목차/빠른찾기에 게시판 추가 |
| `docs/v2-wiki/04-screens-ia.md` | 수정 | SC-030/031/032 추가 |
| `docs/v2-wiki/18-database.md` | 수정 | LS_NOTICE·LS_NOTICE_ATTACH(V56) 추가 |
| `docs/v1-wiki/19-v2-gap-checklist.md` | 수정 | 게시판 항목 체크 + "R1 외 추가 구현" 주석 |

**수용 기준**:
- [ ] v2-wiki 게시판 페이지 생성 + README/04/18 갱신
- [ ] v1-wiki/19 게시판 체크박스 처리
- [ ] 내부 링크 깨짐 0

**의존성**: Phase 1~3 (구현 확정 후). 문서 Phase — PM 직접 처리.

---

## 실행 가이드

1. ~~`/cc-tdd Phase 1: 게시판 BE`~~ ✅ 완료 (2026-06-05)
2. `/cc-tdd Phase 2: 첨부파일 BE — LS_NOTICE_ATTACH 업로드/다운로드/삭제 + 보안 검증`
3. `/cc-tdd Phase 3: 게시판 FE — 목록/상세/작성 화면 + API 클라이언트 + 라우트/LNB`
4. "Phase 4 진행해줘" — 위키 갱신(문서 only, PM 직접)
