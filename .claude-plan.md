# 구현 계획: RESOLUTION(해상도 변경) 외부 증강 경로 분리 → 저작도구 내부 기능화

> 생성: 2026-06-02 / 상태: 진행 중 (Phase 1~3 완료, Phase 4 남음)
> 상세 계획서: ~/.claude/plans/compiled-rolling-meadow.md
> ※ 이전 .claude-plan.md(LogiCraft 산출물 재정비)는 docs/design/logicraft-1차폐기-고도화신규-재정비계획-2026-06-01.md + ~/.claude/plans/twinkling-weaving-river.md 에 원본 보존됨. 본 파일은 RESOLUTION 분리 작업으로 교체(사용자 승인).
> 🎯 완료 시: REVIEWER가 검수완료 영상에 `POST /v1/videos/{rawSn}/resolution`(배율 프리셋)로 해상도 변경 요청 → 저작도구가 직접 ffmpeg 리사이즈 + 라벨 좌표 스케일링 → 새 PENDING 영상 생성. 외부 증강 콜백에서 RESOLUTION 완전 제거.

## Phase 진행 현황

- [x] Phase 1: 외부 증강 경로에서 RESOLUTION + labelMapping 제거 — 외부 위탁/콜백을 WINTER/NIGHT/RAIN으로 축소
- [x] Phase 2: 좌표 스케일 유틸 + 라벨 스케일 복사 — POINT_CN을 배율로 안전 스케일
- [x] Phase 3: 해상도 변경 내부 기능 — 엔드포인트+서비스+ffmpeg/ffprobe로 새 PENDING 영상 생성
- [ ] Phase 4: 산출물 정합 갱신 (문서/LogiCraft, 코드 없음)

## 요구사항 요약

- 목적: RQ-SFR-06-03("해상도 변경은 저작도구가 직접 수행") 정합. 외부 증강 콜백의 RESOLUTION verbatim 복사로 인한 좌표 어긋남(스펙 위반) 제거. 죽은 필드 labelMapping[] 제거.
- 범위: 외부 증강(WINTER/NIGHT/RAIN) 경계 축소 + 내부 해상도변경 신규 기능. 제외: DB 스키마 변경 없음(PARENT_RAW_SN 추적).
- 주요 액터: REVIEWER(해상도 변경 요청), 시스템(ffmpeg/ffprobe)
- 비기능: 보안(CWE-78 ffmpeg 인자 주입 차단·CWE-22 경로·인가·DoS 동시성), 좌표 정합(RQ-SFR-07-02)

## 확정된 설계 결정

| 항목 | 결정 |
|------|------|
| 분리 방식 | 별도 내부 엔드포인트 `POST /v1/videos/{rawSn}/resolution`. 외부 위탁/콜백 미사용 |
| 프리셋 | 배율 프리셋(종횡비 보존) P75(0.75)/P50(0.5)/P25(0.25), enum. scaleX==scaleY==factor |
| 원본 해상도 | ffprobe 실시간 추출 (LsDataRaw에 w/h 필드 없음) |
| 증강 검수 큐 | 내부 해상도변경은 LS_DATA_AUG 미기록 — 새 LsDataRaw(PENDING)로 일반 검수 플로우 진입 |
| 상태행 | LsRawDataStatus는 배정 시점 upsert(증강 경로와 동일) — 신규 영상은 dataSttsCd=PENDING만 |

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | 없음 (LS_DATA_RAW/SRC/LBL/META/ATTR 재사용) |
| 기존 API | 증강 요청 타입·외부 콜백 augType 화이트리스트에서 RESOLUTION 제거 |
| 신규 API | `POST /v1/videos/{rawSn}/resolution` |
| 도메인 모듈 | augment(축소)·webhook(축소)·video(신규)·common.util/batch.entity(좌표 스케일) |

## 위험 요소 (전부 대응 완료)

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | 좌표 포맷 이원화(BBOX flat vs nested) | HIGH | flat/nested JSON 구조 자동감지 + 클램프 (Phase 2 DEV_FIX) |
| 2 | ffmpeg/ffprobe 바이너리 의존 | MED | VideoProbe/VideoResizer 추상화 + stub 테스트 |
| 4 | ffmpeg 인자 주입(CWE-78) | HIGH | ProcessBuilder 리스트 + enum factor/정수만 |
| 5 | 짝수 치수(코덱) | LOW | trunc(dim*f/2)*2 |
| 6 | 트랜잭션 중 ffmpeg(커넥션 고갈) | HIGH | ffmpeg 트랜잭션 밖 + DB INSERT만 REQUIRES_NEW (Persister 분리) |
| 7 | 고아 파일/부분실패 | HIGH | DB 실패 시 Files.deleteIfExists + 단일 트랜잭션 |
| 8 | src 경로 traversal(CWE-22) | MED | resolveSafe(normalize+base) src/dst 양쪽 (Phase 3 DEV_FIX) |
| 9 | ffmpeg DoS 동시성 | MED | Semaphore(fair) 상한 + tryAcquire 타임아웃 + 429 (Phase 3 DEV_FIX) |

---

## 상세 계획

### Phase 1: 외부 증강 경로에서 RESOLUTION + labelMapping 제거 ✅

**생성·수정**: AugmentRequestRequest(enum 3종), AugmentResultRequest(augType 패턴 축소 + labelMapping/LabelMap 제거), 테스트 4개 + Javadoc 정합(ExternalAugmentClient/AugmentReviewService).
**수용 기준**: [x] 외부 RESOLUTION 불가 [x] WINTER/NIGHT/RAIN 회귀 없음 [x] labelMapping 0건
> 구현 완료. QA: code-reviewer CONDITIONAL PASS(MEDIUM Javadoc 정리)·build PASS(964/965).

### Phase 2: 좌표 스케일 유틸 + 라벨 스케일 복사 ✅

**생성·수정**: common/util/LabelCoordinateScaler(flat/nested 자동감지·클램프·빈배열), LsDataLbl.copyForNewSrcScaled(null/blank/original 가드).
**수용 기준**: [x] 4종 타입 정확 스케일 [x] 경계 클램프 [x] JSON 안전 + 가드
> 구현 완료. QA: DEV_FIX 1회(HIGH-1 null가드·HIGH-2 BBOX 이원화 해소) → code-reviewer PASS(9.2)·build PASS(986/987). 테스트 17+28.

### Phase 3: 해상도 변경 내부 기능 (엔드포인트+서비스+ffmpeg/ffprobe) ✅

**생성·수정**: ResolutionPreset/Request/Response, VideoProbe+BrampVideoProbe, VideoResizer+BrampVideoResizer, VideoResolutionService(오케스트레이션) + VideoResolutionPersister(REQUIRES_NEW), LsDataRaw.createFromResolution, VideoController POST /resolution, LsDataLblAttrValRepository.findByLblSnIn.
**서비스 로직**: APPROVED+비증강본 검증 → src resolveSafe → ffprobe(WH검증) → target even(<2 거부) → 중복 자식 409 → Semaphore → ffmpeg resize → [REQUIRES_NEW] createFromResolution(PENDING) → 프레임/라벨(스케일)/속성값/메타 복사 → 고아 정리.
**수용 기준**: [x] APPROVED만 변환 [x] PENDING+PARENT_RAW_SN [x] 라벨 배율 스케일 복사 [x] 프레임/속성값/메타 복사
> 구현 완료. 시나리오 분석 HIGH 9종 방어 적용. QA: DEV_FIX 1회(보안 MEDIUM 2: src 경로 검증·ffmpeg 동시성 상한) → security-reviewer PASS·build PASS(1008/1009). 신규 테스트 23건(video 패키지 47건). code-reviewer 제기 의문(LsRawDataStatus 미생성)은 증강 경로와 동일 패턴(배정 시 upsert)으로 비이슈 확인.

### Phase 4: 산출물 정합 갱신 (코드 외) — 남음

**작업**: D4-인터페이스설계서(INT-006/008 RESOLUTION 제외 + 내부 기능 추가), 기능정의서 FEAT-004 보정, LogiCraft INT 스펙 노트(사용자 검토 후 등록).
**의존성**: Phase 1, 3 완료(충족)

---

## 실행 가이드

1. `/cc-tdd Phase 1` → `/cc-tdd Phase 2` (완료)
2. `/cc-tdd Phase 3` (완료)
3. `"Phase 4 진행해줘"` — D4·기능정의서·LogiCraft 정합 (문서)
