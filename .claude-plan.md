# 작업 계획: LogiCraft 산출물 재정비 (ERD·도메인·용어)

> 갱신: 2026-06-01 / 대상: LogiCraft `KLID-저작도구` (project_id `4ece2c3f-8e99-46f5-9580-71108a76e578`)
> 상세 계획서: `docs/design/logicraft-1차폐기-고도화신규-재정비계획-2026-06-01.md`
> 상세 메모리(NovaTerm): project layer id `3d6eefcb-0318-4b74-911d-678d0101423e`
>
> ※ 이전 계획(PostgreSQL 전환 + Gitea 제거, Phase 0~7)은 완료되어 본 내용으로 교체함.
> ※ **작업은 "Phase 번호"로 부르지 말 것** — 개발 산출물(D1~D9)·CVAT 포팅 Phase·표준화 단계 등과 혼동됨. 항상 아래 **작업명**으로 식별.

## 배경 / 원칙

- **brownfield 고도화**: 1차(상용·타 개발사·**MariaDB**) 베이스라인 vs 고도화 RFP(**영상 단위·PostgreSQL**, 개발 중).
- **1차 = MariaDB 원형 + `deprecated`** (이력 보존, 손대지 않음, dbms enum엔 mariadb 없어 `mysql` 표기).
- **고도화 = PostgreSQL + `new`/`modified`** (현재 코드 기준).
- **BM(1차) 용어·데이터 불간섭** — 충돌 시 고도화용 신규 정의.
- **신규·미배포 서비스** → 코드 컬럼명 변경 시 **새 ALTER 마이그레이션 금지, 기존 CREATE SQL 직접 수정**.
- **진실원**: 1차 = `docs/dev-db-schema-2026-04-23.md`(MariaDB) / 고도화 = `backend/src/main/resources/db/migration/*.sql` + JPA 엔티티.
- **createdBy 식별**: `a35a8f7c`=우리(현 세션) / `4b805dc4`=1차 적재 타 세션 / `null`=BM CSV 일괄. (search 응답엔 createdBy 없음 → get으로만 확인, 삭제 시 소실)

## 작업 현황

### ✅ 완료
- [x] **1차 ERD 폐기** — ERD-001~009 (mysql, deprecated, MariaDB 원형 복원)
- [x] **공통 ERD 분리** — 고도화 신규 ERD-010(라벨링)·011(증강)·012(영상수집)
- [x] **고도화 신규 ERD 생성** — ERD-013(마킹)·014(작업배정)·015(검수·영상상태)·016(시스템설정)·017(비식별)·018(포털)·019(라벨마스터·버전)·020(배치·작업)·021(관제통지)
- [x] **비식별 정의불일치 정정** — `SRC_BKUP_FILE_PATH_NM`→`DE_IDNTF_SRC_FILE_PATH_NM`, `ORGN_*`→`ORGNL_*`. 용어사전(id 1416~1418) + ERD-011/012/017 v3 + 코드(V4/V21/V26/V29/V30/V52·엔티티·View) + 설계문서(D8/D9) 4자 정합. **커밋 d08331b 푸시 완료**
- [x] **domain 정비 일부** — DOMAIN-002(프로젝트관리)·008(생성AI)·009(게시판) deprecated, DOMAIN-007 title "데이터 증강"(내보내기 제거)
- [x] **domain_feature 정비 완료** (2026-06-01) — active 27 / retired 13
  - deprecated 13건 top-level `status=deprecated`: DFEAT-004/005/022/031/032~035/036/013/040/037/038 (brownfield.status는 기수행, 037/038는 'ITEM 활성 유지' 결정 번복). `BROWNFIELD_DEPRECATED_WITHOUT_DECISION` 경고는 알려진 ADR 미도입 이슈로 의도된 무시
  - title 정정 3건(top-level+data.title 동기화): DFEAT-007→"영상/이미지 관리", DFEAT-028→"작업 통계(전체·권한별·상태별·일일 진행률)", DFEAT-027→"검수자/담당자 대시보드"
  - stale 4건 해소: DFEAT-018/019/020/029 (부모 FEAT-001/004 고도화 변경 확인, 1차 baseline preserved 내용 유효 재확인 후 touch)

### ⬜ 남은 작업
- [ ] **BM 메타정정 4건 원복 결정** — id 730(수정일자)·746(승인일자)·748(승인일시)·703(서버상태). 우리(updatedBy)가 수정했으나 BM 불간섭 방침상 원복 검토. 원본 = tool-results `4c5cd1ba` 세션 덤프(`1780134491171`). updatedBy/updatedAt 메타는 복구 불가. 서버상태(703) 도메인 여부V1 잘못 잔존.
- [ ] (사소) `DE_IDNTF_YN` allowed_values Y/F/N 확정 — BM 사전 id 653(`DE_IDENT_YN`)은 불간섭, ERD-012엔 이미 반영.

## 미해결 메타 이슈 (참고)
- 사업용어 26건(구 id 1093~1367)을 source=null+코드0 유령항목으로 오인 삭제→복구(신 id 1390~1415). 원작자 추정 `4b805dc4`이나 복구로 createdBy가 `a35a8f7c`로 오염(불가역). 내용은 원복됨.
- ERD/DFEAT deprecated 시 `decided_by`(폐기 근거 ADR) 미지정 warning 다수 — ADR 체계 도입 시 보완.

## 작업 방법 / 가드
- `update_item`: `get_item`으로 current_version 확인 후 `base_version` 사용(불일치 시 재조회). status/title만 변경 시 `data_mode` 미사용.
- 서브에이전트 분담 가능. **create/update 값은 진실원(문서/코드)에서만 — AI 임의 추정 금지.**
- LogiCraft 변경은 git 아님(MCP DB). 코드/문서 변경만 git 커밋. push는 `nt git push`.
