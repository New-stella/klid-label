# 구현 계획: V2.0 시나리오 변경 — 마킹 + 파이프라인 변경 + 증강 신규영상 + 포털 데이터마트

> 생성: 2026-05-26 20:00
> 상태: 진행 중
> 근거: docs/design/시나리오변경사항_V2.0_2026-05-26.md

## Phase 진행 현황

- [x] Phase 1: CLAUDE.md + 설계 문서 갱신 — V2.0 파이프라인/증강/포털 정책 반영
> 구현: CLAUDE.md V2.0 파이프라인/증강/포털 반영 (04c96a5) + R2,D1~D9 설계문서 전체 반영 (75aaf26) + 다이어그램 (bc8b68f)
- [x] Phase 2: 마킹 도메인 BE — Entity + Service + Controller + Flyway
> 구현: V45 DDL + LsMarking Entity(자동/수동 팩토리+상태전이) + MarkingService(CRUD+자동marks생성+IDOR검증) + MarkingController(POST/GET/DELETE+@PreAuthorize) + DTO record 3종 | 신규 27테스트, 971/971 PASS
- [x] Phase 3: 마킹 → VLM 콜백 비동기 연동 — VlmTimeseriesStep 변경 + VlmResultController 확장
> 구현: BatchStage.MARKING 추가 + BatchOrchestrator 마킹 확인→분기 + VlmTimeseriesStep.runWithMarking(마킹 데이터 포함) + VlmTimeseriesRequest eventName/marks 필드 + VlmResultService 마킹 VLM_COMPLETED 전이 | 신규 10테스트, 981/981 PASS
- [x] Phase 4: 프레임 추출 변경 — FfmpegFrameExtractor 마킹 위치 기반 + 오토라벨 원본 전용
> 구현: extractByMarks(마킹 frameIndex 기반 추출, NATIVE_VIDEO_FPS=30) + YOLO/SAM2 resolveImagePath 원본 전용 + BatchOrchestrator 마킹 유무 분기(parseMarks→extractByMarks/extractBoth) | 신규 10테스트, 138클래스 PASS
- [x] Phase 5: 증강 = 새 영상 — AugmentResultService 확장 + PARENT_RAW_SN + 라벨 복사
> 구현: V46 DDL(PARENT_RAW_SN) + LsDataRaw.createFromAugment + LsDataLbl.copyForNewSrc + AugmentResultService(SUCCESS→새영상+프레임/라벨/메타복사) | 신규 3테스트, 1004 PASS
- [x] Phase 6: 포털 데이터마트 Load + 사용자 작업 별도 적재
> 구현: V47 DDL(LS_PORTAL_USER_LABEL) + LsPortalUserLabel Entity + PortalLabelService(datamart Load/사용자 라벨 CRUD/IDOR) + Controller 3종 API | 신규 7테스트, PASS
- [x] Phase 7: 마킹 FE 화면 — 영상 재생 + 자동/수동 마킹 + 키보드 단축키
> 구현: marking feature 모듈(types/api/store/hooks/components 5종) + MarkingPage + 라우트(/marking/:rawSn) + 키보드 단축키(Space/Del/Enter) + MARKING_KEYS | tsc+vite PASS

## 요구사항 요약

- 목적: 배치 파이프라인에 마킹 단계 추가, 증강을 새 영상으로 관리, 포털 데이터 흐름 변경
- 범위:
  - 포함: 마킹 BE+FE, 파이프라인 변경(프레임 추출/오토라벨), 증강 새 영상, 포털 데이터마트
  - 제외: VLM 모델 본체, 외부 증강 서비스 본체, 관제서버 수신측, 데이터마트 적재 로직(관제서버 책임)
- 주요 액터: REVIEWER(마킹 설정), WORKER(수동 마킹), 배치 시스템(자동 마킹), 외부 VLM, 외부 증강
- 비기능: 마킹 UI 응답성(영상 재생 30fps), VLM 콜백 멱등성, 라벨 복사 데이터 정합성

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | V45: `LS_MARKING` 마킹 테이블, V46: `LS_DATA_RAW.PARENT_RAW_SN` 컬럼, V47: `LS_PORTAL_USER_LABEL` 포털 작업 데이터 |
| 기존 API | 없음 (신규 추가만) |
| 기존 서비스 | BatchOrchestrator(마킹 단계 삽입), FfmpegFrameExtractor(마킹 기반 추출), YoloAutolabelStep(원본만), AugmentResultService(새 영상 생성), PortalLabelService(데이터마트 Load) |
| 설정/환경 | CLAUDE.md 파이프라인 순서 변경 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|:----:|------|
| 1 | 마킹 기반 프레임 추출 시 마킹 0건 → 프레임 0건 | HIGH | 자동 모드 폴백 (마킹 없으면 기존 간격 추출) |
| 2 | 증강 라벨 복사 시 좌표 해상도 불일치 | HIGH | 해상도 변경 증강은 라벨 좌표 스케일링 |
| 3 | VLM 콜백 수신 전 파이프라인 다음 단계 진행 | HIGH | 상태 머신에 MARKING_WAIT 추가 |
| 4 | 포털 DB 데이터마트 테이블 스키마 미확정 | MED | Phase 6에서 사용자 확인 후 진행 |

---

## 상세 계획

### Phase 1: CLAUDE.md + 설계 문서 갱신

**목표**: V2.0 변경 사항을 CLAUDE.md와 설계 문서에 반영. 코드 변경 없음.

**수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `CLAUDE.md` | 수정 | 배치 파이프라인 순서 변경, 증강 새 영상 정책, 포털 데이터 흐름 |

**핵심 변경**:
- 배치 파이프라인 섹션: `마킹(자동/수동) → VLM 시계열(콜백) → 비식별화 → FFmpeg(마킹 위치) → YOLO(원본만) → SAM2 → 트랙 보간`
- 증강 섹션 추가: "증강 = 새 영상. PARENT_RAW_SN 참조. 미검수 상태 시작. 별도 영상 ID로 완료 통지"
- 포털 섹션 변경: "데이터마트 → 포털 DB 적재 → Load + 사용자 작업 별도 저장"

**의존성**: 없음

---

### Phase 2: 마킹 도메인 BE

**목표**: 마킹 Entity/Service/Controller + Flyway 완성. 마킹 CRUD API 동작.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `db/migration/V45__create_ls_marking.sql` | 생성 | 마킹 테이블 DDL |
| `marking/entity/LsMarking.java` | 생성 | 마킹 Entity (rawSn, eventName, markingMode, marks JSON) |
| `marking/entity/LsMarkingItem.java` | 생성 | 마킹 아이템 Embeddable (frameIndex, timestamp) |
| `marking/repository/LsMarkingRepository.java` | 생성 | `@ControlRepo` JpaRepository |
| `marking/dto/MarkingRequest.java` | 생성 | record (rawSn, eventName, mode, intervalSec, marks[]) |
| `marking/dto/MarkingResponse.java` | 생성 | record |
| `marking/dto/MarkItem.java` | 생성 | record (frameIndex, timestamp) |
| `marking/service/MarkingService.java` | 생성 | 마킹 저장/조회/삭제 + VLM 콜백 요청 발송 |
| `marking/controller/MarkingController.java` | 생성 | POST/GET/DELETE 마킹 API |

**핵심 로직 / 시그니처**:
```java
// MarkingController
@PostMapping("/api/v1/videos/{rawSn}/markings")
public ApiResponse<MarkingResponse> createMarking(@PathVariable Long rawSn,
    @Valid @RequestBody MarkingRequest req, @AuthenticationPrincipal TokenClaims actor);

@GetMapping("/api/v1/videos/{rawSn}/markings")
public ApiResponse<List<MarkingResponse>> getMarkings(@PathVariable Long rawSn);

// MarkingService
public MarkingResponse create(Long rawSn, MarkingRequest req, TokenClaims actor);
// - 자동 모드: intervalSec 기반으로 marks 배열 자동 생성
// - 수동 모드: 요청의 marks 배열 그대로 저장
// - 저장 후 VLM 콜백 요청 발송 (VlmClient.requestTimeseries)

// V45 DDL
CREATE TABLE LS_MARKING (
    MARKING_SN BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    RAW_SN BIGINT NOT NULL,
    EVENT_NAME VARCHAR(100) NOT NULL,
    MARKING_MODE VARCHAR(16) NOT NULL, -- AUTO / MANUAL
    INTERVAL_SEC INT, -- 자동 모드 시 프레임 간격
    VIDEO_PATH VARCHAR(500) NOT NULL, -- NAS 경로
    MARKS JSON NOT NULL, -- [{frameIndex, timestamp}]
    STATUS VARCHAR(16) NOT NULL DEFAULT 'PENDING', -- PENDING/VLM_REQUESTED/VLM_COMPLETED
    CREATED_BY BIGINT,
    CREATED_AT DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UPDATED_AT DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX IDX_LM_RAW (RAW_SN),
    FOREIGN KEY (RAW_SN) REFERENCES LS_DATA_RAW(RAW_SN)
);
```

**테스트 케이스**:
- `자동모드_마킹_생성_intervalSec_기반_marks_자동생성`
- `수동모드_마킹_생성_marks_배열_저장`
- `마킹_조회_정상`
- `마킹_생성시_VLM_콜백_요청_발송됨`
- `미존재_영상_마킹시_404`
- `marks_빈_배열_시_400`

**수용 기준**:
- [ ] POST /api/v1/videos/{rawSn}/markings → 201 + 마킹 저장
- [ ] 자동 모드: intervalSec 기반 marks 자동 생성
- [ ] 수동 모드: 요청 marks 그대로 저장
- [ ] VLM 콜백 요청 발송 (ControlNotify 패턴 참조)

**의존성**: Phase 1 완료

---

### Phase 3: 마킹 → VLM 콜백 비동기 연동

**목표**: 마킹 완료 → VLM에 콜백 요청 → VLM 결과 수신 → 파이프라인 다음 단계(비식별화) 트리거.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `batch/step/VlmTimeseriesStep.java` | 수정 | 마킹 데이터 기반 요청으로 변경 |
| `webhook/service/VlmResultService.java` | 수정 | VLM 결과 수신 → 마킹 상태 VLM_COMPLETED 전이 → 파이프라인 재개 |
| `batch/orchestrator/BatchOrchestrator.java` | 수정 | 마킹 단계 삽입, VLM 콜백 대기 로직 |
| `marking/service/MarkingService.java` | 수정 | VLM 요청에 마킹 페이로드(이벤트명+영상경로+marks) 전달 |

**핵심 로직**:
```java
// BatchOrchestrator.process() 변경
// 기존: VLM → DEIDENTIFY → FRAME → YOLO → SAM2 → INTERPOLATE
// 변경: MARKING_CHECK → VLM(콜백) → [대기] → DEIDENTIFY → FRAME(마킹 기반) → YOLO(원본만) → SAM2 → INTERPOLATE

// VlmResultService.handle() 변경
// VLM 결과 수신 → LS_MARKING.STATUS = VLM_COMPLETED → 파이프라인 재개 트리거
```

**테스트 케이스**:
- `VLM_요청에_마킹_페이로드_포함_확인`
- `VLM_결과_수신시_마킹_상태_VLM_COMPLETED_전이`
- `VLM_결과_수신후_파이프라인_재개_트리거`
- `마킹_없는_영상_VLM_단계_SKIP`

**수용 기준**:
- [ ] 마킹 완료 → VLM 콜백 요청 (이벤트명+영상경로+marks)
- [ ] VLM 결과 수신 → 파이프라인 다음 단계 진행
- [ ] 마킹 없는 영상은 기존 흐름 유지

**의존성**: Phase 2 완료

---

### Phase 4: 프레임 추출 변경 + 오토라벨 원본 전용

**목표**: FfmpegFrameExtractor가 마킹 위치 기반으로 프레임 추출. YoloAutolabelStep은 원본만 실행.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `batch/step/FfmpegFrameExtractor.java` | 수정 | 마킹 위치(frameIndex) 기반 추출 모드 추가 |
| `batch/step/YoloAutolabelStep.java` | 수정 | 원본 프레임만 YOLO 실행, 비식별본 공유 |
| `batch/step/Sam2SegmentStep.java` | 수정 | 원본 프레임만 SAM2 실행 (YOLO와 동일) |
| `batch/orchestrator/BatchOrchestrator.java` | 수정 | 마킹 데이터를 프레임 추출에 전달 |

**핵심 로직**:
```java
// FfmpegFrameExtractor — 마킹 위치 기반 추출 메서드 추가
public List<LsDataSrc> extractByMarks(LsDataRaw raw, List<MarkItem> marks);
// - marks의 frameIndex 목록을 FFmpeg -vf select='eq(n,150)+eq(n,300)' 으로 지정 추출
// - 기존 extractBoth는 유지 (마킹 없는 영상 폴백)

// YoloAutolabelStep.run() 변경
// - 원본 프레임(SRC_TYPE='RAW')만 필터하여 YOLO 실행
// - 비식별 프레임은 동일 FRAME_NO 매핑으로 라벨 공유 (별도 YOLO 미실행)
```

**테스트 케이스**:
- `마킹_위치_기반_프레임_추출_정확한_frameIndex`
- `마킹_없을때_기존_간격_추출_폴백`
- `YOLO_원본_프레임만_실행_비식별_미실행`
- `원본_라벨결과가_비식별_프레임에_공유됨`

**수용 기준**:
- [ ] 마킹 위치의 프레임만 추출됨 (원본+비식별 2벌)
- [ ] YOLO/SAM2는 원본만 실행
- [ ] 비식별 프레임은 동일 FRAME_NO로 원본 라벨 공유

**의존성**: Phase 3 완료

---

### Phase 5: 증강 = 새 영상

**목표**: 증강 결과를 새 RAW_SN으로 등록. 원본 라벨/메타 복사. 미검수 상태로 시작.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `db/migration/V46__add_parent_raw_sn.sql` | 생성 | `LS_DATA_RAW.PARENT_RAW_SN` 컬럼 추가 |
| `video/entity/LsDataRaw.java` | 수정 | `parentRawSn` 필드 추가 |
| `webhook/service/AugmentResultService.java` | 수정 | 새 RAW_SN 생성 + 라벨/메타 복사 로직 |
| `augment/service/AugmentReviewService.java` | 수정 | 해상도 변경 증강 처리 |

**핵심 로직**:
```java
// AugmentResultService.handle() 변경
// 기존: 기존 영상에 증강 이미지 추가
// 변경:
//   1. 새 LsDataRaw INSERT (parentRawSn = 원본 rawSn)
//   2. 원본 프레임(LsDataSrc) 복사 → 새 rawSn으로 INSERT
//   3. 원본 라벨(LsDataLbl) 복사 → 새 srcSn으로 INSERT
//   4. 원본 메타(LsDataMeta) 복사 → 새 rawSn으로 INSERT
//   5. 새 영상 상태: PENDING (미검수)
//   6. 해상도 변경 증강: 이미지 해상도만 변경하여 저장

// V46 DDL
ALTER TABLE LS_DATA_RAW ADD COLUMN PARENT_RAW_SN BIGINT NULL;
ALTER TABLE LS_DATA_RAW ADD INDEX IDX_LDR_PARENT (PARENT_RAW_SN);
```

**테스트 케이스**:
- `증강_결과_수신시_새_RAW_SN_생성`
- `새_영상에_원본_라벨_복사_정확성`
- `새_영상에_원본_메타_복사_정확성`
- `새_영상_상태_PENDING`
- `해상도_변경_증강_이미지_리사이즈`
- `증강_영상_완료시_새_RAW_SN으로_TASK_COMPLETED_통지`

**수용 기준**:
- [ ] 증강 결과 → 새 RAW_SN 영상 생성 (PARENT_RAW_SN 참조)
- [ ] 원본 라벨/메타 정확히 복사
- [ ] 새 영상 PENDING 상태 → 배정/수정/검수 흐름 동일
- [ ] 관제서버 통지 시 새 영상 ID로 전송

**의존성**: Phase 1 완료 (Phase 2~4와 병렬 가능)

---

### Phase 6: 포털 데이터마트 Load + 사용자 작업 별도 적재

**목표**: 포털 DB에서 데이터마트 적재 데이터 Load + 사용자 작업 데이터 별도 저장.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `db/migration/V47__create_ls_portal_user_label.sql` | 생성 | 포털 사용자 작업 데이터 테이블 |
| `portal/entity/LsPortalUserLabel.java` | 생성 | 포털 사용자 라벨 Entity |
| `portal/repository/LsPortalUserLabelRepository.java` | 생성 | `@PortalRepo` |
| `portal/service/PortalLabelService.java` | 수정 | 데이터마트 데이터 Load + 사용자 라벨 별도 적재 |
| `portal/controller/PortalLabelController.java` | 수정 | 데이터마트 Load API 추가 |

**핵심 로직**:
```java
// PortalLabelService 변경
// 기존: 포털 사용자가 직접 업로드한 영상의 라벨만 관리
// 변경:
//   1. 데이터마트 적재 영상 목록 조회 (포털 DB)
//   2. 영상 선택 → 라벨/메타 데이터 Load
//   3. 라벨링 화면에 표시
//   4. 사용자 수정 → LS_PORTAL_USER_LABEL에 별도 적재 (원본 미수정)
//   5. 다운로드 시 사용자 작업 데이터 기준

// V47 DDL
CREATE TABLE LS_PORTAL_USER_LABEL (
    USER_LBL_SN BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    PORTAL_USER_NO VARCHAR(100) NOT NULL,
    SOURCE_RAW_SN BIGINT NOT NULL, -- 데이터마트 원본 영상
    SOURCE_SRC_SN BIGINT NOT NULL, -- 데이터마트 원본 프레임
    LBL_TYPE_CD VARCHAR(16) NOT NULL,
    LABEL VARCHAR(255),
    POINTS TEXT,
    CREATED_AT DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UPDATED_AT DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX IDX_LPUL_USER (PORTAL_USER_NO, SOURCE_RAW_SN)
);
```

**테스트 케이스**:
- `데이터마트_영상_목록_조회`
- `데이터마트_라벨_Load_정상`
- `사용자_작업_데이터_별도_적재_원본_미수정`
- `사용자_다운로드시_작업_데이터_기준`
- `다른_사용자_작업_데이터_접근_불가_IDOR`

**수용 기준**:
- [ ] 포털 DB에서 데이터마트 데이터 Load
- [ ] 사용자 수정 → LS_PORTAL_USER_LABEL에 별도 저장 (원본 미수정)
- [ ] IDOR 방어 (본인 작업 데이터만 접근)

**의존성**: Phase 1 완료

---

### Phase 7: 마킹 FE 화면

**목표**: 영상 재생 + 자동/수동 마킹 UI. 키보드 단축키 지원.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/features/marking/` | 생성 | 마킹 feature 모듈 전체 |
| `frontend/src/pages/MarkingPage.tsx` | 생성 | 마킹 전용 페이지 |
| `frontend/src/router/index.tsx` | 수정 | `/marking/:videoId` 라우트 추가 |

**핵심 컴포넌트**:
- `VideoPlayer` — HTML5 video 재생 (재생/일시정지/탐색)
- `MarkingTimeline` — 마킹 포인트 시각화 (타임라인 바)
- `MarkingToolbar` — 자동/수동 모드 전환 + 프레임 간격 설정
- `MarkingList` — 마킹 목록 (이벤트명, 프레임, 시간)

**키보드 단축키** (수동 모드):
- `Space` — 현재 위치에 마킹 추가
- `Delete/Backspace` — 선택된 마킹 삭제
- `Enter` — 마킹 완료 (VLM 요청 발송)

**테스트 케이스**:
- `자동모드_프레임간격_설정_마킹_생성`
- `수동모드_영상재생중_Space키_마킹`
- `마킹_완료_API_호출`
- `모드_전환_UI_상태_변경`

**의존성**: Phase 2 완료 (BE API 필요)
