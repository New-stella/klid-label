# 구현 계획: 배치 파이프라인 시나리오 재정렬 (비식별 선두 + 마킹 비식별영상화)

> 생성: 2026-06-08 16:30
> 상태: 진행 중
> 근거: CLAUDE.md '배치 파이프라인' 설계 타깃 (planned → 코드 반영). 현행 매핑은 Explore 결과 기준.

## Phase 진행 현황

- [x] Phase 1: 배치 순서 재정의 — post-marking 시퀀스에서 비식별 제거 (BE 코어) ✅ 완료 (2026-06-08)
- [x] Phase 2: 비식별 선두 자동화 + 마킹 비식별영상 대상 + marking-ready 상태 (BE) ✅ 완료 (2026-06-08) — 선언적 파이프라인(BatchStep/Context/Pipeline/Config) 리팩토링 흡수, MARKING_READY는 LsDataRaw.dataSttsCd, 자동 재비식별 큐 미신설(외부 수동)
- [x] Phase 3: dev/autolabel-test BE 오케스트레이션 정합 (새 순서) ✅ 완료 (2026-06-08) — dev를 단일 프로덕션 파이프라인으로 수렴, 4단계 토글=조건부 step(isEnabled), AutolabelTestService 은퇴, DevPipelineRunner 신설. FE 인계: /v1/dev/autolabel/run·run-full 제거, pipelineStatus 항상 PROCESSING
- [x] Phase 4: FE 개발도구 영상 업로드 화면 정합 (ANONY 힌트·토글 순서) ✅ 완료 (2026-06-08) — 'ANONY 비식별 대상 아님' 문구 제거, 토글 표시 DEIDENTIFY 선두, pipelineStatus 주석 정합. FE 788/788
- [x] Phase 5: 상태전이/테스트 정합 + 통합 검증 ✅ 완료 (2026-06-08) — 신 시나리오 전체흐름·상태전이 통합테스트 6종 추가, BE 1266/1266 + FE 788/788. 잔여: /cc-verify 브라우저 화면 검증(스택 기동 필요)

## 요구사항 요약

- **목적**: CLAUDE.md에 "설계 확정·코드 미반영(planned)"으로 남은 배치 파이프라인 재정렬을 코드에 반영
- **신 시나리오 순서**: 비식별(전체 영상, 적재 직후 선두) → 마킹(비식별 영상 대상, ready 트리거) → VLM 시계열 → FFmpeg 프레임추출(마킹 위치, 원본+비식별 2벌) → YOLO(원본만) → SAM2 → 트랙 보간
- **범위 (포함)**: BE 배치 오케스트레이션 재정렬·비식별 선두 분리·마킹 대상 비식별영상화·상태전이, dev/autolabel-test 엔드포인트, FE 개발도구 영상 업로드 화면
- **범위 (제외)**: ai-server 추론 로직 변경, 마킹 화면(SCR) 신규 UI, 실가중치 모델, 외부 VLM 실연동
- **이미 구현된 부분 (Explore 확인)**: 비식별 무조건 호출(ANONY 포함) = DeidentifyStep 이미 무조건. 프레임추출 마킹위치 기반 + 2벌 = 이미 구현. → 시나리오 #1(전체 비식별)은 BE 거의 완료, 잔여는 **순서(선두)** 와 **FE 힌트 정합**
- **비기능**: 기존 ~15개 배치 테스트 보호, 상태머신 정합(2원 추적 LsRawDataStatus/LsDataRaw.dataSttsCd 책임 분리 유지), 원본 파일 절대 미변경

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | (검토) marking-ready 상태값 추가 시 코드 상수만 — 신규 컬럼 없음 목표 |
| 기존 API | `POST /v1/dev/autolabel-test` 오케스트레이션 순서 변경(시그니처 불변), 마킹 스트림 대상 비식별영상 확인 |
| 도메인 모듈 | `batch.orchestrator`, `batch.step`, `batch.status`, `marking.listener`, `dev.service`, (적재) `ingest`/`upload` |
| 설정/환경 | 없음 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | 비식별을 마킹-트리거 배치에서 분리 → 적재 직후 자동 실행 구조 신설(이벤트/상태머신 변화) | HIGH | 적재 시점 @Async 비식별 트리거 + 'MARKING_READY' 전이 신설. 기존 MarkingBatchBridge는 잔여 배치만 트리거. 단계별 테스트 |
| 2 | 프레임추출이 더이상 같은 run의 DeidentifyStep 결과를 못 받음 → 저장된 비식별 경로를 LsDataRaw에서 읽어야 함 | HIGH | FfmpegFrameExtractor 입력을 deidVideoPath 인자 → LsDataRaw 조회로 전환. DE_IDNTF_YN='Y' 선행 보장 |
| 3 | 비식별 실패(F) 시 마킹/배치 진입 차단 흐름 | MED | 비식별 미완료(F)면 MARKING_READY 미전이 + 재시도 큐. 마킹 화면 진입 가드 |
| 4 | dev/autolabel-test는 사람 마킹이 없는 단축 경로 — 새 순서에서 마킹 단계 합성/생략 필요 | MED | dev 경로는 비식별 선두 후 합성 마킹(또는 기존 marks)로 잔여 배치 직접 호출 |
| 5 | 기존 배치 테스트 다수가 구 순서 단언 | MED | 각 Phase에서 해당 테스트를 신 순서로 갱신(보호) |

---

## 상세 계획

### Phase 1: 배치 순서 재정의 — post-marking 시퀀스에서 비식별 제거

**목표**: 마킹완료가 트리거하는 배치에서 비식별 단계를 제거하고, 순서를 `VLM → 프레임추출 → YOLO → SAM2 → 트랙보간`으로 확정. 프레임추출은 저장된 비식별 경로를 LsDataRaw에서 읽는다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `batch/orchestrator/BatchOrchestrator.java` | 수정 | process()에서 DeidentifyStep 호출 제거(현 라인 113-114), 순서 VLM→FRAME→YOLO→SAM2→INTERP, javadoc(32-43) 갱신 |
| `batch/orchestrator/BatchStage.java` | 수정 | post-marking enum 순서 재정의(DEIDENTIFY를 선두 전용 단계로 분리 또는 시퀀스에서 제외) |
| `batch/step/FfmpegFrameExtractor.java` | 수정 | `extractByMarks` 입력 deidVideoPath 인자 → LsDataRaw에서 비식별 경로 조회로 전환 |
| `batch/orchestrator/BatchOrchestratorTest.java` | 수정 | 신 순서 단언, 비식별 미호출 검증 |
| `batch/step/FfmpegFrameExtractorTest.java` | 수정 | 저장 경로 기반 입력 단언 |

**핵심 로직 / 시그니처**:
```java
// BatchOrchestrator.process(rawSn): 비식별 호출 제거, 순서
//   loadRaw → [VLM] → [FRAME_EXTRACT(저장된 비식별경로 사용)] → [YOLO] → [SAM2] → [INTERPOLATE] → COMPLETED
// FfmpegFrameExtractor: extractByMarks(LsDataRaw raw, List<MarkItem> marks)  // deidVideoPath 인자 제거, raw에서 도출
```

**테스트 케이스** (cc-tdd RED):
- `배치_파이프라인이_비식별_단계를_호출하지_않는다`
- `배치_순서가_VLM_프레임추출_YOLO_SAM2_보간이다`
- `프레임추출이_LsDataRaw의_비식별경로를_읽어_2벌_추출한다`
- `비식별_미완료_영상은_프레임추출에서_차단된다`

**수용 기준**:
- [ ] BatchOrchestrator가 DeidentifyStep을 호출하지 않음 (post-marking)
- [ ] 프레임추출이 저장된 비식별 경로로 원본+비식별 2벌 생성
- [ ] 기존 배치 테스트가 신 순서로 통과

**의존성**: 없음 (Phase 2의 선행)

### Phase 2: 비식별 선두 자동화 + 마킹 비식별영상 대상 + marking-ready 상태

**목표**: 영상 적재 직후 비식별을 자동 실행(선두)하고, 완료 시 'MARKING_READY' 상태로 전이한다. 마킹은 비식별 영상을 대상으로 하며, 마킹 완료가 잔여 배치(Phase 1 시퀀스)를 트리거한다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `batch/listener/IngestDeidentifyBridge.java` (신규) | 생성 | 영상 적재 이벤트 → @Async DeidentifyStep 실행 → 성공 시 MARKING_READY 전이 |
| `batch/step/DeidentifyStep.java` | 수정 | 선두 단독 실행 진입점 정리(재사용), 실패 시 'F' + 재시도 큐 유지 |
| `assignment/entity/LsRawDataStatus.java` | 수정 | `MARKING_READY` 상수/전이 메서드 추가(또는 기존 PENDING 의미 재정의) |
| `marking/listener/MarkingBatchBridge.java` | 수정 | 마킹 완료 시 잔여 배치만 트리거(비식별 제외), SKIP_STATUSES 조정 |
| `marking/.../*Stream*` (마킹 스트림) | 확인/수정 | 마킹 대상이 비식별 영상 경로인지 검증(GET /v1/videos/{rawSn}/stream) |
| 관련 테스트 | 수정/생성 | `IngestDeidentifyBridgeTest`, `MarkingBatchBridgeTest`, 상태전이 테스트 |

**핵심 로직 / 시그니처**:
```java
// IngestDeidentifyBridge: @TransactionalEventListener(AFTER_COMMIT) onVideoIngested(event)
//   → deidentifyStep.run(raw) (@Async) → 성공: status.transitionTo(MARKING_READY) / 실패: 'F' + retryQueue
// MarkingBatchBridge.onMarkingCompleted: markBatchQueued → asyncBatchRunner(잔여 배치, 비식별 없음)
```

**상태 전이 (목표)**:
```
적재 PENDING → (자동 비식별) → MARKING_READY → (마킹완료) BATCH_QUEUED → PROCESSING → ASSIGNED
                  비식별 실패 → FAILED + 재시도 큐
```

**테스트 케이스**:
- `영상_적재시_비식별이_자동_선두_실행된다`
- `비식별_완료시_MARKING_READY로_전이된다`
- `비식별_미완료영상은_마킹_진입이_차단된다`
- `마킹_완료가_비식별을_제외한_잔여배치를_트리거한다`
- `마킹_대상_스트림이_비식별_영상이다`

**수용 기준**:
- [ ] 적재 직후 비식별 자동 실행 + MARKING_READY 전이
- [ ] 마킹 완료 → 비식별 없는 잔여 배치 트리거
- [ ] 비식별 실패 시 마킹 차단 + 재시도 큐
- [ ] 상태머신 2원 추적 책임 분리 유지

**의존성**: Phase 1

### Phase 3: dev/autolabel-test BE 오케스트레이션 정합

**목표**: 개발용 단축 경로(`POST /v1/dev/autolabel-test`)를 새 순서에 맞춘다 — 업로드 → 비식별 선두 → (합성/기존 마킹) → 잔여 배치. 4단계 토글 의미를 새 순서에 맞게 정리.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `dev/service/DevAutolabelTestService.java` | 수정 | 트리거 순서를 비식별 선두 → 잔여 배치로. enabledStages(FRAME/DEIDENT/YOLO/SAM2) 매핑 정리 |
| `dev/dto/AutolabelTestRequest.java` | 수정 | (필요 시) 토글 키/기본값 정합. prvcTypeCd는 비식별 무조건이므로 표시용만 |
| `dev/.../AutolabelTestServiceTest.java` | 수정 | 새 순서·토글 단언 |

**테스트 케이스**:
- `dev_업로드가_비식별을_선두로_실행한다`
- `dev_토글_DEIDENT_off시_선두_비식별을_건너뛴다`
- `dev_파이프라인이_신_순서로_진행한다`

**수용 기준**:
- [ ] dev 업로드가 신 순서(비식별 선두)로 동작
- [ ] 4단계 토글이 새 순서 의미와 일치
- [ ] 기존 dev 엔드포인트 권한/검증(@PreAuthorize REVIEWER, 확장자 allowlist) 유지

**의존성**: Phase 1, 2

### Phase 4: FE 개발도구 영상 업로드 화면 정합

**목표**: 개발도구 영상 업로드 화면(`/dev/autolabel-test`)의 구 시나리오 흔적을 제거하고 신 순서를 반영한다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/pages/dev/DevAutolabelTestPage.tsx` | 수정 | PrvcType 힌트 'ANONY=비식별 대상 아님' 제거(라인 38-42) → '전체 비식별' 정합, DEIDENTIFY 단계 힌트(라인 96) 갱신, STAGE 순서/라벨 신 순서(비식별 선두) |
| `frontend/src/features/dev/types.ts` | 수정 | STAGE_KEYS 순서/주석 신 순서 반영 |
| `frontend/src/pages/dev/__tests__/DevAutolabelTestPage.test.tsx` | 수정 | 힌트·토글 순서 단언 |

**테스트 케이스**:
- `ANONY_선택시_비식별_대상_아님_문구가_없다`
- `단계_토글이_비식별_선두_순서로_표시된다`
- `비식별_단계_힌트가_전체_무조건으로_표기된다`

**수용 기준**:
- [ ] 'ANONY=비식별 대상 아님' 문구 제거
- [ ] 토글 순서·라벨이 신 시나리오와 일치
- [ ] FE 테스트 통과 + ui-tester 화면 검증

**의존성**: Phase 3 (BE 토글 의미 확정 후)

### Phase 5: 상태전이/테스트 정합 + 통합 검증

**목표**: 영향받은 배치 테스트 전수 정합 + 신 시나리오 통합 검증.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/src/test/.../batch/**` | 수정 | 구 순서 단언 테스트(BatchOrchestrator*, MarkingBatchBridge*, Deidentify*, Ffmpeg*, Status/Transition*) 신 순서 정합 |
| (검증) | — | `/cc-verify` 통합 시나리오 + 화면 플로우 |

**테스트 케이스**:
- `전체_파이프라인_적재_비식별_마킹_VLM_프레임_오토라벨_보간_정상흐름`
- `상태전이_PENDING_MARKING_READY_BATCH_QUEUED_PROCESSING_ASSIGNED`

**수용 기준**:
- [ ] 기존 ~15개 배치 테스트 신 순서로 전부 GREEN
- [ ] 통합 시나리오 검증 PASS

**의존성**: Phase 1~4

---

## 실행 가이드

1. `/cc-tdd Phase 1: 배치 순서 재정의 — post-marking 비식별 제거 + 프레임추출 비식별경로 조회`
2. `/cc-tdd Phase 2: 비식별 선두 자동화 + 마킹 비식별영상 대상 + marking-ready 상태전이`
3. `/cc-tdd Phase 3: dev/autolabel-test BE 오케스트레이션 신 순서 정합`
4. `/cc-tdd Phase 4: FE 개발도구 영상 업로드 화면 정합 (ANONY 힌트·토글 순서)`
5. `/cc-tdd Phase 5: 배치 테스트 전수 정합 + 통합 검증` → `/cc-verify`
