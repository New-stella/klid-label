# 구현 계획: 비식별 상태 머신·표시 개선 (완료 후 마킹대기 전이 + 진행중/실패 배지 + 미완료 진입 차단)

> 생성: 2026-06-29 17:20
> 상태: 진행 중
> 브랜치: feat/onprem-install-package
> 직전 완료 플랜(영상 이벤트 타입 관제 마스터 연동, Phase 1~5 전부 완료)은 본 플랜으로 대체됨.

## Phase 진행 현황

- [x] Phase 1: BE 비식별 전이 단일화 — 조기 MARKING_READY 전이 제거(비식별 완료 시에만 전이), dev/운영 경로 일관화
- [x] Phase 2: BE 비식별 상태 목록 노출 — 영상 목록에 procLog 파생 비식별상태(진행중/실패/완료) 노출
- [x] Phase 3: FE 상태 배지 + 마킹 진입 차단 — 목록·상세에 비식별 진행중/실패 배지, 미완료 영상 마킹 진입 차단

## 요구사항 요약

- 목적: dev 업로드/운영 적재 경로가 KPST 비식별 **제출 직후**(미완료) 곧바로 `MARKING_READY`로 전이해, 사용자가 마킹 진입은 되는데 영상이 안 나오고(스트림 NOT_FOUND) 진행/실패 상태도 안 보인다. "비식별 완료 후에만 마킹대기로 넘어가고, 진행중/실패가 화면에 보이도록" 정합화한다.
- 범위(포함): ① 조기 MARKING_READY 전이 제거(완료 시에만 전이) ② 영상 목록에 비식별 진행중/실패/완료 상태 노출 ③ FE 배지 표시 + 미완료 영상 마킹 진입 차단. dev 업로드(DevPipelineRunner) + 운영 적재(AsyncDeidentifyRunner/KpstDeidentPollJob 완료) 양 경로 일관 적용.
- 범위(제외, out of scope): 진행률(%) 표시, 실패 사유 상세 노출, KPST 테스트 서버 자체 미처리 문제(별도 KPST팀 문의), mock 모드 전환, dataSttsCd 새 상태값 추가(방식 ① 미채택).
- 주요 액터: WORKER/REVIEWER(마킹 대상 목록·진입), 시스템(배치 비식별 파이프라인·폴링).
- 요구 목록:
  - R1: 비식별 완료(`LS_DEIDENT_PROC_LOG.PROC_STTS_CD=SUCCEEDED` → `deIdntfYn='Y'`) 후에만 `LsDataRaw.dataSttsCd`를 `MARKING_READY`로 전이. 제출/요청 직후 전이 금지. | 출처: 사용자 명시
  - R2: 비식별 요청·처리 중(procLog WAITING/POLLING/REQUESTED) 영상은 "비식별 진행중" 상태로 노출. | 출처: 사용자 명시
  - R3: 비식별 실패(procLog `PROC_STTS_CD=FAILED` 또는 `deIdntfYn='F'`) 영상은 "비식별 실패" 상태로 노출. | 출처: 사용자 명시
  - R4: 비식별 미완료(진행중/실패) 영상은 마킹 화면 진입 차단 + 마킹 대상 목록에 상태 배지 표시. | 출처: 구체화 Q&A
  - R5: dev 업로드 경로 + 운영 적재 경로 모두 동일 동작. | 출처: 구체화 Q&A
  - R6: 표시 수준 = 비식별 진행중 / 비식별 실패 / 마킹대기(완료) 3상태 배지. 진행률·실패사유 상세 제외. | 출처: 구체화 Q&A
- 수용 기준:
  - [x] AC1: KPST 모드에서 비식별 제출 직후 해당 영상 `dataSttsCd != MARKING_READY`(`PENDING` 유지), 폴링 SUCCEEDED 시점에만 `MARKING_READY`로 전이된다. (dev/운영 양 경로) — Phase 1 완료
  - [x] AC2: mock 모드(동기 완료)에서는 `run()` 완료 직후 `MARKING_READY` 전이가 유지된다(회귀 없음). + DEV_FIX로 mock 영속(deIdntfYn='Y'+procLog SUCCEEDED) 실커밋 보장 — Phase 1 완료
  - [x] AC3: 영상 목록 응답에 비식별 진행중/실패/완료가 구분되어 내려오고, FE가 진행중/실패 배지로 표시한다. — Phase 2·3 완료
  - [x] AC4: 비식별 미완료(진행중/실패) 영상은 FE에서 마킹 진입이 차단되고(버튼 비활성/안내), BE `MarkingService` 가드(`deIdntfYn='Y'`)가 백스톱으로 유지된다. — Phase 3 완료
- 비기능: 목록 쿼리 procLog 조인은 페이지 단위 batch lookup으로 N+1 회피(기존 `lookupExportInfos` 패턴 재사용). 상태 머신 표면 변경 최소화(회귀 위험 최소).

## 설계 결정 (방식 ② 채택)

"비식별 진행중/실패"를 **dataSttsCd 새 상태(①)로 추가하지 않고**, `dataSttsCd=PENDING` 유지 + `LS_DEIDENT_PROC_LOG` 파생으로 노출(②)한다.

| 항목 | ① 새 dataStts 상태 | ② procLog 파생 (채택) |
|------|------------------|---------------------|
| DB 마이그레이션 | 불필요 | 불필요 |
| 상태 머신 변경 | 큼(상수+전이+실패 6곳 추가) | 없음 |
| 실패 전이 누락 위험 | 높음(현재 실패 경로는 dataSttsCd 미변경) | 없음(procLog가 진실원) |
| 마킹 진입 차단 | `MarkingService:78` 자연 차단 | `MarkingService:68` 자연 차단(이미 구현) |
| 회귀 위험 | 높음 | 낮음 |

→ ②가 정석. 공통 선결: 조기 `MARKING_READY` 전이 제거(Phase 1 완료).

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | **없음** (`DATA_STTS_CD` VARCHAR 무제약, procLog 기존 테이블 재사용) |
| 기존 API | `GET /v1/videos` 응답 DTO에 필드 추가(하위호환 — 필드 추가만) |
| 도메인 모듈 | `batch`(runner/step/status), `video`(query/dto/repository), FE `features/video`·`pages`·`components/common` |
| 설정/환경 | 없음 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | 조기 전이 제거가 mock 모드(동기 완료)를 깨뜨림 | HIGH | (해결됨) `run()` 완료 여부(deIdntfYn='Y') 기준 조건부 전이 + self-proxy로 mock 영속 보장. 양 경로 테스트로 고정 |
| 2 | 목록 procLog 조인 N+1 | MED | 페이지 rawSn 집합 batch lookup(기존 `lookupExportInfos` 패턴) |
| 3 | 실패 영상이 PENDING으로만 남아 진행중과 구분 불가 | HIGH | procLog 최신행 `PROC_STTS_CD=FAILED`/`deIdntfYn='F'` 파생 규칙으로 실패 구분 |
| 4 | 운영 폴링 경로의 이중 MARKING_READY(submit직후+완료) | MED | (해결됨) submit직후 전이 제거로 완료 단일 지점(`applyBatchCompletion`)만 남김 |

---

## 상세 계획

### Phase 1: BE 비식별 전이 단일화 (조기 MARKING_READY 제거) ✅

**목표**: KPST 모드에서 비식별 제출 직후가 아니라 **완료(deIdntfYn='Y') 시점에만** `dataSttsCd=MARKING_READY`로 전이. mock 모드(동기 완료)는 기존대로 유지. dev/운영 양 경로 일관.

> 구현: `DeidentResult`(record, completed/deidFilePath) 신규 — `run()` 반환을 String→DeidentResult로 변경해 동기완료(mock)/지연위탁(KPST) 명시 구분. `AsyncDeidentifyRunner`/`DevPipelineRunner`가 completed일 때만 `markRawDataMarkingReady`. `BatchContext.deidentCompleted` 브릿지. **DEV_FIX**: `execute()→run()` self-invocation이 `@Transactional(REQUIRES_NEW)` 프록시를 우회해 mock 'Y'가 미영속되던 결함을 `ObjectProvider<DeidentifyStep>` self-proxy 호출로 수정(신규 `DeidentifyStepExecutePersistenceIntegrationTest`로 RED→GREEN 차단). QA: build PASS(62/62)·code-reviewer PASS·adversarial-critic ROBUST.

**수용 기준**: AC1·AC2 충족.

---

### Phase 2: BE 비식별 상태 목록 노출 (procLog 파생)

**목표**: 영상 목록(`GET /v1/videos`)이 각 영상의 비식별 상태(진행중/실패/완료)를 구분해 내려주도록 procLog 파생 필드 추가.

**근거**: 목록은 `VideoQueryService.list()`(`:60-89`)가 `VideoSummaryResponse`(`status`/`dataSttsCd`만 노출, `deIdntfYn`·procLog 미노출)를 반환하며 procLog를 조인하지 않음. 실패 영상은 `dataSttsCd=PENDING`+`deIdntfYn='F'`로만 구분되므로(`KpstDeidentTxService` 실패 경로가 dataSttsCd 미변경), 진행중/실패 구분에 procLog 파생 필요.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `video/service/VideoQueryService.java` | 수정 | `list()`에서 페이지 rawSn 집합으로 procLog 최신행 batch lookup(N+1 회피), 파생 status 계산 |
| `video/dto/VideoSummaryResponse.java` | 수정 | `deIdntfYn` + 파생 `deidentStatus`(IN_PROGRESS/FAILED/DONE/NONE) 필드 추가 |
| `batch/repository/LsDeidentProcLogRepository.java` | 수정 | rawSn 집합 → 최신 procLog 조회 메서드 추가(`findLatestByDataRawSnIn`) |
| `video/service/VideoQueryServiceTest.java` | 수정 | 파생 status 계산 케이스 |

**핵심 로직 / 시그니처**:
```java
// 파생 규칙 (영상 1건)
// deIdntfYn=='Y'                                       → DONE   (완료 = 마킹대기 가능)
// procLog 최신 PROC_STTS_CD=='FAILED' 또는 deIdntfYn=='F' → FAILED (비식별 실패)
// procLog 최신 WAITING/POLLING/REQUESTED               → IN_PROGRESS (비식별 진행중)
// procLog 없음 & deIdntfYn=='N'                         → NONE (적재 직후/비식별 미시작 — 규칙은 구현 시 확정)
List<LsDeidentProcLog> findLatestByDataRawSnIn(Collection<Long> rawSns); // 각 rawSn 최신 1행
```

**DTO 필드**:
| 이름 | 타입 | 설명 |
|------|------|------|
| deIdntfYn | String | 'Y'/'F'/'N' |
| deidentStatus | String(enum-like) | IN_PROGRESS / FAILED / DONE / NONE |

**테스트 케이스**:
- `비식별_진행중_영상은_deidentStatus가_IN_PROGRESS로_내려온다`
- `비식별_실패_영상은_deidentStatus가_FAILED로_내려온다`
- `비식별_완료_영상은_deidentStatus가_DONE으로_내려온다`
- `목록_N건_조회시_procLog를_건별_조회하지_않는다(batch lookup)`

**수용 기준**:
- [x] AC3(BE) 충족: 목록 응답에 진행중/실패/완료 구분 노출
- [x] N+1 없음(batch lookup)

> 구현: `VideoSummaryResponse`에 `deIdntfYn`+`deidentStatus`(IN_PROGRESS/FAILED/DONE/NONE) 추가, `LsDeidentProcLogRepository.findLatestByDataRawSnIn`(MAX(PROC_LOG_SN) IN), `VideoQueryService.lookupDeidentInfos` batch 파생. **DEV_FIX**: 선재 N+1 2건(`countByRawSn`→`countByRawSnsGrouped`, `lookupCctvNames`→`findAllById`) 제거 + KPST 폴링 분기(POLL_WAITING/POLLING→IN_PROGRESS) 테스트 3 + @Schema/Javadoc + V74 복합인덱스. QA: build PASS·api-designer PASS·database-reviewer(쿼리 정확)·performance(N+1 폐쇄)·code-reviewer CONDITIONAL PASS·coverage 보강.

**의존성**: Phase 1 (전이 의미 확정 후 파생 규칙 일관)

---

### Phase 3: FE 상태 배지 + 마킹 진입 차단

**목표**: 목록·상세에 "비식별 진행중/실패" 배지 표시, 미완료 영상의 마킹 진입 차단(버튼 비활성 + 안내).

**근거**: `VideoListPage.tsx:306-314`가 `v.status`(=dataSttsCd) 기반 배지 표시. 진입은 `TaskListPage.tsx:722` `navigate('/marking/...')`가 `firstSrcSn`(프레임 존재)만 보고 비식별 상태 미확인. 라벨 맵은 `StatusBadge.tsx:26-41 statusConfig`. BE 가드(`MarkingService.java:68` deIdntfYn='Y')가 백스톱으로 이미 존재.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/features/video/types.ts` | 수정 | `Video`에 `deidentStatus`/`deIdntfYn` 추가 |
| `frontend/src/components/common/StatusBadge.tsx` | 수정 | `BadgeStatus` union + config에 `비식별 진행중`/`비식별 실패` 추가 |
| `frontend/src/pages/VideoListPage.tsx` | 수정 | "처리 단계" 컬럼: deidentStatus IN_PROGRESS/FAILED면 비식별 배지 우선 표시 |
| `frontend/src/pages/TaskListPage.tsx` | 수정 | 마킹 진입(navigate) 전 deident 완료 여부 가드 — 미완료면 비활성/안내 |
| `frontend/src/pages/MarkingPage.tsx` | 수정 | 직접 진입 시에도 미완료면 차단 안내(백스톱) |
| `frontend/src/features/video/__tests__/VideoListPage.test.tsx` | 수정 | 배지 표시 케이스 |
| `frontend/src/pages/__tests__/MarkingPage.test.tsx` | 수정 | 미완료 진입 차단 케이스 |

**핵심 로직**:
```typescript
// VideoListPage 처리단계 셀
if (v.deidentStatus === 'IN_PROGRESS') return <StatusBadge status="DEIDENT_IN_PROGRESS" /> // "비식별 진행중"
if (v.deidentStatus === 'FAILED')      return <StatusBadge status="DEIDENT_FAILED" />      // "비식별 실패"
// else 기존 dataSttsCd 기반 배지
// TaskList 진입 가드: deident 미완료면 onClick 차단 + 툴팁 "비식별 완료 후 마킹 가능"
```

**테스트 케이스**:
- `비식별_진행중_영상은_목록에서_비식별진행중_배지가_표시된다`
- `비식별_실패_영상은_비식별실패_배지가_표시된다`
- `비식별_미완료_영상은_마킹_진입_버튼이_비활성화된다`
- `비식별_완료_영상만_마킹_화면으로_진입된다`

**수용 기준**:
- [x] AC3(FE) 충족: 진행중/실패 배지 표시
- [x] AC4 충족: 미완료 진입 차단 + 안내

> 구현: `DEIDENT_STATUS`(as const)+`isMarkingBlocked` 헬퍼(types.ts), StatusBadge `DEIDENT_IN_PROGRESS`(파랑)/`DEIDENT_FAILED`(빨강), VideoListPage 처리단계 배지 우선분기, TaskListPage 마킹버튼 disabled+aria 가드, MarkingPage role=alert 백스톱. QA: build PASS(858 vitest+tsc)·code-reviewer PASS(8.5/10, MEDIUM 매직스트링은 후속 폴리시). 브라우저 실동작 검증은 Step 4.

**의존성**: Phase 2 (DTO 필드)

---

## 실행 가이드

1. ~~`/cc-tdd Phase 1: BE 비식별 전이 단일화`~~ ✅ 완료
2. `/cc-tdd Phase 2: BE 비식별 상태 목록 노출 (procLog 파생 deidentStatus)`
3. `/cc-tdd Phase 3: FE 비식별 상태 배지 + 마킹 진입 차단`
