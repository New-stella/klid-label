# 구현 계획: 데이터마트 뷰 재편 — 라벨내용 뷰 제거 + export 폴더경로 노출 + 변경점 뷰 신설

> 생성: 2026-07-20 | 상태: 진행 중 | 브랜치: feat/datamart-view-slim
> 🎯 목표: 검수 승인 시 export 폴더(JSON+이미지)에 라벨 내용이 있으므로 데이터마트에서 라벨 내용 뷰(V_COMPLETED_LABEL/LABEL_ATTR)를 제거하고, 관제서버가 ① export 폴더 경로만 픽업 + ② 변경점·메타만 DB 뷰로 쿼리하도록 데이터마트 연동을 슬림화한다.

## Phase 진행 현황

- [x] Phase 1: [BE] 데이터마트 뷰 재편 마이그레이션(V114) — LABEL/LABEL_ATTR 제거 + VIDEO에 EXPORT_PATH_NM 추가 + LABEL_CHANGE 신설 ✅ 완료 (2026-07-20, DatamartViewSlimIT 6/6 PASS, SQL리뷰 결함0)
- [ ] Phase 2: 문서 동기화(CLAUDE.md·D9·D4·v2-wiki) + 통합 검증

## 요구사항 요약

- 목적: export 폴더에 라벨 JSON이 있으므로 데이터마트 라벨 내용 뷰를 제거하고, 관제는 export 폴더 경로 + 변경점 + 메타만 DB에서 가져가도록 정리.
- 범위(포함): 데이터마트 뷰 재편(Flyway 마이그레이션) + 관련 설계/위키 문서.
- 범위(제외): 관제 REST 조회 API 신설(현행 공유 DB 뷰 SELECT 방식 유지) · TASK_COMPLETED/MODIFIED 통지 로직 변경 · export 저장 로직 변경 · 경로 저장 로직 신설(이미 전부 DB 저장됨).
- 주요 액터: 관제서버(공유 DB 뷰 SELECT 소비자).
- 요구 목록:
  - R1: `V_COMPLETED_LABEL`·`V_COMPLETED_LABEL_ATTR` **제거**(라벨 좌표·속성은 export JSON에 존재, 중복) | 출처: 사용자 명시
  - R2: `V_COMPLETED_VIDEO`에 **`EXPORT_PATH_NM`(+`FRAME_CNT`)** 노출 — `LS_DATASET_EXPORT` 최신 SUCCEEDED/ACTIVE 버전 조인, 영상 1건=1 row 유지 | 출처: Q&A 확정
  - R3: **`V_COMPLETED_LABEL_CHANGE`** 신설 — `LS_DATA_LBL_HSTRY` 기반 변경점(RAW_SN·SRC_SN·LBL_SN·CHG_KIND_CD·REG_DT·REG_ID), APPROVED 게이트. 관제가 뷰 SELECT로 변경점 쿼리 | 출처: Q&A 확정
  - R4: `V_COMPLETED_FRAME`(프레임 이미지 경로쌍)·`V_COMPLETED_META`(시계열 메타)·`TASK_MODIFIED` 통지 **유지** | 출처: Q&A 확정
  - R5: 문서 동기화 — CLAUDE.md 데이터마트 섹션·D9·D4·v2-wiki 18-database에 뷰 재편 반영 | 출처: 문서 동기화 규칙
- 수용 기준:
  - AC1: 마이그레이션 적용 후 `V_COMPLETED_LABEL`·`V_COMPLETED_LABEL_ATTR` 뷰가 **존재하지 않는다**.
  - AC2: `V_COMPLETED_VIDEO` 조회 시 `EXPORT_PATH_NM`(+FRAME_CNT)이 노출되고, 검수완료(APPROVED) 영상 1건당 1 row(export 버전 중복으로 행 증식 없음)를 반환한다.
  - AC3: `V_COMPLETED_LABEL_CHANGE` 조회 시 APPROVED 영상의 라벨 변경점(변경종류·시각·작업자)이 반환된다.
  - AC4: `V_COMPLETED_FRAME`·`V_COMPLETED_META`는 컬럼·동작 변경 없이 유지된다.
  - AC5: 마이그레이션은 멱등(재실행 안전)하고, 애플리케이션 기동(Flyway validate)·기존 테스트가 GREEN이다(뷰는 엔티티 미매핑이라 ddl-auto validate 무영향).
- 비기능:
  - 뷰 변경은 **관제서버 연동 계약**에 영향 → 문서에 계약 변경 명시(관제팀 협의 대상). LS_* 저작도구 소유 뷰라 MNG_* 선승인 대상은 아님.
  - EXPORT_PATH_NM 조인은 SUCCEEDED 최신 버전 1건으로 축약(LEFT JOIN + 서브쿼리/윈도우로 1:1 보장), 미export(경로 null) 허용.
  - 경로 노출은 파일시스템 경로 문자열만(민감정보 아님). PII·토큰 노출 없음.

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | Flyway V114 신규 — `DROP VIEW IF EXISTS V_COMPLETED_LABEL/LABEL_ATTR`, `CREATE OR REPLACE VIEW V_COMPLETED_VIDEO`(EXPORT_PATH_NM/FRAME_CNT 추가), `CREATE OR REPLACE VIEW V_COMPLETED_LABEL_CHANGE`. LS_* 뷰만, MNG_* 무관 |
| 기존 API | 없음(뷰 SELECT는 관제 소비, 저작도구 앱 코드는 뷰 미참조) |
| 도메인 모듈 | 없음(엔티티/서비스 무변경). 마이그레이션 SQL만 |
| 설정/환경 | 없음 |
| 문서 | CLAUDE.md(데이터마트 섹션)·docs/design/D9·D4(+design-full)·docs/v2-wiki/18-database.md |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | LS_DATASET_EXPORT 다중 버전(EXPORT_VER_NO) 조인으로 V_COMPLETED_VIDEO 행 증식(1영상 N행) | HIGH | 최신 SUCCEEDED/ACTIVE 1건만 LEFT JOIN(상관 서브쿼리 or ROW_NUMBER). AC2 "1영상=1row" 테스트 |
| 2 | 관제서버가 V_COMPLETED_LABEL을 SELECT 중이면 뷰 제거로 그쪽 쿼리 파손 | MED | 연동 계약 변경 → 문서 명시·관제팀 협의 노트. M2M deprecated·뷰 SELECT 방식이므로 관제측 반영 필요를 문서화 |
| 3 | 뷰 제거/변경이 다른 마이그레이션·테스트의 뷰 참조를 깨뜨림 | MED | 앱 코드 뷰 미참조 확인됨. 뷰 존재/컬럼 검증 테스트(있으면) 갱신. build-validator 회귀 확인 |
| 4 | LABEL_CHANGE의 APPROVED 게이트 조인이 히스토리 대량행에서 성능 저하 | LOW | SRC_SN→APPROVED EXISTS 게이트(기존 뷰 패턴 재사용), LS_DATA_LBL_HSTRY 인덱스(IDX_LDLH_SRC) 활용 |

---

## 상세 계획

### Phase 1: [BE] 데이터마트 뷰 재편 마이그레이션 (V114)

**목표**: 단일 Flyway 마이그레이션으로 라벨 내용 뷰 제거 + VIDEO 경로 노출 + 변경점 뷰 신설. 앱 코드 무변경.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/src/main/resources/db/migration/V114__slim_datamart_views_expose_export_path.sql` | 생성 | ①`DROP VIEW IF EXISTS klid_at.V_COMPLETED_LABEL, V_COMPLETED_LABEL_ATTR` ②`CREATE OR REPLACE VIEW V_COMPLETED_VIDEO`(기존 V102 정의 + `EXPORT_PATH_NM`·`FRAME_CNT` 컬럼 추가, LS_DATASET_EXPORT 최신 SUCCEEDED 조인) ③`CREATE OR REPLACE VIEW V_COMPLETED_LABEL_CHANGE`(LS_DATA_LBL_HSTRY 기반) |
| 뷰 검증 테스트(있으면) | 수정/생성 | 뷰 존재·컬럼·행수 검증(Testcontainers). 없으면 신규 `DatamartViewSlimIT` 최소 검증 |

**핵심 SQL (개략 — 실제는 기존 V102/V52 정의를 baseline으로 정확히 재작성)**:
```sql
-- 1) 라벨 내용 뷰 제거
DROP VIEW IF EXISTS V_COMPLETED_LABEL;
DROP VIEW IF EXISTS V_COMPLETED_LABEL_ATTR;

-- 2) VIDEO 뷰에 export 폴더 경로 노출 (기존 V102 컬럼 전체 보존 + 아래 추가)
CREATE OR REPLACE VIEW V_COMPLETED_VIDEO AS
SELECT ... (V102 기존 컬럼 전부) ...,
       e.EXPORT_PATH_NM,
       e.FRAME_CNT
FROM LS_DATASET_VIDEO_META m
JOIN LS_DATA_RAW r ON ...
JOIN LS_RAW_DATA_STATUS s ON ... AND s.DATA_STTS_CD='APPROVED'
LEFT JOIN LATERAL (
  SELECT ex.EXPORT_PATH_NM, ex.FRAME_CNT
  FROM LS_DATASET_EXPORT ex
  WHERE ex.DATA_RAW_SN = r.RAW_SN AND ex.EXPORT_STTS_CD='SUCCEEDED'
  ORDER BY ex.EXPORT_VER_NO DESC LIMIT 1
) e ON TRUE
WHERE m.ACTIVE_YN='Y';

-- 3) 변경점 뷰 신설 (APPROVED 게이트)
CREATE OR REPLACE VIEW V_COMPLETED_LABEL_CHANGE AS
SELECT h.LBL_HSTRY_SN, src.RAW_SN, h.SRC_SN, h.LBL_SN,
       h.CHG_KIND_CD, h.REG_ID, h.REG_DT
FROM LS_DATA_LBL_HSTRY h
JOIN LS_DATA_SRC src ON src.SRC_SN = h.SRC_SN
WHERE EXISTS (SELECT 1 FROM LS_RAW_DATA_STATUS s
              WHERE s.RAW_SN = src.RAW_SN AND s.DATA_STTS_CD='APPROVED');
```
> ⚠ 정확한 컬럼·조인·물리명(STNG/표준용어)은 기존 V52/V101/V102/V104 정의와 LS_DATASET_EXPORT(V105/V106)·LS_DATA_LBL_HSTRY(V112) 실 컬럼을 대조해 재작성. PostgreSQL 문법. LATERAL/상관서브쿼리 중 프로젝트 관례 선택(행 증식 0 보장이 핵심).

**테스트 케이스** (RED, Testcontainers IT):
- `V_COMPLETED_LABEL과_LABEL_ATTR_뷰가_제거되어_존재하지_않는다`
- `V_COMPLETED_VIDEO에_EXPORT_PATH_NM이_노출되고_APPROVED영상당_1row다`(export 2버전 있어도 최신 1건)
- `미export_영상은_EXPORT_PATH_NM이_null이어도_VIDEO에_노출된다`
- `V_COMPLETED_LABEL_CHANGE가_APPROVED영상의_변경점을_반환한다`(ADDED/UPDATED/DELETED)
- `비APPROVED영상의_변경점은_LABEL_CHANGE에_안나온다`
- `V_COMPLETED_FRAME·V_COMPLETED_META는_무영향이다`

**수용 기준**: AC1~AC5

**의존성**: 없음 (LS_DATASET_EXPORT V105/V106·LS_DATA_LBL_HSTRY V112 선행 존재)

---

### Phase 2: 문서 동기화 + 통합 검증

**목표**: 뷰 재편을 설계·위키 문서에 반영(관제 연동 계약 변경 명시) + 전체 검증.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `CLAUDE.md` | 수정 | "데이터마트 적재용 View" 섹션: LABEL/LABEL_ATTR 제거, VIDEO에 EXPORT_PATH_NM, LABEL_CHANGE 신설, FRAME/META 유지 반영. "관제=경로 픽업+변경점/메타 쿼리" 서술 |
| `docs/design/D9-데이터베이스설계서.md`(+design-full) | 수정 | 데이터마트 뷰 명세 갱신(제거 2·추가 1·컬럼추가 1·유지 2) |
| `docs/design/D4-인터페이스설계서.md` | 수정 | 관제 조회 계약(뷰 SELECT) 변경 반영 |
| `docs/v2-wiki/18-database.md` | 수정 | 데이터마트 뷰 목록 갱신 |

**작업**: `"Phase 2 진행해줘"`(문서) → requirements-verifier 전체 + design-verifier(D9/D4 baseline) + `/cc-deploy-check`(선택).

**수용 기준**: AC5 + R5 문서 반영 + 전체 R1~R5 역추적 CONFORM.

**의존성**: Phase 1.

---

## 실행 가이드

1. `/cc-tdd Phase 1: 데이터마트 뷰 재편 마이그레이션(V114)` — 마이그레이션 + 뷰 검증 IT
2. `"Phase 2 진행해줘"` — 문서 동기화 → 통합 검증
