# 구현 계획: hwpx 산출물 주입 8종 확대

> 생성: 2026-06-05 18:00
> 상태: 진행 중
> 선행: R2 PoC 완료 (커밋 4297929, 한글 검증 통과) — `scripts/hwpx_fill.py` + 후처리 4종 확립

## Phase 진행 현황

- [ ] Phase 1: 코어 패키지화 + 문서 config 레지스트리 — R2 동작 불변 보장
- [ ] Phase 2: D4·D9 주입 (다이어그램 없음 — 행/카드 복제 검증)
- [ ] Phase 3: Mermaid 렌더 인프라 + D5·D8 주입
- [ ] Phase 4: D1 주입 (최복잡 — UC별/클래스별 4섹션 반복 + mermaid 26)
- [ ] Phase 5: D3·D2 주입 (컴포넌트 카드 + 화면 상세 카드)
- [ ] Phase 6: 일괄 CLI + cc-doc-gen 연계 + 최종 한글 검증 게이트

## 요구사항 요약

- **목적**: cc-doc-gen이 생성하는 md 산출물을 발주처 hwpx 템플릿(표지·개정이력 제외 본문)에 자동 주입 — 제출 산출물 수작업 제거
- **범위 (포함)**: R2·D1·D2·D3·D4·D5·D8·D9 8종 (md 소스 × hwpx 템플릿 교집합). 다이어그램 이미지 렌더(PlantUML+Mermaid) 포함
- **범위 (제외)**: R1·R3 (hwpx 템플릿 없음), D7·D10·D11·D12 (cc-doc-gen 범위 외 시험 문서), 표지·개정이력(section0 — byte-identical 보존), 역방향(hwpx→md) 동기화
- **주요 액터**: PM/문서 담당자 (스크립트 실행 + 한글 육안 검증)
- **비기능**: 원본 템플릿 무수정, 각 문서 `--verify` 라운드트립 자동 검증 + 한글 수동 검증 게이트, md 재생성(cc-doc-gen) 시 재실행 가능(멱등)

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | 없음 |
| 기존 API | 없음 |
| 코드 | `scripts/hwpx_fill.py` → `scripts/hwpxfill/` 패키지 분리 + 문서별 config 8종 |
| 설정/환경 | mermaid 렌더용 `npx @mermaid-js/mermaid-cli` (최초 1회 다운로드), 기존 tools/plantuml.jar |
| 문서 | `~/.claude/commands/cc-doc-gen.md`에 hwpx 변환 단계 추가 (Phase 6) |

## 핵심 전제 (PoC 확립 — 모든 Phase 공통)

1. **후처리 4종 필수**: treatAsChar 1→0(주입 표만), linesegarray 전량 제거, tbl id 유일화(+cellAddr 중복 가드), 빈 문단 정규화(절 제목 앞 1개만). NovaTerm 메모리 4ae9b255.
2. **section0 = 표지·개정이력** — byte-identical 보존. 본문은 section1+.
3. 이미지 manifest는 `content.hpf`의 `<opf:item>`, ZIP mimetype 첫 엔트리 STORED.
4. **참조 구현** `~/Documents/workspace/md_uml`(한글 검증 완료) — 매핑 막히면 `src/utils/export/docs/{d1~d11,r2}.js` + `core/hwpxReplacer.js` 대조가 최단 진단. 단 md_uml은 태그 사전 삽입 템플릿, 우리는 무태그 원본 템플릿 + 헤더 텍스트 매칭 방식 유지.
5. 행 늘어난 표는 `hp:sz height` 비례 확대 (md_uml 정합).
6. 출력 = `docs/design/hwpx/{원본명}_filled.hwpx`.

## md 소스 ↔ hwpx 템플릿 구조 실측 (2026-06-05)

| 문서 | md 구조 | hwpx 본문 표(행×열) | 다이어그램 | 난도 |
|------|---------|--------------------|-----------|:---:|
| R2 | GFM 56행 + html_table 13 + plantuml 4 | 7개 (기술서 2×4 카드) | PlantUML 4 (완료) | ✅ PoC |
| D4 | GFM 47행 + html_table 13 | 4개 — 목록 6×14, 명세 6×13 (광폭·병합 셀) | 없음 | 중 |
| D9 | GFM 832행 | 5개 — 목록 6×4, 정의 5×7, 테이블 명세 9×9 카드 | 없음 | 중 |
| D5 | GFM 133행 + mermaid 2 | 6개 — 구조도 1×1 ×2, NFR 표 5×2 ×2 | Mermaid 2 | 중 |
| D8 | GFM 601행 + mermaid 12 | 4개 — ERD 2×4, 엔티티 명세 7×10 카드 | Mermaid 12 (ERD) | 중상 |
| D1 | GFM 591행 + mermaid 26 | 7개 — 목록 7×3, SD 4×4, DCD 3×4, 클래스 정의 10×5 카드 | Mermaid 26 (SD+DCD) | **상** |
| D3 | GFM 520행 + mermaid 13 | 6개 — 구조도 3×4, 목록 8×4, 명세 15×4 카드, IF 명세 7×4 | Mermaid 13 | 상 |
| D2 | GFM 279행 + html_table 13 | 6개 — 구조도 1×1, 목록 4×3 ×2, 화면 상세 16×8 카드 | 구조도 1 (mermaid 아님 — md 실측 필요) | 상 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | 병합 셀(rowSpan/colSpan) 복제 시 cellAddr 어긋남 — D4 13~14열, D9 9×9, D2 16×8 | HIGH | md_uml BUG-D4-INTF 선례: 복제 행 rowSpan=1 정규화 + rowSpan 그룹(연속 빈 행) 통째 대체. cellAddr 중복 가드가 회귀 감지 |
| 2 | Mermaid 렌더 환경 — mmdc 미설치, npx 첫 실행 시 chromium 다운로드 | MED | Phase 3에서 인프라 분리 구축. 실패 시 `(별첨: {다이어그램명})` 텍스트 폴백 (PoC에 폴백 경로 기구현) |
| 3 | D1 4섹션 반복 구조 (UC별 ×13 × 섹션 3 + 클래스별 카드 ×N) — 카드 안 sub-list 행 복제 중첩 | HIGH | md_uml `expandSubListRowsInBlock` 로직 이식. Phase 4 단독 배정 |
| 4 | md 포맷 드리프트 — cc-doc-gen 재생성 시 헤더/구조 변화로 파서 침묵 실패 | MED | 문서별 `--verify`에 "주입 행수 ≥ 1 + md 추출 건수 == hwpx 행수" 단언. 0건 주입 시 명시 에러 (조용한 빈 문서 금지) |
| 5 | 한글 수동 검증 병목 (8종 × 육안) | LOW | Phase별 게이트로 분산 (2·3·4·5 종료 시 해당 문서만 확인) |
| 6 | D2 화면 상세 16×8 표 — 입출력 필드 행 복제 + 컨트롤 셀 구조 복잡 | MED | md_uml `docs/d2.js` 매핑 대조. 화면 캡처 이미지는 범위 외(텍스트·표만) |

---

## 상세 계획

### Phase 1: 코어 패키지화 + 문서 config 레지스트리

**목표**: 47KB 단일 스크립트를 core/config 구조로 분리하되 **R2 출력이 동작 불변**(verify 26종 동일 PASS + 주입 통계 동일)함을 보증. 이후 Phase가 config 추가만으로 문서를 확장할 수 있는 골격 완성.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `scripts/hwpxfill/__init__.py` | 생성 | 패키지 |
| `scripts/hwpxfill/core.py` | 생성 | ZIP in-memory 처리, 표 탐색(헤더 텍스트 매칭), 행 복제, 카드 블록 복제, 셀 문단 주입 (기존 함수 이동) |
| `scripts/hwpxfill/postprocess.py` | 생성 | treatAsChar/linesegarray/tbl id/cellAddr/빈 문단 정규화/height 비례 확대 (기존 함수 이동) |
| `scripts/hwpxfill/render.py` | 생성 | PlantUML 렌더(smetana) + 이미지 임베드(content.hpf opf:item + hp:pic) (기존 함수 이동) |
| `scripts/hwpxfill/verify.py` | 생성 | 라운드트립 검증 프레임워크 — 공통 검증(무결성·section0·후처리 4종) + 문서별 검증 등록 |
| `scripts/hwpxfill/docs/r2.py` | 생성 | R2 config: 표 매핑(헤더 텍스트→md 섹션), 카드 정의(UCD·기술서), 다이어그램(plantuml) — 기존 R2 특화 로직 이동 |
| `scripts/hwpxfill/docs/__init__.py` | 생성 | `get_doc_config(doc_key)` 레지스트리 (md_uml `docs/index.js` 구조 차용) |
| `scripts/hwpx_fill.py` | 수정 | CLI 셸로 축소 — `--doc R2`(자동 감지: md 파일명 prefix), 기존 인자 호환 유지 |
| `scripts/test_hwpx_pagebreak.py` | 수정 | import 경로만 갱신 (14종 전부 유지) |

**핵심 시그니처**:
```python
# docs/__init__.py
def get_doc_config(doc_key: str) -> DocConfig  # 'R2'|'D1'|...|'D9', 미등록 시 명시 에러
# DocConfig: md_path_pattern, table_mappings[{hwpx_header_text, md_section, columns}],
#            card_sections[{header_text, repeat_source, content_builder}], diagrams{engine, ...}
# core.py
def fill_document(cfg: DocConfig, md_path, hwpx_path, out_dir) -> FillStats
```

**테스트 케이스**:
- `R2_재생성_출력이_기존_verify_26종_전부_PASS` (동작 불변 회귀)
- `기존_pagebreak_테스트_14종_전부_PASS_유지`
- `미등록_doc_key_요청시_명시_에러` (D1 등 미구현 키)
- `R2_주입_통계_불변` (서브시스템 4·UC 13·액터 7·UCD 4·기술서 13)

**수용 기준**:
- [ ] `python3 scripts/hwpx_fill.py --md docs/design/R2-유스케이스명세서.md --hwpx "docs/cbd/hwpx/KLID_OO_유스케이스 명세서 v1.0.hwpx" --out docs/design/hwpx/` 기존 인자 그대로 동작
- [ ] R2 `--verify` 26/26 + pytest 14/14 PASS (수 감소 0)
- [ ] core/postprocess/render에 문서명("유스케이스" 등) 하드코딩 0건 (md_uml coreIsolation 원칙)

**의존성**: 없음

### Phase 2: D4·D9 주입 (다이어그램 없음)

**목표**: 다이어그램 없는 2종으로 행/카드 복제 + 병합 셀 처리를 검증. `D4-인터페이스설계서_filled.hwpx`, `D9-데이터베이스설계서_filled.hwpx` 생성 + 한글 검증.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `scripts/hwpxfill/docs/d4.py` | 생성 | §1 인터페이스 목록(6×14 광폭 행 복제) + §2 명세 카드(6×13, 송수신 좌우대조 — html_table 13개 소스) |
| `scripts/hwpxfill/docs/d9.py` | 생성 | §1 DB 목록 + §2 DB 정의 + §3 테이블 명세 카드(9×9 — 테이블별 반복, 컬럼 행 복제 sub-list) |
| `scripts/hwpxfill/core.py` | 수정 | rowSpan 그룹 복제 정규화 (md_uml line 159~196 이식: 복제 행 rowSpan=1 + 연속 빈 행 그룹 대체) |
| `scripts/test_hwpx_d4_d9.py` | 생성 | 문서별 회귀 |

**참조**: md_uml `docs/d4.js`(13열 좌우대조 — 커밋 e9ac197), `docs/d9.js`. D4 광폭 표는 BUG-D4-INTF-EMPTYROW 선례 필독.

**테스트 케이스**:
- `D4_목록_행수가_md_표_행수와_일치`
- `D4_명세_카드수가_md_html_table수와_일치`
- `D4_복제행_rowSpan_전부_1_정규화`
- `D9_테이블_명세_카드수_md_테이블수_일치` (소유 LS_* 전수)
- `D9_카드내_컬럼_행복제_rowAddr_연속`
- `공통_후처리_4종_PASS` (treatAsChar·linesegarray·tbl id·cellAddr — 문서별 verify 자동 포함)

**수용 기준**:
- [ ] 2종 `--verify` ALL PASS + 주입 0건 시 명시 에러 (위험 #4)
- [ ] ✋ 한글 육안: 광폭 표 페이지 분할·병합 셀 정상 (사용자 게이트)

**의존성**: Phase 1

### Phase 3: Mermaid 렌더 인프라 + D5·D8 주입

**목표**: mermaid→PNG 렌더 경로 확립 후 다이어그램 적은 D5(2개)와 ERD 중심 D8(12개) 생성.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `scripts/hwpxfill/render.py` | 수정 | `render_mermaid(code) -> png_path` — `npx -y @mermaid-js/mermaid-cli`(mmdc) subprocess, 실패 시 `(별첨: …)` 텍스트 폴백 + 리포트 경고. 이미지 폭은 셀 폭(cellSz width) 기반 HWPUNIT 환산 |
| `scripts/hwpxfill/docs/d5.py` | 생성 | §1·§2 구조도(1×1 표에 이미지 1장씩) + §3 NFR 표(5×2 카드 — NFR별 반복) |
| `scripts/hwpxfill/docs/d8.py` | 생성 | §1 ERD(2×4 표 — mermaid erDiagram 12장 이미지) + §2 엔티티 명세 카드(7×10 — 엔티티별 반복, 속성 행 복제) |
| `scripts/test_hwpx_d5_d8.py` | 생성 | 문서별 회귀 |

**테스트 케이스**:
- `mermaid_렌더_성공시_PNG_생성_및_content_hpf_opf_item_등록`
- `mermaid_렌더_실패시_별첨_텍스트_폴백_및_경고_리포트` (mmdc 강제 실패 mock)
- `D5_NFR_카드수_md_NFR수_일치`
- `D8_엔티티_카드수_md_엔티티수_일치` + `속성_행수_일치`
- `이미지_id_instid_유일` (기존 검증 자동 적용)

**수용 기준**:
- [ ] 2종 `--verify` ALL PASS, 이미지 렌더 성공률 리포트 출력
- [ ] ✋ 한글 육안: ERD 이미지 크기·선명도, 엔티티 카드 분할 (사용자 게이트)

**의존성**: Phase 1 (Phase 2와 독립이나 순차 권장 — core 변경 충돌 방지)

### Phase 4: D1 주입 (최복잡 단독 Phase)

**목표**: D1 클래스설계서 — UC별 반복 3섹션(목록 7×3·SD 4×4·DCD 3×4) + 클래스별 카드(10×5) + mermaid 26장. CBD 4섹션 반복 규칙(UC↔SD↔DCD 1:1, 결번 동조) 보존.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `scripts/hwpxfill/docs/d1.py` | 생성 | §1 UC별 클래스 목록(카드 ×UC수) + §2 SD 카드(이미지) + §3 DCD 카드(이미지) + §4 클래스 정의 카드(속성/오퍼레이션 sub-list 행 복제) |
| `scripts/hwpxfill/core.py` | 수정 | 카드 내 sub-list 행 복제 (md_uml `expandSubListRowsInBlock` 이식 — 카드 1개 + 내부 행 N개 동시 확장) |
| `scripts/test_hwpx_d1.py` | 생성 | 문서별 회귀 |

**테스트 케이스**:
- `UC별_목록_카드수_md_UC수_일치` + `SD_DCD_카드수_UC수_일치` (1:1 동조)
- `클래스_정의_카드내_속성행_오퍼레이션행_복제_정합`
- `mermaid_26장_렌더_또는_폴백_집계_리포트`
- `결번_UC_처리` (md 부록 추적 매트릭스와 카드 ID 순서 일치)

**수용 기준**:
- [ ] `--verify` ALL PASS, 카드 ID(KLID-AT-SD/DCD/DC-NNN) 누락 0
- [ ] ✋ 한글 육안: 카드 반복 페이지 흐름 (사용자 게이트)

**의존성**: Phase 3 (mermaid 인프라)

### Phase 5: D3·D2 주입

**목표**: D3 컴포넌트설계서(CO별 카드 + IF×오퍼레이션 반복 + mermaid 13) + D2 UI설계서(화면 상세 16×8 카드 + html_table 13).

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `scripts/hwpxfill/docs/d3.py` | 생성 | §1 구조도 카드(이미지+헤더 표) + §2 목록 + §3 명세 카드(15×4) + §4 IF 명세(오퍼레이션별 반복 7×4) |
| `scripts/hwpxfill/docs/d2.py` | 생성 | §1 구조도 + §2 화면 목록 + §3 화면 상세 카드(16×8 — 입출력 필드 행 복제 + 처리 내용 장문 셀) + §4 컴포넌트 명세 |
| `scripts/test_hwpx_d2_d3.py` | 생성 | 문서별 회귀 |

**참조**: md_uml `docs/d3.js`(신구조 매핑 — 커밋 91dcdb7), `docs/d2.js`(UI 컴포넌트 행 누수 차단 — 커밋 97bcea0). D2 md의 구조도 형식(mermaid 아님 — fences 0)은 구현 시 실측 후 이미지/텍스트 결정.

**테스트 케이스**:
- `D3_CO_카드수_UC수_일치` (UC↔CO 1:1) + `IF_명세_반복수_오퍼레이션_총수_일치`
- `D2_화면_카드수_md_화면수_일치` + `입출력_필드_행수_일치`
- `D2_화면목록에_UI컴포넌트_행_누수_없음` (md_uml 97bcea0 선례)

**수용 기준**:
- [ ] 2종 `--verify` ALL PASS
- [ ] ✋ 한글 육안: 16×8 광폭 카드 페이지 분할 (사용자 게이트)

**의존성**: Phase 3 (D3 mermaid), Phase 4 권장 (sub-list 복제 공유)

### Phase 6: 일괄 CLI + cc-doc-gen 연계 + 최종 검증

**목표**: 8종 한 번에 생성하는 일괄 모드 + cc-doc-gen 워크플로 연결. 전체 재검증.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `scripts/hwpx_fill.py` | 수정 | `--all` (docs/design/*.md × docs/cbd/hwpx 자동 페어링, 8종 순회 + 통합 리포트), `--doc R2 D1 …` 다중 선택 |
| `~/.claude/commands/cc-doc-gen.md` | 수정 | Step 7 "hwpx 변환(선택)" 추가 — md 생성 후 `scripts/hwpx_fill.py --all` 안내 + 한글 검증 게이트 명시 (하네스 메타파일 — PM 직접 수정 예외) |
| `scripts/test_hwpx_all.py` | 생성 | 8종 일괄 스모크 (verify 집계) |

**테스트 케이스**:
- `all_모드_8종_전부_생성_및_verify_집계_리포트`
- `md_누락_문서는_스킵_및_명시_보고` (조용한 실패 금지)
- `기존_filled_출력_덮어쓰기_전_경고`

**수용 기준**:
- [ ] `--all` 1회 실행으로 8종 생성, 통합 리포트(문서별 주입 통계·이미지 성공률·verify 결과)
- [ ] ✋ 최종 한글 육안: 8종 전체 (사용자 게이트)
- [ ] cc-doc-gen 문서에 변환 단계 반영

**의존성**: Phase 2~5 전부

---

## 실행 가이드

1. `/cc-tdd Phase 1: hwpxfill 코어 패키지화 + config 레지스트리 (R2 동작 불변)` → `/cc-tdd Phase 2: D4·D9 주입 (rowSpan 그룹 복제)`
2. `/cc-tdd Phase 3: mermaid 렌더 인프라 + D5·D8 주입` → `/cc-tdd Phase 4: D1 주입 (UC별 4섹션 반복 + sub-list)`
3. `/cc-tdd Phase 5: D3·D2 주입` → `/cc-tdd Phase 6: --all 일괄 CLI + cc-doc-gen 연계`

> 각 Phase 종료 시 해당 문서 한글 육안 검증 게이트 (사용자) — PASS 후 다음 Phase 진행.
