# 구현 계획: AI 탐지 폴리곤 출력 + 오토라벨 저장흐름 정상화 + 라벨 관리 일원화 + 히스토리 + AI Tool 팝업(추적)

> 생성: 2026-07-20 | 상태: **전체 완료** ✅ | 브랜치: feat/fe-loading-states
> 최종 검증: requirements-verifier(R1~R12 CONFORM) · design-verifier(CONFORM) · BE 2465/2468·FE 1307 GREEN(실패 3=기존 DatasetExport). 후속: 브라우저 E2E(dev서버 부재)·.hwpx 재생성·V_COMPLETED_LABEL 뷰 AI_INFO 확장(데이터마트 AI분류 시).
> 🎯 목표: ① AI 탐지/추적 시 박스/폴리곤 출력 선택(폴리곤=탐지→자동분할) ② 오토라벨이 수동 라벨과 동일하게 "작업본→명시 저장"으로 동작(자동저장·기존라벨 삭제 제거, 중복방지) ③ 수동/오토 라벨 관리 일원화 ④ 라벨 저장 시 히스토리 등록 + FE 리스트 노출 ⑤ 매뉴얼 "AI Tool" 팝업(형태+라벨+일반/트랙)을 AI 추적에도 적용

## Phase 진행 현황

- [x] Phase 1: [BE] 온라인 오토라벨 자동저장 제거 — 좌표만 반환(persist 폐지) ✅ (QA 8종·DEV_FIX 1회)
- [x] Phase 2: [BE] 라벨 저장 경로에 히스토리 등록 + 히스토리 조회 API ✅ (QA 9종·DEV_FIX 1회, 트랙삭제 DELETED 감사 보강)
- [x] Phase 3: [BE+ai-server] 폴리곤 파이프라인(탐지→박스별 자동분할) + shape 파라미터 + 추적 형태 출력 ✅ (QA 9종·DEV_FIX 1회, 전체 2458/2461 GREEN)
- [x] Phase 4: [FE] 오토라벨 결과 작업본 병합(IoU 중복방지·기존보존) + AI Tool 팝업(형태·라벨·일반/트랙) ✅ (QA 6종·DEV_FIX 1회 R12, vitest 1289 GREEN)
- [x] Phase 5: [FE] 라벨 관리 일원화 마감 + 히스토리 리스트 UI ✅ (QA 5종·DEV_FIX 1회 출처보존[BE+FE], BE 2465/2468·FE 1307 GREEN, R9 CONFORM·Mass Assignment 차단)
- [x] Phase 6: 문서 동기화 + 전체 통합 검증 ✅ (wiki+design+design-full 갱신, requirements CONFORM·design-verifier CONFORM)

## 요구사항 요약

- 목적: 캔버스 라벨링에서 오토라벨(AI 탐지·추적)이 수동 라벨과 **동일한 저장 모델**로 동작하도록 정상화하고, AI 탐지·추적에 **형태(박스/폴리곤) + 라벨 선택**(매뉴얼 "AI Tool" 팝업)을 제공하며, **라벨 히스토리**를 등록·표시한다.
- 범위(포함): 온라인(캔버스 버튼) 오토라벨 흐름, AI 추적(SAM 트랙) 도구, 라벨 저장 경로, 라벨 히스토리, AI Tool 팝업, 관련 FE 패널.
- 범위(제외):
  - **배치 파이프라인 자동 오토라벨**(`YoloAutolabelStep`/`Sam2SegmentStep` 등 비동기 배치)은 변경하지 않음 — 자동저장이 정상 동작인 경로.
  - **포털 경로**(autolabel 403, ADR-013) 변경 없음.
  - 데이터마트 View·비식별·마킹 파이프라인 무접촉.
- 주요 액터: WORKER/REVIEWER (라벨링 작업자·검수자).
- 요구 목록:
  - R1: AI 탐지 실행 시 출력 형태(박스/폴리곤)를 선택 가능 | 출처: 사용자 명시
  - R2: 폴리곤 선택 시 → 탐지(박스 검출) → 각 박스 자동 분할(SAM) → POLYGON 좌표 반환 | 출처: Q&A 확정
  - R3: 기존 박스 경로 하위호환, 기본값=박스 | 출처: (가정)
  - R4: 기존 클래스/라벨 선택이 박스·폴리곤 두 경로 모두 반영 | 출처: (가정)
  - R5: FE 노출 문구 모델명(YOLO/SAM) 금지 — "박스/폴리곤" 등 사용자 용어 | 출처: 프로젝트 구속([[fe-ui-no-tech-model-names]])
  - R6: R1~R2·R12는 R1요구사항정의서 외 **추가 개선 결정** → docs 동기화 기록 | 출처: Q&A 확정
  - R7: AI 탐지 실행 시 **기존 라벨 보존**(삭제 금지) | 출처: 사용자 보고
  - R8: AI 탐지 재실행 시 **중복 등록 방지** — 같은 클래스 + 박스 IoU 겹침(임계값) 스킵 | 출처: Q&A 확정
  - R9: 수동/오토 라벨 **관리 일원화** — 객체목록·패널 표시 / 저장·영속화 방식 / 트랙·속성·클래스 정보 | 출처: Q&A 확정
  - R10: 온라인 오토라벨 **자동 저장 제거** → 수동처럼 작업본에 반영 후 명시 저장 | 출처: 사용자 보고
  - R11: 캔버스 저장 시 **라벨 히스토리 등록** + FE에 히스토리 **리스트 노출** | 출처: 사용자 보고
  - R12: **AI 추적(트랙)에도 형태(바운딩박스/폴리곤) + 라벨 선택 팝업 적용** — 매뉴얼 "AI Tool" 디자인(형태 라디오 + 라벨 선택 + **일반/트랙** 모드) 준수. 추적 결과가 선택 형태(박스/폴리곤)로 산출 | 출처: 사용자 명시(매뉴얼 이미지)
- 수용 기준:
  - AC1: AI 탐지 모달에서 **박스/폴리곤 선택** 후 실행 시 각각 박스·폴리곤 라벨이 **작업본(캔버스)** 에 나타난다.
  - AC2: 폴리곤 선택 시 검출 박스마다 자동 분할된 폴리곤 좌표가 반환·표시된다(모델명 미노출).
  - AC3: 박스 선택 시 검출 결과가 작업본에 추가된다(기존 동작과 시각적으로 동일한 박스).
  - AC4: 클래스/라벨 필터가 박스·폴리곤 두 경로 모두 반영된다.
  - AC5: AI 탐지 실행 후에도 **기존 라벨(수동·기존 오토)이 사라지지 않는다**.
  - AC6: 같은 클래스이면서 기존 라벨과 IoU ≥ 임계값으로 겹치는 검출은 **추가되지 않는다**(중복 스킵).
  - AC7: AI 탐지만으로는 **DB에 저장되지 않는다** — 사용자가 명시 저장(저장 버튼)해야 영속화된다.
  - AC8: 오토라벨과 수동 라벨이 **동일한 저장 API(`PUT /frames/{srcSn}/labels`)** 로 저장되고, 객체목록·속성패널에서 동일하게 편집·삭제된다.
  - AC9: 라벨 저장 시 `LS_DATA_LBL_HSTRY`에 변경 이력(추가/수정/삭제 종류 + 시각 + 작업자)이 기록된다.
  - AC10: FE 라벨링 화면에서 해당 프레임/영상의 **히스토리 리스트**가 조회·표시된다.
  - AC11: 기존 회귀 없음 — 배치 파이프라인 오토라벨·포털·데이터마트 View 무영향(기존 테스트 GREEN).
  - AC12: **AI 추적 도구에서 형태(바운딩박스/폴리곤) + 라벨을 선택**한 뒤 객체를 지정하면, 선택 형태로 추적 결과가 작업본에 반영된다. 팝업은 매뉴얼 "AI Tool" 구성(형태 라디오 + 라벨 + 일반/트랙)을 따른다(모델명 미노출).
- 비기능:
  - 폴리곤 경로는 박스 N개마다 SAM 호출 → **박스 개수 상한**(sysconfig, 기본 상한 예: 30~50)·**단계 타임아웃**·**부분 실패 시 성공분만 반환** + 진행 표시. Resilience4j 정책 준수.
  - 보안: 기존 온라인 오토라벨/추적의 IDOR(`accessGuard`)·작업락(`workLockService`)·좌표검증·포털차단(ADR-013)·in-flight 락을 폴리곤·추적 경로에도 동일 적용. 히스토리 조회 API도 IDOR·인가 동일.

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | `LS_DATA_LBL_HSTRY` 확장(변경종류 코드·작업자 컬럼 추가) — 신규 Flyway V마이그레이션(LS_* 전용, MNG_* 무관) |
| 기존 API | `POST /v1/frames/{srcSn}/autolabel` — **응답 의미 변경**(persist→좌표 반환) + `shape` 파라미터. `POST /v1/frames/{srcSn}/sam2-track` — 형태(박스/폴리곤) 출력 옵션. `PUT /v1/frames/{srcSn}/labels` — 히스토리 기록 부수효과. **신규** `GET /v1/frames/{srcSn}/label-history` |
| 도메인 모듈 | `label`(autolabel/sam2track/service/controller/dto), `version`(LsDataLblHstry), ai-server(sam2/yolo 조합 — BE 오케스트레이션이라 변경 최소) |
| 설정/환경 | `LS_SYSTEM_CONFIG` 신규 키(폴리곤 박스 상한·IoU 임계값 기본값). application.yml 무변경 원칙 |
| FE | `AutolabelClassModal`/신규 AI Tool 팝업, `useAutolabel`·`useSam2Track`, `api.ts`(requestAutolabel·history·track shape), `LabelingPage`, 라벨 store 병합/dedup, 히스토리 리스트, `DarkToolbar` 추적 트리거 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | autolabel 응답 의미 변경(persist 폐지)이 기존 소비자/테스트 파괴 | HIGH | 온라인 경로 한정, 배치 경로 무접촉. 기존 테스트 의미 재정의 + 회귀 GREEN 확인 |
| 2 | FE refetch→store append 전환 시 미저장 편집 유실·중복 | HIGH | `setLabels` 덮어쓰기 제거, `addLabel` 병합 + dedup. undo/redo·dirty 정합 테스트 |
| 3 | 폴리곤 파이프라인 N회 SAM 호출 지연/부분실패 | MED | 박스 상한·타임아웃·부분성공 반환·진행 표시 |
| 4 | IoU dedup 임계값 오설정 시 정상 검출 스킵/중복 허용 | MED | sysconfig 기본값 + 단위테스트(경계 IoU) |
| 5 | 히스토리 스키마 확장이 기존 삭제이력 기록과 충돌 | MED | 하위호환 컬럼 추가(nullable/기본값), 기존 `recordDeletion` 유지 |
| 6 | 저장 시 오토 출처(source=AUTO·confidence) 유실 | MED | bulkUpsert/serializeLabel이 source·confidence 보존 확인 |
| 7 | AI 추적 형태(박스) 출력 시 폴리곤→bbox 변환·라벨 반영 누락 | MED | 트랙 결과 폴리곤의 외접 bbox 산출 + 선택 라벨 부여. 단위테스트 |

---

## 상세 계획

### Phase 1: [BE] 온라인 오토라벨 자동저장 제거 — 좌표만 반환

**목표**: 캔버스 트리거 오토라벨(`POST /frames/{srcSn}/autolabel`)이 **DB에 저장하지 않고 검출 좌표만 반환**한다(SAM2 분할과 동일 패턴). 배치 파이프라인 오토라벨은 무변경.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `label/service/AutolabelOnlineService.java` | 수정 | `persistService.persist(...)` 제거 → 좌표를 응답 DTO로 매핑 반환. IDOR·작업락·in-flight·mock 차단·좌표검증 유지 + AI호출 후 작업락 재확인 신설 |
| `label/dto/AutolabelResponse.java` | 수정 | `savedCount`→`detectedCount` 의미 전환(과도기 mirror). `labels[]` lblSn=null 허용 |
| `label/service/AutolabelPersistService.java` | 수정/폐지 | 온라인 persist 호출 제거. 미호출 시 dead-code화(삭제/주석) |
| `label/controller/AutolabelController.java` | 수정 | 응답 계약 Javadoc 갱신(저장 안 함) |

**핵심 로직 / 시그니처**:
```java
public AutolabelResponse autolabel(Long srcSn, TokenClaims actor, List<String> classes);
// IDOR → 작업락 → in-flight → AI 호출 → mock 차단 → 좌표검증 → 작업락 재확인 → (DB 저장 X) → 응답
```

**테스트 케이스** (RED):
- `온라인_오토라벨은_DB에_저장하지_않고_좌표만_반환한다`
- `오토라벨_실행해도_기존_라벨_row가_삭제되지_않는다`
- `AI호출_완료_후_작업락이_걸리면_409로_차단하고_좌표를_반환하지_않는다`
- `좌표검증_실패시_400이며_inflight_락이_해제된다`
- `mock_응답이면_빈_결과와_안내메시지를_반환한다`
- `타인_프레임_접근시_403`

**수용 기준**: AC7, AC5(서버측), AC11

> 구현: AutolabelOnlineService(persist 제거·TOCTOU 재확인 신설)·AutolabelResponse(detectedCount+savedCount mirror)·AutolabelController(Javadoc) 수정, AutolabelPersistService 삭제, 테스트 재작성+409 락해제 실증. | FE 배선(invalidateQueries 제거·res.labels 병합·lblSn 타입)은 Phase 4로 이월(동일 PR). 문서 동기화는 Phase 6.

**의존성**: 없음 (첫 Phase)

---

### Phase 2: [BE] 라벨 저장 경로 히스토리 등록 + 히스토리 조회 API

**목표**: 수동/오토 공통 저장 경로(`LabelService.bulkUpsert`)에서 **변경 이력을 `LS_DATA_LBL_HSTRY`에 기록**하고, 프레임 단위 **히스토리 조회 API**를 제공한다.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `db/migration/V{next}__extend_ls_data_lbl_hstry.sql` | 생성 | `CHG_KIND_CD`(ADDED/UPDATED/DELETED), `REG_ID`(작업자) 컬럼 추가(nullable/기본값) |
| `version/entity/LsDataLblHstry.java` | 수정 | 변경종류·작업자 필드 + `recordChange(...)` 팩토리. 기존 `recordDeletion` 유지 |
| `version/repository/LsDataLblHstryRepository.java` | 수정 | 프레임 단위 최신순 페이징 조회 |
| `label/service/LabelService.java` | 수정 | `bulkUpsert`에서 ADDED/UPDATED/DELETED 히스토리 saveAll(동일 트랜잭션) |
| `label/controller/LabelController.java` | 수정 | `GET /v1/frames/{srcSn}/label-history` — IDOR·인가·페이징 |
| `label/dto/LabelHistoryResponse.java` | 생성 | `{regDt, actor, changeKind, lblSn, label?}` record |

**테스트 케이스** (RED):
- `라벨_신규저장시_ADDED_히스토리가_기록된다`
- `라벨_수정저장시_UPDATED_히스토리가_기록된다`
- `히스토리_조회는_최신순_페이징으로_반환한다`
- `타인_프레임_히스토리_조회시_403`
- `비식별신고_삭제이력(recordDeletion)_기록은_회귀없이_유지된다`

**수용 기준**: AC9, AC10(BE), AC8(공통 저장경로)

> 구현: V112 마이그레이션(CHG_KIND_CD/REG_ID nullable+backfill)·LabelChangeKind·LsDataLblHstry(recordChange, recordDeletion 위임)·Repository(findBySrcSn Page)·LabelHistoryResponse·LabelService(bulkUpsert 히스토리 saveAll+getHistory)·LabelController(GET label-history). DEV_FIX: TrackEditService 삭제 시 DELETED 히스토리 원자 기록+end-to-end·sort 서버고정·dead code 제거. | design-verifier DESIGN-STALE(D8 EN-040·D1 DC-093·D9 테이블)=Phase 6 문서동기화.

**의존성**: 없음

---

### Phase 3: [BE+ai-server] 폴리곤 파이프라인(탐지→박스별 자동분할) + shape 파라미터 + 추적 형태 출력

**목표**: `POST /frames/{srcSn}/autolabel`에 `shape`(BBOX 기본|POLYGON) 추가(POLYGON=YOLO 박스→박스별 SAM 분할→폴리곤, 저장 안 함). **AI 추적(`POST /frames/{srcSn}/sam2-track`)에도 형태(박스/폴리곤) 출력 옵션 추가**(R12) — 폴리곤이 기본, 박스 선택 시 폴리곤 외접 bbox 산출. ai-server는 기존 엔드포인트 재사용(BE 오케스트레이션).

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `label/dto/AutolabelRequest.java` | 수정 | `shape`(BBOX/POLYGON, 기본 BBOX) + 검증 |
| `label/service/AutolabelOnlineService.java` | 수정 | shape 분기: POLYGON=YOLO→박스별 `aiServerClient.segment(box)`→폴리곤. 박스 상한·타임아웃·부분성공 |
| `label/dto/Sam2TrackRequest.java` / `Sam2TrackResponse.java` | 수정 | 추적 요청에 `shape`(박스/폴리곤) + `label` 반영, 응답 형태 분기 |
| `label/service/Sam2TrackService`(해당 서비스) | 수정 | 추적 결과를 선택 형태(폴리곤 or 외접 bbox)로 매핑 + 선택 라벨 부여. 저장 안 함(좌표 반환) |
| `common/config` sysconfig 키 | 추가 | `autolabel.polygon.max-boxes`, 세그먼트 타임아웃 |

**테스트 케이스** (RED):
- `shape_미지정이면_기존과_동일하게_박스를_반환한다`(하위호환)
- `shape_POLYGON이면_검출_박스마다_폴리곤을_반환한다`
- `클래스_필터가_폴리곤_경로에도_반영된다`
- `박스_개수가_상한을_초과하면_상한까지만_분할한다`
- `일부_박스_분할_실패시_성공분만_반환한다`
- `추적_shape가_박스면_폴리곤_외접_bbox로_반환한다`(R12)
- `추적_결과에_선택_라벨이_부여된다`(R12)

**수용 기준**: AC1(BE), AC2, AC3, AC4, AC7, AC12(BE)

> 구현: AutolabelShape enum·AutolabelRequest/Response(shape/shapeType/polygon)·Sam2TrackRequest/ResponseDto(shape)·AutolabelOnlineService(폴리곤 분기: 박스상한 sysconfig V113·wall-clock 예산·부분실패 성공분만·mock스킵·TOCTOU)·Sam2TrackService(미저장 draft 전환+외접bbox)·ConfigKeys. ai-server 무변경(기존 box-prompt/track 재사용). DEV_FIX: 429 fail-fast 복구·부분실패 message·Sam2Track 오류경로 테스트·격리 flake(TrackEditServiceIT findOrCreate)·PortalSam2 javadoc. | api-designer HIGH(FE 미배선)=Phase 4. design-verifier DESIGN-STALE(D1 SD-004/DC-075·D3 CO-004·D11 TC-014-14)=Phase 6.

**의존성**: Phase 1

---

### Phase 4: [FE] 오토라벨 결과 작업본 병합(IoU 중복방지·기존보존) + AI Tool 팝업(형태·라벨·일반/트랙)

**목표**: 오토라벨/추적 결과를 **작업본 store에 병합**(기존 보존 + IoU dedup). 매뉴얼 "AI Tool" 팝업(형태 바운딩박스/폴리곤 + 라벨 + **일반/트랙**)을 AI 탐지·추적에 적용(모델명 미노출). 저장은 기존 `PUT /labels`로 사용자 명시.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `features/label/components/AutolabelClassModal.tsx`(또는 신규 AiToolPopup) | 수정/생성 | 형태(박스/폴리곤 라디오) + 라벨 선택 + **일반/트랙** 버튼(매뉴얼 AI Tool 구성). `onConfirm(shape, label, mode)` |
| `features/label/hooks/useAutolabel.ts` | 수정 | `autolabel(classIds, shape)` |
| `features/label/hooks/useSam2Track.ts` | 수정 | 추적에 shape·label 전달(R12) |
| `features/label/api.ts` | 수정 | `requestAutolabel(shape)`, `requestSam2Track(shape,label)` 응답 정규화 |
| `pages/label/LabelingPage.tsx` | 수정 | `runAutolabel`/추적: `invalidateQueries` 제거 → store dedup 병합. 일반/트랙 모드 분기 |
| `features/label/utils/labelDedup.ts` | 생성 | 같은 클래스 + IoU ≥ 임계값 스킵 병합 유틸 |
| `features/label/components/DarkToolbar.tsx` | 수정 | AI 추적 트리거가 AI Tool 팝업 열도록 배선 |

**테스트 케이스** (RED):
- `오토라벨_결과가_기존_라벨을_유지한채_작업본에_추가된다`
- `같은_클래스이고_IoU가_임계값이상이면_추가되지_않는다`
- `폴리곤_선택시_폴리곤_라벨로_추가된다`
- `AI_Tool_팝업에서_트랙_모드_선택시_추적이_선택형태로_반영된다`(R12)
- `모달에_모델명(YOLO/SAM)이_노출되지_않는다`
- `오토라벨_직후_자동저장_호출이_없다`

**수용 기준**: AC1, AC3, AC4, AC5, AC6, AC7(FE), AC12(FE)

> 구현: labelDedup.ts(IoU 클래스인지)·AiToolModal(형태/라벨/일반·트랙, 모델명 미노출, AutolabelClassModal 대체)·useLabelStore(mergeAutoLabels 클라id·1회undo·dirty, pendingTracks stash/drain)·api.ts(shape/polygon/converters)·useAutolabel/useSam2Track(stale·부분실패)·useLabels(refetch dirty가드)·LabelingPage(runAiTool 병합·no PUT, handleTracked 현재/미래 분리·진입 drain). DEV_FIX: R12 추적 사일런트 실패(보류캐시)+shape 전 구간 배선. fullstack-coordinator 계약 PASS. | 브라우저 검증=Step4 cc-verify. design-verifier DESIGN-STALE(D2 SC-005)=Phase 6.

**의존성**: Phase 1, Phase 3

---

### Phase 5: [FE] 라벨 관리 일원화 마감 + 히스토리 리스트 UI

**목표**: 오토라벨/추적 라벨과 수동 라벨을 객체목록·속성패널·저장에서 **동일 관리**(편집·삭제·저장). 라벨 **히스토리 리스트** 컴포넌트 추가.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `features/label/api.ts` | 수정 | `getLabelHistory(srcSn, page)` |
| `features/label/hooks/useLabelHistory.ts` | 생성 | 히스토리 조회 훅 |
| `features/label/components/LabelHistoryPanel.tsx` | 생성 | 변경종류·시각·작업자 리스트(버전 패널과 구분) |
| `pages/label/LabelingPage.tsx` | 수정 | 히스토리 패널 배치 + 저장 성공 시 invalidate |
| `features/label/components/ObjectAttributePanel.tsx` / `ObjectClassTree.tsx` | 확인/수정 | 오토 라벨도 수동과 동일 편집·삭제 + 저장 시 source·confidence 보존 |

**테스트 케이스** (RED):
- `히스토리_리스트가_변경종류_시각_작업자를_표시한다`
- `저장_성공시_히스토리가_갱신된다`
- `오토_라벨도_수동과_동일하게_삭제·편집된다`
- `저장시_오토_출처와_신뢰도가_보존된다`

**수용 기준**: AC8, AC9(FE 연동), AC10

> 구현: getLabelHistory·useLabelHistory·LabelHistoryPanel(변경종류·시각·작업자, 버전패널과 구분)·저장 시 invalidate·오토/수동 편집 일원화. DEV_FIX(BE+FE): 온라인 오토라벨 저장 시 AUTO 출처·신뢰도 보존 — LabelItemDto provenance(source/confScore/algorithm)+bulkUpsert createAuto*+AI_INFO, serializeLabel 전송. security-reviewer Mass Assignment 차단 PASS(AUTO_LBL_YN 서버파생·검증·내부채널). requirements-verifier R9-②③ CONFORM. | design-verifier DESIGN-STALE(D2 SC-005·D8 EN-040)=Phase 6. 참고: V_COMPLETED_LABEL 뷰 AI_INFO 미조인(향후 뷰 확장 별도).

**의존성**: Phase 2, Phase 4

---

### Phase 6: 문서 동기화 + 전체 통합 검증

**목표**: R6 문서화 + 전체 요구 역추적·통합 검증.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `docs/v2-wiki/04-screens-ia.md` | 수정 | AI 탐지/추적 형태·라벨 선택(AI Tool 팝업)·저장모델·히스토리 반영 |
| `docs/v2-wiki/18-database.md` | 수정 | `LS_DATA_LBL_HSTRY` 확장 반영 |
| `docs/design/D8-엔티티관계모형설계서*` (EN-040) · `D1-클래스설계서*` (DC-093/DC-071) · `D9-데이터베이스설계서*` | 수정 | LS_DATA_LBL_HSTRY 컬럼(CHG_KIND_CD/REG_ID)·용도(삭제전용→변경이력)·recordChange·LabelService→LsDataLblHstryRepository 의존 반영 (design-verifier DESIGN-STALE) |
| `docs/v1-wiki/19-v2-gap-checklist.md` | 수정 | AI 탐지 폴리곤·AI Tool 팝업 = "요구사항 외 추가 결정"(R6) 명시 |

**작업**: `"Phase 6 진행해줘"`(문서) → requirements-verifier 전체 + `/cc-verify` + `/cc-deploy-check`.

**수용 기준**: AC11 + R6 문서 반영 + 전체 R1~R12 역추적 CONFORM.

**의존성**: Phase 1~5 완료.

---

## 실행 가이드

1. `/cc-tdd Phase 1: 온라인 오토라벨 자동저장 제거(좌표 반환)` → `/cc-tdd Phase 2: 라벨 히스토리 등록 + 조회 API`
2. `/cc-tdd Phase 3: 폴리곤 파이프라인 + shape + 추적 형태 출력`
3. `/cc-tdd Phase 4: FE 오토라벨 병합 + AI Tool 팝업(형태·라벨·일반/트랙)`
4. `/cc-tdd Phase 5: FE 라벨 관리 일원화 + 히스토리 리스트 UI`
5. `"Phase 6 진행해줘"` — 문서 동기화 → 전체 검증(`/cc-verify`)
