# 구현 계획: 관제서버 통지 시스템 + 설계 변경 반영

> 생성: 2026-05-26 17:00
> 상태: 진행 중
> 근거: docs/설계_vs_구현_갭분석_2026-05-26.md

## Phase 진행 현황

- [x] Phase 1: 설계 변경 코드 반영 — Quartz 단일화 + WebSocket 제거 + Export 정리
> 구현: application-{dev,stg,prd}.yml 클러스터 제거, build.gradle websocket 제거, export/ 삭제 | 빌드 860/860 PASS
- [x] Phase 2: 통지 인프라 기반 — Flyway V44 + Entity + Event + ControlNotifyClient
> 구현: V44 DDL + LsControlNotifyFallback Entity(상태전이+sanitizeError) + Repository(claimAtomically CAS) + ControlNotifyClient(Resilience4j CB/Retry) + DTO record 2종 + Event record 2종 | 신규 28테스트, 888/888 PASS
- [ ] Phase 3: 통지 비즈니스 로직 — 디바운서 + 폴백큐 + 이벤트 리스너 + 기존 서비스 수정
- [ ] Phase 4: 관제서버 조회 API — TaskQueryController + 조회 3종 (기존 JWT 인증)
- [ ] Phase 5: Observability — HealthIndicator + Micrometer 메트릭

## 요구사항 요약

- 목적: 검수 완료/라벨 수정 시 관제서버에 비동기 통지 + 관제서버가 상세 데이터를 조회 API로 취득
- 범위:
  - 포함: ControlNotifyClient, 디바운서(60s), 폴백큐(DB 영속), 이벤트 리스너, 조회 API 3종, 설계 변경 반영
  - 제외: 관제서버 수신측 구현, 양방향 M2M 인증, Export 기능
- 비기능: 멱등성(requestId), 재시도(Resilience4j CB+Retry), 폴백(DB큐, max 5회→DEAD), 디바운스(60s), PII 미포함

## 핵심 설계 결정

| 결정 | 선택 | 근거 |
|------|------|------|
| 패키지 | `controlnotify` (신규) | 기존 `notification`은 내부 로그용. 혼동 방지 |
| 디바운서 | `@Scheduled(fixedDelay=10000)` | 단일 인스턴스. Gitea fallback과 일관 |
| 폴백 재시도 | `@Scheduled(fixedDelay=300000)` | `isClustered: false`이므로 Quartz 불필요 |
| 이벤트 발행 | `@TransactionalEventListener(AFTER_COMMIT)` | 롤백 시 통지 미발행 보장 |
| 조건 활성화 | `@ConditionalOnProperty("authoring.control-notify.enabled")` | local/dev Bean 미등록 |
| 조회 API 인증 | 기존 JWT (동일 도메인 스토리지 공유) | 별도 인증 불필요 |
| 참조 패턴 | `version/fallback/` 5파일 구조 복제 | Entity/Repo/Service/RetryJob/Config |

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | V44: `LS_CONTROL_NOTIFY_FALLBACK` 테이블 신규 |
| 기존 서비스 | ReviewService.approve(), LabelService, MetaService에 이벤트 발행 1줄 추가 |
| common/client | ControlNotifyClient 신규 |
| common/config | WebClientConfig, Resilience4jConfig에 Bean 추가 |
| application.yml | `authoring.control-notify.*` 설정 블록 추가 |
| build.gradle | `spring-boot-starter-websocket` 제거 |
| export/ | 패키지 삭제 (범위 외) |
| 설정 3파일 | Quartz `isClustered: 'false'` |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|:----:|------|
| 1 | 트랜잭션 롤백 시 통지 전송 | HIGH | `@TransactionalEventListener(AFTER_COMMIT)` |
| 2 | 디바운서 셧다운 시 윈도우 누락 | HIGH | `@PreDestroy` flush + graceful shutdown |
| 3 | 폴백 큐 무한 증가 | MED | max retry 5회 → DEAD_LETTER 전환 |
| 4 | ReviewService 이벤트 추가로 기존 테스트 영향 | LOW | `@MockBean ApplicationEventPublisher` |

---

## 상세 계획

### Phase 1: 설계 변경 코드 반영

**목표**: 금일 확정된 설계 변경을 코드에 반영. 빌드 성공 확인.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/src/main/resources/application-dev.yml` | 수정 | `isClustered: 'false'`, `clusterCheckinInterval` 제거 |
| `backend/src/main/resources/application-stg.yml` | 수정 | 동일 |
| `backend/src/main/resources/application-prd.yml` | 수정 | 동일 |
| `backend/build.gradle` | 수정 | `spring-boot-starter-websocket` 의존성 제거 |
| `backend/src/.../export/entity/LsDataSet.java` | 삭제 | 범위 외 |
| `backend/src/.../export/repository/LsDataSetRepository.java` | 삭제 | 범위 외 |

**테스트 케이스**:
- `Quartz_단일모드에서_Job_정상_실행`
- `WebSocket_의존성_제거후_빌드_성공`
- `Export_Entity_제거후_컴파일_성공`

**수용 기준**:
- [ ] 3개 profile yml에서 `isClustered: 'false'`
- [ ] `build.gradle`에 websocket 의존성 없음
- [ ] export 패키지 삭제 후 빌드 + 기존 테스트 전체 PASS

**의존성**: 없음

---

### Phase 2: 통지 인프라 기반

**목표**: ControlNotify 도메인의 DB 테이블 + Entity + DTO + Client + Event 클래스 생성. Client가 관제서버에 HTTP 통지를 전송할 수 있는 상태.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `backend/src/main/resources/db/migration/V44__create_ls_control_notify_fallback.sql` | 생성 | 폴백 큐 테이블 DDL |
| `backend/src/.../controlnotify/fallback/LsControlNotifyFallback.java` | 생성 | Entity — 정적 팩토리 `pending()`, 상태 전이 `markRetrying()/markDead()` |
| `backend/src/.../controlnotify/fallback/LsControlNotifyFallbackRepository.java` | 생성 | `@ControlRepo` JpaRepository |
| `backend/src/.../controlnotify/dto/TaskCompletedPayload.java` | 생성 | record (rawSn, reviewerName, approvedAt, totalFrames, labeledFrames, requestId) |
| `backend/src/.../controlnotify/dto/TaskModifiedPayload.java` | 생성 | record (rawSn, lastModifiedAt, frameIds[], changeTypes[], requestId) |
| `backend/src/.../controlnotify/event/ReviewApprovedEvent.java` | 생성 | record (rawSn, reviewerNo, approvedAt) |
| `backend/src/.../controlnotify/event/TaskModifiedEvent.java` | 생성 | record (rawSn, srcSn, changeType, modifierNo) |
| `backend/src/.../common/client/ControlNotifyClient.java` | 생성 | WebClient + Resilience4j. `@ConditionalOnProperty` |
| `backend/src/.../common/client/dto/ControlNotifyRequest.java` | 생성 | 통지 요청 DTO |
| `backend/src/main/resources/application.yml` | 수정 | `authoring.control-notify.*` 설정 추가 |
| `backend/src/.../common/config/WebClientConfig.java` | 수정 | `controlNotifyWebClient` Bean 추가 |
| `backend/src/.../common/config/Resilience4jConfig.java` | 수정 | `controlNotify` CB/Retry 인스턴스 추가 |

**핵심 로직 / 시그니처**:
```java
// ControlNotifyClient — GiteaClient 패턴 준용
@Component
@ConditionalOnProperty(name = "authoring.control-notify.enabled", havingValue = "true")
public class ControlNotifyClient {
    public Mono<Void> sendTaskCompleted(TaskCompletedPayload payload);
    public Mono<Void> sendTaskModified(TaskModifiedPayload payload);
}

// V44 DDL
CREATE TABLE LS_CONTROL_NOTIFY_FALLBACK (
    QUEUE_SN BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    IDEMPOTENCY_KEY VARCHAR(64) NOT NULL UNIQUE,
    EVENT_TYPE VARCHAR(32) NOT NULL,
    RAW_SN BIGINT NOT NULL,
    PAYLOAD JSON NOT NULL,
    RETRY_COUNT INT NOT NULL DEFAULT 0,
    MAX_RETRY INT NOT NULL DEFAULT 5,
    STATUS VARCHAR(16) NOT NULL DEFAULT 'PENDING',
    LAST_ERROR VARCHAR(2000),
    NEXT_RETRY_AT DATETIME,
    CREATED_AT DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UPDATED_AT DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX IDX_LCNF_STATUS (STATUS, NEXT_RETRY_AT)
);
```

**참조 패턴 파일**:
- `version/fallback/LsGiteaFallbackQueue.java` — Entity 구조
- `common/client/GiteaClient.java` — WebClient + Resilience4j

**테스트 케이스**:
- `TASK_COMPLETED_통지_정상_송신시_200`
- `관제서버_타임아웃시_CircuitBreaker_동작`
- `관제서버_500_응답시_Retry_3회_후_실패`
- `Flyway_V44_마이그레이션_정상_실행`
- `LsControlNotifyFallback_Entity_상태전이_검증`
- `TaskCompletedPayload에_PII_미포함`

**수용 기준**:
- [ ] ControlNotifyClient가 POST 요청 전송 (MockWebServer 검증)
- [ ] Resilience4j CB(50%)+Retry(3회) 적용
- [ ] 페이로드에 PII/토큰/원본 경로 미포함
- [ ] V44 마이그레이션 정상 실행
- [ ] enabled=false 시 Bean 미등록

**의존성**: Phase 1 완료

---

### Phase 3: 통지 비즈니스 로직

**목표**: 디바운서(60s 윈도우), 폴백 큐(실패 복원), 이벤트 리스너 연결. 검수 완료→TASK_COMPLETED, 수정→TASK_MODIFIED 전체 흐름 동작.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `controlnotify/service/ControlNotifyService.java` | 생성 | 오케스트레이션: 이벤트→Client→실패 시 폴백 |
| `controlnotify/service/ControlNotifyDebouncer.java` | 생성 | ConcurrentMap 60s 윈도우. `@Scheduled(fixedDelay=10000)` |
| `controlnotify/fallback/ControlNotifyFallbackService.java` | 생성 | 폴백 큐 CRUD + 상태 관리 |
| `controlnotify/fallback/ControlNotifyFallbackRetryJob.java` | 생성 | `@Scheduled(fixedDelay=300000)` 재시도 |
| `controlnotify/fallback/ControlNotifySchedulingConfig.java` | 생성 | `@ConditionalOnProperty` + `@EnableScheduling` |
| `controlnotify/listener/ControlNotifyEventListener.java` | 생성 | `@TransactionalEventListener(AFTER_COMMIT)` 2핸들러 |
| `review/service/ReviewService.java` | 수정 | approve()에 `publishEvent(ReviewApprovedEvent)` |
| `label/service/LabelService.java` | 수정 | upsert/delete에 `publishEvent(TaskModifiedEvent)` |
| `meta/service/MetaService.java` | 수정 | update에 `publishEvent(TaskModifiedEvent)` |

**핵심 로직 / 시그니처**:
```java
// ControlNotifyDebouncer
@Scheduled(fixedDelay = 10_000)
public void flushExpiredWindows(); // 60초 경과 윈도우 drain → Service.sendModified()

@PreDestroy
public void flushAll(); // 셧다운 시 잔여 flush

// ControlNotifyEventListener
@TransactionalEventListener(phase = AFTER_COMMIT)
public void onReviewApproved(ReviewApprovedEvent event);
// → ControlNotifyService.sendCompleted() (즉시)

@TransactionalEventListener(phase = AFTER_COMMIT)
public void onTaskModified(TaskModifiedEvent event);
// → ControlNotifyDebouncer.accumulate() (디바운스)

// ReviewService.approve() 수정
eventPublisher.publishEvent(new ReviewApprovedEvent(rawSn, reviewerNo, Instant.now()));
```

**참조 패턴 파일**:
- `version/fallback/GiteaFallbackQueueService.java` — FallbackService 구조
- `version/fallback/GiteaFallbackRetryJob.java` — RetryJob 구조

**테스트 케이스**:
- `검수_승인시_TASK_COMPLETED_이벤트_발행`
- `라벨_수정시_TASK_MODIFIED_이벤트_발행`
- `메타_수정시_TASK_MODIFIED_이벤트_발행`
- `디바운서_60초_윈도우_내_3회_수정_1회_통지`
- `디바운서_다른_영상은_별도_윈도우`
- `통지_실패시_폴백큐_INSERT`
- `폴백_재시도_성공시_삭제`
- `폴백_5회_초과시_DEAD_전환`
- `트랜잭션_롤백시_이벤트_미발행`
- `셧다운시_잔여_윈도우_flush`

**수용 기준**:
- [ ] approve() → TASK_COMPLETED 통지 전송
- [ ] 라벨/메타 수정 → 60초 디바운스 후 TASK_MODIFIED 통지
- [ ] 통지 실패 → 폴백 저장 → 5분 후 재시도 → 성공 시 삭제
- [ ] 5회 실패 → DEAD 전환
- [ ] AFTER_COMMIT — 롤백 시 통지 미발행
- [ ] enabled=false 시 전체 무동작

**의존성**: Phase 2 완료

---

### Phase 4: 관제서버 조회 API

**목표**: 관제서버가 통지 수신 후 상세 데이터를 조회할 수 있는 API 3종. 기존 JWT 인증 재사용.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `controlnotify/controller/TaskQueryController.java` | 생성 | GET 3종 |
| `controlnotify/service/TaskQueryService.java` | 생성 | 조회 로직 (기존 Repository 재사용) |
| `controlnotify/dto/TaskSummaryResponse.java` | 생성 | record |
| `controlnotify/dto/TaskLabelsResponse.java` | 생성 | record |
| `controlnotify/dto/TaskMetaResponse.java` | 생성 | record |

**핵심 로직 / 시그니처**:
```java
@RestController
@RequestMapping("/api/v1/tasks")
@ConditionalOnProperty(name = "authoring.control-notify.enabled", havingValue = "true")
public class TaskQueryController {
    @GetMapping("/{rawSn}/summary")
    public ApiResponse<TaskSummaryResponse> getSummary(@PathVariable Long rawSn);

    @GetMapping("/{rawSn}/labels")
    public ApiResponse<List<TaskLabelsResponse>> getLabels(
        @PathVariable Long rawSn,
        @RequestParam(required = false) List<Long> frameIds);

    @GetMapping("/{rawSn}/meta")
    public ApiResponse<TaskMetaResponse> getMeta(@PathVariable Long rawSn);
}
```

**테스트 케이스**:
- `영상별_요약_조회_정상_200`
- `영상별_라벨_목록_조회_비식별_좌표만_포함`
- `특정_프레임_라벨_필터_조회`
- `영상별_메타데이터_조회`
- `미존재_영상_조회시_404`
- `미인증_조회시_401`
- `응답에_원본_이미지_경로_미포함`

**수용 기준**:
- [ ] 3개 조회 API 정상 응답
- [ ] PII/원본 경로/토큰 미포함
- [ ] 미존재 rawSn → 404
- [ ] 미인증 → 401
- [ ] frameIds 필터 동작

**의존성**: Phase 2 완료 (Phase 3과 병렬 가능)

---

### Phase 5: Observability 보강

**목표**: ControlNotify 운영 모니터링 — 헬스체크 + 메트릭.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `observability/health/ControlNotifyHealthIndicator.java` | 생성 | 관제서버 연결 헬스 |
| `observability/metrics/ControlNotifyMetrics.java` | 생성 | 카운터 5종 + 게이지 1종 |
| `controlnotify/service/ControlNotifyService.java` | 수정 | 메트릭 호출 추가 |

**참조 패턴**: `observability/health/GiteaHealthIndicator.java`, `observability/metrics/ExternalApiMetrics.java`

**테스트 케이스**:
- `통지_성공시_success_카운터_증가`
- `통지_실패시_failed_카운터_증가`
- `폴백큐_깊이_게이지_정확성`
- `헬스인디케이터_정상시_UP`
- `헬스인디케이터_장애시_DOWN`

**수용 기준**:
- [ ] `/actuator/prometheus`에 `control.notify.*` 메트릭 노출
- [ ] `/actuator/health`에 controlNotify 인디케이터 포함

**의존성**: Phase 3 완료
