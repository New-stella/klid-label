# 구현 계획: '영상 처리 현황' 마킹 진입 개편 (배치 버튼 → 마킹 팝업)

> 생성: 2026-07-16
> 상태: ✅ 완료 (전체 Phase 완료 2026-07-16)

## Phase 진행 현황

- [x] Phase 1: MarkingModal 컴포넌트 (자동 intervalFrames 폼 + 수동 배치 통합) ✅ 완료 (2026-07-16, 테스트 10/10 · 적대적검증 ROBUST)
- [x] Phase 2: VideoListPage 배선 (배치→마킹 버튼 교체 · 상태기반 노출 · REVIEWER 게이트) ✅ 완료 (2026-07-16, 신규 17/17 · 회귀 131/131 · 적대적검증 ROBUST · 데드코드 정리)
  - 결정: 재배정 버튼 유지(무회귀) — 미배정+canMark만 "마킹" 버튼으로 대체

## 요구사항 요약

- 목적: '영상 처리 현황'에서 마킹 작업을 시작하는 진입점을 명확화 — 기존 작업자 배정 버튼을 "마킹"으로 통합하고, 자동(프레임 간격)/수동(작업자 배치)을 한 팝업에서 선택
- 범위:
  - 포함: `VideoListPage`(영상 처리 현황)의 마킹 진입 버튼·상태기반 노출·자동/수동 선택 팝업·자동 트리거·수동 배치 통합 (**FE-only**)
  - 제외(out of scope): 마킹 화면 자체(`/marking/:rawSn` 비식별 스트리밍·키보드 단축키 마킹), BE 자동마킹/배정 API(이미 존재 — 신규 없음), 마킹 완료 전용 플래그 컬럼 신설
- 주요 액터: REVIEWER(검수자) — 마킹 버튼·팝업 접근
- 요구 목록:
  - R1: 기존 '배치/재배치'(작업자 배정) 버튼을 "마킹" 버튼으로 변경 | 출처: 사용자 명시
  - R2: 해당 영상 마킹 완료 상태면 "마킹" 버튼 비노출 | 출처: 사용자 명시
  - R3: "마킹" 클릭 → 자동/수동 선택 팝업 | 출처: 사용자 명시
  - R4: 자동 선택 → 프레임 간격(intervalFrames) 입력 → 즉시 서버 자동마킹 트리거(작업자 배치 없이). 기존 `createMarking(mode='AUTO')` 재사용 | 출처: 사용자 명시 + 탐색
  - R5: 수동 선택 → 기존 배치/재배치(작업자 선택) UI(`AssignModal`)를 팝업 흐름으로 통합 | 출처: 사용자 명시 + 탐색
  - R6: 마킹 버튼·팝업은 REVIEWER 게이트 | 출처: 사용자 명시
- 수용 기준:
  - AC1: `dataSttsCd == 'MARKING_READY'`(+ 비식별 완료) 영상에만 "마킹" 버튼 노출, `PROCESSING/COMPLETED/FAILED/PENDING`은 미노출
  - AC2: "마킹" 클릭 시 자동/수동 선택 팝업 표시
  - AC3: 자동 + 유효 intervalFrames 입력 → `createMarking(rawSn,{mode:'AUTO',intervalFrames})` 호출, 성공 시 목록 쿼리 무효화 + 성공 피드백, 실패 시 에러 피드백
  - AC4: intervalFrames 미입력/0/음수/비정수 → 제출 차단 + 검증 메시지 (BE @Min 이전 FE 1차 검증)
  - AC5: 수동 선택 → 기존 배정(`AssignModal` assign/reassign)이 팝업 흐름에서 동작(작업자 선택·배정 성공)
  - AC6: 비REVIEWER는 마킹 버튼 미노출(기존 `isReviewer` 게이트 유지)
- 비기능 요구사항: 기존 좌표/줌/라벨 기능 무회귀. any 금지·strict TS·불변성. 접근성(모달 포커스·ESC·라벨).

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | 없음 |
| 기존 API | 없음 (자동마킹 `POST /v1/videos/{rawSn}/markings`·배정 `/v1/assignments` 그대로 재사용) |
| BE 도메인 모듈 | 없음 |
| FE 모듈 | `pages/VideoListPage.tsx`(버튼 교체·노출로직·팝업 오픈), 신규 `features/marking/components/MarkingModal.tsx`, 재사용 `features/task/components/AssignModal.tsx`·`features/marking/api.ts(createMarking)` |
| 설정/환경 | 없음 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | "마킹 완료" 전용 플래그 부재 — `dataSttsCd` 전이로 간접 판정 | MED | MARKING_READY=노출, 그 외=숨김. 목록 응답에 이미 `dataSttsCd` 있음(BE 변경 불필요). 판정 규칙을 순수 헬퍼로 단일화 |
| 2 | 자동마킹 API 권한이 REVIEWER+WORKER (목표는 REVIEWER) | LOW | REVIEWER는 하위집합이라 통과. WORKER의 키보드 마킹 경로가 같은 API를 쓰므로 BE 축소 금지 → **FE 버튼 게이트(isReviewer)로만 제한** |
| 3 | 수동=AssignModal 통합 방식(중첩 모달 vs 흐름 전환) | MED | MarkingModal에서 수동 선택 시 AssignModal로 흐름 전환(모달 교체)으로 단순화. 중첩 모달 지양 |
| 4 | 자동마킹은 MARKING_READY에서 1회만 허용(BE 가드) | LOW | 노출 조건과 BE 가드 일치(MARKING_READY) → 혼선 없음. 실패 응답 메시지 노출 |

---

## 상세 계획

### Phase 1: MarkingModal 컴포넌트 (자동 폼 + 수동 배치 통합)

**목표**: '마킹' 진입 팝업 — 자동/수동 선택 → 자동은 intervalFrames 입력·트리거, 수동은 작업자 배치로 연결. 재사용 가능한 독립 컴포넌트로 단위 테스트까지.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/features/marking/components/MarkingModal.tsx` | 생성 | 자동/수동 선택 + 자동 intervalFrames 폼 + 수동→AssignModal 흐름 |
| `frontend/src/features/marking/components/__tests__/MarkingModal.test.tsx` | 생성 | 단위 테스트 |
| `frontend/src/features/marking/hooks/useCreateMarking.ts` | 생성(없으면) | React Query mutation 래퍼(onSuccess 목록 무효화) |
| `frontend/src/features/marking/api.ts` | 참조 | 기존 `createMarking(rawSn,{mode,intervalFrames})` 재사용 |
| `frontend/src/features/task/components/AssignModal.tsx` | 참조 | 수동 배치 재사용 |

**핵심 로직 / 시그니처**:
```tsx
interface MarkingModalProps {
  rawSn: number;
  videoName?: string;
  open: boolean;
  onClose: () => void;
  onMarked?: () => void;   // 자동마킹 트리거 성공 콜백(목록 무효화 등)
}
// 단계: 'select'(자동/수동) → 'auto'(intervalFrames 폼) 제출 | 'manual' → AssignModal(assign) 흐름
// 자동 제출: useCreateMarking().mutate({ rawSn, mode: 'AUTO', intervalFrames })
```

**폼 필드 (자동)**:
| 이름 | 타입 | 검증 | 설명 |
|------|------|------|------|
| intervalFrames | number | 정수 ≥ 1 (FE 1차) | 자동 마킹 프레임 간격 |

**테스트 케이스** (cc-tdd RED):
- `자동_선택시_프레임간격_입력폼이_표시된다`
- `프레임간격_미입력_또는_0이하면_제출_차단_또는_검증메시지`
- `유효한_프레임간격_제출시_createMarking을_AUTO모드로_호출한다`
- `자동마킹_성공시_onMarked_콜백과_닫기가_호출된다`
- `자동마킹_실패시_에러메시지를_표시한다`
- `수동_선택시_작업자_배치(AssignModal)_흐름으로_전환된다`
- `ESC_또는_닫기로_모달이_닫힌다`

**수용 기준**:
- [ ] AC2 팝업 자동/수동 선택 렌더
- [ ] AC3 자동 유효 입력 → createMarking(AUTO) 호출·성공 피드백
- [ ] AC4 intervalFrames 검증(≥1 정수)
- [ ] AC5 수동 → AssignModal 배치 흐름

**의존성**: 없음 (기존 api/AssignModal 재사용)

### Phase 2: VideoListPage 배선 (배치→마킹 버튼 · 상태기반 노출 · REVIEWER 게이트)

**목표**: '영상 처리 현황' 행 액션의 배정 버튼을 "마킹" 버튼으로 교체하고, `dataSttsCd` 기반 노출/숨김, 클릭 시 MarkingModal 오픈. 기존 배정 인라인 버튼은 마킹 팝업(수동)으로 통합.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/src/pages/VideoListPage.tsx` | 수정 | 배정/재배정 버튼(105-138, 336-368) → "마킹" 버튼 + 노출조건 + MarkingModal 오픈 상태 |
| `frontend/src/features/marking/markingEligibility.ts` | 생성 | 순수 헬퍼 `canMark(video)`·`isMarkingDone(video)` (상태 판정 단일 출처) |
| `frontend/src/features/marking/__tests__/markingEligibility.test.ts` | 생성 | 상태별 판정 단위 테스트 |
| `frontend/src/pages/__tests__/VideoListPage.test.tsx` | 수정/생성 | 노출/숨김·버튼 라벨·모달 오픈 테스트 |
| `frontend/src/features/video/types.ts` | 참조 | `Video.status(=dataSttsCd)`·`deidentStatus`·`deIdntfYn` |

**핵심 로직 / 시그니처**:
```ts
// markingEligibility.ts (순수 함수)
export function canMark(v: { status?: string; deIdntfYn?: string; deidentStatus?: string }): boolean;
// true: dataSttsCd === 'MARKING_READY' && 비식별 완료(deIdntfYn==='Y' 또는 deidentStatus==='DONE')
export function isMarkingDone(v: { status?: string }): boolean; // dataSttsCd ∈ {PROCESSING, COMPLETED, FAILED}
```
- VideoListPage: `isReviewer && canMark(v)` 일 때만 "마킹" 버튼 노출. 클릭 → `setMarkingTarget({rawSn,name})` → `<MarkingModal>`.
- 기존 배정(수동)은 MarkingModal 내부 AssignModal로 흡수. 일괄 배정 바는 최소변경(유지 또는 마킹 문맥에 맞게 후속) — 이번은 행 단위 배정 버튼의 마킹 대체가 핵심.

**테스트 케이스** (cc-tdd RED):
- `MARKING_READY_비식별완료_영상에_마킹버튼이_노출된다`
- `PROCESSING_COMPLETED_FAILED_영상엔_마킹버튼이_비노출된다`
- `PENDING_또는_비식별미완_영상엔_마킹버튼이_비노출된다`
- `비REVIEWER면_마킹버튼이_노출되지_않는다`
- `마킹버튼_클릭시_MarkingModal이_열린다`
- `canMark_isMarkingDone_상태별_판정_단위테스트`

**수용 기준**:
- [ ] AC1 상태기반 노출/숨김
- [ ] AC6 REVIEWER 게이트
- [ ] AC2 클릭 → 팝업 오픈
- [ ] 기존 배정(수동) 흐름이 마킹 팝업에서 정상 동작

**의존성**: Phase 1 (MarkingModal)

## 실행 가이드

1. `/cc-tdd Phase 1: MarkingModal 컴포넌트 (자동 intervalFrames 폼 + 수동 AssignModal 통합)` → `/cc-tdd Phase 2: VideoListPage 배치→마킹 버튼 교체 + 상태기반 노출 + REVIEWER 게이트`
