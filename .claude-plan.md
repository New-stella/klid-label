# 구현 계획: LogiCraft MCP 업로드 (저작도구 프로젝트 모델링)

> 생성: 2026-05-20
> 대상 프로젝트: `19e88da4-f1f5-4767-b181-7ee5abde3248` (저작도구)
> Kickoff 저장 완료 (Tier 1 5개 + Tier 2 일부)

## Kickoff 확정 사항 (요약)

| 항목 | 값 | 영향 |
|------|-----|-----|
| communication_style | rest_primary_events_secondary | DFEAT.triggers/consumes 는 사이드 이펙트만, SEQ 로 도메인 호출 표현 |
| deployment | on_premise | INFRA 자체운영, SLO 보수적 |
| service_architecture | modular_monolith | DOMAIN 은 논리 경계, 동일 INFRA 배포 |
| scale | 100 users / 10 QPS / 10TB | NFR 보통 수준 |
| lifecycle | **greenfield** | brownfield 메타·LEGACY-* 카탈로그 사용 안 함 |
| persistence | single_rdb (MariaDB klid_system 공유) | ERD 단일 DBMS |
| auth_model | in_house (관제·포털 JWT 인계) | ROLE 3종, channel 분기 |
| compliance | 개인정보보호법 / ISMS / K-DOC | POLICY·GUARD·NFR(보안)·RISK 가산 |
| ai_features | true (절반만 적용) | MODEL_USAGE 3개 + GUARD + POLICY |
| accessibility | wcag_aa | 포털 SCREEN 의 NFR |

## 업로드 Phase 진행 현황

- [x] **Phase A-0** — RFP 21건 등록 완료 (RFP-001 ~ RFP-021, status=approved, warnings 없음)
- [x] **Phase A-1** — REQ 15건 등록 완료 (REQ-001 ~ REQ-015, derived_from_rfp 링크 25개 자동 생성) — **V1.8 추가**: req-control-notify 신규 (총 16건)로 갱신 예정. 추가 derived_from_rfp 1 (sr-sfr-08)
- [x] **Phase A-2** — ADR 11건 등록 완료 (ADR-001 ~ ADR-010 + ADR-011 V2 파이프라인 재배치, ADR-008 은 v2 정정)
- [x] **Phase A-3** — FEAT 7건 등록 완료 (FEAT-001 ~ FEAT-007, links 25개 자동 생성)
- [x] **Phase A-5** — AC 26건 등록 완료 (AC-001 ~ AC-026, verifies 26 link 자동 생성)
- [x] **Phase A-4** — TERM 20건 등록 완료 (TERM-001 ~ TERM-020)
- [x] **Phase A 전체 완료** — 100 ITEM (RFP 21 + REQ 15 + ADR 11 + FEAT 7 + AC 26 + TERM 20) + 76 auto link
- [x] **Phase B 완료** — 다이어그램 6건 (CTX/CNT/CMP/SEQ×2/STATE)
- [x] **Phase C 완료** — DOMAIN 9 + CMP-001 + DFEAT 31 = 41 ITEM. 9도메인 전체 + collaborators 10 link + specializes/AC ~80 auto link
- [ ] **Phase B** — 다이어그램 (C4 3종 + SEQ 2~3 + STATE 1) — 약 7 ITEM
- [ ] **Phase C** — 도메인 (DOMAIN 9 + DFEAT ~40) — 약 50 ITEM
- [ ] **Phase D** — 데이터·인터페이스 (ERD 논리/물리 + API ~60 + EVT ~10) — 약 70 ITEM
- [ ] **Phase E** — 파생·운영 (UC ~20 + SCREEN ~25 + ROLE 3 + NFR ~8 + RISK ~5 + INFRA ~6 + MIG 1) — 약 70 ITEM
- [ ] **Phase F** — AI 거버넌스 (MODEL 3 + GUARD ~3 + POLICY ~2) — 약 8 ITEM
- [ ] **Phase G** — IA (NAV 2 + SHELL 2) + SCREEN static_renders[] HTML — 약 4 ITEM + HTML N
- [ ] **Phase H** — 외부 연동 (EXTSYS 5 + INT ~10 + INTSPEC ~3) — 약 18 ITEM
- [ ] **Phase I** — 구현 인벤토리 (sync_codebase 자동 + code_module 메타보강 + constant ~10) — code_module ~수백 / constant ~10

**총 ~300 ITEM** (Phase 단위로 끊고 매 Phase 후 사용자 검토)

## 각 Phase 세부 계획

### Phase A-0 — RFP (Phase 0)

소스: `docs/ccarch-authoring/04-source-requirements.md` + `docs/requirements/[붙임2] 제안요청서(수정)_260303.pdf`

발주처 원본 rfp_code 1:1 보존. 본 도구가 적용받는 모든 RFP 등록 (책임 외 SFR-01/02/04/05/10/11/12/13/14 + ECR/TER/QUR/PSR/DAR/PMR/COR 전체 제외).

**SFR 9건** (p.29~41)

| rfp_code | type | 제목 | 페이지 |
|---------|------|------|-------|
| SFR-03 | SFR | 시계열 메타데이터 (외부 VLM 호출) | 29 |
| SFR-06 | SFR | 생성형 AI 학습데이터 제작 고도화 (검수 측면) | 31 |
| SFR-07 | SFR | 생성형 AI 데이터 증강 자동화 (검수 측면) | 31 |
| SFR-08 | SFR | 학습데이터 저작도구 고도화 ★핵심 | 32 |
| SFR-09 | SFR | 영상·이미지 개인정보 비식별 처리 | 32 |
| SFR-15 | SFR | 외부 포털 — 저작도구 활용기능 제공 부분 | 37-38 |
| SFR-16 | SFR | 이미지 학습데이터셋 10만장 | 39-40 |
| SFR-17 | SFR | 영상 학습데이터셋 5천건 CoT | 40-41 |
| SFR-18 | SFR | 개인정보 보호조치 (영상암호화·접근통제) | 41 |

**PER 4건** (p.42~44)

| rfp_code | type | 제목 | 페이지 |
|---------|------|------|-------|
| PER-02 | PER | 용량·응답시간 3초 이하 | 42 |
| PER-03 | PER | 기능 지연(10s+) 사전 경고 | 43 |
| PER-04 | PER | 오류 응답 3초 이내 | 43 |
| PER-05 | PER | 시스템 자원 80%·DB pool·메모리 누수 방지 | 44 |

**SIR 3건** (p.46~48)

| rfp_code | type | 제목 | 페이지 |
|---------|------|------|-------|
| SIR-02 | SIR | 사용자 편의성·도움말 | 46 |
| SIR-04 | SIR | 사용 단말·반응형 화면 (1024~1920) | 47 |
| SIR-06 | SIR | 온라인 도움말·에러 메시지 | 48 |

**SER 5건** (p.79~81)

| rfp_code | type | 제목 | 페이지 |
|---------|------|------|-------|
| SER-01 | SER | 개발 시 보안준수 (로그인5회·암호화·하드코딩금지) | 79 |
| SER-02 | SER | 정보시스템 보안 (비번 9자·관리자페이지 접근통제) | 80 |
| SER-03 | SER | 시큐어 코딩 가이드 준수 | 80 |
| SER-04 | SER | 보안 취약점 점검 (OWASP 10) | 81 |
| SER-05 | SER | 개인정보 위탁계약 | 81 |

### Phase A — 원형 (Phase 1)

소스: `docs/ccarch-authoring/05-requirements.md` (REQ 15건 이미 정리됨) + 기존 자료

1. **REQ 15건** — V1.8 책임 범위 (ccarch-authoring/05-requirements.md 그대로 매핑, derived_from_rfp 채움)
2. **FEAT ~30건** — 9개 도메인 × 평균 3~4 기능 (사용자/권한, 배치 파이프라인 3단계, 라벨링, 검수, 비식별, 버전관리, 증강 검수, 내보내기, 포털)
3. **AC ~30건** — REQ/FEAT 검증 기준 (Gherkin)
4. **ADR ~10건** — 주요 결정:
   - ADR-001 klid_system 공유 DB (저작도구 전용 DB 미생성)
   - ADR-002 V1.3 ADMIN → REVIEWER 권한 통합
   - ADR-003 ai-server 분리 (Stateless 추론)
   - ADR-004 CVAT 포팅 9 모듈 선택 (전체 fork 배제)
   - ADR-005 외부 양방향 M2M 통합 deprecated
   - ADR-006 비식별 호출 조건부 (PRVC/PSDO 만)
   - ADR-007 Gitea 자동 커밋 (라벨 버전관리)
   - ADR-008 VLM 외부 위탁 — 시계열 정보 호출 연동만 보유 (V1.7)
   - ADR-009 포털 다운로드 SFR-15 본체 이관 (V1.5)
   - ADR-010 SFR-06/11 본체 외부 시스템 이관 (V1.8)
5. **TERM ~20건** — 용어 사전 (라벨링/검수/배정/오토라벨링/SAM2/VLM/POI/만남이력 등)

### Phase B — 다이어그램 (Phase 2)

1. **C4 Context** — 저작도구 + 5개 외부(관제/포털/Gitea/비식별/ai-server)
2. **C4 Container** — backend / ai-server / frontend / DB / 외부
3. **C4 Component** — backend 도메인 패키지 9개 매핑
4. **SEQ-001** — 배치 파이프라인 V2 (Quartz 1건/분 → 외부 VLM 시계열 호출 → 비식별화(무조건) → FFmpeg(원본+비식별 2벌) → YOLO → SAM2 → 트랙 보간)
5. **SEQ-002** — 라벨 저장 → Gitea 커밋
6. **STATE** — 검수 상태 머신 (배정 → 작업중 → 제출 → 승인/반려 → 재작업)

### Phase C — 도메인 (Phase 3 일부)

9개 DOMAIN:
- DOMAIN-001 사용자/권한
- DOMAIN-002 배치 파이프라인
- DOMAIN-003 라벨링
- DOMAIN-004 검수
- DOMAIN-005 비식별화
- DOMAIN-006 버전관리 (Gitea)
- DOMAIN-007 데이터 증강 검수
- DOMAIN-008 내보내기
- DOMAIN-009 포털

각 DOMAIN 에 평균 4~5개 DFEAT. DFEAT.specializes_feature 로 Phase A 의 FEAT 연결.
DFEAT.invokes_apis / persists_in_tables 미래 ID 미리 적어둠 (워크플로 가이드 § "절대 규칙").

### Phase D — 데이터·인터페이스 (Phase 3 나머지)

1. **ERD 논리(한글)** — 31개 재사용 테이블 + LS_DATA_LBL 3컬럼 추가의 의미 모델 (한국 공공 SI 관행)
2. **ERD 물리(영문 snake_case)** — MariaDB 10.11 네이티브 타입, logical_source 로 논리 참조 (based_on 자동)
3. **API ~60건** — `docs/api-specs` / 기존 컨트롤러 어노테이션에서 추출 (REVIEWER/WORKER/PORTAL 분기)
4. **EVT ~10건** — 라벨_저장 / 검수_제출 / 검수_승인 / 비식별_완료 / 배치_프레임_완료 등 (주력은 REST 라 도메인 이벤트 적음)

### Phase E — 파생·운영 (Phase 4)

1. **UC ~20건** — 사용자 시나리오 (도메인별 2~3개)
2. **SCREEN ~25건** — `frontend/src/pages` 트리 + portal/manage 분리
   - 각 SCREEN 의 static_renders[] HTML 은 Phase G 에서 일괄 작성 (수량이 많아 분리)
3. **ROLE 3건** — REVIEWER / WORKER / PORTAL_USER (V1.3 단일화 반영)
4. **NFR ~8건** — 성능/가용성/보안/접근성(WCAG AA)/개인정보/감사로깅/배치성능/저장공간
5. **RISK ~5건** — 비식별 API 장애 / Gitea 다운 / ai-server GPU 경합 / klid_system DDL 충돌 / 대용량 영상 업로드
6. **INFRA ~6건** — backend / ai-server / frontend / MariaDB(공유) / Gitea / 비식별서버
7. **MIG 1건** — V1.0 → V1.8 마이그레이션 묶음 (LS_DATA_LBL 3컬럼)

### Phase F — AI 거버넌스 (Phase 7 절반)

1. **MODEL_USAGE 3건** — YOLO (객체탐지) / SAM2 (세그멘테이션 + 트래킹) / 외부 VLM (시계열 메타 호출 — 모델 본체는 외부)
2. **GUARD ~3건** — VLM 시계열 응답 hallucination 가드 / PII 마스킹 가드 / 모델 응답 형식 검증
3. **POLICY ~2건** — 라벨 품질 정책 / AI 결과 검수 우선순위 정책

PROMPT_TEMPLATE / EVAL / DATASET 는 추후 보완.

### Phase G — IA + SCREEN 정적 와이어프레임

1. **NAV-001** — 관리 메뉴 (REVIEWER/WORKER, `/manage/*`)
2. **NAV-002** — 포털 메뉴 (PORTAL_USER, `/portal/*`)
3. **SHELL-001** — 데스크톱 셸 (관리 화면용 - 좌측 사이드바 + 상단 헤더)
4. **SHELL-002** — 반응형 셸 (포털용 - PC/태블릿/모바일 + 모바일 하단 탭바)
5. **SCREEN 25개** 각각 `upload_static_render` 호출:
   - 메인 page 1개 + 분기 modal/drawer/toast (필요시)
   - wf-* 클래스 카탈로그 사용 (`get_item_schema('screen_spec')` workflow_notes 참고)

### Phase H — 외부 연동 (Phase 11)

EXTSYS 5개:
- EXTSYS-001 Gitea (vendor: 자체호스팅, kind: dev-tool, criticality: high — 라벨 버전관리)
- EXTSYS-002 비식별 API (vendor: TBD, kind: regulatory, criticality: critical, data_sensitivity: pii)
- EXTSYS-003 ai-server (vendor: 자체, kind: internal_legacy 또는 component, 같은 프로젝트 내 sub-process)
- EXTSYS-004 관제서버 (vendor: CUDO, kind: auth+notify-receiver — JWT 발급 + **V1.8 outbound 완료/수정 통지 수신**)
- EXTSYS-005 포털 (vendor: 자체, kind: auth, JWT 발급)

INT ~11개 (각 EXTSYS 당 1~3개), INTSPEC ~3개 (OpenAPI 가 있는 것만).

**V1.8 신규 ID 미리 적기**:
- `if-control-notify-spi` (Outbound 비동기 EVENT — 저작도구 → 관제서버 영상 단위 TASK_COMPLETED/TASK_MODIFIED)
- `comp-control-notify-client` (Backend INFRA — Resilience4j + 디바운서 + dead-letter + 재등록 큐)
- `req-control-notify` (15개 REQ 중 신규 1건)
- `uc-task-completion-notify`, `uc-task-modification-notify` (UC 2건)

`discover_external_integrations` 로 후보 추출 → 사용자 검토 → 일괄 등록.

### Phase I — 구현 인벤토리 (Phase 8)

1. `sync_codebase(dry_run=true)` 로 미리보기 → 변경 확인 → 실제 적용
   - backend `kr.co.cudo.authoring.*` Java 파일 자동 인덱싱
   - frontend `src/**/*.{ts,tsx}` 자동 인덱싱
   - ai-server `app/**/*.py` 는 sync_codebase 지원 여부 확인 후
2. 등록된 code_module 에 realizes_features 보강 (FEAT/DFEAT/SCREEN 매핑)
3. **constant ~10건** — JWT_EXPIRY_MS / 페이지 크기 / 배치 1건당분 / 비식별 타임아웃 / YOLO confidence 임계값 / 라벨 좌표 정밀도 등

## 진행 정책

- 매 Phase 후 사용자 검토 (워크플로 가이드 § "★ 작업 정책")
- ITEM 채움 품질 룰 준수 (가이드 § "★★ 최소 채움 후 등록 금지"):
  - `get_item_schema(type)` 必读 후 등록
  - recommended 필드도 채움 (description / rationale / decided_by / based_on_adrs)
  - 모르는 필드는 비우고 등록 응답 보고에 명시 ("X 필드는 정보 부족 — 추후 보완 필요")
  - `warnings[]` 응답은 사용자 노출
- 미래 ID 미리 적어둠 (DFEAT.triggers, API.implements_features 등 — unresolved_links 자동 매칭)
- 한글 직접 입력 (UTF-8). `\uXXXX` escape 금지

## 실행 가이드

1. `/cc` (또는 직접 위임) → Phase A 부터 순차 진행
2. 각 Phase 시작 시 TaskCreate 로 sub-task 등록 → TaskUpdate 로 진척 트래킹
3. 매 Phase 완료 시 사용자에게 결과 보고 + 다음 Phase 진행 여부 확인

## 결정 사항

- lifecycle=greenfield 결정 (사용자 선택) → brownfield 메타·1차 카탈로그 단계 제거
- AI Phase 7 는 절반만 (MODEL+GUARD+POLICY) — PROMPT/EVAL/DATASET 는 추후
- SCREEN.static_renders[] HTML 생성은 Phase E 와 분리 (수량 많음 → Phase G 일괄)
- 외부 연동은 `discover_external_integrations` 후보 추출 → 사용자 검토 → 등록 (시나리오 D)
- ai-server 는 EXTSYS-003 로 등록 (modular_monolith 내 sub-process 지만 외부 HTTP 호출이라 INT 추적 의미 있음)
