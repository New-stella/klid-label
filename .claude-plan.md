# 구현 계획: cudo_246 실서버 이슈 배치 (6버그)

> 생성: 2026-07-23
> 상태: 전체 완료 (Phase 1~7, 미커밋)
> 유형: ISSUE 배치 (BE+FE, 규모 XL)

## Phase 진행 현황

- [x] Phase 1: 증강 해상도변경 path 가드 수정 (E) — BE ✅ (resolveSafeSource raw||deid 두 base, 3서비스, 신규 7테스트, security PASS)
- [x] Phase 2: 영상 승인 시 메타 자동 확정·동결 → export null 해소 (F) — BE ✅ (autoApprove+백필치유 healLateFrozen, 결정적정렬, 65테스트 GREEN. 후속폴리시: heal 낙관락 래핑·부분인덱스)
- [x] Phase 3: 검수 제출 취소 기능 (B) — BE+FE ✅ (cancelSubmit PENDING→ASSIGNED, WORKER 본인, 상태머신+FE버튼, 15테스트 GREEN, api/보안 PASS)
- [x] Phase 4: 시계열 메타 신규 등록 지원 (A) — BE+FE ✅ (MetaService upsert+검토행, FE manual-timeseries키+MAX_LEN 2000, 9테스트 GREEN)
- [x] Phase 5: evidence 선택객체 추가 중복 제거 (C) — FE ✅ (commaHas/appendCommaUnique, obj_id 세트-skip + frame_id 독립 dedup, 신규 4테스트, 388 GREEN, code-review PASS)
- [x] Phase 6: 검수자 메타 확인·승인 표면 (D) — BE+FE ✅ (MetaResponse에 reviewSn+status 배치조인, TimeseriesSidePanel REVIEWER 승인/반려+배지, useMetaReview 훅, 신규 4BE+6FE 테스트, BE스위트+FE1461 GREEN, code-review PASS. LOW: 동일 metaSn 다중 검토행 시 toMap 비결정적-현재 발생경로 없음)
- [x] Phase 7: 영상 승인 시 시계열 메타(LS_DATA_META_REVIEW) 자동 승인 — BE ✅ (MetaService.autoApproveOnVideoApproval, ReviewService.approve에서 evntAnno 직후·materialize 직전 호출, REJECTED제외·멱등·no-op, 신규 6+회귀 GREEN 전체 2865, code-review PASS)

## 요구사항 요약

- 목적: cudo_246 실사용 테스트에서 발견된 6개 버그를 수정해 라벨링·검수·증강·export 정상화
- 범위: 아래 R1~R6. 제외: 새 기능(요구 외), 무관 리팩토링
- 요구 목록:
  - R1 (E): 해상도 변경 요청이 "경로가 허용된 저장 경로를 벗어납니다" 없이 정상 진행. 근본원인=`frameSourcePath`가 비식별 경로 반환하는데 `resolveSafeFile`이 `storageRawPath` base로만 검증. | 출처: 사용자 명시
  - R2 (F): 검수 완료(영상 approve) 후 export 프레임 JSON에 `event_annotation`이 실제 등록값대로 포함(null 아님). 확정방향=**영상 승인 시 해당 event_annotation 자동 APPROVED 확정+동결**. | 출처: 사용자 명시 + Q&A(영상승인=자동동결)
  - R3 (B): WORKER가 **검수 시작 전(REVIEW_PENDING)** 자기 제출을 취소 → ASSIGNED 복귀. REVIEWING/완료 후 취소 불가, 타인 취소 불가. | 출처: Q&A(검수 시작 전만)
  - R4 (A): 시계열 메타 저장 시 기존 항목이 없어도 신규 등록되고 성공 토스트. (현재 item 0건이면 무감) | 출처: 사용자 명시
  - R5 (C): evidence "선택 객체 추가" 시 이미 등록된 obj_id/frame_id는 중복 등록 안 됨(동일 객체 재추가·동일 프레임 2객체 모두). | 출처: 사용자 명시
  - R6 (D): 검수자 라벨링 화면에서 메타(event_annotation·시계열)를 **읽기 확인 + 승인/반려** 가능. | 출처: Q&A(읽기+승인/반려)
- 수용 기준:
  - R1: deid 프레임 존재 영상에서 `POST /v1/videos/{rawSn}/resolution` 정상, `../` traversal은 여전히 400
  - R2: 영상 approve 후 `LS_DATASET_VIDEO_META.EVNT_ANNO_CN` non-null + 프레임 JSON `event_annotation` 등록값 포함
  - R3: PENDING서 cancel 200→ASSIGNED, REVIEWING서 cancel 409, 타 WORKER cancel 403
  - R4: meta 0건 영상서 저장→등록 성공, 재조회 시 반영
  - R5: 같은 객체 2회/동일 프레임 2객체 추가 시 obj_id·frame_id 중복 없음
  - R6: REVIEWER 화면 메타 내용 표시 + 승인/반려 동작
- 비기능: 보안(R1 path traversal 가드 유지 CWE-22, R3 IDOR 본인/권한 검증 CWE-639), 회귀 없음(기존 테스트 GREEN)

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | 없음(기존 테이블 사용). 단 R4 meta INSERT 경로 확인 |
| 기존 API | ReviewController에 cancel 추가(신규, 하위호환) / meta update upsert화 |
| 도메인 | video(resolution), review, evntanno/dataset(export freeze), auto(meta), label(FE) |
| 설정 | 없음 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | R1 path 가드 완화가 traversal 취약점 유발 | HIGH(보안) | raw+deid 두 base 허용하되 normalize+startsWith 유지, `../` 차단 테스트 |
| 2 | R2 자동 동결이 REJECTED 메타까지 동결 | MED | REJECTED 제외(AUTO_GENERATED/PENDING만 자동 APPROVED) |
| 3 | R3 상태전이 동시성(제출↔검수시작 경합) | MED | @Version/상태 검증으로 REVIEW_PENDING만 취소, 아니면 409 |
| 4 | R2 변경이 기존 별도 승인 흐름과 충돌 | MED | 별도 approve/reject 유지, approve 멱등 |

---

## 상세 계획

### Phase 1: 증강 해상도변경 path 가드 수정 (E)

**목표**: deid 프레임이 있는 APPROVED 영상에서 해상도 변경이 경로 오류 없이 진행.

**근본원인**: `ResolutionDerivativeService.frameSourcePath()`는 비식별 경로(`deidFilePath`, `/app/storage/deidentified/...`) 우선 반환. 그러나 `VideoResolutionService.resolveSafeFile`·`ResolutionDerivativeService.resolveSafeFile`은 `storageRawPath(/app/storage/raw)` 단일 base로만 `startsWith` 검증 → deid 경로 거부.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `video/service/VideoResolutionService.java` | 수정 | resolveSafeFile이 storageRawPath + storageDeidentifiedPath **두 base 허용** |
| `video/service/ResolutionDerivativeService.java` | 수정 | 동일 — measureDimensions/resolveSafeFile 두 base 허용 |
| (스캔) `video/service/Resolution*.java`, `webhook/service/Augment*.java` | 점검 | frameSourcePath(deid)+rawPath base 동일 패턴 있으면 함께 수정 |

**핵심 로직**: `resolveSafeFile(filePath)` → normalize 후 `resolved.startsWith(rawBase) || resolved.startsWith(deidBase)` 통과, 아니면 400. 두 base 모두 `.toAbsolutePath().normalize()`. storageDeidentifiedPath는 기존 주입 프로퍼티(`STORAGE_DEIDENTIFIED_PATH`).

**테스트 케이스**:
- `비식별_프레임_경로는_deid_base로_통과`
- `원본_프레임_경로는_raw_base로_통과`
- `상위경로_traversal(..)_은_여전히_거부`
- `두_base_모두_밖이면_거부`

**수용 기준**:
- [ ] deid 프레임 영상 해상도변경 정상 진행(경로오류 없음)
- [ ] `../` traversal 입력은 400 유지

**의존성**: 없음

### Phase 2: 영상 승인 시 메타 자동 확정·동결 → export null 해소 (F)

**목표**: 영상 검수 승인 시 event_annotation을 자동 확정·동결해 export 프레임 JSON에 항상 포함.

**근본원인**: WORKER 저장 시 `LS_EVNT_ANNO_REVIEW`가 AUTO_GENERATED로 생성·고착. materialize는 APPROVED만 동결 → 영상 승인해도 메타 미동결 → export null(rawSn 24 실측).

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `review/service/ReviewService.java` | 수정 | approve() 시 해당 rawSn event_annotation review 자동 APPROVED 전이(AUTO_GENERATED/PENDING, REJECTED 제외) 후 동결 트리거 |
| `evntanno/service/EvntAnnoReviewService.java` | 수정 | autoApproveOnVideoApproval(rawSn) 노출 — 기존 approve/동결 로직 재사용, 메타 없으면 no-op |
| `dataset/service/DatasetVideoMetaSnapshotService.java` | 점검 | 자동 승인 후 materialize가 EVNT_ANNO_CN 동결 순서 보장 |

**핵심 로직**: `ReviewService.approve` 성공 경로 → event_annotation review 존재 && status ∈ {AUTO_GENERATED, PENDING} 이면 APPROVED 전이 → materialize가 동결. REJECTED 제외(명시 반려 존중). 멱등(이미 APPROVED skip).

**테스트 케이스**:
- `영상승인시_AUTO_GENERATED_메타_자동_APPROVED_동결`
- `영상승인시_PENDING_메타_자동_APPROVED_동결`
- `영상승인시_REJECTED_메타는_자동승인_제외`
- `승인후_export_EVNT_ANNO_CN_non_null`
- `메타없는_영상_승인은_정상(no-op)`

**수용 기준**:
- [ ] 영상 approve 후 `EVNT_ANNO_CN` non-null + 프레임 JSON event_annotation 포함
- [ ] 반려된 메타는 자동 동결 안 됨

**의존성**: 없음

### Phase 3: 검수 제출 취소 기능 (B)

**목표**: WORKER가 검수 시작 전(REVIEW_PENDING) 자기 제출을 취소해 ASSIGNED 복귀.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `review/controller/ReviewController.java` | 수정 | `POST /reviews/{videoId}/cancel-submit` (WORKER) 추가 |
| `review/service/ReviewService.java` | 수정 | cancelSubmit(videoId, actor): REVIEW_PENDING만 → ASSIGNED, 본인 배정 검증 |
| `frontend/src/features/review/**` 또는 라벨링/검수 화면 | 수정 | 제출 취소 버튼 + 훅(REVIEW_PENDING만 노출) |

**핵심 로직**: REVIEW_PENDING + actor=본인 배정 WORKER → dataSttsCd ASSIGNED, 제출 메타 롤백. REVIEWING/APPROVED → 409. 타인 → 403.

**테스트 케이스**:
- `REVIEW_PENDING서_본인_취소_성공_ASSIGNED복귀`
- `REVIEWING서_취소시_409`
- `타_WORKER_취소시_403`
- `미인증_401`

**수용 기준**:
- [ ] PENDING서 취소 200 + ASSIGNED
- [ ] REVIEWING/완료서 취소 거부
- [ ] FE 취소 버튼 PENDING 상태만 노출

**의존성**: 없음

### Phase 4: 시계열 메타 신규 등록 지원 (A)

**목표**: 시계열 메타에 기존 항목이 없어도 저장 시 신규 등록.

**근본원인**: `TimeseriesSidePanel.handleSave`가 `sourceItems.length===0`이면 조용히 return. BE meta update가 기존 metaKey 수정만 지원.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `auto` 도메인 meta service/controller | 수정 | 시계열 meta upsert(없으면 INSERT with 표준 metaKey) 지원 |
| `frontend/.../TimeseriesSidePanel.tsx` | 수정 | items 0건일 때도 신규 등록 payload(표준 metaKey) |
| `frontend/.../useUpdateMeta.ts` | 점검 | 신규 등록 호환 |

**핵심 로직**: BE는 (rawSn/srcSn, metaKey) upsert. 시계열 표준 metaKey 상수 확인 후 신규 등록 시 사용. FE handleSave는 items 0건이면 표준 metaKey로 1건 등록.

**테스트 케이스**:
- `meta_0건_영상서_시계열_저장시_신규등록`
- `기존_meta_있으면_수정(회귀없음)`
- `저장후_재조회시_반영`

**수용 기준**:
- [ ] meta 0건 영상서 저장 성공 + 등록 + 성공 토스트

**의존성**: 없음

### Phase 5: evidence 선택객체 추가 중복 제거 (C)

**목표**: "선택 객체 추가" 시 이미 있는 obj_id/frame_id 중복 방지.

**근본원인**: PR#46 `appendComma`/`appendNewline`가 무중복 append. 같은 객체 재추가 시 obj_id 중복, 동일 프레임 2객체 추가 시 frame_id(currentSrcSn) 중복.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/.../EventAnnotationPanel.tsx` | 수정 | appendComma/appendNewline 또는 appendSelectedObject에서 기존 원소 중복이면 skip |

**핵심 로직**: append 전 콤마/개행 분해 → trim 비교로 이미 존재하면 미추가. obj_id·frame_id·obj_bbox 원소 단위 dedup. obj_label은 라벨명 특성상 중복 허용 검토(obj_id 기준 dedup 핵심).

**테스트 케이스**:
- `같은객체_2회추가시_objId_중복없음`
- `동일프레임_2객체_추가시_frameId_1개`
- `다른객체_추가는_정상_누적`

**수용 기준**:
- [ ] obj_id·frame_id 중복 없음, 서로 다른 객체는 정상 누적

**의존성**: 없음(PR#46 회귀 수정)

### Phase 6: 검수자 메타 확인·승인 표면 (D)

**목표**: 검수자 라벨링 화면에서 메타(event_annotation·시계열) 내용을 읽고 승인/반려.

**근본원인**: REVIEWER 화면에 메타 리뷰 표면 부족(객체 확인만 체감). EventAnnotationPanel은 REVIEWER 승인/반려 UI 보유하나 가시성 부족.

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `frontend/.../EventAnnotationPanel.tsx` | 수정/점검 | REVIEWER 읽기 확인 + 승인/반려 명확화 |
| `frontend/src/pages/label/LabelingPage.tsx` | 점검 | REVIEWER 메타 탭 노출 확인(showMeta=!portalMode) |

**핵심 로직**: REVIEWER가 메타 탭에서 event_annotation·시계열 값을 명확히 보고 승인/반려. Phase 2로 영상승인=자동동결이므로 이 승인/반려는 보조(명시 반려 시 자동승인 제외 연동).

**테스트 케이스**:
- `REVIEWER_메타탭_event_annotation_값_표시`
- `REVIEWER_승인_반려_동작`
- `WORKER는_승인반려_미노출`

**수용 기준**:
- [ ] REVIEWER가 메타 내용 확인 + 승인/반려 동작

**의존성**: Phase 2(자동동결 연동)

---

## 실행 가이드

1. Phase 1: BE path 가드 (developer-hotfix)
2. Phase 2: BE 메타 자동 동결 (developer-tdd)
3. Phase 3: BE+FE 검수 취소 (developer-tdd)
4. Phase 4: BE+FE 시계열 등록 (developer-tdd)
5. Phase 5: FE evidence dedup (developer-hotfix)
6. Phase 6: FE 검수자 메타 (developer-tdd)
