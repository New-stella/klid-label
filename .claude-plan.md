# 구현 계획: 시계열 메타 UI 전체 개편

> 생성: 2026-05-27 19:30
> 상태: 진행 중

## Phase 진행 현황

- [ ] Phase 1: FE 메타 타입/컴포넌트 개편 — enum 폼 삭제 → 자연어 textarea
- [ ] Phase 2: LabelingPage 사이드 패널에 시계열 메타 패널 추가

## 요구사항 요약

- 목적: VLM 시계열 결과가 자연어 텍스트 블록이므로, enum 드롭다운을 textarea로 교체하고 캔버스에서도 확인/수정 가능하게 함
- 범위:
  - 포함: FE 타입 개편, EnvMetaForm/EventMetaForm 삭제 → TimeseriesTextPanel 생성, MetaReviewPage 개편, LabelingPage 사이드 패널 추가
  - 제외: BE API 변경 없음 (기존 K/V PUT API 그대로 사용, metaKey 기반), VLM 외부 연동
- 주요 액터: WORKER(수정), REVIEWER(수정+검토)

## 영향 범위

| 영역 | 변경 내용 |
|------|---------|
| DB 스키마 | 없음 (기존 LS_DATA_META K/V 구조 그대로) |
| 기존 API | 없음 (GET/PUT /v1/frames/{srcSn}/meta 그대로) |
| 도메인 모듈 | FE auto feature, label feature |
| 설정/환경 | 없음 |

## 위험 요소

| # | 위험 | 영향 | 대응 |
|---|------|------|------|
| 1 | VLM 응답 포맷 미확정 | LOW | 단일 텍스트 블록으로 가정. 향후 구조 변경 시 textarea만 수정 |
| 2 | 라벨링 사이드 패널 공간 부족 | MED | 접이식(collapsible) 패널로 구현, 기존 라벨 패널과 독립 |

---

## 상세 계획

### Phase 1: FE 메타 타입/컴포넌트 개편

**목표**: MetaReviewPage가 enum 드롭다운 대신 자연어 textarea로 VLM 시계열 결과를 표시/수정

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `features/auto/types.ts` | 수정 | EnvMeta/EventMeta → 단일 VlmTimeseriesText 문자열. FrameMeta에서 envMeta/eventMeta → vlmText |
| `features/auto/components/EnvMetaForm.tsx` | 삭제 | enum 드롭다운 폼 제거 |
| `features/auto/components/EventMetaForm.tsx` | 삭제 | enum 드롭다운 폼 제거 |
| `features/auto/components/TimeseriesTextPanel.tsx` | 생성 | 자연어 textarea (vlmText 표시/수정) |
| `features/auto/hooks/useMeta.ts` | 수정 | 응답 매핑 변경 (K/V items → vlmText 추출) |
| `features/auto/hooks/useUpdateMeta.ts` | 수정 | 요청 매핑 변경 (vlmText → metaKey/metaVal PUT) |
| `pages/MetaReviewPage.tsx` | 수정 | EnvMetaForm/EventMetaForm → TimeseriesTextPanel 교체 |

**핵심 로직**:
```typescript
// TimeseriesTextPanel — 자연어 시계열 텍스트 표시/수정
interface TimeseriesTextPanelProps {
  vlmText: string;
  onChange: (text: string) => void;
  disabled?: boolean;
}
// textarea로 VLM 자연어 텍스트 표시, 수정 시 onChange 콜백
```

```typescript
// useMeta 응답 매핑 — BE K/V에서 VLM 텍스트 추출
// items[].metaKey === 'VLM_TIMESERIES' → vlmText
// BE가 metaVal에 자연어 전체 저장

// useUpdateMeta 요청 매핑 — vlmText → PUT items
// { items: [{ metaKey: 'VLM_TIMESERIES', metaVal: vlmText }] }
```

**테스트 케이스**:
- `TimeseriesTextPanel_vlmText_렌더링`
- `textarea_수정시_onChange_호출`
- `MetaReviewPage_자연어_텍스트_표시`
- `MetaReviewPage_수정_후_저장_PUT_호출`

**수용 기준**:
- [ ] MetaReviewPage에서 VLM 자연어 텍스트가 textarea로 표시됨
- [ ] WORKER/REVIEWER 모두 텍스트 수정 + 저장 가능
- [ ] 기존 enum 드롭다운 폼(EnvMetaForm/EventMetaForm) 완전 제거

**의존성**: 없음

### Phase 2: LabelingPage 사이드 패널에 시계열 메타 추가

**목표**: 라벨링 캔버스 화면 오른쪽 사이드 패널에 접이식 시계열 메타 패널이 추가되어 프레임 전환 시 해당 메타 자동 로드 + 인라인 수정 가능

**생성·수정 파일**:
| 파일 | 동작 | 설명 |
|------|------|------|
| `features/label/components/TimeseriesSidePanel.tsx` | 생성 | 접이식 패널 — TimeseriesTextPanel 재사용 + useMeta/useUpdateMeta 훅 |
| `pages/LabelingPage.tsx` | 수정 | RightPanel 영역에 TimeseriesSidePanel 추가 (ObjectClassTree 아래) |

**핵심 UI 구조**:
```
RightPanel (w-72):
├── ObjectClassTree (기존 — 라벨 목록)
├── ObjectAttributePanel (기존 — 선택 라벨 속성)
└── TimeseriesSidePanel (신규 — 접이식)
    ├── 토글 헤더: "시계열 메타 ▾"
    ├── textarea (vlmText)
    └── [저장] 버튼
```

**프레임 전환 연동**:
```typescript
// LabelingPage에서 현재 srcSn이 변경되면 TimeseriesSidePanel의 useMeta(srcSn)이 자동 재조회
// React Query 캐시로 프레임 간 전환 시 즉시 표시
```

**테스트 케이스**:
- `LabelingPage_사이드패널에_시계열_메타_렌더링`
- `시계열_패널_접기_펼치기_토글`
- `프레임_전환시_메타_자동_갱신`
- `메타_수정_후_저장_성공_토스트`

**수용 기준**:
- [ ] 라벨링 화면 오른쪽에 접이식 시계열 메타 패널 표시
- [ ] 프레임 전환 시 해당 프레임의 VLM 텍스트 자동 로드
- [ ] textarea 수정 → 저장 → PUT API 호출 → 성공 토스트

**의존성**: Phase 1 (TimeseriesTextPanel 컴포넌트 + 타입 정의)
