# 구현 계획: ccarch-authoring 정합성 정렬 ✅ 완료

> 생성: 2026-05-19 · 완료: 2026-05-19
> ccarch 워크스페이스 업로드(MCP)는 제외 (사용자 지시).

## Phase 진행 현황 (전체 완료)

- [x] **Phase 1** (`c6c7fd1`) — VLM 외부 위탁: VlmClient(SSRF/HTTPS/응답검증), VlmTimeseriesStep, ai-server vlm.py 회귀가드, VlmMetaStep NO-OP. 754/754 PASS, DEV_FIX 1회
- [x] **Phase 2** (`11a76d3`) — webhook 3종 + HMAC: HmacWebhookFilter + 영속 IdempotencyLedger(V39) + ExternalUrlValidator(CGNAT/IPv4-mapped/IDN) + V40 UNIQUE + DTO @Size + prd 환경변수 강제. 794/794 PASS, DEV_FIX 2회
- [x] **Phase 3** (`809b9ae`) — Deidentify 비동기 + Gitea fallback 큐: idempotencyKey + recordIssued(ledger 활용) + V41 LS_GITEA_FALLBACK_QUEUE + GiteaFallbackRetryJob(CAS race 차단) + sanitizeError + 결정적 idempotencyKey. 841/841 PASS, DEV_FIX 1회
- [x] **Phase 4** (`be9016d`) — 표준 컬럼 + Augment race + VlmMetaStep 폐기: V42 LS_DATA_META/LS_DATA_AUG 비동기 표준 컬럼 + V43 자연키 backfill + AugmentResultService race 흡수 + VlmMetaStep cleanup. 860/860 PASS

## 14건 GAP 처리 결과

| Gap | 처리 | Phase |
|-----|------|-------|
| A-1 DeidentifyClient 비동기 정합 | DONE | Phase 3 |
| A-2 VlmClient 신설 | DONE | Phase 1 |
| A-3 AiServerClient.extractVideoMeta 제거 | DONE | Phase 1 |
| A-4 GiteaClient fallback 큐 | DONE | Phase 3 |
| B-1 배치 단계 명확화 (VlmTimeseriesStep) | DONE | Phase 1 |
| C-2 VLM REQ-004 외부 위탁 흐름 | DONE | Phase 1 |
| C-3 범위 외 코드 정리 (ai-server vlm.py 영상메타) | DONE | Phase 1 (회귀 가드) |
| D-1 UC-04 VLM 위탁 USECASE 구현 | DONE | Phase 1 + Phase 3 |
| D-2 비동기 결과 수신 엔드포인트 3종 | DONE | Phase 2 |
| E-1 비동기 표준 컬럼 (idempotencyKey/externalJobId/retryCount/deadLetterAt) | DONE | Phase 2 (LS_DEIDENT_PROC_LOG) + Phase 4 (LS_DATA_META + LS_DATA_AUG) |
| F-1 16 COMPONENT 매핑 | DONE | Phase 1~4 누적 (VlmClient + webhook + fallback) |
| G-1 패키지 구조 ccarch COMPONENT 정렬 | DONE | Phase 2 webhook 신설 + Phase 3 version/fallback |
| H-1 ccarch MCP 업로드 | **제외** | (사용자 지시) |
| C-1 7개 FEATURE 매핑 | DONE | 누적 (라벨링/검수/비식별/버전/증강/생성영상/VLM검토 모두 매핑) |

## 누적 산출물

- 신규 마이그레이션 5종: V39 (LS_WEBHOOK_IDEMPOTENCY) / V40 (LS_DEIDENT_PROC_LOG.EXTERNAL_JOB_ID UNIQUE) / V41 (LS_GITEA_FALLBACK_QUEUE) / V42 (표준 컬럼 4종) / V43 (backfill)
- 신규 컴포넌트: VlmClient + VlmTimeseriesStep + webhook 3 컨트롤러 + 3 Service + HmacWebhookFilter + WebhookIdempotencyLedger(인터페이스+영속/InMemory) + ExternalUrlValidator + GiteaFallbackQueueService + GiteaFallbackRetryJob + LsGiteaFallbackQueue
- 삭제: AiServerClient.extractVideoMeta + VlmMetaRequest/Response DTO + VlmMetaStep + Test
- 누적 테스트: 신규 ~120건, 총 860/860 PASS (skipped 1)
- 4회 commit, 90 files changed (+6419 / -1005)

## 운영 활성화 전 체크리스트 (코드 변경 범위 외 — 후속 인프라/SOP)

### 차단 항목
- LB X-Forwarded-For trust 정책 (Nginx/ALB header + `server.forward-headers-strategy`)
- 분산 rate limit (Redis 기반 sliding window — 멀티 인스턴스 운영 시)
- webhook HMAC 시크릿 회전 SOP 문서화

### 모니터링
- Micrometer 카운터: `gitea.fallback.queue.enqueue/full/dead_letter`, `webhook.idempotency.duplicate`
- DEAD_LETTER 항목 30일 retention 정책 + 운영 알림 (Slack/이메일)
- BE 인스턴스 NTP 동기화 확인 (큐 nextRetryAt 시계 드리프트)

### 환경 변수 필수
- `VLM_CLIENT_ENABLED=true` + `VLM_SERVICE_URL` + `VLM_SERVICE_TOKEN`
- `GITEA_FALLBACK_ENABLED=true` (stg/prd)
- `WEBHOOK_HMAC_SECRET_DEIDENTIFY` / `WEBHOOK_HMAC_SECRET_VLM` / `WEBHOOK_HMAC_SECRET_AUGMENT` — 32바이트 이상 (부팅 시 검증)

## 결정 사항

- VLM 책임: 외부 위탁 (CLAUDE.md V1.8)
- 비동기 결과 수신: 콜백 웹훅 + HMAC + idempotency allowlist (영속)
- ccarch MCP 업로드: 제외 (사용자 지시)
- Migration 번호: V39/V40 (Phase 2) → V41 (Phase 3) → V42/V43 (Phase 4)
