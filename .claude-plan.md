# 구현 계획: VLM 연동부 v2.0.1 정합 (IntelliVIX Video VLM API describe)

> 생성: 2026-07-07 | 상태: 진행 중 | 브랜치: feat/onprem-install-package
> 직전 완료 플랜(KPST shared-mount)은 `.claude-plan.kpst-sharedmount-done-20260630.md` 로 백업됨.
> 🎯 목표: 계약 확정 전 추정 스텁이던 VLM 위탁/콜백 코드를 외부 벤더 확정 계약(IntelliVIX Video VLM API v2.0.1, `docs/video_vlm_api_ v2.0.1.docx`)의 **describe** 규격에 정합.
> 근거 규격: logicraft **INTSPEC-003**(위탁 요청) · **INTSPEC-002**(콜백 수신) · D4 II-003/II-004(v1.7) · `docs/v2-wiki/09-vlm-timeseries.md`.

## 요구사항 정의 (확정)

- **R1**: 콜백 수신 API(`POST /v1/vlm/result`)가 벤더 describe 콜백 바디 `{request_id, status(completed|failed), results:[{start_sec,end_sec,description}], error:{code,message}}` 를 수용한다. | 출처: INTSPEC-002
- **R2**: `status=completed` 시 `results` 각 구간을 `LS_DATA_META` 에 적재(`META_KEY="{start_sec}-{end_sec}"`, `META_VL=description`) + 검수 대기열(`LS_DATA_META_REVIEW`, PENDING) 진입. | 출처: INTSPEC-002 §4
- **R3**: `status=failed` 시 `error{code,message}` 를 기록(배치 처리 로그/dead-letter)하고 적재하지 않는다. | 출처: INTSPEC-002 §4
- **R4**: `request_id` 로 대상 영상(RAW_SN)을 매칭한다 — 기존 idempotency ledger 재사용(위탁 시 발급한 request_id → rawSn/jobId 매핑). | 출처: 설계 결정(2026-07-07)
- **R5**: 콜백 HMAC 인증 제거(벤더 규격 무인증, 사용자 승인). **단 ledger `isIssued(request_id)` 게이트는 유지** — 우리가 발급하지 않은 request_id 콜백은 거부(무단 주입 방지 보상통제). ⚠ 실운영 시 IP allowlist 재검토 필요. | 출처: 사용자 승인(2026-07-07)
- **R6**: 위탁 요청을 `POST /v1/videovlm/describe` 로 보내며 바디는 `{request_id, media:{type,source_type,path,frame_policy:{mode,framerate,selected_frames}}, callback_url}`. | 출처: INTSPEC-003
- **R7**: `callback_url` 을 실제로 전송한다(기존 `VlmTimeseriesStep` 의 `callbackUrl=null` 버그 수정). | 출처: 블로커 #1
- **R8**: 동기 응답 `{request_id, status:"accepted"}` 를 수용한다(기존 `externalJobId` 필수 + 대문자 status 화이트리스트 검증 제거/교체). | 출처: 블로커 #2·INTSPEC-003
- **R9**: describe 규격상 `event_type`/마킹 시점을 입력받지 않으므로 기존 `eventName`/`marks` 는 전송하지 않는다. | 출처: INTSPEC-003 §5
- **멱등**: 동일 `request_id` 재수신 시 스킵(기존 ledger `isProcessed`).
- **제외(out of scope)**: verify(이벤트 검증, `results:{accuracy,description}`) 콜백은 후속 / 실제 벤더 연동 활성화(`VLM_CLIENT_ENABLED=false` 유지 — 코드 정합만) / FE / 증강·KPST 등 타 연동.
- **수용 기준**:
  1. 콜백 DTO가 새 필드 구조(request_id/status/results/error)로 `@Valid` 통과하고 구 필드(vlmMetaItems/externalJobId/resultFilePath/idempotencyKey) 제거됨.
  2. `HmacWebhookFilter` 가 `/v1/vlm/result` 에 HMAC 미적용, 그러나 `VlmResultService` 가 미발급 request_id 콜백을 UNAUTHORIZED 로 거부.
  3. completed 콜백 → `LS_DATA_META` upsert(META_KEY 시간구간) + 검수큐 진입 테스트 GREEN.
  4. 위탁 요청 바디에 `callback_url` 실제 포함, endpoint `/v1/videovlm/describe`, 동기응답 `status="accepted"` 수용 테스트 GREEN.
  5. 전체 `./gradlew build` GREEN(회귀 없음).

## Phase 진행 현황

- [x] **Phase 1**: 콜백 수신부 정합 (INT-003 / INTSPEC-002) — describe 콜백 바디 + HMAC 제거 + request_id 매핑
  > 구현: VlmResultRequest(request_id/status/results[start_sec,end_sec,description]/error 교체·구필드제거), VlmResultService(isIssued게이트·resolveRawSn·completed 구간별 META upsert+검수큐·failed 미적재·원자적 tx), HmacWebhookFilter(vlm HMAC 제거+4MB bounded read), WebhookIdempotencyLedger(+rawSn·resolveRawSn·markProcessedInTx REQUIRED·비관적락), LsMarking(VLM_FAILED), V78(RAW_SN)·V79(REVIEW UNIQUE+상태우선 dedup), application*.yml(vlm HMAC 시크릿 제거) | QA: build PASS(235슈트)·req CONFORM(R1~R5)·DB/adversarial/security 초기 결함(tx원자성 CRITICAL·DoS·검수큐중복·Segment·failed마킹·V79상태무시) DEV_FIX 2회로 전부 CLOSED·adversarial ROBUST | 잔여: [Phase2] recordIssued(CHANNEL_VLM,rawSn) 위탁측 배선+E2E / [사용자결정] IP allowlist 미구현(prd 잔여리스크) / [후속] VLM_FAILED 복구잡·Augment REQUIRES_NEW 원자성
- [x] **Phase 2**: 위탁 요청부 정합 (INT-002 / INTSPEC-003) — describe 요청 바디 + callback_url 전송 + 동기응답 + **recordIssued 배선 + E2E**
  > 구현: VlmTimeseriesRequest/Response(describe 바디·{request_id,status:accepted}), VlmClient(/v1/videovlm/describe·request_id echo 검증), VlmTimeseriesStep(request_id 발급·비식별경로 fail-closed·callback_url 실주입·describe 직전 recordIssued(CHANNEL_VLM,rawSn)·마킹전이 명시영속 save), LsMarking.markVlmRequested(PENDING-only 상태가드), VlmResultService(status 화이트리스트), WebhookCallbackDefaults(base URL 단일화) | QA: build PASS(1721)·code PASS(9/10)·req CONFORM(R6~R9+recordIssued)·DB clean·adversarial ROBUST. 초기 WEAK(마킹전이 유실·테스트사각) DEV_FIX1(save 명시영속+관통 통합테스트로 실증), 그 회귀(retry durable 역행) DEV_FIX2(상태가드)로 CLOSED | 잔여(후속): VLM_COMPLETED 재위탁 스킵·VLM_FAILED 복구잡·orphan ISSUED 정리·augment base URL 리터럴 / [사용자결정] IP allowlist 미구현(prd 잔여리스크)

---

## Phase 1 — 콜백 수신부 정합 (describe 콜백) ✅ 완료

**유형**: 외부 연동 + 보안(인증 변경) → code-reviewer + build-validator + security-reviewer + requirements-verifier + adversarial-critic + database-reviewer

**대상 파일**
- `backend/src/main/java/kr/co/cudo/authoring/webhook/dto/VlmResultRequest.java` — 필드 교체: `{request_id, status(completed|failed), results:[{start_sec,end_sec,description}], error:{code,message}}`. 구 필드 제거. `@Valid` 검증(status 패턴, 상호 조건 @AssertTrue, Segment @Min/@Max/@AssertTrue isRangeOrdered).
- `backend/src/main/java/kr/co/cudo/authoring/webhook/service/VlmResultService.java` — isIssued 게이트 + resolveRawSn 역조회 + completed 구간별 META upsert(배치조회)+검수큐 + failed 미적재+VLM_FAILED 전이 + 단일 tx 원자성(markProcessedInTx REQUIRED, 원장 마킹 마지막) + 중복 metaKey 400.
- `backend/src/main/java/kr/co/cudo/authoring/common/security/HmacWebhookFilter.java` — vlm HMAC 제거 + 본문 4MB size-cap(contentLength<0 411 조기거부 + readCapped bounded read).
- `backend/src/main/java/kr/co/cudo/authoring/webhook/idempotency/{WebhookIdempotencyLedger,PersistentWebhookIdempotencyLedger,InMemory...,LsWebhookIdempotency,Repository}.java` — rawSn 슬롯 + resolveRawSn + markProcessedInTx(REQUIRED) + findByIdmpKeyForUpdate(비관적락).
- `backend/src/main/java/kr/co/cudo/authoring/marking/entity/LsMarking.java` — STATUS_VLM_FAILED + markVlmFailed().
- `backend/src/main/resources/db/migration/V78__add_ls_webhook_idempotency_raw_sn.sql`, `V79__add_ls_data_meta_review_unique.sql`(상태우선 dedup).
- `backend/src/main/resources/application.yml`·`application-prd.yml` — vlm HMAC 시크릿/fail-closed 제거.
- 테스트: VlmResultServiceTest, VlmResultServiceMarkingTest, VlmResultRequestMetaItemTest, HmacWebhookFilterTest, V79MetaReviewDedupMigrationTest 등.

---

## Phase 2 — 위탁 요청부 정합 (describe 요청)

**유형**: 외부 연동 → code-reviewer + build-validator + requirements-verifier (+ adversarial/security: recordIssued 원자성·E2E)

**대상 파일**
- `backend/src/main/java/kr/co/cudo/authoring/common/client/dto/VlmTimeseriesRequest.java` — describe 바디로 교체: `{request_id, media:{type,source_type,path,frame_policy:{mode,framerate,selected_frames}}, callback_url}`. 구 필드(videoUri/eventName/marks/idempotencyKey) 정리.
- `backend/src/main/java/kr/co/cudo/authoring/common/client/dto/VlmTimeseriesResponse.java` — `{request_id, status}` (status="accepted"). 구 `externalJobId` 필수·대문자 화이트리스트 제거.
- `backend/src/main/java/kr/co/cudo/authoring/common/client/VlmClient.java` — `SUBMIT_PATH` → `/v1/videovlm/describe`. 상관관계를 `X-Idempotency-Key` 헤더 → 바디 `request_id` 로. 응답 검증을 status="accepted"/request_id echo 기준으로.
- `backend/src/main/java/kr/co/cudo/authoring/batch/step/VlmTimeseriesStep.java` — describe 요청 빌드: `media.path=raw.getRawFilePathNm()`(비식별 경로), `frame_policy`(기본 frame_interval), **`callback_url` 실제 주입(null 버그 수정)**, `request_id` 발급 후 **`ledger.recordIssued(request_id, CHANNEL_VLM, externalJobId, rawSn)` 등록**(Phase 1 콜백부의 isIssued/resolveRawSn 게이트를 채움 — 위탁 성공과 원자적). `eventName`/`marks` 미전송.
- `backend/src/main/resources/application.yml` — `vlm.client.url` 유지, 필요 시 `vlm.client.callback-url`(우리 콜백 base) 추가.
- **테스트**: `VlmClientTest`, `VlmTimeseriesStepTest` + **batch→callback E2E 통합 테스트**(위탁 시 recordIssued 등록 → 콜백 수신부가 실제로 rawSn 역조회·적재까지 — adversarial 결함1/2 폐쇄 검증).

**HIGH 시나리오(방어 필수)**
- ⚠ [블로커/결함1] recordIssued 미배선 → 콜백 100% 401 → 방어: 위탁 성공 시 recordIssued(CHANNEL_VLM,rawSn) 등록 + E2E로 실증.
- ⚠ [블로커] callback_url null 전송 → 방어: Step에서 실제 callback URL 주입 + 요청 직전 not-null 가드.
- ⚠ [상관관계] request_id↔rawSn 등록이 위탁과 원자적이지 않으면 매핑 유실 → 방어: 원자적 등록(tx/AFTER_COMMIT 일관).
- ⚠ [외부장애] describe 45s 초과 → 방어: 기존 Resilience4j timeout/retry/circuit 유지.

**테스트 케이스(RED)**
- `describe_위탁_요청에_callback_url_media_frame_policy_포함()`
- `위탁_요청_endpoint가_videovlm_describe()`
- `동기응답_status_accepted_수용하고_request_id_echo_확인()`
- `위탁_성공_시_request_id가_CHANNEL_VLM_rawSn과_함께_ledger에_등록됨()`
- `eventName_marks는_요청에_포함되지_않음()`
- `E2E_위탁_등록_후_describe_콜백이_rawSn_역조회로_적재된다()`

---

## 실행 가이드

1. `/cc-tdd Phase 1: VLM 콜백 수신부 describe 정합 + HMAC 제거` → `/cc-tdd Phase 2: VLM 위탁 요청부 describe 정합 + recordIssued 배선`
2. 전체 완료 후 `/cc-verify` (통합 시나리오) → `/cc-commit`

## 주의 / 미해결
- ⚠ **보안 트레이드오프**: 콜백 HMAC 제거 = 무인증 인바운드. request_id issued 게이트로 최소 방어하되, 실운영 전 IP allowlist 재검토(코드에 TODO 주석 명시). security-reviewer가 CWE-306(Missing Auth)로 지적 시 = **사용자 승인된 결정**이므로 waive(request_id 게이트 존재 확인됨).
- `VLM_CLIENT_ENABLED=false` 유지 — 코드 정합만, 실연동 활성화는 별도.
- request_id↔rawSn 매핑·META_KEY 포맷은 INTSPEC-002/003의 문서화된 설계 결정.
- **후속(범위 밖)**: VLM_FAILED 자동 복구 잡 미구현(수동/후속 재처리) · Augment `handle()`도 동일 markProcessed REQUIRES_NEW 원자성 잠재이슈(후속 DEV_FIX 권장).
- `deploy/onprem/` 하위 복제본은 패키징 자동 재생성 대상(수동 동기화 불요).
